---
phase: 05-bridge-registry-and-the-no-bridge-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/concierge/src/bridge.ts
autonomous: true
requirements: [BRG-01, BRG-03, BRG-04, BRG-05]
user_setup: []

must_haves:
  truths:
    - "A page component can construct a registry and register a bridge, receiving an unsubscriber."
    - "A stale unsubscriber fired after a replacement does not clear the newer registration, including when the replacement object is === the original."
    - "A snapshot value captured from a proxy-backed store is a detached copy; the store itself is not frozen as a side effect."
    - "A handler with no bridge can build an honest off-page sentence bounded at 180 characters."
  artifacts:
    - path: "packages/concierge/src/bridge.ts"
      provides: "createBridge, captureSnapshot, offPageResult (exported); cloneDetached and makeDefaultNormalizer (module-private)"
      contains: "export function createBridge"
      min_lines: 320
  key_links:
    - from: "packages/concierge/src/bridge.ts"
      to: "packages/concierge/src/contract.ts"
      via: "assertSingleInstance() as the first statement of createBridge's body"
      pattern: "assertSingleInstance\\(\\);"
    - from: "packages/concierge/src/bridge.ts"
      to: "packages/concierge/src/host.ts"
      via: "warnHost for the three diagnostic latches"
      pattern: "warnHost\\("
    - from: "packages/concierge/src/bridge.ts"
      to: "packages/concierge/src/types.ts"
      via: "MESSAGE_MAX_CHARS value import for the off-page bound"
      pattern: "MESSAGE_MAX_CHARS"
---

<objective>
Create `packages/concierge/src/bridge.ts` — the phase's one new runtime module. It holds four units:
the monotonic-token registry closure (`createBridge`), the off-page result helper (`offPageResult`),
the default snapshot normalizer (a structural clone, module-private), and the snapshot capture loop
(`captureSnapshot`).

Purpose: this is the mechanism behind all five phase success criteria. Nothing else in the phase can
be written until these identifiers and signatures exist.

Output: one new source file. **It is deliberately NOT barrelled in this plan.** `tsdown.config.ts`
declares `entry: ["src/index.ts"]`, so an unreferenced module is not bundled: the export surface
stays at 62/51/11 and the existing 87-test suite stays green. Plan 05-03 adds the export line
together with all eleven export pins, which is the only way those can move without a red first run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-CONTEXT.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-PATTERNS.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-RESEARCH.md

<interfaces>
Contracts this module implements. Already in the codebase; do not redesign them.

From `packages/concierge/src/types.ts:1103-1124`:
  `Bridge<Actions extends Record<string, (...args: never[]) => unknown> = Record<string, (...args: never[]) => unknown>, Snapshot extends Record<string, () => unknown> = Record<string, () => unknown>>` with members `actions: Actions` and `snapshot: Snapshot`.
  `BridgeRegistry<B extends Bridge = Bridge>` with members `readonly id: string`, `read: () => B | null`, `register: (bridge: B) => () => void`.

From `packages/concierge/src/types.ts:668`: `SnapshotNormalizer = <T>(value: T) => T`
From `packages/concierge/src/types.ts:279`: `MESSAGE_MAX_CHARS = 180` — deliberately unannotated. Do not annotate it and do not copy 180 as a second literal.
From `packages/concierge/src/types.ts:103-136`: `ActionResult` with `ok: boolean`, `reason?: ReasonCode | undefined`, `message: string`.
From `packages/concierge/src/types.ts:187`: `"no_bridge"` is already one of the twelve closed `ReasonCode` members. The union is final at twelve.
From `packages/concierge/src/contract.ts:168`: `assertSingleInstance(): void`
From `packages/concierge/src/host.ts:93`: `warnHost(message: string): void`
</interfaces>

Compiler flags in force (`tsconfig.base.json`, verified this session): `lib: ["ES2022"]` with
`types: []` — `console`, `window`, `document`, `setTimeout` and `structuredClone` are all compile
errors. Plus `isolatedDeclarations`, `exactOptionalPropertyTypes`, `noUncheckedIndexedAccess`,
`verbatimModuleSyntax`, `strict`. There is no `noUnusedLocals`.
</context>

<tasks>

<task type="auto">
  <name>Task 1 (05-01-T1): The registry closure and the off-page helper</name>

  <read_first>
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-PATTERNS.md` lines 107-397 — the `src/bridge.ts` section. It reproduces every analog verbatim: module header, imports, factory shape, warn-once latch, try/catch with no binding, the frozen-return divergence, the `assertSingleInstance` call site, and the off-page helper. Do not re-derive these from the source files.
    - `packages/concierge/src/concierge.ts` lines 1-60, then lines 380-440. Two ranges, one read each.
    - `packages/concierge/src/contract.ts` lines 105-170.
    - `packages/concierge/src/host.ts` lines 27-96.
    - `packages/concierge/src/types.ts` lines 235-282, then lines 1100-1126.
  </read_first>

  <files>packages/concierge/src/bridge.ts</files>

  <action>
Create `packages/concierge/src/bridge.ts` containing, in order: the module header, the imports, a
module-scope section separator, then `createBridge` and `offPageResult`. Do not write the clone or
the capture loop in this task — Task 2 appends them. The file must compile standalone at the end of
this task.

MODULE HEADER. Follow `concierge.ts:1-59`: a sentence naming the requirements implemented (BRG-01,
BRG-03, BRG-04, BRG-05), then a numbered "constraints whose violation is SILENT" block, then the
standard no-DOM closing sentence copied verbatim from PATTERNS § "Module header pattern".

The numbered block carries these four, each with its measured justification:
  1. Every mutable binding is a `let` inside `createBridge`'s body, never module scope. A
     module-scope token counter pollutes across SSR requests. Mirror `concierge.ts:17-35`, including
     its note that the original bundler-based justification did not reproduce under rolldown 1.2.0
     and that the rule survived its justification being wrong.
  2. `assertSingleInstance()` is the first statement of `createBridge`'s body and is never hoisted to
     module scope. Restate `catalog.ts:17-23`'s reason: `"sideEffects": false` deletes a
     module-evaluation-time registration from the consumer bundle while it still runs under
     `node dist/index.js`. Add the consequence RESEARCH § Q4 records — a consumer idiomatically
     writes `export const resultsBridge = createBridge("results")` at their own module scope, so a
     contract-version mismatch now throws during the consumer's module evaluation, which is the shape
     `contract.ts:125-131` argues against for core's own module scope. State that the trade was
     weighed and is still correct, because `bridge: null` forever is worse than a loud throw.
  3. The default normalizer CLONES and then freezes; it does not freeze in place. One sentence
     carries the mechanism: cloning fires only read traps (`ownKeys`, `getOwnPropertyDescriptor`,
     `get`), freezing fires write traps (`preventExtensions`, `defineProperty`). Name the three
     measured failure modes of freezing in place — it fails to detach; it freezes the host app's own
     reactive store through the proxy; and it throws `TypeError` out of the capture path on proxy
     shapes whose traps do not satisfy the freeze invariants.
  4. `deepFreeze` (`catalog.ts:672`) skips accessors by testing `"value" in descriptor`, so a getter
     is never invoked. In `catalog.ts` that is a safety feature; here it is exactly why a deep freeze
     cannot detach. Same property, opposite valence — write that sentence so a future reader does not
     "fix" one by breaking the other. State that this file therefore hand-rolls a walk rather than
     reusing `deepFreeze`, and that `deepFreeze` must NOT be run over the clone's result: it was
     measured to freeze the consumer's own objects that fell into the pass-through branch.

**THE SSR-REGISTRATION INVARIANT GOES ON `createBridge`'s JSDoc, NOT IN THIS HEADER.** CONTEXT locks
the placement twice — "record the invariant in a doc comment **on `createBridge`**" — and the
placement is load-bearing for a measured reason: a non-entry module's top-of-file header does not
reach `dist/index.d.ts`. Measured this session, `src/concierge.ts`'s header phrase "constraints whose
violation is SILENT" returns **2 hits in `dist/index.js` and 0 in `dist/index.d.ts`**. The audience
for this invariant is the consumer writing `export const resultsBridge = createBridge("results")` at
their own module scope, and that consumer reads the `.d.ts`. Put it on `createBridge`'s JSDoc, where
it ships. A short cross-reference may also appear in the module header, but the header alone does not
satisfy the decision.

The invariant, stated: a registry created at consumer module scope has a process-global slot, so a
`register()` that ever runs on the server leaks into the next request. It is currently safe only
because registration happens in effects that do not run on the server — an invariant nobody had
written down. Guarding it would need a `typeof window` test, which would need a new `host.ts`
capability, which collides with the hard constraint that core constructs on the server with no
environment guards — so it is recorded and deferred to the Phase 9 adapters, where "am I on the
server" is already known for free. Doc comment only. Add no runtime guard and no `host.ts` change.

IMPORTS. Value imports first, then `import type`, both sorted by module, `.js` extension on every
specifier, matching `concierge.ts:61-73`. This task imports the value `assertSingleInstance` from
`./contract.js`, the value `warnHost` from `./host.js`, the value `MESSAGE_MAX_CHARS` from
`./types.js`, and the types `ActionResult`, `Bridge`, `BridgeRegistry` from `./types.js`. Do not
import `SnapshotNormalizer` yet — Task 2 adds it. `./concierge.js` must never appear in this file's
imports: `resolveBridge` reads `StageDefinition`, a stage concept that lives in `concierge.ts`, and
inverting the direction creates a cycle between the two largest runtime modules. Do not import
`deepFreeze` from `./catalog.js` — see header constraint 4.

`createBridge`. Signature exactly:
`export function createBridge<B extends Bridge = Bridge>(id: string): BridgeRegistry<B>`
The explicit return annotation is required by `isolatedDeclarations`; the `= Bridge` default is
required so `createBridge("results")` typechecks with no explicit type argument. Body, in order:
  - `assertSingleInstance();` as the first statement. Exactly this spelling, exactly once in code.
  - Three closure-scoped annotated `let`s: `slot: { token: number; bridge: B } | null = null`,
    `next = 0`, `warnedOverwrite = false`. A block comment above them, mirroring
    `concierge.ts:389-411`, explains that these are the instance's only mutable state and why they
    live inside the body.
  - `const registry: BridgeRegistry<B> = { … }` with `id`, `read`, `register`.
  - `read` is `(): B | null => slot?.bridge ?? null`.
  - `register` is `(bridge: B): (() => void) => { … }`. It first checks whether a live registration is
    being displaced: if `slot !== null && !warnedOverwrite`, set `warnedOverwrite = true` and call
    `warnHost` with the named message builder described below. Then `const token: number = ++next;`
    and `slot = { token, bridge };`. It stores the bridge object AS GIVEN — no normalization at
    `register()` and none at `read()`, because normalizing at either freezes state at mount time and
    breaks BRG-02. Write that reason as a comment.
  - The returned unsubscriber is `(): void => { if (slot?.token === token) { slot = null; } }` and
    nothing else. A token mismatch is a silent, idempotent no-op with NO warn: React StrictMode
    double-mount, Vue HMR and Svelte remount produce refused cleanups by design, so a warn here fires
    on every dev mount and trains developers to ignore the channel. Write that reason beside the
    mismatch path. Also state why the guard is on the TOKEN and not on the bridge object: a component
    re-registering an object that is `===` its previous registration (a memoized literal, a reused
    `$state` object) would let a stale cleanup match the live registration.
  - `return Object.freeze(registry);` as a bare single-occurrence statement. Immediately above it, a
    doc comment recording the DIVERGENCE from `createConcierge`, which returns unfrozen and says so at
    `concierge.ts:681-698`. Name that comment explicitly so nobody harmonizes the two later: the
    registry is a capability object — holding the reference IS the authorization — so an unfrozen
    `registry.read` is swappable by third-party page script in the same realm, which is the SEC-03
    attack class. The `Concierge` handle is not that. Also record the measured fact that freezing the
    object does not freeze the closure: `register()` still mutates `slot` normally afterwards.

`createBridge`'s doc comment carries the SSR invariant above, and additionally states that `id` is a
human label used only by `explain()` and is NEVER a lookup key — unforgeable identity comes from holding the object reference, which is
why `defineStage` was cut in Phase 4 and why there is no id-to-registry map to attack.

THE OVERWRITE WARN MESSAGE. Put it behind a named module-private function returning `string`, not
inline, for the reason `concierge.ts:145-149` gives: the call site becomes one short statement a
mutation battery can target as a single literal. The house shape is fixed and two existing renderers
agree on it (`concierge.ts:173-181`, `catalog.ts:581-586`):
`concierge: [code] subject "x": problem Fix: fix`. Use code `bridge_overwrite` and subject word
`bridge`, so the rendered prefix is `concierge: [bridge_overwrite] bridge "` followed by the id. The
problem clause says a second component registered over a still-live registration for this bridge, so
the first component's snapshot and actions are no longer reachable. The fix clause says to make sure
only one mounted component registers this bridge, and notes the warning fires once per registry. The
message interpolates `id` and nothing else — never a caught value, never a snapshot value.

`offPageResult`. Signature exactly:
`export function offPageResult(what: string, where: string): ActionResult`
It composes this exact sentence: `what`, then the literal ` is not available because the `, then
`where`, then ` is not open. Open the `, then `where`, then ` and try again.` Bound the result with
`message.length > MESSAGE_MAX_CHARS ? message.slice(0, MESSAGE_MAX_CHARS) : message` and return
`ok: false`, `reason: "no_bridge"`, and the bounded message.

Worked example for the doc comment, measured against the 180 bound: `what` = "The result count",
`where` = "results page" renders 108 characters, leaving 72 of headroom. Measured by execution,
not counted by hand — an off-by-one here would ship inside a doc comment on an exported function,
during the one phase whose gate exists to audit shipped prose for claims measurement contradicts.

The doc comment states: (a) the expected parameter forms — `what` is a capitalised noun phrase naming
the unavailable thing, `where` is a lowercase noun phrase naming the page; (b) that it is BOUNDED,
not SANITIZED — control-character stripping and whitespace collapsing are SEC-06 and land at the
dispatcher boundary in Phase 6, and doing either here would put one policy in two places; (c) that
`MESSAGE_MAX_CHARS` is imported from `./types.js` rather than re-declared, because it is the shared
contract between this bound and Phase 6's truncation and a second constant means two numbers that can
drift; (d) that `"no_bridge"` is one of the twelve closed `ReasonCode` members and the union is final
at twelve.

Do NOT add a `ReasonCode` member. Do NOT implement sanitizing. Do NOT touch any other file.
  </action>

  <verify>
    <automated>pnpm typecheck</automated>
    <automated>pnpm build</automated>
    <automated>pnpm test 2>&amp;1 | grep -E "Tests +87 passed"</automated>
    <automated>bash -c 'grep -v "^\s*[*/]" packages/concierge/src/bridge.ts > /tmp/b1.code; for p in "assertSingleInstance();" "return Object.freeze(registry);" "if (slot?.token === token)" "message.slice(0, MESSAGE_MAX_CHARS)"; do n=$(grep -o -F "$p" /tmp/b1.code | wc -l | tr -d " "); echo "$n  $p"; [ "$n" = "1" ] || exit 1; done; echo "mutant anchors unique: OK"'</automated>
    <automated>bash -c 'grep -c "concierge.js\|catalog.js" packages/concierge/src/bridge.ts | grep -qx 0 &amp;&amp; grep -c "deep freeze" packages/concierge/src/bridge.ts | grep -qx 0 &amp;&amp; echo "import + prose fences: OK"'</automated>
  </verify>

  <done>
`packages/concierge/src/bridge.ts` exists and compiles under the production flags. `pnpm build`
succeeds and the existing 87-test suite is still green, because the module is not referenced by
`src/index.ts` and is therefore not bundled. `createBridge` and `offPageResult` are exported with
explicit return annotations. The four mutant anchor literals each occur exactly once in non-comment
code.
  </done>

  <acceptance_criteria>
    - `pnpm typecheck` exits 0.
    - `pnpm build` exits 0. This also runs `publint --strict` and `attw` at `level: "error"`.
    - `pnpm test` output matches `Tests  87 passed (87)`. An unbarrelled module must not change the suite.
    - The built surface is unchanged at 62 names / 51 types / 11 values. Command: `node -e "const s=require('fs').readFileSync('packages/concierge/dist/index.d.ts','utf8');const m=s.match(/export\s*\{[^}]*\}/g);const l=m[m.length-1].replace(/export\s*\{/,'').replace(/\}/,'').split(',').map(x=>x.trim()).filter(Boolean);console.log(l.length,l.filter(n=>n.startsWith('type ')).length,l.filter(n=>!n.startsWith('type ')).length)"` prints `62 51 11`.
    - Source contains verbatim: `export function createBridge<B extends Bridge = Bridge>(id: string): BridgeRegistry<B>`.
    - Source contains verbatim: `export function offPageResult(what: string, where: string): ActionResult`.
    - `assertSingleInstance();` appears at a character index greater than that of `export function createBridge` — proving it is inside the body, not at module scope. This is mutant **M-05-8**'s target.
    - The unsubscriber's refusal path contains no `warnHost` call: the text between `return (): void => {` and its closing brace has zero `warnHost` occurrences. This is mutant **M-05-11**'s target — adding a warn there must be the only way to make one appear.
    - Each of `assertSingleInstance();`, `return Object.freeze(registry);`, `if (slot?.token === token)`, `message.slice(0, MESSAGE_MAX_CHARS)` occurs **exactly once** in comment-stripped source. These anchor mutants M-05-8, M-05-7, M-05-1, M-05-12. A duplicate occurrence in prose makes `mutate-and-prove.sh` mutate the wrong text and record the inverse of the truth (Known Limitation 3).
    - `grep -c "concierge.js" packages/concierge/src/bridge.ts` returns 0 and `grep -c "catalog.js" packages/concierge/src/bridge.ts` returns 0.
    - `grep -c "deep freeze" packages/concierge/src/bridge.ts` returns 0. The phase gate greps the built artifact for that exact two-word phrase; a new occurrence here would re-introduce it.
    - No mutable state at module scope: `grep -c "^let " packages/concierge/src/bridge.ts` returns 0.
    - The overwrite warn builder contains the literal `concierge: [bridge_overwrite] bridge "`.
    - `offPageResult("The result count", "results page").message` is exactly `The result count is not available because the results page is not open. Open the results page and try again.` and its length is 108.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 (05-01-T2): The structural clone and the capture loop</name>

  <read_first>
    - `packages/concierge/src/bridge.ts` — the file Task 1 wrote. Read it in full before appending.
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-RESEARCH.md` lines 441-498 (Pattern 2, Pattern 3) and lines 584-676 (the clone algorithm specified precisely: cycle safety, plain-object detection, array detection, Date/Map/Set detection, the mandatory try/catch, the cosmetic-freeze finding, symbol keys).
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-PATTERNS.md` lines 256-304 — the `deepFreeze` skeleton and the table of four transferable properties plus the one deliberate divergence.
    - `packages/concierge/src/catalog.ts` lines 650-700.
    - `packages/concierge/src/concierge.ts` lines 445-475.
  </read_first>

  <files>packages/concierge/src/bridge.ts</files>

  <action>
Append to `packages/concierge/src/bridge.ts` a new section separator and three units: `cloneDetached`
(module-private), `makeDefaultNormalizer` (module-private), and `captureSnapshot` (exported). Add
`SnapshotNormalizer` to the existing `import type { … } from "./types.js"` list.

**THE EXOTIC-WARN SIGNAL PATH — read this before writing any of the three units.** CONTEXT locks a
`snapshot_exotic` warn on the exotic-clone fallback, overriding RESEARCH assumption A4 which had said
no such warn is added. That override needs a mechanism, and four other pins in this same task jointly
close the obvious ones: `SnapshotNormalizer` is `<T>(value: T) => T` with no out-channel; module-scope
mutable state is forbidden by header constraint 1 and gated by `grep -c "^let "` returning 0; a second
walk over the clone's result is forbidden (Pitfall 4); and the single `cloneDetached(…) as T` call
site is mutant M-05-3's anchor and must stay live and unique.

The one shape that satisfies all four is a **factory**: `makeDefaultNormalizer` takes an `onExotic`
callback and returns a `SnapshotNormalizer` closure that carries the `cloneDetached` call. The
callback is closed over per snapshot key inside `captureSnapshot`'s body, so no state lives at module
scope, the normalizer's public signature is untouched, and the M-05-3 anchor moves inside the returned
closure while remaining a single live call site.

**Do not instead build the normalizer as an anonymous closure inline in `captureSnapshot`.** That
would leave a named default normalizer with no caller, and M-05-3 would then mutate a function that is
never invoked — recorded by the battery as an escape, which is the inverse of the truth and precisely
what the phase gate exists to prevent.

`cloneDetached(v: unknown, seen: WeakMap<object, unknown>, onExotic: () => void): unknown` —
module-private, not exported. The third parameter is called from the `Date`/`Map`/`Set` catch and from
nowhere else, so `cloneDetached` itself never imports or calls `warnHost`; the emission point stays a
single call site in `captureSnapshot` that the mutation battery can target as one literal.
Copy `deepFreeze`'s skeleton and invert exactly one thing. In order:
  - Primitive/null early-out: `if (typeof v !== "object" || v === null) { return v; }`, identical to
    `catalog.ts:672-674`.
  - `const obj: object = v;` then a `seen.get(obj)` lookup returning the hit when defined. The map is
    a `WeakMap`, not `deepFreeze`'s `WeakSet`, because a clone needs "what did I produce for this",
    not merely "have I walked this". This delivers cycle safety AND DAG identity: a self-referencing
    node satisfies `c.self === c`, and a node reached twice satisfies `c.l === c.r` while
    `c.l !== theOriginal`.
  - ARRAYS, detected with `Array.isArray(obj)`, never `instanceof Array`. Measured: `Array.isArray`
    is the only predicate that is both proxy-transparent and realm-transparent; `instanceof Array`
    returns false cross-realm. Allocate the output array, call `seen.set` with it BEFORE recursing
    into elements, recurse, then `Object.freeze` the output and return it.
  - `Date` / `Map` / `Set`, detected as `instanceof` OR the `Object.prototype.toString` tag, because
    neither predicate alone is complete. Measured: a `Proxy` over a `Date` reports `[object Object]`
    from the tag but passes `instanceof`; a cross-realm `Date` fails `instanceof` but passes the tag.
    The two predicates are blind in exactly opposite directions — reproduce both halves as a comment.
    **This branch MUST be wrapped in `try {} catch {}` with no binding.** A naively proxied `Date` is
    unextractable by every route measured — six routes, all `TypeError` — and `new Map(proxiedMap)`
    fails the same way; a proxy that binds methods to its target (Vue's `reactive` for collections)
    works, so the throw is a property of naive proxying rather than of proxying in general and cannot
    be assumed away. On catch, call `onExotic()` and then fall back to pass-through-by-reference.
    Pass `onExotic` down through every recursive `cloneDetached` call, so a proxied `Date` nested
    inside a plain object still reports. Also record as a comment that freezing a `Date`, `Map` or `Set` is
    cosmetic — measured, their own mutator methods still succeed on a frozen instance — so this
    branch delivers detachment, not immutability, and the doc comment must not claim more.
  - PLAIN OBJECTS, detected as
    `const proto: object | null = Object.getPrototypeOf(obj);` then `proto === Object.prototype || proto === null`.
    **The `|| proto === null` arm is load-bearing and is mutant M-05-6's target**: omitting it
    silently passes through a record built with `Object.create(null)`, which is exactly the shape
    `catalog.byName` uses. Allocate a fresh output object, `seen.set` BEFORE recursing, then iterate
    `Object.keys(obj)` and read each value through an indexed `[[Get]]` so getters ARE invoked —
    invoking them IS the detachment. This is the one place the walk deliberately diverges from
    `deepFreeze`, which skips accessors by testing `"value" in descriptor`. Under
    `noUncheckedIndexedAccess` the indexed read is `unknown`; do not assume presence. Freeze the
    output object and return it.
  - Symbol keys are NOT carried, because `Object.keys` returns enumerable string keys only. State this
    explicitly in a comment rather than leaving it to be discovered: the three target frameworks use
    symbol keys for internal markers (Vue's `__v_raw`, Svelte's internal markers), which is precisely
    the framework reactivity BRG-05 exists to drop, and a snapshot is a payload Phase 8 will hash.
  - EVERYTHING ELSE: `return v;` — by reference, unfrozen. Document the limit: class instances,
    `Object.create({})`, and cross-realm plain objects all land here. The cross-realm miss is the safe
    failure direction (the value is handed back untouched rather than mangled) and must not be chased
    with an `Object.prototype.toString.call(v) === "[object Object]"` fallback, because that same
    predicate is true for class instances and for `Object.create({})` and would start cloning things
    CONTEXT decided to pass through.

`makeDefaultNormalizer(onExotic: () => void): SnapshotNormalizer` — module-private, not exported. The
explicit `SnapshotNormalizer` return annotation is required by `isolatedDeclarations`. Its body is a
single `return` of a generic arrow function `<T>(value: T): T => { … }` whose own body is exactly two
statements: `const seen: WeakMap<object, unknown> = new WeakMap<object, unknown>();` then
`return cloneDetached(value, seen, onExotic) as T;`.

Keeping the returned closure to that shape matters: the second statement is mutant **M-05-3**'s
anchor, and the mutant that replaces `cloneDetached(value, seen, onExotic) as T` with
`Object.freeze(value) as T` was measured this session to compile under TypeScript 7.0.2 with these
exact flags and to reproduce the deep-freeze default's observable — the captured value follows the
store to `"boots"` and `Object.isFrozen(proxy)` becomes `true`, with no throw. Do not inline the
`WeakMap` allocation into the return expression; that would destroy the anchor. A fresh `WeakMap` per
normalizer invocation is correct — the memo must not survive across values, or two unrelated snapshot
keys sharing a sub-object would alias.

**Do not run `deepFreeze` — or any second walk — over the clone's result.** Measured: with a class
instance in the pass-through branch, a second pass leaves `Object.isFrozen(consumerInstance) === true`,
so the consumer's own model object becomes read-only and their next write throws in their code. The
clone's per-node `Object.freeze` is complete for everything it constructs; pass-through values are by
definition not ours to freeze. Write this refusal as a comment so a reviewer does not add the
belt-and-braces pass.

`captureSnapshot` — exported. Signature exactly:
`export function captureSnapshot<B extends Bridge>(bridge: B, id: string, normalize?: SnapshotNormalizer): Record<string, unknown>`
Body:
  - `const out: Record<string, unknown> = {};`
  - `const warned: Set<string> = new Set<string>();` allocated INSIDE the body — see the latch note.
  - Iterate `Object.keys(bridge.snapshot)`. For each key read the getter, skip when it is `undefined`
    (required by `noUncheckedIndexedAccess`), then bind `normalizeValue` **inside the loop body** as
    `normalize ?? makeDefaultNormalizer(<a callback that emits the snapshot_exotic warn for this id and key, latched on `warned`>)`,
    annotated `: SnapshotNormalizer`. Constructing one normalizer per key is trivial in cost and is
    what lets the callback close over the current key directly, with no mutable cursor. Then run
    `try { out[key] = normalizeValue(getter()); } catch { out[key] = undefined; …warn once… }`.
    **Both the invocation and the normalize call are inside the same `try`.** A `try` scoped to
    `getter()` alone does not catch a getter nested inside the returned value — measured, that one
    throws from inside the normalizer — and the escaping `Error`'s message echoes user input, which is
    the covert-PII channel `CLAUDE.md` forbids. The `catch` binds nothing, so there is no caught value
    in scope to interpolate by accident; the property is structural rather than a matter of
    remembering. Transplant the justification paragraph at `concierge.ts:450-468`.
  - `return out;` — the container is deliberately NOT frozen. Record why, following
    `concierge.ts:681-698`'s precedent of writing down a refusal: it is allocated fresh per call and
    handed to exactly one caller, and freezing it would be a seal with no threat behind it while
    still leaving the pass-through values it holds unfrozen — the partial-`readonly` shape Phase 4
    rejected as "worse than none, because a reader stopped looking".

THE TWO CAPTURE WARN LATCHES. Both go through `warnHost`, both use the house shape
`concierge: [code] subject "x": problem Fix: fix` with subject word `snapshot` and the subject string
being the registry id and the key joined by a dot, rendering as `concierge: [snapshot_threw] snapshot "results.filters": …`.
Two distinct codes: `snapshot_threw` for the throwing-getter path and `snapshot_exotic` for the
exotic-clone pass-through fallback. Put each behind a named module-private message builder, as in
Task 1. The `snapshot_threw` problem clause says the getter threw so that key is absent from the
snapshot; its fix clause says to make the getter total, because it runs on every capture. The
`snapshot_exotic` problem clause says the value could not be detached and was carried by reference so
it may still change after capture; its fix clause says to supply a `normalizeSnapshot` that
understands this value, naming `$state.snapshot` as the Svelte case. Neither message interpolates
anything but `id` and the key.

LATCH SCOPE — a deliberate, documented narrowing of CONTEXT's "warn once", which must be written down
rather than chosen silently. The `warned` `Set` is allocated inside `captureSnapshot`'s body, so the
warn is once per key per capture rather than once per key per process. The reason: the only way to get
a process-lifetime latch here is module-scope state, which header constraint 1 forbids outright — under
SSR a module-scope `Set` would suppress request N's warning because request 1 already warned, and a
silenced diagnostic is strictly worse than a repeated one. Capture is a human-rate event (Phase 8 arms
consent in response to a human act), not a render-rate one, so the "a warning that prints forever is a
warning nobody reads" hazard that justifies the `register()` latch does not apply;
`concierge.ts:210-215` makes exactly this argument for declining to warn on a throwing `read()` inside
`explain`. The in-body `Set` still does real work: it keeps one key from emitting both `snapshot_threw`
and `snapshot_exotic` in the same capture. Note in the comment that the `register()` latch is
unaffected and stays per-registry, which is what mutant **M-05-10** targets.

**A caller-supplied `normalize` suppresses the exotic warn, and that is correct.** When the consumer
passes their own normalizer — the Svelte adapter's `$state.snapshot`, for instance — core neither
constructs nor threads `onExotic`, and cannot know whether that normalizer detached. Warning there
would be a claim core has no evidence for. Say so in `captureSnapshot`'s doc comment rather than
leaving the asymmetry to be discovered.

Do NOT export `cloneDetached` or `makeDefaultNormalizer`. The export surface grows by exactly three
values — `createBridge`, `captureSnapshot`, `offPageResult` — and plan 05-03's pins are written
against that number.
  </action>

  <verify>
    <automated>pnpm typecheck</automated>
    <automated>pnpm build</automated>
    <automated>pnpm test 2>&amp;1 | grep -E "Tests +87 passed"</automated>
    <automated>bash -c 'grep -v "^\s*[*/]" packages/concierge/src/bridge.ts > /tmp/b2.code; for p in "cloneDetached(value, seen, onExotic) as T" "Array.isArray(obj)" "proto === null" "return out;"; do n=$(grep -o -F "$p" /tmp/b2.code | wc -l | tr -d " "); echo "$n  $p"; [ "$n" = "1" ] || exit 1; done; echo "clone anchors unique: OK"'</automated>
    <automated>bash -c 'grep -v "^\s*[*/]" packages/concierge/src/bridge.ts > /tmp/b2b.code; n=$(grep -o -F "makeDefaultNormalizer(" /tmp/b2b.code | wc -l | tr -d " "); echo "makeDefaultNormalizer call+decl sites: $n"; [ "$n" = "2" ] || exit 1; echo "factory is on a live call path: OK"'</automated>
    <automated>bash -c 'grep -qF "export function captureSnapshot<B extends Bridge>(bridge: B, id: string, normalize?: SnapshotNormalizer): Record<string, unknown>" packages/concierge/src/bridge.ts &amp;&amp; ! grep -qE "export (function|const) (cloneDetached|makeDefaultNormalizer)" packages/concierge/src/bridge.ts &amp;&amp; grep -qF "makeDefaultNormalizer(" packages/concierge/src/bridge.ts &amp;&amp; echo "signatures + internals: OK"'</automated>
  </verify>

  <done>
`src/bridge.ts` is complete: three exported values (`createBridge`, `captureSnapshot`,
`offPageResult`) and two module-private helpers. `pnpm typecheck` and `pnpm build` pass, the 87-test
suite is still green, and the built export surface is still 62/51/11 because the module remains
unbarrelled.
  </done>

  <acceptance_criteria>
    - `pnpm typecheck` exits 0 and `pnpm build` exits 0.
    - `pnpm test` output matches `Tests  87 passed (87)`.
    - The built surface still parses to `62 51 11` (same command as Task 1).
    - Source contains verbatim: `export function captureSnapshot<B extends Bridge>(bridge: B, id: string, normalize?: SnapshotNormalizer): Record<string, unknown>`.
    - Neither `cloneDetached` nor `makeDefaultNormalizer` is exported: `grep -cE "export (function|const) (cloneDetached|makeDefaultNormalizer)"` returns 0.
    - Each of `cloneDetached(value, seen, onExotic) as T`, `Array.isArray(obj)`, `proto === null`, `return out;` occurs **exactly once** in comment-stripped source. These anchor mutants M-05-3, and the array/null-prototype/return arms M-05-6 depends on.
    - **`makeDefaultNormalizer` is on a live call path.** `makeDefaultNormalizer(` occurs exactly twice in comment-stripped source — once as the declaration, once as the call inside `captureSnapshot`'s loop. If it occurs once, the normalizer was inlined as an anonymous closure and M-05-3 would mutate a function nobody calls, which the battery records as an escape. This criterion is the guard against that specific inversion.
    - The exotic signal path exists end to end: `cloneDetached`'s signature takes `onExotic: () => void` as its third parameter, `onExotic()` is called from inside the `Date`/`Map`/`Set` `catch`, `onExotic` is threaded through every recursive `cloneDetached` call, and `warnHost` does not appear inside `cloneDetached`.
    - `captureSnapshot` binds `normalizeValue` inside the key loop, not before it, so the exotic callback closes over the key. Assert the `normalize ??` expression appears after the `for` that iterates `Object.keys(bridge.snapshot)`.
    - `captureSnapshot`'s doc comment records that a caller-supplied `normalize` suppresses the exotic warn, and why.
    - The `Date`/`Map`/`Set` branch is inside a `try`: the source region containing `instanceof Date` also contains `catch {` before the next top-level `function` keyword. Absence of that `catch` is Pitfall 5.
    - The capture loop's `try` wraps the normalize call, not just the getter. Assert the comment-stripped source contains the substring `normalizeValue(` on the same line as `out[key] =` inside a `try` block, and does NOT contain a line matching `try { out[key] = getter(); }`. This is mutant **M-05-9**'s target and Pitfall 3.
    - `grep -c "deepFreeze" packages/concierge/src/bridge.ts` returns 0 — no belt-and-braces second walk (Pitfall 4).
    - No module-scope mutable state: `grep -c "^let " packages/concierge/src/bridge.ts` returns 0, and `new WeakMap(`, `new Set(`, `new WeakSet(` occur only inside function bodies (Pitfall 6).
    - `grep -c "deep freeze" packages/concierge/src/bridge.ts` returns 0.
    - The two capture warn builders contain the literals `concierge: [snapshot_threw] snapshot "` and `concierge: [snapshot_exotic] snapshot "`, and the two codes are distinct.
    - Behavioural proof of every claim above lands in plans 05-04 and 05-05, which run against `dist/index.js` and therefore cannot precede the barrel in 05-03. This task's gates are compile-level and source-level by necessity, not by omission.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer page component → `registry.register(bridge)` | The bridge object and every snapshot getter on it are consumer-authored code that may throw arbitrary messages |
| third-party page script → the returned registry object | Same realm, same global. The registry object IS the capability |
| consumer snapshot values → the clone walk | Proxied and exotic values that throw on every extraction route |
| core → `warnHost` (host `console`) | Outbound message channel; a caught message would echo user input |
| a second copy of core → `globalThis[Symbol.for("@fullselfbrowsing/concierge.contract")]` | Cross-realm contract slot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | the registry object returned by `createBridge` | mitigate | `return Object.freeze(registry);` as a bare single-occurrence statement; measured to make `registry.read = evil` throw `TypeError` in ESM strict mode. Mutant M-05-7 |
| T-05-02 | Spoofing | bridge identity | mitigate | Identity is the object reference. `id` is a label for `explain()` only and is never a lookup key, so there is no id-to-registry map to attack (`PITFALLS.md:234` item 3) |
| T-05-03 | Denial of service (self-inflicted) | two core copies split the registry, so every handler sees `bridge: null` forever on a page that is definitely open | mitigate | `assertSingleInstance();` as the first statement of `createBridge`'s body, never module scope. Mutant M-05-8 |
| T-05-04 | Elevation of privilege | a captured snapshot that is still a live proxy view makes Phase 8's CON-04 drift check compare a value against itself and pass unconditionally | mitigate | `makeDefaultNormalizer`'s returned closure delegates to `cloneDetached`, a structural clone; it never freezes in place. Mutant M-05-3 against the Shape F fixture |
| T-05-05 | Information disclosure | the `catch` in the capture loop, and the warn builders | mitigate | Every `catch` binds nothing. The `try` wraps the normalizer call, not just the getter, so a nested throwing getter cannot escape. Warn text is fixed prose plus the developer-authored `id` and key only |
| T-05-06 | Denial of service (against the consumer) | the host app's own reactive store | mitigate | Never freeze a value we did not construct: no `deepFreeze` pass over the clone result, and `grep -c deepFreeze` returns 0 in this file |
| T-05-07 | Tampering | a snapshot key silently becoming `undefined`, which a downstream consent comparison would read as "unchanged" | mitigate | Warn once per key per capture naming registry id and key, codes `snapshot_threw` and `snapshot_exotic`. Mutant M-05-10 covers the `register()` latch half |
| T-05-SC | Tampering | npm/pnpm package installs | accept | Phase 5 installs zero packages. Core is dependency-free by constraint C1; `pnpm check:deps` (`scripts/pkg05-zero-runtime-deps.mjs`) asserts the empty external module graph and runs unmodified. No slopcheck gate is required because no package is proposed |
</threat_model>

<verification>
After both tasks:

1. `pnpm build && pnpm test && pnpm typecheck` — all green, `Tests  87 passed (87)`.
2. `git diff --exit-code` shows only `packages/concierge/src/bridge.ts` as a new file.
3. The built export surface is unchanged at 62/51/11 — proof the module is not yet barrelled.
4. Mutant anchor uniqueness confirmed for all eight literals across the two tasks, counted with
   comments stripped for readability but re-confirmed **unfiltered** before plan 05-07 uses them
   (Known Limitation 3: `mutate-and-prove.sh` does not skip comments).
</verification>

<success_criteria>
- `packages/concierge/src/bridge.ts` exists, compiles, and exports exactly three values.
- The existing suite is untouched: 7 files, 87 tests, green.
- Every mutant anchor this plan is responsible for (M-05-1, M-05-3, M-05-6, M-05-7, M-05-8,
  M-05-9, M-05-10, M-05-11, M-05-12) has a unique literal in the written file.
- No import of `./concierge.js` or `./catalog.js`; no module-scope mutable state; no `typeof window`;
  no `structuredClone`; no `ReasonCode` addition; no message sanitizing.
</success_criteria>

<output>
Create `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-01-SUMMARY.md` when done.

The SUMMARY must record, because later plans read them:
- The exact rendered text of all three warn messages (with a sample id and key substituted).
- The exact `offPageResult("The result count", "results page").message` string and its length.
- The unfiltered occurrence count of every mutant anchor literal listed above, **including the final
  spelling of `cloneDetached(value, seen, onExotic) as T`** — plan 05-07's M-05-3 run uses it verbatim.
- Confirmation that `makeDefaultNormalizer(` occurs twice (declaration plus one live call site), so
  M-05-3 mutates a function that is actually invoked.
- The final line count of `src/bridge.ts`.
</output>
