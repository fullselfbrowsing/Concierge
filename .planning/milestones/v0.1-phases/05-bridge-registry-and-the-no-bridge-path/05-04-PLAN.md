---
phase: 05-bridge-registry-and-the-no-bridge-path
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - packages/concierge/test/bridge.test.ts
autonomous: true
requirements: [BRG-01, BRG-02, BRG-04]
user_setup: []

must_haves:
  truths:
    - "A stale unsubscriber fired after a replacement is refused, including when the replacement bridge object is === the one it captured."
    - "register() stores the bridge as given; read() returns that same object by reference, so snapshot getters stay live."
    - "A handler reading the registered bridge after the app moved sees the new values with no re-registration in between."
    - "The returned registry object is frozen: assigning to registry.read throws in ESM strict mode."
    - "register() over a still-live registration warns exactly once per registry; a refused unsubscriber warns never."
  artifacts:
    - path: "packages/concierge/test/bridge.test.ts"
      provides: "BRG-01, BRG-02 and BRG-04 asserted against the built artifact, with the five non-discriminating orderings labelled as contract pins"
      contains: "describe(\"BRG-01"
      min_lines: 260
  key_links:
    - from: "packages/concierge/test/bridge.test.ts"
      to: "packages/concierge/dist/index.js"
      via: "dynamic import in beforeAll behind an existsSync guard"
      pattern: "await import\\(DIST_URL\\.href\\)"
---

<objective>
Create `packages/concierge/test/bridge.test.ts` — the registry half of the phase's runtime proof:
BRG-01 (identity-guarded unsubscriber), BRG-04 (a stale cleanup cannot clear a newer registration),
BRG-02 (the stored bridge stays live), the frozen-capability guarantee, and the two warn policies.

Purpose: CONTEXT § Specific Ideas sets the standard — "a registry that *looks* identity-guarded
without a test proving the stale cleanup is refused would be rejected. Each of the five success
criteria needs a test that fails when the guard is removed." This file is where criteria 1 and 2 get
that test.

**The division of tests across files is Claude's discretion per CONTEXT.** This phase uses two runtime
files: this one for the registry, and `test/bridge-snapshot.test.ts` (plan 05-05) for capture,
detachment and the no-bridge path. They own disjoint concerns, run in the same wave, and both match
the `pnpm test -- bridge` filter. VALIDATION.md's Wave 0 list names only `test/bridge.test.ts`; the
split is recorded here so a later reader does not read it as an omission.

Output: one new test file. The suite grows from 87 tests; the whole suite must be green at the end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-CONTEXT.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-PATTERNS.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-VALIDATION.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-01-SUMMARY.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-03-SUMMARY.md

<interfaces>
Reachable from `../dist/index.js` after plan 05-03:
  `createBridge(id)` returns a frozen `{ id, read, register }`.
  `read()` returns the registered bridge object or `null`.
  `register(bridge)` returns an identity-guarded unsubscriber.
  `captureSnapshot(bridge, id, normalize?)` and `offPageResult(what, where)` also ship, but belong to plan 05-05's file.
</interfaces>

**CASE ID PREFIX: `B`.** Measured this session, the prefixes already in use across `test/` are `C`
(catalog.test.ts, C1-C26), `F` (single-instance.test.ts, F1a-F5) and `S` (concierge.test.ts, S1-S27).
Do not continue `S` — numbering is per-file and Phase 4 owns S1-S27. Use `B1`, `B2`, … sequentially.

**THE ORDERING MATRIX — measured, and five of thirteen prove nothing.** `A` and `B` are distinct
bridge objects; `u1`/`u2`/`u3` are the unsubscribers from the first/second/third `register` call.
Results are what `read()` returns at the end, under three implementations: **token** (correct),
**object-guarded** (Anti-Pattern 6), **naive** (unconditional clear).

| # | Ordering | token | object-guarded | naive | discriminates |
|---|----------|-------|----------------|-------|---------------|
| O1 | `reg A(u1); u1(); reg A(u2)` | A | A | A | **neither** |
| O1b | O1, then stale `u1` fires again | A | null | null | both |
| O2 | `reg A(u1); reg B; u1()` | B | B | null | naive |
| O2b | `reg A(u1); reg A(u2); u1()` | A | null | null | **both** |
| O3 | `reg A(u1); u1(); u1(); reg B` | B | B | B | **neither** |
| O3b | `reg A(u1); u1(); reg B; u1()` | B | B | null | naive |
| O4 | `reg A; reg B(u2); reg A; u2()` | A | A | null | naive |
| O4b | `reg A(u1); reg B; reg A(u3); u1()` | A | null | null | **both** |
| O4c | `reg A(u1); reg A; reg A(u3); u1()` | A | null | null | **both** |
| O5 | `reg A; reg B(u2); u2()` | null | null | null | **neither** |
| O6 | never registered | null | null | null | **neither** |
| O7 | `reg A(u1); u1()` | null | null | null | **neither** |
| O8 | `reg A(u1); reg B(u2); reg A(u3); u2()` | A | A | null | naive |

Only **O1b, O2b, O4b, O4c** discriminate the object-identity defect (mutant M-05-1). Eight
discriminate the naive clear (M-05-2): O1b, O2, O2b, O3b, O4, O4b, O4c, O8.
</context>

<tasks>

<task type="auto">
  <name>Task 1 (05-04-T1): File scaffold, header, and the artifact + contract-registry guards</name>

  <read_first>
    - `packages/concierge/test/concierge.test.ts` lines 1-130 (the "what escapes without this file" header, including the "Two behaviours have no single-literal mutant" block at 90-111) and lines 300-420 (imports, `DIST_URL`, the `beforeAll` artifact guard, the contract-registry reset, the declaration helpers).
    - `packages/concierge/test/export-surface.test.ts` lines 25-50 — Trap 2, the precedent for recording something deliberately NOT asserted rather than faking an assertion.
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-PATTERNS.md` lines 649-760 — the `test/bridge.test.ts` section: header pattern, imports + artifact guard verbatim, the contract-registry reset (REQUIRED and easy to miss), and the `describe`/`it` naming convention.
  </read_first>

  <files>packages/concierge/test/bridge.test.ts</files>

  <action>
Create `packages/concierge/test/bridge.test.ts` with the header, imports, guards and helpers. No `it`
cases in this task beyond what is needed to make the file a valid, passing suite — Tasks 2 and 3 fill
it.

HEADER — "What escapes without this file", numbered, with measured evidence, following
`concierge.test.ts:1-130`. It must name at least these defects and, for each, the measurement that
proves it escapes a naive suite:
  1. **The object-identity guard (Anti-Pattern 6).** Measured against thirteen mount/unmount
     orderings, guarding on `slot.bridge === bridge` instead of on the token passes ten of thirteen —
     including both orderings a developer writes first. Only O1b, O2b, O4b and O4c catch it, and every
     one of them requires a stale unsubscriber to fire AFTER a replacement whose bridge object is
     `===` the one that unsubscriber captured.
  2. **The unconditional clear.** Eight orderings catch it; the five listed in the contract-pin block
     below catch nothing.
  3. **Normalizing at `register()` or `read()`.** Either freezes state at mount time and breaks
     BRG-02 — the handler would read the mount-time value forever, which looks correct on the first
     read and is wrong on every subsequent one.
  4. **An unfrozen registry object.** The registry IS the capability; an unfrozen `registry.read` is
     swappable by third-party page script in the same realm.
  5. **A warn on the refused unsubscriber.** It would fire on every React StrictMode dev mount, and a
     warning that fires on every mount trains developers to ignore the channel.

THE CONTRACT-PIN BLOCK — this is a required section of the header, not optional prose. Following the
precedent of `concierge.test.ts:90-111` and `export-surface.test.ts`'s Trap 2, state plainly that five
of the thirteen orderings this file contains — **O1, O3, O5, O6, O7** — produce identical results on
the correct implementation, on the object-identity defect, and on the naive clear. They are
**contract pins, not validation**, and they must not be counted toward BRG-01 or BRG-04 coverage. Give
each of the five its one-line reason for existing anyway: O1 is the ordering every reader looks for
first and its absence reads as an omission; O3 pins unsubscriber idempotence; O5 pins the ordinary
happy path; O6 pins the initial state; O7 pins the simple unmount. State that exactly four orderings
— O1b, O2b, O4b, O4c — discriminate Anti-Pattern 6. Counting O1 or O3 toward coverage would be the
false-coverage failure Trap 2 already warns about.

IMPORTS AND THE ARTIFACT GUARD — copy `concierge.test.ts:308-345` exactly in shape. `existsSync` from
`node:fs`, `fileURLToPath` from `node:url`, and `beforeAll`, `beforeEach`, `describe`, `expect`, `it`
from `vitest`. `DIST_URL` is `new URL("../dist/index.js", import.meta.url)`; `DIST_PATH` is its
`fileURLToPath`. Bind `createBridge` in `beforeAll` via `await import(DIST_URL.href)` rather than
importing it statically, and leave the binding unannotated — a static import would fail with an opaque
module-resolution error on a fresh checkout BEFORE the existence guard could produce the sentence that
tells a developer to run `pnpm build`. Reproduce that reasoning as a comment, and reproduce the
guard's actionable error message.

THE CONTRACT-REGISTRY RESET — REQUIRED, and the easiest thing in this plan to omit. `createBridge`
calls `assertSingleInstance()`, so this file needs the same reset `concierge.test.ts:318-323` and
`:347-352` use. Hard-code `const KEY = Symbol.for("@fullselfbrowsing/concierge.contract");` rather
than importing the symbol from the artifact under test — the registry key is a cross-realm contract
between two copies of the package that share no bindings, so its identity is the STRING and nothing
else, and importing it would make this suite agree with whatever the artifact happens to say. Use
`delete (globalThis as Record<symbol, unknown>)[KEY];` in a `beforeEach`, not assignment to
`undefined`: `assertSingleInstance` branches on `prior === undefined`, so the slot must be genuinely
absent. Carry both reasons as comments.

HELPERS. Add two small local factories used by Tasks 2 and 3:
  - one that builds a distinct bridge object with a named marker so `read()` identity assertions can
    name which object they got;
  - one that builds a bridge whose `snapshot` reads through a mutable closure variable, so BRG-02 can
    move the app between two reads without re-registering.
Keep both inline in this file. Do NOT add anything to `test/fixtures/` — `scripts/pack-install-check.sh`
copies that directory into a foreign scratch project and a sibling module gets pulled into that
program by accident.

CONSOLE CAPTURE — declare the idiom in the header now, even though Task 3 uses it: it is a PLAIN
global assignment (`globalThis.console = { ...realConsole, warn: sink }`), never the Vitest mocking
API. A grep for that API's namespace prefix over `test/` returns zero across every file today and must
still return zero afterwards, which is also why the note spells the prefix out in prose rather than
writing it. Restoration happens in a `finally`, never after the assertions. The real console is spread
rather than replaced wholesale so an unrelated `console.error` from Vitest itself does not become
"undefined is not a function".

Add one `it` in this task — `B1`, the O6 ordering (never registered, `read()` is `null`) — so the file
is a valid passing suite. Label it in place as a contract pin.
  </action>

  <verify>
    <automated>pnpm build</automated>
    <automated>pnpm test -- bridge</automated>
    <automated>pnpm test</automated>
    <automated>bash -c 'f=packages/concierge/test/bridge.test.ts; grep -qF "Symbol.for(\"@fullselfbrowsing/concierge.contract\")" $f &amp;&amp; grep -qF "delete (globalThis" $f &amp;&amp; grep -qF "await import(DIST_URL.href)" $f &amp;&amp; grep -qF "existsSync(DIST_PATH)" $f &amp;&amp; echo "guards present: OK"'</automated>
    <automated>bash -c 'n=$(grep -rc "vi\.spyOn\|vi\.fn\|vi\.mock" packages/concierge/test/ | grep -v ":0$" | wc -l | tr -d " "); echo "files using vitest mocking API: $n"; [ "$n" = "0" ]'</automated>
  </verify>

  <done>
`test/bridge.test.ts` exists, imports the built artifact behind an `existsSync` guard, resets the
contract registry in `beforeEach`, and carries a header that names the five escaping defects with
measured evidence and labels the five non-discriminating orderings as contract pins. One case (`B1`)
passes. The whole suite is green.
  </done>

  <acceptance_criteria>
    - `pnpm test` passes the whole suite; `pnpm test -- bridge` runs this file and passes.
    - The file contains `Symbol.for("@fullselfbrowsing/concierge.contract")` as a hard-coded literal, not an import from the artifact.
    - The reset uses `delete` on the `globalThis` slot, not assignment to `undefined`. Assert the file contains `delete (globalThis` and does not contain `[KEY] = undefined`.
    - The artifact guard exists: the file contains `existsSync(DIST_PATH)` and an error message naming `pnpm build`.
    - `createBridge` is bound in `beforeAll` via `await import(DIST_URL.href)`, not statically imported.
    - The header names all five escaping defects and, for each, the measurement that proves it escapes.
    - The header contains an explicit contract-pin block naming **O1, O3, O5, O6 and O7** as non-discriminating, and naming **O1b, O2b, O4b, O4c** as the four that discriminate Anti-Pattern 6. Absence of this block is a failure even if every test passes — CONTEXT and VALIDATION both require the labelling.
    - No Vitest mocking API anywhere under `packages/concierge/test/`: a grep for `vi.spyOn`, `vi.fn` and `vi.mock` returns zero non-comment hits across the directory. This must still hold after the file is added.
    - Nothing was added to `packages/concierge/test/fixtures/`: `git status --porcelain packages/concierge/test/fixtures/` is empty.
    - Case ids in this file use the `B` prefix and do not reuse `S`, `C` or `F`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 (05-04-T2): The thirteen orderings — BRG-01 and BRG-04</name>

  <read_first>
    - `packages/concierge/test/bridge.test.ts` — the file Task 1 wrote.
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-RESEARCH.md` lines 705-746 — the complete ordering matrix with the three-implementation comparison, and the statement of which orderings discriminate what.
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-VALIDATION.md` lines 130-145 — the non-discriminating table.
    - `packages/concierge/test/concierge.test.ts` lines 416-430 — the `describe` = requirement id, `it` = case id + claim convention.
  </read_first>

  <files>packages/concierge/test/bridge.test.ts</files>

  <action>
Add all thirteen orderings as `it` cases, under two `describe` blocks whose titles are requirement ids
plus the claim, matching `concierge.test.ts:416`:
  - `describe("BRG-01 — the unsubscriber clears the slot only when the slot still holds its own registration", …)`
  - `describe("BRG-04 — a stale unregister from a remounted component cannot clear a newer registration", …)`

Put the four discriminating orderings (O1b, O2b, O4b, O4c) under BRG-04 and the rest under BRG-01;
the exact division is yours, but every case's `it` title must carry its case id, its ordering in the
`reg A(u1); reg B; u1()` shorthand, and the claim.

Use the matrix in this plan's `<context>` as the source of expected values. Every expectation below is
what the CORRECT (token) implementation returns; do not derive them again.

  - O1 `reg A(u1); u1(); reg A(u2)` → `read()` is `A`. **CONTRACT PIN.**
  - O1b — O1, then the stale `u1` fires again → `read()` is still `A`. **Discriminates both defects.**
  - O2 `reg A(u1); reg B; u1()` → `read()` is `B`. Discriminates the naive clear.
  - O2b `reg A(u1); reg A(u2); u1()` → `read()` is `A`. **Discriminates both.** This is the minimal
    spelling of the defect the ROADMAP note names, and the whole reason the token exists: the same
    object registered twice.
  - O3 `reg A(u1); u1(); u1(); reg B` → `read()` is `B`. **CONTRACT PIN.**
  - O3b `reg A(u1); u1(); reg B; u1()` → `read()` is `B`. Discriminates the naive clear.
  - O4 `reg A; reg B(u2); reg A; u2()` → `read()` is `A`. Discriminates the naive clear.
  - O4b `reg A(u1); reg B; reg A(u3); u1()` → `read()` is `A`. **Discriminates both.** This is the
    realistic one — a component unmounting late after a sibling has come and gone — and it uses
    distinct objects, which makes it harder to dismiss as contrived.
  - O4c `reg A(u1); reg A; reg A(u3); u1()` → `read()` is `A`. **Discriminates both.**
  - O5 `reg A; reg B(u2); u2()` → `read()` is `null`. **CONTRACT PIN.**
  - O6 — never registered → `read()` is `null`. **CONTRACT PIN** (written in Task 1 as `B1`).
  - O7 `reg A(u1); u1()` → `read()` is `null`. **CONTRACT PIN.**
  - O8 `reg A(u1); reg B(u2); reg A(u3); u2()` → `read()` is `A`. Discriminates the naive clear.

**Every one of the five contract-pin cases must carry an inline comment saying so**, cross-referencing
the header block: measured identical across the token guard, the object-identity defect and the naive
clear, therefore a contract pin and not validation. A later reader must not be able to mistake them for
proof. This is a locked requirement from CONTEXT § "Corrections to earlier decisions" and
VALIDATION.md § "Non-discriminating tests"; the file is where the label has to live.

Each discriminating case must carry an inline comment naming WHICH mutant it catches and what that
mutant would return: M-05-1 (`if (slot?.token === token)` becomes `if (slot?.bridge === bridge)`)
returns `null` for O1b, O2b, O4b and O4c; M-05-2 (the guarded clear becomes an unconditional
`slot = null;`) returns `null` for its eight.

Assert identity with `toBe`, never `toEqual` — the claim is reference identity of the registered
object, and `toEqual` would pass on a structurally identical clone, which is exactly the defect
BRG-02 exists to catch. For the "same object registered twice" orderings (O2b, O4c) construct ONE
bridge object and register that same reference twice; constructing two structurally identical objects
would silently turn them into the distinct-object case and destroy the discrimination.

Every case constructs its own registry via `createBridge("results")` — the factory IS the reset, so no
`__resetForTest` is needed and none must be added; adding one would also break the export count.
  </action>

  <verify>
    <automated>pnpm build</automated>
    <automated>pnpm test -- bridge</automated>
    <automated>pnpm test</automated>
    <automated>bash -c 'f=packages/concierge/test/bridge.test.ts; for o in O1b O2b O4b O4c O1 O2 O3 O3b O4 O5 O6 O7 O8; do grep -qF "$o " $f || { echo "missing ordering $o"; exit 1; }; done; echo "all 13 orderings present: OK"'</automated>
    <automated>bash -c 'f=packages/concierge/test/bridge.test.ts; n=$(grep -c "CONTRACT PIN" $f); echo "contract-pin labels: $n"; [ "$n" -ge 5 ]'</automated>
    <automated>bash -c 'f=packages/concierge/test/bridge.test.ts; grep -q "describe(\"BRG-01" $f &amp;&amp; grep -q "describe(\"BRG-04" $f &amp;&amp; echo "requirement describes: OK"'</automated>
  </verify>

  <done>
All thirteen orderings are asserted, the five non-discriminating ones are labelled as contract pins in
place, and each discriminating case names the mutant it catches. The whole suite is green.
  </done>

  <acceptance_criteria>
    - `pnpm test` passes the whole suite.
    - All thirteen ordering ids (O1, O1b, O2, O2b, O3, O3b, O4, O4b, O4c, O5, O6, O7, O8) appear in `it` titles.
    - At least five inline `CONTRACT PIN` labels exist, on exactly the O1, O3, O5, O6 and O7 cases.
    - The four discriminating cases O1b, O2b, O4b, O4c each name mutant **M-05-1** in a comment and state that the object-identity guard returns `null` where the token guard returns the bridge.
    - O2b and O4c register the **same object reference** twice. Assert: each of those two cases contains exactly one bridge-object construction and two `register(` calls on the same identifier. Two separate constructions would silently convert them to the distinct-object case and destroy the discrimination — this is Pitfall 1.
    - Identity assertions use `toBe`, not `toEqual`: `grep -c "toEqual" ` over the two BRG describes returns 0.
    - `describe` titles are requirement ids: the file contains `describe("BRG-01` and `describe("BRG-04`.
    - No `__resetForTest` is added to the registry or referenced by the tests.
    - Behavioural discrimination is proven, not assumed, in plan 05-07: M-05-1 and M-05-2 must each make `pnpm test -- bridge` exit non-zero, with the gate output confirming the build compiled and tests ran.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3 (05-04-T3): BRG-02 liveness, the frozen capability, and the two warn policies</name>

  <read_first>
    - `packages/concierge/test/bridge.test.ts` — the file as Tasks 1 and 2 left it.
    - `packages/concierge/test/concierge.test.ts` lines 1050-1165 — the console-capture idiom with its four load-bearing notes, and the warn-once assertion triad at 1140-1161.
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-PATTERNS.md` lines 759-824 — the console-capture idiom and the warn-once triad reproduced verbatim, including the secret-token fixture shape.
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-01-SUMMARY.md` — the exact rendered text of the `bridge_overwrite` warn message, so the assertion matches what shipped.
  </read_first>

  <files>packages/concierge/test/bridge.test.ts</files>

  <action>
Add three more `describe` blocks.

BLOCK 1 — `describe("BRG-02 — a handler reads live app state through snapshot getters", …)`:
  - A case asserting `registry.read()` is reference-identical (`toBe`) to the exact object passed to
    `register()`. `register()` stores the bridge AS GIVEN and `read()` returns it untouched; a
    normalizer applied at either point would return a detached copy here and fail this assertion. This
    is the direct detector for the "normalize at register()/read()" defect and for mutant M-05-9's
    register-time spelling.
  - A case that registers a bridge whose `snapshot` getter reads a mutable closure variable, reads
    through `registry.read()`, moves the app by writing the closure variable, then reads again through
    the SAME `read()` result with no re-registration in between, and asserts the second read returns
    the new value. This is success criterion 1. Assert both reads explicitly — the before value as
    well as the after — so a getter that was always returning the new value cannot pass vacuously.

BLOCK 2 — `describe("SEC-03 class — the returned registry is a frozen capability object", …)`:
  - A case asserting `Object.isFrozen(registry)` is `true`.
  - A case asserting that assigning to `registry.read` throws. These test files are in NO TypeScript
    program (see `vitest.config.ts` and the file header), and ESM modules are strict-mode, so the
    assignment throws `TypeError` rather than failing silently — measured. Use
    `expect(() => { registry.read = someOtherFn; }).toThrow()`. Add a second assertion that adding a
    new property (`registry.extra = 1`) also throws. This is mutant **M-05-7**'s detector
    (`return Object.freeze(registry);` becomes `return registry;`).
  - A case asserting the freeze does NOT freeze the closure: after `Object.freeze`, `register()` still
    mutates `slot` normally, so `read()` returns the newly registered bridge. This is obvious and
    worth asserting anyway, because "we froze it so it must be immutable" is exactly the reasoning
    `catalog.ts:601-607` calls "a breach that reports success".

BLOCK 3 — `describe("the two warn policies", …)`, using the console-capture idiom:
  - **register-over-live warns exactly once per registry.** Register three times over a live
    registration on one registry and assert the triad from `concierge.test.ts:1140-1161`:
    `expect(captured).toHaveLength(1)` — never `toBeGreaterThan(0)`, because the claim is that the
    latch holds; `expect(captured[0]).toContain(<the registry id>)` — proving the warning carried the
    registry's identity rather than an aggregated summary line; and a negative assertion that it did
    not carry anything from the app. Match the message against the exact rendered text recorded in
    `05-01-SUMMARY.md`, at minimum asserting it contains `[bridge_overwrite]`. This is mutant
    **M-05-10**'s detector (delete the latch assignment and this becomes 3).
  - **a refused unsubscriber warns never.** Run O2b under console capture and assert
    `expect(captured).toHaveLength(0)`. This is mutant **M-05-11**'s detector (add a `warnHost` to the
    refusal path and this becomes 1). Comment that React StrictMode double-mount, Vue HMR and Svelte
    remount all produce refused cleanups by design, so a warn here fires on every dev mount.
  - **no warn on the ordinary first registration.** Register once on a fresh registry under capture
    and assert zero warnings, so the overwrite warn is proven to be conditional rather than
    unconditional.

CONSOLE CAPTURE — copy the exact idiom, all four notes included. Plain global assignment, real console
SPREAD rather than replaced wholesale, restoration in a `finally` and never after the assertions, and
the note that no cast ceremony is needed because this file is in no TypeScript program. Never the
Vitest mocking API; the grep over `test/` must stay at zero.

For the negative-content assertion, use the house secret-token fixture shape: have the displaced
component's bridge carry a marker string such as `SECRET-FROM-THE-APP` somewhere the warn could echo
it if the implementation were careless, and assert `expect(captured[0]).not.toContain("SECRET-FROM-THE-APP")`.
That assertion is the executable form of the security decision; without it the `catch`-with-no-binding
is a convention with no guarantee.
  </action>

  <verify>
    <automated>pnpm build</automated>
    <automated>pnpm test -- bridge</automated>
    <automated>pnpm test</automated>
    <automated>pnpm typecheck</automated>
    <automated>bash -c 'f=packages/concierge/test/bridge.test.ts; grep -qF "Object.isFrozen" $f &amp;&amp; grep -qF "[bridge_overwrite]" $f &amp;&amp; grep -qF "toHaveLength(1)" $f &amp;&amp; grep -qF "toHaveLength(0)" $f &amp;&amp; grep -qF "SECRET-FROM-THE-APP" $f &amp;&amp; echo "warn + freeze assertions: OK"'</automated>
    <automated>bash -c 'f=packages/concierge/test/bridge.test.ts; grep -qF "globalThis.console = { ...realConsole" $f &amp;&amp; grep -qF "} finally {" $f &amp;&amp; ! grep -qE "vi\.(spyOn|fn|mock)" $f &amp;&amp; echo "console capture idiom: OK"'</automated>
  </verify>

  <done>
BRG-02's two claims, the frozen-capability guarantee, and both warn policies are asserted. The whole
suite is green and `pnpm typecheck` passes.
  </done>

  <acceptance_criteria>
    - `pnpm test` passes the whole suite; `pnpm typecheck` exits 0.
    - `registry.read()` reference identity is asserted with `toBe` against the exact object passed to `register()`.
    - The liveness case asserts BOTH the before value and the after value across a single registration, with no `register` call between them.
    - `Object.isFrozen(registry)` is asserted `true`, and both `registry.read = …` and `registry.extra = 1` are asserted to throw. These are mutant **M-05-7**'s detectors.
    - A case asserts that `register()` still works after the freeze, so the freeze is proven not to have sealed the closure.
    - The register-over-live case asserts `toHaveLength(1)` — never `toBeGreaterThan(0)` — plus a `toContain` on the registry id and a `not.toContain` on the app-supplied secret marker. Mutant **M-05-10**.
    - The refused-unsubscriber case asserts `toHaveLength(0)` under console capture. Mutant **M-05-11**.
    - A first-registration case asserts zero warnings, proving the overwrite warn is conditional.
    - The console-capture block spreads the real console, restores in a `finally`, and uses no Vitest mocking API. A repo-wide grep for `vi.spyOn`, `vi.fn`, `vi.mock` over `packages/concierge/test/` still returns zero.
    - `git diff --exit-code` at the repo root is clean after commit.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer page component → `registry.register(bridge)` | Consumer-authored objects; registration order and unmount timing are outside core's control |
| third-party page script → the returned registry object | Same realm, same global. The registry object IS the capability |
| core → `warnHost` (host `console`) | Outbound diagnostic channel; a warn that echoed app data would be a PII channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | the registry object returned by `createBridge` | mitigate | Block 2 asserts `Object.isFrozen(registry)` and that both `registry.read = fn` and `registry.extra = 1` throw. Mutant M-05-7 |
| T-05-13 | Tampering | a stale unsubscriber clearing the live registration, so every later handler sees `bridge: null` on a mounted page | mitigate | O1b, O2b, O4b and O4c under BRG-04, each asserting the token guard's result against the object-guard's `null`. Mutant M-05-1 |
| T-05-14 | Denial of service | an unconditional clear removing a registration that never asked to be removed | mitigate | The eight naive-discriminating orderings. Mutant M-05-2 |
| T-05-05 | Information disclosure | the overwrite warn echoing app-supplied data | mitigate | The `not.toContain("SECRET-FROM-THE-APP")` assertion, which is the executable form of the catch-binds-nothing rule |
| T-05-15 | Denial of service (developer attention) | a warn on every StrictMode dev mount training developers to ignore the channel | mitigate | The refused-unsubscriber case asserts zero warnings. Mutant M-05-11 |
| T-05-SC | Tampering | npm/pnpm package installs | accept | Zero installs. `pnpm check:deps` asserts the empty external module graph. No new test dependency: Vitest 4.1.10 is already present |
</threat_model>

<verification>
1. `pnpm build && pnpm test && pnpm typecheck` — all green.
2. All thirteen orderings present; five labelled `CONTRACT PIN`; four naming M-05-1.
3. Zero Vitest mocking API hits across `packages/concierge/test/`.
4. Nothing added to `packages/concierge/test/fixtures/`.
5. `git diff --exit-code` clean at the repo root after commit.
</verification>

<success_criteria>
- BRG-01, BRG-02 and BRG-04 each have at least one case that a named mutant makes fail.
- The five non-discriminating orderings are present AND labelled, so they are never counted as proof.
- The frozen-capability guarantee and both warn policies are asserted against the built artifact.
</success_criteria>

<output>
Create `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-04-SUMMARY.md` when done.

The SUMMARY must record:
- The case-id-to-ordering map (`B1` is O6, and so on), so plan 05-07 can name which cases a mutant
  turned red.
- The new total test count for the suite.
- The exact warn text captured in the register-over-live case, compared against
  `05-01-SUMMARY.md`'s recorded rendering.
</output>
