---
phase: 10-close-v0-1-release-certification-and-evidence-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/concierge/src/concierge.ts
  - packages/concierge/src/dispatch.ts
  - packages/concierge/src/session.ts
  - packages/concierge/test/dispatcher-batch.test.ts
  - packages/concierge/test/session-consent.test.ts
  - packages/concierge/test/session-lifecycle.test.ts
  - packages/concierge/test-d/actions.test-d.ts
  - packages/concierge/test-d/dispatcher.test-d.ts
autonomous: true
requirements: [DSP-07, SES-02, SES-04, CON-10]
must_haves:
  truths:
    - "Once a terminal handler is entered, success, returned failure, synchronous throw, and asynchronous rejection all commit the terminal boundary per D-10-01/D-10-02 and Audit 3."
    - "A terminal occurrence exposes no row from its batch to direct callers or the transport, and no later call or queued occurrence enters application code per D-10-03 and T-10-01/T-10-02."
    - "A terminal failure reaches the existing immutable app-authored outcome sink before stopped state, while stop is initiated without awaiting the active pump's own drain per D-10-04 and T-10-03."
    - "Direct dispatch Promise identity, retry identity, nonterminal row cardinality/order, ordinary response behavior, and cached stop Promise identity remain unchanged."
    - "No terminal control bit enters ActionResult, a public batch row, Concierge keys, transport response data, or any other agent-visible object."
  artifacts:
    - path: "packages/concierge/src/concierge.ts"
      provides: "Private per-dispatch terminal-entry state and internal batch/session bridge"
      contains: "terminalEntered"
    - path: "packages/concierge/src/dispatch.ts"
      provides: "Frozen internal batch outcome and serial break after the entered terminal handler settles"
      contains: "InternalBatchOutcome"
    - path: "packages/concierge/src/session.ts"
      provides: "Outcome-before-stop terminal branch with zero response attempts"
      contains: "stopNow"
    - path: "packages/concierge/test/dispatcher-batch.test.ts"
      provides: "Terminal entry, serial break, whole-batch silence, and dedup regressions"
      contains: "Q20"
    - path: "packages/concierge/test/session-lifecycle.test.ts"
      provides: "Terminal drain, queue cancellation, and no-self-deadlock regressions"
      contains: "L06"
  key_links:
    - from: "packages/concierge/src/concierge.ts"
      to: "packages/concierge/src/dispatch.ts"
      via: "private dispatch execution state supplied to the serial batch executor"
      pattern: "executeDispatchBatch"
    - from: "packages/concierge/src/session.ts"
      to: "packages/concierge/src/concierge.ts"
      via: "module-private internal outcome lookup; public Concierge keys stay unchanged"
      pattern: "dispatchBatch.*Outcome"
    - from: "packages/concierge/src/session.ts"
      to: "Transport.respond"
      via: "terminal branch returns before the existing response loop"
      pattern: "terminalEntered"
---

<objective>
Implement the already-public terminal-action promise across the dispatcher and Session, closing Audit 3 and D-10-01 through D-10-04 without widening any agent-visible contract.

Purpose: A terminal effect must end the authority loop at handler entry, suppress the whole occurrence, preserve app-authored failure presentation, and avoid deadlocking Session teardown.
Output: Private terminal execution state, an internal frozen batch outcome, Session teardown wiring, and focused runtime/type regressions.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v0.1-MILESTONE-AUDIT.md
@.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-CONTEXT.md
@.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-RESEARCH.md
@.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-PATTERNS.md
@.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-VALIDATION.md
@.planning/phases/06-dispatcher/06-CONTEXT.md
@.planning/phases/07-session-and-the-transport-seam/07-CONTEXT.md
@.planning/phases/08-consent-kernel/08-CONTEXT.md

<interfaces>
From packages/concierge/src/types.ts:
- `ActionDefinition.terminal?: boolean` promises Session teardown, batch short-circuit, and no result envelope.
- `Concierge.dispatch` must remain a non-async function returning the exact cached `Promise<ActionResult>`.
- `Concierge.dispatchBatch` must retain `Promise<ReadonlyArray<Readonly<{ callId: string; result: ActionResult }>>>`.

From packages/concierge/src/dispatch.ts:
- `executeDispatchBatch(ctx, batch, dispatch)` currently owns copied stable ordering, serial entry, abort synthesis, and frozen correlated rows.

From packages/concierge/src/session.ts:
- `runWork()` currently awaits the complete batch, derives `FailureOutcome`, awaits `presentOutcome`, then calls `transport.respond`.
- `stopNow()` calls `enterStopped()`, `performCleanup()`, and `startStopDrain()`; `startStopDrain()` awaits the active pump, so `runWork()` must never await `stopNow()`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 10-01-01: Pin the private terminal boundary and all response-silence invariants</name>
  <files>packages/concierge/test/dispatcher-batch.test.ts, packages/concierge/test/session-consent.test.ts, packages/concierge/test/session-lifecycle.test.ts, packages/concierge/test-d/actions.test-d.ts, packages/concierge/test-d/dispatcher.test-d.ts</files>
  <read_first>
    <file>packages/concierge/test/dispatcher-batch.test.ts</file>
    <file>packages/concierge/test/session-consent.test.ts</file>
    <file>packages/concierge/test/session-lifecycle.test.ts</file>
    <file>packages/concierge/test-d/actions.test-d.ts</file>
    <file>packages/concierge/test-d/dispatcher.test-d.ts</file>
    <file>packages/concierge/src/types.ts</file>
    <file>packages/concierge/src/concierge.ts</file>
    <file>packages/concierge/src/dispatch.ts</file>
    <file>packages/concierge/src/session.ts</file>
    <file>.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-CONTEXT.md</file>
    <file>.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-RESEARCH.md</file>
    <file>.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-PATTERNS.md</file>
  </read_first>
  <behavior>
    - "Q20-Q26: terminal success/failure/throw/reject mark before invocation, settle the entered call, execute no later handler, and project the entire public batch to frozen `[]`; pre-entry validation/consent/abort failures do not stop."
    - "S08-S10: Session performs zero response attempts for a terminal occurrence; returned failure awaits the existing immutable app outcome; a throwing/interrupted outcome sink still reaches stopped state."
    - "L06-L09: queued later occurrences never enter, cleanup happens once, the cached stop Promise keeps reference identity and eventually resolves, and the active pump never awaits itself."
    - "Type guards: `terminal?: boolean` remains accepted while ActionResult, the public correlated row, and `keyof Concierge` gain no terminal-control member."
  </behavior>
  <action>
Trace D-10-01, D-10-02, D-10-03, and D-10-04 explicitly: terminality commits at handler entry across every settlement mode, suppresses the entire public batch, and records the app-owned outcome before nonblocking teardown.
Add the named built-artifact tests before production changes. Reuse the existing action helper with `{ terminal: true }`, Q03/Q11-Q13 serial sentinels, S06/S07 outcome ordering, and L01/L03/L04 stop/queue fixtures. Cover handler success, `{ok:false}`, synchronous throw, rejecting thenable, an earlier nonterminal effect followed by terminal entry, a later call, a later queued occurrence, pre-entry invalid arguments, consent refusal, missing handler, and pre-entry abort. Assert exact event order, zero `respond()` calls for the whole occurrence, direct public `dispatchBatch()` equal to a frozen empty array, no later handler entry, one cleanup, eventual stop settlement, and unchanged direct dispatch/dedup Promise identity. Extend type tests bidirectionally so `ActionDefinition.terminal` remains boolean/optional and neither `ActionResult`, a public batch row, nor `Concierge` exposes `terminal`, `terminalEntered`, or an internal outcome. The RED run must fail only the new terminal runtime cases; existing nonterminal and type cases remain green.
  </action>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge build &amp;&amp; ! node_modules/.bin/vitest run packages/concierge/test/dispatcher-batch.test.ts packages/concierge/test/session-consent.test.ts packages/concierge/test/session-lifecycle.test.ts -t 'Q2[0-6]|S0[8-9]|S10|L0[6-9]'</automated>
    <automated>pnpm --filter @fullselfbrowsing/concierge typecheck</automated>
  </verify>
  <acceptance_criteria>
    - "Q20-Q26, S08-S10, and L06-L09 exist as active named tests; the focused RED command exits nonzero before implementation and reports at least one of those exact IDs."
    - "At least one test observes `terminalEntered` before hostile handler code runs, and separate tests cover success, returned failure, synchronous throw, and asynchronous rejection."
    - "Tests assert exact `[]` public rows and zero Session response attempts for a batch where an earlier call already completed, while the earlier application effect count remains one."
    - "Tests assert no later batch handler and no later queued occurrence handler enters after terminal entry (T-10-02 HIGH)."
    - "A deferred failure outcome proves outcome completion precedes cleanup; a rejected outcome proves cleanup still occurs; both stop Promises resolve within the test's bounded wait (T-10-03 HIGH)."
    - "Typecheck exits 0 and exact-key predicates reject any public terminal-control field (T-10-01 HIGH)."
  </acceptance_criteria>
  <done>The full terminal matrix is pinned RED without weakening existing nonterminal, Promise-identity, public-shape, cancellation, or outcome contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 10-01-02: Implement internal terminal execution and nonblocking Session stop</name>
  <files>packages/concierge/src/concierge.ts, packages/concierge/src/dispatch.ts, packages/concierge/src/session.ts</files>
  <read_first>
    <file>packages/concierge/src/concierge.ts</file>
    <file>packages/concierge/src/dispatch.ts</file>
    <file>packages/concierge/src/session.ts</file>
    <file>packages/concierge/src/types.ts</file>
    <file>packages/concierge/test/dispatcher-batch.test.ts</file>
    <file>packages/concierge/test/session-consent.test.ts</file>
    <file>packages/concierge/test/session-lifecycle.test.ts</file>
    <file>packages/concierge/test-d/actions.test-d.ts</file>
    <file>packages/concierge/test-d/dispatcher.test-d.ts</file>
    <file>.planning/phases/06-dispatcher/06-CONTEXT.md</file>
    <file>.planning/phases/07-session-and-the-transport-seam/07-CONTEXT.md</file>
    <file>.planning/phases/08-consent-kernel/08-CONTEXT.md</file>
    <file>.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-CONTEXT.md</file>
  </read_first>
  <behavior>
    - "Handler entry, not result settlement, sets private terminal state."
    - "The serial executor retains completed internal rows, stops after the entered terminal call settles, and exposes only a frozen internal outcome."
    - "Public dispatchBatch projects a terminal outcome to frozen `[]`; Session consumes internal rows only for app-outcome derivation and never responds."
    - "Session starts stop after the app-outcome barrier without awaiting its active pump's drain."
  </behavior>
  <action>
Implement D-10-01, D-10-02, D-10-03, and D-10-04 through private runtime state only; none of these control facts may enter an agent-visible contract.
Implement a module-private `DispatchExecutionState` with `terminalEntered` and associate it synchronously with every dispatch Promise, including cache hits, without making public `dispatch()` async or replacing the cached Promise. Pass that state into `runDispatchPipeline()` and set it immediately before the existing `handler({...})` call only when `entry.action.terminal === true`; every validation, authorization, consent, abort, missing-handler, and argument guard remains before the marker. Change the serial batch implementation to return a frozen module-private `InternalBatchOutcome` containing frozen completed rows and `terminalEntered`; after awaiting a call whose state is terminal, break before the next handler. Keep a private WeakMap-backed internal batch function for Session, and make public `Concierge.dispatchBatch()` return the existing rows for nonterminal work or one shared frozen empty array for terminal work. Do not add a public key, public row field, `ActionResult` field, barrel export, transport field, or agent-visible diagnostic.

In `runWork()`, obtain the internal outcome, derive the existing `FailureOutcome` from its internal rows, and preserve the existing immutable `presentOutcome` barrier. For a terminal outcome, use a `finally`-equivalent branch so `void stopNow()` runs after outcome settlement or presentation failure, then return before `transport.respond`. Do not await `stopNow()` from the active pump. Rely on the single FIFO pump to hold queued occurrences during the barrier and the existing stop detach/abort path to drain them. Preserve all ordinary nonterminal response, cancellation, catalog-transition, diagnostic, and cleanup behavior.
  </action>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge build &amp;&amp; node_modules/.bin/vitest run packages/concierge/test/dispatcher-batch.test.ts packages/concierge/test/session-consent.test.ts packages/concierge/test/session-lifecycle.test.ts &amp;&amp; pnpm --filter @fullselfbrowsing/concierge typecheck</automated>
    <automated>node_modules/.bin/vitest run packages/concierge/test/dispatcher.test.ts packages/concierge/test/session-routing.test.ts packages/concierge/test/session-catalog.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - "The marker assignment appears immediately before handler invocation and cannot execute on pre-entry validation, consent, missing-handler, or abort failures (D-10-02; T-10-02 HIGH)."
    - "Terminal success, failure, throw, and reject all settle the entered call, produce zero public/transport rows, and prevent every later handler; Q20-Q26 and S08-S10 pass (D-10-03; T-10-01/T-10-02 HIGH)."
    - "Terminal failure awaits the existing app-authored immutable outcome, then enters stopped state even if presentation rejects; no agent response contains the failure (D-10-04; T-10-01 HIGH)."
    - "L06-L09 prove `stop()` identity, eventual resolution, one cleanup, queued-work cancellation, and no self-await deadlock (T-10-03 HIGH)."
    - "Existing nonterminal batch order/cardinality, direct dispatch Promise identity, dedup reuse, abort synthesis, and ordinary Session response suites pass unchanged."
    - "`keyof Concierge`, ActionResult, public batch rows, package exports, and built declarations contain no internal terminal-control member."
  </acceptance_criteria>
  <done>Terminal entry is a private, fail-closed control boundary that silences the whole occurrence, preserves app-owned outcome ordering, and stops Session without deadlock.</done>
</task>

</tasks>

<source_coverage_audit>
| Source | Item | Status | Task |
|---|---|---|---|
| GOAL | Close terminal-action release gap | COVERED | 10-01-01/02 |
| AUDIT | Audit 3 / B4 terminal contract | COVERED | 10-01-01/02 |
| CONTEXT | D-10-01 through D-10-04 | COVERED | 10-01-01/02 |
| RESEARCH | Private outcome, entry commitment, outcome-before-stop, no self-await | COVERED | 10-01-01/02 |
| SECURITY | T-10-01, T-10-02, T-10-03 | COVERED | 10-01-01/02 |
| CONTEXT | D-10-10 publication and deferred product scope | EXCLUDED | No registry/new adapter/transport work appears |
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| handler → core execution state | Hostile application code crosses the terminal commitment point. |
| core batch → agent transport | Private terminal results must never become agent-visible rows. |
| outcome sink → Session teardown | Outside presentation may delay, throw, reject, or reenter before stop. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-01 | Information Disclosure / Elevation of Privilege | HIGH | `dispatchBatch` and `runWork` response boundary | mitigate | Branch on private terminal outcome before any public projection/response; exact empty-array and zero-response tests plus leakage mutant in Plan 05. |
| T-10-02 | Tampering | HIGH | serial batch and FIFO occurrence admission | mitigate | Mark before handler entry, settle current call, break serial loop, hold queued work in the single pump, then stop/drain; success/failure/throw/reject/queued tests. |
| T-10-03 | Denial of Service | HIGH | `stopNow()` / active pump drain | mitigate | Initiate cached stop without awaiting it inside `runWork`; assert Promise identity and bounded eventual settlement. |
</threat_model>

<verification>
Run the quick command from `10-VALIDATION.md`, then require Q20-Q26, S08-S10, L06-L09, all existing focused suites, and core typecheck to pass. Plan 05 supplies the named mutation discrimination before evidence closure.
</verification>

<success_criteria>
- Audit 3 and D-10-01 through D-10-04 are observable on built bytes.
- T-10-01/T-10-02/T-10-03 HIGH mitigations have focused automated proofs.
- Public API/result/transport shapes and direct Promise identity remain byte-compatible.
- No later handler or occurrence enters after terminal entry, and no terminal occurrence reaches the agent.
</success_criteria>

<output>
Create `.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-01-SUMMARY.md` when done.
</output>
