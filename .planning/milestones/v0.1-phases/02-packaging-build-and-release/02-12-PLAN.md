---
phase: 02-packaging-build-and-release
plan: 12
type: execute
wave: 8
depends_on: ["02-10", "02-11"]
files_modified:
  - .planning/phases/02-packaging-build-and-release/02-VALIDATION.md
autonomous: true
requirements: [PKG-01, PKG-02, PKG-03, PKG-04, PKG-05]
user_setup: []

must_haves:
  truths:
    - "The full suite is green from a clean checkout, not only from an incrementally-built tree"
    - "All eleven mutants have been observed failing their named gate in one consecutive run"
    - "The tree is clean after every mutant and at the end of the phase"
    - "The packed tarball's file list has been reviewed against the sourcemap decision"
    - "The validation strategy records what was proven, what is manual-only, and what is vacuous"
  artifacts:
    - path: .planning/phases/02-packaging-build-and-release/02-VALIDATION.md
      provides: "Signed-off validation contract with per-task statuses and the mutant battery result"
      contains: "nyquist_compliant: true"
  key_links:
    - from: .planning/phases/02-packaging-build-and-release/02-VALIDATION.md
      to: .planning/phases/02-packaging-build-and-release
      via: "per-task verification map rows matching real task IDs"
      pattern: "^\\| 02-0"
---

<objective>
The phase gate. Run everything from a clean checkout, re-run the eleven-mutant battery end to end,
review the tarball by eye once, and sign off the validation contract.

Purpose: `02-VALIDATION.md` opens with the sentence this plan exists to honour — *"every gate in
this phase is a structural claim enforced by a shell exit code, and shell exit codes fail silently
green far more readily than type assertions. A green suite here is not evidence of a working
suite."* Each mutant was proven once, in the plan that built its gate, on a tree that had whatever
state that plan left. The battery is run once more, consecutively, on the finished phase, because
a gate that fired in wave 3 may have been weakened by a change in wave 7 and nothing else would
notice.
Output: a recorded clean-checkout run, eleven recorded mutant observations, a reviewed tarball file
list, and `02-VALIDATION.md` signed off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-packaging-build-and-release/02-VALIDATION.md
@package.json
@packages/concierge/package.json
@scripts/mutate-and-prove.sh

Read only these ranges of `.planning/phases/02-packaging-build-and-release/02-RESEARCH.md`:
- lines 993–1022 — the Suite Adequacy Requirement, the eleven-mutant table with measured
  signatures, and the Sampling Rate section's phase-gate list
- lines 759–770 — the sourcemap decision the tarball review is against

Every mutant's exact pattern, replacement and gate is specified in the plan that first proved it:
P1/P2/P3/P4 → `02-04`; P5 → `02-05`; P6/P7/P11 → `02-07`; P10 → `02-09`; P8/P9 → `02-11`.
Read those plans' `<action>` blocks for the command lines rather than re-deriving them, and read
their SUMMARYs for the signatures observed the first time.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean-checkout green, the Node floor on a real v22.12.0, and the tarball reviewed by eye</name>
  <files>.planning/phases/02-packaging-build-and-release/02-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/02-packaging-build-and-release/02-VALIDATION.md` § "Sampling Rate" — the five-item phase-gate list this task executes
    - `.planning/phases/02-packaging-build-and-release/02-03-SUMMARY.md` — the tarball file list recorded when the sourcemap decision was taken; this run's list is compared against it
    - `package.json` (root) — the full script set
  </read_first>
  <action>
    Run the phase gate's first, second, fourth and fifth items. Do not skip the clean part: an
    incrementally-built tree hides a missing build step, a stale `dist/`, and a lockfile that only
    resolves because something is already installed.

    **1. Clean checkout, full suite.** Remove `packages/concierge/dist/` and every `node_modules`
    directory in the workspace, then run, in order and recording each exit code:
    `pnpm install --frozen-lockfile` → `pnpm typecheck` → `pnpm build` → `pnpm test` →
    `pnpm run check:artifact` → `pnpm run check:deps` → `pnpm run check:pack`.
    All seven must exit 0. `--frozen-lockfile` is required: it is what proves the committed lockfile
    is the one CI will install from.

    **2. The Node floor on a real v22.12.0.** Run `pnpm run check:node-floor` and record the printed
    runtime version alongside the developer's own `node --version`. The contrast is the criterion:
    *"not merely on the developer's newer runtime"*.

    **4. `git status --porcelain` empty.** Run it and confirm no output. If a `*.tgz` appears, the
    `.gitignore` line from plan 02-01 is missing or a script packed without `--pack-destination`;
    fix the cause, not the symptom.

    **5. Review the packed tarball's file list by eye, once.** This is a named manual review, not an
    automated assertion. Pack to a temp directory, `tar -tzf` it, and compare the list against the
    one plan 02-03 recorded when the sourcemap decision was taken. It must differ from that list by
    **exactly one entry — `package/src/contract.ts`**, added by plan 02-06 after 02-03 took the
    sourcemap decision. That is the expected delta; anything else is drift. Confirm: `dist/` (four
    files), `src/` (**all three** files — `index.ts`, `types.ts`, `contract.ts`), `README.md`,
    `LICENSE`, `package.json` — and nothing from `test/`,
    `test-d/`, `scripts/`, `.github/`, `.changeset/` or `node_modules/`. Confirm the declaration map
    resolves: `dist/index.d.ts.map`'s `sources` entry points at a path that is present in the
    tarball. Record the full list and the byte size in the SUMMARY, and state explicitly that the
    tarball publishes `src/types.ts` verbatim as a deliberate decision.

    Record every command and exit code in the SUMMARY. If anything is red, stop and fix it in the
    plan that owns it rather than patching it here.
  </action>
  <verify>
    <automated>rm -rf packages/concierge/dist node_modules packages/concierge/node_modules packages/concierge/test/fixtures/*/node_modules && pnpm install --frozen-lockfile && pnpm typecheck && pnpm build && pnpm test && pnpm run check:artifact && pnpm run check:deps && pnpm run check:pack && pnpm run check:node-floor && test -z "$(git status --porcelain)" && echo PHASE_GATE_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - All of `pnpm install --frozen-lockfile`, `pnpm typecheck`, `pnpm build`, `pnpm test`, `pnpm run check:artifact`, `pnpm run check:deps`, `pnpm run check:pack` exit 0 after a full clean
    - `pnpm run check:node-floor` exits 0 and its output shows `v22.12.0`
    - The SUMMARY records the developer's own `node --version` beside the floor version
    - `git status --porcelain` produces no output
    - The tarball file list is recorded in the SUMMARY and contains `package/dist/` (four files), `package/src/index.ts`, `package/src/types.ts`, `package/src/contract.ts`, `package/README.md`, `package/LICENSE`, `package/package.json`
    - The list differs from plan 02-03's recorded list by exactly one entry, `package/src/contract.ts`, and the SUMMARY says so
    - The tarball contains no path matching `test`, `test-d`, `scripts`, `.github`, `.changeset` or `node_modules`
    - The `sources` entry of `dist/index.d.ts.map` names a file that is present in the tarball
    - The SUMMARY states explicitly that `src/types.ts` is published verbatim as a deliberate decision taken in plan 02-03
    - Every command's exit code is recorded in the SUMMARY
  </acceptance_criteria>
  <done>The whole phase is green from nothing, on the runtime the package promises, with a tarball whose contents have been looked at rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the eleven-mutant battery end to end</name>
  <files>packages/concierge/package.json, packages/concierge/src/types.ts, packages/concierge/src/index.ts, packages/concierge/src/contract.ts, packages/concierge/test/fixtures/probe.ts</files>
  <read_first>
    - `02-VALIDATION.md` § "Suite Adequacy Requirement" — the eleven-mutant table with measured signatures
    - The `<action>` blocks of plans `02-04`, `02-05`, `02-07`, `02-09` and `02-11` — the exact pattern, replacement and gate for each mutant
    - The SUMMARYs of those five plans — the signatures observed the first time, which this run must reproduce
    - `scripts/mutate-and-prove.sh` — the harness
  </read_first>
  <action>
    Run all eleven mutants consecutively, on the finished phase, in ascending order P1 → P11. Each
    goes through `scripts/mutate-and-prove.sh` — applied and restored inside one invocation — and
    after each one assert `git diff --exit-code` at the repo root.

    Use the pattern, replacement and gate recorded in the owning plan. Do not re-derive them and do
    not "improve" them; if a pattern no longer matches, the harness aborts with exit 3 (`mutation
    was a no-op`) and that is a finding — a gate whose target moved is a gate that stopped guarding.
    Record it and fix the pattern in the owning plan's terms rather than deleting the mutant.

    | # | Owner | Gate | Signature that must be reproduced |
    |---|---|---|---|
    | P1 | 02-04 | `pnpm build` | `ERROR [publint] … file does not exist` **and** `ERROR [attw]`, exit 1 |
    | P2 | 02-04 | `pnpm run check:artifact` | publint errors on the **packed tarball** (measured: 4), exit 1 |
    | P3a | 02-04 | `publint --strict` | measured: 2 errors, exit non-zero |
    | P3b | 02-04 | `attw --pack … --profile esm-only` | exit 1 |
    | P4 | 02-04 | `pnpm build; echo BUILD_EXIT=$?; pnpm typecheck` | `BUILD_EXIT=0` **and** typecheck non-zero with TS2322 — **non-skippable** |
    | P5a | 02-05 | `check:deps` on a synthesized entry | exit 1, non-empty vendored list **and** non-empty external list |
    | P5b | 02-05 | `pnpm run check:deps` | names the added dependency with a byte size > 0, exit 1 |
    | P6 | 02-07 | `pnpm build && pnpm test -- single-instance` | **F1b is among the failing assertions**: the calling consumer bundle no longer contains the registry key. F2 failing alongside it is expected (module-scope registration throws during module evaluation, so the dynamic import rejects first) — **non-skippable** |
    | P7 | 02-07 | `pnpm build && pnpm test -- single-instance` | **F2** fails: no error thrown |
    | P8 | 02-11 | `pnpm build; echo BUILD_EXIT=$?; pnpm test -- artifact; echo ARTIFACT_EXIT=$?; pnpm typecheck` | `BUILD_EXIT=0`, non-zero `ARTIFACT_EXIT`, TS1485 in `exports.test-d.ts`, `results.test-d.ts` not named |
    | P9 | 02-11 | `pnpm typecheck` | TS2344 on a line whose echoed source contains `_policyNotBivariant`, plus TS2578 on `_policyDegraded` |
    | P10 | 02-09 | `pnpm build && pnpm test -- single-instance; echo DEV_EXIT=$?; pnpm run check:node-floor` | `DEV_EXIT=0` **and** the floor job non-zero — **non-skippable** |
    | P11 | 02-07 | `pnpm build && pnpm test -- export-surface` | 44 names where 45 are expected |

    Two notes carried forward so they are not rediscovered as anomalies:
    - **P11's measured signature in `02-VALIDATION.md` reads "42 names instead of 43"**, because it
      was measured before `contract.ts` added two names. The invariant is the same −1 delta; the
      real numbers on the finished phase are 44 against 45.
    - **P4, P6 and P10 cannot be skipped.** Each proves a claim that is only structural and is
      invisible to every other check in the suite. If any of the three cannot be made to fire, the
      phase is not done — say so rather than recording ten of eleven.

    Any mutant that leaves `dist/` built from mutated source (P4, P6, P7, P8, P10, P11) requires
    `pnpm build` afterwards; the harness restores the source, not the build output. Finish the task
    with `pnpm build && pnpm typecheck && pnpm test && pnpm run check:deps` green and
    `git status --porcelain` empty.

    Record for every mutant: the harness command line, its stdout line, its exit code, and the
    reproduced signature. A mutation proof whose expected diagnostic is vague is not a proof.
  </action>
  <verify>
    <automated>pnpm build && bash scripts/mutate-and-prove.sh packages/concierge/src/types.ts 'export const MESSAGE_MAX_CHARS = 180;' 'export const MESSAGE_MAX_CHARS: 181 = 180;' -- bash -c 'pnpm build; echo BUILD_EXIT=$?; pnpm typecheck' 2>&1 | tee /tmp/gate-p4.log; grep -q "BUILD_EXIT=0" /tmp/gate-p4.log && grep -q "PASS: gate fired" /tmp/gate-p4.log && git diff --exit-code && pnpm build && pnpm test && echo P4_REPROVEN</automated>
  </verify>
  <acceptance_criteria>
    - All thirteen invocations (P1, P2, P3a, P3b, P4, P5a, P5b, P6, P7, P8, P9, P10, P11) were run consecutively on the finished phase
    - Every one produced a non-zero exit from its named gate — i.e. `PASS: gate fired` from the harness, or exit 1 from the P5a scratch probe
    - No invocation aborted with exit 2 (dirty target) or exit 3 (no-op pattern); if any did, it is recorded as a finding with the reason
    - `git diff --exit-code` exits 0 after **each** invocation, not only at the end
    - P4's output contains `BUILD_EXIT=0`; P8's contains `BUILD_EXIT=0` and a non-zero `ARTIFACT_EXIT`; P10's contains `DEV_EXIT=0`
    - F1b is among P6's failing assertions and F2 is P7's, each named in the SUMMARY; P6's expected second failure (F2, for the module-evaluation reason recorded in plan 02-07) is recorded rather than reported as an anomaly
    - P11's output reports 44 against 45, and the SUMMARY notes why `02-VALIDATION.md`'s recorded signature says 42/43
    - The SUMMARY states explicitly that P4, P6 and P10 were each run and each fired
    - After the task, `pnpm build && pnpm typecheck && pnpm test && pnpm run check:deps` all exit 0
    - `git status --porcelain` is empty
    - The SUMMARY records, per mutant, the command line, harness stdout, exit code, and reproduced signature
  </acceptance_criteria>
  <done>Every gate this phase built has been observed failing on the finished phase, consecutively, with the tree clean after each.</done>
</task>

<task type="auto">
  <name>Task 3: Sign off the validation contract</name>
  <files>.planning/phases/02-packaging-build-and-release/02-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/02-packaging-build-and-release/02-VALIDATION.md` — in full, including the per-task map plan 02-01 populated, the manual-only table, and the sign-off checklist
    - The SUMMARYs of plans 02-01 through 02-11 — the evidence each row's status is claimed from
    - This plan's own Task 1 and Task 2 results
  </read_first>
  <action>
    Close the validation contract with evidence, not intent.

    **1. Per-task statuses.** Set each row's Status column in § "Per-Task Verification Map" to ✅
    where that task's automated command was observed green in Task 1's clean-checkout run or in the
    owning plan's SUMMARY, and to ⚠️ or ❌ otherwise with a one-clause reason. Set every row's
    "File Exists" column to ✅ now that the files exist. Do not mark a row ✅ on the strength of the
    plan having been executed; mark it on the strength of a recorded command and exit code.

    **2. Record the battery result, and reconcile the one measurement two SUMMARYs both carry.**
    Add one line under § "Suite Adequacy Requirement" stating that all eleven mutants were re-run
    consecutively at the phase gate, naming the date and the fact that `git diff --exit-code` was
    clean after each. Do not edit the mutant table's measured signatures — except to append a
    parenthetical to P11's row noting that the finished-phase numbers are 44/45 because
    `contract.ts` added two export names after that signature was measured.

    Then **compare the tree-shaking pair recorded in the 02-06 and 02-07 SUMMARYs and assert they
    agree**: both must read *63 B uncalled / 587 B called — the registry code itself contributes 0
    bytes when uncalled*. Plan 02-07 additionally records the two byte lengths F1b actually emitted;
    note them beside the measured pair. This is the only place the two records meet, and it is why
    both plans mandate the identical wording — a "0 bytes uncalled" phrasing in one SUMMARY and
    "63 B uncalled" in the other is a contradiction in the phase's own evidence, not a rounding
    difference. If they disagree, record the disagreement rather than silently preferring one.

    **3. Keep the honest limits honest.** § "Manual-Only Verifications" must still contain exactly
    one row — the OIDC release workflow — and it must still say the workflow **cannot be executed in
    this phase**. Append the outcome of plan 02-10's static review (which of the six named
    properties were verified by inspection) and a pointer to `RELEASING.md`. Do not change its
    status to anything that reads as executed.

    The paragraph below that table records that a `ReadbackAttestation` non-export assertion passes
    **vacuously**. Extend it with the second vacuity finding this phase produced: `serverChallengeBrand`
    and `ConsentAckBase` are **present** in `dist/index.d.ts` as declarations and must be asserted
    absent from the trailing export list only — a file-absence assertion would fail on a correct
    artifact. Confirm neither vacuous assertion is counted anywhere as a passing check.

    **Then record the third honest limit, which is the one most likely to be overclaimed.**
    `assertSingleInstance` has **no production call site** in this phase. Plan 02-06 records the
    intended call sites (`createConcierge`, and each adapter's registration hook) in a doc comment
    only; the runtime that would call them lands in Phases 3–8. So PKG-04's runtime enforcement is
    **proven by test, not active in product**: two copies of core in a real application today would
    *not* fail loudly, because nothing calls the check. State this in the sign-off in those terms,
    and state what the evidence therefore covers — SC-4's "fails loudly" is demonstrated by fixture
    F2 and mutant P7 exercising `assertSingleInstance` directly, not by the guard firing on its own
    in a consumer. This is exactly the overclaim class this phase's standard exists to prevent, and
    it belongs next to the OIDC row rather than being discovered in Phase 3.

    **4. Flip the frontmatter.** Set `nyquist_compliant: true` and `status: complete`, and change
    § "Validation Sign-Off"'s `**Approval:** pending` to approved with the date. Tick each sign-off
    checkbox only where the evidence exists:
    - all tasks have an `<automated>` verify or a Wave 0 dependency;
    - sampling continuity — no three consecutive tasks without an automated verify;
    - Wave 0 covers all MISSING references (compare against the Wave 0 Requirements checklist and
      tick each item that now exists);
    - no watch-mode flags — confirm with a grep that no `--watch` appears in any script, workflow or
      test file;
    - feedback latency < 2 s for the inner loop — record the measured `pnpm typecheck` wall time;
    - all eleven mutants observed failing, each restored in-call, `git diff --exit-code` clean after
      each.

    **5. Tick the Wave 0 Requirements checklist.** Every box in that section now corresponds to a
    file or change that exists, except any this phase deliberately did not do — mark those with a
    one-clause reason rather than a tick. Note in particular that no placeholder
    `packages/concierge-svelte/` was created: the written constraint and the catalog pin are the
    scaffolding, deliberately.

    Change nothing outside `02-VALIDATION.md`. Do not edit `02-RESEARCH.md`, `02-CONTEXT.md`,
    `02-PATTERNS.md`, `ROADMAP.md`, `STATE.md`, or any plan or SUMMARY.
  </action>
  <verify>
    <automated>grep -q "^nyquist_compliant: true" .planning/phases/02-packaging-build-and-release/02-VALIDATION.md && grep -q "^status: complete" .planning/phases/02-packaging-build-and-release/02-VALIDATION.md && test -z "$(grep -F '⬜ pending' .planning/phases/02-packaging-build-and-release/02-VALIDATION.md)" && test -z "$(grep -F 'Approval:** pending' .planning/phases/02-packaging-build-and-release/02-VALIDATION.md)" && test "$(grep -rIh -- '--watch' --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning . | grep -v '^[[:space:]]*[#/*]' | grep -c -- '--watch')" -eq 0 && git diff --stat --name-only | grep -qx ".planning/phases/02-packaging-build-and-release/02-VALIDATION.md" && echo SIGNOFF_OK</automated>
  </verify>
  <acceptance_criteria>
    - `02-VALIDATION.md` frontmatter reads `nyquist_compliant: true` and `status: complete`
    - No row in § "Per-Task Verification Map" still reads `⬜ pending`; every ⚠️ or ❌ carries a reason
    - § "Suite Adequacy Requirement" carries a line recording the consecutive eleven-mutant re-run with its date and the clean-tree assertion
    - P11's row carries the parenthetical explaining the 42/43 vs 44/45 discrepancy; no other measured signature was edited
    - The 02-06 and 02-07 SUMMARYs were compared and both record the tree-shaking pair identically as **63 B uncalled / 587 B called — the registry code itself contributes 0 bytes when uncalled**; any disagreement is recorded rather than resolved by preference
    - § "Manual-Only Verifications" still has exactly one row, still states the workflow cannot be executed in this phase, and now records plan 02-10's static-review outcome and a pointer to `RELEASING.md`
    - The vacuity paragraph names both `ReadbackAttestation` and the `serverChallengeBrand` / `ConsentAckBase` export-list-versus-file distinction
    - The sign-off records the third honest limit: `assertSingleInstance` has **no production call site** until the runtime lands in Phases 3–8, so SC-4's "fails loudly" is demonstrated by fixture F2 and mutant P7 rather than exercised in product, and two copies in a real application would not currently fail loudly
    - § "Validation Sign-Off" has every box ticked with evidence, and `**Approval:**` is approved with a date
    - The § "Wave 0 Requirements" checklist is fully resolved: every box ticked or annotated with a one-clause reason, including the deliberate absence of a placeholder `packages/concierge-svelte/`
    - A repo-wide grep (excluding `node_modules`, `.git`, `.planning`) finds no `--watch` on an **executable** line: `grep -rIh -- '--watch' --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning . | grep -v '^[[:space:]]*[#/*]' | grep -c -- '--watch'` returns 0 — scoped to non-comment lines because plan 02-07 mandates a config comment naming `--watch` as the form never used
    - `git diff --name-only` for this task lists only `02-VALIDATION.md`
  </acceptance_criteria>
  <done>The validation contract records what was proven, with what command, and what remains unproven and why — and the phase can be verified against evidence rather than against intent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| incremental working tree → clean checkout | An incrementally-built tree hides missing steps, stale artifacts, and lockfile drift |
| individual proofs → the finished phase | A gate proven in wave 3 may have been weakened by a change in wave 7 |
| validation record → the verification that reads it | A sign-off claiming more than was observed is the failure this phase's standard exists to prevent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-59 | Tampering | a gate weakened by a later wave and never re-checked | mitigate | The whole eleven-mutant battery is re-run consecutively on the finished phase, not trusted from the wave that built it. |
| T-02-60 | Tampering | a lockfile that only resolves against an already-installed tree | mitigate | `pnpm install --frozen-lockfile` after removing every `node_modules`, which is the same install CI performs. |
| T-02-61 | Tampering | an unrestored mutation shipping in the phase commit | mitigate | `git diff --exit-code` after each of the thirteen invocations, plus `git status --porcelain` empty as a phase-level assertion. This closes the residual accepted in plan 02-02's threat T-02-09. |
| T-02-62 | Information Disclosure | tarball contents drifting from the reviewed decision | mitigate | The file list is reviewed by eye against plan 02-03's record, and the declaration map's `sources` target is confirmed present in the tarball. |
| T-02-63 | Repudiation | a validation sign-off asserting coverage that was never observed | mitigate | Statuses are set from recorded commands and exit codes; the manual-only row keeps its "cannot be executed in this phase" wording; both vacuous assertions are named and excluded from the count. |
| T-02-SC | Tampering | npm/pnpm installs | mitigate | The only install is `pnpm install --frozen-lockfile` from the committed lockfile. `pnpm-lock.yaml` is not in `files_modified`; if it changes, that is itself a finding. |
</threat_model>

<verification>
- Clean checkout: seven commands green, recorded with exit codes
- `pnpm run check:node-floor` green on a real v22.12.0, with the developer's version recorded beside it
- Thirteen mutant invocations, every one firing, `git diff --exit-code` clean after each
- P4, P6 and P10 each explicitly recorded as run and fired
- Tarball file list reviewed and recorded; declaration map `sources` present in the tarball
- Three honest limits recorded: the unexecuted OIDC workflow, the two vacuous assertions, and `assertSingleInstance`'s absent production call site
- `git status --porcelain` empty
- `02-VALIDATION.md` signed off with `nyquist_compliant: true`, no pending rows, and the manual-only row intact
</verification>

<success_criteria>
The phase is green from nothing, on the runtime it promises, with every one of its eleven structural
gates observed failing on the finished code and the tree clean after each — and the validation
contract records exactly that, including the one thing that cannot be verified until the first real
publish.
</success_criteria>

<output>
Create `.planning/phases/02-packaging-build-and-release/02-12-SUMMARY.md` when done.
</output>
