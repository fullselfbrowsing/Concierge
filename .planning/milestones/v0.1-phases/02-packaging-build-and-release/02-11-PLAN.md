---
phase: 02-packaging-build-and-release
plan: 11
type: execute
wave: 7
depends_on: ["02-02", "02-06", "02-07"]
files_modified:
  - packages/concierge/test-d/exports.test-d.ts
  - packages/concierge/test-d/consent-variance.test-d.ts
  - packages/concierge/test-d/consent.test-d.ts
autonomous: true
requirements: [PKG-01]
user_setup: []

must_haves:
  truths:
    - "A value moved into `index.ts`'s type-export block is caught by the type-test program"
    - "That guard reads the public entrypoint, not the module the existing guard reads"
    - "The `snapshotEquality` method-syntax regression fails with a diagnostic that names the invariant"
    - "No cross-reference in the type-test suite still claims M9 has a single symptom"
  artifacts:
    - path: packages/concierge/test-d/exports.test-d.ts
      provides: "MESSAGE_MAX_CHARS export-placement guard, importing from ../src/index.js"
      contains: '../src/index.js'
      min_lines: 20
    - path: packages/concierge/test-d/consent-variance.test-d.ts
      provides: "The named M9 detector — _policyNotBivariant"
      contains: "_policyNotBivariant"
      min_lines: 20
  key_links:
    - from: packages/concierge/test-d/exports.test-d.ts
      to: packages/concierge/src/index.ts
      via: "value import from the public barrel, NOT from types.js"
      pattern: 'from "\.\./src/index\.js"'
    - from: packages/concierge/test-d/consent-variance.test-d.ts
      to: packages/concierge/src/types.ts
      via: "ConsentPolicy variance predicate"
      pattern: "Not<Assignable<ConsentPolicy<Booking>, ConsentPolicy<unknown>>>"
---

<objective>
Close the two test-coverage items Phase 1 deferred into this phase, and prove both fire.

Purpose: both were designed and measured in Phase 1 but deliberately not landed, because adding
them would have changed the diagnostic set that Phase 1's own gate battery pinned. That constraint
is gone. They land here, last, for the same reason in reverse: they change the diagnostic set of
the type-test program, and doing that before the build pipeline is stable makes an unrelated failure
look like a regression.

The first is the more dangerous of the two. `verbatimModuleSyntax` enforcement is
**one-directional**: a type in the value block is `TS1205`, but a value misplaced into the type
block **escapes at exit 0** — from the type-test program *and* from the emit build — while silently
erasing the runtime binding from `dist/index.js`. And the existing guard cannot see it:
`results.test-d.ts`'s `_messageBound` imports `MESSAGE_MAX_CHARS` from `../src/types.js` and is
therefore structurally blind. **The new guard must import from `../src/index.js`.** Someone working
from Phase 1's note alone could close this item wrongly.
Output: two new `test-d` files, one corrected cross-reference, and mutants P8 and P9 observed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-packaging-build-and-release/02-CONTEXT.md
@packages/concierge/test-d/_assert.ts
@packages/concierge/test-d/results.test-d.ts
@packages/concierge/src/index.ts
@packages/concierge/tsconfig.test-d.json

Read only these ranges of `.planning/phases/02-packaging-build-and-release/02-RESEARCH.md`:
- lines 669–736 — both deferrals, defect-proven against the real `src/`, with the exact diagnostics
  and the verified trap table
- lines 1008–1009 — the P8 and P9 rows
- lines 1078–1080 — Pitfall 8

Also read `.planning/phases/02-packaging-build-and-release/02-PATTERNS.md` lines 76–170 (pattern 1:
the strongest analog in the phase — the four house rules, the assertion vocabulary, the divider
style, and the `@ts-expect-error` policy) and lines 173–239 (pattern 2: the form analog, the content
analog, the two placement options with their costs, and the cross-reference that must be updated).

`.planning/STATE.md` § Deferred Items carries both, each with the reason it was deferred.
</context>

<tasks>

<task type="auto">
  <name>Task 1: The MESSAGE_MAX_CHARS export-placement guard, reading the public entrypoint</name>
  <files>packages/concierge/test-d/exports.test-d.ts</files>
  <read_first>
    - `packages/concierge/test-d/results.test-d.ts` — all 108 lines; it is the exact analog. Lines 19–21 are the import convention, 23–25 the divider style, 103–108 the `_messageBound` near-miss this file exists to fix
    - `packages/concierge/test-d/_assert.ts` — all 38 lines; four predicates, the entire vocabulary, and the `@ts-expect-error` policy at lines 5–14
    - `packages/concierge/src/index.ts` — the two source blocks; this guard reads the value block
    - `02-RESEARCH.md` lines 689–711 — the verified trap table and the file skeleton
    - `02-PATTERNS.md` lines 76–170
  </read_first>
  <action>
    Create `packages/concierge/test-d/exports.test-d.ts`.

    The body is two lines of import plus one predicate, from `02-RESEARCH.md:703-710`. The comment
    on the import line is load-bearing and should survive verbatim:
    import `Equals` and `Expect` as types from `./_assert.js`, import `MESSAGE_MAX_CHARS` as a
    **value** from `"../src/index.js"` with the trailing comment
    `// ← index.js. NOT types.js. This is the whole point.`, then declare a single predicate
    `_messageBoundExportedAsValue` asserting `Equals<typeof MESSAGE_MAX_CHARS, 180>`, with a
    `/** … */` doc comment saying that `MESSAGE_MAX_CHARS` reaches the public entrypoint as a VALUE,
    not only as a type.

    **Four house rules, all four measured rather than assumed, all four mandatory:**

    1. **A file-top prose header** naming the deferral and what escapes without it. Every existing
       `test-d` file opens with 15–55 lines of `//` comment before the first import. This one must
       say: `verbatimModuleSyntax` is one-directional; a value moved into `index.ts`'s
       `export type` block exits 0 from `tsc -p tsconfig.test-d.json` *and* from the emit build
       while `dist/index.js` silently loses the binding; `results.test-d.ts`'s `_messageBound`
       imports from `../src/types.js` and structurally cannot see it; and the diagnostic that fires
       here is **TS1485 at the import line**, not TS2344 at the assertion — a reader expecting
       TS2344 will look at the wrong evidence.
    2. **Export nothing.** The imports supply module status; an export drags `isolatedDeclarations`
       in.
    3. **The predicate on ONE line, however long.** `tsc` echoes only the line the failing type
       argument sits on; wrapping silently disabled four of five assertions in Phase 1's plan 01-02.
    4. **Name the alias after the invariant**, with a `/** … */` doc comment above it. `Type 'false'
       does not satisfy the constraint 'true'` is the entire message — the alias name is the
       diagnostic.

    Use the **74-column** `// ----` divider style of `results.test-d.ts:23-25` if you divide the
    file — not `src/types.ts`'s 77-column style. Match the directory you are writing into.

    **Zero `@ts-expect-error` directives.** This file needs none; do not add one. The policy at
    `_assert.ts:5-14` reserves them for object-literal freshness, which predicates cannot model.

    Do **not** delete or modify `results.test-d.ts`'s `_messageBound`, and do **not** "consolidate"
    the two into one file with one import. They read different modules on purpose: `_messageBound`
    pins the literal type at the declaration, this one pins the export placement at the entrypoint.

    Do not modify `packages/concierge/tsconfig.test-d.json` — its `include` already covers
    `test-d/**/*.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge typecheck && grep -q 'from "../src/index.js"' packages/concierge/test-d/exports.test-d.ts && test "$(grep -cE '^import .*\.\./src/types\.js' packages/concierge/test-d/exports.test-d.ts)" -eq 0 && grep -q 'results.test-d.ts' packages/concierge/test-d/exports.test-d.ts && test -z "$(grep -l '^[[:space:]]*export' packages/concierge/test-d/exports.test-d.ts)" && echo EXPORTS_GUARD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @fullselfbrowsing/concierge typecheck` exits 0
    - `packages/concierge/test-d/exports.test-d.ts` imports `MESSAGE_MAX_CHARS` from `"../src/index.js"` as a value import
    - The file contains **no import statement** reading `../src/types.js`:
      `grep -cE '^import .*\.\./src/types\.js' packages/concierge/test-d/exports.test-d.ts` returns 0.
      Asserted as the *import form* anchored at line start, not as the bare path, and this is the
      single most important scoping decision in this plan: the mandated file header states
      *"`results.test-d.ts`'s `_messageBound` imports from `../src/types.js` and structurally cannot
      see it"*, so a bare `grep -c 'src/types.js'` is guaranteed red and the cheapest route to green
      is deleting the one sentence that explains this phase's own named near-miss trap. A comment
      line begins with `//` and can never match `^import`
    - The header sentence naming `results.test-d.ts`'s blindness is **present**:
      `grep -q 'results.test-d.ts' packages/concierge/test-d/exports.test-d.ts` succeeds. Paired
      deliberately with the criterion above — the identifier must appear in a comment and must not
      appear in an import, which is the same idiom plan 02-07 uses for `ReadbackAttestation`
    - The file exports nothing: `grep -c '^[[:space:]]*export' packages/concierge/test-d/exports.test-d.ts` returns 0
    - `grep -c '@ts-expect-error' packages/concierge/test-d/exports.test-d.ts` returns 0
    - The predicate `_messageBoundExportedAsValue` is written on a single line and carries a `/** … */` doc comment
    - The file header states that the firing diagnostic is **TS1485 at the import line**, not TS2344, and states why `results.test-d.ts` is blind to this regression
    - `packages/concierge/test-d/results.test-d.ts` is unchanged: `git diff --exit-code packages/concierge/test-d/results.test-d.ts` exits 0
    - `packages/concierge/tsconfig.test-d.json` is unchanged
    - `pnpm test` still passes and reports the same number of test **files** as before (Vitest's default `test.include` does not match `*.test-d.ts`)
  </acceptance_criteria>
  <done>The regression that erases a runtime binding from the published artifact with no diagnostic anywhere now has a guard that reads the module the regression happens in.</done>
</task>

<task type="auto">
  <name>Task 2: The named M9 detector, and the cross-reference it makes false</name>
  <files>packages/concierge/test-d/consent-variance.test-d.ts, packages/concierge/test-d/consent.test-d.ts</files>
  <read_first>
    - `packages/concierge/test-d/transport.test-d.ts` lines 59–66 — the form analog: the closest existing `Expect<Not<Assignable<…>>>` with a doc comment explaining why the negative *is* the guard
    - `packages/concierge/test-d/actions.test-d.ts` lines 115–142 — the content analog: M9's current sole detector and its documented weakness
    - `packages/concierge/test-d/actions.test-d.ts` lines 44–54 — the header's claim that "exactly two suppression directives appear below and no more", which this plan must not invalidate
    - `packages/concierge/test-d/consent.test-d.ts` lines 146–168 — the cross-reference that becomes false
    - `packages/concierge/src/types.ts` line 518 — `snapshotEquality?: (a: Snapshot, b: Snapshot) => boolean;`
    - `02-RESEARCH.md` lines 673–688 — the measured predicate and its TS2344 signature
  </read_first>
  <action>
    **Placement decision, locked: a new file, `packages/concierge/test-d/consent-variance.test-d.ts`
    — not an addition to `actions.test-d.ts`.** Both options are live in `02-PATTERNS.md:232-235`.
    `actions.test-d.ts` already has `Booking` and imports `ConsentPolicy`, so it is cheaper — but its
    header (lines 1–54) enumerates exactly what the file covers and declares that "exactly two
    suppression directives appear below and no more", a count a grep can confirm. A new file costs a
    ~20-line prose header and buys a clean blast radius on a file whose header is unusually
    load-bearing. Take the new file.

    In it: re-declare a local `interface Booking { readonly id: string }` and import `ConsentPolicy`
    as a type from `../src/types.js`, plus `Expect`, `Not` and `Assignable` from `./_assert.js`.
    Then one predicate, on one line:

    `_policyNotBivariant` asserting `Not<Assignable<ConsentPolicy<Booking>, ConsentPolicy<unknown>>>`,
    with a `/** … */` doc comment saying that function-property syntax keeps `snapshotEquality`'s
    parameters contravariant while method syntax would make them bivariant.

    The file header must explain **why the negative is the guard**, in the manner of
    `transport.test-d.ts:59-66`, and must explain what this file adds that
    `actions.test-d.ts:_policyDegraded` does not: under the method-syntax regression the assignment
    guarded by `_policyDegraded`'s directive starts *succeeding*, so the directive goes unused and a
    lone **TS2578** is the only symptom — a failure mode a reviewer silently deletes. This predicate
    makes the same regression fail with **TS2344** on a line whose echoed source text carries the
    name `_policyNotBivariant`, so the diagnostic names the invariant instead of pointing at an
    unused directive. State that both detectors are kept: one names the invariant, the other proves
    a real assignment is rejected.

    Same four house rules as Task 1 — prose header, export nothing, one line per predicate, alias
    named after the invariant. Zero `@ts-expect-error` directives in this file; the two in
    `actions.test-d.ts` stay where they are and their count must not change.

    **Then correct the cross-reference.** `consent.test-d.ts:163-167` currently says M9's *"single
    symptom is an unused directive on `_policyDegraded` in `actions.test-d.ts` (plan 01-06)"*. That
    sentence becomes false the moment this file lands. Amend that sentence — and only that sentence —
    to say M9 now has two detectors, naming `_policyNotBivariant` in
    `consent-variance.test-d.ts` and the TS2344 it produces, while keeping the surrounding paragraph's
    point intact: `DigestLike`'s method syntax still has **no** mutant and one must not be invented.
    Change nothing else in `consent.test-d.ts`.

    Do **not** edit `actions.test-d.ts`. Do **not** edit `src/types.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge typecheck && grep -q "_policyNotBivariant" packages/concierge/test-d/consent-variance.test-d.ts && test "$(grep -c '@ts-expect-error' packages/concierge/test-d/actions.test-d.ts)" -eq 2 && test -z "$(grep -F 'single symptom' packages/concierge/test-d/consent.test-d.ts)" && git diff --exit-code packages/concierge/test-d/actions.test-d.ts packages/concierge/src/types.ts && echo M9_DETECTOR_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @fullselfbrowsing/concierge typecheck` exits 0
    - `packages/concierge/test-d/consent-variance.test-d.ts` exists and contains `_policyNotBivariant` on a single line, with a `/** … */` doc comment
    - The predicate is exactly `Expect<Not<Assignable<ConsentPolicy<Booking>, ConsentPolicy<unknown>>>>`
    - The file exports nothing and contains zero `@ts-expect-error` directives
    - Its header explains why the negative is the guard and what it adds over `_policyDegraded`'s TS2578-only symptom
    - `grep -c '@ts-expect-error' packages/concierge/test-d/actions.test-d.ts` still returns exactly 2
    - `git diff --exit-code packages/concierge/test-d/actions.test-d.ts packages/concierge/src/types.ts` exits 0
    - `consent.test-d.ts` no longer contains the phrase `single symptom`, names `_policyNotBivariant` and `consent-variance.test-d.ts`, and still states that `DigestLike` has no mutant and one must not be invented
    - `git diff --stat packages/concierge/test-d/consent.test-d.ts` shows a change confined to that one paragraph
  </acceptance_criteria>
  <done>M9's regression now fails with a diagnostic that names the invariant, and no file in the suite still claims the old single-symptom story.</done>
</task>

<task type="auto">
  <name>Task 3: Mutants P8 and P9 — prove both new guards fire, with the measured diagnostics</name>
  <files>packages/concierge/src/index.ts, packages/concierge/src/types.ts</files>
  <read_first>
    - `packages/concierge/src/index.ts` — read the type block's tail and the `./types.js` value block verbatim; P8's pattern is the contiguous span from `  SessionConfig,` through the end of the `./types.js` value block
    - `packages/concierge/src/types.ts` line 518 — P9's pattern, exactly as written including leading whitespace
    - `scripts/mutate-and-prove.sh` — the harness and its compound-gate idiom
    - `02-RESEARCH.md` lines 689–701 — the verified trap table: `index.js` → TS1485 exit 2; `types.js` → exit 0 (blind); the emit build → exit 0 with the binding erased
  </read_first>
  <action>
    Two `mutate-and-prove.sh` invocations. Confirm `pnpm typecheck && pnpm build && pnpm test` is
    green first.

    **P8 — `MESSAGE_MAX_CHARS` moved into `index.ts`'s type-export block.**
    - Target: `packages/concierge/src/index.ts`
    - Pattern: the contiguous span beginning `  SessionConfig,` and ending with the closing
      `} from "./types.js";` of the **value** block, read verbatim from the file. It contains `/`
      characters, so this mutant re-exercises the harness fix from plan 02-02.
    - Replacement: the same span with `  MESSAGE_MAX_CHARS,` moved from the value block up into the
      type block, immediately after `  SessionConfig,`. One substitution performs the whole move —
      do not add it to the type block without removing it from the value block, or the diagnostic
      becomes a duplicate-export error rather than TS1485.
    - Gate: `bash -c 'pnpm build; echo BUILD_EXIT=$?; pnpm test -- artifact; echo ARTIFACT_EXIT=$?; pnpm typecheck'`

    Four observations, all required:
    - **`BUILD_EXIT=0`** — the emit build is silent on a change that erases a runtime binding from
      the published artifact. This is the third row of the verified trap table and the reason the
      guard is needed at all.
    - **`ARTIFACT_EXIT` non-zero** — plan 02-07's `artifact.test.ts` catches the erasure at the
      artifact, where it actually harms a consumer.
    - **`pnpm typecheck` non-zero with `TS1485`**, reported at the **import line** of
      `exports.test-d.ts` — the text is *"'MESSAGE_MAX_CHARS' resolves to a type-only declaration and
      must be imported using a type-only import when 'verbatimModuleSyntax' is enabled"*.
    - **`results.test-d.ts` is NOT named in the typecheck output.** `_messageBound` imports from
      `../src/types.js` and is structurally blind to this regression; that blindness is the whole
      justification for the new file, and observing it is what proves the two guards are not
      redundant. Record the full list of files named in the diagnostic output.

    **P9 — `snapshotEquality` regressed to method syntax.**
    - Target: `packages/concierge/src/types.ts`
    - Pattern: `  snapshotEquality?: (a: Snapshot, b: Snapshot) => boolean;` exactly, including its
      two leading spaces
    - Replacement: `  snapshotEquality?(a: Snapshot, b: Snapshot): boolean;`
    - Gate: `pnpm typecheck`

    Expected: harness `PASS`, with **`TS2344`** — *"Type 'false' does not satisfy the constraint
    'true'"* — reported on the line whose echoed source text contains **`_policyNotBivariant`**.
    Expect **`TS2578`** as well, on `_policyDegraded` in `actions.test-d.ts`: that is M9's old,
    lone, deletable symptom, and seeing both in the same run is the evidence that the deferral did
    what it was for. Record both diagnostics and their file locations.

    After both: `pnpm build` (the harness restores sources, not build output), then
    `pnpm typecheck && pnpm build && pnpm test && pnpm run check:deps` all green,
    `git diff --exit-code` clean, `git status --porcelain` empty.
  </action>
  <verify>
    <automated>bash scripts/mutate-and-prove.sh packages/concierge/src/types.ts '  snapshotEquality?: (a: Snapshot, b: Snapshot) => boolean;' '  snapshotEquality?(a: Snapshot, b: Snapshot): boolean;' -- pnpm typecheck 2>&1 | tee /tmp/p9.log; grep -q "PASS: gate fired" /tmp/p9.log && grep -q "TS2344" /tmp/p9.log && grep -q "_policyNotBivariant" /tmp/p9.log && git diff --exit-code && echo P9_PROVEN</automated>
  </verify>
  <acceptance_criteria>
    - P8: harness exit 0 with `PASS: gate fired`
    - P8: the gate output contains the literal line `BUILD_EXIT=0`
    - P8: the gate output contains an `ARTIFACT_EXIT=` line with a non-zero value
    - P8: the typecheck output contains `TS1485` reported in `packages/concierge/test-d/exports.test-d.ts` at the import line
    - P8: the typecheck output does **not** name `packages/concierge/test-d/results.test-d.ts`, and the full list of named files is recorded in the SUMMARY
    - P9: harness exit 0 with `PASS: gate fired`
    - P9: the typecheck output contains `TS2344` with the text `Type 'false' does not satisfy the constraint 'true'`, on a line whose echoed source contains `_policyNotBivariant`
    - P9: the typecheck output also contains `TS2578` for `_policyDegraded` in `actions.test-d.ts`, and both diagnostics with their file:line locations are recorded in the SUMMARY
    - After each invocation, `git diff --exit-code` on the mutated file exits 0
    - After the task, `pnpm typecheck && pnpm build && pnpm test && pnpm run check:deps` all exit 0
    - `git status --porcelain` is empty at the end of the task
  </acceptance_criteria>
  <done>Both deferred guards have been observed firing with the exact diagnostics measured in research, and the blindness of the existing guard has been observed rather than asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `src/index.ts`'s export blocks → published runtime surface | A value in the type block is erased from `dist/index.js` with no diagnostic from either program |
| declaration syntax → variance of a consent comparator | Method syntax makes `snapshotEquality`'s parameters bivariant, weakening a consent check silently |
| type-test diagnostics → reviewer attention | A lone unused-directive error is the failure mode a reviewer deletes rather than investigates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-55 | Tampering | a value export silently erased from the published artifact | mitigate | `exports.test-d.ts` reads `../src/index.js`, the module the regression happens in. Mutant P8 proves it fires with TS1485 while the emit build stays silent and the existing `types.js`-based guard stays blind. |
| T-02-56 | Tampering | `snapshotEquality` regressed to bivariant parameters | mitigate | `_policyNotBivariant` fails with TS2344 naming the invariant. Consent comparison is the mechanism by which "the human confirmed **this** payload" is checked; a bivariant comparator accepts a comparator for the wrong snapshot type. |
| T-02-57 | Repudiation | a guard whose only symptom is an unused directive | mitigate | The named predicate is added **alongside** the directive, not instead of it, and mutant P9 requires both TS2344 and TS2578 to be observed in the same run. |
| T-02-58 | Repudiation | a stale cross-reference asserting single-symptom coverage | mitigate | `consent.test-d.ts`'s claim is corrected in the same plan that makes it false, asserted by a grep for the old phrase. |
| T-02-SC | Tampering | npm/pnpm installs | accept | This plan installs nothing and touches no manifest or lockfile. |
</threat_model>

<verification>
- `pnpm --filter @fullselfbrowsing/concierge typecheck` exits 0 with both new files present
- `exports.test-d.ts` has an `import` from `../src/index.js` and no `import` from `../src/types.js`, while its header still names `results.test-d.ts` as the blind guard
- `actions.test-d.ts` still has exactly two `@ts-expect-error` directives and is unchanged
- `consent.test-d.ts` no longer says `single symptom`
- P8 observed: `BUILD_EXIT=0`, non-zero `ARTIFACT_EXIT`, TS1485 in `exports.test-d.ts`, `results.test-d.ts` not named
- P9 observed: TS2344 naming `_policyNotBivariant`, plus TS2578 on `_policyDegraded`
- `pnpm typecheck && pnpm build && pnpm test && pnpm run check:deps` all exit 0
- `git status --porcelain` empty
</verification>

<success_criteria>
Both Phase 1 deferrals are closed with guards that read the right modules, each has been observed
failing with the exact diagnostic measured in research, the existing guard's blindness has been
demonstrated rather than claimed, and no cross-reference in the suite still describes the old
coverage.
</success_criteria>

<output>
Create `.planning/phases/02-packaging-build-and-release/02-11-SUMMARY.md` when done.
</output>
