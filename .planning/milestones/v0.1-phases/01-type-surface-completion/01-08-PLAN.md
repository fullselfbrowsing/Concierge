---
phase: 01-type-surface-completion
plan: 08
type: execute
wave: 8
depends_on: ["01-07"]
files_modified:
  - packages/concierge/src/index.ts
  - README.md
autonomous: true
requirements: [SC-2, SC-3, SC-7]
user_setup: []

must_haves:
  truths:
    - "Every type this phase added is importable from the package entry point"
    - "The published README's ActionResult block matches the shipped type"
  artifacts:
    - path: packages/concierge/src/index.ts
      provides: "The complete export surface: 10 new types plus MESSAGE_MAX_CHARS"
      contains: "AnyActionDefinition"
    - path: README.md
      provides: "A design-contract block that agrees with types.ts"
      contains: "ReasonCode"
  key_links:
    - from: packages/concierge/src/index.ts
      to: packages/concierge/src/types.ts
      via: "export type re-export"
      pattern: 'from "\./types\.js"'
    - from: README.md
      to: "ActionResult.reason"
      via: "rendered design contract"
      pattern: "reason\\?:"
---

<objective>
Make everything this phase added reachable, and make the docs stop contradicting the contract.

`packages/concierge/src/index.ts` is the package entry point and currently exports 29 types and 3
values. Ten of the types this phase declared are not exported, and two of them —
`AnyActionDefinition` and `TurnIdentityProvenance` — appear *in the public surface* via
`StageDefinition.actions`, `ConciergeConfig.crossStage`, and `TransportCapabilities`, so they cannot
stay module-private.

`README.md` line 72 renders `reason?: string; // stable machine-readable failure code` inside the
"Design contract" section. That is the only documentation reference to any type this phase changes —
`grep -n "userTurnIdentity\|deferUntilDelivered\|readbackHash\|ConsentAck\|reason?:\|TransportCapabilities\|snapshotEquality" README.md` returns exactly that one line — and Plan 02 made it false.

Purpose: an unexported type that appears in the public surface forces consumers to re-derive it, and
a README that contradicts the shipped contract on day one is worse than no README.
Output: the completed export surface and the corrected README block.

**Serial phase.** Plan 8 of 9 in a single serial sequence. Do not parallelize.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/concierge/src/index.ts
@packages/concierge/src/types.ts

Read only these ranges of `.planning/phases/01-type-surface-completion/01-RESEARCH.md`:
- lines 634–656 — Export Surface Impact: the complete enumerated table of additions
- lines 238–245 — Runtime State Inventory: the `README.md:72` finding and the grep that proves it is the only one
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete the export surface in index.ts</name>
  <files>packages/concierge/src/index.ts</files>
  <read_first>
    - `packages/concierge/src/index.ts` — the current 29 type exports and 3 value exports, in full
    - `packages/concierge/src/types.ts` — the exported declarations as they now stand after Plans 02–07
    - `.planning/phases/01-type-surface-completion/01-RESEARCH.md` lines 634–656 — the enumerated addition table
  </read_first>
  <action>
    Add ten type exports to the existing `export type { … } from "./types.js"` block, placed under
    the comment groups they belong to so the file's organisation survives:
    - Results group: `FailureReason`, `ReasonCode`
    - Consent group: `Readback`, `ReadbackReceipt`, `ReadbackSink`, `DigestLike`, `ServerChallenge`
    - Actions group: `AnyActionDefinition`
    - Transport group: `TurnIdentityProvenance`
    - Concierge group: `Scheduler`

    Add `MESSAGE_MAX_CHARS` to the value export line alongside `USER_CANCELLED`, `USER_DECLINED`,
    and `CONSENT_GRADE_ORDER`. It is a value, not a type — `verbatimModuleSyntax` requires it in the
    plain `export { … }` statement, not the `export type { … }` block.

    **Remove nothing.** `AbandonReason` stays exported: D-01 reuses it as a named subset of
    `ReasonCode`, which is what closed the orphan defect rather than deleting the symbol.

    **Do not export the `ServerChallenge` brand symbol.** It is a module-private `declare const` and
    `isolatedDeclarations` emits it correctly without an export.

    **Do not export `ReadbackAttestation`** — it is not declared, and D-12 item 1 defers it to
    Phase 8 which designs the kernel that consumes it.

    Update the file's header doc comment only if it is now inaccurate; it currently says the package
    exports the design contract only, which remains true.
  </action>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @fullselfbrowsing/concierge typecheck` exits 0
    - `packages/concierge/src/index.ts` exports all ten of `FailureReason`, `ReasonCode`, `Readback`, `ReadbackReceipt`, `ReadbackSink`, `DigestLike`, `ServerChallenge`, `AnyActionDefinition`, `TurnIdentityProvenance`, `Scheduler`
    - The value export line lists exactly `USER_CANCELLED`, `USER_DECLINED`, `CONSENT_GRADE_ORDER`, `MESSAGE_MAX_CHARS`
    - `MESSAGE_MAX_CHARS` appears in the `export { … }` statement and NOT in the `export type { … }` block
    - `AbandonReason` is still exported
    - No brand symbol and no `ReadbackAttestation` is exported
    - Every name in `index.ts` resolves: the typecheck passing is the proof, since `verbatimModuleSyntax` rejects a re-export of a missing name
  </acceptance_criteria>
  <done>Every type Phase 1 added is importable from the package entry point, and nothing that was exported before was dropped.</done>
</task>

<task type="auto">
  <name>Task 2: Correct README.md's ActionResult block</name>
  <files>README.md</files>
  <read_first>
    - `README.md` lines 59–78 — the "Design contract" section, item 2, containing the stale block
    - `packages/concierge/src/types.ts` — the shipped `ActionResult`, `ReasonCode`, and `MESSAGE_MAX_CHARS`, which the README must now match
    - `.planning/phases/01-type-surface-completion/01-RESEARCH.md` lines 238–245
  </read_first>
  <action>
    Update the fenced `ActionResult` block under "### 2. Every action returns `{ok, message}`" so it
    renders the shipped type rather than the pre-Phase-1 one. The `reason` line must show the closed
    union — `reason?: ReasonCode | undefined` — rather than `string`, and its trailing comment should
    say the codes are a closed set rather than merely "stable machine-readable".

    Add one sentence to the prose below the block stating that `message` is a best-effort
    human-facing sentence bounded by `MESSAGE_MAX_CHARS` and is never a consent artifact. Keep the
    existing prose about failures being honest and carrying a recovery hint — it is still accurate
    and it is the paragraph's point.

    Keep the edit minimal. Do not restructure the section, do not renumber the six design-contract
    points, and do not touch any other part of the README. This is the only documentation reference
    to any type this phase changes.

    After editing, re-run the grep that established the finding —
    `grep -n "userTurnIdentity\|deferUntilDelivered\|readbackHash\|ConsentAck\|reason?:\|TransportCapabilities\|snapshotEquality" README.md`
    — and confirm every line it returns is now consistent with `types.ts`.
  </action>
  <verify>
    <automated>grep -n "reason?: ReasonCode" README.md && grep -c "reason?: string" README.md | grep -qx 0 && echo README_CONSISTENT</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `reason?: ReasonCode | undefined` inside the design-contract `ActionResult` block
    - `grep -c "reason?: string" README.md` returns 0
    - `README.md` states that `message` is never a consent artifact and names `MESSAGE_MAX_CHARS`
    - The six design-contract points are still numbered 1 through 6 and none was reordered
    - `git diff --stat README.md` shows changes confined to the design-contract `ActionResult` block and its following paragraph
    - The re-run grep returns only lines consistent with the shipped `types.ts`
  </acceptance_criteria>
  <done>The published README's design contract agrees with the shipped type on every line the grep surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package entry point → consumer | `index.ts` is the entire importable surface; a type used in a public signature but not exported is unusable |
| README → developer expectation | The design contract is a public commitment; a false line is a security-relevant misstatement when it describes a consent type |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-26 | Repudiation | README claiming an open `reason?: string` while the shipped type is closed | mitigate | Correct line 72 and its paragraph. The README's "Design contract" is listed in CONTEXT under "Public-facing statements these types must not contradict"; shipping a contradiction on day one undermines the one claim the project narrowed its wording to keep true. |
| T-01-27 | Information Disclosure | exporting the `ServerChallenge` brand symbol | mitigate | Do not export it. Exporting the brand would let an app construct a `ServerChallenge` by intersecting, defeating the produce-nothing rule the brand exists to enforce. Asserted by the acceptance criteria. |
| T-01-28 | Tampering | dropping `AbandonReason` from the export surface | mitigate | Keep it. D-01 reuses it as a named subset; deleting the symbol would break the MCP elicitation correspondence its doc comment claims and would silently change the public surface. |
| T-01-SC | Tampering | npm/pnpm installs | accept | No packages installed. |
</threat_model>

<verification>
- `pnpm --filter @fullselfbrowsing/concierge typecheck` exits 0
- `pnpm typecheck` from the repo root exits 0
- `grep -c "reason?: string" README.md` returns 0
- `git diff --exit-code pnpm-lock.yaml` exits 0
</verification>

<success_criteria>
Every type Phase 1 added is importable from `@fullselfbrowsing/concierge`, the brand symbol and
`ReadbackAttestation` are not exported, nothing previously exported was dropped, and the README's
design contract matches the shipped `ActionResult`.
</success_criteria>

<output>
Create `.planning/phases/01-type-surface-completion/01-08-SUMMARY.md` when done.
</output>
