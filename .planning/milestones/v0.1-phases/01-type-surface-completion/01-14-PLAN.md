---
phase: 01-type-surface-completion
plan: 14
type: execute
wave: 14
gap_closure: true
closes: [WR-04, WR-05, WR-07, IN-01]
depends_on: ["01-13"]
files_modified:
  - packages/concierge/src/types.ts
  - packages/concierge/test-d/actions.test-d.ts
  - packages/concierge/test-d/consent.test-d.ts
  - packages/concierge/test-d/results.test-d.ts
  - packages/concierge/test-d/transport.test-d.ts
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-7, TRN-01]
user_setup: []

must_haves:
  truths:
    - "The consent gate selector is a closed pair — widened to `string`, a typo can no longer silently pick the weak gate or none"
    - "A readback receipt's hash and canonical bytes cannot be widened to `unknown` without the suite going red"
    - "An ack cannot arm without turn identity, a delivery outcome cannot become an open string, and a result cannot lose its success flag or its message"
    - "The redaction policy's doc comment describes the type that actually shipped"
  artifacts:
    - path: packages/concierge/test-d/actions.test-d.ts
      provides: "ConsentPolicy member pins that are not expressed in terms of ConsentPolicy"
      contains: "_bindToIsClosed"
    - path: packages/concierge/test-d/consent.test-d.ts
      provides: "Receipt hash/canonical pins and the ack turn-identity pin"
      contains: "_receiptCanonicalIsBytes"
    - path: packages/concierge/test-d/results.test-d.ts
      provides: "The two ActionResult required-member pins (WR-07)"
      contains: "_resultOkRequired"
    - path: packages/concierge/test-d/transport.test-d.ts
      provides: "The DeliveryReport.outcome closed-union pin (WR-07)"
      contains: "_deliveryOutcomeIsClosed"
    - path: packages/concierge/src/types.ts
      provides: "A RedactionPolicy doc comment that matches the required member it describes"
      contains: "no implicit default"
  key_links:
    - from: "ConsentPolicy.bindTo"
      to: "the strong user-turn gate versus the weak response gate"
      via: "a closed two-member literal union, pinned against its literal spellings rather than against ConsentPolicy itself"
      pattern: "_bindToIsClosed"
    - from: "ReadbackReceipt.canonical"
      to: "the WebAuthn no-reserialize rule the field exists to enforce"
      via: "a pin that fires when the field is widened to `unknown`"
      pattern: "_receiptCanonicalIsBytes"
---

<objective>
Close WR-04, WR-05, WR-07, and IN-01: **four contracts in the shipped types are correct and
completely unguarded, and one doc comment describes a type that does not exist.**

**WR-04 — `_snapshotInferred` is self-referential.** `actions.test-d.ts:180` asserts
`Equals<NonNullable<(typeof confirm)["consent"]>, ConsentPolicy<Booking>>`. Both sides are expressed
in terms of `ConsentPolicy`, so any change *inside* `ConsentPolicy` changes both sides identically
and the assertion stays true. It pins the `Snapshot` type argument and nothing else. Measured:
`bindTo: "userTurn" | "response"` → `bindTo: string` **exits 0 against the full suite**. `bindTo` is
the selector between the strong gate and the weak one — widened to `string`, `bindTo: "usreTurn"`
typechecks, and whether the Phase 8 runtime then falls back to `"response"` or to no gate at all, the
compiler said nothing. `minGrade` and `onMissing` are unguarded for the same reason. `Transport` is
the only interface in the whole suite carrying a `keyof` pin; `ConsentPolicy` — the consent gate's own
declaration — has none.

**WR-05 — two of the receipt's four fields are pinned only by the fixture.**
`consent.test-d.ts:63-66` correctly explains why the object fixture alone is insufficient — `"SHA-256"`
assigns happily to a widened `string` — and then adds `Equals` predicates for `alg` and
`canonicalization` only. `hash` and `canonical` were left on the fixture, which detects *removal*
(TS2353 excess-property) but not *widening*, since any value assigns to a widened type. Measured:
`canonical: Uint8Array` → `canonical: unknown` **exits 0**. That is not cosmetic: `unknown` lets a
sink return a re-serialized string in the field whose entire justification (`:568-574`) is WebAuthn's
rule that intermediaries must not parse-and-reserialize — reopening the exact hazard the field exists
to close, with no diagnostic.

**WR-07 — four more required/closed contracts have no assertion anywhere.** All four escaped the full
four-file suite: `ConsentAckBase.userTurnId` made optional (the value `bindTo: "userTurn"` compares —
optional, an ack can arm with no turn identity and the strongest binding has nothing to check);
`DeliveryReport.outcome` widened to `string` (`01-VERIFICATION.md` cites this literal union as the
evidence for SC-1, and battery mutant M2 tests the *hook's parameter*, not the union — widened,
`outcome === "completed"` still compiles and every other value silently passes an exhaustive check);
`ActionResult.ok` made optional; `ActionResult.message` made optional.

**IN-01 — `RedactionPolicy`'s doc claims a default the type cannot have.** `:697-704` reads "Required
for any action with a non-empty schema. Defaults to `"drop"`", but `ActionDefinition.redact` (`:736`)
is a **required** member with no optionality, so nothing ever defaults. Being stricter than documented
is the safe direction, but the prose reads as "I can omit this" on a security control, and SEC-01's
whole point is that redaction fails closed.

These are the cheapest findings in the set: three are test-only additions against contracts that
already exist correctly, and the fourth is a one-line reword. They are also the ones most likely to be
skipped, which is why they get a plan rather than a footnote.

Purpose: close the surface, not the symptom. A contract that is correct and unguarded is one edit away
from being incorrect and unnoticed.
Output: **ten** named guards across four test files — six in Task 2 (`_bindToIsClosed`,
`_minGradeIsGrade`, `_onMissingShape`, `_policyKeys`, `_receiptHashIsString`,
`_receiptCanonicalIsBytes`) and four in Task 3 (`_ackCarriesTurnIdentity`,
`_deliveryOutcomeIsClosed`, `_resultOkRequired`, `_resultMessageRequired`) — and one corrected
doc comment.

**Serial phase.** Plan 14 of the gap-closure sequence. No other plan may edit or transiently mutate
`packages/concierge/src/types.ts` in this wave.

## Constraint carried into every task

Do not weaken, rewrite, or delete a pre-existing assertion. `_snapshotInferred` in particular stays
exactly as it is: it is M3's and M10's detector and the phase gate depends on it. WR-04 is closed by
**adding** pins beside it, not by replacing it. The review's criticism is that it is insufficient,
not that it is wrong.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-type-surface-completion/01-REVIEW.md
@packages/concierge/src/types.ts
@packages/concierge/test-d/_assert.ts
@packages/concierge/test-d/actions.test-d.ts
@packages/concierge/test-d/consent.test-d.ts

Read `.planning/phases/01-type-surface-completion/01-VALIDATION.md` § "Assertion Mechanism —
predicates, not directives" and the M10 correction in § "Ten-mutant battery", which records why
`_requiresIsString` stays silent under M10 and must not be "fixed" into firing.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reword the RedactionPolicy doc comment to describe the type that shipped (IN-01)</name>
  <files>packages/concierge/src/types.ts</files>
  <read_first>
    - `packages/concierge/src/types.ts` lines 693–704 — `RedactionPolicy` and its doc comment
    - `packages/concierge/src/types.ts` line 736 — `ActionDefinition.redact`, the required member the comment describes
    - `.planning/REQUIREMENTS.md` § Security, SEC-01
    - `.planning/phases/01-type-surface-completion/01-REVIEW.md` § IN-01
  </read_first>
  <action>
    `RedactionPolicy`'s doc comment says "Required for any action with a non-empty schema. Defaults to
    `"drop"`". `ActionDefinition.redact` is a required member with no optionality, so nothing ever
    defaults — every action must state a policy explicitly. The type is stricter than the prose, which
    is the safe direction, but the prose will be read as "I can omit this" on a security control.

    Reword to describe the shipped type, and keep the safety guidance that made the original sentence
    worth having. The replacement must say all three of:
    - Required on **every** action; there is no implicit default and no way to omit it.
    - `"drop"` is the value to choose when in doubt, because telemetry leaks are opt-in and never
      accidental. This preserves what the original was trying to convey.
    - Where the enforcement lives: **Phase 3 owns it (SEC-01)**. Naming the phase stops the next
      reader assuming the requirement is unowned.

    **Do not write "SEC-01 fails the build for a non-empty schema with no redaction policy".** It
    contradicts SEC-01's own text. Measured: `REQUIREMENTS.md:85` reads "Redaction is required at
    declaration time for any action with a non-empty schema, **and an unspecified policy defaults to
    dropping arguments**" — a default that the shipped required member makes unreachable. Asserting
    the opposite in a doc comment would leave `types.ts` and `REQUIREMENTS.md` in direct
    contradiction, which is the same class of defect IN-01 is closing.

    **Record the divergence as a Phase 3 handoff in the SUMMARY instead**, under a heading naming
    SEC-01. State three things: (1) `ActionDefinition.redact` (`types.ts:736`) is required with no
    optionality, so the "unspecified policy defaults to dropping" half of SEC-01 describes a state the
    type cannot reach; (2) SEC-01's wording should be corrected to "an action with a non-empty schema
    and no redaction policy fails the build", which is what the type actually supports and what Phase 3
    will implement; (3) `CLAUDE.md:21` carries the same claim ("Redaction is required for any action
    with a non-empty schema and defaults to `drop`") and is **out of scope for this sequence** — flag
    it for the user rather than editing it, because project instruction files are not this plan's to
    change. After this task, those two documents are the only surviving carriers of the claim, and
    Phase 3 will plan against SEC-01.

    This is a comment-only edit to `types.ts`. Do not change `RedactionPolicy`'s type, do not make
    `ActionDefinition.redact` optional, and do not add a default anywhere.

    **Do not touch `src/index.ts`.**
  </action>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @fullselfbrowsing/concierge typecheck` exits 0
    - The comment no longer claims a default: `grep -c 'Defaults to' packages/concierge/src/types.ts` returns 0 for the `RedactionPolicy` block, and the file contains the phrase `no implicit default`
    - The comment names Phase 3 and SEC-01 as the enforcement point, and does **not** assert that SEC-01 fails the build — `grep -c 'fails the build' packages/concierge/src/types.ts` returns 0 within the `RedactionPolicy` block
    - The SUMMARY carries a Phase 3 handoff section naming SEC-01, stating the divergence, proposing the corrected SEC-01 wording, and flagging `CLAUDE.md:21` as carrying the same claim and being out of scope for this sequence
    - The diff for this task touches only comment lines: no line changes a type, a modifier, or a member name
    - `git diff --exit-code -- packages/concierge/test-d packages/concierge/src/index.ts` exits 0
  </acceptance_criteria>
  <done>The one doc comment in the file that described a type the file does not contain now describes the one it does, without losing the safety guidance that made it worth writing.</done>
</task>

<task type="auto">
  <name>Task 2: Pin ConsentPolicy's members against their literal spellings, and the receipt's remaining two fields (WR-04, WR-05)</name>
  <files>packages/concierge/test-d/actions.test-d.ts, packages/concierge/test-d/consent.test-d.ts</files>
  <read_first>
    - `packages/concierge/test-d/actions.test-d.ts` lines 144–180 — `_requiresIsString`, `confirm`, `_nameNotWidened`, `_snapshotInferred`, and the comment explaining why `_requiresIsString` stays silent under M10
    - `packages/concierge/src/types.ts` lines 355–407 — `ConsentPolicy`, all five members
    - `packages/concierge/test-d/consent.test-d.ts` lines 50–72 — the receipt fixture and the two literal pins this task extends
    - `packages/concierge/test-d/transport.test-d.ts` line 137 — `_transportKeys`, the only `keyof` pin in the suite and the model for `_policyKeys`
    - `.planning/phases/01-type-surface-completion/01-REVIEW.md` § WR-04 and § WR-05
  </read_first>
  <action>
    Add the six pins that make `ConsentPolicy` and `ReadbackReceipt` guarded rather than merely correct.
    Every predicate on ONE line however long — both files' headers record that wrapping silently
    disabled four of five assertions in plan 01-02. Export nothing.

    **In `actions.test-d.ts`, immediately after `_snapshotInferred` (WR-04).** Leave
    `_snapshotInferred` untouched — it is M3's and M10's detector. Add beside it, with a comment
    stating plainly why: *`_snapshotInferred` expresses both sides in terms of `ConsentPolicy`, so any
    change inside `ConsentPolicy` changes both sides identically and it stays true. It pins the
    `Snapshot` argument and nothing else. The pins below name the literal spellings, which is what
    makes them able to see a widening.*

    Required alias names:
    - `_bindToIsClosed` — `Expect<Equals<ConsentPolicy["bindTo"], "userTurn" | "response">>`. Comment
      it with the stakes: this is the selector between the strong gate and the weak one, and widened
      to `string` a misspelling like `"usreTurn"` typechecks while the compiler says nothing about
      which gate the Phase 8 runtime will then choose.
    - `_minGradeIsGrade` — `Expect<Equals<ConsentPolicy["minGrade"], ConsentGrade | undefined>>`.
      Note that this is the dial `buildCatalog` enforces at build time (CAT-04) and the reason D-04 cut
      `impact` rather than shipping a second, weaker severity axis beside it.
    - `_onMissingShape` — an `Expect<Equals<…>>` pinning `ConsentPolicy["onMissing"]` against its
      member types **spelled literally**, unioned with `undefined`. Under the flat `ActionResult`
      (option-b) that is `{ reason?: ReasonCode | undefined; message: string } | undefined` —
      measured green against the shipped declaration, and measured firing under MUT-I4. Read the
      shipped declaration rather than assuming, because plan 01-13 may have replaced the `Pick` with
      an explicit object type.

      **The right-hand side must NOT be expressed as `Pick<ActionResult, "reason" | "message">`, and
      this prohibition is the whole point of the pin.** `onMissing` is *declared* as that `Pick`
      (`types.ts:406`), so writing the RHS the same way puts both sides through `ActionResult` — any
      change inside `ActionResult` then changes both sides identically and the assertion stays true
      forever. That is the literal mechanism of WR-04's complaint about `_snapshotInferred`, and
      reproducing it inside the plan that closes WR-04 would be the sequence failing on its own
      terms. Spell the members out.
    - `_policyKeys` — `Expect<Equals<keyof ConsentPolicy, "requires" | "bindTo" | "snapshotEquality" | "minGrade" | "onMissing">>`.
      Model it on `_transportKeys` and say so in the comment: the member set is closed, so a new dial
      cannot appear beside `minGrade` unnoticed, which is exactly the failure D-04 spent four entries
      preventing.

    **In `consent.test-d.ts`, beside `_receiptAlgIsLiteral` and `_receiptCanonicalizationIsLiteral`
    (WR-05).** The comment at `:63-66` already explains why the fixture alone is insufficient. Extend
    the same treatment to the two fields it stopped short of:
    - `_receiptHashIsString` — `Expect<Equals<ReadbackReceipt["hash"], string>>`.
    - `_receiptCanonicalIsBytes` — an `Expect<Equals<…>>` pinning `ReadbackReceipt["canonical"]`
      against the type plan 01-10 landed. **Read the shipped declaration:** 01-10 was planned to type
      it `Readonly<Uint8Array>` to close the element-write hole, so the pin must name that type, not a
      bare `Uint8Array`. If they disagree, the pin is wrong, not the source.

    Comment `_receiptCanonicalIsBytes` with what it protects: widened to `unknown`, a sink can hand
    back a re-serialized string in the field whose entire justification is WebAuthn's rule that
    intermediaries must not parse-and-reserialize — so the widening reopens the hazard the field
    exists to close, and nothing else in the repository would notice.

    Extend the sentence at `:63-66` to say the coverage is now complete across all four fields, so a
    later reader does not repeat the reviewer's discovery that two of the four were fixture-only.

    **Defect-first is mandatory.** Apply and restore **inside a single Bash tool call** — never across
    two — and assert the tree is clean afterward:

      `perl -0pi -e '<substitution>' packages/concierge/src/types.ts && pnpm --filter @fullselfbrowsing/concierge typecheck; EXIT=$?; git checkout -- packages/concierge/src/types.ts; git diff --exit-code -- packages/concierge/src/types.ts && echo "MUTANT_EXIT=$EXIT TREE_CLEAN"`

    The `/tmp` sandbox idiom is an accepted and safer alternative — copy `src/`, `test-d/`,
    `packages/concierge/tsconfig.json`, `tsconfig.base.json`, and
    `packages/concierge/node_modules/@standard-schema` into a scratch directory, repoint the `extends`
    path, add a `tsconfig.test-d.json` with `noEmit: true` and `rootDir: "."`, and run `tsc` there.
    If you use it, say so in the SUMMARY and still assert `git status --porcelain` is empty.

    - **MUT-I** — `ConsentPolicy.bindTo: "userTurn" | "response"` → `bindTo: string`. Expect TS2344
      naming `_bindToIsClosed`. This mutation exits 0 against the suite as it stands today; that is the
      finding.
    - **MUT-I2** — add a sixth member to `ConsentPolicy` (any name, any type). Expect TS2344 naming
      `_policyKeys`.
    - **MUT-I3** — `minGrade?: ConsentGrade` → `minGrade?: string`. Expect **exactly one TS2344**
      naming `_minGradeIsGrade`. Measured. `_policyKeys` cannot substitute for this: it pins the
      member *set*, so a widened `minGrade` leaves it green — the dial `buildCatalog` enforces at
      build time would be silently open to any string.
    - **MUT-I4** — replace `onMissing`'s type with `{ message: string }`, dropping `reason`. Expect
      **exactly one TS2344** naming `_onMissingShape`. Measured. `_policyKeys` stays green here too,
      because the member is still present. This mutation is also the check that `_onMissingShape` was
      written with a literal right-hand side: a pin expressed via `Pick<ActionResult, …>` would still
      fire here, but would **not** fire on a change inside `ActionResult` — so if this row passes,
      re-read the assertion and confirm the RHS names the member types directly.
    - **MUT-J** — `ReadbackReceipt.canonical` → `unknown`. Expect TS2344 naming
      `_receiptCanonicalIsBytes`. Also exits 0 today.
    - **MUT-J2** — `ReadbackReceipt.hash: string` → `unknown`. Expect TS2344 naming
      `_receiptHashIsString`.

    Record all six in the SUMMARY: mutation, exit code, diagnostic codes, guard names echoed.
  </action>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge typecheck &amp;&amp; git diff --exit-code -- packages/concierge/src/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @fullselfbrowsing/concierge typecheck` exits 0
    - **Defect-first proof, MUT-I (mandatory):** `bindTo` → `string` produces TS2344 naming `_bindToIsClosed`
    - **Defect-first proof, MUT-I2 (mandatory):** a sixth `ConsentPolicy` member produces TS2344 naming `_policyKeys`
    - **Defect-first proof, MUT-I3 (mandatory):** `minGrade?: ConsentGrade` → `minGrade?: string` produces exactly one TS2344 naming `_minGradeIsGrade`
    - **Defect-first proof, MUT-I4 (mandatory):** replacing `onMissing`'s type with `{ message: string }` produces exactly one TS2344 naming `_onMissingShape`
    - `_onMissingShape`'s right-hand side spells the member types literally and contains no `Pick<ActionResult` — `grep -c 'Pick<ActionResult' packages/concierge/test-d/actions.test-d.ts` returns 0
    - **All six guards added in this task are named in at least one mutation row**, and the SUMMARY states that mapping explicitly
    - **Defect-first proof, MUT-J (mandatory):** `canonical` → `unknown` produces TS2344 naming `_receiptCanonicalIsBytes`
    - **Defect-first proof, MUT-J2 (mandatory):** `hash` → `unknown` produces TS2344 naming `_receiptHashIsString`
    - Each mutation applied and restored inside a single tool call; `git diff --exit-code -- packages/concierge/src/types.ts` exits 0 after each
    - `actions.test-d.ts` contains `_bindToIsClosed`, `_minGradeIsGrade`, `_onMissingShape`, `_policyKeys`
    - `consent.test-d.ts` contains `_receiptHashIsString` and `_receiptCanonicalIsBytes`
    - `_snapshotInferred`, `_nameNotWidened`, and `_requiresIsString` are byte-identical to their pre-task form
    - `_receiptCanonicalIsBytes` names the type that actually shipped in `types.ts`, verified by reading the declaration rather than assumed
    - Neither file exports anything: `test -z "$(grep -l '^[[:space:]]*export' packages/concierge/test-d/actions.test-d.ts packages/concierge/test-d/consent.test-d.ts)"`
  </acceptance_criteria>
  <done>The consent gate's own declaration is pinned against literal spellings rather than against itself, its member set is closed the way `Transport`'s already was, and all four receipt fields are guarded instead of two.</done>
</task>

<task type="auto">
  <name>Task 3: Pin the four required/closed contracts that have no assertion anywhere (WR-07)</name>
  <files>packages/concierge/test-d/consent.test-d.ts, packages/concierge/test-d/results.test-d.ts, packages/concierge/test-d/transport.test-d.ts</files>
  <read_first>
    - `packages/concierge/test-d/consent.test-d.ts` lines 288–303 — `_commonSnapshot` and `_commonPayload`, the two members of `ConsentAckBase` that *are* pinned, and the comment explaining why both were needed
    - `packages/concierge/test-d/transport.test-d.ts` lines 31–53 — `_metaHook`, `_batchHook`, `_batchRejectsBareId`, which test the delivery hook's *parameter* rather than the outcome union
    - `packages/concierge/test-d/results.test-d.ts` — the whole file, as plan 01-13 left it
    - `packages/concierge/src/types.ts` lines 245–273, 409–441, and the `ActionResult` declaration as plan 01-13 left it
    - `.planning/phases/01-type-surface-completion/01-REVIEW.md` § WR-07
  </read_first>
  <action>
    Four contracts, each correct in the source and each with no assertion anywhere in the suite. All
    four mutations exit 0 against the current four-file suite; that is the finding. One line each,
    placed in the file that owns the type.

    **In `consent.test-d.ts`** — `_ackCarriesTurnIdentity`, pinning `ConsentAck<Booking, null>["userTurnId"]`
    as `string`. Place it beside `_commonSnapshot` and `_commonPayload`, which pin the other two shared
    members, and extend that block's comment to say the set is now complete. Comment the stakes:
    `userTurnId` is the value `bindTo: "userTurn"` compares, so made optional an ack can arm with no
    turn identity at all and the strongest binding in the design has nothing to check.

    **In `transport.test-d.ts`** — `_deliveryOutcomeIsClosed`, pinning `DeliveryReport["outcome"]` as
    `"completed" | "interrupted"`. Place it beside `_metaHook` / `_batchHook` and say exactly what it
    adds that they do not: those two pin the hook's *parameter type*, so battery mutant M2 catches a
    hook that drops the report — but nothing catches the union inside the report widening. Widened to
    `string`, `outcome === "completed"` still compiles and every other value silently passes an
    exhaustive check. `01-VERIFICATION.md` cites this literal union as the evidence for SC-1, so it is
    a contract the phase has already claimed as verified.

    **In `results.test-d.ts`** — `_resultOkRequired` and `_resultMessageRequired`. Pin `ok` and
    `message` against the shape plan 01-13 landed. **Read the shipped declaration first:** under the
    flat shape these are `Equals<ActionResult["ok"], boolean>` and
    `Equals<ActionResult["message"], string>`; under a discriminated union `ActionResult["ok"]` is
    `boolean` (the union of the branches) and an indexed access still resolves, but write the pins
    against what is actually there and say in the comment which shape they were written for, so the
    next reader is not misled if the shape moves again. Comment the stakes: `ok` is the flag every
    dispatcher branches on, and `message` is the field `MESSAGE_MAX_CHARS` and the whole `ConsentGrade`
    ladder are written about.

    **Defect-first is mandatory.** Apply and restore **inside a single Bash tool call**, and assert the
    tree is clean afterward:

      `perl -0pi -e '<substitution>' packages/concierge/src/types.ts && pnpm --filter @fullselfbrowsing/concierge typecheck; EXIT=$?; git checkout -- packages/concierge/src/types.ts; git diff --exit-code -- packages/concierge/src/types.ts && echo "MUTANT_EXIT=$EXIT TREE_CLEAN"`

    The `/tmp` sandbox idiom is an accepted and safer alternative; if used, say so in the SUMMARY and
    still assert `git status --porcelain` is empty.

    - **MUT-G** — `ConsentAckBase.userTurnId: string` → optional. Expect TS2344 naming
      `_ackCarriesTurnIdentity`.
    - **MUT-H** — `DeliveryReport.outcome` → `string`. Expect TS2344 naming `_deliveryOutcomeIsClosed`.
    - **MUT-M** — `ActionResult.ok` → optional. Expect TS2344 naming `_resultOkRequired`.
    - **MUT-N** — `ActionResult.message` → optional. Expect TS2344 naming `_resultMessageRequired`.

    Note for MUT-G: plan 01-10 added `readonly` to `userTurnId`, so the mutation is "make it optional
    while keeping `readonly`", not "remove `readonly`". Keep the two axes separate — 01-10 owns the
    `readonly` mutation and this task owns the optionality one, and a mutation that changes both proves
    neither.

    Record all four in the SUMMARY: mutation, exit code, diagnostic codes, guard names echoed.
  </action>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge typecheck &amp;&amp; git diff --exit-code -- packages/concierge/src/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @fullselfbrowsing/concierge typecheck` exits 0
    - **Defect-first proof, MUT-G (mandatory):** `ConsentAckBase.userTurnId` made optional produces TS2344 naming `_ackCarriesTurnIdentity`
    - **Defect-first proof, MUT-H (mandatory):** `DeliveryReport.outcome` widened to `string` produces TS2344 naming `_deliveryOutcomeIsClosed`
    - **Defect-first proof, MUT-M (mandatory):** `ActionResult.ok` made optional produces TS2344 naming `_resultOkRequired`
    - **Defect-first proof, MUT-N (mandatory):** `ActionResult.message` made optional produces TS2344 naming `_resultMessageRequired`
    - Each mutation applied and restored inside a single tool call; `git diff --exit-code -- packages/concierge/src/types.ts` exits 0 after each
    - `consent.test-d.ts` contains `_ackCarriesTurnIdentity`; `transport.test-d.ts` contains `_deliveryOutcomeIsClosed`; `results.test-d.ts` contains `_resultOkRequired` and `_resultMessageRequired`
    - The two `ActionResult` pins were written against the shipped declaration, and their comment names which shape they were written for
    - No pre-existing assertion in any of the three files was modified or deleted
    - No file exports anything: `test -z "$(grep -l '^[[:space:]]*export' packages/concierge/test-d/*.test-d.ts)"`
    - `git diff --exit-code -- packages/concierge/src/index.ts` exits 0
  </acceptance_criteria>
  <done>Four contracts the phase already claimed as verified now have a detector each, every one observed red under the mutation that escaped the original suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consent policy declaration → the gate that runs | `bindTo` selects between the strong user-turn binding and the weak response binding; a typo picks silently |
| readback sink → verifier | `ReadbackReceipt.canonical` is the byte channel; a widened type lets a re-serialized value take its place |
| ack → the user-turn binding | `userTurnId` is the value `bindTo: "userTurn"` compares; without it the strongest gate has nothing to check |
| doc comment → security control | `RedactionPolicy`'s prose is what a developer reads before deciding whether they can omit a policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-44 | **Elevation of Privilege** | `ConsentPolicy.bindTo` widened to `string` | mitigate | `_bindToIsClosed` pins the literal pair, and `_policyKeys` closes the member set the way `_transportKeys` closes `Transport`'s. Widened, `bindTo: "usreTurn"` typechecks and the gate the whole design rests on is selected by a typo. Measured escaping the full suite today, because `_snapshotInferred` compares `ConsentPolicy` against itself and can only see the `Snapshot` argument. |
| T-01-45 | **Tampering** | `ReadbackReceipt.canonical` widened to `unknown` | mitigate | `_receiptCanonicalIsBytes` and `_receiptHashIsString`. `unknown` lets a sink return a re-serialized string in the field whose justification is WebAuthn's no-parse-and-reserialize rule — reopening the hazard the field exists to close. Measured escaping the full suite today; the two fields were pinned only by an object fixture, which detects removal but not widening. |
| T-01-54 | **Spoofing** | `ConsentAckBase.userTurnId` made optional | mitigate | `_ackCarriesTurnIdentity`. An ack that can arm with no turn identity leaves `bindTo: "userTurn"` — the binding that distinguishes a human act from an agent-generated follow-up — with nothing to compare. Sits beside `_commonSnapshot` and `_commonPayload`, completing the shared-member set. |
| T-01-55 | **Tampering** | `DeliveryReport.outcome` widened to an open string | mitigate | `_deliveryOutcomeIsClosed`. `01-VERIFICATION.md` cites this literal union as SC-1's evidence, but the existing guards pin the delivery hook's *parameter*, not the union inside the report. Widened, every non-`completed` value silently passes an exhaustive check and a truncated readback arms consent. |
| T-01-56 | Repudiation | `ActionResult.ok` or `message` made optional | mitigate | `_resultOkRequired` and `_resultMessageRequired`. `ok` is the flag every dispatcher branches on; `message` is the field `MESSAGE_MAX_CHARS` and the `ConsentGrade` ladder are written about. Both escaped the full suite. |
| T-01-48 | **Information Disclosure** | `RedactionPolicy`'s doc claiming a default the type cannot have | mitigate | Reword to describe the shipped required member, keep the "choose `drop` when in doubt" guidance, and name SEC-01 / Phase 3 as the enforcement point. Prose that reads "I can omit this" on a redaction policy is how telemetry leaks stop being opt-in. |
| T-01-64 | **Tampering** | a working assertion weakened to make a new one fit | mitigate | `_snapshotInferred`, `_nameNotWidened`, and `_requiresIsString` are asserted byte-identical after Task 2. `01-VALIDATION.md` records that `_requiresIsString` stays silent under M10 **by construction** and must not be "fixed" into firing; WR-04 is closed by adding pins beside `_snapshotInferred`, never by replacing it. |
| T-01-51 | Tampering | `src/index.ts` export surface changed | mitigate | Off-limits; asserted by `git diff --exit-code`. |
| T-01-58 | Tampering | a mutant left un-reverted in `types.ts` | mitigate | Every mutation applies and restores inside a single tool call; `git diff --exit-code` is an acceptance criterion on both mutation tasks. MUT-G is deliberately scoped to optionality only so it does not confound plan 01-10's `readonly` mutation. |
| T-01-SC | Tampering | npm/pnpm installs | accept | No packages installed; `pnpm-lock.yaml` unchanged. No package-manager install task exists in this plan. |
</threat_model>

<verification>
- `pnpm --filter @fullselfbrowsing/concierge typecheck` exits 0
- **Ten** mutations — MUT-I, MUT-I2, MUT-I3, MUT-I4, MUT-J, MUT-J2 (Task 2) and MUT-G, MUT-H, MUT-M, MUT-N (Task 3) — each observed non-zero naming its guard
- **Ten** guards added, and every one is named in at least one mutation row
- `git diff --exit-code -- packages/concierge/src/types.ts` exits 0 after every mutation
- `_snapshotInferred`, `_nameNotWidened`, and `_requiresIsString` are byte-identical to their pre-plan form
- `_receiptCanonicalIsBytes` and the two `ActionResult` pins name the types that actually shipped, confirmed by reading the declarations
- `grep -c 'Defaults to' packages/concierge/src/types.ts` returns 0 within the `RedactionPolicy` block
- `git diff --exit-code -- packages/concierge/src/index.ts` and `git diff --exit-code pnpm-lock.yaml` both exit 0
</verification>

<success_criteria>
The consent gate's own declaration is pinned against literal spellings and a closed member set, so a
`bindTo` typo cannot silently select the weak gate. All four receipt fields are guarded, so the
canonical bytes cannot become `unknown` and reopen the parse-and-reserialize hazard. The ack's turn
identity, the delivery outcome, and both required `ActionResult` members each have a detector. The
redaction policy's prose describes the type that shipped. Every one of the **ten** new guards was
observed red under one of ten mutations, each of which escapes the suite as it stands today.
</success_criteria>

<output>
Create `.planning/phases/01-type-surface-completion/01-14-SUMMARY.md` when done.
</output>
