---
phase: 09-react-and-svelte-adapters
plan: 09
type: execute
wave: 5
depends_on: [09-04, 09-05]
files_modified:
  - scripts/phase-09-adapter-budget.mjs
autonomous: true
requirements: [ADP-03]
must_haves:
  truths:
    - "Independent discovery exactly equals the explicit two-file React and two-file Svelte production inventories."
    - "Each adapter independently contains no more than 150 authored nonblank, non-comment production lines."
    - "Production source contains none of the five loop statement forms and none of the forbidden scheduler/cache/catalog/consent/transport/result responsibilities."
    - "Self-tests prove one negative control for each loop form plus unlisted-file and over-limit failures."
    - "Budget CLI modes are fixed and unknown/missing-path/vacuous scans fail."
  artifacts:
    - path: "scripts/phase-09-adapter-budget.mjs"
      provides: "Exact inventory, lexical LOC, TypeScript AST responsibility/loop gate, and controls"
      contains: "DoStatement"
  key_links:
    - from: "scripts/phase-09-adapter-budget.mjs"
      to: "packages/concierge-react/src/index.ts|packages/concierge-react/src/client.tsx"
      via: "closed React inventory and independent <=150 total"
      pattern: "concierge-react/src"
    - from: "scripts/phase-09-adapter-budget.mjs"
      to: "packages/concierge-svelte/src/index.ts|packages/concierge-svelte/src/client.svelte.ts"
      via: "closed Svelte inventory and independent <=150 total"
      pattern: "concierge-svelte/src"
---

<objective>
Make thin-adapter ownership mechanically enforceable through exact file discovery, independent line budgets, TypeScript AST responsibility checks, and exhaustive loop-form controls.

Purpose: ADP-03 and D-09-13 fail if code can move into an uncounted file or hide core-like control flow behind a different loop syntax.
Output: One fast deterministic budget gate with `check` and `self-test` modes.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md
@.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md
@.planning/phases/09-react-and-svelte-adapters/09-04-SUMMARY.md
@.planning/phases/09-react-and-svelte-adapters/09-05-SUMMARY.md
@packages/concierge-react/src/index.ts
@packages/concierge-react/src/client.tsx
@packages/concierge-svelte/src/index.ts
@packages/concierge-svelte/src/client.svelte.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 09-09-01: Enforce exact production inventories and independent 150-line totals</name>
  <files>scripts/phase-09-adapter-budget.mjs</files>
  <read_first>
    <file>.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md</file>
    <file>.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md</file>
    <file>packages/concierge-react/src/index.ts</file>
    <file>packages/concierge-react/src/client.tsx</file>
    <file>packages/concierge-svelte/src/index.ts</file>
    <file>packages/concierge-svelte/src/client.svelte.ts</file>
  </read_first>
  <behavior>
    - "React inventory is exactly src/index.ts plus src/client.tsx; Svelte inventory is exactly src/index.ts plus src/client.svelte.ts."
    - "Recursive independent discovery rejects every additional production extension/file and every missing enumerated file."
    - "A lexical state machine removes line/block comments without deleting comment-like string/template content."
    - "Each package reports file subtotals and an independent integer total <=150."
  </behavior>
  <action>
Create a Node script with only `check` and `self-test` modes; reject any other/missing mode. Hard-code the two exact closed inventories above, then independently recurse under each `src` and discover `.ts`, `.tsx`, `.svelte.ts`, and `.svelte` production candidates while excluding only generated maps/declarations and test/fixture directories. Normalize/sort and require exact set equality; first require every expected path is a nonempty regular file so an empty glob cannot pass.

Count authored production lines with a deterministic lexical scanner that tracks code, single/double/template strings, escapes, line comments, and block comments across lines. Count each nonblank line containing code/type syntax once; exclude only blank/comment-only lines. Emit package, explicit file list/subtotals, measured total, and limit; enforce React <=150 and Svelte <=150 separately, never a combined allowance. This implements the counting half of D-09-13 and provides the exact B1 target.
  </action>
  <verify>
    <automated>node scripts/phase-09-adapter-budget.mjs check</automated>
    <automated>node --check scripts/phase-09-adapter-budget.mjs</automated>
  </verify>
  <done>Exact independent discovery covers all four authored production files and each adapter reports/passes its own lexical <=150 line total.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09-09-02: Reject every loop statement and forbidden core responsibility with independent controls</name>
  <files>scripts/phase-09-adapter-budget.mjs</files>
  <read_first>
    <file>scripts/phase-09-adapter-budget.mjs</file>
    <file>.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md</file>
    <file>packages/concierge-react/src/client.tsx</file>
    <file>packages/concierge-svelte/src/client.svelte.ts</file>
    <file>packages/concierge/src/types.ts</file>
  </read_first>
  <behavior>
    - "TypeScript AST traversal rejects ForStatement, ForInStatement, ForOfStatement, WhileStatement, and DoStatement."
    - "Syntax-aware import/call/identifier checks reject core construction, action/catalog ownership, schedulers/timers, retry/dedupe caches, queues, consent transitions, transport routing, and result sanitation."
    - "One isolated fixture for each of the five loop forms is detected by its exact SyntaxKind."
    - "An unlisted production file and an over-limit production file each fail independently."
    - "Strings/comments containing forbidden words remain valid negative controls and do not create false positives."
  </behavior>
  <action>
Use the installed TypeScript 7 compiler API to parse every enumerated `.ts`, `.tsx`, and `.svelte.ts` file (`ScriptKind.TS`/`TSX` as appropriate) and recursively reject the five exact loop SyntaxKinds: `ForStatement`, `ForInStatement`, `ForOfStatement`, `WhileStatement`, and `DoStatement`. Also inspect imports, call expressions, new expressions, and identifiers for forbidden responsibilities: `createConcierge`, `defineAction`, catalog/stage matching, setTimeout/setInterval/scheduler/retry, dedupe/cache/queue ownership, consent state/transitions, transport dispatch/routing, or result aggregation/sanitation. Allow only context, refs/effects, guards, direct supplied registry registration/cleanup, and `$state.snapshot`; do not use regex alone.

Implement `self-test` entirely in a validated temp tree. Run separate fixtures for classic `for`, `for..in`, `for..of`, `while`, and `do..while` and require the exact SyntaxKind diagnostic for each; add one unlisted source, one over-limit listed source, one forbidden `createConcierge` call, and comment/string false-positive controls. The self-test fails if any invalid case passes or valid control fails. This completes D-09-13/T07 and makes B1 discriminating rather than a self-reported count.
  </action>
  <verify>
    <automated>node scripts/phase-09-adapter-budget.mjs self-test</automated>
    <automated>node scripts/phase-09-adapter-budget.mjs check</automated>
  </verify>
  <done>All five loop forms and every forbidden core-like responsibility are rejected by syntax-aware checks, with independent named controls for inventory and threshold bypass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| filesystem discovery → explicit inventory | File movement can hide code from budgets. |
| comments/strings → code counter | Naive text removal can miscount or be gamed. |
| source syntax → responsibility verdict | Regex can miss loop forms or flag inert prose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-07 | Tampering | ADP-03 budget/boundary | mitigate | Exact discovery equality, lexical counts, TS AST, five loop controls, unlisted/over-limit controls. |
| T-09-08 | Repudiation | budget evidence | mitigate | Fixed CLI modes, named diagnostics, self-tests, and later B1 immutable mutant. |
| T-09-SC | Tampering | parser dependency | mitigate | Reuse already-pinned root TypeScript 7; no new package install. |
</threat_model>

<verification>
Run only `check`, `self-test`, and `node --check`; each is a sub-30-second local gate. Mutation/release batteries remain in Plans 09-12/09-13.
</verification>

<success_criteria>
- ADP-03 means only independent <=150 line budgets plus forbidden-responsibility/B1 proof.
- Exact production inventories cannot omit or add a file silently.
- Every JavaScript loop statement syntax is rejected and has its own negative control.
- The gate is fast, deterministic, syntax-aware, and non-vacuous.
</success_criteria>

<output>
Create `.planning/phases/09-react-and-svelte-adapters/09-09-SUMMARY.md` when done.
</output>
