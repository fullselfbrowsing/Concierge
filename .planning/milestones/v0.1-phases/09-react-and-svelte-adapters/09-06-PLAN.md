---
phase: 09-react-and-svelte-adapters
plan: 06
type: execute
wave: 5
depends_on: [09-04, 09-05]
files_modified:
  - examples/adapter-ssr/package.json
  - examples/adapter-ssr/astro.config.mjs
  - examples/adapter-ssr/tsconfig.json
  - pnpm-lock.yaml
autonomous: true
requirements: [ADP-04]
must_haves:
  truths:
    - "The one Astro harness is private, non-publishable, and configured with official React and Svelte integrations before source is added."
    - "Astro and all Svelte-aware checking use package-local TypeScript 6.0.3 while core/React remain on root TypeScript 7."
    - "The example depends on the three live workspace package names and no foreign aliases."
    - "Fresh-process tests can select separate output directories without changing application source."
  artifacts:
    - path: "examples/adapter-ssr/package.json"
      provides: "Private Astro/app dependency and command contract"
      contains: "@fullselfbrowsing/concierge-svelte"
    - path: "examples/adapter-ssr/astro.config.mjs"
      provides: "Official dual-framework static SSR configuration"
      contains: "ADAPTER_SSR_OUT_DIR"
    - path: "examples/adapter-ssr/tsconfig.json"
      provides: "Strict local TS6 Astro program"
      contains: "astro/tsconfigs/strict"
  key_links:
    - from: "examples/adapter-ssr/astro.config.mjs"
      to: "@astrojs/react|@astrojs/svelte"
      via: "official integrations array"
      pattern: "integrations"
    - from: "examples/adapter-ssr/package.json"
      to: "packages/concierge-react|packages/concierge-svelte|packages/concierge"
      via: "workspace development links"
      pattern: "workspace:\*"
---

<objective>
Land the Astro manifest, configuration, TypeScript domain, integrations, and lockfile before any example source or SSR build is attempted.

Purpose: Dependency/configuration ordering makes the subsequent normal build deterministic and keeps the Svelte/Astro TS6 boundary explicit.
Output: A private `examples/adapter-ssr` toolchain skeleton ready for the real two-island source plan.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md
@.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md
@.planning/phases/09-react-and-svelte-adapters/09-04-SUMMARY.md
@.planning/phases/09-react-and-svelte-adapters/09-05-SUMMARY.md
@pnpm-workspace.yaml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 09-06-01: Define the private dual-framework Astro configuration and local TS6 domain</name>
  <files>examples/adapter-ssr/package.json, examples/adapter-ssr/astro.config.mjs, examples/adapter-ssr/tsconfig.json</files>
  <read_first>
    <file>.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md</file>
    <file>.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md</file>
    <file>packages/concierge/package.json</file>
    <file>packages/concierge-react/package.json</file>
    <file>packages/concierge-svelte/package.json</file>
    <file>pnpm-workspace.yaml</file>
  </read_first>
  <action>
Create private package `@fullselfbrowsing/concierge-adapter-ssr` under the already-declared `examples/*` workspace glob. Add scripts `check: astro check` and `build: astro build`; add workspace development links for `@fullselfbrowsing/concierge`, `@fullselfbrowsing/concierge-react`, and `@fullselfbrowsing/concierge-svelte`; pin Astro 7.2.0, official React 6.0.2/Svelte 9.0.1 integrations, Astro check 0.9.10, and local TypeScript 6.0.3. Keep it private and omit publish/files metadata.

Configure `output: "static"`, official `react()` and `svelte()` integrations, and an output directory selected only from validated `ADAPTER_SSR_OUT_DIR` or a local default so the next plan can build twice into separate roots. Extend `astro/tsconfigs/strict` and keep local TS6; do not force root TS7, add an experimental Container API, add browser adapters, create source/pages, or run Astro check/build yet. This is the configuration-first half of D-09-14 and D-09-15.
  </action>
  <verify>
    <automated>node -e 'const fs=require("node:fs");const p=JSON.parse(fs.readFileSync("examples/adapter-ssr/package.json","utf8"));if(!p.private||p.devDependencies?.typescript!=="6.0.3")process.exit(1);for(const n of ["@fullselfbrowsing/concierge","@fullselfbrowsing/concierge-react","@fullselfbrowsing/concierge-svelte","astro","@astrojs/react","@astrojs/svelte"])if(!p.devDependencies?.[n])process.exit(1)'</automated>
    <automated>node -e 'const fs=require("node:fs");const c=fs.readFileSync("examples/adapter-ssr/astro.config.mjs","utf8"),t=fs.readFileSync("examples/adapter-ssr/tsconfig.json","utf8");for(const x of ["@astrojs/react","@astrojs/svelte","ADAPTER_SSR_OUT_DIR","output: \"static\""])if(!c.includes(x))process.exit(1);if(!t.includes("astro/tsconfigs/strict"))process.exit(1)'</automated>
  </verify>
  <done>The one private Astro package has official integrations, exact live workspace dependencies, strict local TS6, and fresh-output support before any source/build exists.</done>
</task>

<task type="auto">
  <name>Task 09-06-02: Reconcile and verify the Astro/integration lock importer</name>
  <files>pnpm-lock.yaml</files>
  <read_first>
    <file>examples/adapter-ssr/package.json</file>
    <file>examples/adapter-ssr/astro.config.mjs</file>
    <file>pnpm-lock.yaml</file>
    <file>pnpm-workspace.yaml</file>
  </read_first>
  <action>
Run a normal workspace install once and inspect the `examples/adapter-ssr` importer. Require exact Astro/integration/local-TS6 pins and workspace links to all three packages; reject registry aliases for the local packages or a root TS7 substitution. Prove the installed Astro CLI and both integration modules resolve from this example, but do not execute check/build until Plan 09-07 creates the real source tree. Reserve the frozen reinstall for plan-close.
  </action>
  <verify>
    <automated>pnpm --dir examples/adapter-ssr exec astro --version | rg -F 'astro  v7.2.0'</automated>
    <automated>node -e 'const fs=require("node:fs");const s=fs.readFileSync("pnpm-lock.yaml","utf8");if(!s.includes("examples/adapter-ssr:")||!s.includes("@astrojs/react")||!s.includes("@astrojs/svelte")||!s.includes("6.0.3"))process.exit(1)'</automated>
  </verify>
  <done>The example importer resolves its official integrations, all three workspace packages, and local TS6 exactly, with no source/build performed out of order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| workspace packages → Astro renderer | A registry alias/workspace miss can test different bytes than the adapters. |
| root TS7 → Astro/Svelte checkers | Unsupported peer forcing can make the harness nondeterministic. |
| output directory input → filesystem | Fresh-process isolation must not permit broad or escaped write targets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-04 | Information Disclosure | SSR harness setup | mitigate | Official static renderers, request isolation prepared, no source/server effects yet. |
| T-09-06 | Tampering | integration compiler domain | mitigate | Exact first-party integrations with local TS6. |
| T-09-08 | Denial of Service | output paths | mitigate | Accept only validated owned output roots; actual temp cleanup is in 09-07. |
| T-09-SC | Tampering | Astro packages | mitigate | All exact packages are `[VERIFIED]` in research and locked. |
</threat_model>

<verification>
After task checks, run `pnpm install --frozen-lockfile` and repeat CLI/importer assertions. Absence of `src/` is expected here; no missing-source command may be interpreted as a passing SSR test.
</verification>

<success_criteria>
- Astro config/dependencies/lockfile precede all example source and build work.
- One private example uses both official integrations and three live workspace package names.
- Local TypeScript 6.0.3 owns Astro/Svelte checks; root TS7 is unchanged.
- No UI, source page, or SSR evidence is claimed by this plan.
</success_criteria>

<output>
Create `.planning/phases/09-react-and-svelte-adapters/09-06-SUMMARY.md` when done.
</output>
