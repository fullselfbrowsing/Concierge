---
phase: 09-react-and-svelte-adapters
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - packages/concierge-svelte/package.json
  - packages/concierge-svelte/tsconfig.json
  - packages/concierge-svelte/svelte.config.js
  - packages/concierge-svelte/LICENSE
  - pnpm-lock.yaml
autonomous: true
requirements: [ADP-02, PKG-04]
must_haves:
  truths:
    - "The Svelte package declares live core only as a workspace peer plus development link and Svelte ^5 as its framework peer."
    - "Svelte owns package-local TypeScript 6.0.3 and builds only with svelte-package, never tsdown."
    - "After root, React, and Svelte skeleton responsibilities land, the static checker reports exactly the predefined reduced eight IDs."
  artifacts:
    - path: "packages/concierge-svelte/package.json"
      provides: "Publish, peer, local TS6, and svelte-package contract"
      contains: "@sveltejs/package"
    - path: "packages/concierge-svelte/svelte.config.js"
      provides: "Owning Svelte preprocessing configuration"
      contains: "vitePreprocess"
    - path: "packages/concierge-svelte/tsconfig.json"
      provides: "Package-local Svelte/TS6 checking program"
      contains: "bundler"
  key_links:
    - from: "packages/concierge-svelte/package.json"
      to: "packages/concierge/package.json"
      via: "workspace:^ peer and workspace:* development link"
      pattern: "@fullselfbrowsing/concierge"
    - from: "packages/concierge-svelte/package.json"
      to: "packages/concierge-svelte/svelte.config.js"
      via: "svelte-package and svelte-check scripts"
      pattern: "svelte-package"
---

<objective>
Create the bounded Svelte package/toolchain skeleton and prove the exact eleven-to-eight RED transition before adapter runtime implementation begins.

Purpose: Rune compilation and the local TS6 boundary must be fixed in the package contract rather than discovered while implementing consent behavior.
Output: A publish-ready Svelte manifest/config/license skeleton, reconciled lockfile, and exact post-skeleton checker result.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md
@.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md
@.planning/phases/09-react-and-svelte-adapters/09-02-SUMMARY.md
@packages/concierge/package.json
@packages/concierge/src/types.ts
@pnpm-workspace.yaml
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 09-03-01: Define the Svelte peer, rune packaging, and local TS6 contracts</name>
  <files>packages/concierge-svelte/package.json, packages/concierge-svelte/tsconfig.json, packages/concierge-svelte/svelte.config.js, packages/concierge-svelte/LICENSE</files>
  <read_first>
    <file>.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md</file>
    <file>.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md</file>
    <file>packages/concierge/package.json</file>
    <file>packages/concierge/src/types.ts</file>
    <file>packages/concierge/LICENSE</file>
  </read_first>
  <action>
Create `@fullselfbrowsing/concierge-svelte` as ESM-only and side-effect-free with root and `./client.svelte` export maps. Each public entry must expose `types`, `svelte`, `import`, and `default` conditions to future unbundled `dist` files. Allow only `dist`, `src`, `README.md`, and `LICENSE`. Declare `@fullselfbrowsing/concierge: workspace:^` and `svelte: ^5.0.0` as peers; add core `workspace:*`, Svelte 5.56.8, `@sveltejs/package` 2.5.8, `svelte-check` 4.7.5, `@testing-library/svelte` 5.4.2, the Svelte Vite plugin 7.2.0, and package-local `typescript` 6.0.3 as development dependencies. Core must never appear in `dependencies` or bundled metadata per D-09-04.

Define scripts `build: svelte-package`, `typecheck: svelte-check --tsconfig ./tsconfig.json`, and the exact root `svelte-lifecycle` test project. Configure `vitePreprocess()` in the owning Svelte config and a strict bundler-resolution TS program covering future `src/**/*.ts`, `src/**/*.svelte.ts`, `test/**/*.ts`, and `test/**/*.svelte`; do not extend or invoke root TS7 from Svelte package checks. Do not add tsdown, a bundling config, `createContext`, or source stubs. Copy core's license byte-for-byte. This fixes D-09-09, D-09-10, and D-09-11 before implementation.
  </action>
  <verify>
    <automated>node -e 'const fs=require("node:fs");const p=JSON.parse(fs.readFileSync("packages/concierge-svelte/package.json","utf8"));if(p.name!=="@fullselfbrowsing/concierge-svelte"||p.scripts?.build!=="svelte-package"||p.devDependencies?.typescript!=="6.0.3"||p.peerDependencies?.["@fullselfbrowsing/concierge"]!=="workspace:^"||p.dependencies?.["@fullselfbrowsing/concierge"]||JSON.stringify(p).includes("tsdown"))process.exit(1)'</automated>
    <automated>node -e 'const fs=require("node:fs");const c=fs.readFileSync("packages/concierge-svelte/svelte.config.js","utf8"),t=fs.readFileSync("packages/concierge-svelte/tsconfig.json","utf8");if(!c.includes("vitePreprocess")||!t.includes("src/**/*.svelte.ts")||!fs.readFileSync("packages/concierge-svelte/LICENSE").equals(fs.readFileSync("packages/concierge/LICENSE")))process.exit(1)'</automated>
  </verify>
  <done>The Svelte package has live names, peer-only core, Svelte ^5, explicit framework export conditions, local TS6, and an owning svelte-package toolchain with no runtime source or bundler.</done>
</task>

<task type="auto">
  <name>Task 09-03-02: Lock the Svelte toolchain and prove the exact post-skeleton RED set</name>
  <files>pnpm-lock.yaml</files>
  <read_first>
    <file>packages/concierge-svelte/package.json</file>
    <file>packages/concierge-svelte/tsconfig.json</file>
    <file>pnpm-lock.yaml</file>
    <file>scripts/phase-09-contract-check.mjs</file>
    <file>.planning/phases/09-react-and-svelte-adapters/09-RED-BASELINE.json</file>
  </read_first>
  <action>
Reconcile the workspace lockfile with the Svelte package's exact `[VERIFIED]` dependencies and inspect its importer. The local importer must select TypeScript 6.0.3 for Svelte/package/check tooling while root stays 7.0.2, and it must resolve core as a peer plus workspace development link. Then run the already-implemented `post-skeleton` checker mode. It must report exactly the predefined eight IDs—React runtime/artifact, Svelte runtime/artifact, Astro SSR, exact tarball, budget, mutation closure—and nothing else. Do not alter the eleven-ID baseline record to manufacture this reduction; `baseline-verify` must still pass independently.
  </action>
  <verify>
    <automated>node scripts/phase-09-contract-check.mjs baseline-verify</automated>
    <automated>node scripts/phase-09-contract-check.mjs post-skeleton</automated>
    <automated>node -e 'const fs=require("node:fs");const s=fs.readFileSync("pnpm-lock.yaml","utf8");if(!s.includes("packages/concierge-svelte:")||!s.includes("typescript:")||!s.includes("6.0.3"))process.exit(1)'</automated>
  </verify>
  <done>The Svelte importer is lockfile-complete in its TS6 domain, the original eleven-ID record is unchanged, and current tree state is exactly the declared eight-ID post-skeleton baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Svelte source → published output | A generic bundler can erase rune reactivity while still producing runnable code. |
| root TS7 → Svelte tooling | Forcing an unsupported compiler peer can hide invalid or unstable checks. |
| skeleton state → RED transition | Adding stubs or rewriting the baseline can create a false GREEN transition. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-03 | Tampering | snapshot compiler seam | mitigate | Svelte-aware source/exports and later exact packed real-rune proof. |
| T-09-05 | Elevation of Privilege | core peer topology | mitigate | Exact manifest and lock importer assertions. |
| T-09-06 | Tampering | Svelte transform | mitigate | `svelte-package` only, framework export condition, local TS6 checker. |
| T-09-08 | Repudiation | RED transition | mitigate | Immutable eleven-ID record plus independent exact eight-ID current evaluation. |
| T-09-SC | Tampering | Svelte dependencies | mitigate | Only `[VERIFIED]` exact pins from the legitimacy audit and frozen lockfile. |
</threat_model>

<verification>
After focused checks, run `pnpm install --frozen-lockfile`, both checker modes, and the two manifest/config assertions. Do not run a Svelte build while the source remains deliberately missing.
</verification>

<success_criteria>
- Svelte builds only with svelte-package and checks only with local TypeScript 6.0.3.
- Core is peer+dev only; Svelte is an honest ^5 peer.
- Root and `./client.svelte` have framework-aware export conditions.
- Baseline remains eleven persisted IDs while current post-skeleton state is exactly eight IDs.
</success_criteria>

<output>
Create `.planning/phases/09-react-and-svelte-adapters/09-03-SUMMARY.md` when done.
</output>
