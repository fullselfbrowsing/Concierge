---
phase: 09-react-and-svelte-adapters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase-09-contract-check.mjs
  - .planning/phases/09-react-and-svelte-adapters/09-RED-BASELINE.json
  - package.json
  - vitest.config.ts
  - pnpm-lock.yaml
autonomous: true
requirements: [ADP-01, ADP-02, ADP-03, ADP-04, PKG-04]
must_haves:
  truths:
    - "The untouched adapter baseline is persisted as exactly eleven named missing-contract IDs before any package skeleton lands."
    - "The checker has distinct baseline-record, baseline-verify, post-skeleton, final, and self-test modes with fixed cardinalities; no later plan invokes an undeclared mode."
    - "Core tests remain in a DOM-free Node project, adapter artifact/Astro SSR tests have a separate Node project, and React/Svelte lifecycle tests have dedicated framework projects."
    - "Every Vitest command used for evidence rejects zero collected files or tests."
  artifacts:
    - path: "scripts/phase-09-contract-check.mjs"
      provides: "Exact RED transition and final static-contract checker"
      contains: "baseline-record"
    - path: ".planning/phases/09-react-and-svelte-adapters/09-RED-BASELINE.json"
      provides: "Persisted exact eleven-ID initial baseline with source digest"
      contains: "C09-11-MUTATION-CLOSURE"
    - path: "vitest.config.ts"
      provides: "Four non-overlapping test projects"
      contains: "node-artifact-ssr"
  key_links:
    - from: "vitest.config.ts"
      to: "packages/concierge/test/**/*.test.ts"
      via: "DOM-free node project exact include"
      pattern: "name:.*node"
    - from: "scripts/phase-09-contract-check.mjs"
      to: ".planning/phases/09-react-and-svelte-adapters/09-RED-BASELINE.json"
      via: "exact ordered ID set and digest verification"
      pattern: "11"
---

<objective>
Persist the reproducible eleven-failure Phase 09 baseline and establish exact, non-overlapping test routing before either adapter package exists.

Purpose: Every later GREEN transition and mutation kill needs a fixed starting contract, while the existing core suite must never inherit DOM/framework transforms.
Output: One static contract checker with all lifecycle modes declared up front, an immutable baseline record, root test dependencies, and explicit Vitest projects.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md
@.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md
@.planning/phases/09-react-and-svelte-adapters/09-VALIDATION.md
@package.json
@vitest.config.ts
@pnpm-workspace.yaml
@packages/concierge/package.json
@packages/concierge/src/index.ts
@packages/concierge/src/bridge.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 09-01-01: Persist the exact eleven-ID RED baseline and checker state machine</name>
  <files>scripts/phase-09-contract-check.mjs, .planning/phases/09-react-and-svelte-adapters/09-RED-BASELINE.json</files>
  <read_first>
    <file>.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md</file>
    <file>.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md</file>
    <file>.planning/phases/09-react-and-svelte-adapters/09-VALIDATION.md</file>
    <file>packages/concierge/src/index.ts</file>
    <file>vitest.config.ts</file>
  </read_first>
  <behavior>
    - "`baseline-record` succeeds only when the current tree produces exactly the ordered eleven IDs and writes their digest plus baseline input hashes."
    - "`baseline-verify` checks the persisted record without reevaluating the now-changing tree."
    - "`post-skeleton` succeeds only for the exact reduced eight-ID set after Plans 09-01 through 09-03."
    - "`final` succeeds only when no contract ID remains and every required path exists; a missing path is always a failure, never an empty successful search."
    - "`self-test` proves an unknown mode, duplicate ID, wrong cardinality, missing required path, and vacuous zero-match probe all fail."
  </behavior>
  <action>
Create a Node checker with exactly these modes: `baseline-record`, `baseline-verify`, `post-skeleton`, `final`, and `self-test`; reject every other argument. Define the initial ordered IDs as `C09-01-VITEST-ROUTING`, `C09-02-REACT-SKELETON`, `C09-03-REACT-RUNTIME`, `C09-04-REACT-ARTIFACT`, `C09-05-SVELTE-SKELETON`, `C09-06-SVELTE-RUNTIME`, `C09-07-SVELTE-ARTIFACT`, `C09-08-ASTRO-SSR`, `C09-09-EXACT-TARBALL`, `C09-10-ADAPTER-BUDGET`, and `C09-11-MUTATION-CLOSURE`. `baseline-record` must evaluate exactly those eleven before skeleton edits, require cardinality 11 and uniqueness, then atomically write `09-RED-BASELINE.json` with schema version, ordered IDs, SHA-256 over the IDs and inspected baseline paths, and a timestamp. Do not make the checker itself one of the missing contracts.

Define the post-skeleton set now as the exact ordered eight IDs `C09-03`, `C09-04`, `C09-06`, `C09-07`, `C09-08`, `C09-09`, `C09-10`, and `C09-11` using the full names above. `baseline-verify` validates only the immutable record/digest; `post-skeleton` reevaluates the tree and demands exactly those eight; `final` demands zero and additionally performs explicit `existsSync`/file checks before reading every final source, package, example, script, evidence, validation, and security artifact. Implement D-09-01 through D-09-17 as concrete static fingerprints (live package names/paths, `createBridge`, lifecycle placement, literal guard, peer topology, directive, rune normalizer, Astro, exact-tarball script, budget, ledgers), not comments-only counts. Every grep-like content assertion first proves the target is a regular nonempty file. `self-test` operates only on disposable synthetic fixtures and proves the state machine cannot pass vacuously.
  </action>
  <verify>
    <automated>node scripts/phase-09-contract-check.mjs baseline-record</automated>
    <automated>node scripts/phase-09-contract-check.mjs baseline-verify</automated>
    <automated>node scripts/phase-09-contract-check.mjs self-test</automated>
  </verify>
  <done>The initial tree is durably recorded as exactly eleven named failures, all future checker modes and expected transitions are implemented now, and vacuous/missing-path cases are proven red.</done>
</task>

<task type="auto">
  <name>Task 09-01-02: Route core, artifact/SSR, React, and Svelte tests into explicit projects</name>
  <files>package.json, vitest.config.ts, pnpm-lock.yaml</files>
  <read_first>
    <file>package.json</file>
    <file>vitest.config.ts</file>
    <file>pnpm-workspace.yaml</file>
    <file>.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md</file>
    <file>scripts/phase-09-contract-check.mjs</file>
  </read_first>
  <action>
Keep the existing project name `node`, environment `node`, and narrow its include to `packages/concierge/test/**/*.test.ts` so core remains DOM-free. Add `node-artifact-ssr` with environment `node` and exactly three includes: `packages/concierge-react/test/artifact.test.ts`, `packages/concierge-svelte/test/artifact.test.ts`, and `examples/adapter-ssr/test/ssr.test.ts`. Add `react-lifecycle` with jsdom, `@vitejs/plugin-react`, and exact include `packages/concierge-react/test/lifecycle.test.tsx`; add `svelte-lifecycle` with jsdom, `@sveltejs/vite-plugin-svelte` configured as `svelte({ hot: false })`, and exact include `packages/concierge-svelte/test/lifecycle.test.ts`. Ensure the four include sets are pairwise disjoint and no broad `packages/*/test` glob remains.

Add exact root development pins `vite@8.1.5`, `@vitejs/plugin-react@5.2.0`, `@sveltejs/vite-plugin-svelte@7.2.0`, `svelte@5.56.8`, and `jsdom@29.1.1`, all `[VERIFIED]` in Phase 09 research. Reconcile `pnpm-lock.yaml` with a normal install, but reserve the frozen reinstall for plan-close verification. Do not add DOM libraries to the core TypeScript program, change root TypeScript 7.0.2, use `passWithNoTests`, or run lifecycle suites in Node. This enacts D-09-02, D-09-07, D-09-09, D-09-11, and the T-09-04/T-09-06 routing boundary.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project node packages/concierge/test/bridge.test.ts --reporter=json | node -e 'let s="";process.stdin.on("data",d=&gt;s+=d).on("end",()=&gt;{const j=JSON.parse(s);if(!j.success||j.numTotalTestSuites&lt;=0||j.numTotalTests&lt;=0||j.testResults.length&lt;=0)process.exit(1)})'</automated>
    <automated>node -e 'const fs=require("node:fs");const s=fs.readFileSync("vitest.config.ts","utf8");for(const x of ["node-artifact-ssr","react-lifecycle","svelte-lifecycle","packages/concierge/test/**/*.test.ts"])if(!s.includes(x))process.exit(1);if(s.includes("packages/*/test/**/*.test.ts")||s.includes("passWithNoTests"))process.exit(1)'</automated>
  </verify>
  <done>Core stays in its existing DOM-free Node project, artifact/SSR and both framework lifecycles have exact disjoint routes, and a real core test run reports positive suite/test/file counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| tree state → RED ledger | A changing skeleton must not rewrite or reinterpret the initial eleven failures. |
| include globs → test projects | An overly broad or empty route can make framework tests run under the wrong environment or not run at all. |
| command exit → evidence | Exit zero without collected tests is not evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-04 | Information Disclosure | SSR/artifact routing | mitigate | Dedicated Node artifact/SSR project; lifecycle suites cannot match it. |
| T-09-06 | Tampering | framework transforms | mitigate | Dedicated plugin-backed React/Svelte projects with exact includes. |
| T-09-08 | Repudiation | RED and test evidence | mitigate | Fixed ID/cardinality digests, explicit file existence, positive JSON counts, and self-tests. |
| T-09-SC | Tampering | dependency installation | mitigate | Install only `[VERIFIED]` exact pins from the Phase 09 package legitimacy audit with the workspace lockfile. |
</threat_model>

<verification>
Run the three task commands, then `pnpm install --frozen-lockfile` and rerun the positive-count core command. `baseline-verify` must still report the immutable eleven-ID record after routing changes; do not invoke `post-skeleton` until Plans 09-02 and 09-03 exist.
</verification>

<success_criteria>
- The exact eleven-ID baseline is persisted and reproducible independently of later tree state.
- Only five declared checker modes exist; all unknown/vacuous cases fail.
- Four Vitest projects have disjoint exact includes and appropriate environments/plugins.
- The core Node project collects and passes a positive number of files/tests without DOM dependencies.
</success_criteria>

<output>
Create `.planning/phases/09-react-and-svelte-adapters/09-01-SUMMARY.md` when done.
</output>
