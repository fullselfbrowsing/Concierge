---
phase: 09-react-and-svelte-adapters
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - packages/concierge-react/package.json
  - packages/concierge-react/tsconfig.json
  - packages/concierge-react/tsdown.config.ts
  - packages/concierge-react/LICENSE
  - pnpm-lock.yaml
autonomous: true
requirements: [ADP-01, PKG-04]
must_haves:
  truths:
    - "The React package declares @fullselfbrowsing/concierge only as a workspace peer plus development link."
    - "The root and /client entries are distinct, ESM-only, and only the built client entry receives one explicit client directive."
    - "React supports peers ^18.2.0 or ^19.0.0 while development uses the exact researched React 19 toolchain."
  artifacts:
    - path: "packages/concierge-react/package.json"
      provides: "Publish and peer contract for @fullselfbrowsing/concierge-react"
      contains: "@fullselfbrowsing/concierge"
    - path: "packages/concierge-react/tsdown.config.ts"
      provides: "Two-entry ESM build with fileName-sensitive client banner"
      contains: "fileName"
    - path: "packages/concierge-react/tsconfig.json"
      provides: "Strict TypeScript 7 React package program"
      contains: "jsx"
  key_links:
    - from: "packages/concierge-react/package.json"
      to: "packages/concierge/package.json"
      via: "workspace:^ peer and workspace:* development link"
      pattern: "@fullselfbrowsing/concierge"
    - from: "packages/concierge-react/tsdown.config.ts"
      to: "dist/client.js"
      via: "client-only banner callback"
      pattern: "use client"
---

<objective>
Create the bounded React package/build skeleton without implementing lifecycle behavior or merging it into the oversized root setup plan.

Purpose: The package graph, entry split, TypeScript domain, and directive transform are contracts that the React runtime can implement against deterministically.
Output: A publish-ready manifest/config/license skeleton and reconciled lockfile for `@fullselfbrowsing/concierge-react`.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md
@.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md
@.planning/phases/09-react-and-svelte-adapters/09-01-SUMMARY.md
@packages/concierge/package.json
@packages/concierge/tsconfig.json
@packages/concierge/tsdown.config.ts
@package.json
@pnpm-workspace.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 09-02-01: Define the React peer, entry, compiler, and directive contracts</name>
  <files>packages/concierge-react/package.json, packages/concierge-react/tsconfig.json, packages/concierge-react/tsdown.config.ts, packages/concierge-react/LICENSE</files>
  <read_first>
    <file>.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md</file>
    <file>.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md</file>
    <file>packages/concierge/package.json</file>
    <file>packages/concierge/tsdown.config.ts</file>
    <file>packages/concierge/LICENSE</file>
  </read_first>
  <action>
Create `@fullselfbrowsing/concierge-react` as private false, ESM-only, side-effect-free, with package root and `./client` export maps pointing to future `dist/index` and `dist/client` types/default files. Allow only `dist`, `src`, `README.md`, and `LICENSE` in the package. Declare `@fullselfbrowsing/concierge: workspace:^` in `peerDependencies` and `workspace:*` in `devDependencies`, never `dependencies`/bundled dependencies, per D-09-04. Declare React/React DOM peer range `^18.2.0 || ^19.0.0`; pin React/React DOM 19.2.8, their researched types, and Testing Library React 16.3.2 as development dependencies. Scripts must name `build: tsdown`, strict `typecheck`, and the exact root `react-lifecycle` project; do not add a second runtime wrapper or call `createConcierge`.

Extend the root strict TypeScript 7 config for TSX/source and type-test inputs. Configure one tsdown build with entries `src/index.ts` and `src/client.tsx`, ESM/neutral/dts output, React/core externals, and the resolved `banner` callback: return `"use client";` only when `fileName` is `client.js` or ends in `/client.js`, otherwise return `undefined`. This deliberately avoids relying on incidental directive preservation and never banners the root/core entry per D-09-08. Copy the existing core license byte-for-byte. Source is intentionally absent until Plan 09-04; do not add throwing runtime stubs that would falsify the exact post-skeleton RED set.
  </action>
  <verify>
    <automated>node -e 'const fs=require("node:fs");const p=JSON.parse(fs.readFileSync("packages/concierge-react/package.json","utf8"));if(p.name!=="@fullselfbrowsing/concierge-react"||p.peerDependencies?.["@fullselfbrowsing/concierge"]!=="workspace:^"||p.devDependencies?.["@fullselfbrowsing/concierge"]!=="workspace:*"||p.dependencies?.["@fullselfbrowsing/concierge"]||!p.exports?.["./client"])process.exit(1)'</automated>
    <automated>node -e 'const fs=require("node:fs");const s=fs.readFileSync("packages/concierge-react/tsdown.config.ts","utf8");for(const x of ["src/index.ts","src/client.tsx","fileName","use client"])if(!s.includes(x))process.exit(1);if(!fs.readFileSync("packages/concierge-react/LICENSE").equals(fs.readFileSync("packages/concierge/LICENSE")))process.exit(1)'</automated>
  </verify>
  <done>The React package has the exact live namespace, peer/dev topology, two-entry ESM contract, explicit client-only banner, strict TS7 config, and matching license without any runtime implementation.</done>
</task>

<task type="auto">
  <name>Task 09-02-02: Reconcile and freeze the React skeleton dependency graph</name>
  <files>pnpm-lock.yaml</files>
  <read_first>
    <file>packages/concierge-react/package.json</file>
    <file>package.json</file>
    <file>pnpm-workspace.yaml</file>
    <file>pnpm-lock.yaml</file>
    <file>scripts/phase-09-contract-check.mjs</file>
  </read_first>
  <action>
Run the normal workspace install once to reconcile the lockfile for the new package and exact `[VERIFIED]` React development pins. Inspect the lock importer for `packages/concierge-react`: core must resolve as both peer and development workspace link, React framework packages must satisfy the declared peer range, and no core tarball/runtime dependency may be nested under the adapter. Keep root TypeScript at 7.0.2. Do not run the full adapter build or lifecycle suite while runtime source is deliberately absent; preserve the eleven-ID baseline record with `baseline-verify`.
  </action>
  <verify>
    <automated>node scripts/phase-09-contract-check.mjs baseline-verify</automated>
    <automated>node -e 'const fs=require("node:fs");const s=fs.readFileSync("pnpm-lock.yaml","utf8");if(!s.includes("packages/concierge-react:")||!s.includes("@fullselfbrowsing/concierge"))process.exit(1)'</automated>
  </verify>
  <done>The lockfile contains the React importer and exact development toolchain without a bundled/nested core, while the persisted initial RED baseline remains unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| adapter manifest → consumer graph | An ordinary core dependency can create duplicate singleton authority. |
| source entry → built client artifact | Transforming or spreading the directive can break server/client boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-05 | Elevation of Privilege | core dependency topology | mitigate | Exact peer+dev manifest checks and lock importer inspection. |
| T-09-06 | Tampering | React client directive | mitigate | Installed tsdown banner callback targets only client.js; artifact proof follows in 09-04/09-08. |
| T-09-SC | Tampering | React packages | mitigate | Use only `[VERIFIED]` exact development pins and a frozen workspace lockfile. |
</threat_model>

<verification>
After task-local checks, run `pnpm install --frozen-lockfile`, repeat both manifest/config assertions, and confirm `git diff -- pnpm-lock.yaml` represents only the declared root/React importer changes.
</verification>

<success_criteria>
- React package name and every core/framework dependency key use the live namespace.
- Core is peer+dev only and external to the two-entry build.
- Only `dist/client.js` is deliberately bannered with one client directive.
- The reconciled lockfile installs frozen without changing the initial RED record.
</success_criteria>

<output>
Create `.planning/phases/09-react-and-svelte-adapters/09-02-SUMMARY.md` when done.
</output>
