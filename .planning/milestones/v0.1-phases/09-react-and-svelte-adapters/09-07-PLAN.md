---
phase: 09-react-and-svelte-adapters
plan: 07
type: execute
wave: 6
depends_on: [09-06]
files_modified:
  - examples/adapter-ssr/src/shared/catalog.ts
  - examples/adapter-ssr/src/components/ReactIsland.tsx
  - examples/adapter-ssr/src/components/SvelteIsland.svelte
  - examples/adapter-ssr/src/pages/index.astro
  - examples/adapter-ssr/test/ssr.test.ts
autonomous: true
requirements: [ADP-04]
must_haves:
  truths:
    - "One normal Astro page server-renders both real built adapters from one immutable shared action/catalog declaration."
    - "Each render constructs fresh request-local Concierge and createBridge registry pairs; no mutable app instance lives at module scope."
    - "Browser globals are absent and both registries remain null after React and Svelte server rendering."
    - "Two fresh-process normal check/build runs produce deterministic catalog evidence and distinct request identities without leaked registration."
    - "The harness contains deterministic text/evidence only and no styling, UI system, controls, or visual gate."
  artifacts:
    - path: "examples/adapter-ssr/src/shared/catalog.ts"
      provides: "Shared immutable declarations and request-local core/registry factory"
      contains: "createBridge"
    - path: "examples/adapter-ssr/src/pages/index.astro"
      provides: "One normal dual-framework SSR route and machine evidence"
      contains: "adapter-ssr-evidence"
    - path: "examples/adapter-ssr/test/ssr.test.ts"
      provides: "T04 repeated fresh-process normal build detector"
      contains: "ASTRO_SSR_EVIDENCE"
  key_links:
    - from: "examples/adapter-ssr/src/components/ReactIsland.tsx"
      to: "@fullselfbrowsing/concierge-react/client"
      via: "injected Concierge/BridgeRegistry/Bridge props"
      pattern: "ConciergeProvider"
    - from: "examples/adapter-ssr/src/components/SvelteIsland.svelte"
      to: "@fullselfbrowsing/concierge-svelte/client.svelte"
      via: "native context/effect helpers with injected props"
      pattern: "useConciergeBridge"
    - from: "examples/adapter-ssr/test/ssr.test.ts"
      to: "examples/adapter-ssr/src/pages/index.astro"
      via: "two fresh child-process Astro builds and parsed HTML evidence"
      pattern: "registries=null"
---

<objective>
Build the one deterministic normal-Astro SSR harness and prove both adapters remain lifecycle-silent across repeated fresh processes.

Purpose: ADP-04 and D-09-14/15 require real metaframework rendering, shared catalog equivalence, request-local construction, and empty registries—not a Node import shortcut.
Output: One headless Astro page, two real framework islands, and a positive-count repeated-build SSR suite.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md
@.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md
@.planning/phases/09-react-and-svelte-adapters/09-06-SUMMARY.md
@packages/concierge/src/index.ts
@packages/concierge-react/src/client.tsx
@packages/concierge-svelte/src/client.svelte.ts
@examples/adapter-ssr/package.json
@examples/adapter-ssr/astro.config.mjs

<interfaces>
The example—not either adapter—uses public `defineAction`, `createConcierge`, and `createBridge`. It passes the resulting existing Concierge, BridgeRegistry, and ordinary Bridge to framework components. Both adapter imports use their client subpaths; neither component constructs core/catalog state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 09-07-01: Render two real injected islands from one shared catalog declaration</name>
  <files>examples/adapter-ssr/src/shared/catalog.ts, examples/adapter-ssr/src/components/ReactIsland.tsx, examples/adapter-ssr/src/components/SvelteIsland.svelte, examples/adapter-ssr/src/pages/index.astro</files>
  <read_first>
    <file>.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md</file>
    <file>.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md</file>
    <file>packages/concierge/src/index.ts</file>
    <file>packages/concierge-react/src/client.tsx</file>
    <file>packages/concierge-svelte/src/client.svelte.ts</file>
    <file>examples/adapter-ssr/astro.config.mjs</file>
  </read_first>
  <behavior>
    - "Normal Astro check/build imports both official integrations and both adapter client entries."
    - "Both islands receive constructed instances/registries/bridges; neither calls createConcierge/createBridge."
    - "One shared immutable declaration produces deeply equal catalog projections for React and Svelte."
    - "The page evidence reports browser globals absent, request identities distinct, and both post-render registries null."
  </behavior>
  <action>
Define one immutable action/stage declaration at module scope in `shared/catalog.ts`; mutable objects are forbidden there. Export `createRequestHarness(renderId)` that creates fresh React and Svelte registries with public `createBridge`, constructs two Concierge instances from the shared declarations (the Svelte instance explicitly sets `normalizeSnapshot: svelteSnapshotNormalizer`), creates ordinary bridge props, and returns immutable string identity tokens plus catalog/registry observations. The example owns these constructors; adapter source does not, satisfying D-09-01 and D-09-14.

ReactIsland imports only `@fullselfbrowsing/concierge-react/client`, calls the provider/live-value/registration surface with injected props, and renders deterministic text. SvelteIsland imports only `@fullselfbrowsing/concierge-svelte/client.svelte`, supplies context and invokes effect registration during component initialization with injected props. `index.astro` calls the factory per render, SSR-renders both islands without hydration directives, compares/records equal catalog projections, and emits one parseable `application/json` block with id `adapter-ssr-evidence`, absent-global booleans, distinct immutable request IDs, and pre/post registry values. No CSS/style/class, controls, random/time data, window/document/navigator reads, module-scope Concierge/registry/bridge, or visual product markup is allowed.
  </action>
  <verify>
    <automated>pnpm --dir examples/adapter-ssr check</automated>
    <automated>pnpm --dir examples/adapter-ssr build</automated>
    <automated>node -e 'const fs=require("node:fs");for(const f of ["examples/adapter-ssr/src/shared/catalog.ts","examples/adapter-ssr/src/components/ReactIsland.tsx","examples/adapter-ssr/src/components/SvelteIsland.svelte","examples/adapter-ssr/src/pages/index.astro"]){if(!fs.statSync(f).isFile()||fs.statSync(f).size===0)process.exit(1);const s=fs.readFileSync(f,"utf8");if(/&lt;style\b|class=|tailwind|document\.|window\.|navigator\./u.test(s))process.exit(1)}'</automated>
  </verify>
  <done>A normal Astro build server-renders both injected real adapters from one shared catalog with deterministic no-UI evidence and null registries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09-07-02: Prove two fresh-process normal builds have zero registration or state leakage</name>
  <files>examples/adapter-ssr/test/ssr.test.ts</files>
  <read_first>
    <file>examples/adapter-ssr/package.json</file>
    <file>examples/adapter-ssr/astro.config.mjs</file>
    <file>examples/adapter-ssr/src/shared/catalog.ts</file>
    <file>examples/adapter-ssr/src/pages/index.astro</file>
    <file>vitest.config.ts</file>
  </read_first>
  <behavior>
    - "The test launches ordinary Astro check/build in a fresh child process and parses the emitted page—not an experimental renderer."
    - "A second child process builds to another validated temporary output root and cannot inherit first-process identities/registrations."
    - "Both runs show equal shared catalogs, absent globals, distinct request identities, and React/Svelte registry values null."
    - "The suite emits exact `ASTRO_SSR_EVIDENCE renders=2 catalogs=shared registries=null globals=absent fresh=true`."
  </behavior>
  <action>
Implement the exact `node-artifact-ssr` test as a Node orchestrator. For each of two iterations, allocate an owned `mkdtemp` root, set a validated `ADAPTER_SSR_OUT_DIR`, launch normal `astro check` then `astro build` in a new process with bounded timeout and captured stderr, require nonempty built `index.html`, parse the one evidence JSON block, and assert both built adapter entries were exercised. Across runs require deterministic catalog content/digest, unique request identity strings, globals absent, and every pre/post React/Svelte registry observation `null`. Clean only the owned temp roots in `finally`. Emit the exact evidence line only after all assertions. Mark the reachable server-registration seam for SSR1/T04; do not use the Container API or accept a missing build/page/evidence file as success per D-09-15.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project node-artifact-ssr examples/adapter-ssr/test/ssr.test.ts --reporter=json | node -e 'let s="";process.stdin.on("data",d=&gt;s+=d).on("end",()=&gt;{const j=JSON.parse(s);if(!j.success||j.numTotalTestSuites&lt;=0||j.numTotalTests&lt;=0||j.testResults.length!==1)process.exit(1)})'</automated>
    <automated>pnpm --dir examples/adapter-ssr check &amp;&amp; pnpm --dir examples/adapter-ssr build</automated>
  </verify>
  <done>Positive-count evidence proves two fresh normal Astro processes render both adapters from one shared catalog with absent globals, fresh request objects, and zero server registrations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| long-lived server module → request objects | Module-scope mutable instances can leak one request's registry/state into another. |
| framework renderer → client lifecycle | Server render must not execute adapter registration. |
| child process/output → evidence parser | Missing/stale output or reused process state can create false SSR evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-04 | Information Disclosure | normal Astro SSR | mitigate | Fresh request factory, two fresh processes, absent globals, four registry-null boundaries, SSR1 marker. |
| T-09-06 | Tampering | real framework compilation | mitigate | Official integrations and real built client entries in ordinary Astro check/build. |
| T-09-08 | Repudiation | SSR evidence | mitigate | Positive Vitest counts, required nonempty page/evidence block, exact evidence marker. |
| T-09-SC | Tampering | installed integrations | mitigate | Use frozen `[VERIFIED]` dependencies from Plan 09-06. |
</threat_model>

<verification>
Run only the focused Astro check/build and one positive-count SSR test during tasks. Full workspace, tarball, mutation, and release evidence remain terminal gates in Plan 09-13.
</verification>

<success_criteria>
- ADP-04 is proven by normal Astro SSR, not packaging or import-only checks.
- Both built adapters use one shared catalog declaration and injected request-local objects.
- Two fresh processes show absent globals, fresh identities, and null registries.
- The harness stays headless and deterministic with exactly five owned source/test files.
</success_criteria>

<output>
Create `.planning/phases/09-react-and-svelte-adapters/09-07-SUMMARY.md` when done.
</output>
