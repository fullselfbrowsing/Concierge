---
phase: 09-react-and-svelte-adapters
plan: 08
type: execute
wave: 7
depends_on: [09-07]
files_modified:
  - scripts/phase-09-package-check.mjs
autonomous: true
requirements: [ADP-02, PKG-04]
must_haves:
  truths:
    - "One run packs exactly @fullselfbrowsing/concierge, concierge-react, and concierge-svelte once and uses those same three SHA-256-identified archives for every authoritative check."
    - "Each exact archive passes direct tar inspection, publint, and ATTW before the isolated consumer installs it."
    - "The outside-workspace consumer typechecks all public declarations with TypeScript 7 and skipLibCheck false and resolves one physical core from both adapters."
    - "A compiler-transformed genuine $state flow uses packed core/Svelte public APIs for review plus completed delivery; nested mutation moves the live getter, confirm returns exact consent_stale, and handler count remains zero."
    - "React and Svelte public registration paths reject disposable adapter-literal mismatches before register while the exact core archive remains unchanged."
    - "Every package-check mode is declared by this plan and missing archives, zero tests, workspace links, or extra tarballs fail."
  artifacts:
    - path: "scripts/phase-09-package-check.mjs"
      provides: "Exact tarball artifact, graph, TS7, SSR, real-rune consent, mismatch, and self-test harness"
      contains: "svelte-consent"
  key_links:
    - from: "scripts/phase-09-package-check.mjs"
      to: "packages/concierge/package.json"
      via: "one exact core archive packed from the live package"
      pattern: "@fullselfbrowsing/concierge"
    - from: "scripts/phase-09-package-check.mjs"
      to: "packages/concierge-svelte/src/client.svelte.ts"
      via: "packed consumer compiles a real rune and passes exported normalizer to createConcierge"
      pattern: "consent_stale"
    - from: "scripts/phase-09-package-check.mjs"
      to: "packages/concierge-react/src/client.tsx|packages/concierge-svelte/src/client.svelte.ts"
      via: "disposable literal-only mismatch patches exercise public lifecycle registration"
      pattern: "EXPECTED_CONTRACT_VERSION"
---

<objective>
Prove the exact three publishable archives in one isolated consumer, including declarations, singleton topology, server safety, real Svelte consent drift, and both adapter literal mismatches.

Purpose: D-09-12 and D-09-16 cannot be closed by workspace links, source imports, proxy lookalikes, or separately repacked validation inputs.
Output: One self-cleaning package harness with declared focused modes and an authoritative shared-archive `all` mode.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md
@.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md
@.planning/phases/09-react-and-svelte-adapters/09-07-SUMMARY.md
@packages/concierge/package.json
@packages/concierge/src/index.ts
@packages/concierge/src/types.ts
@packages/concierge/test/consent-kernel.test.ts
@packages/concierge-react/package.json
@packages/concierge-react/src/client.tsx
@packages/concierge-svelte/package.json
@packages/concierge-svelte/src/client.svelte.ts
@examples/adapter-ssr/test/ssr.test.ts

<interfaces>
The consent probe uses only shipped public APIs: `defineAction`, `createConcierge`, `createBridge`, `Concierge.dispatch`, and `svelteSnapshotNormalizer`. `InvocationMeta.deferUntilDelivered` receives an effect callback; the probe invokes it with `{ responseId: "review-response", outcome: "completed" }` before mutating nested rune state and dispatching `confirm` with a distinct response id.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 09-08-01: Build the exact-triplet artifact, lint, declaration, topology, and SSR harness</name>
  <files>scripts/phase-09-package-check.mjs</files>
  <read_first>
    <file>.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md</file>
    <file>.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md</file>
    <file>packages/concierge/package.json</file>
    <file>packages/concierge-react/package.json</file>
    <file>packages/concierge-svelte/package.json</file>
    <file>scripts/pack-install-check.sh</file>
  </read_first>
  <behavior>
    - "CLI accepts only `artifacts`, `svelte-consent`, `mismatch`, `all`, and `self-test`; every substantive run validates an owned temp root and exact three-archive enumeration."
    - "`all` packs once and passes the same archive paths/digests to tar, publint, ATTW, install, TS7, SSR, consent, and mismatch stages."
    - "Tarballs exclude private tests/fixtures/bundled core; direct `publint run <tgz> --strict` and `attw <tgz> --profile esm-only` pass on each exact file."
    - "Installed manifests/realpaths/npm graph prove peer+dev source manifests and one consumer core, with no adapter-nested core."
    - "TS7 skipLibCheck false and positive-count React/Svelte server tests consume only installed public entries."
  </behavior>
  <action>
Create one Node orchestrator and declare all five modes at the top; reject unknown/missing/extra arguments. Every substantive mode creates a validated `mkdtemp` root outside the repository, builds all three packages, invokes `pnpm pack --pack-destination` once per live directory `packages/concierge`, `packages/concierge-react`, and `packages/concierge-svelte`, enumerates exactly three `.tgz` files by their live package identities, and records SHA-256. In `all`, never repack between stages. Support one optional `PHASE09_ARCHIVE_EXPORT_DIR` environment value for release CI only: require an existing empty directory outside the repository, and after every `all` assertion passes copy those same three archives plus their digest manifest there. This environment option never changes mode semantics or creates evidence. Clean only the validated owned temp root in `finally` and bound every child process/output.

For `artifacts` and the artifact stage of `all`, inspect exact tar entries and manifests; reject tests, fixtures, bundled core source, foreign aliases, missing targets, or extra archive. Run `pnpm exec publint run <exact-tgz> --strict` and `pnpm exec attw <exact-tgz> --profile esm-only` directly on each archive (no `--pack`, no package-directory repack). Create one npm consumer outside workspace globs with exact `file:` archives and pinned consumer-only React/Svelte/Vite/Vitest/jsdom/TypeScript tools, install with scripts disabled, and reject symlinks/realpaths entering the repository. Use npm graph plus `createRequire` from each installed adapter manifest to prove both resolve the same physical `@fullselfbrowsing/concierge` as the consumer and no nested core exists per D-09-04/D-09-16.

Compile a public-entry fixture with the consumer's TypeScript 7.0.2, `skipLibCheck: false`, and no private/dist path imports. Run a scratch Vitest config with exact Node server and framework-transformed files; JSON results must have positive suites/tests/files. React `renderToString`, compiled Svelte server render, and Node imports of roots/client entries must run with browser globals absent and registry register count zero. `self-test` uses synthetic temp fixtures to prove missing archive, fourth archive, workspace realpath, duplicate core, zero-test JSON, and failed child process are rejected. Do not persist release evidence in this plan.
  </action>
  <verify>
    <automated>node --check scripts/phase-09-package-check.mjs</automated>
    <automated>node scripts/phase-09-package-check.mjs self-test</automated>
    <automated>node scripts/phase-09-package-check.mjs artifacts</automated>
  </verify>
  <done>The declared harness directly validates one exact triplet's tar contents, publint/ATTW results, TS7 declarations, installed graph/realpaths, and positive-count server safety without workspace resolution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09-08-02: Prove packed real-$state consent drift and literal-only mismatch failure</name>
  <files>scripts/phase-09-package-check.mjs</files>
  <read_first>
    <file>scripts/phase-09-package-check.mjs</file>
    <file>packages/concierge/test/consent-kernel.test.ts</file>
    <file>packages/concierge/src/types.ts</file>
    <file>packages/concierge-react/src/client.tsx</file>
    <file>packages/concierge-svelte/src/client.svelte.ts</file>
  </read_first>
  <behavior>
    - "A scratch `.svelte.ts` module is compiler-transformed; it creates a genuine nested `$state` object, not a proxy mock."
    - "Packed core plus packed Svelte run public review dispatch, invoke the captured delivery callback with exact completed report, mutate nested state, prove the registered live getter moved, then return exact consent_stale on confirm with handler count zero."
    - "Identity normalizer S1 makes that exact detector fail because consent no longer closes correctly."
    - "Disposable React and Svelte archives change only the adapter-owned literal; unchanged core digest is verified before/after."
    - "Both public lifecycle registration paths fail before register with package name, expected/found, and upgrade guidance."
  </behavior>
  <action>
Implement the `svelte-consent` stage through the installed scratch consumer's Svelte Vite plugin/Vitest jsdom project so `.svelte.ts` runes receive the real client compiler transform. In a rune-aware module create nested `$state({ booking: { amount: 41, seat: "A" } })`; construct a live ordinary bridge whose getter returns that state; use public `createBridge("booking")`; define review and consequential confirm actions using the same smallest public consent/delivery shape as `packages/concierge/test/consent-kernel.test.ts`; and construct core explicitly with `normalizeSnapshot: svelteSnapshotNormalizer`. Register the bridge, dispatch review with `callId`, `responseId: "review-response"`, and a public `deferUntilDelivered` callback capture; invoke that callback with `{ responseId: "review-response", outcome: "completed" }`; mutate both `amount` and nested `seat`; assert the live getter reports the new values; dispatch confirm with distinct `responseId: "confirm-response"`; assert the exact complete result includes `{ ok: false, reason: "consent_stale" }`; and assert the consequential handler count is exactly zero. Require positive Vitest file/test/assertion counts. This is the authoritative D-09-12/T03/S1 oracle.

Implement `mismatch` by unpacking each exact adapter archive into a disposable directory, locating the unique adapter-owned expected-version literal in built client output, replacing it exactly once with an incompatible integer, and repacking only that adapter. Keep the original core archive byte-identical and verify its digest before/after. Install each mismatched adapter with the original core and other original adapter, then invoke the actual public React hook mount and compiled Svelte component/effect registration path. Both must throw before the spy registry count changes, with the live package name, expected/found values, and upgrade/reinstall guidance. A peer install error, direct private guard call, source patch, compile failure, or generic nonzero exit receives no credit. `all` runs artifact, server, consent, and both mismatch stages over one initial exact triplet; it emits one machine JSON summary but does not write terminal evidence.
  </action>
  <verify>
    <automated>node scripts/phase-09-package-check.mjs svelte-consent</automated>
    <automated>node scripts/phase-09-package-check.mjs mismatch</automated>
  </verify>
  <done>The exact packed Svelte/core public flow proves detached review evidence against moving genuine rune state and zero handler entry, while both literal-only adapter mismatches fail actionably before registration with unchanged core.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| repository → exact tarballs | Packaging transforms/filtering can change declarations, directives, or rune code. |
| exact tarballs → isolated npm graph | Workspace links or nested core copies can hide split singleton authority. |
| rune live state → reviewed consent snapshot | Identity normalization makes drift compare a proxy against itself. |
| adapter literal → core version | Tautological/bypassed checks can allow incompatible registration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-03 | Tampering | packed consent snapshot | mitigate | Real compiler-transformed nested `$state`, completed delivery, exact stale result, zero handler. |
| T-09-04 | Information Disclosure | packed SSR | mitigate | Positive-count React/Svelte server tests with globals absent and zero register. |
| T-09-05 | Elevation of Privilege | singleton/version topology | mitigate | One core realpath plus literal-only public-path mismatch failures. |
| T-09-06 | Tampering | package transforms | mitigate | Direct exact-tgz tar/publint/ATTW, TS7 declarations, directive/rune runtime. |
| T-09-08 | Repudiation | harness results | mitigate | Declared modes, exact archive hashes, positive counts, semantic assertions, self-tests. |
| T-09-SC | Tampering | isolated npm install | mitigate | Exact local project archives plus only `[VERIFIED]` pinned consumer tools; lock/archive digests recorded. |
</threat_model>

<verification>
At plan close run `node scripts/phase-09-package-check.mjs all` once. It must report one shared three-archive digest set and all stages green. Do not generate mutation/release evidence until terminal Plan 09-13.
</verification>

<success_criteria>
- Exact live package names/paths and public `createBridge`/Concierge APIs are used throughout.
- The same authoritative archive triplet receives tar, publint, ATTW, install, TS7, SSR, consent, and mismatch proof.
- D-09-12 includes review, completed delivery, nested mutation, moving live getter, exact consent_stale, and handler count zero.
- One physical core and both actionable literal mismatch guards are proven outside workspace globs.
</success_criteria>

<output>
Create `.planning/phases/09-react-and-svelte-adapters/09-08-SUMMARY.md` when done.
</output>
