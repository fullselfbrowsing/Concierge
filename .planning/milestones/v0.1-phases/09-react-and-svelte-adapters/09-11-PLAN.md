---
phase: 09-react-and-svelte-adapters
plan: 11
type: execute
wave: 9
depends_on: [09-10, 09-12]
files_modified:
  - package.json
  - scripts/phase-09-test-check.mjs
  - scripts/phase-09-workflow-check.mjs
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
autonomous: true
requirements: [ADP-01, ADP-02, ADP-03, ADP-04, PKG-04]
must_haves:
  truths:
    - "One test command runs exactly the two lifecycle and three Node artifact/SSR files and requires positive JSON file/test/suite counts."
    - "CI retains all current core gates in order and then makes every Phase 09 package/SSR/budget/evidence gate blocking."
    - "Release retains existing type/build/test/artifact/dependency/pack/Node-floor/provenance behavior and blocks publication until same-revision Phase 09 evidence and exact archives verify."
    - "Release uploads exactly the already-checked three archives/digest manifest exported by package-check `all`; it does not repack for upload."
    - "A deterministic local workflow checker proves ordering and absence of ignored failures without adding a workflow-linter dependency."
  artifacts:
    - path: "scripts/phase-09-test-check.mjs"
      provides: "Exact five-file positive-count Vitest orchestrator"
      contains: "node-artifact-ssr"
    - path: "scripts/phase-09-workflow-check.mjs"
      provides: "Deterministic CI/release static ordering and blocking checks"
      contains: "continue-on-error"
    - path: ".github/workflows/ci.yml"
      provides: "Inherited plus blocking Phase 09 CI gates"
      contains: "pnpm run check:phase09"
    - path: ".github/workflows/release.yml"
      provides: "Evidence-bound exact-three-archive release gate/upload"
      contains: "PHASE09_ARCHIVE_EXPORT_DIR"
  key_links:
    - from: "package.json"
      to: "scripts/phase-09-package-check.mjs|scripts/phase-09-adapter-budget.mjs|scripts/phase-09-mutation-battery.mjs"
      via: "stable Phase 09 commands using only declared script modes"
      pattern: "check:phase09"
    - from: ".github/workflows/release.yml"
      to: "scripts/phase-09-package-check.mjs"
      via: "validated external export directory receives same checked archives"
      pattern: "PHASE09_ARCHIVE_EXPORT_DIR"
---

<objective>
Wire exact positive-count adapter tests and all immutable Phase 09 gates into existing CI/release without weakening prior core/provenance behavior or introducing an unplanned validator.

Purpose: D-09-17 requires additive blocking gates, while task-local planning must avoid running the terminal package/mutation/release battery before evidence exists.
Output: Stable root commands, exact Vitest aggregation, deterministic workflow linting, and updated CI/release workflows.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md
@.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md
@.planning/phases/09-react-and-svelte-adapters/09-10-SUMMARY.md
@.planning/phases/09-react-and-svelte-adapters/09-12-SUMMARY.md
@package.json
@vitest.config.ts
@.github/workflows/ci.yml
@.github/workflows/release.yml
@scripts/phase-09-package-check.mjs
@scripts/phase-09-adapter-budget.mjs
@scripts/phase-09-mutation-battery.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 09-11-01: Add exact positive-count Phase 09 test and gate commands</name>
  <files>scripts/phase-09-test-check.mjs, package.json</files>
  <read_first>
    <file>package.json</file>
    <file>vitest.config.ts</file>
    <file>packages/concierge-react/test/lifecycle.test.tsx</file>
    <file>packages/concierge-svelte/test/lifecycle.test.ts</file>
    <file>packages/concierge-react/test/artifact.test.ts</file>
    <file>packages/concierge-svelte/test/artifact.test.ts</file>
    <file>examples/adapter-ssr/test/ssr.test.ts</file>
  </read_first>
  <behavior>
    - "The test orchestrator requires exactly five expected nonempty files and invokes only react-lifecycle, svelte-lifecycle, and node-artifact-ssr."
    - "Vitest JSON must be successful with positive suite/test counts and testResults exactly equal the five canonical files."
    - "Root commands use only contract modes final, package mode all, budget mode check, and mutation mode verify all declared by their owning plans."
  </behavior>
  <action>
Create a Node test orchestrator that first checks all five canonical test files are nonempty regular files, spawns Vitest with exactly `--project react-lifecycle --project svelte-lifecycle --project node-artifact-ssr --reporter=json`, parses stdout, requires success plus positive total suite/test counts, normalizes `testResults[].name`, and requires exact set equality with both lifecycle, both artifact, and Astro SSR tests. Reject extra/missing files, zero counts, non-JSON output, timeout, or nonzero process. Do not use shell pipelines, `passWithNoTests`, Jest flags, or broad globs.

Add root scripts: `test:phase09` for that orchestrator; `check:phase09:packages` as `node scripts/phase-09-package-check.mjs all`; `check:phase09:budget` as budget `check`; `check:phase09:static` as contract `final`; `check:phase09:evidence` as mutation `verify all`; aggregate `check:phase09` in the dependency order test/Astro check+normal build/package/budget/static/evidence; and `check:phase09:release` as `check:phase09` followed by the workflow checker added in Task 2. Keep current root TypeScript/build/test/core release scripts unchanged. Do not execute aggregate commands yet because terminal evidence is intentionally absent until Plan 09-13.
  </action>
  <verify>
    <automated>node scripts/phase-09-test-check.mjs</automated>
    <automated>node -e 'const p=require("./package.json");const exact={"check:phase09:packages":"node scripts/phase-09-package-check.mjs all","check:phase09:budget":"node scripts/phase-09-adapter-budget.mjs check","check:phase09:static":"node scripts/phase-09-contract-check.mjs final","check:phase09:evidence":"node scripts/phase-09-mutation-battery.mjs verify all"};for(const [k,v] of Object.entries(exact))if(p.scripts?.[k]!==v)process.exit(1);if(!p.scripts?.["test:phase09"]||!p.scripts?.["check:phase09"]||!p.scripts?.["check:phase09:release"])process.exit(1)'</automated>
  </verify>
  <done>One exact positive-count command owns all five Phase 09 test files and root scripts invoke only declared modes without disturbing inherited core scripts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09-11-02: Make inherited and Phase 09 CI/release gates blocking and statically provable</name>
  <files>scripts/phase-09-workflow-check.mjs, .github/workflows/ci.yml, .github/workflows/release.yml</files>
  <read_first>
    <file>.github/workflows/ci.yml</file>
    <file>.github/workflows/release.yml</file>
    <file>package.json</file>
    <file>RELEASING.md</file>
    <file>scripts/phase-09-package-check.mjs</file>
    <file>scripts/phase-09-mutation-battery.mjs</file>
  </read_first>
  <behavior>
    - "Static checker proves current core typecheck/build/test/artifact/deps/pack and Node-floor behavior remains, then Phase 09 runs after prerequisites."
    - "No Phase 09 or inherited gate has continue-on-error or an ignored exit."
    - "Release sets a validated runner-temp archive export directory, runs full same-revision Phase 09 verification before changesets publish, and uploads exactly three tgz plus digest manifest."
    - "Workflow syntax and ordering are checked without a new workflow-linter dependency."
  </behavior>
  <action>
Add the Phase 09 aggregate command to CI only after the existing frozen install, typecheck, build, root test, core artifact/dependency/pack gates; preserve the separate exact Node-floor job and all current action pins/permissions. In release, preserve trusted-publishing/OIDC, full history, frozen install, root typecheck/build/test, and changesets action. Before changesets publication, run the existing core artifact/dependency/pack/Node-floor gates, allocate an empty `${{ runner.temp }}/phase09-archives`, set `PHASE09_ARCHIVE_EXPORT_DIR` only for `pnpm run check:phase09:release`, then upload exactly the three `.tgz` plus digest manifest produced by that same checked invocation. Never repack for upload, never use `continue-on-error`, and ensure publication cannot run when the Phase 09 step fails per D-09-16/D-09-17.

Create a dependency-free Node static checker that requires both workflow files exist/nonempty, scans structured lines/steps, proves inherited and Phase 09 command occurrence/order, rejects duplicate/missing commands, `continue-on-error`, `|| true`, ignored exit codes, broad archive globs that admit a fourth package, phase09 execution after publish, or a second pack between verification/upload. Validate exact live paths/package names and the export env. This script has no modes or arguments and adds no workflow-linter dependency.
  </action>
  <verify>
    <automated>node --check scripts/phase-09-workflow-check.mjs &amp;&amp; node scripts/phase-09-workflow-check.mjs</automated>
    <automated>node -e 'const fs=require("node:fs");for(const f of [".github/workflows/ci.yml",".github/workflows/release.yml"]){const s=fs.readFileSync(f,"utf8");if(!s.includes("pnpm run check:phase09")||s.includes("continue-on-error"))process.exit(1)}const r=fs.readFileSync(".github/workflows/release.yml","utf8");if(!r.includes("PHASE09_ARCHIVE_EXPORT_DIR")||r.indexOf("check:phase09:release")&gt;r.indexOf("changesets/action"))process.exit(1)'</automated>
  </verify>
  <done>Deterministic local checks prove CI/release preserve inherited gates, block on complete Phase 09 verification, and upload the exact already-tested triplet before publication with no ignored failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Vitest routing → aggregate result | Missing or duplicate collection can make a green aggregate vacuous. |
| pull request → CI status | Reordered/ignored gates can merge unverified changes. |
| release revision → publication | Stale evidence or repacked artifacts can differ from checked bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-04 | Information Disclosure | SSR CI | mitigate | Exact Astro test/file counts and normal build in blocking aggregate. |
| T-09-05 | Elevation of Privilege | package release | mitigate | Exact package all/evidence verify before publish. |
| T-09-06 | Tampering | compiler/artifact CI | mitigate | Both local toolchain builds and exact archive checks. |
| T-09-08 | Repudiation | workflow/test status | mitigate | Exact file equality, positive counts, static ordering/ignored-exit rejection. |
| T-09-SC | Tampering | workflow tools | mitigate | Add no workflow-linter dependency; preserve pinned existing actions and the frozen lock. |
</threat_model>

<verification>
Run only the exact five-file test orchestrator, package-script static assertion, and workflow checker. Do not run `check:phase09` or release/mutation batteries until Plan 09-13 generates terminal evidence.
</verification>

<success_criteria>
- Exact five test files run once in the correct projects with positive counts.
- CI retains all inherited gates and adds blocking Phase 09 verification after prerequisites.
- Release verifies and uploads the same exact three archives before changesets publish.
- No unsupported flags, ignored exits, new workflow-linter dependency, or undeclared checker modes remain.
</success_criteria>

<output>
Create `.planning/phases/09-react-and-svelte-adapters/09-11-SUMMARY.md` when done.
</output>
