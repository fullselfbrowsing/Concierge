---
phase: 09-react-and-svelte-adapters
plan: 10
type: execute
wave: 8
depends_on: [09-08, 09-09]
files_modified:
  - packages/concierge-react/README.md
  - packages/concierge-svelte/README.md
  - README.md
  - RELEASING.md
autonomous: true
requirements: [ADP-01, ADP-02, ADP-03, ADP-04, PKG-04]
must_haves:
  truths:
    - "React docs use only the canonical provider/consumer/live-value/registration exports and inject public createBridge/createConcierge results from app setup."
    - "Svelte docs use only native context/effect exports and configure the exported real snapshot normalizer explicitly at app createConcierge time."
    - "Docs distinguish client compatibility/singleton guards from server authorization and never claim adapters own consent, catalogs, transport, or results."
    - "Root docs point to the single headless Astro SSR harness and no longer describe the adapters as absent."
    - "Release docs describe exact three-tarball, Phase 8-preserving, terminal-evidence gates with the live paths/commands."
  artifacts:
    - path: "packages/concierge-react/README.md"
      provides: "Canonical React injection/lifecycle/server guidance"
      contains: "useConciergeBridge"
    - path: "packages/concierge-svelte/README.md"
      provides: "Canonical Svelte context/effect/real-rune normalizer guidance"
      contains: "svelteSnapshotNormalizer"
    - path: "README.md"
      provides: "Top-level adapter availability, ownership, and Astro proof"
      contains: "examples/adapter-ssr"
    - path: "RELEASING.md"
      provides: "Exact triplet and immutable evidence release procedure"
      contains: "phase-08-mutation-battery.mjs verify ledgers"
  key_links:
    - from: "packages/concierge-react/README.md"
      to: "packages/concierge-react/src/client.tsx"
      via: "copy-paste examples use exact canonical exports"
      pattern: "@fullselfbrowsing/concierge-react/client"
    - from: "packages/concierge-svelte/README.md"
      to: "packages/concierge-svelte/src/client.svelte.ts"
      via: "explicit normalizeSnapshot configuration and native helpers"
      pattern: "normalizeSnapshot"
---

<objective>
Document the exact built adapter APIs, ownership/security boundaries, real Astro proof, and evidence-bound release procedure before terminal evidence is generated.

Purpose: Consumer guidance is part of the release input and must not drift after mutation/release evidence; it must teach the same injection-only public paths that tests prove.
Output: Two package READMEs plus corrected root and releasing guidance.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md
@.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md
@.planning/phases/09-react-and-svelte-adapters/09-08-SUMMARY.md
@.planning/phases/09-react-and-svelte-adapters/09-09-SUMMARY.md
@README.md
@RELEASING.md
@packages/concierge/README.md
@packages/concierge-react/src/client.tsx
@packages/concierge-svelte/src/client.svelte.ts
@scripts/phase-09-package-check.mjs
@scripts/phase-09-adapter-budget.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 09-10-01: Write canonical React and Svelte package usage/security contracts</name>
  <files>packages/concierge-react/README.md, packages/concierge-svelte/README.md</files>
  <read_first>
    <file>.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md</file>
    <file>packages/concierge-react/src/client.tsx</file>
    <file>packages/concierge-react/package.json</file>
    <file>packages/concierge-svelte/src/client.svelte.ts</file>
    <file>packages/concierge-svelte/package.json</file>
    <file>packages/concierge/README.md</file>
  </read_first>
  <action>
Write package-specific, copy-pasteable canonical paths using only live names. React docs separate server-safe root type/metadata imports from `@fullselfbrowsing/concierge-react/client`, show application setup constructing core with public `createConcierge` and `createBridge`, then inject those exact objects into `ConciergeProvider`/`useConciergeBridge`; show `useConciergeValue` receiving a plain current value with no app ref; explain StrictMode exact cleanup/latest committed reads and zero SSR registration per D-09-01, D-09-05 through D-09-08. Never imply the adapter calls/wraps `createConcierge`.

Svelte docs separate root from `@fullselfbrowsing/concierge-svelte/client.svelte`, show app setup explicitly passing `normalizeSnapshot: svelteSnapshotNormalizer` to public core `createConcierge`, then `provideConcierge` and `useConciergeBridge` during component initialization with an existing public `createBridge` registry and ordinary bridge. Show a genuine `$state` nested value and explain reviewed snapshot detachment/live getter drift per D-09-09 through D-09-12. Both documents explain singleton/literal guards as client compatibility/integrity defenses, not server identity/authorization; prohibit hidden catalog/session/consent/transport/result ownership and avoid aliases or deferred UI APIs.
  </action>
  <verify>
    <automated>node -e 'const fs=require("node:fs");const checks={"packages/concierge-react/README.md":["@fullselfbrowsing/concierge-react/client","@fullselfbrowsing/concierge","createBridge","ConciergeProvider","useConciergeValue","useConciergeBridge","server authorization"],"packages/concierge-svelte/README.md":["@fullselfbrowsing/concierge-svelte/client.svelte","@fullselfbrowsing/concierge","createBridge","provideConcierge","useConciergeBridge","normalizeSnapshot: svelteSnapshotNormalizer","$state","consent_stale","server authorization"]};for(const [f,xs] of Object.entries(checks)){if(!fs.statSync(f).isFile()||fs.statSync(f).size===0)process.exit(1);const s=fs.readFileSync(f,"utf8");for(const x of xs)if(!s.includes(x))process.exit(1)}'</automated>
    <automated>node -e 'const fs=require("node:fs");for(const f of ["packages/concierge-react/README.md","packages/concierge-svelte/README.md"]){const s=fs.readFileSync(f,"utf8");if(/Vue|Angular|Solid|devtools|UI component|transport adapter/u.test(s))process.exit(1)}'</automated>
  </verify>
  <done>Both package READMEs exactly match built canonical APIs, keep construction in application/core code, explain real lifecycle/snapshot semantics, and state the client-versus-server security boundary.</done>
</task>

<task type="auto">
  <name>Task 09-10-02: Correct root adapter guidance and the immutable three-package release procedure</name>
  <files>README.md, RELEASING.md</files>
  <read_first>
    <file>README.md</file>
    <file>RELEASING.md</file>
    <file>packages/concierge-react/README.md</file>
    <file>packages/concierge-svelte/README.md</file>
    <file>scripts/phase-09-package-check.mjs</file>
    <file>scripts/phase-08-mutation-battery.mjs</file>
  </read_first>
  <action>
Update root README adapter status and examples to the two live package names/client subpaths and point to the single deterministic `examples/adapter-ssr`; describe it as a headless normal-SSR harness, never a product UI. State the independent <=150 production-source budgets and injection-only ownership without restating deferred frameworks as shipped.

Update RELEASING after package docs so it names core-first publication followed by the two peer adapters, exact three archive paths/digests, direct archive publint/ATTW, one physical core, TS7 declaration plus local TS6 Svelte/Astro domains, normal Astro SSR, real `$state` consent drift, budget, and immutable mutations. Phase 8 instructions must point only to `.planning/phases/08-consent-kernel/{08-MUTATION-REGISTER.json,08-MUTATION-EVIDENCE.json,08-VALIDATION.md,08-SECURITY.md,08-VERIFICATION.md}` and the three real commands `node scripts/phase-08-mutation-battery.mjs verify all`, `verify inputs`, and `verify ledgers` run in a disposable snapshot. Explicitly say release evidence is nested in `08-MUTATION-EVIDENCE.json`; do not mention a nonexistent Phase 8 release file/script. State that Plan 09-13 is terminal and any later source/manifest/docs/workflow drift invalidates verify-only evidence per D-09-16/D-09-17.
  </action>
  <verify>
    <automated>node -e 'const fs=require("node:fs");const r=fs.readFileSync("README.md","utf8"),x=fs.readFileSync("RELEASING.md","utf8");for(const s of ["@fullselfbrowsing/concierge-react","@fullselfbrowsing/concierge-svelte","examples/adapter-ssr"])if(!r.includes(s))process.exit(1);for(const s of ["08-consent-kernel/08-MUTATION-EVIDENCE.json","phase-08-mutation-battery.mjs verify all","phase-08-mutation-battery.mjs verify inputs","phase-08-mutation-battery.mjs verify ledgers","phase-09-package-check.mjs all"])if(!x.includes(s))process.exit(1)'</automated>
    <automated>node -e 'const fs=require("node:fs");const r=fs.readFileSync("README.md","utf8"),x=fs.readFileSync("RELEASING.md","utf8");if((r.match(/examples\/adapter-ssr/gu)||[]).length&lt;1||(x.match(/\.planning\/phases\/08-consent-kernel/gu)||[]).length&lt;1)process.exit(1)'</automated>
  </verify>
  <done>Root and release docs use only live packages/APIs/paths, describe the real Astro/tarball/budget proof, and preserve the actual nested Phase 8 release evidence and disposable verification commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| documentation → consumer implementation | Wrong names or hidden construction can bypass the tested adapter boundary. |
| client guard → security claim | Compatibility checks can be misrepresented as server authorization. |
| release guide → immutable evidence | Invented Phase 8 paths or wrong ordering can discard provenance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-03 | Spoofing | security guidance | mitigate | Explicit client-integrity/not-server-authorization wording. |
| T-09-05 | Elevation of Privilege | package topology docs | mitigate | Exact core peer/createBridge and singleton/version guidance. |
| T-09-06 | Tampering | framework setup docs | mitigate | Canonical built entry/compiler examples only. |
| T-09-08 | Repudiation | release documentation | mitigate | Actual Phase 8 paths/commands and terminal drift rule. |
| T-09-SC | Tampering | dependencies | accept | Documentation plan installs no packages. |
</threat_model>

<verification>
Run the four focused static assertions only. Package/tarball/mutation/release batteries are intentionally deferred to terminal closure after workflows and mutation infrastructure are the last remaining edits.
</verification>

<success_criteria>
- All docs use `@fullselfbrowsing/*`, public `createBridge`, and actual repo paths.
- Package examples mirror the tested canonical APIs and contain no adapter-side core construction.
- Client guards are never described as server authorization.
- RELEASING names actual Phase 8 nested evidence and disposable verify commands.
</success_criteria>

<output>
Create `.planning/phases/09-react-and-svelte-adapters/09-10-SUMMARY.md` when done.
</output>
