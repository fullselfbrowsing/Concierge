---
phase: 09-react-and-svelte-adapters
plan: 04
type: execute
wave: 4
depends_on: [09-03]
files_modified:
  - packages/concierge-react/src/index.ts
  - packages/concierge-react/src/client.tsx
  - packages/concierge-react/test/lifecycle.test.tsx
  - packages/concierge-react/test/artifact.test.ts
  - packages/concierge-react/test-d/public.test-d.ts
autonomous: true
requirements: [ADP-01, PKG-04]
must_haves:
  truths:
    - "React context returns the exact injected Concierge and fails actionably outside its provider."
    - "StrictMode setup/cleanup/setup leaves the current bridge live, stale cleanup cannot clear replacement, and final unmount empties createBridge()."
    - "A rerendered plain value is read from the latest committed render without application-owned refs."
    - "Server import/render performs zero registrations and only the packed client entry begins with one client directive."
    - "Every registration calls assertSingleInstance and compares the adapter literal before createBridge().register."
  artifacts:
    - path: "packages/concierge-react/src/client.tsx"
      provides: "Canonical React provider, consumer, live-value, and bridge hooks"
      contains: "useConciergeBridge"
    - path: "packages/concierge-react/test/lifecycle.test.tsx"
      provides: "T01 cleanup and T02 latest committed state lifecycle evidence"
      contains: "StrictMode"
    - path: "packages/concierge-react/test/artifact.test.ts"
      provides: "Built entry, directive, guard, and server-render evidence"
      contains: "renderToString"
  key_links:
    - from: "packages/concierge-react/src/client.tsx"
      to: "@fullselfbrowsing/concierge"
      via: "types plus assertSingleInstance/CONTRACT_VERSION; supplied BridgeRegistry.register"
      pattern: "assertSingleInstance"
    - from: "packages/concierge-react/test/lifecycle.test.tsx"
      to: "packages/concierge-react/src/client.tsx"
      via: "real React StrictMode mount/rerender/unmount"
      pattern: "react-lifecycle"
---

<objective>
Implement and prove the canonical thin React adapter against the live core `createBridge` registry and real React lifecycle.

Purpose: ADP-01 requires exact instance identity, latest committed values, StrictMode-safe cleanup, server silence, and runtime package guards—not a wrapper around core construction.
Output: Two production entries plus positive-count lifecycle, type, and built-artifact suites.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md
@.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md
@.planning/phases/09-react-and-svelte-adapters/09-02-SUMMARY.md
@packages/concierge/src/index.ts
@packages/concierge/src/types.ts
@packages/concierge/src/bridge.ts
@packages/concierge/src/contract.ts
@packages/concierge-react/package.json
@packages/concierge-react/tsdown.config.ts

<interfaces>
Use the live public core contracts exactly:

- `createBridge<B extends Bridge>(id: string): BridgeRegistry<B>` constructs the existing single-slot registry.
- `BridgeRegistry.register(bridge)` returns the exact monotonic-token unsubscriber.
- `Bridge` contains ordinary `actions` functions and late-reading `snapshot` getters.
- `CONTRACT_VERSION` and `assertSingleInstance` are public runtime exports from `@fullselfbrowsing/concierge`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 09-04-01: Implement exact Context, latest-commit getters, and StrictMode cleanup</name>
  <files>packages/concierge-react/src/index.ts, packages/concierge-react/src/client.tsx, packages/concierge-react/test/lifecycle.test.tsx</files>
  <read_first>
    <file>.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md</file>
    <file>.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md</file>
    <file>packages/concierge/src/index.ts</file>
    <file>packages/concierge/src/types.ts</file>
    <file>packages/concierge/src/bridge.ts</file>
    <file>packages/concierge-react/package.json</file>
  </read_first>
  <behavior>
    - "Provider/consumer preserve exact Concierge identity and the missing-provider error names @fullselfbrowsing/concierge-react."
    - "`useConciergeValue(value)` returns a stable getter whose ref changes only after committed effects and observes the newest rerendered sentinel."
    - "`useConciergeBridge(registry, bridge)` guards then registers only inside `useEffect` and returns that exact registration unsubscriber."
    - "Real StrictMode setup/cleanup/setup, repeated stale cleanup, same-object replacement, and final unmount produce the required registry states."
  </behavior>
  <action>
Write the named lifecycle tests first in the dedicated `react-lifecycle` project. In `src/client.tsx`, put the exact `"use client";` prologue before imports. Export one canonical surface only: `ConciergeProvider`, `useConcierge`, `useConciergeValue`, and `useConciergeBridge`. The provider accepts an already constructed `Concierge`, stores the exact reference in a private null-default `createContext`, and never registers. The consumer throws fixed package-named remediation when absent. `useConciergeValue<T>(value)` owns a ref, updates it in a post-commit effect, and returns a stable callback reading `ref.current`; application tests/examples pass plain values and contain no mirror ref per D-09-05 and D-09-06.

`useConciergeBridge` accepts an existing `BridgeRegistry<B>` (normally from public `createBridge`) and an ordinary `Bridge`. Inside one `useEffect`, call `assertSingleInstance()`, compare imported core `CONTRACT_VERSION` against an adapter-owned literal `EXPECTED_CONTRACT_VERSION = 1`, throw a fixed error containing `@fullselfbrowsing/concierge-react`, expected/found values, and upgrade/reinstall guidance on mismatch, then call `registry.register(bridge)` and return that exact function. No register call, timer, host global, custom token, ref count, or cleanup guard may occur outside the effect per D-09-01 through D-09-03.

The lifecycle test mounts real `<StrictMode>`, wraps a live `createBridge("react-test")` only to retain returned cleanup functions, proves setup/cleanup/setup, re-fires the first stale cleanup after replacement, rerenders a plain nested state sentinel, awaits committed effects, reads through the live bridge getter, and finally unmounts to `null`. Record named markers for T01/R1 exact cleanup and T02/R2 stale value. `src/index.ts` exports only types/inert metadata and never re-exports client hooks.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project react-lifecycle packages/concierge-react/test/lifecycle.test.tsx --reporter=json | node -e 'let s="";process.stdin.on("data",d=&gt;s+=d).on("end",()=&gt;{const j=JSON.parse(s);if(!j.success||j.numTotalTestSuites&lt;=0||j.numTotalTests&lt;=0||j.testResults.length!==1)process.exit(1)})'</automated>
    <automated>pnpm --filter @fullselfbrowsing/concierge-react typecheck</automated>
  </verify>
  <done>Real StrictMode and rerender tests prove exact cleanup, stale-cleanup refusal, final null state, and latest committed plain values through the canonical injection-only hooks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09-04-02: Pin React public types, packed directive shape, guards, and server silence</name>
  <files>packages/concierge-react/test-d/public.test-d.ts, packages/concierge-react/test/artifact.test.ts</files>
  <read_first>
    <file>packages/concierge-react/src/index.ts</file>
    <file>packages/concierge-react/src/client.tsx</file>
    <file>packages/concierge-react/package.json</file>
    <file>packages/concierge-react/tsdown.config.ts</file>
    <file>packages/concierge/test/artifact.test.ts</file>
  </read_first>
  <behavior>
    - "Public generic signatures preserve Concierge, Bridge, and BridgeRegistry types and reject missing/wrong provider or hook arguments."
    - "Built client begins with exactly one client directive; React root and core entry contain none."
    - "Built server import and React renderToString have browser globals absent and observe zero registry registrations."
    - "Artifact source contains an adapter literal comparison and package/expected/found/upgrade diagnostic before register."
  </behavior>
  <action>
Add `@ts-expect-error`/positive type fixtures against `@fullselfbrowsing/concierge-react/client`, never private source paths, pinning the four canonical signatures and generic preservation. Add one Node artifact suite that requires a successful package build, reads nonempty `dist/index.js` and `dist/client.js`, parses the first statement/directive count, rejects client markers in root/core, imports both built entries with `window`, `document`, and `navigator` absent, and runs React `renderToString` around a supplied Concierge/registry/bridge while a register spy remains exactly zero. Assert the built client retains the adapter-owned literal and actionable diagnostic before the reachable register call. Do not invoke publint/ATTW here; Plan 09-08 runs them against the exact authoritative tarball per D-09-08 and D-09-16.
  </action>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge-react build &amp;&amp; pnpm --filter @fullselfbrowsing/concierge-react typecheck</automated>
    <automated>pnpm exec vitest run --project node-artifact-ssr packages/concierge-react/test/artifact.test.ts --reporter=json | node -e 'let s="";process.stdin.on("data",d=&gt;s+=d).on("end",()=&gt;{const j=JSON.parse(s);if(!j.success||j.numTotalTestSuites&lt;=0||j.numTotalTests&lt;=0||j.testResults.length!==1)process.exit(1)})'</automated>
  </verify>
  <done>Types and built artifacts expose one canonical client surface, preserve the client-only directive, execute package guards before register, and remain import/render safe with zero server registration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| React render/effect → core registry | Render-time registration or wrong cleanup can leak/clear live page authority. |
| committed props → bridge getter | A stale closure can make handlers act on superseded application state. |
| built client entry → server renderer | A stripped/spread directive or import side effect can contaminate SSR. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-01 | Tampering | StrictMode cleanup | mitigate | Real lifecycle sequence, retained stale cleanup, exact returned function, R1 marker. |
| T-09-02 | Tampering | latest value getter | mitigate | Adapter-owned post-commit ref and direct bridge read after rerender, R2 marker. |
| T-09-04 | Information Disclosure | React SSR | mitigate | Built import/render with globals absent and register count zero. |
| T-09-05 | Elevation of Privilege | package guard | mitigate | assertSingleInstance plus literal comparison immediately before register. |
| T-09-06 | Tampering | directive transform | mitigate | Source and built first-statement/count assertions. |
| T-09-SC | Tampering | installed packages | mitigate | No new install; use the verified/frozen skeleton dependencies. |
</threat_model>

<verification>
Run both task command pairs, then rerun `node scripts/phase-09-contract-check.mjs baseline-verify`. Do not run the full workspace or tarball battery in task-local verification.
</verification>

<success_criteria>
- T01 and T02 have real positive-count lifecycle tests and named mutant seams.
- All registration is effect-only and guard-before-register; adapters never call/wrap createConcierge.
- Root is server-safe; client artifact has exactly one directive; SSR registration count is zero.
- React authored production surface is exactly `src/index.ts` and `src/client.tsx` for the later budget gate.
</success_criteria>

<output>
Create `.planning/phases/09-react-and-svelte-adapters/09-04-SUMMARY.md` when done.
</output>
