---
phase: 08-consent-kernel
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - packages/concierge/src/concierge.ts
  - packages/concierge/test/consent-kernel.test.ts
autonomous: true
requirements: [CON-01, CON-02, CON-03, CON-04, CON-05, CON-06, CON-08, CON-09]
user_setup: []
must_haves:
  truths:
    - "A successful review creates no authority until its owned completed delivery report arrives; missing, failed, interrupted, stale, or duplicate callbacks stay closed."
    - "One review name owns at most one later handler entry across every gated action that requires it, and retrying one callId returns the exact same Promise without another transition."
    - "Confirm uses the exact frozen validated review payload and detached review snapshot, compares current state immediately before handler entry, and consumes before application code."
    - "Same-boundary and pre-entry abort attempts do not consume; drift, comparator failure, fresh review, handler entry/failure, decline, and dismissal cannot rearm authority."
    - "No consent-bearing policy can arm or enter a handler with achieved grade none, even if its minGrade is omitted or explicitly none and an upstream catalog defense is fault-injected away."
  artifacts:
    - path: "packages/concierge/src/concierge.ts"
      provides: "Factory-local generation ledger, delivery arming, binding, late snapshot comparison, one-shot ack injection"
      contains: "consent"
    - path: "packages/concierge/test/consent-kernel.test.ts"
      provides: "Built-artifact K-case state-machine and exact identity proof"
      contains: "CON-01"
  key_links:
    - from: "dispatch cache Promise"
      to: "review ledger mutations"
      via: "all transitions occur only inside runDispatchPipeline after cache installation"
      pattern: "runDispatchPipeline"
    - from: "captured review args/snapshot"
      to: "handler ctx.ack"
      via: "stored object references, never confirm-time reconstruction"
      pattern: "ack"
---

<objective>
Implement the delivery-armed, generation-guarded, review-keyed, one-shot consent state machine for non-attested evidence.

Purpose: This is the core safety boundary: freshness, completed delivery, turn/response separation, snapshot equality, and one-shot consumption must all hold before application code begins.

Output: A lazy factory-local ledger inside createConcierge and a focused built-artifact consent suite proving D-08-01 through D-08-08.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-consent-kernel/08-CONTEXT.md
@.planning/phases/08-consent-kernel/08-RESEARCH.md
@.planning/phases/08-consent-kernel/08-PATTERNS.md
@.planning/phases/08-consent-kernel/08-VALIDATION.md
@.planning/phases/08-consent-kernel/08-02-SUMMARY.md
<dependency_rationale>Plan 08-02 supplies the captured profile, complete catalog, and construction gates. This plan edits only concierge.ts and can run in parallel with Plan 08-05's Session work.</dependency_rationale>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1 (08-03-01): Replace review authority by generation and arm only on owned completed delivery</name>
  <read_first>
    - packages/concierge/src/concierge.ts — edited single build, factory-local maps, runDispatchPipeline ordering, non-async dispatch cache, handler normalization, and captured config seams.
    - packages/concierge/test/consent-kernel.test.ts — new edited built-dist K01+ suite.
    - packages/concierge/src/bridge.ts — captureSnapshot and detachment semantics; reuse without editing.
    - packages/concierge/src/dispatch.ts — exact ToolBatch metadata forwarding and serial batch boundary; preserve without editing.
    - packages/concierge/src/types.ts — ConsentPolicy, DeliveryReport, ConsentAck, USER_DECLINED, USER_CANCELLED, and closed reasons.
    - .planning/phases/06-dispatcher/06-CONTEXT.md — non-async exact-Promise dedupe and commit/cancellation contracts.
    - .planning/phases/08-consent-kernel/08-CONTEXT.md — D-08-01 through D-08-05 and D-08-08.
  </read_first>
  <files>packages/concierge/src/concierge.ts, packages/concierge/test/consent-kernel.test.ts</files>
  <behavior>
    - K01/K02: no review and pending delivery return onMissing or consent_required with zero gated-handler entries.
    - K03-K08: successful review installs pending before a synchronous callback; only matching completed response ownership arms; interruption, missing/throwing hook, late/duplicate callback do not.
    - K09-K11: a fresh validated review replaces pending/armed authority and exact duplicate callId returns one Promise/generation.
    - K12: two gated actions sharing one review cannot fan one confirmation into two effects.
    - N01/N02: under a test-isolated bypass of only the catalog grade issue, none-profile policies with omitted or explicit-none minGrade still cannot arm or enter a handler after completed delivery.
  </behavior>
  <action>
Write K01-K12 first, build, and retain the expected RED observations before production edits. Per D-08-01/D-08-02, add lazily allocated Map state and a monotonic generation counter inside each createConcierge closure, keyed only by the globally unique review action name. Derive the set of review names from the complete catalog; add no module state or transport ledger.

After ordinary argument validation succeeds for a real review action, immediately invalidate its prior slot, reuse the exact frozen `validatedSnapshot.value` as both review-handler args and future ack payload, resolve/capture the review-time bridge snapshot through the existing resolveBridge/captureSnapshot/normalizer seams, and install the new generation. Preserve the Phase 6 commit window and resolve the handler's live bridge at its established pre-entry point; the earlier capture must not hoist or replace handler bridge resolution.

Only after the normalized review result is successful may delivery registration proceed. Require a callable captured hook and nonempty review response id. Install `pendingDelivery` before invoking the hook so synchronous delivery is safe. A callback may mutate only when review key, generation, and response id still own the slot. Matching `outcome: "completed"` derives at most the profile-clipped relayed grade. Add one module-private measured-grade predicate whose load-bearing rule is `achievedGrade !== "none"`; call it before any transition to `armed`. If clipping or evidence evaluation yields none, never create an armed slot: retain only a closed grade-unavailable terminal for the owned generation so the later attempt returns the fixed `grade_unavailable` result without entering application code. Interruption, failed review, missing/malformed hook, registration throw, response mismatch, late/repeated callback, or supersession destroys/leaves closed without echoing caught values. Keep dispatch non-async and every mutation behind the existing cached pipeline.

Add N01/N02 as a deliberate defense-in-depth runtime probe. In a separately reset Vitest module graph, mock only the built internal catalog call so it delegates to the real `buildCatalog` with a delivered profile while `createConcierge` itself still captures the caller's actual none/none profile; export no test hook and change no public type. Drive one response-bound action with omitted `minGrade` and one with explicit `minGrade: "none"` through successful review and completed delivery. Both must return `grade_unavailable`, create no armed authority, and record zero gated-handler entries. The ordinary unmocked catalog tests in Plan 08-02 remain the primary construction defense; this isolated probe exists so removing the independent runtime none guard is behaviorally observable.
  </action>
  <verify><automated>pnpm --filter @fullselfbrowsing/concierge build &amp;&amp; pnpm exec vitest run packages/concierge/test/consent-kernel.test.ts</automated></verify>
  <done>K01-K12 prove one factory-local generation, pending-before-callback ordering, completed owned delivery arming, exact retry identity, and shared-review one-shot authority.</done>
  <acceptance_criteria>
    - Named tests were observed RED before the state machine existed and GREEN after it.
    - Fresh review invalidates immediately after successful validation even if later commit/handler/delivery fails.
    - No failed/interrupted/stale/duplicate callback leaves armed authority or leaks a caught sentinel.
    - N01/N02 prove a none achieved grade cannot arm even when only the upstream catalog floor is deliberately bypassed in an isolated module graph; no production test seam or public export is added.
    - Two factories and two review names are isolated; one shared review cannot fan out.
    - `dispatch` remains non-async and duplicate callId returns the exact same Promise object.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (08-03-02): Bind, compare late, consume atomically, and inject the exact ack</name>
  <read_first>
    - packages/concierge/src/concierge.ts — Task 1 ledger plus validation/commit/abort/bridge/handler order being edited.
    - packages/concierge/test/consent-kernel.test.ts — Task 1 K-cases and new binding/drift/identity cases.
    - packages/concierge/src/bridge.ts — captureSnapshot failure/detachment and default normalization limits.
    - packages/concierge/src/message.ts — existing authored result sanitation boundary.
    - packages/concierge/src/types.ts — exact ConsentAck union, policy onMissing, reason constants, and ServerChallenge non-minting boundary.
    - .planning/phases/05-bridge-registry-and-the-no-bridge-path/05-CONTEXT.md — snapshot detachment and no-bridge contracts.
    - .planning/phases/08-consent-kernel/08-CONTEXT.md — D-08-05 through D-08-08 and exact payload idea.
  </read_first>
  <files>packages/concierge/src/concierge.ts, packages/concierge/test/consent-kernel.test.ts</files>
  <behavior>
    - K13-K16: missing/empty/same user-turn or response boundary fails before handler; same-boundary and abort preserve otherwise armed authority.
    - K17-K20: late detached snapshot drift or comparator throw destroys with consent_stale; unchanged snapshot enters once.
    - K21-K24: ack payload/snapshot are the stored references, consume precedes handler/reentrancy, and handler throw/invalid result/second confirm never restores.
    - Declined and dismissed terminal observations return exact USER_DECLINED/USER_CANCELLED semantics once and require a fresh review.
    - A none achieved grade or a grade below `max("delivered", policy.minGrade ?? "delivered")` is destroyed with grade_unavailable before handler entry; explicit none never weakens this runtime floor.
  </behavior>
  <action>
Add K13-K24 RED cases, including reference identity rather than only deep equality. For a gated action resolve the slot named by `consent.requires`; absent/pending uses the frozen declared `onMissing` result or authored consent_required. For userTurn binding require nonempty review and confirm ids, captured profile provenance exactly human-attested, and different ids. For response binding require nonempty different response ids. Same-boundary refusal and any pre-entry abort return closed without consuming.

Preserve Phase 6 validation, commit-window, and cancellation ordering. Immediately before handler entry, capture/normalize the current detached snapshot and invoke the action's function-property comparator when present; otherwise use a private strict structural comparison over captured primitives/arrays/plain records and supported detached Date/Map/Set values, with exotic leaves compared by identity. Comparator/capture throw or mismatch deletes the generation and returns consent_stale. If the armed achieved grade is below minGrade, delete it and return grade_unavailable.

Before the ordinary minimum-grade comparison, call Task 1's module-private measured-grade predicate again so no anomalous or fault-injected armed state with achieved grade none can reach handler entry. Compute the runtime effective minimum independently as the stronger of `"delivered"` and `policy.minGrade ?? "delivered"`; an explicit `"none"` is clamped to delivered. If achieved grade is none or below that effective minimum, delete the owned generation and return `grade_unavailable`. This entry guard is mandatory even though the delivery transition already rejects none, and it must not rely on catalog validity.

Atomically transition/delete the owned armed generation before application code, then create one newly frozen ConsentAck with stored review ids, stored snapshot reference, exact stored payload reference, achieved grade, and verified evidence available at that grade. Do not mint or infer ServerChallenge. Pass the ack through the existing single handler context. Handler success, throw/rejection, invalid result, reentrant confirm, and second confirm never restore it. Retain decline/dismiss as terminal non-authority observations only long enough for the next gated attempt to return the existing frozen USER_DECLINED/USER_CANCELLED result, then remove them; a fresh review replaces them immediately.
  </action>
  <verify><automated>pnpm --filter @fullselfbrowsing/concierge build &amp;&amp; pnpm exec vitest run packages/concierge/test/consent-kernel.test.ts &amp;&amp; pnpm --filter @fullselfbrowsing/concierge typecheck</automated></verify>
  <done>K13-K24 prove genuine-boundary binding, late drift destruction, exact stored identity, consume-before-entry, terminal refusal/dismissal, and permanent one-shot behavior.</done>
  <acceptance_criteria>
    - Same-boundary and pre-entry abort leave one armed generation usable by a later valid boundary; no other listed terminal does.
    - Snapshot comparison occurs after the commit window and final abort check, immediately before entry; mismatch/throw is consent_stale.
    - The gated handler observes `ack.payload === reviewHandlerArgs` and the exact stored frozen snapshot object.
    - Reentrancy and handler failure cannot run the effect twice or restore consent.
    - The same measured-grade predicate is checked both before arming and before handler entry; the runtime effective minimum is never below delivered for omitted or explicit-none policies.
    - Every result passes existing sanitizer/reason vocabulary; no catch-all reason or caught text is introduced.
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Agent calls → review ledger | Retries, same-turn calls, and shared review targets can mint or consume authority twice. |
| Delivery callback → armed state | Hostile synchronous/late/repeated callbacks can arm the wrong generation. |
| Live app state → handler entry | Drift or a throwing comparator can invalidate what was reviewed. |

## STRIDE Threat Register
| Threat ID | Severity | Category | Component | Disposition | Mitigation and verification |
|-----------|----------|----------|-----------|-------------|-----------------------------|
| T-08-01 | high | Spoofing | turn/response binding | mitigate | Nonempty distinct ids plus human-attested provenance; K13-K16 block same/forgeable boundaries. |
| T-08-02 | high | Elevation of privilege | delivery arming | mitigate | Pending-before-hook, generation+response ownership, completed-only K03-K11. |
| T-08-03 | high | Tampering | payload/snapshot | mitigate | Detached exact args, late capture/comparison, destructive stale outcome, K17-K22. |
| T-08-04 | high | Elevation of privilege | achieved-grade arming and entry | mitigate | The catalog-independent measured-grade predicate blocks none both before arming and before entry; the runtime minimum is clamped to delivered, and N01/N02 plus the G none-guard mutant detect removal. |
| T-08-07 | high | Repudiation / Elevation | dedupe/consume | mitigate | Existing exact Promise cache plus consume-before-handler and reentrancy/second-confirm K09/K21-K24. |
| T-08-10 | high | Information disclosure | callbacks/comparator | mitigate | No-binding catches, fixed results, secret-sentinel tests. |
| T-08-SC | low | Tampering | supply chain | accept | No install or lockfile change. |
</threat_model>

<verification>
- Build before exact consent suite; runtime tests import dist.
- Package typecheck and all prior dispatcher/bridge contracts remain green.
</verification>
<success_criteria>
- D-08-01 through D-08-08 are observable and mutation-ready.
- D-08-09's weakest-profile contract is independently defended at runtime: achieved none can neither arm nor enter, regardless of an omitted or explicit-none policy minimum.
- No application handler begins without one owned, delivered, fresh, exact, atomically consumed grant.
</success_criteria>
<output>Create `.planning/phases/08-consent-kernel/08-03-SUMMARY.md` when done.</output>
