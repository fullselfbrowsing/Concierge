---
phase: 08-consent-kernel
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - packages/concierge/src/consent-profile.ts
  - packages/concierge/src/catalog.ts
  - packages/concierge/src/concierge.ts
  - packages/concierge/test-d/catalog.test-d.ts
  - packages/concierge/test/catalog.test.ts
  - packages/concierge/test/concierge.test.ts
  - packages/concierge/test/diagnostic-safety.test.ts
autonomous: true
requirements: [CAT-04, CON-07, TRN-03, TRN-05]
user_setup: []

must_haves:
  truths:
    - "A gated catalog refuses every action whose effective minimum grade or user-turn binding exceeds the declared app profile, naming each offender and the exact fix in one aggregate error."
    - "Every consent-bearing action has an inherent delivered floor: omitted or explicit none minGrade cannot weaken it, so a frozen none/none profile rejects all gated actions while ungated direct dispatch still builds."
    - "An attested action cannot construct without both the app readback presenter and injected digest, and no policy is silently downgraded."
    - "The captured profile belongs to one Concierge instance, survives caller mutation, and is available to Session through a private non-enumerable marker without module-scoped mutable state."
  artifacts:
    - path: "packages/concierge/src/consent-profile.ts"
      provides: "Safe profile snapshot, ordering/dominance helpers, and private Concierge profile marker"
      contains: "consentProfileOf"
    - path: "packages/concierge/src/catalog.ts"
      provides: "CAT-04/TRN-03 and attested-seam aggregate validation inside the one flat build"
      contains: "consent_grade_unavailable"
    - path: "packages/concierge/test/catalog.test.ts"
      provides: "Mixed aggregate, omitted/explicit-none effective-floor, every-offender, and seam-presence runtime proof"
      contains: "CAT-04"
  key_links:
    - from: "packages/concierge/src/concierge.ts"
      to: "packages/concierge/src/catalog.ts"
      via: "one buildCatalog call carrying the frozen profile and captured evidence seams"
      pattern: "buildCatalog"
    - from: "packages/concierge/src/consent-profile.ts"
      to: "packages/concierge/src/session.ts"
      via: "later private profile read; no exported/public Concierge member"
      pattern: "consentProfileOf"
    - from: "ConsentPolicy.bindTo"
      to: "ConsentProfile.userTurnIdentity"
      via: "human-attested-only build rule"
      pattern: "user_turn_identity_unavailable"
    - from: "ConsentPolicy.minGrade"
      to: "ConsentProfile.consentGrade"
      via: "max(delivered, minGrade ?? delivered) effective-minimum comparison"
      pattern: "consent_grade_unavailable"
---

<objective>
Make declared consent capability a captured, factory-local build authority and enforce CAT-04/TRN-03 plus attested-seam availability during the existing single catalog build.

Purpose: A runtime gate is already too late when an app or transport cannot possibly supply the grade or provenance an action declares. This plan rejects impossible configurations once, names every action, and gives Session a trustworthy captured ceiling for its later dominance check.

Output: One private profile module, four new structured catalog issue codes, the one flat profile-aware build call, frozen per-Concierge profile ownership, and exact aggregate/runtime/type tests.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-consent-kernel/08-CONTEXT.md
@.planning/phases/08-consent-kernel/08-RESEARCH.md
@.planning/phases/08-consent-kernel/08-PATTERNS.md
@.planning/phases/08-consent-kernel/08-VALIDATION.md
@.planning/phases/08-consent-kernel/08-01-SUMMARY.md
@.planning/phases/04-stages-catalog-assembly-and-explain/04-CONTEXT.md

<interfaces>
From Plan 08-01:
- `ConsentProfile = { readonly consentGrade: ConsentGrade; readonly userTurnIdentity: TurnIdentityProvenance }`.
- `ConciergeConfig.consentProfile?: ConsentProfile | undefined`.

Existing catalog boundary:
- `buildCatalog(actions, options?)` performs one per-action pass plus complete-name post-pass and throws one CatalogValidationError containing frozen structured issues.
- CatalogIssue fields remain `{ code, action, vendor?, problem, fix }`.
- createConcierge currently calls buildCatalog exactly once over stage actions followed by crossStage actions; every stage catalog is a projection of that result.
</interfaces>

<dependency_rationale>
Plan 08-01 supplies the public profile type. This plan must precede both the ledger and Session work because it captures the declared ceiling, makes impossible policies unconstructible, and installs the private profile marker those independent Wave 3 plans consume.
</dependency_rationale>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (08-02-01): Aggregate CAT-04/TRN-03 and attested-seam build failures</name>
  <read_first>
    - packages/concierge/src/catalog.ts — edited CatalogIssueCode, BuildCatalogOptions, hostile-property helpers, one per-action pass, complete-name post-pass, and aggregate throw.
    - packages/concierge/test-d/catalog.test-d.ts — edited BuildCatalogOptions/profile shape and catalog type pins.
    - packages/concierge/test/catalog.test.ts — edited ordered issue arrays, mixed-fault aggregation, hostile declaration, and diagnostic tests.
    - packages/concierge/src/types.ts — ConsentProfile, ConsentPolicy, CONSENT_GRADE_ORDER, ReadbackSink, DigestLike, and provenance unions from Plan 08-01.
    - .planning/phases/08-consent-kernel/08-CONTEXT.md — D-08-09, D-08-10, D-08-12, and D-08-17.
    - .planning/phases/08-consent-kernel/08-VALIDATION.md — T-08-01/T-08-04 and original rows 08-03-01/02 (implemented here as 08-02-01/02).
  </read_first>
  <files>packages/concierge/src/catalog.ts, packages/concierge/test-d/catalog.test-d.ts, packages/concierge/test/catalog.test.ts</files>
  <behavior>
    - RED: exact catalog cases fail because grade, provenance, and missing evidence seams are not yet checked.
    - Every valid offending action contributes one deterministic issue per independent fault; existing schema/redaction/reference faults remain in the same aggregate throw.
    - Profile absence is exactly none/none, not a permissive default; both omitted `minGrade` and explicit `minGrade: "none"` on a consent-bearing action still require at least delivered and are rejected.
    - A delivered profile accepts those same omitted/explicit-none policies, proving the floor is delivered rather than an accidental blanket rejection.
    - Forward references and the one-flat-build complete-name behavior stay green.
  </behavior>
  <action>
Write the exact runtime and type cases first, run a built catalog suite, and retain the named RED failures. Extend CatalogIssueCode with exactly `consent_grade_unavailable`, `user_turn_identity_unavailable`, `readback_presenter_missing`, and `digest_missing`. Extend BuildCatalogOptions with EOPT-safe `consentProfile`, `presentReadback`, and `digest` members using the existing public types; these are construction evidence, not alternate catalogs.

Inside the existing single per-action pass, after consent is safely captured and without filtering the action from later checks, compute every consent-bearing action's effective minimum as the stronger of `"delivered"` and `policy.minGrade ?? "delivered"`. Concretely, start from `policy.minGrade ?? "delivered"` and clamp any explicit `"none"` back to `"delivered"`; `"none"` is never an actionable human-in-loop grade. Rank that effective minimum against `options.consentProfile?.consentGrade ?? "none"`. Per D-08-09/D-08-10, append `consent_grade_unavailable` once for every action above the ceiling, including response-bound actions with omitted `minGrade` and response-bound actions that explicitly declare `minGrade: "none"`. Report their required grade as `delivered`, not `none`. Append `user_turn_identity_unavailable` for every `bindTo: "userTurn"` action unless the declared provenance is exactly `human-attested`, including absent/none/agent-forgeable profiles. For an action whose effective requested grade is `attested`, independently append presenter and digest issues when their captured options are not callable. Every issue names the gated action—not its review target—and carries required value, declared value, and an actionable fix in structured fields. Never lower minGrade, treat explicit none as an escape hatch, rewrite bindTo, stop after one offender, or build a second catalog.

Keep hostile action-property reads within existing own-property/catch boundaries and never echo getter values. Add separate catalog cases for an absent none/none profile with a response-bound omitted `minGrade`, an absent none/none profile with explicit `minGrade: "none"`, the same two policies under a delivered profile, a stronger profile, grade-only offender, both provenance offenders, several actions sharing one review, both missing seams, one missing seam, mixed existing/new faults, and post-pass forward references. Update existing consent-bearing tests to declare only the capability they genuinely need so old CAT-03 behavior is isolated rather than accidentally failing under the new weakest default. Do not narrow the public `ConsentGrade` or `ConsentPolicy` types: explicit `none` remains representable but cannot lower the kernel's inherent delivered floor.
  </action>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge build &amp;&amp; pnpm exec vitest run packages/concierge/test/catalog.test.ts &amp;&amp; pnpm --filter @fullselfbrowsing/concierge typecheck</automated>
  </verify>
  <done>One catalog build reports every impossible grade/provenance/evidence seam alongside existing issues, while valid stronger profiles and complete forward-reference catalogs build unchanged.</done>
  <acceptance_criteria>
    - The pre-implementation run is RED on named CAT-04/TRN-03/seam cases rather than incidental TypeErrors.
    - Issue order is deterministic and every offender appears exactly once per applicable code.
    - Absent profile is none/none; every gated action fails and an ungated action builds.
    - Response-bound policies with omitted `minGrade` and explicit `minGrade: "none"` both emit `consent_grade_unavailable` requiring delivered under a none profile, and both build under a delivered profile.
    - `bindTo: "userTurn"` accepts only human-attested, never agent-forgeable merely because an id can exist.
    - Attested requirements independently require callable presentReadback and digest; lower grades do not.
    - Existing issue aggregation, forward reference, self-reference, redaction, and diagnostic tests remain green.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (08-02-02): Capture the per-Concierge profile and preserve the single flat build</name>
  <read_first>
    - packages/concierge/src/consent-profile.ts — new edited internal module; follow factory-local/no-module-mutable-state patterns from bridge.ts.
    - packages/concierge/src/concierge.ts — edited config capture, one buildCatalog call, mutable factory state, and exact five-string-key return handle.
    - packages/concierge/test/concierge.test.ts — edited one-flat-build, factory isolation, frozen projection, and consent-reference fixtures.
    - packages/concierge/test/diagnostic-safety.test.ts — edited hostile subject/getter and no-secret-echo assertions.
    - packages/concierge/src/bridge.ts — governing monotonic/factory-local ownership analog; read only.
    - packages/concierge/src/catalog.ts — Task 1's BuildCatalogOptions and exact new issue codes.
    - .planning/phases/08-consent-kernel/08-PATTERNS.md — private marker, captured config, and no WeakMap guidance.
  </read_first>
  <files>packages/concierge/src/consent-profile.ts, packages/concierge/src/concierge.ts, packages/concierge/test/concierge.test.ts, packages/concierge/test/diagnostic-safety.test.ts</files>
  <behavior>
    - RED: caller mutation, invalid/accessor profile, and cross-instance profile cases expose the absent capture/ownership mechanism.
    - GREEN: createConcierge reads profile and evidence seams once, freezes a detached valid profile, and calls buildCatalog exactly once with them.
    - The returned handle still has exactly five enumerable string keys and is deliberately not frozen.
    - A structural Concierge not authored by createConcierge reads as weakest profile for Session compatibility.
  </behavior>
  <action>
Create internal `consent-profile.ts` with immutable module declarations only. Define one frozen `WEAKEST_CONSENT_PROFILE` (`none`/`none`), descriptor-aware `snapshotConsentProfile(value)` that returns it for absence and otherwise reads each own data field once, validates the closed grade/provenance literals, copies/freezes the result, and throws one fixed detail-free configuration error for accessors, proxies/exotics, malformed values, or reads that throw. Add grade/provenance ordering helpers and `profileDominates(actual, declared)` for Plan 08-05.

Use one private unique-symbol marker plus non-enumerable, non-writable, non-configurable attachment/read helpers to carry the captured profile on the core-authored Concierge handle. Do not export the symbol from the package, add a public Concierge key, use a module-level WeakMap/registry, freeze the returned Concierge, or trust a caller-supplied lookalike symbol. `consentProfileOf` returns the weakest profile for a structural non-core Concierge.

In createConcierge per D-08-09, capture `consentProfile`, `presentReadback`, `digest`, and `normalizeSnapshot` exactly once before outside runtime use. Pass the frozen profile plus the actual presenter/digest references to the existing one flat `buildCatalog([...stage actions, ...crossStage], options)` call. Attach the private profile to the unchanged five-method return handle. Add built-artifact cases for caller mutation after construction, two interleaved Concierge instances with different profiles, absent profile, accessor/throw containment with secret sentinels, exact one-build behavior, and attested construction failures. Update only existing test fixtures whose declared consent now legitimately requires an explicit profile.
  </action>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge build &amp;&amp; pnpm exec vitest run packages/concierge/test/concierge.test.ts packages/concierge/test/diagnostic-safety.test.ts packages/concierge/test/catalog.test.ts</automated>
    <automated>pnpm --filter @fullselfbrowsing/concierge typecheck</automated>
    <automated>bash -c '! rg -n "WeakMap&lt;Concierge|new WeakMap.*Concierge|buildCatalog.*buildCatalog" packages/concierge/src/consent-profile.ts packages/concierge/src/concierge.ts'</automated>
  </verify>
  <done>Each Concierge owns one detached immutable declared ceiling, impossible configs fail during its sole flat catalog build, and Session can later read the ceiling privately without widening the public handle.</done>
  <acceptance_criteria>
    - Captured profile and seam getters are read once; later caller mutation cannot change catalog or private Session authority.
    - Two factories never share profile state, counters, or issues.
    - Invalid/accessor/throwing profile input fails with fixed prose and no secret/caught value.
    - Exactly one buildCatalog invocation remains in createConcierge and every per-stage catalog remains a projection.
    - Returned Concierge retains exactly five enumerable string members, no public profile member, and no module-scoped mutable registry.
    - Built focused suites and typecheck are green with pnpm-lock.yaml unchanged.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JavaScript config → captured profile | Accessors, mutation, invalid enum values, or a forged stronger ceiling could make an impossible gate appear valid. |
| Consent declaration → flat catalog | Per-action validation can under-report, fail fast, or accidentally validate the review target instead of the gated action. |
| Concierge handle → Session | A public/mutable or module-global profile channel could cross instances or be overwritten after build. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation and verification |
|-----------|----------|----------|-----------|-------------|-----------------------------|
| T-08-01 | high | Spoofing | user-turn build gate | mitigate | Every userTurn policy requires human-attested provenance; absent/agent-forgeable profiles produce named issues and negative tests. |
| T-08-04 | high | Elevation of privilege | grade/profile build gate | mitigate | Every consent policy is clamped to at least delivered before the per-offender rank comparison; omitted or explicit none cannot bypass the weakest profile, and focused catalog tests plus C-floor mutants are blocking. |
| T-08-05 | high | Tampering | attested seam availability | mitigate | Attested actions require both actual presenter and digest references during construction, preventing a declaration-only attested path. |
| T-08-10 | high | Tampering / Information disclosure | hostile config capture | mitigate | Descriptor-aware one-read snapshot, fixed errors, no caught-value binding, per-instance frozen profile, and secret-sentinel tests. |
| T-08-SC | low | Tampering | package supply chain | accept | No package install or dependency change; release input hashes later enforce manifest/lock identity. |
</threat_model>

<verification>
- Catalog focused suite proves complete ordered aggregation and weakest defaults.
- Concierge/diagnostic suites prove one flat build, profile immutability/isolation, and safe hostile config handling.
- Package typecheck remains green; no public runtime export or lockfile change occurs.
</verification>

<success_criteria>
- CAT-04 and TRN-03 are enforceable before runtime with actionable named issues, including the inherent delivered floor for omitted or explicit-none policies.
- Attested policies cannot construct without their two injected seams.
- Both direct dispatch and Session share one captured declared ceiling without a public or global registry.
</success_criteria>

<output>
Create `.planning/phases/08-consent-kernel/08-02-SUMMARY.md` when done.
</output>
