---
phase: 08-consent-kernel
plan: 06
type: execute
wave: 5
depends_on:
  - 08-04
  - 08-05
files_modified:
  - packages/concierge/test/fixtures/stub-transport.ts
  - packages/concierge/test-d/stub-transport.test-d.ts
  - packages/concierge/test/stub-transport.test.ts
  - packages/concierge/test/consent-kernel.test.ts
  - packages/concierge/test/session-consent.test.ts
autonomous: true
requirements:
  - CON-01
  - CON-02
  - CON-03
  - CON-04
  - CON-05
  - CON-06
  - CON-07
  - CON-08
  - CON-09
  - CON-10
  - CAT-04
  - TRN-02
  - TRN-03
  - TRN-05
must_haves:
  truths:
    - "The exact Phase 7 stub can drive every consent, delivery-evidence, capability, and outcome branch deterministically without network access."
    - "A review whose delivery is interrupted never releases its action, even after a genuine later user turn; only a new successful review can arm consent."
    - "The full consent and outcome matrix proves ordering and one-shot behavior through public package surfaces."
  artifacts:
    - path: packages/concierge/test/fixtures/stub-transport.ts
      provides: "Extended test-only transport fixture, sibling controls, immutable histories, and shared event log"
    - path: packages/concierge/test-d/stub-transport.test-d.ts
      provides: "Compile-time proof that the transport public shape remains exact"
    - path: packages/concierge/test/stub-transport.test.ts
      provides: "Fixture behavior, ordering, immutability, and failure-injection tests"
    - path: packages/concierge/test/consent-kernel.test.ts
      provides: "Flagship interrupted-delivery and complete consent matrix"
  key_links:
    - from: packages/concierge/test/fixtures/stub-transport.ts
      to: Transport
      via: "the transport retains the exact six enumerable string keys while new controls remain on the sibling fixture object"
      pattern: "transport.*controls"
    - from: packages/concierge/test/consent-kernel.test.ts
      to: packages/concierge/test/fixtures/stub-transport.ts
      via: "public-flow tests manually report delivery and outcome events"
      pattern: "createStubTransport"
---

<objective>
Extend the exact test-only transport fixture and use it to prove the entire consent kernel and outcome barrier end to end.

Purpose: The security contract needs deterministic, public-surface tests for timing, identity, interruption, and ordering—not isolated helper assertions.
Output: A backward-compatible Phase 7 stub extension, type/runtime fixture contracts, and integrated no-network test matrices.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-session-and-the-transport-seam/07-CONTEXT.md
@.planning/phases/08-consent-kernel/08-CONTEXT.md
@.planning/phases/08-consent-kernel/08-PATTERNS.md
@.planning/phases/08-consent-kernel/08-04-SUMMARY.md
@.planning/phases/08-consent-kernel/08-05-SUMMARY.md
@packages/concierge/test/fixtures/stub-transport.ts
@packages/concierge/test/stub-transport.test.ts

<interfaces>
Extend the existing `createStubTransport` fixture in place per D-08-21. The object assigned to `transport` must still satisfy the exact Phase 7 six-key runtime and type contract; all test-driving APIs belong on the returned sibling controls object. Histories expose immutable snapshots, and each attempted event is recorded before an injected failure is raised.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the Phase 7 stub with consent and outcome controls</name>
  <files>packages/concierge/test/fixtures/stub-transport.ts, packages/concierge/test-d/stub-transport.test-d.ts, packages/concierge/test/stub-transport.test.ts</files>
  <read_first>packages/concierge/test/fixtures/stub-transport.ts, packages/concierge/test-d/stub-transport.test-d.ts, packages/concierge/test/stub-transport.test.ts, packages/concierge/src/types.ts, .planning/phases/08-consent-kernel/08-VALIDATION.md — canonical T-08-02, T-08-06, T-08-08, and T-08-10 definitions</read_first>
  <behavior>
    - Existing Phase 7 construction and controls remain source-compatible, and `Object.keys(transport)` stays exactly the six established keys.
    - Sibling controls can register and manually complete/interrupt delivery callbacks, select actual consent profiles, present outcomes, and inject synchronous/asynchronous failures.
    - Delivery, outcome, and response attempts append immutable snapshots to both focused histories and one monotonically ordered shared event history before failure injection.
    - No fixture symbol is exported by the production entry point or included as a production module in the packed tarball.
  </behavior>
  <action>Extend the exact fixture in place per D-08-21; do not create a competing stub. Add sibling-only controls for the review delivery callback lifecycle, manual `DeliveryReport` issuance, actual capability profiles, the required outcome sink, configurable completed/interrupted/throw/reject behavior, response failures, and a shared monotonic event history. Preserve every old control and default so Phase 7 tests still compile unchanged except where the new required SessionConfig sink is supplied. Record each attempted boundary crossing before triggering its configured failure. Deep-copy and freeze all externally visible histories and reports so tests cannot mutate fixture truth. Keep delivery callbacks owned and single-fire in the fixture, but allow explicit late/duplicate attempts so production code—not the fixture—proves rejection. Add type-level and runtime assertions for the exact six-key `Transport`, sibling controls, readonly histories, and test-only packaging boundary. Map delivery controls to canonical T-08-02, attestation controls to T-08-06, outcome controls to T-08-08, and hostile callback/getter failure injection to T-08-10.</action>
  <acceptance_criteria>
    - All pre-Phase-8 stub tests retain their behavioral assertions.
    - Type tests reject placing any new control on `Transport`, and runtime tests assert its exact key set.
    - Histories distinguish attempt order from success and cannot be mutated by consumers.
    - `pnpm --filter @fullselfbrowsing/concierge build &amp;&amp; pnpm exec vitest run packages/concierge/test/stub-transport.test.ts &amp;&amp; pnpm --filter @fullselfbrowsing/concierge typecheck` is green and the suite imports the built package rather than a production-private helper.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge build &amp;&amp; pnpm exec vitest run packages/concierge/test/stub-transport.test.ts &amp;&amp; pnpm --filter @fullselfbrowsing/concierge typecheck</automated>
  </verify>
  <done>The established test-only stub can deterministically drive all Phase 8 boundaries without expanding the production Transport contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the flagship interruption case and the full public-flow matrix</name>
  <files>packages/concierge/test/consent-kernel.test.ts, packages/concierge/test/session-consent.test.ts</files>
  <read_first>packages/concierge/test/fixtures/stub-transport.ts, packages/concierge/test/consent-kernel.test.ts, packages/concierge/test/session-consent.test.ts, packages/concierge/src/index.ts, .planning/phases/08-consent-kernel/08-VALIDATION.md — canonical T-08-01 through T-08-10 detector meanings</read_first>
  <behavior>
    - Flagship: review presentation is interrupted, a genuine new human turn arrives, the action remains closed, and no stale delivery completion can arm it.
    - Positive: a fresh review, completed delivery, verified receipt, and matching confirmed attestation release the exact frozen payload once.
    - Negative matrix covers same-turn replay, changed args, late/duplicate callback, wrong turn/generation/response/hash, forged profile, decline, dismissal, interruption, and every missing evidence component.
    - Outcome history proves failures are presented before responses, all-success bypasses presentation, and a later FIFO occurrence survives an interrupted outcome.
  </behavior>
  <action>Drive the real built package exclusively through public APIs and the extended Phase 7 fixture—no private helper imports, fake network, or clock sleeps. Implement the D-08-22 flagship first: capture a review, report interrupted delivery, emit a genuine different user turn, attempt execution, and assert exact `consent_required` closure plus zero handler and response effects; then attempt late completion and prove it remains closed. Add the complete positive and negative matrix covering D-08-01 through D-08-20. Assert exact diagnostic reason codes, frozen acknowledged arguments, one-shot consumption, response/outcome ordering, actual-capability dominance, callback ownership, and listener cleanup. Include the TRN-02 regression: only eligible completed work may advance to later work; interrupted occurrences release nothing yet do not poison a genuinely new occurrence. Keep detector names aligned with 08-VALIDATION: T-08-01 boundary/provenance; T-08-02 completed-delivery arming; T-08-03 payload/snapshot drift; T-08-04 evidence-derived rather than capability-derived grade; T-08-05 canonical receipt/hash verification; T-08-06 confirmed human act; T-08-07 dedupe/reentrancy/one-shot; T-08-08 app outcome before agent response; and T-08-10 hostile-object/callback containment. Avoid assertions against implementation-local names.</action>
  <acceptance_criteria>
    - The flagship fails if interrupted delivery is treated as consent or if a later turn implicitly re-arms it.
    - Every locked decision has at least one public-flow detector, and each critical conjunction has a remove-one-component negative.
    - Tests use deterministic fixture events and complete without external services or timing races.
    - `pnpm --filter @fullselfbrowsing/concierge build &amp;&amp; pnpm exec vitest run packages/concierge/test/stub-transport.test.ts packages/concierge/test/consent-kernel.test.ts packages/concierge/test/session-consent.test.ts &amp;&amp; pnpm --filter @fullselfbrowsing/concierge typecheck` is green and every named detector selects at least one test.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge build &amp;&amp; pnpm exec vitest run packages/concierge/test/stub-transport.test.ts packages/concierge/test/consent-kernel.test.ts packages/concierge/test/session-consent.test.ts &amp;&amp; pnpm --filter @fullselfbrowsing/concierge typecheck</automated>
  </verify>
  <done>The exact production flow proves the consent kernel, transport capability boundary, outcome barrier, and Phase 7 lifecycle invariants together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test controls → production public API | The fixture injects hostile timing and evidence without bypassing package boundaries. |
| transport callbacks → consent ledger | Late, duplicate, interrupted, or forged events attempt to change owned state. |
| application outcome → agent response | Ordering history proves the release barrier under failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Spoofing / Elevation of privilege | user-turn provenance and boundary | mitigate | Public-flow cases reject missing/same ids and agent-forgeable profiles while accepting only a genuinely different human-attested turn. |
| T-08-02 | Elevation of privilege | review/delivery arming | mitigate | Flagship and callback matrix prove review return, pending, interruption, late completion, and duplicates cannot arm; only owned completed delivery can. |
| T-08-03 | Tampering | reviewed payload and snapshot | mitigate | Changed arguments, detached-state drift, comparator throw, and fresh-review supersession are varied independently with exact reference-identity assertions. |
| T-08-04 | Elevation of privilege | capability versus achieved proof | mitigate | Capability-only and forged-profile cases remain closed; occurrence evidence alone derives the achieved grade. |
| T-08-05 | Tampering / Spoofing | canonical receipt and hashes | mitigate | Canonical bytes, receipt literals, digest, and every hash location have remove-one/change-one public-flow negatives. |
| T-08-06 | Spoofing | delivery hash versus human act | mitigate | Confirmed/declined/dismissed, wrong turn, stale generation, and hash-only cases prove a delivery hash is not confirmation. |
| T-08-07 | Repudiation / Elevation of privilege | retry, reentrancy, and one-shot use | mitigate | Duplicate callId Promise identity, reentrant entry, handler failure, shared-review fan-out, and second-confirm histories prove one consumption. |
| T-08-08 | Information disclosure / Tampering | app outcome before agent response | mitigate | Minimal frozen histories exclude model text and shared ordering proves completed app presentation precedes every response with no retry on failure. |
| T-08-10 | Information disclosure / Tampering | hostile callbacks and objects | mitigate | Fixture-injected getter/callback throws, malformed reports, fixed diagnostics, and secret sentinels prove containment without caught-value echo. |
| T-08-SC | Tampering | package supply chain | mitigate | Fixture adds no package; final pack inspection and lockfile hash gate remain mandatory. |
</threat_model>

<verification>
Build first, run the type contract, then run all three named runtime suites against `dist`. Also run the unchanged Phase 7 stub/session suites to ensure the extension is backward-compatible.
</verification>

<success_criteria>
- The exact existing stub supports the full Phase 8 matrix while Transport remains exact.
- Interrupted delivery cannot become consent through time, a new turn, or a late callback.
- Positive attestation and outcome flows work through public boundaries.
- The fixture remains test-only and network-free.
</success_criteria>

<output>
Create `.planning/phases/08-consent-kernel/08-06-SUMMARY.md` when done.
</output>
