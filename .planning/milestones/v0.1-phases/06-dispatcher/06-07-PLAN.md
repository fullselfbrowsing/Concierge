---
phase: 06-dispatcher
plan: 07
type: execute
wave: 6
gap_closure: true
closes: [VER-06-01, VER-06-02, VER-06-03]
depends_on: ["06-06"]
files_modified:
  - packages/concierge/src/dispatch.ts
  - packages/concierge/src/concierge.ts
  - packages/concierge/src/types.ts
  - packages/concierge/test/dispatcher.test.ts
  - packages/concierge/test/dispatcher-batch.test.ts
autonomous: true
requirements: [DSP-01, DSP-02, DSP-05, DSP-06, DSP-07, SEC-06, TRN-04]
user_setup: []

must_haves:
  truths:
    - "Malformed InvocationMeta primitive fields never throw synchronously; direct dispatch returns one frozen sanitized honest failure without entering a handler."
    - "A malformed callId inside ToolBatch cannot reject the batch or remove correlation: the batch resolves with exactly one frozen row for that input."
    - "Fallback key derivation preserves the tagged collision fix but treats BigInt and cyclic or aliased graphs as unkeyable, so repeated BigInt calls run without deduplication and without throwing."
    - "Malformed batch JSON becomes an empty object and follows the ordinary action validator; a rejecting schema returns invalid_args and later calls still settle."
  artifacts:
    - path: "packages/concierge/src/concierge.ts"
      provides: "Runtime InvocationMeta primitive validation before retry-key derivation"
      contains: "snapshotInvocationMeta"
    - path: "packages/concierge/src/dispatch.ts"
      provides: "Never-throw retry keys, BigInt no-key degradation, and parse-to-empty-object batch routing"
      contains: "deriveDispatchKey"
    - path: "packages/concierge/test/dispatcher.test.ts"
      provides: "R06 locked BigInt behavior and R68 malformed-metadata totality regression"
      contains: "[R68]"
    - path: "packages/concierge/test/dispatcher-batch.test.ts"
      provides: "Q04 validator-mediated malformed JSON, preserved Q16 immutable-result coverage, and Q17 malformed-call correlation regression"
      contains: "[Q17]"
    - path: "packages/concierge/src/types.ts"
      provides: "ToolCall.arguments documentation matching DSP-06"
      contains: "validation"
  key_links:
    - from: "packages/concierge/src/concierge.ts"
      to: "packages/concierge/src/dispatch.ts"
      via: "snapshotInvocationMeta succeeds before deriveDispatchKey"
      pattern: "snapshotInvocationMeta[\\s\\S]*deriveDispatchKey"
    - from: "packages/concierge/src/dispatch.ts"
      to: "Concierge.dispatch"
      via: "executeDispatchBatch substitutes an empty object and delegates to the same single-call validator"
      pattern: "args = \\{\\}[\\s\\S]*await dispatch"
    - from: "packages/concierge/test/dispatcher-batch.test.ts"
      to: "packages/concierge/dist/index.js"
      via: "Q04 and Q17 exercise the built package while the existing Q16 immutable nested-result case remains unchanged"
      pattern: "Q04|Q16|Q17"
---

<objective>
Close the three runtime contract failures reported by Phase 6 verification.

VER-06-01 makes dispatch total for malformed metadata and preserves one correlated batch row. VER-06-02 restores the locked BigInt no-dedup behavior without undoing the injective tagged encoder used for other values. VER-06-03 restores ROADMAP success criterion 3 and DSP-06: malformed wire JSON becomes an empty object and reaches ordinary schema validation.

Purpose: The Phase 6 goal is not met while a malformed call can escape the result boundary, a locked unkeyable-value rule is contradicted, or the batch parser bypasses the validator named by the roadmap.

Output: repaired dispatcher boundaries plus built-artifact regressions R68 and Q17, preserved Q16 immutable-result coverage, and corrected R06/Q04 expectations.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-dispatcher/06-CONTEXT.md
@.planning/phases/06-dispatcher/06-RESEARCH.md
@.planning/phases/06-dispatcher/06-VERIFICATION.md
@.planning/phases/06-dispatcher/06-04-SUMMARY.md
@.planning/phases/06-dispatcher/06-05-SUMMARY.md
@.planning/phases/06-dispatcher/06-06-SUMMARY.md

<interfaces>
From packages/concierge/src/concierge.ts:
- snapshotInvocationMeta(meta: InvocationMeta | undefined): InvocationMetaSnapshot
- dispatch(ctx: StageContext, name: string, args: unknown, meta?: InvocationMeta): Promise&lt;ActionResult>

From packages/concierge/src/dispatch.ts:
- deriveDispatchKey(name: string, args: unknown, meta: InvocationMeta | undefined, authorizationScope: number | null): string | null
- authoredResult(ok: boolean, message: string, reason?: ReasonCode | undefined): ActionResult
- executeDispatchBatch(ctx: StageContext, batch: ToolBatch, dispatch: (...): Promise&lt;ActionResult>): Promise&lt;ReadonlyArray&lt;Readonly&lt;{ callId: string; result: ActionResult }>>>

Locked behavior:
- 06-CONTEXT.md § Dedup says a cycle or BigInt disables fallback deduplication and must not throw.
- ROADMAP Phase 6 success criterion 3 and DSP-06 say malformed JSON becomes an empty object and then reaches action validation.
- The public dispatch and dispatchBatch signatures remain unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (06-07-T1): Contain malformed invocation metadata in direct and batch result boundaries</name>
  <read_first>
    - packages/concierge/src/concierge.ts — snapshotInvocationMeta, non-async dispatch, and the current meta failure branch.
    - packages/concierge/src/dispatch.ts — deriveDispatchKey and authoredResult.
    - packages/concierge/src/types.ts — InvocationMeta primitive field types and the closed ReasonCode vocabulary.
    - packages/concierge/test/dispatcher.test.ts — built-artifact harness and the existing R55-R67 review regressions.
    - packages/concierge/test/dispatcher-batch.test.ts — built-artifact harness, correlation assertions, and Q15.
    - .planning/phases/06-dispatcher/06-VERIFICATION.md — VER-06-01 artifacts and missing behavior.
  </read_first>
  <files>packages/concierge/src/concierge.ts, packages/concierge/src/dispatch.ts, packages/concierge/test/dispatcher.test.ts, packages/concierge/test/dispatcher-batch.test.ts</files>
  <behavior>
    - R68: callId as Symbol, non-string responseId/userTurnId, non-finite or non-number outputIndex, and a throwing metadata getter each return a Promise that resolves to one frozen reasonless failure with message "The invocation metadata is invalid."; no case throws synchronously or enters the handler.
    - Q17: a ToolCall carrying a Symbol callId resolves the whole batch with one frozen row whose callId is the same snapshotted value and whose result is the same frozen invalid-metadata failure; the handler count remains zero.
    - Existing Q16 keeps proving nested batch results remain frozen and reference-identical across cached retries; valid metadata, exact callId Promise identity, and batch metadata forwarding remain unchanged.
  </behavior>
  <action>
Add R68 and Q17 first with exact markers [RED:R68:malformed-metadata-totality] and [RED:Q17:malformed-callid-correlation], build without changing production, and prove the focused tests fail for the verifier-reported escape rather than a fixture or load error. Do not rename or repurpose the existing Q16 case or its [RED:Q16:immutable-nested-result] marker.

Then make snapshotInvocationMeta validate its copied primitive fields before it returns ok: responseId, userTurnId, and callId are each undefined or string; outputIndex is undefined or a finite number. Read each public field only inside the existing no-binding containment block. A failed read or type check returns the failure branch. In dispatch, replace the current dishonest aborted result for a metadata snapshot failure with authoredResult(false, "The invocation metadata is invalid.") and no ReasonCode; do not add a new public code or export. The result must remain sanitized and frozen through authoredResult, and dispatch must remain non-async so valid retry identity is unchanged.

Make deriveDispatchKey independently honor its never-throw contract: if a runtime callId is present but not a string, return null instead of interpolating it. This is defense in depth; the public dispatch path must still reject malformed metadata before the pipeline so returning null cannot let the handler run.

For Q17, preserve the snapshotted raw callId value in the correlation row instead of interpolating or coercing it. The TypeScript input contract remains string; this runtime behavior exists only to keep an untyped malformed caller connected to exactly one result. Do not weaken ToolCall or InvocationMeta types.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm exec vitest run packages/concierge/test/dispatcher.test.ts packages/concierge/test/dispatcher-batch.test.ts --testNamePattern="^\\[(R68|Q16|Q17)\\]"</automated>
    <automated>pnpm --filter @fullselfbrowsing/concierge typecheck</automated>
  </verify>
  <done>Malformed metadata cannot escape synchronously or disconnect a batch row, and no invalid metadata reaches application code.</done>
  <acceptance_criteria>
    - R68 and Q17 pass against dist with their exact registered markers and no rejected batch; existing Q16 still passes with [RED:Q16:immutable-nested-result].
    - Each malformed direct call returns a Promise and one exact frozen sanitized reasonless result.
    - The Q17 row, result, and result container are frozen; its correlation value is preserved by identity and the handler count is zero.
    - Valid callId retries still satisfy p1 === p2 and dispatch remains a non-async function.
    - No new ReasonCode, public export, telemetry seam, or cast is introduced.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (06-07-T2): Restore BigInt fallback-key degradation without reopening lossy collisions</name>
  <read_first>
    - packages/concierge/src/dispatch.ts — CanonicalValue, canonicalInvocationValue, encodeInvocationValue, and deriveDispatchKey.
    - packages/concierge/test/dispatcher.test.ts — R05, R06, and R06a.
    - .planning/phases/06-dispatcher/06-CONTEXT.md — locked Dedup decision.
    - .planning/phases/06-dispatcher/06-VERIFICATION.md — VER-06-02 and the rejected unaccepted override.
    - .planning/phases/06-dispatcher/06-REVIEW-FIX.md — collision fix that must remain for non-BigInt values.
  </read_first>
  <files>packages/concierge/src/dispatch.ts, packages/concierge/test/dispatcher.test.ts</files>
  <behavior>
    - R06: two calls with equal BigInt arguments do not throw, return distinct Promise objects, and invoke the handler twice.
    - R05: cyclic arguments retain the same no-throw, no-dedup behavior.
    - R06a: undefined properties, non-finite numbers, negative zero, and sparse-array distinctions remain injective and never share a Promise incorrectly.
  </behavior>
  <action>
Change R06 back to the locked expectation of calls 2, same false, and threw false, then prove the current tagged BigInt branch makes that case red.

Keep the tagged canonical encoder and every R06a distinction added by the review fix. Change only BigInt handling: canonicalInvocationValue must treat a BigInt as deliberately unkeyable so encodeInvocationValue catches the condition and returns null; deriveDispatchKey then runs the call through the normal no-dedup path. Update the keying documentation to name BigInt alongside cyclic and aliased graphs. Do not revert to raw JSON.stringify for supported values, do not give all unkeyable calls a shared sentinel key, and do not alter callId-keyed retries.

This implements the locked 06-CONTEXT.md § Dedup decision despite the later review's reasonable alternative; no accepted override exists.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm exec vitest run packages/concierge/test/dispatcher.test.ts --testNamePattern="^\\[R0(5|6)"</automated>
  </verify>
  <done>BigInt and cyclic arguments run normally without fallback deduplication, while the collision-safe encoder remains intact for every supported keyable value.</done>
  <acceptance_criteria>
    - R06 observes two handler calls, distinct Promises, and no synchronous throw.
    - R05 and R06a remain green.
    - Equal valid serializable arguments still deduplicate and callId remains authoritative.
    - No shared key is emitted for BigInt, cyclic, or aliased values.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (06-07-T3): Route malformed batch JSON through ordinary schema validation</name>
  <read_first>
    - packages/concierge/src/dispatch.ts — executeDispatchBatch parse catch and serial dispatch delegation.
    - packages/concierge/src/types.ts — ToolCall.arguments wire-format comment.
    - packages/concierge/test/dispatcher-batch.test.ts — Q04 and later-call continuation fixture.
    - .planning/ROADMAP.md — Phase 6 success criterion 3.
    - .planning/REQUIREMENTS.md — DSP-05 and DSP-06 wording.
    - .planning/phases/06-dispatcher/06-RESEARCH.md — Pattern 4 and Parse Errors Bypass Validation.
    - .planning/phases/06-dispatcher/06-VERIFICATION.md — VER-06-03 and the absent override.
  </read_first>
  <files>packages/concierge/src/dispatch.ts, packages/concierge/src/types.ts, packages/concierge/test/dispatcher-batch.test.ts</files>
  <behavior>
    - Q04: malformed raw JSON is observed by the validator as an empty object, the validator rejects it with issues, the handler does not run for that call, and the correlated row is invalid_args.
    - Q04: a later valid call still validates, enters the handler, and receives a successful correlated row.
    - Q05: valid JSON primitives continue to reach schema validation unchanged.
  </behavior>
  <action>
Rewrite Q04 before production so its validator records every input, returns issues for an object without the required value field, and accepts the later valid object. Its final observation must require validated to contain the empty object followed by the later valid object, handled to contain only the later call, and both frozen correlated rows to settle in order. Rename the case prose so it states the ROADMAP/DSP-06 contract rather than the superseded direct-rejection review behavior, while keeping case ID Q04 and marker [RED:Q04:malformed-json-validation].

In executeDispatchBatch, narrow the JSON.parse catch to assign args = {} and then continue through the existing metadata construction and await dispatch path. Remove the catch's authored invalid_args row and continue statement. Do not duplicate validation in batch and do not allow the malformed call to skip serial dispatch; the action's Standard Schema validator remains the sole ordinary argument-validation boundary.

Update ToolCall.arguments documentation in types.ts to say malformed JSON becomes an empty object and is then validated. Preserve the immutable batch envelope, stable ordering, metadata snapshot, abort handling, and one-row-per-input behavior.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm exec vitest run packages/concierge/test/dispatcher-batch.test.ts --testNamePattern="^\\[Q0(4|5)\\]"</automated>
    <automated>pnpm build &amp;&amp; pnpm exec vitest run packages/concierge/test/dispatcher.test.ts packages/concierge/test/dispatcher-batch.test.ts &amp;&amp; pnpm --filter @fullselfbrowsing/concierge typecheck</automated>
  </verify>
  <done>Malformed batch JSON reaches the same Standard Schema validation path as direct calls, fails honestly under the rejecting schema, and does not stop later calls.</done>
  <acceptance_criteria>
    - Q04 proves the validator received an empty object; a static authored result cannot satisfy it.
    - The malformed call does not enter the handler because the fixture schema rejects the empty object.
    - The later valid call settles successfully and batch ordering/correlation remain intact.
    - ToolCall documentation, ROADMAP criterion 3, DSP-06, source, and Q04 now describe one behavior.
    - Both complete dispatcher suites and package typecheck pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JavaScript caller → dispatch | Runtime values can violate InvocationMeta's TypeScript primitive contracts. |
| ToolBatch → parser/dispatcher | Raw JSON and malformed call metadata must not reject the serial result loop. |
| fallback key → retry cache | Unsafe serialization can either throw or collapse distinct calls into one effect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-G01 | Denial of service / Repudiation | snapshotInvocationMeta and deriveDispatchKey | mitigate | Validate copied primitive fields, keep key derivation total, and prove direct plus batch result completeness with R68/Q17 while preserving Q16's immutable cached-result proof. |
| T-06-G02 | Tampering | BigInt fallback key | mitigate | Return no key for BigInt while retaining the injective encoder for supported values; callers needing replay identity supply callId. |
| T-06-G03 | Tampering | malformed batch JSON | mitigate | Substitute an empty object and require ordinary Standard Schema validation before handler entry; Q04 uses a rejecting schema. |
| T-06-G04 | Information disclosure / Spoofing | authored metadata failure | mitigate | Emit one fixed sanitized reasonless result and never echo, stringify, or interpolate the malformed value. |
| T-06-SC | Tampering | package supply chain | accept | This plan installs no package and changes no manifest or lockfile. |
</threat_model>

<gap_coverage>
| Verification gap | Task | Closure evidence |
|------------------|------|------------------|
| VER-06-01 malformed Symbol callId escapes and rejects batch | 06-07-T1 | R68 and Q17 against dist; existing Q16 remains unchanged |
| VER-06-02 BigInt incorrectly deduplicates | 06-07-T2 | corrected R06 plus preserved R05/R06a |
| VER-06-03 malformed JSON bypasses validator | 06-07-T3 | corrected Q04 observes empty-object validation |
</gap_coverage>

<verification>
Run the built-artifact single and batch suites and package typecheck. No implementation file outside the five declared paths changes, no public API widens, and no normal dispatcher behavior regresses.
</verification>

<success_criteria>
- Every direct call returns a Promise and malformed metadata resolves to one frozen honest result.
- Every batch input, including a Symbol callId, retains one frozen correlated row.
- BigInt disables only fallback deduplication; it does not throw and does not weaken the collision-safe encoder for other values.
- Malformed JSON reaches Standard Schema as an empty object and later batch calls still settle.
</success_criteria>

<output>
Create .planning/phases/06-dispatcher/06-07-SUMMARY.md when done.
</output>
