---
phase: 06-dispatcher
plan: 05
type: execute
wave: 4
depends_on: ["06-02", "06-04"]
files_modified:
  - packages/concierge/src/dispatch.ts
  - packages/concierge/src/concierge.ts
  - packages/concierge/src/types.ts
  - packages/concierge/src/index.ts
autonomous: true
requirements: [DSP-06, DSP-07, DSP-08, SEC-06, TRN-04]
user_setup: []

must_haves:
  truths:
    - "ToolBatch parsing and serial execution are Concierge capabilities independent of Transport."
    - "Malformed JSON cannot crash or bypass schema validation, and later calls still settle."
    - "Every batch call produces one immutable correlated result row, including calls remaining after abort."
    - "Batch metadata forwards to InvocationMeta without casts and repeated callIds reuse single-call dedup."
  artifacts:
    - path: "packages/concierge/src/types.ts"
      provides: "Exact public dispatchBatch signature with inline immutable rows"
      contains: "dispatchBatch"
    - path: "packages/concierge/src/dispatch.ts"
      provides: "Raw-arguments parser and serial ToolBatch executor"
      contains: "dispatchBatch"
    - path: "packages/concierge/src/concierge.ts"
      provides: "Batch method wired to the exact same single-call dispatch function"
      contains: "return { dispatch, dispatchBatch"
  key_links:
    - from: "packages/concierge/src/dispatch.ts"
      to: "Concierge.dispatch"
      via: "await one call at a time in stable outputIndex order"
      pattern: "await dispatch"
    - from: "packages/concierge/src/concierge.ts"
      to: "packages/concierge/src/types.ts"
      via: "returned dispatchBatch member"
      pattern: "dispatchBatch"
---

<objective>
Add the transport-independent ToolBatch parser and serial executor on top of the completed single-call boundary.

Purpose: Complete DSP-06/DSP-07/TRN-04 while preserving all single-call dedup, validation, timing, and sanitization guarantees.

Output: final public batch signature, wired runtime method, truthful entrypoint prose, and green runtime/type suites.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-dispatcher/06-CONTEXT.md
@.planning/phases/06-dispatcher/06-RESEARCH.md
@.planning/phases/06-dispatcher/06-PATTERNS.md
@.planning/phases/06-dispatcher/06-02-SUMMARY.md
@.planning/phases/06-dispatcher/06-04-SUMMARY.md

<interfaces>
- The signature below is the ratified contract from `06-CONTEXT.md` § Public dispatch seams and `06-RESEARCH.md` § Open Questions (RESOLVED 2026-08-05).
- Add `dispatchBatch: (ctx: StageContext, batch: ToolBatch) => Promise<ReadonlyArray<Readonly<{ callId: string; result: ActionResult }>>>` to Concierge.
- Do not create or export a BatchResult name.
- Batch calls the exact cached single-call `dispatch`; it does not duplicate stage, validation, commit, bridge, handler, normalizer, or sanitizer logic.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (06-05-T1): Implement stable serial ToolBatch execution and immutable correlation rows</name>
  <read_first>
    - `packages/concierge/src/dispatch.ts` — modified helper module from 06-04.
    - `packages/concierge/src/concierge.ts` — completed single-call dispatch and returned object.
    - `packages/concierge/src/types.ts` — ToolCall, ToolBatch, InvocationMeta, ActionResult, Concierge.
    - `packages/concierge/test/dispatcher-batch.test.ts` — Wave 0 executable contract.
    - `packages/concierge/test-d/dispatcher.test-d.ts` — exact batch signature and key-set pins.
    - `packages/concierge/test-d/transport.test-d.ts` — optional metadata construction contract.
    - `.planning/phases/06-dispatcher/06-CONTEXT.md` — serial, abort-complete, and `aborted`-only decisions.
  </read_first>
  <files>packages/concierge/src/dispatch.ts, packages/concierge/src/concierge.ts, packages/concierge/src/types.ts</files>
  <action>
Add the exact `dispatchBatch` member to `Concierge` with the inline readonly result row and no exported alias. Implement it as an async inner function owned by the Concierge instance; only the outer single-call `dispatch` is forbidden from being async. Decorate a copied `batch.calls` array with original positions, sort by `outputIndex` then original position, and never mutate the caller's array. For each call in a `for...of` loop, parse only `call.arguments` with JSON.parse; on parse throw substitute `{}` and continue through ordinary dispatch validation. Construct InvocationMeta directly with `responseId`, `callId`, `outputIndex`, `userTurnId`, `signal`, and `deferUntilDelivered`, including explicit undefined values and no cast. Await the existing `dispatch(ctx, call.name, args, meta)` before entering the next iteration.

When the batch signal is already aborted before a remaining call starts, append an authored, sanitized `aborted` result row without invoking dispatch or breaking the loop. An abort during the current call is handled by single dispatch; after it settles, every remaining call gets the same aborted spelling. Never add `batch_aborted`. Freeze each fresh `{callId, result}` row and freeze the final array. Repeated callIds must flow through the existing single-call cache. Wire the method into `createConcierge`'s returned object.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm exec vitest run packages/concierge/test/dispatcher-batch.test.ts packages/concierge/test/dispatcher.test.ts</automated>
    <automated>pnpm --filter @fullselfbrowsing/concierge typecheck</automated>
  </verify>
  <done>Batch and single-call runtime suites pass, final type pins pass, and every ToolBatch call has one immutable result row.</done>
  <acceptance_criteria>
    - Ordering is stable by outputIndex and original position; maximum handler concurrency remains one.
    - Malformed JSON becomes `{}` and reaches dispatch validation; later calls run.
    - Metadata forwards without a cast and retains exact references.
    - Abort produces complete rows without `break`, Promise.all, or a new reason code.
    - Repeated callIds execute once through single dispatch.
    - Typecheck has no remaining Phase 6 diagnostics.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 (06-05-T2): Document the final direct-loop and batch boundary without widening exports</name>
  <read_first>
    - `packages/concierge/src/index.ts` — modified package header.
    - `packages/concierge/src/types.ts` — modified Concierge and ToolBatch documentation.
    - `packages/concierge/src/concierge.ts` — returned method set.
    - `packages/concierge/test/export-surface.test.ts` — 65/51/14 public surface pins.
    - `packages/concierge/test/artifact.test.ts` — runtime value export pins.
    - `.planning/phases/06-dispatcher/06-CONTEXT.md` — TRN-04 and Phase 7 boundary.
  </read_first>
  <files>packages/concierge/src/index.ts, packages/concierge/src/types.ts</files>
  <action>
Update entrypoint and Concierge documentation to name both direct methods and the immutable callId/result batch envelope. State that an application agent loop supplies StageContext and ToolBatch directly and needs no Transport. Preserve the truthful boundary: Session ownership, transport subscription/respond routing, telemetry, and consent gating are not implemented here. State that dispatchBatch preserves stable outputIndex order and returns every aborted result. Do not export a batch-row type or any helper and do not change the export counts.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm typecheck &amp;&amp; pnpm test</automated>
    <automated>pnpm exec vitest run packages/concierge/test/export-surface.test.ts packages/concierge/test/artifact.test.ts</automated>
  </verify>
  <done>The shipped contract describes direct single/batch use accurately and the public surface remains 65 names, 51 types, and 14 values.</done>
  <acceptance_criteria>
    - Prose names dispatch(ctx,...) and dispatchBatch(ctx,batch) and does not claim a Session or Transport loop exists.
    - No new barrel export or named row interface exists.
    - Export-surface and artifact suites retain their existing counts.
    - Full build, typecheck, and test suite pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ToolBatch → parser | Raw argument strings and attacker-chosen ordering indices enter core. |
| batch → single dispatch | Metadata and abort state must cross without bypassing existing controls. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-06-03 | HIGH | Tampering | JSON parse/input | mitigate | Narrow parse catch, `{}` fallback, same schema validation route. |
| T-06-08 | HIGH | Tampering / Denial of service | batch ordering | mitigate | Stable decorated sort, serial await, one immutable row per input. |
| T-06-07 | HIGH | Tampering / Repudiation | batch abort | mitigate | Existing signal propagation plus synthesized remaining aborted rows; never break. |
| T-06-01 | HIGH | Tampering | repeated batch callId | mitigate | Delegate every live call to exact single-call cache. |
| T-06-05 | HIGH | Spoofing | synthesized abort message | mitigate | Reuse authored final sanitizer boundary. |
| T-06-SC | LOW | Tampering | package supply chain | accept | No install or package change. |
</threat_model>

<verification>
Both dispatcher suites, package typecheck, full test suite, and export-surface pins are green.
</verification>

<success_criteria>
- DSP-06/DSP-07/TRN-04 are reachable through Concierge without a Transport.
- Batch parsing, seriality, correlation, abort completeness, and metadata forwarding match the Wave 0 contract.
- The final API adds one method and zero exported names.
</success_criteria>

<output>
Create `.planning/phases/06-dispatcher/06-05-SUMMARY.md` when done.
</output>
