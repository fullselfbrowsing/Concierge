---
phase: 06-dispatcher
plan: 06
type: execute
wave: 5
depends_on: ["06-05"]
files_modified:
  - scripts/phase-06-mutation-battery.mjs
  - scripts/check-no-telemetry.mjs
  - .planning/phases/06-dispatcher/06-MUTATION-REGISTER.json
  - .planning/phases/06-dispatcher/06-MUTATION-EVIDENCE.json
  - .planning/phases/06-dispatcher/06-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DSP-01, DSP-02, DSP-03, DSP-04, DSP-05, DSP-06, DSP-07, DSP-08, DSP-09, SEC-02, SEC-06, TRN-04]
user_setup: []

must_haves:
  truths:
    - "Every Phase 6 detector is proven to fail under the mutation it names, after the mutant compiles and tests actually run."
    - "The full build, type, runtime, artifact, dependency, pack, and Node-floor gates pass on restored source."
    - "Requirements record only measured evidence, including SEC-02's structural no-telemetry satisfaction and BRG-03's real-dispatch join."
  artifacts:
    - path: "scripts/phase-06-mutation-battery.mjs"
      provides: "Fail-closed runner and verifier for the complete expected M-06 register"
      contains: "EXPECTED_M06_IDS"
    - path: "scripts/check-no-telemetry.mjs"
      provides: "AST-aware SEC-02 audit over production TypeScript"
      contains: "createSourceFile"
    - path: ".planning/phases/06-dispatcher/06-MUTATION-REGISTER.json"
      provides: "Exact target, unique literal, replacement, intended suite, and intended detector for every M-06 mutant"
      contains: "M-06-S01"
    - path: ".planning/phases/06-dispatcher/06-MUTATION-EVIDENCE.json"
      provides: "Machine-readable compiled/tests-ran/killed/restored evidence for the exact register"
      contains: "registerDigest"
    - path: ".planning/phases/06-dispatcher/06-VALIDATION.md"
      provides: "Completed task map, mutation register, gate evidence, and Nyquist sign-off"
      contains: "nyquist_compliant: true"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Evidence-backed completion status for all twelve Phase 6 requirement IDs"
      contains: "DSP-09"
  key_links:
    - from: ".planning/phases/06-dispatcher/06-VALIDATION.md"
      to: "scripts/phase-06-mutation-battery.mjs"
      via: "complete-register verification over machine-readable mutation evidence"
      pattern: "M-06-"
    - from: ".planning/REQUIREMENTS.md"
      to: "packages/concierge/test/dispatcher.test.ts"
      via: "named case evidence"
      pattern: "dispatcher.test.ts"
---

<objective>
Prove the final dispatcher against source mutations, run every phase gate, and record only measured requirement completion.

Purpose: A green suite without a detector battery is insufficient for the phase's silent failure modes: async Promise wrapping, wrong-stage lookup, bypassed validation, post-effect delay, abort races, unsafe result spreading, and parallel batches can all return plausible results.

Output: signed `06-VALIDATION.md` and evidence-backed Phase 6 rows in `REQUIREMENTS.md`.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-dispatcher/06-CONTEXT.md
@.planning/phases/06-dispatcher/06-RESEARCH.md
@.planning/phases/06-dispatcher/06-VALIDATION.md
@.planning/phases/06-dispatcher/06-01-SUMMARY.md
@.planning/phases/06-dispatcher/06-02-SUMMARY.md
@.planning/phases/06-dispatcher/06-03-SUMMARY.md
@.planning/phases/06-dispatcher/06-04-SUMMARY.md
@.planning/phases/06-dispatcher/06-05-SUMMARY.md

<interfaces>
- Mutation harness usage: `scripts/mutate-and-prove.sh target literal replacement -- gate...`; exit 0 means caught and restored, but the caller must separately prove build success and non-zero test execution.
- Runtime gates must build first because tests import `dist/index.js`; rebuild after every source probe because the harness restores src but not dist.
- `node scripts/phase-06-mutation-battery.mjs run single|batch` executes the selected group and writes `06-MUTATION-EVIDENCE.json`. The runner and register contain the complete immutable 54-ID set before the first subgroup run. `verify single` requires exactly the 34 S rows green while permitting the 20 registered B rows to remain explicitly pending/unexecuted; only `verify all` requires all 54 evidence rows green. Every green row proves a unique applied mutant, build success, more than zero intended tests, the intended failing case or type diagnostic, harness kill, restored green gates, and clean scoped source.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (06-06-T1): Run and record the single-call and security mutation battery</name>
  <read_first>
    - `.planning/phases/06-dispatcher/06-VALIDATION.md` — modified task map, mutant register, Wave 0 checklist, and sign-off.
    - `packages/concierge/src/concierge.ts` — final non-async dispatch, stage gate, cache, and bridge call.
    - `packages/concierge/src/dispatch.ts` — final validation, wait, normalizer, and sanitizer helpers.
    - `packages/concierge/src/message.ts` — independent sanitizer branches.
    - `packages/concierge/src/host.ts` — host timer fallback.
    - `packages/concierge/test/dispatcher.test.ts` — named intended detectors.
    - `packages/concierge/test-d/dispatcher.test-d.ts` — API and Scheduler detectors.
    - `scripts/mutate-and-prove.sh` — exact harness contract and known limitations 1-3.
  </read_first>
  <files>scripts/phase-06-mutation-battery.mjs, .planning/phases/06-dispatcher/06-MUTATION-REGISTER.json, .planning/phases/06-dispatcher/06-MUTATION-EVIDENCE.json, .planning/phases/06-dispatcher/06-VALIDATION.md</files>
  <action>
Create the dedicated battery runner and the complete 54-row JSON register before running any subgroup, then fill the validation task cells. From the outset, hard-code immutable `EXPECTED_SINGLE_IDS` (the 34 S IDs below), `EXPECTED_BATCH_IDS` (the 20 B IDs specified in Task 2), and `EXPECTED_M06_IDS` as their exact 54-ID concatenation; the register must match `EXPECTED_M06_IDS` with no missing, duplicate, or extra IDs even though the B rows are not executed in this task. The single set is: M-06-S01 outer async; S02 cache-hit wrapper; S03 delayed cache insertion; S04 failure-cache removal; S05 callId-key removal; S06 key-namespace collision; S07 serialization catch removal; S08 module-scope cache; S09 pending expiry; S10 creation-time instead of settlement-time timestamp; S11 all-key sweep removal; S12 commit default changed; S13 dedupe default changed; S14 namesByStage bypass; S15 prototype-bearing lookup; S16 validation bypass; S17 original rather than transformed args; S18 wait moved after handler; S19 omitted effects treated read-only; S20 initial abort check removed; S21 listener re-check removed; S22 canceller removed; S23 listener cleanup removed; S24 resolveBridge bypass; S25 exception echo; S26 untrusted result spread/pass-through; S27 malformed-result guard removal; S28 contradictory success reversed; S29 contradictory failure reversed; S30 control replacement removed; S31 whitespace collapse removed; S32 shared bound removed; S33 surrogate adjustment removed; S34 Scheduler canceller type widened.

Each register row must contain id, group, tracked target, an exact literal pattern whose unfiltered occurrence count is one, replacement, detector kind, intended test file, and non-empty intended case IDs or exact type diagnostic locations. Initialize the 20 B evidence rows as explicitly pending/unexecuted, never absent or implicitly passing. The runner must call `mutate-and-prove.sh`, capture the gate rather than pipe it, require a successful `pnpm build` marker before the detector, obtain a Vitest JSON report proving `numTotalTests &gt; 0` for every executed mutant (including S34 before its typecheck detector), and require the registered intended failing case IDs or type diagnostic. It must reject compile/parse-only failures, zero-test runs, wrong-test failures, harness ABORT/escape, missing selected-group evidence, and a register/evidence digest mismatch. `verify single` must require the exact 54-row register, exactly all 34 S evidence rows green, and exactly all 20 B rows still pending or already green; it must reject any B escape/failure state but does not require B execution. Only `verify all` may sign the complete register, and it requires all 54 rows green. After every probe rebuild restored source, run the relevant green suite and package typecheck, compare scoped status before/after, and write `targetRestored`, `restoredGreen`, and `scopedTreeClean` only from successful predicates. Run the single group; any escape is a blocking HIGH finding and must leave validation unsigned.
  </action>
  <verify>
    <automated>node scripts/phase-06-mutation-battery.mjs run single &amp;&amp; node scripts/phase-06-mutation-battery.mjs verify single</automated>
    <automated>bash -c 'test -z "$(git status --porcelain --untracked-files=all -- packages/concierge/src packages/concierge/test packages/concierge/test-d pnpm-lock.yaml)"'</automated>
  </verify>
  <done>Every single-call/security mutant is caught by its intended assertion after compiling and running tests, restored source is green, and the register records exact evidence.</done>
  <acceptance_criteria>
    - Every listed mutation has a unique unfiltered literal or an explicitly anchored multiline literal.
    - Each PASS row records build success, non-zero tests, named red cases, and clean restoration.
    - Async-wrapper, pending-expiry, wrong-stage, prototype-key, input, exception, injection, and abort-race threats each have a distinct detector.
    - No HIGH escape remains and git status contains no mutated source.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 (06-06-T2): Run and record the batch mutation battery</name>
  <read_first>
    - `.planning/phases/06-dispatcher/06-VALIDATION.md` — modified register from Task 1.
    - `scripts/phase-06-mutation-battery.mjs` — complete 54-ID runner created by Task 1; Task 2 owns any batch-runner corrections but must not add or remove expected IDs.
    - `packages/concierge/src/dispatch.ts` — final batch parser/executor.
    - `packages/concierge/src/concierge.ts` — batch wiring to single dispatch.
    - `packages/concierge/test/dispatcher-batch.test.ts` — intended Q-series detectors.
    - `scripts/mutate-and-prove.sh` — restore and PASS semantics.
  </read_first>
  <files>scripts/phase-06-mutation-battery.mjs, .planning/phases/06-dispatcher/06-MUTATION-REGISTER.json, .planning/phases/06-dispatcher/06-MUTATION-EVIDENCE.json, .planning/phases/06-dispatcher/06-VALIDATION.md</files>
  <action>
Use the 20 batch rows and IDs already present in the complete Task 1 register and `EXPECTED_M06_IDS`; do not add, remove, or reorder expected IDs in this task: M-06-B01 input-order iteration; B02 tie-break removal; B03 caller-array mutation; B04 Promise.all concurrency; B05 uncaught JSON.parse; B06 parse fallback bypassing validation; B07 responseId omission; B08 callId metadata omission; B09 outputIndex omission; B10 userTurnId omission; B11 signal omission; B12 deferUntilDelivered omission; B13 break on abort; B14 dispatch already-aborted remaining call; B15 result-row omission; B16 positional result without callId; B17 mutable row; B18 mutable array; B19 batch_aborted introduction; B20 single-dispatch bypass/dedup loss. Run the batch group so the runner replaces each B row's explicit pending state with measured evidence while preserving the 34 green S rows and the unchanged 54-ID register digest. Update DSP-06/DSP-07/TRN-04 only after `verify all` proves exactly all 54 evidence rows green with zero pending rows or escapes. Do not hand-edit evidence statuses; the runner derives them from command results.
  </action>
  <verify>
    <automated>node scripts/phase-06-mutation-battery.mjs run batch &amp;&amp; node scripts/phase-06-mutation-battery.mjs verify all</automated>
    <automated>bash -c 'test -z "$(git status --porcelain --untracked-files=all -- packages/concierge/src packages/concierge/test packages/concierge/test-d pnpm-lock.yaml)"'</automated>
  </verify>
  <done>Every batch parser/order/serial/abort/correlation mutation is caught, source is restored, and all batch validation rows are green.</done>
  <acceptance_criteria>
    - Sort, seriality, parse fallback, metadata, abort completeness, correlation, immutability, and dedup reuse each have an independent red detector.
    - No mutation is credited for a build failure or zero-test run.
    - `batch_aborted` remains absent from production source.
    - Both dispatcher suites pass on restored source.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3 (06-06-T3): Run the phase gate and record requirement evidence</name>
  <read_first>
    - `.planning/phases/06-dispatcher/06-VALIDATION.md` — modified file to sign only after measured gates.
    - `.planning/REQUIREMENTS.md` — modified checkboxes and traceability rows.
    - `scripts/check-no-telemetry.mjs` — NEW AST-aware SEC-02 audit target.
    - `.planning/phases/06-dispatcher/06-CONTEXT.md` — SEC-02 structural wording and deferred boundaries.
    - `.planning/phases/06-dispatcher/06-RESEARCH.md` — Sampling Rate, Phase Gate, and Security Domain.
    - `packages/concierge/test/dispatcher.test.ts` — evidence IDs.
    - `packages/concierge/test/dispatcher-batch.test.ts` — evidence IDs.
  </read_first>
  <files>scripts/check-no-telemetry.mjs, .planning/phases/06-dispatcher/06-MUTATION-EVIDENCE.json, .planning/phases/06-dispatcher/06-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <action>
Create `scripts/check-no-telemetry.mjs` using the already-installed TypeScript compiler API, then run the exact phase gate `pnpm build && pnpm typecheck && pnpm test && pnpm check:artifact && pnpm check:deps && pnpm check:pack && pnpm check:node-floor` and record each exit and headline count. The audit must recursively enumerate a non-zero set of `packages/concierge/src/**/*.ts`, require the Phase 6 dispatcher production files to be present, parse every file with `ts.createSourceFile`, fail on any parse diagnostic, and inspect AST nodes rather than raw comment text. Across all production source it rejects telemetry/onTelemetry/onError declarations, callback/property names (including string/computed names), imports/exports, call targets, and telemetry emissions such as `emit("telemetry", ...)`; comments and JSDoc containing those words are ignored by construction. In the dispatcher result path (`dispatch.ts`, `concierge.ts`, `message.ts`, and `host.ts`) it also fails on a catch binding or any caught-value `.message`/`.stack`, stringification, interpolation, return, result-field assignment, console/warn/callback argument, or other forwarding path, enforcing the locked no-binding generic-error boundary. The command exits non-zero on unreadable files, an empty scan, an unavailable parser, or any ambiguous/unhandled node shape; it prints the scanned-file count and zero findings on success. Assert no Vitest mocking API and no dependency/lockfile delta as before. Mark `wave_0_complete:true`, `nyquist_compliant:true`, status complete, and every sign-off box only if all mutation rows, the AST audit, and all gates are green. In REQUIREMENTS.md mark DSP-01..DSP-09, SEC-06, and TRN-04 complete with named dispatcher test evidence. Mark SEC-02 satisfied structurally only after the AST audit proves Phase 6 emits no telemetry at all and runtime evidence proves handler exception text reaches neither result nor console. Update BRG-03's traceability status with the real-dispatch resolveBridge-to-null-to-handler join. If runtime lookup tests complete SEC-03's previously pending dispatch-side proof, add that evidence without remapping SEC-03 into this phase's requirements. Do not mark deferred telemetry, Session, consent, or adapter work complete.
  </action>
  <verify>
    <automated>node scripts/phase-06-mutation-battery.mjs verify all</automated>
    <automated>node scripts/check-no-telemetry.mjs</automated>
    <automated>pnpm build &amp;&amp; pnpm typecheck &amp;&amp; pnpm test &amp;&amp; pnpm check:artifact &amp;&amp; pnpm check:deps &amp;&amp; pnpm check:pack &amp;&amp; pnpm check:node-floor</automated>
    <automated>bash -c 'test -z "$(git status --porcelain --untracked-files=all -- packages/concierge/src packages/concierge/test packages/concierge/test-d pnpm-lock.yaml)"'</automated>
  </verify>
  <done>All gates and mutants are green, validation is signed, and all twelve Phase 6 requirements carry truthful measured status.</done>
  <acceptance_criteria>
    - Full phase gate exits 0 and headline test/artifact counts are recorded.
    - Validation contains no TBD, pending, W0-missing, unchecked sign-off, unaddressed HIGH escape, missing expected mutant, zero-test row, compile-only kill, or incomplete evidence row.
    - All twelve requirement IDs are checked and have evidence in traceability.
    - SEC-02 explicitly says no telemetry channel exists and none was added.
    - BRG-03 records the end-to-end join; deferred Phase 7/8/9 items remain deferred.
    - Source/test/type files and lockfile are clean after mutation work.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mutation harness → evidence | A wrong/no-op/compile-breaking mutation can produce a false PASS. |
| implementation → planning status | Green tests can be overstated as requirement completion without discriminating evidence. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-06-01 | HIGH | Tampering | replay/double actuation | mitigate | Dedicated identity/cache mutants must compile and redden named R cases. |
| T-06-02 | HIGH | Elevation of privilege | wrong-stage invocation | mitigate | namesByStage bypass mutant plus prototype-key lookup mutants. |
| T-06-03 | HIGH | Tampering | input validation | mitigate | Validation-skip/original-value/parse-bypass mutants. |
| T-06-04 | HIGH | Information disclosure | exception/PII | mitigate | Catch-binding/echo mutant and no-channel source audit. |
| T-06-05 | HIGH | Spoofing / Information disclosure | control/model channel | mitigate | Four independent sanitizer mutants. |
| T-06-06 | HIGH | Tampering | prototype lookup | mitigate | byName/prototype mutations with handler-zero assertions. |
| T-06-07 | HIGH | Tampering / Repudiation | abort races | mitigate | Initial check, listener, re-check, canceller, cleanup, and batch-abort mutants. |
| T-06-10 | HIGH | Repudiation | false mutation evidence | mitigate | Unfiltered unique count, compiled-build proof, non-zero test proof, named case, restore proof. |
| T-06-SC | LOW | Tampering | package supply chain | mitigate | Package Legitimacy Audit says no installs; lockfile and dependency graph must remain unchanged. |
</threat_model>

<source_coverage_audit>
GOAL is covered by 06-04/05/06. DSP-01..05, DSP-08/09, SEC-02/06 and TRN-04 are covered by 06-01/03/04/06. DSP-06/07 are covered by 06-02/05/06. Research API seams are pinned in 06-01 and implemented in 06-04/05. Timestamp/default reconciliation is implemented in 06-04 as 600/600 plus pending retention and settled access eviction. All non-deferred CONTEXT decisions are assigned across 06-01..06; telemetry, consent, normalizeSnapshot, batch_aborted split, ServerSafeConcierge, and return-object freezing remain excluded exactly as deferred.
</source_coverage_audit>

<verification>
All registered mutants are killed by their intended detector; all repository gates pass on restored source; planning artifacts record measured evidence only.
</verification>

<success_criteria>
- No HIGH-severity threat or mutation escape remains.
- `06-VALIDATION.md` is complete, Nyquist-compliant, and evidence-backed.
- Every Phase 6 requirement ID is complete in REQUIREMENTS.md with exact test evidence.
- The repository is clean and no dependency, telemetry channel, or deferred subsystem was added.
</success_criteria>

<output>
Create `.planning/phases/06-dispatcher/06-06-SUMMARY.md` when done.
</output>
