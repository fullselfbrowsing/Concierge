---
phase: 06-dispatcher
plan: 04
type: execute
wave: 3
depends_on: ["06-01", "06-03"]
files_modified:
  - packages/concierge/src/dispatch.ts
  - packages/concierge/src/concierge.ts
  - packages/concierge/src/types.ts
  - packages/concierge/src/index.ts
  - packages/concierge/test/concierge.test.ts
autonomous: true
requirements: [DSP-01, DSP-02, DSP-03, DSP-04, DSP-05, DSP-08, DSP-09, SEC-02, SEC-06, TRN-04]
user_setup: []

must_haves:
  truths:
    - "A caller supplies StageContext to dispatch and an off-stage or prototype-key name never reaches a handler."
    - "Retries share the exact final Promise while pending and inside the settled access window, including failure results."
    - "Validation and the cancellable commit window complete before any non-read-only handler enters."
    - "Handler crashes and malformed results become honest sanitized ActionResults without leaking caught details."
    - "A mounted bridge is resolved through the one existing resolver; null is passed honestly when absent."
  artifacts:
    - path: "packages/concierge/src/dispatch.ts"
      provides: "Internal key, validation, delay, normalization, and sanitization helpers"
      min_lines: 180
    - path: "packages/concierge/src/concierge.ts"
      provides: "Non-async context-aware dispatch and instance-local dedup state"
      contains: "function dispatch("
    - path: "packages/concierge/src/types.ts"
      provides: "Final single-call public signature and 600/600 default documentation"
      contains: "dispatch: (ctx: StageContext"
  key_links:
    - from: "packages/concierge/src/concierge.ts"
      to: "namesByStage and catalog.byName"
      via: "stage authorization before lookup"
      pattern: "namesByStage"
    - from: "packages/concierge/src/concierge.ts"
      to: "resolveBridge"
      via: "second and final caller after commit wait"
      pattern: "resolveBridge\\(stage\\)"
    - from: "packages/concierge/src/dispatch.ts"
      to: "packages/concierge/src/message.ts"
      via: "final result sanitizer"
      pattern: "sanitizeMessage"
---

<objective>
Replace the Phase 4 stub with the real context-aware single-call dispatcher.

Purpose: Deliver DSP-01..05, DSP-08, DSP-09, SEC-02, SEC-06, TRN-04, and the Phase 5 BRG-03 hand-forward without hidden current-context state or duplicate bridge lookup.

Output: a green single-call suite, final single-call type signature, truthful package prose, and deletion of the obsolete stub test.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dispatcher/06-CONTEXT.md
@.planning/phases/06-dispatcher/06-RESEARCH.md
@.planning/phases/06-dispatcher/06-PATTERNS.md
@.planning/phases/03-action-declaration-and-build-time-validation/03-08-SUMMARY.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-07-SUMMARY.md
@.planning/phases/06-dispatcher/06-01-SUMMARY.md
@.planning/phases/06-dispatcher/06-03-SUMMARY.md

<interfaces>
- These contracts and cache rules are ratified in `06-CONTEXT.md` §§ Public dispatch seams and Dedup and recorded in `06-RESEARCH.md` § Open Questions (RESOLVED 2026-08-05); this plan implements them without selecting alternatives.
- Set `Concierge.dispatch` to `(ctx: StageContext, name: string, args: unknown, meta?: InvocationMeta) => Promise<ActionResult>`.
- Keep the outer function non-async.
- Set both documented defaults to 600 ms.
- Preserve `Scheduler` exactly; use `config.scheduler ?? readHostScheduler()` only for commit delay.
- Use one handler context `{args, bridge, meta, ack: undefined}`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (06-04-T1): Implement stage-scoped orchestration, Promise-identity dedup, validation, commit delay, and bridge hand-forward</name>
  <read_first>
    - `packages/concierge/src/dispatch.ts` — NEW internal helper module.
    - `packages/concierge/src/concierge.ts` — modified file; catalog, namesByStage, resolveIndex, resolveBridge, stub, and factory closure.
    - `packages/concierge/src/types.ts` — modified public dispatch signature, config defaults, ActionHandler context, ReasonCode.
    - `packages/concierge/src/catalog.ts` — CatalogEntry and frozen null-prototype byName contract.
    - `packages/concierge/src/message.ts` — boundedMessage/sanitizeMessage from 06-03.
    - `packages/concierge/src/host.ts` — warnHost/readHostScheduler from 06-03.
    - `packages/concierge/test/dispatcher.test.ts` — complete Wave 0 behavior contract.
    - `packages/concierge/test-d/dispatcher.test-d.ts` — exact final signature and Scheduler pins.
    - `.planning/phases/06-dispatcher/06-CONTEXT.md` — every locked single-call decision.
  </read_first>
  <files>packages/concierge/src/dispatch.ts, packages/concierge/src/concierge.ts, packages/concierge/src/types.ts</files>
  <action>
Create internal `dispatch.ts` for stateless helpers: namespaced key derivation (`id:${callId}` else `args:${name}:${JSON.stringify(args)}`) returning null on serialization throw; guarded Standard Schema validation that awaits sync/async output and passes its `value`; cancellable pre-handler wait; runtime ReasonCode checking over all twelve literals; fresh-result normalization reading only `ok`, `reason`, and `message` under a getter/proxy guard; and final `sanitizeMessage` application to every authored or handler result. Exact authored results are: unknown stage/name `{ok:false,reason:"unknown_action",message:"This action is not available in the current stage."}`; invalid args `{ok:false,reason:"invalid_args",message:"The action arguments are invalid."}`; abort `{ok:false,reason:"aborted",message:"The action was cancelled before it ran."}`; handler crash `{ok:false,reason:"handler_error",message:"Something went wrong."}`; invalid return `{ok:false,reason:"invalid_result",message:"The action returned an invalid result."}`. A catalog entry with absent/non-callable handler returns `{ok:false,message:"This action is unavailable because no handler is registered."}` and warns once; do not invent a ReasonCode.

In `createConcierge`, add lazy instance-local `let` state exactly as the three coordinated stores ratified in `06-CONTEXT.md` § Dedup: `Map<string, Promise<ActionResult>>`, `Map<string, number>` settled-window timestamps from `Date.now()`, and `Set<string>` pending keys. A pending entry has no expiring timestamp and is never replaced regardless of elapsed wall time. Attach settlement observation without replacing the final cached Promise; when that Promise settles, remove the pending marker and write that instant as the start of its settled window. On every keyed access, sweep every settled key whose age since settlement is greater than `dedupeWindowMs`. Store the final pipeline Promise synchronously before returning it and cache success and failure results alike. Per the resolved research decision, `commitWindowMs` and `dedupeWindowMs` both default to 600 ms; dedup uses no Scheduler and no timer.

Resolve the stage index from the supplied ctx on every call. Check the indexed `namesByStage` projection before reading `catalog.byName`; use that frozen null-prototype record directly and do not build another handler registry. Return unknown_action for wrong-stage, absent, `__proto__`, and `constructor`. Validate before waiting. `effects.readOnly === true` alone bypasses the wait; absent effects waits. Resolve the active stage's bridge after the wait through existing module-private `resolveBridge`, pass null for no active stage/no bridge/throwing read, and invoke the handler with `{args: validated.value, bridge, meta: meta ?? {}, ack: undefined}`. Separate key, validation, wait, handler, and normalizer catches; handler catch has no binding. Scheduler absence or scheduling failure warns once with fixed prose and runs immediately; caught details never enter the warning. Result contradictions follow CONTEXT: success reason is stripped with warn-once, failure with no reason passes with warn-once, and an unknown reason is invalid_result. Keep all warning latches lazy and per Concierge instance.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm exec vitest run packages/concierge/test/dispatcher.test.ts</automated>
    <automated>bash -c 'set -euo pipefail; file=packages/concierge/test-d/dispatcher.test-d.ts; log=/tmp/concierge-06-04-type.log; errors=/tmp/concierge-06-04-type-errors.log; batch_line=$(rg -n "^type _dispatchBatchSignature" "$file" | cut -d: -f1); keys_line=$(rg -n "^type _conciergeKeys" "$file" | cut -d: -f1); test -n "$batch_line" &amp;&amp; test -n "$keys_line"; set +e; pnpm --filter @fullselfbrowsing/concierge exec tsc -p tsconfig.test-d.json --pretty false &gt;"$log" 2&gt;&amp;1; rc=$?; set -e; test "$rc" -ne 0; rg "error TS" "$log" &gt;"$errors"; test "$(wc -l &lt;"$errors" | tr -d " ")" = 2; saw_batch=0; saw_keys=0; while IFS= read -r diagnostic; do parsed=$(printf "%s\n" "$diagnostic" | sed -nE "s#.*test-d/dispatcher\\.test-d\\.ts\\(([0-9]+),[0-9]+\\): error (TS[0-9]+):.*#\\1 \\2#p"); test -n "$parsed" || { echo "unrelated diagnostic: $diagnostic"; exit 1; }; line=${parsed%% *}; code=${parsed##* }; if test "$line" = "$batch_line" &amp;&amp; test "$code" = TS2339; then saw_batch=$((saw_batch + 1)); elif test "$line" = "$keys_line" &amp;&amp; test "$code" = TS2344; then saw_keys=$((saw_keys + 1)); else echo "unexpected diagnostic: $diagnostic"; exit 1; fi; done &lt;"$errors"; test "$saw_batch" = 1 &amp;&amp; test "$saw_keys" = 1'</automated>
  </verify>
  <done>Every single-call runtime case passes; typecheck's only planned Phase 6 failure is the still-absent batch member pinned for 06-05.</done>
  <acceptance_criteria>
    - `dispatch` is not async and same-key calls return the cached Promise object directly.
    - Promise/timestamp/pending stores are lazy and per instance; pending entries never expire, each settled window begins at settlement, eviction occurs on access with no timer, and both defaults are 600 ms.
    - Wrong-stage and prototype names cannot reach handler lookup or invocation.
    - Validator output, commit ordering, read-only bypass, abort cleanup, and resolveBridge ordering match the action text exactly.
    - Every exit is freshly normalized and sanitized; exception text has no result, console, or telemetry path.
    - Single-call tests pass and the remaining type failure names only dispatchBatch.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 (06-04-T2): Remove the stub contract and make shipped prose truthful</name>
  <read_first>
    - `packages/concierge/src/concierge.ts` — modified header, DISPATCH_NOT_IMPLEMENTED, seal-count prose, return object.
    - `packages/concierge/src/index.ts` — package header that currently says nothing dispatches.
    - `packages/concierge/test/concierge.test.ts` — S27 and all coupled stub/count prose.
    - `packages/concierge/src/types.ts` — ActionResult and Scheduler comments whose Phase 6 obligations are now settled.
    - `.planning/phases/06-dispatcher/06-CONTEXT.md` — explicit instruction to delete S27 rather than repurpose it.
  </read_first>
  <files>packages/concierge/src/concierge.ts, packages/concierge/src/index.ts, packages/concierge/src/types.ts, packages/concierge/test/concierge.test.ts</files>
  <action>
Delete `DISPATCH_NOT_IMPLEMENTED` and its returning function shape together. Remove S27 wholesale and delete/update its header/history prose; do not convert it into a real-dispatch case. Correct `concierge.ts`'s module header and the four-seal statement to the measured three assembly seals after stub deletion. Update `index.ts` to state that direct context-aware dispatch now validates, waits, resolves a bridge, invokes, normalizes, and sanitizes while Session, Transport wiring, and consent gating remain outside Phase 6. Update `types.ts` Scheduler and config docs: the signature is settled, omitted scheduler structurally falls back through host.ts, no-host warns and skips only the commit window, and dedupe is timer-free with a 600 ms default. Preserve the 65-name/51-type/14-value export surface and do not export dispatch/message helpers.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm exec vitest run packages/concierge/test/dispatcher.test.ts packages/concierge/test/concierge.test.ts</automated>
    <automated>bash -c '! rg -n "DISPATCH_NOT_IMPLEMENTED|S27|Phase 6 dispatch stub|nothing here dispatches" packages/concierge/src packages/concierge/test/concierge.test.ts'</automated>
  </verify>
  <done>The stub and its test are gone, shipped prose describes the real direct dispatcher, and no public export count changes.</done>
  <acceptance_criteria>
    - The constant, old function body, S27 case, and all dependent claims are absent.
    - `Object.freeze(` commentary in concierge.ts says three and matches source.
    - Package prose does not claim Session, Transport routing, or consent exists.
    - Existing concierge tests and the full single-call dispatcher suite pass.
    - No new runtime or type export appears.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application loop → dispatch | Untrusted context, name, args, metadata, and replay key enter core. |
| schema/handler/bridge → dispatcher | Consumer callbacks can throw, transform, return malformed objects, or expose live state. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-06-01 | HIGH | Tampering | dedup cache | mitigate | Non-async exact-Promise cache, pending retention, failure caching, access sweep, per-instance state. |
| T-06-02 | HIGH | Elevation of privilege | stage authorization | mitigate | Explicit ctx and namesByStage gate before frozen byName lookup. |
| T-06-03 | HIGH | Tampering | arguments | mitigate | Standard Schema revalidation and transformed output before handler. |
| T-06-04 | HIGH | Information disclosure | exception boundary | mitigate | No-binding catch, fixed result, no telemetry seam, marker-absence tests. |
| T-06-05 | HIGH | Spoofing / Information disclosure | outbound message | mitigate | Fresh allowlisted result plus single sanitizeMessage boundary. |
| T-06-06 | HIGH | Tampering | handler lookup | mitigate | Frozen null-prototype byName; named prototype-key tests. |
| T-06-07 | HIGH | Tampering / Repudiation | commit/abort race | mitigate | Pre-handler wait, listener re-check, idempotent cleanup/canceller, read-only exact gate. |
| T-06-SC | LOW | Tampering | package supply chain | accept | No install or dependency change. |
</threat_model>

<verification>
Single-call tests pass against dist; source/type checks prove the exact cache, lookup, and no-telemetry boundaries; only the planned batch type pin remains red.
</verification>

<success_criteria>
- Direct context-aware dispatch satisfies all single-call requirements without hidden mutable context.
- Retry, stage, validation, abort, exception, output, and prototype threats are mitigated and test-discriminated.
- Phase 4 stub artifacts are deleted rather than normalized or repurposed.
</success_criteria>

<output>
Create `.planning/phases/06-dispatcher/06-04-SUMMARY.md` when done.
</output>
