---
phase: 06-dispatcher
plan: 08
type: execute
wave: 7
gap_closure: true
closes: [VER-06-04]
depends_on: ["06-07"]
files_modified:
  - scripts/phase-06-mutation-battery.mjs
  - .planning/phases/06-dispatcher/06-MUTATION-REGISTER.json
  - .planning/phases/06-dispatcher/06-MUTATION-EVIDENCE.json
  - .planning/phases/06-dispatcher/06-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DSP-01, DSP-02, DSP-03, DSP-04, DSP-05, DSP-06, DSP-07, DSP-08, DSP-09, SEC-02, SEC-06, TRN-04]
user_setup: []

must_haves:
  truths:
    - "The mutation register proves all three repaired runtime contracts with named built-artifact detectors and contains no pending or escaped row."
    - "Mutation evidence is invalidated by any relevant source, test, harness, manifest, or configuration change and matches the current immutable register digest."
    - "A persistent ledger check compares 06-VALIDATION.md to the live mutation digest and Vitest totals, requires R68/Q17 detector evidence, and requires exactly one row for 06-07-T1, 06-07-T2, 06-07-T3, 06-08-T1, 06-08-T2, and 06-08-T3."
    - "REQUIREMENTS.md describes the implemented contracts and mandatorily cites DSP-01/R68, DSP-02/R06, DSP-06/Q04, and DSP-07/Q17 without renaming or repurposing the existing Q16 immutable-result case."
    - "Build, type, runtime, telemetry, artifact, dependency, pack, and Node-floor gates pass on restored source."
  artifacts:
    - path: "scripts/phase-06-mutation-battery.mjs"
      provides: "57-row register, strict mutation evidence, and live ledger verification"
      contains: "verifyLedgers"
    - path: ".planning/phases/06-dispatcher/06-MUTATION-REGISTER.json"
      provides: "Current immutable 57-mutant definitions"
      contains: "M-06-S36"
    - path: ".planning/phases/06-dispatcher/06-MUTATION-EVIDENCE.json"
      provides: "57 measured green rows bound to the current revision"
      contains: "revisionDigest"
    - path: ".planning/phases/06-dispatcher/06-VALIDATION.md"
      provides: "Current digest, detector counts, test totals, R68/Q17 evidence, and all six gap-closure task rows"
      contains: "06-07-T1"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Truthful mandatory DSP-01, DSP-02, DSP-06, and DSP-07 traceability"
      contains: "R68"
  key_links:
    - from: "scripts/phase-06-mutation-battery.mjs"
      to: ".planning/phases/06-dispatcher/06-MUTATION-REGISTER.json"
      via: "serialized immutable register digest"
      pattern: "registerDigest"
    - from: "scripts/phase-06-mutation-battery.mjs"
      to: ".planning/phases/06-dispatcher/06-VALIDATION.md"
      via: "verify ledgers compares live digest and Vitest totals and requires R68, Q17, and every 06-07/06-08 closure-task row"
      pattern: "verifyLedgers"
    - from: ".planning/REQUIREMENTS.md"
      to: "packages/concierge/test/dispatcher.test.ts"
      via: "DSP-01 and DSP-02 cite the mandatory R68 and R06 evidence"
      pattern: "R68|R06"
    - from: ".planning/REQUIREMENTS.md"
      to: "packages/concierge/test/dispatcher-batch.test.ts"
      via: "DSP-06 and DSP-07 cite Q04/Q17 while Q16 retains its immutable nested-result meaning"
      pattern: "Q04|Q16|Q17"
---

<objective>
Close the Phase 6 evidence-freshness gap after the runtime repairs in 06-07.

Purpose: Mutation JSON can be current while human-readable validation and requirement ledgers remain stale. This plan makes the current digest, detector totals, runtime totals, and locked behavior claims executable rather than editorial, then regenerates all evidence from restored source.

Output: a 57-row current mutation register/evidence pair, a persistent live ledger check, refreshed 06-VALIDATION.md, and corrected Phase 6 traceability in REQUIREMENTS.md.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-dispatcher/06-CONTEXT.md
@.planning/phases/06-dispatcher/06-RESEARCH.md
@.planning/phases/06-dispatcher/06-VERIFICATION.md
@.planning/phases/06-dispatcher/06-VALIDATION.md
@.planning/phases/06-dispatcher/06-06-SUMMARY.md
@.planning/phases/06-dispatcher/06-07-SUMMARY.md

<interfaces>
Existing mutation CLI:
- node scripts/phase-06-mutation-battery.mjs refresh
- node scripts/phase-06-mutation-battery.mjs self-test
- node scripts/phase-06-mutation-battery.mjs run single|batch
- node scripts/phase-06-mutation-battery.mjs run range &lt;first-id&gt; &lt;last-id&gt; (inclusive, contiguous, one group, one to four mutants)
- node scripts/phase-06-mutation-battery.mjs verify single|all

New persistent ledger gate:
- node scripts/phase-06-mutation-battery.mjs verify ledgers
- It first enforces verify all, then measures the live Vitest report and compares its file/test totals plus the current register digest/counts to 06-VALIDATION.md and the Phase 6 rows in REQUIREMENTS.md.

Runtime tests import packages/concierge/dist/index.js, so every runtime evidence command builds first. Mutation work must restore its target byte-for-byte and leave packages/concierge/src, test, test-d, manifests, and pnpm-lock.yaml clean.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (06-08-T1): Extend the mutation register and add a live ledger-consistency gate</name>
  <read_first>
    - scripts/phase-06-mutation-battery.mjs — expected ID arrays, S07, B05/B06, register/evidence validation, revisionDigest, verify, self-test, and CLI dispatch.
    - .planning/phases/06-dispatcher/06-MUTATION-REGISTER.json — current 54-row generated shape.
    - .planning/phases/06-dispatcher/06-MUTATION-EVIDENCE.json — current generated evidence shape.
    - packages/concierge/src/concierge.ts — final metadata validation from 06-07.
    - packages/concierge/src/dispatch.ts — final BigInt and malformed-JSON branches from 06-07.
    - packages/concierge/test/dispatcher.test.ts — R06 and R68 exact markers.
    - packages/concierge/test/dispatcher-batch.test.ts — Q04, existing Q16, and new Q17 exact markers.
    - .planning/phases/06-dispatcher/06-VERIFICATION.md — VER-06-04 stale-digest and stale-total findings.
  </read_first>
  <files>scripts/phase-06-mutation-battery.mjs, .planning/phases/06-dispatcher/06-MUTATION-REGISTER.json, .planning/phases/06-dispatcher/06-MUTATION-EVIDENCE.json</files>
  <action>
Extend the immutable expected sets from 34 single plus 20 batch rows to 36 single plus 21 batch rows, preserving every existing ID in order and appending exactly M-06-S35, M-06-S36, and M-06-B21. Replace literal 34, 20, and 54 verification arithmetic and messages with EXPECTED_SINGLE_IDS.length, EXPECTED_BATCH_IDS.length, and EXPECTED_M06_IDS.length so a future register extension cannot leave the verifier certifying the prior count.

Add M-06-S35 against the unique metadata primitive-validation guard from 06-07, replacing it with an accepting path and requiring only R68 with marker [RED:R68:malformed-metadata-totality]. Add M-06-S36 against the unique BigInt-unkeyable branch, replacing it with a keyable tagged return that compiles and requiring only R06 with marker [RED:R06:bigint-args]. Add M-06-B21 against the same runtime metadata guard but run the batch detector only, requiring Q17 with marker [RED:Q17:malformed-callid-correlation]. Duplicate target literals across S35/B21 are intentional: one proves the direct boundary and one proves row completeness. Keep the existing Q16 detector and [RED:Q16:immutable-nested-result] marker unchanged.

Rewrite B05's baseline literal for the new parse-to-empty-object catch and keep its replacement as uncaught JSON.parse, detected by Q04. Invert B06 to the locked contract: its baseline is args = {} in the parse catch, its mutant authors invalid_args and continues before dispatch, and Q04 must fail because the validator no longer observes the empty object. Keep every other mutant definition and detector unchanged.

Add an inclusive `run range &lt;first-id&gt; &lt;last-id&gt;` CLI path backed by a named range selector over EXPECTED_M06_IDS. Require both endpoints to exist, appear in ascending contiguous order, belong to the same single/batch group, and select one to four mutants; reject unknown, reversed, cross-group, empty, or larger ranges. Reuse the existing per-mutant execution and evidence update path so every range can start from fresh pending or mixed green/pending evidence, skips only current green rows, and fails on the first incomplete row. Preserve `run single|batch` compatibility, but use only bounded ranges for Task 2 regeneration.

Add verify ledgers as a fail-closed CLI mode implemented by a named verifyLedgers function and a side-effect-free ledger-snapshot validator shared with self-test. It must call the existing all-row verifier, run pnpm build, run the full Vitest suite with JSON output in a unique temporary directory, require a readable successful report with zero pending/todo/unhandled/suite failures, and compare the measured test-file count and passed/total count to the unique pnpm test evidence row in 06-VALIDATION.md. It must parse the mutation-evidence section and require exactly the current register digest, current single/batch/total counts, and zero pending rows. It must require unique R68 and Q17 detector entries in 06-VALIDATION.md, verify the exact [RED:R68:malformed-metadata-totality] and [RED:Q17:malformed-callid-correlation] markers still exist in their test files, and also reject any rename or repurposing of [RED:Q16:immutable-nested-result]. It must require exactly one complete task-map row for each of 06-07-T1, 06-07-T2, 06-07-T3, 06-08-T1, 06-08-T2, and 06-08-T3. The row evidence must respectively name R68+Q17, R06, Q04, the 57-row register plus range/ledger self-tests, 57/57 plus verify all, and final gates plus verify ledgers. It must parse unique REQUIREMENTS.md Phase 6 traceability rows and require DSP-01 to cite R68 plus malformed-metadata containment while retaining R01/R02 valid-string identity, DSP-02 to cite R06 plus BigInt no-dedup behavior, DSP-06 to cite Q04 plus empty-object validation, and DSP-07 to cite Q17 result completeness. Any missing, ambiguous, duplicate, stale, or contradictory row exits non-zero.

Extend self-test with in-memory counterexamples against the shared ledger-snapshot validator. Require separate negative cases for a wrong mutation digest, an off-by-one test total, a stale detector count, missing R68 detector evidence, missing Q17 detector evidence, replacing Q17 with the colliding Q16 ID, missing DSP-01/R68, DSP-02/R06, DSP-06/Q04, or DSP-07/Q17 citation, and removal of each one of the six required closure-task rows in turn. Add range-selector negatives for unknown, reversed, cross-group, and five-mutant ranges. The self-test must fail if any counterexample is accepted and must not edit planning ledgers or generated evidence. Then run self-test and refresh; refresh must write exactly 57 pending rows and no green claim.
  </action>
  <verify>
    <automated>node scripts/phase-06-mutation-battery.mjs self-test &amp;&amp; node scripts/phase-06-mutation-battery.mjs refresh</automated>
    <automated>node -e "const fs=require('node:fs');const r=JSON.parse(fs.readFileSync('.planning/phases/06-dispatcher/06-MUTATION-REGISTER.json','utf8'));const e=JSON.parse(fs.readFileSync('.planning/phases/06-dispatcher/06-MUTATION-EVIDENCE.json','utf8'));if(r.mutants.length!==57||e.rows.length!==57||e.rows.some(x=&gt;x.status!=='pending')||r.registerDigest!==e.registerDigest)process.exit(1)"</automated>
  </verify>
  <done>The harness owns a strict 57-ID register, the new gap detectors are named, generated evidence is explicitly pending, and live ledger comparison has proven negative controls.</done>
  <acceptance_criteria>
    - EXPECTED_M06_IDS contains exactly 57 unique ordered IDs: S01-S36 followed by B01-B21.
    - S35, S36, and B21 compile and target R68, R06, and Q17 respectively; Q16 remains [RED:Q16:immutable-nested-result].
    - B05/B06 now defend parse-to-empty-object-then-validate, not the superseded direct rejection.
    - All verification counts derive from expected-array lengths; no certification branch hardcodes 34, 20, or 54.
    - The range selector accepts only inclusive contiguous same-group spans of one to four registered IDs and its invalid-range self-tests pass.
    - verify ledgers fails on separate missing-row counterexamples for 06-07-T1, 06-07-T2, 06-07-T3, 06-08-T1, 06-08-T2, and 06-08-T3, plus every missing-detector, missing-citation, stale-digest/count, and Q16-collision counterexample; refresh leaves 57 pending rows.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 (06-08-T2): Regenerate all mutation evidence against the repaired source</name>
  <read_first>
    - scripts/phase-06-mutation-battery.mjs — 57-row runner created by Task 1.
    - .planning/phases/06-dispatcher/06-MUTATION-REGISTER.json — generated pending register.
    - .planning/phases/06-dispatcher/06-MUTATION-EVIDENCE.json — generated pending evidence.
    - packages/concierge/test/dispatcher.test.ts — exact intended R-case fingerprints.
    - packages/concierge/test/dispatcher-batch.test.ts — exact intended Q-case fingerprints.
    - scripts/mutate-and-prove.sh — atomic apply/gate/restore semantics.
  </read_first>
  <files>scripts/phase-06-mutation-battery.mjs, .planning/phases/06-dispatcher/06-MUTATION-REGISTER.json, .planning/phases/06-dispatcher/06-MUTATION-EVIDENCE.json</files>
  <action>
Regenerate from freshly pending evidence with these exact inclusive shards, each as its own independently runnable CLI invocation: single S01-S04, S05-S08, S09-S12, S13-S16, S17-S20, S21-S24, S25-S28, S29-S32, and S33-S36; batch B01-B04, B05-B08, B09-B12, B13-B16, B17-B20, and B21-B21. Each shard selects at most four contiguous mutants, updates evidence after each row, and stays within the 30-second feedback target independently; do not replace these with the monolithic `run single` or `run batch` commands. After the ninth single shard, verify single must require all 36 single rows green and permit only the 21 explicitly pending batch rows. After the final one-row batch shard, verify all must require exactly 57 green rows and zero pending or escaped rows, proving no gap between shard boundaries.

Do not hand-edit generated JSON status, digest, hashes, timestamps, detector output, or restoration fields. If a literal no longer matches exactly once, a mutant does not compile, the intended test count is zero, a foreign test fails, an expected marker is absent, source is not byte-restored, or scoped status differs, fix the definition or implementation and rerun the affected bounded shard. Every accepted row must retain the current full-tree revisionDigest, compiled-build proof, non-zero exact detector, harness kill, target hash equality, restored green gates, and clean scoped tree.

After the last mutant, rebuild restored source and run both complete dispatcher suites plus package typecheck. Leave no source, test, type-test, manifest, or lockfile mutation behind.
  </action>
  <verify>
    <automated>node scripts/phase-06-mutation-battery.mjs run range M-06-S01 M-06-S04</automated>
    <automated>node scripts/phase-06-mutation-battery.mjs run range M-06-S05 M-06-S08</automated>
    <automated>node scripts/phase-06-mutation-battery.mjs run range M-06-S09 M-06-S12</automated>
    <automated>node scripts/phase-06-mutation-battery.mjs run range M-06-S13 M-06-S16</automated>
    <automated>node scripts/phase-06-mutation-battery.mjs run range M-06-S17 M-06-S20</automated>
    <automated>node scripts/phase-06-mutation-battery.mjs run range M-06-S21 M-06-S24</automated>
    <automated>node scripts/phase-06-mutation-battery.mjs run range M-06-S25 M-06-S28</automated>
    <automated>node scripts/phase-06-mutation-battery.mjs run range M-06-S29 M-06-S32</automated>
    <automated>node scripts/phase-06-mutation-battery.mjs run range M-06-S33 M-06-S36</automated>
    <automated>node scripts/phase-06-mutation-battery.mjs verify single</automated>
    <automated>node scripts/phase-06-mutation-battery.mjs run range M-06-B01 M-06-B04</automated>
    <automated>node scripts/phase-06-mutation-battery.mjs run range M-06-B05 M-06-B08</automated>
    <automated>node scripts/phase-06-mutation-battery.mjs run range M-06-B09 M-06-B12</automated>
    <automated>node scripts/phase-06-mutation-battery.mjs run range M-06-B13 M-06-B16</automated>
    <automated>node scripts/phase-06-mutation-battery.mjs run range M-06-B17 M-06-B20</automated>
    <automated>node scripts/phase-06-mutation-battery.mjs run range M-06-B21 M-06-B21</automated>
    <automated>node scripts/phase-06-mutation-battery.mjs verify all</automated>
    <automated>pnpm build &amp;&amp; pnpm exec vitest run packages/concierge/test/dispatcher.test.ts packages/concierge/test/dispatcher-batch.test.ts &amp;&amp; pnpm --filter @fullselfbrowsing/concierge typecheck</automated>
    <automated>bash -c 'test -z "$(git status --porcelain --untracked-files=all -- packages/concierge/src packages/concierge/test packages/concierge/test-d packages/concierge/package.json package.json pnpm-lock.yaml)"'</automated>
  </verify>
  <done>All 57 compiled mutants are killed by their exact named detectors against the repaired revision, and restored source is green and clean.</done>
  <acceptance_criteria>
    - Evidence contains 36 green single rows, 21 green batch rows, and zero pending/escape/failure rows.
    - S35/R68, S36/R06, B05/Q04, B06/Q04, and B21/Q17 have exact fingerprints and non-zero test counts; the pre-existing Q16 immutable-result test remains green.
    - All 15 explicit shard commands select one to four registered mutants, are independently restartable, and stay within the 30-second feedback target; final verify all proves complete 57-row coverage.
    - Register and evidence digests match and every row's revisionDigest matches current tracked inputs.
    - Source and test targets are byte-restored and scoped git status is clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3 (06-08-T3): Measure final gates and refresh validation and requirement ledgers</name>
  <read_first>
    - .planning/phases/06-dispatcher/06-VALIDATION.md — stale digest, task map, mutation table, phase-gate rows, and sign-off.
    - .planning/REQUIREMENTS.md — Dispatch requirements and Phase 6 traceability rows.
    - .planning/phases/06-dispatcher/06-MUTATION-REGISTER.json — current digest and 57 definitions.
    - .planning/phases/06-dispatcher/06-MUTATION-EVIDENCE.json — current measured rows.
    - .planning/phases/06-dispatcher/06-VERIFICATION.md — exact stale claims and four gaps being closed.
    - scripts/check-no-telemetry.mjs — positive-control and live SEC-02 audit.
    - scripts/phase-06-mutation-battery.mjs — verify all and verify ledgers modes.
    - packages/concierge/test/dispatcher.test.ts — R06/R68 and unchanged Phase 6 single-call evidence.
    - packages/concierge/test/dispatcher-batch.test.ts — Q04/Q17, preserved Q16, and unchanged Phase 6 batch evidence.
  </read_first>
  <files>.planning/phases/06-dispatcher/06-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <action>
First run mutation verify all, telemetry self-test and live audit, and the exact phase gate: pnpm build, pnpm typecheck, pnpm test, pnpm check:artifact, pnpm check:deps, pnpm check:pack, and pnpm check:node-floor. Also run a full Vitest JSON report and derive its test-file count, passed count, total count, pending count, and todo count from the report rather than copying the verifier's old numbers.

Refresh 06-VALIDATION.md without rewriting historical plans. Add exactly one gap-closure task row for each of 06-07-T1, 06-07-T2, 06-07-T3, 06-08-T1, 06-08-T2, and 06-08-T3. Record their evidence as R68+Q17 malformed-metadata totality/correlation, R06 BigInt no-dedup, Q04 empty-object validation, the 57-row register plus bounded-range/ledger negative controls, 57/57 plus verify all, and final release gates plus verify ledgers, respectively. Replace the obsolete mutation digest/count evidence with the exact current register digest and measured 36/36 single, 21/21 batch, 57/57 total values. Replace the old pnpm test totals and any affected build/artifact headlines with the values just measured. Add unique detector evidence for R68 and Q17, retain Q16 only as the immutable nested-result regression, and preserve status complete and Nyquist sign-off only because every current command is green.

Correct REQUIREMENTS.md's Phase 6 traceability, not the locked requirement text. DSP-01 must cite R68 for malformed-metadata containment while retaining R01/R02 for exact Promise identity with valid string callIds; it must not claim invalid callIds are deduplicated. DSP-02 must state that equal BigInt and cyclic or aliased arguments do not throw and do not deduplicate, cite R05/R06, and retain R06a's injective collision evidence for keyable values. DSP-06 must state malformed JSON becomes an empty object, reaches action validation, is rejected by Q04's schema, and later calls continue. DSP-07 must add Q17's proof that malformed call metadata still yields one correlated row, alongside the existing serial/order/abort evidence; Q16 continues to mean immutable nested batch results across cached retries. Leave all other Phase 6 evidence truthful and leave Phase 7/8/9 requirements untouched.

After the edits, run verify ledgers. It must independently rerun/parse live test evidence and reject any mismatch between the planning ledgers, current register/evidence, named tests, or measured totals. Remove no verifier report and do not alter historical plans 06-01 through 06-06.
  </action>
  <verify>
    <automated>node scripts/phase-06-mutation-battery.mjs verify all &amp;&amp; node scripts/check-no-telemetry.mjs --self-test &amp;&amp; node scripts/check-no-telemetry.mjs</automated>
    <automated>pnpm build &amp;&amp; pnpm typecheck &amp;&amp; pnpm test &amp;&amp; pnpm check:artifact &amp;&amp; pnpm check:deps &amp;&amp; pnpm check:pack &amp;&amp; pnpm check:node-floor</automated>
    <automated>node scripts/phase-06-mutation-battery.mjs verify ledgers</automated>
    <automated>bash -c 'test -z "$(git status --porcelain --untracked-files=all -- packages/concierge/src packages/concierge/test packages/concierge/test-d packages/concierge/package.json package.json pnpm-lock.yaml)"'</automated>
  </verify>
  <done>Validation and requirement ledgers match current generated evidence and live test totals, and every Phase 6 release/security gate passes on restored source.</done>
  <acceptance_criteria>
    - 06-VALIDATION.md contains the current register digest, 57/57 mutation result, current live test-file/test totals, unique R68/Q17 evidence, and exactly one row for 06-07-T1, 06-07-T2, 06-07-T3, 06-08-T1, 06-08-T2, and 06-08-T3.
    - REQUIREMENTS.md DSP-01, DSP-02, DSP-06, and DSP-07 mandatorily match R68, R06, Q04, and Q17; Q16 retains only its immutable nested-result semantics.
    - verify ledgers exits 0 only after comparing all required detector, citation, task-row, generated-evidence, and live-test facts; self-test proves each missing class is rejected.
    - Telemetry self-test/live audit and every release gate exit 0.
    - No Phase 7/8/9 item is marked complete and plans 06-01 through 06-06 remain byte-identical.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mutation harness → evidence JSON | A no-op, compile-only, wrong-test, or unrestored mutant can falsely certify coverage. |
| generated evidence → planning ledgers | Human-readable digest and test totals can drift after source or test changes. |
| traceability prose → requirement status | A checked requirement can cite a test that proves the opposite behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-G05 | Tampering / Repudiation | mutation register and runner | mitigate | Exact ordered IDs, one-occurrence mutants, compiled build, non-zero named detector, fingerprint, byte restore, revision digest, and clean-tree proof. |
| T-06-G06 | Repudiation | 06-VALIDATION.md | mitigate | verify ledgers compares the recorded digest/counts to generated evidence and a live Vitest JSON report, then requires unique R68/Q17 evidence and all six closure-task rows. |
| T-06-G07 | Spoofing | REQUIREMENTS.md traceability | mitigate | Parse DSP-01/DSP-02/DSP-06/DSP-07 rows and require R68/R06/Q04/Q17 plus locked behavior terms while preserving Q16's existing meaning. |
| T-06-G08 | Information disclosure | SEC-02 evidence | mitigate | Retain AST positive controls and live no-telemetry audit; do not add an output channel. |
| T-06-SC | Tampering | package supply chain | accept | No install task exists; manifests and lockfile must remain unchanged and clean. |
</threat_model>

<source_coverage_audit>
| Source | ID | Feature or constraint | Plan coverage | Status | Notes |
|--------|----|-----------------------|---------------|--------|-------|
| GOAL | — | Every retried, malformed, aborted, or crashing call produces exactly one honest result and no effect fires twice | 06-04, 06-05, 06-07, 06-08 | COVERED | 06-07 closes malformed totality and locked key/parse behavior; 06-08 re-proves the whole boundary. |
| REQ | DSP-01 | Valid string callId returns exact Promise identity and malformed metadata remains inside the result boundary | 06-01, 06-04, 06-06, 06-07, 06-08 | COVERED | R01/R02 remain unchanged; DSP-01 traceability must cite R68 for malformed-ID containment. |
| REQ | DSP-02 | Fallback key or no-dedup degradation without throw | 06-01, 06-04, 06-07, 06-08 | COVERED | R05/R06/R06a and S07/S36. |
| REQ | DSP-03 | Handler crash becomes generic non-leaking result | 06-01, 06-04, 06-06, 06-08 | COVERED | Unchanged R34-R36 plus telemetry audit. |
| REQ | DSP-04 | Missing handler settles honestly | 06-01, 06-04, 06-06, 06-08 | COVERED | Unchanged R11/R12. |
| REQ | DSP-05 | Independent schema validation | 06-01, 06-04, 06-07, 06-08 | COVERED | Q04 rejoins malformed wire input to the existing R13-R18a validator boundary. |
| REQ | DSP-06 | Malformed JSON becomes empty object then validation rejects | 06-02, 06-05, 06-07, 06-08 | COVERED | Corrected Q04 and inverted B06. |
| REQ | DSP-07 | Serial ordered complete batch results | 06-02, 06-05, 06-07, 06-08 | COVERED | Existing Q01-Q16 retain their meanings; Q17 adds malformed-call correlation. |
| REQ | DSP-08 | Commit window and abort cancellation | 06-01, 06-03, 06-04, 06-06, 06-08 | COVERED | Unchanged R20-R33/Q09-Q12. |
| REQ | DSP-09 | Malformed handler result becomes honest failure | 06-01, 06-04, 06-06, 06-08 | COVERED | Unchanged R37-R45. |
| REQ | SEC-02 | No thrown detail reaches telemetry | 06-01, 06-04, 06-06, 06-08 | COVERED | Structural AST audit plus R34-R36; no telemetry seam added. |
| REQ | SEC-06 | Every outgoing message is sanitized | 06-01, 06-03, 06-04, 06-07, 06-08 | COVERED | Invalid-metadata result uses authoredResult; R47-R51 remain green. |
| REQ | TRN-04 | Direct use without Transport | 06-01, 06-02, 06-04, 06-05, 06-07, 06-08 | COVERED | All new regressions call Concierge directly. |
| RESEARCH | validation | Parse errors substitute empty object and use the identical validation path | 06-07-T3, 06-08-T1/T3 | COVERED | Restores Research Pattern 4 and Parse Errors Bypass Validation guidance. |
| RESEARCH | keying | Key derivation never throws; unkeyable values return no key | 06-07-T1/T2, 06-08-T1/T2 | COVERED | Metadata guard plus BigInt/cycle mutation detectors. |
| RESEARCH | architecture | Preserve stage gate, serial batch, single dispatch delegation, commit wait, bridge resolver, normalizer, sanitizer, and no new dependencies | 06-01 through 06-07 | COVERED | Gap work changes no subsystem boundary or public seam. |
| RESEARCH | evidence | Build before dist tests; exact mutation detectors; bounded feedback; full phase gate | 06-08-T1/T2/T3 | COVERED | Fifteen bounded ranges regenerate all 57 rows; verify all and live ledger mode enforce completeness and revision invalidation. |
| CONTEXT | Public dispatch seams | Context-first dispatch and transport-independent immutable batch rows | 06-01, 06-04, 06-05, 06-07 | COVERED | Signatures stay unchanged. |
| CONTEXT | Dedup | Non-async exact Promise cache; callId authority; cycle and BigInt no-dedup; lazy per-instance settlement window | 06-04, 06-07-T1/T2, 06-08 | COVERED | Restores the only contradicted locked detail. |
| CONTEXT | Batch and lookup | Serial outputIndex order, every aborted call settles, frozen stage lookup, one bridge resolver | 06-04, 06-05, 06-07 | COVERED | Q17 adds malformed-row totality; existing Q16 continues to prove immutable cached nested results. |
| CONTEXT | Result and sanitizer | Flat ActionResult normalizer and one sanitized outbound boundary | 06-03, 06-04, 06-07-T1 | COVERED | Metadata failure is reasonless because no existing code truthfully names it. |
| CONTEXT | Scheduler and SEC-02 | Optional structural timer; structural no-telemetry satisfaction | 06-03, 06-04, 06-06, 06-08 | COVERED | Unchanged and re-gated. |
| CONTEXT | Deferred Ideas | Telemetry seam, consent gate, normalizeSnapshot wiring, batch_aborted split, ServerSafeConcierge, returned-object freezing | EXCLUDED | COVERED | Explicitly deferred to later phases; absent from both gap plans. |
</source_coverage_audit>

<verification>
The 57-row register and evidence match the repaired revision, all bounded mutation shards and live ledger verification pass, all release/security gates are green, and existing plans 06-01 through 06-06 are unchanged.
</verification>

<success_criteria>
- Every Phase 6 mutation row is green against the current source and exact intended detector.
- A stale mutation digest, detector count, test total, missing R68/Q17 detector, missing closure-task row, or missing DSP-01/DSP-02/DSP-06/DSP-07 evidence claim fails verify ledgers.
- Validation and requirements record only measured current behavior.
- The final repository has no applied mutant, dependency change, or deferred subsystem.
</success_criteria>

<output>
Create .planning/phases/06-dispatcher/06-08-SUMMARY.md when done.
</output>
