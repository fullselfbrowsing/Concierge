---
phase: 07-session-and-the-transport-seam
plan: 06
type: execute
wave: 5
depends_on: ["07-05"]
files_modified:
  - scripts/phase-07-mutation-battery.mjs
  - packages/concierge/test/fixtures/probe.ts
  - scripts/pack-install-check.sh
  - .planning/phases/07-session-and-the-transport-seam/07-MUTATION-REGISTER.json
  - .planning/phases/07-session-and-the-transport-seam/07-MUTATION-EVIDENCE.json
  - .planning/phases/07-session-and-the-transport-seam/07-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SES-01, SES-02, SES-03, SES-04, TRN-02]
user_setup: []

must_haves:
  truths:
    - "Every load-bearing Session branch is killed by a compiled or package-valid mutant whose exact named detector ran non-zero, while every target is restored byte-for-byte and restored gates are green."
    - "A foreign strict/EOPT consumer can import, type, construct, and stop createSession from the packed package, while the test stub is absent from barrel and tarball."
    - "The full release gate passes with no dependency/manifest/lockfile change and live runtime/type/artifact/package counts are recorded rather than copied from the Phase 6 baseline."
    - "Validation is signed complete only when SES-01..04 are complete, TRN-02 is recorded as a Phase 7 seam partial that remains pending for Phase 8 consent-kernel proof, and every task, test, register/evidence digest, and fresh full-suite report agrees."
  artifacts:
    - path: "scripts/phase-07-mutation-battery.mjs"
      provides: "Thirty-mutant bounded runner, restoration proof, evidence generator, input-integrity mode, and live-ledger verifier"
      contains: "M-07-C01"
    - path: ".planning/phases/07-session-and-the-transport-seam/07-MUTATION-REGISTER.json"
      provides: "Immutable ordered Phase 7 mutation definitions and shared digest"
      contains: "M-07-P02"
    - path: ".planning/phases/07-session-and-the-transport-seam/07-MUTATION-EVIDENCE.json"
      provides: "Measured per-mutant build/detector/restoration/revision evidence and release/package hashes"
      contains: "registerDigest"
    - path: "packages/concierge/test/fixtures/probe.ts"
      provides: "Foreign exactOptionalPropertyTypes consumer proof for factory, new public types, six-key transport, and Promise stop"
      contains: "createSession"
    - path: "scripts/pack-install-check.sh"
      provides: "Tarball stub exclusion and runtime createSession callable gate"
      contains: "stub-transport"
    - path: ".planning/phases/07-session-and-the-transport-seam/07-VALIDATION.md"
      provides: "Live Phase 7 Nyquist, threat, mutation, release, and per-task evidence"
      contains: "status: complete"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Evidence-rich completion traceability for SES-01..04 and the pending Phase 8 remainder of TRN-02"
      contains: "SES-04"
  key_links:
    - from: "scripts/phase-07-mutation-battery.mjs"
      to: "session catalog/routing/lifecycle test markers"
      via: "Each mutant names one exact file, case id, and RED fingerprint after a successful build"
      pattern: "intendedCaseIds|expectedFailureFingerprint"
    - from: "scripts/pack-install-check.sh"
      to: "packed tarball and fixtures/probe.ts"
      via: "Tar listing rejects stub content before foreign typecheck/runtime import"
      pattern: "stub-transport|createSession"
    - from: "verify ledgers"
      to: "07-VALIDATION.md and REQUIREMENTS.md"
      via: "Generated evidence plus a fresh Vitest JSON report must match every recorded task/requirement/count"
      pattern: "verify ledgers"
---

<objective>
Certify the Session/Transport seam with mutation, foreign-consumer, tarball, release, lockfile, and live-ledger evidence.

Purpose: A green happy-path suite is insufficient for authority/lifecycle code; the final gate must prove the tests discriminate every locked failure mode and that the shipped package matches the source contract without including the test transport.

Output: A 30-row immutable mutation register/evidence set, hardened pack/foreign probes, all release gates, an explicit dependency-input verifier, complete Phase 7 validation, SES-01..04 closure, and a mechanically enforced TRN-02 Phase 8 handoff.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-session-and-the-transport-seam/07-CONTEXT.md
@.planning/phases/07-session-and-the-transport-seam/07-RESEARCH.md
@.planning/phases/07-session-and-the-transport-seam/07-PATTERNS.md
@.planning/phases/07-session-and-the-transport-seam/07-VALIDATION.md
@.planning/phases/07-session-and-the-transport-seam/07-01-SUMMARY.md
@.planning/phases/07-session-and-the-transport-seam/07-02-SUMMARY.md
@.planning/phases/07-session-and-the-transport-seam/07-03-SUMMARY.md
@.planning/phases/07-session-and-the-transport-seam/07-04-SUMMARY.md
@.planning/phases/07-session-and-the-transport-seam/07-05-SUMMARY.md

<decision_traceability>
CTX-07-01 through CTX-07-15 are the planner-local aliases defined in Plans 07-01 through 07-05 for every locked implementation bullet in 07-CONTEXT.md. This plan proves all of them by named mutation, package, and live-ledger evidence; it adds no new product decision.
</decision_traceability>

<interfaces>
Phase 6 mutation proof contract to preserve:
- Runtime mutant credit requires successful build, testsRan &gt; 0, exact named failing marker, harness kill, byte-identical target restore, restored green gates, and clean scoped tree.
- Package mutant credit requires a successful pack/build precondition, the exact package detector to fail, byte restore, restored package gate, and clean scoped tree.
- Register/evidence share one digest; evidence rows are generated only by the harness.
- Bounded `run range START END` accepts contiguous same-group ranges of one to four registered ids.

Exact Phase 7 ordered register (30 rows):
- Catalog: M-07-C01..C09.
- Routing: M-07-R01..R09.
- Lifecycle: M-07-L01..L08.
- Diagnostics: M-07-D01..D02.
- Package/guard: M-07-P01..P02.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (07-06-01): Create foreign/package gates and the pending 30-mutant evidence system</name>
  <read_first>
    - scripts/phase-06-mutation-battery.mjs — bounded range runner, register digest, exact fingerprints, restoration, revision digest, self-tests, and live-ledger architecture.
    - scripts/mutate-and-prove.sh — atomic apply/gate/restore behavior and exit semantics.
    - .planning/phases/06-dispatcher/06-MUTATION-REGISTER.json — immutable schemaVersion 2 definition shape.
    - .planning/phases/06-dispatcher/06-MUTATION-EVIDENCE.json — generated evidence row and digest schema.
    - .planning/phases/06-dispatcher/06-VALIDATION.md — live mutation/release/task evidence format.
    - packages/concierge/test/fixtures/probe.ts — current foreign value/type annotations and exactOptionalPropertyTypes consumer.
    - scripts/pack-install-check.sh — current pack, foreign tsc, runtime import, cleanup trap, and tgz path handling.
    - packages/concierge/package.json — unchanged files list and candidate transient P01 package mutation target.
    - packages/concierge/test/session-catalog.test.ts — exact C01-C16 markers.
    - packages/concierge/test/session-routing.test.ts — exact J01-J18 markers.
    - packages/concierge/test/session-lifecycle.test.ts — exact L01-L16 markers.
    - packages/concierge/test/stub-transport.test.ts — exact U01-U08 markers.
    - packages/concierge/test/single-instance.test.ts — exact F7 direct-guard detector.
    - .planning/phases/07-session-and-the-transport-seam/07-VALIDATION.md — required task ids, threat index, minimum mutations, and sign-off contract.
  </read_first>
  <files>scripts/phase-07-mutation-battery.mjs, packages/concierge/test/fixtures/probe.ts, scripts/pack-install-check.sh, .planning/phases/07-session-and-the-transport-seam/07-MUTATION-REGISTER.json, .planning/phases/07-session-and-the-transport-seam/07-MUTATION-EVIDENCE.json</files>
  <action>
Per CTX-07-13/14, extend the foreign probe to import createSession as a value and TransportStatus, SessionDiagnosticCode, SessionDiagnostic, SessionConfig, and Session as types from the installed package. Define a hand-written six-key transport and structural Concierge under foreign strict + exactOptionalPropertyTypes, include computed optional values that may be undefined, type the factory/handle exactly, and prove stop is Promise&lt;void&gt;. Extend pack-install-check.sh immediately after `pnpm pack` to list tar entries and fail if any path contains `stub-transport` or `test/fixtures`; retain the external temporary project, cleanup trap, shipped declaration check with skipLibCheck false, and runtime import, and add a callable createSession assertion. Do not widen package files or alter a manifest to make this pass.

Create phase-07-mutation-battery.mjs by adapting Phase 6's harness architecture, not its dispatcher definitions. Define exactly these mutations and detector markers:

- M-07-C01 removes initial setTools → C01; C02 removes connected replay → C04; C03 compares stage/deep value instead of catalog reference → C03; C04 throws fixed-catalog error before stop → C06; C05 reconciles a queued context against `confirmedCatalog` instead of the last successfully `publishedCatalog` → C16; C06 unconditionally aborts the successfully-published-but-unconfirmed epoch and republishes when the queued context catalog is the same `publishedCatalog` reference → C15; C07 disables `transitionDraining` serialization so a nested context/status transition publishes reentrantly → C10/C11/C15/C16; C08 removes the post-callback generation/latest-request check so resumed outer B overwrites C → C10/C11/C15/C16; C09 lets `maybePump` run while publication is pending, published reconciliation is incomplete, or the transition drain is non-empty → C11-C16.
- M-07-R01 permits two pumps → J01; R02 reads context at execution → J07; R03 omits active epoch abort → J09; R04 omits queued/held epoch abort → J10/C11; R05 forwards only the transport signal → J11/J12; R06 skips one accepted queued dispatch → J10; R07 invokes one accepted occurrence twice → J02; R08 retries respond after a throw → J04; R09 replaces the descriptor-backed lazy facade with an eager shallow spread/property copy → J15-J18.
- M-07-L01 allocates a new stop Promise → L01; L02 moves the shared stopped/token-invalidated transition after outside cleanup → L01/L05/C08/C09/C13/C14; L03 permits one response after stop → L03/L05; L04 permits recursive post-teardown setContext/stage output → L05/L08/L13; L05 recursively emits a nested stage instead of queueing it → L11; L06 keys unsubscribe by callback identity → L09; L07 discards detached accepted queue/publication-in-progress/published-but-unconfirmed entries instead of draining them → L04/C13/C14; L08 retains and iterates the live listener collection instead of the captured snapshot → L10.
- M-07-D01 interpolates a caught value/identifier into a diagnostic → L14; D02 allows onDiagnostic throw to escape → L15.
- M-07-P01 transiently includes the test fixture in package files and is killed only by the tarball exclusion check; P02 removes createSession's direct assertSingleInstance call and is killed only by F7.

This final replacement preserves the exact 30-row/9-catalog distribution: new M-07-C05 replaces the former caller-specific “later setContext publication failure marks stopped late” target, and new M-07-C06 replaces the former caller-specific “connected replay publication failure marks stopped late” target. Those displaced targets were redundant with the shared M-07-L02 mark/token-after-outside-cleanup mutation, whose required detectors now include C08/C09/C13/C14 in addition to L01/L05; failure-emitted accepted-work drainage remains independently covered by M-07-L07 and C13/C14. No prior failure-stop, serialization, generation, or publication-gate behavior loses mutation coverage.

Each source literal must occur exactly once at refresh time. Runtime mutations must build successfully before the intended exact file/case run; P01 must successfully create a tarball containing the forbidden file before the exclusion detector fails. Every row records testsRan or the package-equivalent non-zero detector observation, exact fingerprint, before/after target hash, restored gates, scoped cleanliness, and a revision digest over production source, relevant tests/scripts, manifests, TypeScript/Vitest/build configs, and lockfile while excluding generated evidence and mutable human ledgers. Record immutable baseline hashes for root/package manifests and pnpm-lock.yaml at refresh and require them unchanged in verify all.

Implement self-test, refresh, run range, verify group/all, `verify inputs`, and verify ledgers modes. `verify inputs` is exactly `node scripts/phase-07-mutation-battery.mjs verify inputs` with no additional positional arguments; unknown verify targets or extra arguments write `Usage: node scripts/phase-07-mutation-battery.mjs verify inputs` to stderr and exit 64. At refresh, store lowercase hexadecimal SHA-256 hashes of the raw file bytes, with no newline or encoding normalization, in `inputHashes` keyed by exactly `package.json`, `packages/concierge/package.json`, and `pnpm-lock.yaml`. The mode validates register/evidence digest agreement, the exact three-path schema, file existence, and each recomputed hash without requiring mutation rows to be green. Success writes exactly `PASS: inputs unchanged — 3 files, lockfile byte-identical` to stdout and exits 0. Malformed evidence writes `FAIL: input evidence malformed — <reason>` and drift writes `FAIL: input drift — <path>` to stderr and exits 1; never print file contents. Make verify ledgers invoke the same internal input verifier.

Self-test must use temporary in-memory/on-disk fixtures outside tracked workspace files and reject duplicate/missing/reordered ids, stale/shared digests, a no-op/multi-occurrence mutant, compile-only credit, zero tests, wrong marker, unrestored source, dirty scope, five-mutant/reversed/cross-group ranges, a missing one of the fourteen task rows 07-01-01 through 07-06-03, a missing requirement mapping, stale runtime totals, stub-presence/package-exclusion drift, missing/extra/swapped/renamed input-hash rows, a missing input file, a stale hash, an unknown verify target, and an extra argument. Add explicit register-schema negative controls that remove or reorder C05/C06/C07/C08/C09/R09, assert C05/C06 carry the new targets rather than either displaced stop-order target, and add detector negative controls that substitute C10-C16 or J15-J18 with a neighboring marker. The two replacement identity mutants and four previously added reentrancy/admission/lazy-read mutants cannot be credited by one combined source edit or one generic suite failure. `refresh` writes exactly thirty pending generated rows and no green claim; never hand-edit generated JSON.
  </action>
  <verify>
    <automated>node scripts/phase-07-mutation-battery.mjs self-test &amp;&amp; node scripts/phase-07-mutation-battery.mjs refresh</automated>
    <automated>node -e "const fs=require('node:fs');const r=JSON.parse(fs.readFileSync('.planning/phases/07-session-and-the-transport-seam/07-MUTATION-REGISTER.json','utf8'));const e=JSON.parse(fs.readFileSync('.planning/phases/07-session-and-the-transport-seam/07-MUTATION-EVIDENCE.json','utf8'));if(r.mutants.length!==30||e.rows.length!==30||e.rows.some(x=&gt;x.status!=='pending')||r.registerDigest!==e.registerDigest||Object.keys(e.inputHashes??{}).sort().join('|')!=='package.json|packages/concierge/package.json|pnpm-lock.yaml')process.exit(1)"</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs verify inputs</automated>
    <automated>pnpm check:pack</automated>
    <automated>bash -c '! rg -n "createStubTransport|stub-transport" packages/concierge/src packages/concierge/package.json'</automated>
  </verify>
  <done>The packed consumer exercises the new public seam, the stub exclusion is executable, and one immutable 30-id register owns explicitly pending generated evidence, exact input hashes, and strict counterexample tests.</done>
  <acceptance_criteria>
    - Foreign probe compiles from the installed tarball with exact factory/new-type/six-key/EOPT/Promise-stop usage and runtime import sees callable createSession.
    - Actual tar entries contain no stub/test fixture; source barrel contains no stub export.
    - Register order is exactly C01-C09, R01-R09, L01-L08, D01-D02, P01-P02 and each row names the exact detector above.
    - M-07-C05/C06 contain the actual-published-catalog reconciliation targets and not the displaced caller-specific stop-order targets; M-07-L02 names C08/C09/C13/C14 so the shared stop-first behavior remains mutation-covered.
    - Runtime credit requires build + non-zero intended case + exact RED marker; P01 requires successful forbidden tar creation + exact exclusion failure.
    - All relevant source/test/config/package inputs participate in revision/hash invalidation while generated evidence and human ledgers do not create self-invalidating digests.
    - Self-test passes every stated mutation, new-mutant independence, ledger, CLI-usage, and input-integrity negative control; refresh leaves exactly 30 pending rows with matching digest and exactly three SHA-256 input hashes.
    - `verify inputs` has the exact accepted invocation, usage/exit semantics, success/failure output, path set, hash recomputation, and no-content-disclosure behavior specified above, and verify ledgers reuses it.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 (07-06-02): Execute bounded mutation shards and the full release/package gate</name>
  <read_first>
    - scripts/phase-07-mutation-battery.mjs — pending runner and exact ordered definitions from Task 1.
    - .planning/phases/07-session-and-the-transport-seam/07-MUTATION-REGISTER.json — immutable pending register.
    - .planning/phases/07-session-and-the-transport-seam/07-MUTATION-EVIDENCE.json — pending generated evidence and baseline hashes.
    - packages/concierge/src/session.ts — catalog/routing/lifecycle/diagnostic mutation targets.
    - packages/concierge/src/index.ts — public boundary checked for stub absence.
    - packages/concierge/package.json — unchanged package boundary and transient P01 target.
    - packages/concierge/test/session-catalog.test.ts — C detector markers.
    - packages/concierge/test/session-routing.test.ts — J detector markers.
    - packages/concierge/test/session-lifecycle.test.ts — L detector markers.
    - packages/concierge/test/single-instance.test.ts — F7 detector.
    - scripts/pack-install-check.sh — P01 exclusion detector and foreign package gate.
  </read_first>
  <files>scripts/phase-07-mutation-battery.mjs, .planning/phases/07-session-and-the-transport-seam/07-MUTATION-REGISTER.json, .planning/phases/07-session-and-the-transport-seam/07-MUTATION-EVIDENCE.json</files>
  <action>
As proof for CTX-07-01 through CTX-07-15, begin from freshly pending evidence. Run these exact inclusive contiguous shards independently: C01-C03, C04-C06, C07-C09, R01-R04, R05-R08, R09-R09, L01-L04, L05-L08, D01-D02, and P01-P02. Each shard contains one to four same-group mutants, persists each row atomically, and must remain below the validation ledger's per-shard feedback bound. After each group, run its verifier; after P02 run verify all and require exactly thirty green rows with zero pending/escaped/failed.

Do not hand-edit register/evidence. If a literal is absent/non-unique, a mutant cannot build/package, no intended test runs, a wrong marker fires, a foreign case fails, target hash differs after restore, a restored gate fails, or scoped status differs, correct the definition/test/implementation and rerun the affected bounded shard. A build failure alone never kills a runtime mutant, and a package-script syntax failure never kills P01.

On restored source run a fresh full Vitest JSON report and the exact release gate: pnpm build, pnpm typecheck, pnpm test, pnpm check:artifact, pnpm check:deps, pnpm check:pack, and pnpm check:node-floor. Record exact file/pass/total/pending/todo counts and each command exit in generated evidence. Verify the root/package manifest and lockfile hashes equal Task 1 baselines; require no package install and no fixture tar entry. Leave no applied mutant, generated scratch report, tarball, source/test edit, manifest edit, or lock change behind.
  </action>
  <verify>
    <automated>node scripts/phase-07-mutation-battery.mjs run range M-07-C01 M-07-C03</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs run range M-07-C04 M-07-C06</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs run range M-07-C07 M-07-C09 &amp;&amp; node scripts/phase-07-mutation-battery.mjs verify catalog</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs run range M-07-R01 M-07-R04</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs run range M-07-R05 M-07-R08</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs run range M-07-R09 M-07-R09 &amp;&amp; node scripts/phase-07-mutation-battery.mjs verify routing</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs run range M-07-L01 M-07-L04</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs run range M-07-L05 M-07-L08 &amp;&amp; node scripts/phase-07-mutation-battery.mjs verify lifecycle</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs run range M-07-D01 M-07-D02 &amp;&amp; node scripts/phase-07-mutation-battery.mjs verify diagnostics</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs run range M-07-P01 M-07-P02 &amp;&amp; node scripts/phase-07-mutation-battery.mjs verify all</automated>
    <automated>pnpm build &amp;&amp; pnpm typecheck &amp;&amp; pnpm test &amp;&amp; pnpm check:artifact &amp;&amp; pnpm check:deps &amp;&amp; pnpm check:pack &amp;&amp; pnpm check:node-floor</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs verify inputs</automated>
  </verify>
  <done>All thirty mutants are killed by exact non-vacuous detectors on restored source, every release/package gate is green, and dependency inputs remain byte-identical to their recorded baselines.</done>
  <acceptance_criteria>
    - Evidence contains 9 green catalog, 9 green routing, 8 green lifecycle, 2 green diagnostic, and 2 green package/guard rows with zero other status.
    - Every runtime row compiled and ran its exact named case/marker; P01 packed the forbidden fixture before the exclusion detector failed; P02 ran F7.
    - All targets restore byte-for-byte, all restored focused gates pass, and scoped production/test/package state is clean after every row.
    - Ten bounded shards each select at most four contiguous same-group ids and final verify all proves no gap between shards.
    - Fresh release evidence records live counts with zero pending/todo and all seven commands exit 0.
    - Root/package manifests and pnpm-lock.yaml match baseline hashes exactly and the tarball contains no stub fixture.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3 (07-06-03): Sign complete validation, close SES, and preserve the TRN-02 handoff</name>
  <read_first>
    - .planning/phases/07-session-and-the-transport-seam/07-VALIDATION.md — pending fourteen-row task map, threat index, behavioral matrix, Wave 0 list, and sign-off.
    - .planning/REQUIREMENTS.md — Phase 7 requirement checkboxes and traceability rows.
    - .planning/phases/07-session-and-the-transport-seam/07-MUTATION-REGISTER.json — current exact digest and 30 definitions.
    - .planning/phases/07-session-and-the-transport-seam/07-MUTATION-EVIDENCE.json — thirty green rows, release facts, live totals, and input hashes.
    - scripts/phase-07-mutation-battery.mjs — verify all, verify inputs, and verify ledgers modes.
    - packages/concierge/test/session-catalog.test.ts — C01-C16 evidence.
    - packages/concierge/test/session-routing.test.ts — J01-J18 evidence.
    - packages/concierge/test/session-lifecycle.test.ts — L01-L16 evidence.
    - packages/concierge/test/stub-transport.test.ts — U01-U08 evidence.
    - packages/concierge/test/single-instance.test.ts — F7 direct guard evidence.
    - scripts/pack-install-check.sh — tarball exclusion and foreign consumer evidence.
  </read_first>
  <files>.planning/phases/07-session-and-the-transport-seam/07-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <action>
Per CTX-07-01 through CTX-07-15, run verify all and verify inputs first, then read measured evidence rather than copying the 12-file/252-test baseline. Update every one of the fourteen 07-VALIDATION task rows 07-01-01 through 07-06-03 with exact existing file/case ids, green status, and the command that actually ran. Record the immutable register digest, 9/9/8/2/2 group counts, 30/30 total, zero pending/escape, build/non-zero-detector/restoration guarantees, ten bounded shards, actual final runtime file/pass/total/pending/todo counts, export 69/54/15, F7, foreign consumer, stub tarball absence, dependency zero-byte gate, Node floor, and exact manifest/lock hashes. Mark each Wave 0 file present only after filesystem and named-case evidence agree. Set frontmatter exactly to `status: complete`, `nyquist_compliant: true`, and `wave_0_complete: true`, check every sign-off box only while all live evidence is green, and replace `**Approval:** pending` with `**Approval:** approved YYYY-MM-DD — register <digest>; 30/30 green; release gate green`, using the actual UTC date and evidence digest.

Update only SES-01, SES-02, SES-03, SES-04, and the TRN-02 trace row in REQUIREMENTS.md. Mark SES-01..04 checkboxes complete and replace their pending traceability rows with evidence-rich wording: SES-01 cites C01-C16 plus M-07-C01..C09/M-07-R03..R04; SES-02 cites C11-C16/J01-J06/J15-J18 plus M-07-C05/C06/C09/M-07-R01/R06/R07/R08/R09; SES-03 cites J07-J18 plus M-07-R02..R05/R09; SES-04 cites C07-C14 and L01-L16 plus M-07-C07..C09/M-07-L02/L07/M-07-D02. Do not check TRN-02 and do not label it Complete: its literal requirement includes exercising the full consent kernel, which is locked to Phase 8. Keep the requirement wording unchanged and set its trace row to `Partial — Phase 7 delivers U01-U08 reusable no-network fixture and Session seam/package proof; Phase 8 must reuse this exact fixture to exercise the consent kernel before TRN-02 can be Complete.` Change no other requirement checkbox, status, or wording.

Run verify ledgers last. It must call the internal input verifier, build and parse a fresh full Vitest JSON report, and reject any mismatch among the generated evidence/digest, fourteen task ids, four completed SES requirements plus the pending/Partial TRN-02 handoff, thirty mutation ids/counts, the exact C05/C06 replacement targets and L02 displaced-coverage mapping, named RED markers through C16/J18/L16/U08/F7, Wave 0 file presence, live totals, 69/54/15 export counts, F7, stub exclusion, release exits, and manifest/lock hashes. It must also reject `Approval: pending`, a missing/invalid approval date or digest, any frontmatter value other than complete/true/true, any pending task/Wave 0 row, a checked or Complete TRN-02, or omission of the exact Phase 8 consent-kernel handoff. If any fact differs, correct the ledger or underlying gate and rerun; never weaken the verifier or sign off stale evidence.
  </action>
  <verify>
    <automated>node scripts/phase-07-mutation-battery.mjs verify all &amp;&amp; node scripts/phase-07-mutation-battery.mjs verify inputs</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs verify ledgers</automated>
    <automated>pnpm build &amp;&amp; pnpm typecheck &amp;&amp; pnpm test &amp;&amp; pnpm check:artifact &amp;&amp; pnpm check:deps &amp;&amp; pnpm check:pack &amp;&amp; pnpm check:node-floor</automated>
  </verify>
  <done>Validation is complete from live digest-bound evidence, SES-01..04 are closed, and TRN-02 remains explicitly pending with its exact Phase 8 consent-kernel handoff without changing any other requirement.</done>
  <acceptance_criteria>
    - 07-VALIDATION has exactly fourteen green task rows, thirty green mutation rows/evidence count, current digest, current test totals, all Wave 0 files, complete/true/true frontmatter, and a dated approved marker matching the digest and 30/30 result.
    - All six T-07 threats have a verified mitigation and residual-risk statement; no high-severity threat is accepted or left pending.
    - REQUIREMENTS marks exactly SES-01..04 complete, leaves TRN-02 unchecked with the exact Partial Phase 8 handoff, and changes no other checkbox/status.
    - verify ledgers independently recomputes and validates every task, requirement, mutation, marker, package, export, count, hash, approval, frontmatter flag, and TRN-02 boundary claim, rejecting every pending or inconsistent signoff state.
    - Full release gate remains green after ledger edits; no source/test/manifests/lockfile or historical Phase 1-6 plan is altered.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mutation definition → evidence JSON | A no-op, compile-only, wrong-test, unrestored, or stale-revision mutant can falsely certify a security branch. |
| Source package → packed foreign consumer | Source exports can disagree with built declarations/runtime, or test-only controls can leak into the tarball. |
| Generated evidence → human ledgers | Counts, task ids, requirement citations, and sign-off can drift after code/test changes. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation and verification |
|-----------|----------|----------|-----------|-------------|-----------------------------|
| T-07-01 | high | Elevation of privilege / Tampering | catalog/epoch evidence | mitigate | C01-C16 plus M-07-C01..C09 and M-07-R03/M-07-R04 prove publication/replay/reference authority, serialized latest-wins reentrancy, reconciliation against the catalog actually published, publication-gated admission, failure-stop ordering, and old-work cancellation; verify ledgers requires citations. |
| T-07-02 | high | Tampering / Repudiation | routing cardinality evidence | mitigate | C11-C16/J01-J06/J15-J18 plus M-07-C05/C06/C09/M-07-R01/R06/R07/R08/R09 prove publication-gated FIFO, exactly one dispatch for cancelled and promoted provisional occurrences, one response attempt per returned row while active, and no retry. |
| T-07-03 | high | Spoofing / Repudiation | envelope evidence | mitigate | J07-J18 plus M-07-R02/M-07-R05/M-07-R09 prove arrival context, lazy guarded ids/calls/hook forwarding, signal-only composition, hostile-getter parity, and a real handler join. |
| T-07-04 | high | Tampering / Denial of service | transition/stop/listener evidence | mitigate | C10-C16, L01-L13, M-07-C05..C09, and M-07-L01..L08 prove transition serialization, actual-published identity reconciliation, publication gating, mark/cache ordering, provisional/detached drain, response/stage suppression, tokenized snapshots, and zero post-stop output. |
| T-07-05 | high | Information disclosure / Denial of service | diagnostics/envelope evidence | mitigate | C08/C09/C13/C14, J14-J18, L14-L16, M-07-L02/M-07-L07/M-07-R09, and M-07-D01/M-07-D02 prove stop-before-diagnostic publication failures, failure-emitted-work drainage, dispatcher-contained hostile getters, immutable fixed detail-free messages, and contained sinks. |
| T-07-06 | high | Tampering | package/core/evidence integrity | mitigate | Exact type/export pins, F7/P02, P01 tar exclusion, foreign EOPT consumer, input hashes, revision digest, live ledger verifier, and full release gate are all blocking. |
| T-07-SC | low | Tampering | package supply chain | accept | Package Legitimacy Audit is not applicable because no install occurs; input hashes, check:deps, package manifests, and lockfile prove no supply-chain change. |

## Residual Risk

Evidence is valid only for its recorded revision/input hashes, and a future vendor transport can still misreport its self-declared capabilities. This phase proves the transport-neutral seam and test instrument, not trust in an unimplemented vendor; every Phase 7 high-severity threat is mitigated and mechanically verified.
</threat_model>

<source_coverage_audit>
| Source | ID | Feature or constraint | Plan coverage | Status | Notes |
|--------|----|-----------------------|---------------|--------|-------|
| GOAL | — | Session owns catalog↔transport loop so navigation/reconnect cannot leave stale tools, with no network | 07-02, 07-03, 07-04, 07-05, 07-06 | COVERED | Stub drives serialized latest-wins publication, reconciliation against the last successfully published transport reference, confirmation-gated admission, lazy hostile-envelope routing, and complete teardown against the built runtime; mutation/package proof closes the phase outcome. |
| REQ | SES-01 | Publish current stage catalog on start/change/reconnect | 07-01, 07-03, 07-04, 07-06 | COVERED | Exact lifecycle contract, C01-C16 serialized latest-wins/actual-published reconciliation proof, and M-07-C01..C09 mutants. |
| REQ | SES-02 | Route batch through dispatch and return one result per call | 07-03, 07-04, 07-06 | COVERED | C11-C16/J01-J06/J15-J18, existing total dispatchBatch, and publication-reconciliation/gate/occurrence/lazy-read mutants. |
| REQ | SES-03 | Carry turn identity and delivery hook intact to handler | 07-01, 07-04, 07-06 | COVERED | Exact envelope pins plus J07-J18 lazy facade, hostile-getter parity, real-handler, and signal-composition proof. |
| REQ | SES-04 | Stop unregisters and cancels/drains in-flight work | 07-01, 07-03, 07-05, 07-06 | COVERED | Promise type, transactional startup, publication-in-progress/published-but-unconfirmed batch drain, L01-L16, and lifecycle/diagnostic mutants. |
| REQ | TRN-02 | Configurable no-network stub exercises the full consent kernel | 07-01, 07-02, 07-03, 07-04, 07-05, 07-06, then Phase 8 | COVERED (Phase 7 seam); FINAL PENDING | U01-U08 and Session/package proof deliver the reusable no-I/O seam; consent-kernel exercise is explicitly excluded from Phase 7 and must reuse this fixture in Phase 8 before the requirement is Complete. |
| RESEARCH | contracts | Six-key neutral Transport, four-key Promise Session, exact EOPT config and diagnostics | 07-01, 07-03, 07-06 | COVERED | Source/type/artifact/foreign pins and 69/54/15 count. |
| RESEARCH | architecture | Transactional hot factory with factory-local state, serialized transition drain, and direct single-instance guard | 07-03, 07-05, 07-06 | COVERED | C07/C10-C16, separate publishing/published/confirmed state, L setup/stop matrix, F7/P02. |
| RESEARCH | catalog | Reference identity epoch, explicit no-context empty catalog, connected replay, fixed/dynamic publication fail-close | 07-03, 07-04, 07-06 | COVERED | C01-C16 and M-07-C01..C09 mutants, including actual-published identity reconciliation, latest-wins, and admission confirmation. |
| RESEARCH | routing | Explicit FIFO, accepted occurrence, one attempt, arrival context, lazy envelope facade, signal composition, no duplicated dispatcher policy | 07-03, 07-04, 07-06 | COVERED | C11-C16/J01-J18 and M-07-C05/C06/C09/M-07-R01..R09; same-published B work retains arrival context while later work captures C. |
| RESEARCH | lifecycle | Cached stop before outside code, transition/publication invalidation, provisional/detached drain, independent cleanup, tokenized queued notification | 07-03, 07-05, 07-06 | COVERED | C13/C14, L01-L13, M-07-C07..C09, and M-07-L01..L08; L02 replaces the two redundant caller-specific late-stop mutants. |
| RESEARCH | diagnostics | Closed fresh immutable replacement diagnostics with fixed safe messages | 07-01, 07-03, 07-04, 07-05, 07-06 | COVERED | Publication/getter sentinel-absence cases, type pins, L14-L16, and M-07-D01/D02. |
| RESEARCH | fixture/package | Frozen profiles/controls/histories, no I/O, test-only and tarball excluded | 07-02, 07-06 | COVERED | U01-U08, P01, static/barrel/tar checks. |
| RESEARCH | evidence | Built-dist tests, bounded compiled mutation proof, release gates, live counts and ledgers | 07-03, 07-04, 07-05, 07-06 | COVERED | Exact focused commands, 30-row battery, seven-command release gate, explicit input verifier, and verify ledgers. |
| RESEARCH | packages | No package install; manifests and lockfile unchanged | 07-01, 07-02, 07-06 | COVERED | Accepted low supply-chain risk is proven by recorded input hashes and check:deps. |
| CONTEXT | CTX-07-01 | Required status lifecycle and connected replay | 07-01, 07-03, 07-06 | COVERED | Transport pin plus queued connected transitions, C04/C09-C14, and M-07-C02/C07-C09. |
| CONTEXT | CTX-07-02 | Initial context or one frozen empty catalog/null stage | 07-03, 07-06 | COVERED | C01/C02 + C01 mutant. |
| CONTEXT | CTX-07-03 | Retain context; publish by catalog identity; notify by stage value | 07-03, 07-04, 07-06 | COVERED | C03/C05/C10-C12/C15/C16/J07/J08 plus generation, actual-published-reference, and arrival-context mutants. |
| CONTEXT | CTX-07-04 | Fixed catalog stops before detail-free identity-change failure | 07-03, 07-05, 07-06 | COVERED | C06/C13/L cleanup plus M-07-C04/M-07-L02/L07. |
| CONTEXT | CTX-07-05 | Session-wide complete-batch FIFO | 07-03, 07-04, 07-06 | COVERED | Publication-aware gate C11-C16, J01, and M-07-C05/C06/C09/M-07-R01. |
| CONTEXT | CTX-07-06 | Arrival context/epoch and old-work cancellation | 07-03, 07-04, 07-06 | COVERED | C10-C16/J07-J10 plus M-07-C05/C06/C08/C09/M-07-R02..R04; C15 proves promotion and C16 proves cancellation after actual B publication. |
| CONTEXT | CTX-07-07 | Preserve envelope evidence, compose only signal | 07-04, 07-06 | COVERED | J11-J18 plus M-07-R05/R09; lazy passthrough leaves guarded snapshot ownership in Phase 6. |
| CONTEXT | CTX-07-08 | One dispatch occurrence and one non-retried response attempt per row | 07-03, 07-04, 07-06 | COVERED | C11-C16/J02-J05/J15-J18 plus M-07-C05/C06/C09/M-07-R06..R09. |
| CONTEXT | CTX-07-09 | Hot standalone frozen transactional factory | 07-03, 07-05, 07-06 | COVERED | C01/C07/C10-C16/L06/F7 and factory-local transition/publication/admission state. |
| CONTEXT | CTX-07-10 | Cached Promise stop and complete drain | 07-01, 07-03, 07-05, 07-06 | COVERED | Type pin, C13/C14, L01-L04, M-07-C09, and M-07-L01/L07. |
| CONTEXT | CTX-07-11 | Mark-first independent teardown and no post-stop output | 07-03, 07-05, 07-06 | COVERED | C08/C09/C13/C14, L02-L08, M-07-C09/M-07-L02/L03/L07; L02 is the shared displaced-target detector. |
| CONTEXT | CTX-07-12 | Monotonic tokenized reentrant stage subscribers | 07-05, 07-06 | COVERED | L09-L13 + L04/L05/L06. |
| CONTEXT | CTX-07-13 | Reusable test-only non-exported stub | 07-02, 07-03, 07-06 | COVERED | U08, barrel negative, P01 tar mutant. |
| CONTEXT | CTX-07-14 | Frozen configurable profiles, controls, failures, immutable histories, no I/O | 07-02, 07-06 | COVERED | U01-U08 and static/package evidence. |
| CONTEXT | CTX-07-15 | Narrow immutable safe replacement diagnostics | 07-01, 07-03, 07-04, 07-05, 07-06 | COVERED | C08/C09/C13/C14, J14-J18, L14-L16, and M-07-D01/D02. |
</source_coverage_audit>

<verification>
All thirty exact mutations, all focused runtime/type/artifact tests, the seven-command release gate, foreign packed consumer, tarball exclusion, zero-dependency check, Node floor, baseline input hashes, and live ledger verification must agree on restored source.
</verification>

<success_criteria>
- Thirty of thirty mutation rows are green with exact non-vacuous detectors, restoration, and revision integrity.
- Packed consumers receive the exact factory/types and never receive the stub fixture.
- Every release gate passes with byte-identical dependency inputs and measured current counts.
- Validation is signed complete only from live generated evidence; SES-01..04 close, TRN-02 remains pending for Phase 8 consent proof, and verify ledgers rejects every stale, pending-signoff, or boundary-inconsistent counterexample.
</success_criteria>

<output>
Create .planning/phases/07-session-and-the-transport-seam/07-06-SUMMARY.md when done.
</output>
