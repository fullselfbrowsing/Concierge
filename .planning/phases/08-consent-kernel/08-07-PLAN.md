---
phase: 08-consent-kernel
plan: 07
type: execute
wave: 6
depends_on: [08-06, 08-08]
files_modified:
  - scripts/phase-08-mutation-battery.mjs
  - .planning/phases/08-consent-kernel/08-MUTATION-REGISTER.json
  - .planning/phases/08-consent-kernel/08-MUTATION-EVIDENCE.json
  - packages/concierge/test/fixtures/probe.ts
  - scripts/pack-install-check.sh
  - .planning/phases/08-consent-kernel/08-VALIDATION.md
  - .planning/phases/08-consent-kernel/08-SECURITY.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CON-01, CON-02, CON-03, CON-04, CON-05, CON-06, CON-07, CON-08, CON-09, CON-10, CAT-04, TRN-02, TRN-03, TRN-05, SEC-04]
must_haves:
  truths:
    - "Every Phase 8 security invariant is killed by a compiled mutant and a named, nonzero behavioral detector."
    - "Release evidence is revision-bound, reproducible, and proves the public artifact, foreign-consumer contract, package contents, dependency boundary, and Node floor."
    - "No requirement, decision, threat, or validation row closes before its live evidence is green."
    - "The Phase 7 stub remains absent from the tarball and pnpm-lock.yaml remains byte-identical."
  artifacts:
    - path: scripts/phase-08-mutation-battery.mjs
      provides: "Isolated mutation runner, negative self-tests, release snapshot, and ledger verifier"
    - path: .planning/phases/08-consent-kernel/08-MUTATION-EVIDENCE.json
      provides: "Immutable current-revision compile/detector/restoration evidence"
    - path: .planning/phases/08-consent-kernel/08-VALIDATION.md
      provides: "Complete task, requirement, decision, threat, mutation, and release ledgers"
    - path: .planning/phases/08-consent-kernel/08-SECURITY.md
      provides: "ASVS L1 disposition audit for T-08-01 through T-08-10"
  key_links:
    - from: .planning/phases/08-consent-kernel/08-MUTATION-REGISTER.json
      to: scripts/phase-08-mutation-battery.mjs
      via: "each exact source replacement names one compiled detector and fingerprint"
      pattern: "M-08-"
    - from: .planning/phases/08-consent-kernel/08-MUTATION-EVIDENCE.json
      to: .planning/REQUIREMENTS.md
      via: "ledger verification permits closure only when current-revision evidence and release gates are green"
      pattern: "verify ledgers"
---

<objective>
Produce non-vacuous mutation and release evidence, independently audit every Phase 8 threat, and close traceability only from observed results.

Purpose: Convert the consent kernel's tests and documentation into revision-bound proof that fails when any critical guard is removed.
Output: A 47-mutant battery, immutable evidence, foreign-package proof, completed validation/security ledgers, and evidence-gated requirement closure.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-consent-kernel/08-CONTEXT.md
@.planning/phases/08-consent-kernel/08-RESEARCH.md
@.planning/phases/08-consent-kernel/08-VALIDATION.md
@.planning/phases/08-consent-kernel/08-06-SUMMARY.md
@.planning/phases/08-consent-kernel/08-08-SUMMARY.md
@.planning/phases/07-session-and-the-transport-seam/07-MUTATION-REGISTER.json
@.planning/phases/07-session-and-the-transport-seam/07-MUTATION-EVIDENCE.json
@scripts/phase-07-mutation-battery.mjs
@scripts/pack-install-check.sh

<interfaces>
Retain the Phase 7 battery's disposable-snapshot, exact-literal, revision-digest, fingerprint, restoration, and read-only release-snapshot guarantees. Runtime tests consume built `dist`, so every detector builds first and invokes an exact Vitest file/name pattern; never use `pnpm test -- fragment`. A compile failure is infrastructure failure, never a mutant kill.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define and self-test the exact Phase 8 mutation register</name>
  <files>scripts/phase-08-mutation-battery.mjs, .planning/phases/08-consent-kernel/08-MUTATION-REGISTER.json</files>
  <read_first>scripts/phase-07-mutation-battery.mjs, .planning/phases/07-session-and-the-transport-seam/07-MUTATION-REGISTER.json, .planning/phases/08-consent-kernel/08-VALIDATION.md — authoritative T-08-01..T-08-10 meanings, packages/concierge/test/catalog.test.ts, packages/concierge/test/consent-kernel.test.ts, packages/concierge/test/readback-canonicalization.test.ts, packages/concierge/test/session-consent.test.ts, packages/concierge/test/readme-security.test.ts, README.md — root SEC-04 worked-example mutation target</read_first>
  <action>Adapt—not replace—the Phase 7 isolation model and create exactly 47 ordered pending mutants. G01-G14 retain their coverage of premature/interrupted arming, missing generation/response ownership, late/duplicate callback, same-boundary use, skipped fresh-review invalidation, early/live snapshot, comparator throw/mismatch, recomputed payload, consume-after-handler, and decline/dismiss/reuse re-arming (D-08-01..08). Add G15 targeting the single module-private `achievedGrade !== "none"` predicate used before arming and before handler entry; its named N01/N02 detector uses Plan 08-03's isolated catalog substitution so changing that predicate to accept none makes an omitted/explicit-none policy enter the handler. Map G rows only to canonical T-08-01 (self-approved/forgeable boundary), T-08-02 (review return or partial delivery arms), T-08-03 (payload/state drift), T-08-04 for G15's capability-clipped none elevation, T-08-07 (retry/reentrancy/one-shot), and T-08-10 where a hostile callback is the actual detector. E01-E14 cover trusting receipt claims, omitted bytes/report/attestation `readbackHash` comparisons, non-confirmed or wrong-turn attestation, lone surrogate, non-JSON/non-finite/sparse values, alias/cycle, accessor/toJSON, exotic/proxy, broken hand UTF-8, and wrong JCS order/number form (D-08-12..17); map them to canonical T-08-04 (capability versus achieved evidence) only where grade derivation changes, T-08-05 (canonicalization/receipt/hash), T-08-06 (hash versus human act), and T-08-10 (hostile object execution/leak). C01-C06 retain grade/provenance catalog issues, forged profile provenance, copied declared grade, weak actual capability, and validation-after-effects coverage. Add C07 that replaces the catalog's `max("delivered", policy.minGrade ?? "delivered")` floor with the old permissive none floor; its named catalog/createConcierge detector covers both omitted and explicit `minGrade: "none"` under an absent none/none profile. Map provenance self-approval to T-08-01, the effective-floor/capability-as-proof/dominance defects to T-08-04, and hostile capability reads to T-08-10. O01-O07 respond before outcome, disclose model/args text, retry sink/respond, release after interruption, echo caught data, or call the sink for all-success/make rows mutable (D-08-18..20); map the outcome/narration/retry barrier to T-08-08 and only hostile callback/error leakage to T-08-10. P01-P02 retain fixture export/package isolation under T-08-SC. P03 mutates the client-assertion-is-not-authorization warning in root `README.md`; add P04 that deletes or bypasses the ordered current-server-policy reauthorization of the authenticated principal for the exact action immediately before the effect. Both target root `README.md`—never `packages/concierge/README.md`—and map to canonical T-08-09, completing D-08-21..23.

Give every register row explicit canonical threat references and make register validation reject an unknown ID or a group-to-threat pairing that conflicts with 08-VALIDATION. Each row must match one live source literal exactly once, compile successfully, run a named detector selecting at least one test, exit nonzero with its exact `[RED:M-08-…]` fingerprint, restore byte-identically, and record unique original/mutant/revision digests. Self-tests must reject zero-test selectors, build-failure “kills,” wrong fingerprints, duplicate revisions, stale evidence, changed live endpoints, dirty restoration, release-input mutation, missing threat mappings, and conflicting canonical mappings. No test source may be mutated.</action>
  <acceptance_criteria>The register has exactly 15 G, 14 E, 7 C, 7 O, and 4 P rows; every locked decision and canonical T-08-01..10 has a correctly classified detector; C07 removes the effective delivered floor, G15 removes the independent runtime none guard, and P03/P04 target the root README warning/current-policy authorization respectively; all negative harness controls fail closed; protected package manifests and pnpm-lock.yaml have recorded input hashes.</acceptance_criteria>
  <verify><automated>node scripts/phase-08-mutation-battery.mjs self-test &amp;&amp; node scripts/phase-08-mutation-battery.mjs refresh &amp;&amp; node scripts/phase-08-mutation-battery.mjs verify inputs</automated></verify>
  <done>A non-vacuous, exact 47-row pending register is valid against the current clean source revision.</done>
</task>

<task type="auto">
  <name>Task 2: Run bounded mutants and capture the full release/package proof</name>
  <files>scripts/phase-08-mutation-battery.mjs, .planning/phases/08-consent-kernel/08-MUTATION-EVIDENCE.json, packages/concierge/test/fixtures/probe.ts, scripts/pack-install-check.sh</files>
  <read_first>scripts/phase-08-mutation-battery.mjs, .planning/phases/08-consent-kernel/08-MUTATION-REGISTER.json, packages/concierge/test/fixtures/probe.ts, scripts/pack-install-check.sh, packages/concierge/package.json, package.json</read_first>
  <action>Run disposable mutation shards with at most four concurrent workers and regenerate evidence only after all 47 mutants compile, execute their nonzero named detectors, are killed, and restore exactly. Evidence rows must retain commands, selected-test counts, exits, fingerprints, target hashes, and infrastructure status; any compile failure, zero selector, missing fingerprint, timeout, or restoration drift leaves the row red. Update the foreign-consumer probe and pack gate for the new public consent/readback/outcome types and required SessionConfig outcome sink, while asserting the exact six-key Transport, callable artifact, no test/fixture/stub tar entries, and runtime import. From one immutable read-only snapshot run `pnpm build`, `pnpm typecheck`, `pnpm test`, `pnpm check:artifact`, `pnpm check:deps`, `pnpm check:pack`, and `pnpm check:node-floor`; record commands, exits, counts, artifact surface, dependency bytes, tar listing, foreign type/runtime result, and Node version. Bracket gates with digests, require live endpoints clean, and prove pnpm-lock.yaml equals its pre-Phase-8 bytes.</action>
  <acceptance_criteria>Evidence is 47/47 green at one current revision; every mutant compiled and ran a real named failing detector, including C07, G15, and P04; seven release gates pass; the foreign consumer compiles/runs; the stub is absent from source exports and tarball; lockfile and protected inputs are unchanged.</acceptance_criteria>
  <verify><automated>node scripts/phase-08-mutation-battery.mjs run all --jobs 4 &amp;&amp; node scripts/phase-08-mutation-battery.mjs verify all &amp;&amp; node scripts/phase-08-mutation-battery.mjs verify inputs</automated></verify>
  <done>Current immutable evidence binds all mutation, release, dependency, packaging, foreign-consumer, and restoration facts.</done>
</task>

<task type="auto">
  <name>Task 3: Audit security and close validation/requirements from evidence</name>
  <files>.planning/phases/08-consent-kernel/08-VALIDATION.md, .planning/phases/08-consent-kernel/08-SECURITY.md, .planning/REQUIREMENTS.md, scripts/phase-08-mutation-battery.mjs</files>
  <read_first>.planning/phases/08-consent-kernel/08-VALIDATION.md, .planning/phases/08-consent-kernel/08-MUTATION-EVIDENCE.json, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/08-consent-kernel/08-CONTEXT.md, README.md, .planning/phases/08-consent-kernel/08-01-SUMMARY.md, .planning/phases/08-consent-kernel/08-02-SUMMARY.md, .planning/phases/08-consent-kernel/08-03-SUMMARY.md, .planning/phases/08-consent-kernel/08-04-SUMMARY.md, .planning/phases/08-consent-kernel/08-05-SUMMARY.md, .planning/phases/08-consent-kernel/08-06-SUMMARY.md, .planning/phases/08-consent-kernel/08-08-SUMMARY.md</read_first>
  <action>Replace projections in 08-VALIDATION with observed commands/counts/digests and remap its original rows: contracts→08-01; catalog/profile→08-02; ledger→08-03; canonicalization and attestation→08-04; session capability/outcome→08-05; stub/E2E→08-06; root README→08-08; mutation/release/ledgers→08-07. Preserve the validation index's canonical threat meanings verbatim when writing register/evidence/security mappings: T-08-01 self-approved/forgeable boundary; T-08-02 review/partial-delivery arming; T-08-03 payload/state drift; T-08-04 capability-as-achieved-proof, including the inherent delivered floor and runtime none guard; T-08-05 canonicalization/receipt/hash forgery; T-08-06 delivery hash versus human act; T-08-07 retry/reentrancy/one-shot; T-08-08 model rewriting app failure prose; T-08-09 client assertion as server authorization, including exact-action reauthorization of the authenticated principal under current server policy immediately before effect; T-08-10 hostile callback/object escape or secret leak. Add complete GOAL/REQ/RESEARCH/CONTEXT coverage tables mapping CON-01..10, CAT-04, TRN-02, TRN-03, TRN-05, SEC-04; D-08-01..23; T-08-01..10; and every research constraint to a summary, detector, mutant, and release fact. Write 08-SECURITY as a fresh ASVS L1 audit: inspect live controls and independent negative evidence, close every high threat only as mitigated, record residuals honestly, and never inherit a prior disposition. Only after `verify all`, release gates, and the audit are green, mark Phase 8 formal IDs complete in REQUIREMENTS; promote TRN-02 from its Phase 7 Partial handoff and add current runtime evidence for TRN-05 without altering unrelated requirements. Make `verify ledgers` reject missing/duplicate mappings, unchecked Phase 8 IDs, premature closure, stale digests, non-mitigated high threats, any D-08 gap, incomplete validation frontmatter, changed lockfile, tarball stub presence, a package-README SEC-04 target, missing C07/G15/P04 evidence, README reauthorization that is absent or not immediately before the effect, or any threat meaning that conflicts with 08-VALIDATION.</action>
  <acceptance_criteria>Validation is complete and revision-bound; all four source classes have zero unplanned items; every D-08 and threat maps to evidence; all high threats are mitigated; only evidenced requirement rows close; TRN-02/TRN-05 cite current runtime proof.</acceptance_criteria>
  <verify><automated>node scripts/phase-08-mutation-battery.mjs verify all &amp;&amp; node scripts/phase-08-mutation-battery.mjs verify inputs &amp;&amp; node scripts/phase-08-mutation-battery.mjs verify ledgers</automated></verify>
  <done>Phase 8 has consistent live requirement, decision, threat, mutation, release, and source-coverage ledgers with no silent gap.</done>
</task>

</tasks>

<source_coverage_audit>
| Source | Items | Coverage |
|--------|-------|----------|
| GOAL | exact readback-gated execution plus user-visible failure outcome | 08-03..07 |
| REQ | CON-01..10, CAT-04, TRN-03, SEC-04; carried TRN-02/TRN-05 | frontmatter + Task 3 ledger |
| RESEARCH | lazy ledger, strict JCS/UTF-8, branded profile, actual-capability dominance, immutable outcome, exact fixture, no dependency | 08-02..07 |
| CONTEXT | D-08-01..08→08-03; 09..11→08-02/05; 12..17→08-04; 18..20→08-05; 21..22→08-06; 23→08-08 | all covered; server implementation/vendor adapter remain excluded as explicitly out of scope |
</source_coverage_audit>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| source/register → evidence | Mutants and detectors can be stale, vacuous, or revision-mismatched. |
| repository → package consumer | Built declarations/runtime and tar contents cross into a foreign install. |
| evidence → planning ledgers | Documentary closure must not outrun observed controls. |

## STRIDE / ASVS L1 Threat Register
| Threat ID | Category | Disposition | Required closing evidence |
|-----------|----------|-------------|---------------------------|
| T-08-01 | Spoofing / Elevation | mitigate | distinct non-empty boundary ids, human-attested provenance, build/session gates, and G/C self-approval mutants |
| T-08-02 | Elevation | mitigate | pending-before-callback plus completed generation/response-owned delivery and G partial/interrupted/late callback mutants |
| T-08-03 | Tampering | mitigate | exact detached payload, late snapshot comparison, destructive mismatch/throw handling, and G drift/identity mutants |
| T-08-04 | Elevation | mitigate | capabilities remain ceilings, every gated action has an inherent delivered floor, achieved none cannot arm or enter, occurrence evidence derives achieved grade, and C07/G15 plus C/E capability-as-proof mutants |
| T-08-05 | Tampering / Spoofing | mitigate | strict JCS/UTF-8, retained-byte fresh digest verification, every hash comparison, and E canonicalization/receipt mutants |
| T-08-06 | Spoofing | mitigate | separate confirmed attestation bound to trustworthy turn/generation/response plus E act/hash mutants |
| T-08-07 | Repudiation / Elevation | mitigate | exact-Promise dedupe, consume-before-handler, no restore, and G retry/reentrancy/one-shot mutants |
| T-08-08 | Information disclosure / Tampering | mitigate | app-authored minimal outcome completes before response, no model text/no retry, and O barrier mutants |
| T-08-09 | Spoofing / Replay | mitigate | guarded root README server challenge lifecycle, immediate current-policy exact-action reauthorization, and root-README P03/P04 mutants |
| T-08-10 | Information disclosure / Tampering | mitigate | descriptor-aware snapshots, contained callbacks, fixed diagnostics, no caught-value echo, and G/E/C/O hostile-boundary mutants |
| T-08-SC | Tampering | mitigate | no installs, zero dependency bytes, protected-input hashes, lock identity, foreign pack gate |
</threat_model>

<verification>
`verify ledgers` is the terminal gate; it must consume current evidence rather than prose and fail on any mutation, release, requirement, decision, threat, package, or source-audit gap.
</verification>

<success_criteria>
- Exactly 47 compiled mutants are killed by named nonzero behavioral detectors and restored byte-identically.
- All seven release gates and the foreign-consumer probe pass from one digest-bracketed snapshot.
- The stub is absent from production exports/tarball and pnpm-lock.yaml is unchanged.
- D-08-01..23, T-08-01..10, all formal Phase 8 IDs, TRN-02, and TRN-05 close only from current evidence.
</success_criteria>

<output>Create `.planning/phases/08-consent-kernel/08-07-SUMMARY.md` when done.</output>
