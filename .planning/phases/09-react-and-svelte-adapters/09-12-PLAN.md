---
phase: 09-react-and-svelte-adapters
plan: 12
type: execute
wave: 8
depends_on: [09-08, 09-09]
files_modified:
  - .planning/phases/09-react-and-svelte-adapters/09-MUTATION-REGISTER.json
  - scripts/phase-09-mutation-battery.mjs
autonomous: true
requirements: [ADP-01, ADP-02, ADP-03, ADP-04, PKG-04]
must_haves:
  truths:
    - "The register contains compiled immutable R1, R2, S1, SSR1, B1, P1, and C1 defects with exact one-occurrence replacements and named semantic killers."
    - "Every mutant starts from the same clean revision snapshot, changes one registered seam, compiles before testing, and never modifies the live tree."
    - "A nonzero exit counts only when positive test/assertion counts and the registered semantic fingerprint prove the intended kill."
    - "Self-test and one preflight run do not write final mutation/release evidence; evidence generation exists only in `run all` for terminal Plan 09-13."
    - "All mutation CLI verbs/subverbs used by terminal workflow/closure are declared and implemented here."
  artifacts:
    - path: ".planning/phases/09-react-and-svelte-adapters/09-MUTATION-REGISTER.json"
      provides: "Exact seven-mutant minimum register and killer metadata"
      contains: "M-09-C1"
    - path: "scripts/phase-09-mutation-battery.mjs"
      provides: "Immutable compile/kill runner, evidence generator, and drift verifier"
      contains: "verify all"
  key_links:
    - from: "scripts/phase-09-mutation-battery.mjs"
      to: ".planning/phases/09-react-and-svelte-adapters/09-MUTATION-REGISTER.json"
      via: "schema/exact-replacement/compile/killer execution"
      pattern: "M-09-(R1|R2|S1|SSR1|B1|P1|C1)"
    - from: ".planning/phases/09-react-and-svelte-adapters/09-MUTATION-REGISTER.json"
      to: "scripts/phase-09-package-check.mjs|scripts/phase-09-adapter-budget.mjs|adapter/Astro tests"
      via: "named positive-count semantic killer per mutant"
      pattern: "assertionFingerprint"
---

<objective>
Build the exact immutable mutation machinery after all behavioral/package/budget seams exist, without generating final evidence before docs and workflows finish.

Purpose: T-09-08 requires compiled, assertion-observed deliberate defects; a generic failure or final evidence generated before later edits is not proof.
Output: Seven-mutant minimum register and a self-tested/preflighted runner ready for terminal `run all`.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md
@.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md
@.planning/phases/09-react-and-svelte-adapters/09-VALIDATION.md
@.planning/phases/09-react-and-svelte-adapters/09-08-SUMMARY.md
@.planning/phases/09-react-and-svelte-adapters/09-09-SUMMARY.md
@.planning/phases/08-consent-kernel/08-MUTATION-REGISTER.json
@scripts/phase-08-mutation-battery.mjs
@packages/concierge-react/src/client.tsx
@packages/concierge-svelte/src/client.svelte.ts
@examples/adapter-ssr/test/ssr.test.ts
@scripts/phase-09-package-check.mjs
@scripts/phase-09-adapter-budget.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 09-12-01: Register exact compiled R1/R2/S1/SSR1/B1/P1/C1 defects and semantic killers</name>
  <files>.planning/phases/09-react-and-svelte-adapters/09-MUTATION-REGISTER.json</files>
  <read_first>
    <file>.planning/phases/09-react-and-svelte-adapters/09-VALIDATION.md</file>
    <file>.planning/phases/08-consent-kernel/08-MUTATION-REGISTER.json</file>
    <file>packages/concierge-react/src/client.tsx</file>
    <file>packages/concierge-react/test/lifecycle.test.tsx</file>
    <file>packages/concierge-svelte/src/client.svelte.ts</file>
    <file>scripts/phase-09-package-check.mjs</file>
    <file>scripts/phase-09-adapter-budget.mjs</file>
  </read_first>
  <behavior>
    - "Each required ID has one target, exactBefore, exactAfter, expected occurrence count 1, compile command, named killer, expected fingerprint, and T01–T08/requirement mapping."
    - "R1 replaces exact React cleanup; R2 closes over initial React value; S1 returns identity; SSR1 registers during render; B1 omits a source; P1 makes core ordinary/bundled; C1 tautologizes/removes literal mismatch."
    - "Every replacement remains syntactically/type/build compilable so only its semantic detector can kill it."
  </behavior>
  <action>
Create a strict JSON schema-versioned register with exactly unique IDs and no dynamic code. Register at least: `M-09-R1`, exact React effect cleanup changed to a compiling no-op/substitute, killed by T01 StrictMode/final-null marker; `M-09-R2`, stable getter changed to close over the initial render value, killed by T02 rerender/direct-read sentinel; `M-09-S1`, exact `$state.snapshot(value)` return changed to `value`, killed by T03 exact packed consent-stale/zero-handler marker; `M-09-SSR1`, a reachable React registration inserted during render before the effect while leaving compilation green, killed by T04 normal Astro/server registry-null marker; `M-09-B1`, one exact budget inventory entry removed while script syntax remains valid, killed by T07 self-test/discovery disagreement; `M-09-P1`, one adapter manifest moves core from peer-only to ordinary runtime dependency while remaining valid JSON/buildable, killed by T05 exact tar graph/realpath/content marker; `M-09-C1`, the adapter-owned literal comparison becomes tautological/bypassed while core remains unchanged, killed by T05 mismatch diagnostic/register-zero marker.

Every row records exact one-occurrence before/after strings from the final files, affected package, compile command, one focused Vitest/package/budget killer command, expected positive file/test/assertion counts, expected nonzero exit, and assertion fingerprint. Map R1→T01/ADP-01, R2→T02/ADP-01, S1→T03/ADP-02, SSR1→T04/ADP-04, B1→T07/ADP-03, P1/C1→T05/PKG-04; the runner itself supplies T08. Do not map packaging transforms to ADP-04 or swap D-09-15 (fresh-process SSR) with D-09-16 (exact tarballs).
  </action>
  <verify>
    <automated>node -e 'const fs=require("node:fs");const j=JSON.parse(fs.readFileSync(".planning/phases/09-react-and-svelte-adapters/09-MUTATION-REGISTER.json","utf8"));const ids=j.rows.map(r=&gt;r.id),need=["M-09-R1","M-09-R2","M-09-S1","M-09-SSR1","M-09-B1","M-09-P1","M-09-C1"];if(new Set(ids).size!==ids.length||need.some(x=&gt;!ids.includes(x)))process.exit(1);for(const r of j.rows)if(r.occurrences!==1||!r.compileCommand||!r.killerCommand||!r.assertionFingerprint)process.exit(1)'</automated>
    <automated>node -e 'const fs=require("node:fs");const j=JSON.parse(fs.readFileSync(".planning/phases/09-react-and-svelte-adapters/09-MUTATION-REGISTER.json","utf8"));for(const r of j.rows){const s=fs.readFileSync(r.target,"utf8"),n=s.split(r.exactBefore).length-1;if(n!==1)process.exit(1)}'</automated>
  </verify>
  <done>All seven minimum defects are exact, one-occurrence, compilable, and mapped to the canonical T01/T02/T03/T04/T05/T07 meanings plus named semantic killers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09-12-02: Implement immutable compilation/kill orchestration without early evidence generation</name>
  <files>scripts/phase-09-mutation-battery.mjs</files>
  <read_first>
    <file>.planning/phases/09-react-and-svelte-adapters/09-MUTATION-REGISTER.json</file>
    <file>scripts/phase-08-mutation-battery.mjs</file>
    <file>.planning/phases/08-consent-kernel/08-MUTATION-EVIDENCE.json</file>
    <file>scripts/phase-09-package-check.mjs</file>
    <file>scripts/phase-09-adapter-budget.mjs</file>
    <file>vitest.config.ts</file>
  </read_first>
  <behavior>
    - "CLI accepts only `self-test`, `preflight <registered-id>`, `run all --jobs <1-4>`, `verify evidence`, `verify release`, and `verify all`."
    - "Self-test/preflight use disposable trees and never create/update Phase 09 evidence files."
    - "Each mutant copies one immutable baseline, applies one exact replacement, compiles, runs one named positive-count detector, proves semantic kill, and restores/discards the copy."
    - "Compile failure, missing/ambiguous replacement, timeout, zero tests/assertions, unrelated nonzero, survivor, dirty live tree, or stale hashes are infrastructure failures."
    - "`run all` is the only evidence/ledger-generating command and refuses to run until all final release input paths exist."
  </behavior>
  <action>
Adapt the proven Phase 8 immutable architecture without changing Phase 8 files. Parse only the declared CLI forms above. Snapshot a clean release-input manifest into a validated temporary root; include all adapter/example source, manifests/configs/tests, root package/lock/Vitest, package/docs/workflows, Phase 09 scripts/register, and exact prior-evidence digests. For every mutant make a fresh copy from that one baseline, require exact occurrence one, apply only registered replacement, hash before/after/diff, run the compile command successfully, then run the named killer. Parse Vitest/package JSON/markers to require positive file/test/assertion counts and the exact semantic fingerprint; generic exit 1 never counts. Bound jobs 1–4 and subprocess time/output; never write the live tree.

`self-test` must synthesize ambiguous/missing needles, compile failure, timeout, zero tests, unrelated failure, survivor, stale input/register/test digest, duplicate ID, and live-tree-write attempts and require rejection. `preflight <id>` executes one real mutant through compile/kill/restoration but discards results. `run all --jobs N` is the sole command allowed to atomically create/update `09-MUTATION-EVIDENCE.json`, `09-RELEASE-EVIDENCE.json`, `09-VALIDATION.md`, and `09-SECURITY.md`; it first requires every final source/manifest/doc/workflow path and runs only in terminal Plan 09-13.

Before any Phase 09 gate, `run all` hashes the five actual live Phase 8 records `.planning/phases/08-consent-kernel/08-MUTATION-REGISTER.json`, `08-MUTATION-EVIDENCE.json` (whose nested `release` member is the release evidence), `08-VALIDATION.md`, `08-SECURITY.md`, and `08-VERIFICATION.md`. It creates a disposable repository snapshot and runs exactly `node scripts/phase-08-mutation-battery.mjs verify all`, `verify inputs`, and `verify ledgers` inside that snapshot; it records positive exits/output fingerprints and then proves all five live hashes unchanged. Do not invent a separate Phase 8 release record or release-check script. After functional/mutation gates, generate prospective Phase 09 evidence/validation/security in the disposable snapshot, run contract `final` and all read-only verifiers there, then atomically copy the four outputs to live paths only if every check passes. `verify evidence`, `verify release`, and `verify all` are read-only and reject any drift in revision, release input manifest, source/tests/register, exact archive digests, command counts, Phase 8 hashes, or Phase 09 ledgers. Do not generate evidence during this plan.
  </action>
  <verify>
    <automated>node --check scripts/phase-09-mutation-battery.mjs &amp;&amp; node scripts/phase-09-mutation-battery.mjs self-test</automated>
    <automated>node scripts/phase-09-mutation-battery.mjs preflight M-09-R1</automated>
    <automated>test ! -e .planning/phases/09-react-and-svelte-adapters/09-MUTATION-EVIDENCE.json &amp;&amp; test ! -e .planning/phases/09-react-and-svelte-adapters/09-RELEASE-EVIDENCE.json</automated>
  </verify>
  <done>Runner self-tests and one compiled real preflight pass from disposable immutable trees, every terminal CLI form exists, and no final evidence file is generated before docs/workflows finish.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| register literal → disposable source | Ambiguous/stale replacements can mutate the wrong seam. |
| compiler/test runner → kill verdict | Compile/infra/zero-test failures can masquerade as behavioral kills. |
| mutable working tree → baseline | Cross-mutant contamination invalidates independence/restoration. |
| release inputs → evidence | Early or stale evidence can be reused after docs/workflow/source drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-01 | Tampering | R1 lifecycle | mitigate | Exact compiled cleanup mutation and T01 semantic marker. |
| T-09-02 | Tampering | R2 latest state | mitigate | Exact compiled closure mutation and T02 sentinel. |
| T-09-03 | Tampering | S1 normalizer | mitigate | Exact compiled identity mutation and packed T03 oracle. |
| T-09-04 | Information Disclosure | SSR1 | mitigate | Compiled render registration and normal Astro null-registry killer. |
| T-09-05 | Elevation of Privilege | P1/C1 | mitigate | Compiled graph/literal defects and exact package public-path killers. |
| T-09-07 | Tampering | B1 budget | mitigate | Exact inventory omission and independent discovery/control killer. |
| T-09-08 | Repudiation | mutation verdict/evidence | mitigate | Immutable baseline, compile-first, positive assertions, fingerprints, digest binding. |
| T-09-SC | Tampering | mutation dependencies | mitigate | No install or new package; use frozen verified workspace/snapshot dependencies. |
</threat_model>

<verification>
Run only self-test and one R1 preflight here; confirm both final evidence files remain absent. Full seven-mutant execution and release batteries are terminal Plan 09-13 work.
</verification>

<success_criteria>
- Required R1/R2/S1/SSR1/B1/P1/C1 rows are exact and compile-capable.
- One immutable snapshot feeds independent disposable mutants; live tree is never edited.
- Semantic kills require positive assertion evidence and registered fingerprints.
- No final mutation/release evidence exists before workflows and docs are complete.
</success_criteria>

<output>
Create `.planning/phases/09-react-and-svelte-adapters/09-12-SUMMARY.md` when done.
</output>
