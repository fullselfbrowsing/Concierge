---
phase: 09-react-and-svelte-adapters
plan: 05
type: execute
wave: 4
depends_on: [09-03]
files_modified:
  - packages/concierge-svelte/src/index.ts
  - packages/concierge-svelte/src/client.svelte.ts
  - packages/concierge-svelte/test/Harness.svelte
  - packages/concierge-svelte/test/lifecycle.test.ts
  - packages/concierge-svelte/test/artifact.test.ts
autonomous: true
requirements: [ADP-02, PKG-04]
must_haves:
  truths:
    - "Svelte native context carries the exact injected Concierge and fails actionably when absent."
    - "A component-initialization helper registers only inside $effect and returns the exact createBridge().register unsubscriber as teardown."
    - "The exported rune-aware normalizer calls $state.snapshot through the resolved no-cast overload spelling."
    - "Built Svelte output is produced by svelte-package with framework-aware declarations/conditions and remains server-importable without registration."
    - "Every Svelte registration performs singleton and adapter-literal guards immediately before register."
  artifacts:
    - path: "packages/concierge-svelte/src/client.svelte.ts"
      provides: "Native context, effect registration, guards, and real snapshot normalizer"
      contains: "$state.snapshot"
    - path: "packages/concierge-svelte/test/lifecycle.test.ts"
      provides: "Compiled context/effect/mount/remount/teardown evidence"
      contains: "svelte-lifecycle"
    - path: "packages/concierge-svelte/test/artifact.test.ts"
      provides: "svelte-package output, manifest condition, and server-import evidence"
      contains: "client.svelte"
  key_links:
    - from: "packages/concierge-svelte/src/client.svelte.ts"
      to: "@fullselfbrowsing/concierge"
      via: "public types/guards and supplied BridgeRegistry.register"
      pattern: "assertSingleInstance"
    - from: "packages/concierge-svelte/test/Harness.svelte"
      to: "packages/concierge-svelte/src/client.svelte.ts"
      via: "component initialization invokes native context/effect helper"
      pattern: "useConciergeBridge"
---

<objective>
Implement and prove the canonical thin Svelte 5 adapter using native context/effects and the compiler's real `$state.snapshot` intrinsic.

Purpose: ADP-02 depends on framework compilation and lifecycle behavior; source-shaped proxies, manual subscriptions, or tsdown output receive no credit.
Output: Two production entries plus compiled positive-count lifecycle and built-artifact suites.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md
@.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md
@.planning/phases/09-react-and-svelte-adapters/09-03-SUMMARY.md
@packages/concierge/src/index.ts
@packages/concierge/src/types.ts
@packages/concierge/src/bridge.ts
@packages/concierge/src/contract.ts
@packages/concierge-svelte/package.json
@packages/concierge-svelte/svelte.config.js

<interfaces>
Use the live core interfaces and the resolved normalizer spelling:

- `createBridge(id)` returns the existing `BridgeRegistry`; tests/examples construct it outside the adapter.
- `SnapshotNormalizer` is `<T>(value: T) => T`.
- Export `svelteSnapshotNormalizer<T>(value: T): T` as a generic overload with one `unknown -> unknown` implementation returning `$state.snapshot(value)`, plus a compile-time `SnapshotNormalizer` assignment. No assertion/cast is needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 09-05-01: Implement native context, $effect cleanup, guards, and the no-cast rune normalizer</name>
  <files>packages/concierge-svelte/src/index.ts, packages/concierge-svelte/src/client.svelte.ts, packages/concierge-svelte/test/Harness.svelte, packages/concierge-svelte/test/lifecycle.test.ts</files>
  <read_first>
    <file>.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md</file>
    <file>.planning/phases/09-react-and-svelte-adapters/09-RESEARCH.md</file>
    <file>packages/concierge/src/index.ts</file>
    <file>packages/concierge/src/types.ts</file>
    <file>packages/concierge/src/bridge.ts</file>
    <file>packages/concierge-svelte/package.json</file>
  </read_first>
  <behavior>
    - "`provideConcierge`/`useConcierge` use one private context key, preserve reference identity, and throw fixed package remediation when missing."
    - "`useConciergeBridge` called during component initialization creates one `$effect`; its setup guards/registers and its teardown is the exact returned unsubscriber."
    - "Mount, replacement/remount, stale teardown, and destroy produce live/new/null createBridge registry states with no custom subscription loop."
    - "The exported generic overload delegates directly to `$state.snapshot` and compiles as `SnapshotNormalizer` without `as` or `any`."
  </behavior>
  <action>
Write `Harness.svelte` and its named dedicated-project tests first so the real Svelte transform owns `$effect`. In `client.svelte.ts`, export only `provideConcierge`, `useConcierge`, `useConciergeBridge`, and `svelteSnapshotNormalizer`. Use `setContext`/`getContext` with one module-private symbol and exact reference identity; never use a store, `createContext`, a subscription loop, or `createConcierge` per D-09-01 and D-09-09. Implement the normalizer exactly as the resolved research overload: a generic public signature, an `unknown` implementation signature returning `$state.snapshot(value)`, and a compile-only `SnapshotNormalizer` assignment—no cast, identity fallback, structured clone, JSON, or core copier per D-09-10.

`useConciergeBridge` accepts an existing live `BridgeRegistry` (normally public `createBridge`) and ordinary bridge, invokes one `$effect`, and inside setup calls `assertSingleInstance`, compares core `CONTRACT_VERSION` with adapter literal `EXPECTED_CONTRACT_VERSION = 1`, throws fixed `@fullselfbrowsing/concierge-svelte` expected/found/upgrade text on mismatch, calls `registry.register`, and returns that exact function. The harness calls it during component initialization and exposes only test-observable state. Tests mount/destroy/remount through Testing Library Svelte, retain a teardown to exercise stale order, and assert no module/render registration. Mark T03/S1 on the normalizer seam and T04 on lifecycle-only registration; full consent drift is intentionally authoritative only in Plan 09-08's exact packed consumer.
  </action>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge-svelte typecheck</automated>
    <automated>pnpm exec vitest run --project svelte-lifecycle packages/concierge-svelte/test/lifecycle.test.ts --reporter=json | node -e 'let s="";process.stdin.on("data",d=&gt;s+=d).on("end",()=&gt;{const j=JSON.parse(s);if(!j.success||j.numTotalTestSuites&lt;=0||j.numTotalTests&lt;=0||j.testResults.length!==1)process.exit(1)})'</automated>
  </verify>
  <done>Native Svelte context/effect behavior registers and tears down the supplied live registry correctly, and the exported no-cast overload calls the real compiler intrinsic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 09-05-02: Prove svelte-package output, declarations, conditions, and server import safety</name>
  <files>packages/concierge-svelte/test/artifact.test.ts</files>
  <read_first>
    <file>packages/concierge-svelte/src/index.ts</file>
    <file>packages/concierge-svelte/src/client.svelte.ts</file>
    <file>packages/concierge-svelte/package.json</file>
    <file>packages/concierge-svelte/svelte.config.js</file>
    <file>packages/concierge/test/artifact.test.ts</file>
  </read_first>
  <behavior>
    - "Build runs svelte-package and produces nonempty root/client JavaScript plus adjacent declarations at manifest targets."
    - "Root and client export maps contain types/svelte/import/default conditions and no tsdown/bundled core."
    - "Built entries import under Node with browser globals absent and produce zero registration calls at import time."
    - "Built normalizer output retains Svelte compiler/runtime transformation rather than an identity or hand clone."
  </behavior>
  <action>
Add a Node artifact suite that requires a successful `svelte-package` build, verifies every manifest target is a nonempty regular file, inspects output/source maps/declarations for the root and `client.svelte` entries, and rejects private tests, bundled core implementation, tsdown output, missing framework conditions, or an identity/clone replacement. Import both built entries with browser globals absent and a spy registry not registered. Assert package source and built reachability retain the adapter literal guard and real compiled snapshot path. Do not run publint/ATTW here; the authoritative exact `.tgz` inputs are validated directly in Plan 09-08 per D-09-11 and D-09-16.
  </action>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge-svelte build &amp;&amp; pnpm --filter @fullselfbrowsing/concierge-svelte typecheck</automated>
    <automated>pnpm exec vitest run --project node-artifact-ssr packages/concierge-svelte/test/artifact.test.ts --reporter=json | node -e 'let s="";process.stdin.on("data",d=&gt;s+=d).on("end",()=&gt;{const j=JSON.parse(s);if(!j.success||j.numTotalTestSuites&lt;=0||j.numTotalTests&lt;=0||j.testResults.length!==1)process.exit(1)})'</automated>
  </verify>
  <done>The package-local TS6/svelte-package pipeline emits valid framework-aware public artifacts, real compiled snapshot logic, and server-safe imports with zero registration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| component lifecycle → core registry | Registration outside `$effect` leaks authority into SSR or past teardown. |
| rune proxy → consent normalizer | Identity or generic cloning makes reviewed evidence move with live state. |
| source → svelte-package output | Wrong builder/conditions can erase framework transform semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-03 | Tampering | real snapshot normalizer | mitigate | Exact overload/intrinsic seam plus S1 marker and packed consent proof in 09-08. |
| T-09-04 | Information Disclosure | server lifecycle | mitigate | Effect-only registration, import-zero test, and later real Astro SSR. |
| T-09-05 | Elevation of Privilege | guard/core topology | mitigate | Guards immediately before supplied registry register. |
| T-09-06 | Tampering | Svelte compilation | mitigate | svelte-package/local TS6 conditions and artifact inspection. |
| T-09-SC | Tampering | installed packages | mitigate | No new install; use verified locked skeleton dependencies. |
</threat_model>

<verification>
Run both focused command pairs and `node scripts/phase-09-contract-check.mjs baseline-verify`. Reserve exact tarball linting, TS7 declarations, real `$state` consent delivery, and full workspace gates for Plans 09-08/09-13.
</verification>

<success_criteria>
- Svelte production source is exactly `src/index.ts` and `src/client.svelte.ts`.
- Context/effect use native Svelte 5 primitives and exact core cleanup.
- The exported normalizer compiles without a cast and invokes `$state.snapshot` directly.
- svelte-package output and server imports are proven by positive-count suites.
</success_criteria>

<output>
Create `.planning/phases/09-react-and-svelte-adapters/09-05-SUMMARY.md` when done.
</output>
