---
phase: 02-packaging-build-and-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - packages/concierge/LICENSE
  - .gitignore
  - .planning/phases/02-packaging-build-and-release/02-VALIDATION.md
autonomous: true
requirements: [PKG-01]
user_setup: []

must_haves:
  truths:
    - "The repo's compiler is TypeScript 7.0.2 exactly, and the existing type-test program still exits 0 under it"
    - "The bundler, test runner, both artifact gates, the module-graph engine, and changesets are installed and resolvable from the repo root"
    - "A packed tarball left in the working tree cannot break a `git status --porcelain` empty assertion"
    - "`npm pack` and `pnpm pack` produce the same LICENSE-bearing tarball"
    - "The phase validation strategy names real task IDs rather than requirement IDs"
  artifacts:
    - path: package.json
      provides: "TS 7.0.2 exact, pnpm 11.17.0, and the six dev tools this phase runs on"
      contains: '"typescript": "7.0.2"'
    - path: packages/concierge/LICENSE
      provides: "The MIT license as a real file, so npm pack and pnpm pack agree"
      min_lines: 15
    - path: .gitignore
      provides: "Packed tarballs excluded from the working tree"
      contains: "*.tgz"
    - path: .planning/phases/02-packaging-build-and-release/02-VALIDATION.md
      provides: "Per-task verification map with one row per task, and wave_0_complete flipped"
      contains: "wave_0_complete: true"
  key_links:
    - from: package.json
      to: node_modules/.bin
      via: "root devDependencies resolvable under pnpm's strict layout"
      pattern: "\"rolldown\": \"1\\.2\\.0\""
    - from: packages/concierge/package.json
      to: packages/concierge/LICENSE
      via: "files array"
      pattern: "\"LICENSE\""
---

<objective>
Close the four toolchain deltas the ROADMAP records between this repo and the settled stack, and
close the LICENSE defect that is currently masked by an implementation detail of `pnpm pack`.

Purpose: nothing else in Phase 2 can run. There is no bundler, no test runner, no module-graph
engine, no changesets, and the compiler is two majors behind the pin. This plan is pure
toolchain — no behaviour changes, and `pnpm --filter @fullselfbrowsing/concierge typecheck` must
exit 0 before and after every task.
Output: TS 7.0.2 exact, pnpm 11.17.0, six dev tools, `packages/concierge/LICENSE`, a `*.tgz`
gitignore line, and a validation map that names real task IDs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-packaging-build-and-release/02-CONTEXT.md
@.planning/phases/02-packaging-build-and-release/02-VALIDATION.md
@package.json
@packages/concierge/package.json
@.gitignore

Read only these ranges of `.planning/phases/02-packaging-build-and-release/02-RESEARCH.md`:
- lines 96–137 — Standard Stack (the exact version pins) and the Package Legitimacy Audit
- lines 771–782 — Migration mechanics: the ordering hazards table
- lines 808–820 — The LICENSE finding (`pnpm pack` 3 entries vs `npm pack` 2)
- lines 1148–1150 — Open Question 1, pnpm 11's lockfile version (LOW confidence, resolved by doing)

Do not read the whole file. It is 1,248 lines.

Also read `.planning/phases/02-packaging-build-and-release/02-PATTERNS.md` lines 418–444 (root
`package.json` field-order convention and the "do not harmonize `engines.node`" rule) and lines
783–828 (the `.gitignore` audit).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin TypeScript 7.0.2, install the phase's toolchain, and close the LICENSE and *.tgz gaps</name>
  <files>package.json, pnpm-lock.yaml, packages/concierge/LICENSE, .gitignore</files>
  <read_first>
    - `package.json` (root) — the file being modified; note the field order is `packageManager` → `engines` → `scripts` → `devDependencies`
    - `packages/concierge/package.json` — read the `files` array; it already lists `LICENSE`
    - `LICENSE` (repo root) — the file being copied
    - `.gitignore` — the build-artifact block is lines 1–5
    - `02-RESEARCH.md` lines 96–137 (version pins + legitimacy audit) and 808–820 (the LICENSE finding)
    - `02-PATTERNS.md` lines 418–444 and 783–828
  </read_first>
  <action>
    Four changes, one commit.

    **1. TypeScript.** In root `package.json`, change `devDependencies.typescript` from `"^5.7.0"`
    to `"7.0.2"` — **exact, no caret**. The caret is the delta the ROADMAP names; a range would let
    a future install drift off the pin that `isolatedDeclarations` dts timing depends on.

    **2. Install the toolchain** as root dev dependencies at these exact versions:
    `tsdown@0.22.14`, `rolldown@1.2.0`, `vitest@4.1.10`, `publint@0.3.22`,
    `@arethetypeswrong/cli@0.18.5`, `@changesets/cli@2.31.1`. Use one `pnpm add -Dw` invocation so
    the lockfile changes once.

    `rolldown@1.2.0` is **not** in RESEARCH.md's install command and is required anyway: under
    pnpm's strict `node_modules` layout a transitive dependency of `tsdown` is not resolvable from
    a root-level script, and `scripts/pkg05-zero-runtime-deps.mjs` (plan 02-05) does
    `import { rolldown } from "rolldown"` from the repo root. `1.2.0` is the version tsdown itself
    depends on (`~1.2.0`), so this pin adds no second copy. It is row 3 of the Package Legitimacy
    Audit and is Approved.

    All six packages are Approved in `02-RESEARCH.md` § Package Legitimacy Audit — provenance is
    `./CLAUDE.md` (a settled decision document) plus an npm registry check, not a web search. Do
    **not** open a legitimacy checkpoint for them. If `pnpm add` reports a version that no longer
    exists on the registry, stop and report rather than substituting a nearby version.

    **3. `packages/concierge/LICENSE`.** Create it as a byte-for-byte copy of the repo-root
    `LICENSE`. Prefer `cp LICENSE packages/concierge/LICENSE` over retyping. Do **not** edit
    `packages/concierge/package.json`'s `files` array — it already lists `LICENSE`; the file is
    what is missing. Today `pnpm pack` silently copies the workspace-root LICENSE into the tarball
    (3 entries) while `npm pack` does not (2 entries), and `attw --pack` and `publint` both invoke
    `npm pack` internally — so CI's view of the artifact currently differs from the published one.

    **4. `.gitignore`.** Add a single line `*.tgz` to the build-artifact block (immediately after
    `*.tsbuildinfo`, before the blank line). A bare `pnpm pack` with no `--pack-destination` writes
    a ~116 kB tarball into the package directory, and the phase gate asserts
    `git status --porcelain` is empty.

    Do **not** touch `engines.node` at either level. Root `engines.node: ">=22.12.0"` is the
    package's consumer floor; pnpm 11's own `>=22.13` is a contributor requirement. RESEARCH.md is
    explicit that these must not be harmonized.
  </action>
  <verify>
    <automated>pnpm exec tsc --version | grep -qx "Version 7.0.2" && pnpm --filter @fullselfbrowsing/concierge typecheck && node -e "process.exit(require('fs').readFileSync('.gitignore','utf8').split('\n').includes('*.tgz')?0:1)" && cmp LICENSE packages/concierge/LICENSE && echo TOOLCHAIN_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec tsc --version` prints exactly `Version 7.0.2`
    - Root `package.json` `devDependencies.typescript` is the string `7.0.2` with no `^` or `~`
    - Root `package.json` `devDependencies` contains `tsdown` `0.22.14`, `rolldown` `1.2.0`, `vitest` `4.1.10`, `publint` `0.3.22`, `@arethetypeswrong/cli` `0.18.5`, `@changesets/cli` `2.31.1`
    - `node -e "import('rolldown').then(m=>{if(typeof m.rolldown!=='function')process.exit(1)})"` run from the repo root exits 0 — rolldown is resolvable from a root script under pnpm's strict layout
    - `pnpm --filter @fullselfbrowsing/concierge typecheck` exits 0 under TS 7.0.2 with no diagnostics
    - `cmp LICENSE packages/concierge/LICENSE` exits 0 (byte-identical)
    - `packages/concierge/package.json` is unchanged: `git diff --exit-code packages/concierge/package.json` exits 0
    - `.gitignore` contains a line that is exactly `*.tgz`
    - `grep -c '"node": ">=22.13"' package.json` returns 0 — `engines.node` was not harmonized to pnpm's floor
    - `git status --porcelain` lists only the four intended files plus `pnpm-lock.yaml`
  </acceptance_criteria>
  <done>The compiler is 7.0.2 exactly, the six tools are installed and resolvable from the root, the package carries its own LICENSE, and a stray tarball can no longer dirty the tree.</done>
</task>

<task type="auto">
  <name>Task 2: Bump packageManager to pnpm 11.17.0 in a commit that contains nothing else</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - `package.json` (root) — the `packageManager` line, currently `pnpm@10.33.0`
    - `pnpm-lock.yaml` — note its first line, `lockfileVersion: '9.0'`
    - `02-RESEARCH.md` lines 771–782 (ordering hazards) and 1148–1150 (Open Question 1)
    - `02-RESEARCH.md` lines 1090–1092 — Pitfall 12: why 11.0.x is not acceptable
  </read_first>
  <action>
    Change root `package.json` `packageManager` from `"pnpm@10.33.0"` to `"pnpm@11.17.0"`, then run
    `pnpm install --no-frozen-lockfile` and commit **this change and the resulting lockfile churn
    and nothing else**. Do not fold any other edit into this commit; a major package-manager bump's
    lockfile diff is unreviewable when mixed with real changes (RESEARCH Pitfall 15).

    `pnpm@11.1.3` is a hard floor, not a preference: below it `pnpm publish` returns a 404 on the
    PUT when auth relies on OIDC alongside an `.npmrc` written by `actions/setup-node`, *after*
    provenance signing appears to succeed. 11.17.0 is far past it. Do not pin 11.0.x.

    **Record the answer to Open Question 1 in the SUMMARY.** RESEARCH.md flags LOW confidence on
    whether pnpm 11.17.0 writes `lockfileVersion: '9.0'` or a newer format — it was never installed
    in the research session. Read `pnpm-lock.yaml`'s first line after the install and write the
    observed value into the SUMMARY, along with the line count of `git diff --stat pnpm-lock.yaml`.
    That is the whole point of isolating this commit.

    Leave `engines.node` alone at both levels.
  </action>
  <verify>
    <automated>grep -q '"packageManager": "pnpm@11.17.0"' package.json && pnpm --version | grep -qx "11.17.0" && pnpm install --frozen-lockfile && pnpm --filter @fullselfbrowsing/concierge typecheck && echo PNPM11_OK</automated>
  </verify>
  <acceptance_criteria>
    - Root `package.json` contains `"packageManager": "pnpm@11.17.0"`
    - `pnpm --version` prints `11.17.0`
    - `pnpm install --frozen-lockfile` exits 0 — the committed lockfile is consistent with the new package manager
    - `pnpm --filter @fullselfbrowsing/concierge typecheck` still exits 0
    - The commit for this task touches exactly two files: `package.json` and `pnpm-lock.yaml` (`git show --stat --name-only` lists no others)
    - The SUMMARY records the observed `lockfileVersion` value from `pnpm-lock.yaml` line 1 and the `git diff --stat pnpm-lock.yaml` line count
    - `grep -c "pnpm@10" package.json` returns 0
  </acceptance_criteria>
  <done>pnpm is 11.17.0, the lockfile churn from the bump is isolated in one reviewable commit, and Open Question 1 is answered with an observed value rather than an assumption.</done>
</task>

<task type="auto">
  <name>Task 3: Expand the phase validation map to per-task rows and close Wave 0 bookkeeping</name>
  <files>.planning/phases/02-packaging-build-and-release/02-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/02-packaging-build-and-release/02-VALIDATION.md` — in full; it is 184 lines
    - All twelve plan files: `.planning/phases/02-packaging-build-and-release/02-*-PLAN.md`, reading only each file's task-name lines (those indented by exactly two spaces) and its `requirements:` frontmatter field
  </read_first>
  <action>
    `02-VALIDATION.md` § "Per-Task Verification Map" carries a single `*pending*` row and says the
    table is filled in "as part of the first plan in the phase". This is that plan.

    Inventory the tasks with one command:

    `grep -n '^  <name>Task' .planning/phases/02-packaging-build-and-release/02-*-PLAN.md`

    Replace the placeholder row with one row per task. The first column must be the task ID in the
    literal form `02-{plan}-T{n}` — e.g. `02-07-T3` — because the phase-gate plan (02-12) counts
    rows by matching `^| 02-0`. Fill the remaining columns from the plan the task lives in:
    **Plan** = `02-NN`; **Wave** = that plan's `wave` frontmatter value; **Requirement** = the
    requirement ID(s) the task serves; **Threat Ref** = the `T-02-NN` id from that plan's
    `<threat_model>` if one applies, else `—`; **Secure Behavior** = one clause; **Test Type** =
    one of `artifact lint` / `build gate` / `unit` / `integration` / `type` / `mutation` /
    `static review`; **Automated Command** = copied verbatim from that task's
    `<verify><automated>` block; **File Exists** = ✅ or ❌ with the wave that creates it;
    **Status** = `⬜ pending`.

    Then set `wave_0_complete: true` in the frontmatter. **Leave `status: draft` and
    `nyquist_compliant: false` alone** — plan 02-12 flips those at the phase gate, after the
    eleven-mutant battery is the evidence for them.

    Do not edit the § "Suite Adequacy Requirement" table, the § "Wave 0 Requirements" checklist, or
    the § "Manual-Only Verifications" table. Do not edit `02-RESEARCH.md`, `02-CONTEXT.md`,
    `02-PATTERNS.md`, or any plan file.
  </action>
  <verify>
    <automated>grep -q "^wave_0_complete: true" .planning/phases/02-packaging-build-and-release/02-VALIDATION.md && grep -q "^nyquist_compliant: false" .planning/phases/02-packaging-build-and-release/02-VALIDATION.md && test "$(grep -c '^| 02-0' .planning/phases/02-packaging-build-and-release/02-VALIDATION.md)" -ge 31 && test -z "$(grep -F '*pending*' .planning/phases/02-packaging-build-and-release/02-VALIDATION.md)" && echo VALIDATION_MAP_OK</automated>
  </verify>
  <acceptance_criteria>
    - § "Per-Task Verification Map" has at least 31 rows whose first column matches `02-{plan}-T{n}`
    - Every task returned by `grep -n '^  <name>Task' .planning/phases/02-packaging-build-and-release/02-*-PLAN.md` appears in exactly one row
    - `grep -F '*pending*' .planning/phases/02-packaging-build-and-release/02-VALIDATION.md` returns nothing
    - Every row's Automated Command cell is non-empty and matches that task's `<verify><automated>` text
    - Frontmatter reads `wave_0_complete: true`
    - Frontmatter still reads `status: draft` and `nyquist_compliant: false`
    - `git diff --stat` for this task shows changes confined to `02-VALIDATION.md`
  </acceptance_criteria>
  <done>The validation strategy maps to real task IDs with real commands, and Wave 0 is recorded closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo lockfile | Six new dev dependency edges enter the build graph here |
| package manager → published tarball | pnpm and npm disagree about what `files: ["…","LICENSE"]` resolves to |
| working tree → phase gate | An untracked artifact left in the tree defeats a `git status --porcelain` empty assertion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | six new root devDependencies | mitigate | Exact-version pins (no ranges) for all six plus `typescript`; every one is Approved in `02-RESEARCH.md` § Package Legitimacy Audit with provenance = CLAUDE.md + npm registry, not search. `pnpm install --frozen-lockfile` asserted green after the bump. |
| T-02-02 | Tampering | dependency install scripts | mitigate | Record in the SUMMARY whether `pnpm add` reported any ignored build/postinstall scripts. pnpm 11 does not run dependency build scripts without an explicit `onlyBuiltDependencies` allowlist; do not add one. |
| T-02-03 | Spoofing | `packages/concierge/LICENSE` absent while listed in `files` | mitigate | Add the real file. The current state is load-bearing on `pnpm pack` copying the workspace-root LICENSE while `npm pack` does not — so `attw`/`publint` (which shell out to `npm pack`) inspect a different artifact than the one published. |
| T-02-04 | Repudiation | a lockfile diff mixed with functional changes | mitigate | The pnpm 11 bump is its own commit containing exactly `package.json` + `pnpm-lock.yaml`, asserted by `git show --stat`. |
| T-02-05 | Information Disclosure | a stray `*.tgz` in the working tree | mitigate | `.gitignore` entry added in the same plan that first makes packing routine. |
| T-02-SC | Tampering | npm/pnpm installs | mitigate | `02-RESEARCH.md` § Package Legitimacy Audit lists zero `[SLOP]` and zero `[SUS]` packages; every entry is `Approved` against the npm registry. No blocking human checkpoint is required. If any task in this phase proposes a package **not** in that table, stop and run the Package Legitimacy Gate first. |
</threat_model>

<verification>
- `pnpm exec tsc --version` is `Version 7.0.2`
- `pnpm --version` is `11.17.0`
- `pnpm install --frozen-lockfile` exits 0
- `pnpm --filter @fullselfbrowsing/concierge typecheck` exits 0
- `node -e "import('rolldown')"` resolves from the repo root
- `cmp LICENSE packages/concierge/LICENSE` exits 0
- `.gitignore` contains `*.tgz`
- `02-VALIDATION.md` has ≥29 per-task rows and `wave_0_complete: true`
- `git status --porcelain` is empty at the end of the plan
</verification>

<success_criteria>
The four ROADMAP-recorded deltas are closed (TS 7.0.2 exact, pnpm 11.17.0, the toolchain installed,
CI present in the next plans), `packages/concierge` carries its own LICENSE so `npm pack` and
`pnpm pack` agree, and the validation strategy names real task IDs. Nothing about the package's
behaviour changed and the type-test program is green under the new compiler.
</success_criteria>

<output>
Create `.planning/phases/02-packaging-build-and-release/02-01-SUMMARY.md` when done.
</output>
