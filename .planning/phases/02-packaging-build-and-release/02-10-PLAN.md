---
phase: 02-packaging-build-and-release
plan: 10
type: execute
wave: 7
depends_on: ["02-08", "02-09"]
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - .changeset/config.json
  - RELEASING.md
  - CONTRIBUTING.md
  - pnpm-workspace.yaml
  - package.json
autonomous: true
requirements: [PKG-01, PKG-02, PKG-03]
user_setup: []

must_haves:
  truths:
    - "CI runs the typecheck gate before the build, so a type error cannot pass"
    - "The Node floor is checked in a dedicated job that consumes a tarball artifact with npm and node only"
    - "The release path uses OIDC trusted publishing with no long-lived token anywhere"
    - "Changesets parses its config and will not try to version the private fixture packages"
    - "The two build toolchains are written down as invariants, with the failure each prevents"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "build job (typecheck → build → test → checks → pack → upload) and a pinned node-floor job"
      contains: "'22.12.0'"
      min_lines: 40
    - path: .github/workflows/release.yml
      provides: "changesets + npm trusted publishing over OIDC"
      contains: "id-token: write"
      min_lines: 25
    - path: .changeset/config.json
      provides: "Strict-JSON changesets config with an explicit empty ignore list"
      contains: "\"ignore\""
    - path: RELEASING.md
      provides: "The first-publish checklist, including verifying the provenance attestation"
      contains: "attestation"
      min_lines: 20
    - path: CONTRIBUTING.md
      provides: "Two new non-negotiables: svelte-package never tsdown, and the build is not centralized"
      contains: "svelte-package"
  key_links:
    - from: .github/workflows/ci.yml
      to: scripts/pack-install-check.sh
      via: "check:pack step in the build job"
      pattern: "check:pack"
    - from: .github/workflows/release.yml
      to: npm trusted publishing
      via: "id-token permission, no NPM_TOKEN"
      pattern: "id-token: write"
    - from: pnpm-workspace.yaml
      to: "@sveltejs/package"
      via: "catalog pin for the second build toolchain"
      pattern: "@sveltejs/package"
---

<objective>
Wire CI, the release path, and the two written constraints that keep Phase 9 from silently breaking
Svelte reactivity.

Purpose: every gate built in plans 02-03 through 02-09 exists as a local command. CI is what makes
them run on every change, in the order that matters — and one ordering is load-bearing: `typecheck`
**before** `build`, because mutant P4 proved the build is green on a program that does not
typecheck. The release path is where this phase decides the project's supply-chain posture: OIDC
trusted publishing with no long-lived token, and automatic provenance.
Output: `ci.yml`, `release.yml`, `.changeset/config.json`, `RELEASING.md`, two new CONTRIBUTING
non-negotiables, and catalog pins for the second toolchain.

**One honest limit, recorded rather than dressed up.** Nothing publishes until v0.1 completes, so
`release.yml` ships unexecuted. Its verification in this phase is **static review only**. Do not
write an acceptance criterion that implies it ran.

`.github/` and `.changeset/` do not exist. This plan creates both from nothing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-packaging-build-and-release/02-CONTEXT.md
@.planning/phases/02-packaging-build-and-release/02-VALIDATION.md
@CONTRIBUTING.md
@pnpm-workspace.yaml
@package.json

Read only these ranges of `.planning/phases/02-packaging-build-and-release/02-RESEARCH.md`:
- lines 442–493 — the CI shape and why the floor is a separate job rather than a matrix entry
- lines 637–668 — `svelte-package` scaffolding: what "scaffolded" can honestly mean, and the
  recommendation (do 1 and 2, not 3)
- lines 821–898 — changesets + OIDC: what `changeset publish` actually runs, the exact version that
  fixes the 404, the workflow, and the table of what goes wrong if it is wrong
- lines 1087–1101 — Pitfalls 11, 12, 13, 14 and 15
- lines 1119–1143 — the Security Domain table
- lines 1151–1152 — Open Question 2

Also read `.planning/phases/02-packaging-build-and-release/02-PATTERNS.md` lines 636–695 —
patterns 11, 12 and 13, including the ⚠ callout that the repo's JSONC precedent does **not**
transfer to `.changeset/config.json`, and the exact CONTRIBUTING bullet form.

`.planning/phases/02-packaging-build-and-release/02-08-SUMMARY.md` names the two private fixture
package names that task 2 must account for.
</context>

<tasks>

<task type="auto">
  <name>Task 1: ci.yml — a build job and a pinned node-floor job, artifact-passed</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `02-RESEARCH.md` lines 458–493 — the recommended CI shape; treat the YAML there as the source
    - `package.json` (root) — the exact script names to call: `typecheck`, `build`, `test`, `check:artifact`, `check:deps`, `check:pack`
    - `.planning/phases/02-packaging-build-and-release/02-04-SUMMARY.md` — mutant P4's note that a CI running only `build` would be green on that mutant
    - `02-RESEARCH.md` lines 1063–1068 — Pitfalls 3 and 4
  </read_first>
  <action>
    Create `.github/workflows/ci.yml` with two jobs.

    **`build`** — `runs-on: ubuntu-latest`, triggered on push and pull_request:
    `actions/checkout@v5` → `pnpm/action-setup@v4` (it resolves pnpm from `packageManager`, so pass
    no version) → `actions/setup-node@v5` with `node-version: 24` and `cache: pnpm` →
    `pnpm --version` printed and asserted to start with `11.` →
    `pnpm install --frozen-lockfile` → `pnpm typecheck` → `pnpm build` → `pnpm test` →
    `pnpm run check:artifact` → `pnpm run check:deps` → `pnpm run check:pack` →
    `pnpm pack --pack-destination ${{ runner.temp }}` from the package directory →
    `actions/upload-artifact@v4` with the `.tgz`.

    **The `typecheck`-before-`build` order is load-bearing and must carry a comment.** Mutant P4
    demonstrated a program where `pnpm build` exits 0 and `pnpm typecheck` exits non-zero; a
    workflow that runs only `build`, or runs `build` first and stops on its success, is green on
    exactly that mutant. Say so on the line.

    The `pnpm --version` assertion exists because RESEARCH assumption A2 — that
    `pnpm/action-setup@v4` resolves pnpm from `packageManager` without extra config — is an
    assumption, and getting a pnpm below 11.1.3 reintroduces the OIDC 404. Comment it.

    **`node-floor`** — `needs: build`, `runs-on: ubuntu-latest`:
    `actions/download-artifact@v4` → `actions/setup-node@v5` with
    **`node-version: '22.12.0'` — quoted, exact** → a `node -e` step asserting
    `process.version === 'v22.12.0'` and throwing `floor drifted: <version>` otherwise →
    `npm init -y && npm install --no-audit --no-fund ./*.tgz` →
    a `node --input-type=module -e` step importing `@fullselfbrowsing/concierge`, calling
    `assertSingleInstance()`, and throwing unless `MESSAGE_MAX_CHARS === 180`.

    Four comments are mandatory here, each naming a silent-failure mode. **Write every one as a
    standalone line beginning with `#`, never as a trailing inline comment** — the acceptance
    criteria scope their assertions to non-comment lines, and an inline comment would defeat that
    scoping and make the comment self-invalidating:
    - `node-version: '22.12.0'` quoted and exact — `22.12` silently resolves to the newest 22.12.x
      and `22` to the newest 22.x, which is the "developer's newer runtime" the criterion excludes.
    - the `process.version` assertion — what turns the pin into a checked claim.
    - **no `pnpm` anywhere in this job** — `pnpm@11.17.0` refuses to start on Node 22.12.0
      (`requires at least Node.js v22.13`), so the job would fail on tooling rather than on the
      artifact, and the obvious remedy abandons the requirement.
    - **the local equivalent of this job is `scripts/node-floor-check.sh`** (root script
      `pnpm run check:node-floor`, plan 02-09). This job deliberately does **not** call it — the
      script builds and packs with pnpm before switching runtimes, whereas here `setup-node` pins
      the runtime and `download-artifact` supplies the tarball, so the steps genuinely differ. The
      consequence is an asymmetry worth naming: `scripts/node-floor-check.sh` is exercised only
      locally and at plan 02-12's phase gate, so it can rot without CI noticing. The comment is what
      makes the two halves findable from each other. Record the same asymmetry in the SUMMARY.

    A separate job rather than a matrix entry, because the *steps* differ — no pnpm, no build,
    tarball input — not just the Node version. State that in a comment.

    Do **not** add `pnpm run check:node-floor` to the `build` job; the floor is the other job's
    entire purpose, and `check:node-floor` needs a modern runtime to build and pack.
    Do **not** add a publish step here.

    Pin every action by major tag as written above and record the versions used in the SUMMARY —
    RESEARCH assumption A3 is that `@v5` is current for `checkout` and `setup-node`; if the
    workflow's first real run reports an unresolvable action, that assumption is what failed.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q "pnpm typecheck" .github/workflows/ci.yml && grep -q "check:pack" .github/workflows/ci.yml && grep -q "frozen-lockfile" .github/workflows/ci.yml && test "$(grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep -c 'node-version:')" -eq 2 && grep -q "node-version: 24" .github/workflows/ci.yml && grep -q "node-version: '22.12.0'" .github/workflows/ci.yml && test "$(awk '/node-floor:/,0' .github/workflows/ci.yml | grep -v '^[[:space:]]*#' | grep -c pnpm)" -eq 0 && grep -q "scripts/node-floor-check.sh" .github/workflows/ci.yml && echo CI_STATIC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists with exactly two jobs, `build` and `node-floor`, and `node-floor` declares `needs: build`
    - The `build` job runs, in this order: `pnpm install --frozen-lockfile`, `pnpm typecheck`, `pnpm build`, `pnpm test`, `pnpm run check:artifact`, `pnpm run check:deps`, `pnpm run check:pack`
    - `pnpm typecheck` appears **before** `pnpm build` in the file, with a comment naming mutant P4 as the reason
    - The `build` job asserts `pnpm --version` begins with `11.`
    - `grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep 'node-version:'` yields **exactly two** lines: `node-version: 24` in the `build` job and `node-version: '22.12.0'` — quoted — in the `node-floor` job, and nothing else. Stated as an exact two-line inventory rather than as "no `22.12` or bare `22` anywhere": `'22.12.0'` *contains* both of those substrings and the mandated comments quote both as the hazard, so the absence form is unsatisfiable by construction
    - The `node-floor` job contains a `process.version` assertion against `v22.12.0`
    - From the `node-floor:` line to the end of the file, **no executable line** mentions `pnpm`: `awk '/node-floor:/,0' .github/workflows/ci.yml | grep -v '^[[:space:]]*#' | grep -c pnpm` returns 0. Scoped to non-comment lines deliberately — the third mandatory comment in that job *names* pnpm's `>=22.13` floor, and an unfiltered grep would make it self-invalidating
    - The `node-floor` job calls `assertSingleInstance()` and asserts `MESSAGE_MAX_CHARS === 180`
    - The `build` job uploads the packed tarball and `node-floor` downloads it
    - Four standalone `#` comments are present in the `node-floor` job: the quoted-exact-version hazard, the `process.version` assertion, the no-pnpm-on-the-floor finding, and the pointer to `scripts/node-floor-check.sh` as the local equivalent. None is written as a trailing inline comment
    - `grep -q "scripts/node-floor-check.sh" .github/workflows/ci.yml` succeeds
    - The SUMMARY records the action versions used, notes that the workflow's first real execution is its first genuine test, and records the CI/local asymmetry: `pnpm run check:node-floor` is deliberately **not** a CI step, so `scripts/node-floor-check.sh` is exercised only locally and at plan 02-12's phase gate and can rot without CI noticing. (No contradiction with plan 02-09's "CI and local run the same file" — that applies to `check:pack`, which CI *does* call.)
  </acceptance_criteria>
  <done>Every local gate now has a CI step, in the order that makes the typecheck gate meaningful, and the floor is checked in a job that is genuinely pinned and genuinely pnpm-free.</done>
</task>

<task type="auto">
  <name>Task 2: changesets, the OIDC release workflow, and a durable first-publish checklist</name>
  <files>.changeset/config.json, .github/workflows/release.yml, RELEASING.md, package.json</files>
  <read_first>
    - `02-RESEARCH.md` lines 821–898 — what `changeset publish` actually runs in this workspace (`pnpm publish`, not `npm publish`), the pnpm ≥ 11.1.3 requirement with its dated release note, the workflow, and the failure table
    - `02-RESEARCH.md` lines 1087–1095 — Pitfalls 11, 12 and 13
    - `02-PATTERNS.md` lines 655–665 — the ⚠ strict-JSON callout for `.changeset/config.json`
    - `.planning/phases/02-packaging-build-and-release/02-08-SUMMARY.md` — the two private fixture package names
    - `package.json` (root) — the `scripts` block
  </read_first>
  <action>
    **1. `.changeset/config.json` — strict JSON, no comments.** The repo's JSONC precedent does
    **not** transfer: `tsconfig.test-d.json` carries `//` comments because *TypeScript* accepts them;
    `@changesets/config` reads its config with a strict JSON parse and will fail on one. This is the
    single most likely place in this phase to copy the wrong repo pattern.

    Write a standard changesets config with `$schema`, `changelog: "@changesets/cli/changelog"`,
    `commit: false`, `access: "public"`, `baseBranch: "main"`, an **explicit empty
    `"ignore": []`**, and **`"privatePackages": false`**.

    `ignore: []` is explicit and empty deliberately, so Phase 9's first private package has an
    obvious home instead of the discovery that `changeset version` wants to version a package that
    must not publish. `privatePackages: false` is required *now*, because plan 02-08 added two
    private fixture packages to the workspace; without it changesets treats private packages as
    versionable. Since the file cannot carry a comment, put the note about what goes in `ignore` —
    and why `privatePackages` is `false` — in `CONTRIBUTING.md` (task 3) and reference it from
    `RELEASING.md`.

    Verify the file two ways, and **keep the two claims separate** — this is the trap here.

    *Claim 1 — the config is well-formed and says what it must.* Parse the file with `JSON.parse`
    and assert `ignore` is an empty array and `privatePackages` is `false`. This is an exit-code
    assertion and belongs in the `&&` chain.

    *Claim 2 — changesets does not consider the fixtures versionable.* Run
    `pnpm exec changeset status`, **capture its output**, and assert the output names neither
    `@fullselfbrowsing/concierge-fixture-alpha` nor `-beta`. Do **not** put `changeset status` in an
    `&&` chain and do **not** treat its exit code as the signal.

    `changeset status` exits **1** whenever it finds changed packages against `baseBranch: main`
    with no changeset file present. Plans 02-01 through 02-11 all modify `packages/concierge` or the
    root, so on any execution branch it will exit 1 **for a reason that has nothing to do with the
    fixtures** — it is reporting an unreleased change to `@fullselfbrowsing/concierge`. Explicitly
    accept exit 1 when that is its sole cause. Only a *fixture name appearing in the output* is
    evidence that `privatePackages: false` did not take; if that happens, fix the config and do not
    add a placeholder changeset. Record the captured output verbatim in the SUMMARY, along with
    which packages it named and the exit code, so the distinction is on the record rather than
    re-derived.

    **2. `.github/workflows/release.yml`.** Use the workflow at `02-RESEARCH.md:851-883` as the
    source. On push to `main`, with `concurrency` keyed on workflow+ref, and
    `permissions: { contents: write, pull-requests: write, id-token: write }`.
    Steps: `actions/checkout@v5` with **`fetch-depth: 0`** → `pnpm/action-setup@v4` →
    `actions/setup-node@v5` with `node-version: 24`, `registry-url: 'https://registry.npmjs.org'`,
    `cache: pnpm` → `npm install -g npm@latest` → `pnpm install --frozen-lockfile` →
    `pnpm typecheck && pnpm build && pnpm test` → `changesets/action@v1` with
    `version: pnpm changeset version` and `publish: pnpm changeset publish`, and
    `GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}` in `env`.

    Four prohibitions, each with a **standalone `#` comment** naming the symptom — never a trailing
    inline comment, because the acceptance criteria scope their absence assertions to non-comment
    lines and an inline comment would defeat that scoping:
    - **No `NPM_TOKEN` anywhere.** Token auth wins over OIDC and the publish succeeds *without*
      provenance — a silent downgrade, and the only failure in the set that produces a green build
      and a degraded artifact.
    - **No `--provenance` flag.** Provenance attestations are generated automatically for public
      repos on GitHub Actions.
    - **No `auth-token-line: false`.** That input does not exist in `actions/setup-node`'s current
      `action.yml`; it was proposed and closed as a duplicate.
    - **`fetch-depth: 0` is required** — changesets cannot determine what was released otherwise.

    Comment the pnpm requirement where it matters: `changeset publish` shells out to **`pnpm
    publish`** in this workspace (verified by reading `@changesets/cli@2.31.1`'s `getPublishTool`),
    and pnpm below **11.1.3** returns a **404 on the PUT** when auth relies on OIDC alongside the
    `.npmrc` that `actions/setup-node` writes with an unresolved `${NODE_AUTH_TOKEN}` placeholder —
    after provenance signing appears to succeed. `packageManager` pins 11.17.0, far past it.

    **3. Root `release` script.** Add `"release": "changeset publish"`.

    **4. `RELEASING.md`.** A durable first-publish checklist, because the silent-downgrade failure is
    invisible from the workflow's own exit code. It must contain:
    - the version floors and where each comes from: pnpm ≥ 11.1.3, npm ≥ 11.5.1, Node ≥ 22.14.0;
    - the trusted-publisher configuration required on the npm side (repo + workflow binding);
    - **"verify the provenance attestation appears on the npm package page"** as a step that must be
      done by a human on the first real release, with the explicit note that a successful publish
      without an attestation is to be treated as a **failed** publish;
    - the statement that this workflow **has never been executed**, because nothing publishes until
      v0.1 completes, and that its verification to date is static review only;
    - a pointer to `CONTRIBUTING.md` for what goes in `.changeset/config.json`'s `ignore` list.

    **5. Static review, recorded as static review.** Verify by inspection and `grep` — not by
    running — that the workflow satisfies: `permissions.id-token: write` present; **no `NPM_TOKEN`
    is assigned or referenced anywhere under `.github/`**; no `--provenance` on an executable line;
    no `auth-token-line`; `fetch-depth: 0` present; `node-version: 24`. Record each as a static check
    in the SUMMARY. Do not write any statement implying the workflow was executed.

    **Scope the two absence checks; do not widen them back.** The `NPM_TOKEN` check is scoped to
    `.github/`, to non-comment lines, and to *assignment or reference* context
    (`NPM_TOKEN:` / `NPM_TOKEN=` / `secrets.NPM_TOKEN`) for a specific reason: the prohibition itself
    is recorded as a comment in `release.yml` and as prose in `RELEASING.md` and `CONTRIBUTING.md`,
    and a repo-wide bare grep is red the moment those exist. The cheapest way to make a bare grep
    green is to delete the comment — which would erase the **only recorded mitigation for T-02-46
    and T-02-47** from a workflow that never executes in this phase and therefore has no other
    evidence behind it. The same reasoning applies to `--provenance`: the mandated comment names the
    flag, so the assertion is scoped to non-comment lines.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('.changeset/config.json','utf8'))" && node -e "const c=require('./.changeset/config.json');process.exit(Array.isArray(c.ignore)&&c.ignore.length===0&&c.privatePackages===false?0:1)" && { pnpm exec changeset status > /tmp/changeset-status.log 2>&1 || true; } && ! grep -q 'concierge-fixture-' /tmp/changeset-status.log && grep -q "id-token: write" .github/workflows/release.yml && grep -q "fetch-depth: 0" .github/workflows/release.yml && test "$(grep -rIh 'NPM_TOKEN' .github/ | grep -v '^[[:space:]]*#' | grep -cE 'NPM_TOKEN[[:space:]]*[:=]|secrets\.NPM_TOKEN')" -eq 0 && grep -q 'NPM_TOKEN' .github/workflows/release.yml && test "$(grep -v '^[[:space:]]*#' .github/workflows/release.yml | grep -cE -- '--provenance|auth-token-line')" -eq 0 && grep -q "attestation" RELEASING.md && echo RELEASE_STATIC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "JSON.parse(...)"` on `.changeset/config.json` exits 0 — the file is strict JSON with no `//` comments
    - `.changeset/config.json` has `"ignore": []` (explicit and empty) and `"privatePackages": false`
    - `pnpm exec changeset status`'s **captured output** names neither `@fullselfbrowsing/concierge-fixture-alpha` nor `-beta`. Its **exit code is not asserted**: it exits 1 whenever changed packages exist against `baseBranch: main` with no changeset file, which is true on every execution branch in this phase because plans 02-01..02-11 all touch `packages/concierge` or the root. Exit 1 caused solely by an unreleased change to `@fullselfbrowsing/concierge` is accepted and recorded; a fixture name in the output is not
    - The SUMMARY records `changeset status`'s verbatim output, the packages it named, and its exit code — and states that a non-zero exit here is **not** evidence that `privatePackages: false` failed
    - `.github/workflows/release.yml` contains `id-token: write`, `contents: write`, `pull-requests: write` and `fetch-depth: 0`
    - No `NPM_TOKEN` is assigned or referenced under `.github/`: `grep -rIh 'NPM_TOKEN' .github/ | grep -v '^[[:space:]]*#' | grep -cE 'NPM_TOKEN[[:space:]]*[:=]|secrets\.NPM_TOKEN'` returns 0
    - `grep -q 'NPM_TOKEN' .github/workflows/release.yml` **succeeds** — the prohibition is recorded as a comment in the workflow it governs. The two criteria are deliberately opposed: the string must be *present in a comment* and *absent from every assignment*. Scoping the absence check this way is what keeps the executor from deleting the comment to go green, which would erase the only recorded mitigation for T-02-46 and T-02-47 on a workflow that never runs in this phase
    - `.github/workflows/release.yml` has no `--provenance` and no `auth-token-line` on an **executable** line: `grep -v '^[[:space:]]*#' .github/workflows/release.yml | grep -cE -- '--provenance|auth-token-line'` returns 0 — scoped to non-comment lines because the mandated comments name both flags
    - It uses `changesets/action@v1` with `pnpm changeset version` and `pnpm changeset publish`
    - It carries a comment stating that `changeset publish` shells out to `pnpm publish` and that pnpm below 11.1.3 returns a 404 on the PUT under OIDC
    - Root `package.json` `scripts.release` is `changeset publish`
    - `RELEASING.md` contains all five required items, including the attestation-verification step and the explicit statement that the workflow has never been executed
    - The SUMMARY records the six static checks individually and contains **no** statement implying the release workflow ran
  </acceptance_criteria>
  <done>The release path is configured for OIDC trusted publishing with no long-lived credential, changesets parses its config and leaves the private fixtures alone, and the one thing that cannot be verified in this phase is written down as unverified with a checklist for when it can be.</done>
</task>

<task type="auto">
  <name>Task 3: Write down the two build toolchains as invariants, and pin the second one</name>
  <files>pnpm-workspace.yaml, CONTRIBUTING.md</files>
  <read_first>
    - `CONTRIBUTING.md` lines 18–27 — the "Non-negotiables" section; six bullets, each in the form **bold claim → the concrete failure the alternative caused**
    - `CONTRIBUTING.md` line 55 — *"Run `pnpm typecheck && pnpm test` before pushing"*, a sentence that has been false until this phase
    - `pnpm-workspace.yaml` — as edited by plan 02-08; there is still no `catalog:` key
    - `02-RESEARCH.md` lines 637–668 — what "scaffolded" can honestly mean, and why the recommendation is a written rule plus a catalog pin rather than a placeholder package
    - `02-RESEARCH.md` lines 1096–1098 — Pitfall 14
  </read_first>
  <action>
    The ROADMAP says the second build toolchain — `svelte-package`, which cannot pre-bundle runes
    without silently killing reactivity — is *scaffolded here rather than discovered in Phase 9*.
    Research is explicit about what scaffolding can honestly mean: **do the written constraint and
    the catalog pin; do not create a placeholder `concierge-svelte` package.** A placeholder pulls
    `svelte` and `@sveltejs/package` (and `chokidar`, `svelte2tsx`, `semver`, `sade`) into the dev
    graph now and adds a package that must be kept out of publishing for seven phases, while
    preventing the actual risk — someone pointing the wrong builder at it later — no better than a
    written rule does.

    **1. Catalog pins.** Add a `catalog:` key to `pnpm-workspace.yaml` with `svelte: ^5.0.0` and
    `"@sveltejs/package": ^2.5.8`. Keep the `packages:` list exactly as plan 02-08 left it,
    including the fixtures glob and its comment. Do **not** install either package — the catalog
    entry is a pin waiting for Phase 9's consumer, not a dependency.

    **2. Two new CONTRIBUTING non-negotiables**, in the section's existing form (bold claim, then
    the concrete failure the alternative caused):

    - **`concierge-svelte` builds with `svelte-package`, never tsdown.** Runes are
      compiler-transformed; pre-bundling them produces code that runs and is not reactive. No error,
      no warning — the symptom is "the snapshot doesn't update", which is indistinguishable from a
      bridge bug. `@sveltejs/package` transpiles file-by-file and deliberately does not bundle, and
      it requires a `svelte` export condition.
    - **The build is not centralized.** Root scripts stay `pnpm -r build` so each package declares
      its own builder. This looks like duplication and is the structural guard against the bullet
      above: the moment a shared build config exists that every package inherits,
      `concierge-svelte` inherits it in Phase 9.

    Add a third short note in the same section, or immediately after it, recording the divergence
    this phase deliberately introduced: **the test runner *is* centralized** (one root
    `vitest.config.ts`, root `test` script) while the builder is not — one shared runner,
    per-package builders — because the runner has no equivalent hazard.

    **3. Make line 55 true.** `CONTRIBUTING.md` currently instructs contributors to run
    `pnpm typecheck && pnpm test` before pushing, which has been a lie — `pnpm test` exited 0 as a
    silent no-op. It is true now, and it should be extended to `pnpm typecheck && pnpm build &&
    pnpm test`, because `pnpm build` is where `publint` and `attw` run.

    **4. Record what goes in `.changeset/config.json`'s `ignore` list**, since that file is strict
    JSON and cannot carry a comment. One short subsection: `ignore` is deliberately empty; a package
    goes in it when it is a workspace member that must never be published and is not covered by
    `private: true`; `privatePackages: false` is set because the two `test/fixtures/adapter-*`
    packages are private workspace members that changesets would otherwise offer to version.

    **5. Note the pnpm/Node floor distinction**, briefly: contributors need Node ≥ 22.13 because
    pnpm 11 requires it, while the *package* declares `engines.node: ">=22.12.0"` for consumers.
    These are different numbers for different audiences and must not be harmonized.

    Do not restructure `CONTRIBUTING.md`, do not touch its other sections, and do not create
    `packages/concierge-svelte/`.
  </action>
  <verify>
    <automated>grep -q "svelte-package" CONTRIBUTING.md && grep -q "not centralized" CONTRIBUTING.md && grep -q "pnpm typecheck && pnpm build && pnpm test" CONTRIBUTING.md && grep -q "@sveltejs/package" pnpm-workspace.yaml && grep -q "catalog:" pnpm-workspace.yaml && test ! -d packages/concierge-svelte && pnpm install --frozen-lockfile && pnpm typecheck && pnpm build && pnpm test && echo TOOLCHAIN_DOC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm-workspace.yaml` has a `catalog:` key containing `svelte: ^5.0.0` and `"@sveltejs/package": ^2.5.8`
    - Its `packages:` list still contains `packages/*`, `examples/*` and `packages/concierge/test/fixtures/*` with plan 02-08's comment intact
    - Neither `svelte` nor `@sveltejs/package` appears in any `dependencies` or `devDependencies`: `git diff --exit-code pnpm-lock.yaml` exits 0
    - `packages/concierge-svelte/` does not exist
    - `CONTRIBUTING.md` § Non-negotiables contains the two new bullets, each naming the concrete failure the alternative causes
    - `CONTRIBUTING.md` records the deliberate runner-centralized / builder-not-centralized divergence
    - `CONTRIBUTING.md` line about pre-push now reads `pnpm typecheck && pnpm build && pnpm test`
    - `CONTRIBUTING.md` records what belongs in `.changeset/config.json`'s `ignore` list and why `privatePackages` is `false`
    - `CONTRIBUTING.md` records that contributors need Node ≥ 22.13 for pnpm 11 while the package floor stays `>=22.12.0`, and that the two must not be harmonized
    - `pnpm install --frozen-lockfile && pnpm typecheck && pnpm build && pnpm test` all exit 0
  </acceptance_criteria>
  <done>The constraint that would otherwise be discovered in Phase 9 as a silent reactivity bug is written down where a contributor will read it, the second toolchain is version-pinned without being installed, and the pre-push instruction is no longer a lie.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → npm registry | Publish authority crosses here; whatever proves identity is the highest-value target in the repo |
| repository secrets → workflow env | A long-lived token in scope silently outranks OIDC |
| CI green signal → release decision | Gates that do not run, or run in the wrong order, produce a trusted-but-empty signal |
| workspace membership → publish set | Private fixture packages must never become publish candidates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-46 | Spoofing / Elevation | publish-token exfiltration from CI | mitigate | npm trusted publishing over OIDC with `id-token: write`; **no `NPM_TOKEN` anywhere in the repository or in this workflow's env**, asserted by a grep scoped to `.github/`, to non-comment lines, and to assignment/reference context — deliberately narrow so that the comment recording this mitigation cannot be deleted to make the assertion pass. Trusted-publisher configuration binds publish rights to a specific repo and workflow rather than to a transferable secret. |
| T-02-47 | Repudiation | silent provenance downgrade | mitigate | A stray `NPM_TOKEN` makes publish succeed *without* an attestation — green build, degraded artifact, invisible from the exit code. `RELEASING.md` records that a successful publish without an attestation is to be treated as a failed publish, and requires a human to check the npm page on the first release. |
| T-02-48 | Tampering | dependency substitution at install time in CI | mitigate | `pnpm install --frozen-lockfile` in both workflows; `pnpm --version` asserted to be 11.x so the lockfile is read by the package manager it was written by. |
| T-02-49 | Tampering | a type error reaching a published artifact | mitigate | CI runs `pnpm typecheck` before `pnpm build`, with mutant P4 named in a comment as the reason the order is not cosmetic. |
| T-02-50 | Spoofing | a floor job that silently runs on a newer runtime | mitigate | Quoted exact `node-version: '22.12.0'` plus an in-job `process.version` assertion, and no pnpm in that job. |
| T-02-51 | Spoofing | a private fixture package published by `changeset publish` | mitigate | `privatePackages: false` plus `private: true` on both fixtures; `changeset status` asserted not to name either. |
| T-02-52 | Tampering | `.changeset/config.json` written as JSONC by pattern-matching the repo | mitigate | Strict-JSON parse asserted, and the explanatory prose that would have been a comment is relocated to `CONTRIBUTING.md`. |
| T-02-53 | Repudiation | claiming the release path is verified when it has never run | accept | Recorded as manual-only in `02-VALIDATION.md` and restated in `RELEASING.md`. Verification in this phase is static review of six named properties; an acceptance criterion forbids any SUMMARY statement implying execution. |
| T-02-54 | Tampering | third-party GitHub Actions in the publish path | accept | `changesets/action@v1`, `pnpm/action-setup@v4`, `actions/*@v5` pinned by major tag, not by SHA. Residual risk accepted for v0.1 and recorded here; revisit SHA pinning when the first real publish happens. |
| T-02-SC | Tampering | npm/pnpm installs | mitigate | This plan installs nothing. The catalog pins for `svelte` and `@sveltejs/package` are version declarations only; an acceptance criterion asserts `pnpm-lock.yaml` is unchanged. |
</threat_model>

<verification>
- `.github/workflows/ci.yml`: two jobs, `typecheck` before `build`, quoted exact floor version, zero `pnpm` in the floor job
- `.changeset/config.json` parses as strict JSON with `ignore: []` and `privatePackages: false`
- `pnpm exec changeset status`'s captured output names neither fixture package (its exit code is not asserted — see Task 2)
- `.github/workflows/release.yml`: `id-token: write` and `fetch-depth: 0` present; `--provenance` and `auth-token-line` absent from every **non-comment** line
- No `NPM_TOKEN` assignment or `secrets.NPM_TOKEN` reference under `.github/`, while the prohibition itself remains present as a comment in `release.yml`
- `RELEASING.md` records the attestation check and that the workflow has never run
- `CONTRIBUTING.md` carries both new non-negotiables and a true pre-push command
- `pnpm install --frozen-lockfile && pnpm typecheck && pnpm build && pnpm test` all exit 0
</verification>

<success_criteria>
Every gate this phase built runs in CI in the order that makes it meaningful, the floor is checked
on a genuinely pinned runtime by a job that cannot use pnpm, the release path uses OIDC with no
long-lived credential and a written checklist for the one thing that cannot be verified yet, and the
two build toolchains are recorded as invariants with the failure each prevents.
</success_criteria>

<output>
Create `.planning/phases/02-packaging-build-and-release/02-10-SUMMARY.md` when done.
</output>
