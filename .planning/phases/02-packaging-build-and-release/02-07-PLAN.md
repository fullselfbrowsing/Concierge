---
phase: 02-packaging-build-and-release
plan: 07
type: execute
wave: 5
depends_on: ["02-06"]
files_modified:
  - vitest.config.ts
  - package.json
  - packages/concierge/test/single-instance.test.ts
  - packages/concierge/test/artifact.test.ts
  - packages/concierge/test/export-surface.test.ts
autonomous: true
requirements: [PKG-04]
user_setup: []

must_haves:
  truths:
    - "`pnpm test` runs real assertions instead of exiting 0 as a silent no-op"
    - "Two independently-evaluated copies of the built artifact share one registry record"
    - "A consumer bundle that calls assertSingleInstance retains the registry; one that does not, does not"
    - "A contract-version mismatch throws a message naming both versions and the remediation"
    - "The shipped declaration file's export list is pinned by name and by count"
    - "Value exports survive into the built artifact at their expected values"
  artifacts:
    - path: vitest.config.ts
      provides: "One shared node project; typecheck mode deliberately off"
      contains: "projects"
      min_lines: 15
    - path: packages/concierge/test/single-instance.test.ts
      provides: "PKG-04a/b — shared registry, tree-shaking survival, and the mismatch throw"
      contains: "@fullselfbrowsing/concierge.contract"
      min_lines: 40
    - path: packages/concierge/test/export-surface.test.ts
      provides: "Artifact-level export-list guard: 45 names, and the two unexported declarations absent from the list"
      contains: "45"
    - path: packages/concierge/test/artifact.test.ts
      provides: "Value exports survive into dist/index.js at their expected values"
      contains: "MESSAGE_MAX_CHARS"
  key_links:
    - from: packages/concierge/test/single-instance.test.ts
      to: packages/concierge/dist/index.js
      via: "dynamic import of the BUILT artifact, never src/"
      pattern: "\\.\\./dist/index\\.js"
    - from: package.json
      to: vitest.config.ts
      via: "root test script"
      pattern: '"test":\s*"vitest run"'
---

<objective>
Bring the test runner, and write the first runtime tests this repository has ever had.

Purpose: `pnpm test` currently exits **0 with no output** — a silent green test command, which is
exactly what a CI author wires up and trusts. Closing that is not cleanup; it is the difference
between CI meaning something and not. On top of it sit PKG-04's two runtime claims: that two
adapters resolving core independently share one instance, and that a version mismatch fails loudly.
Output: `vitest.config.ts`, a real root `test` script, three test files, and mutants P6, P7 and P11
observed failing.

**One hard structural rule: these tests import `../dist/index.js`, never `../src/`.** The entire
finding behind `assertSingleInstance` is about what survives tree-shaking; a test against `src/`
proves nothing and passes forever.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-packaging-build-and-release/02-VALIDATION.md
@packages/concierge/src/contract.ts
@packages/concierge/src/index.ts
@packages/concierge/package.json
@package.json

Read only these ranges of `.planning/phases/02-packaging-build-and-release/02-RESEARCH.md`:
- lines 152–215 — the tree-shaking measurements this plan turns into assertions (**63 B uncalled /
  587 B called**, i.e. the registry code contributes 0 bytes when uncalled; and the naive
  module-scope form absent from every consumer bundle)
- lines 279–321 — fixtures F1, F2 and F3, and what each is for
- lines 712–735 — why the type-level suite stays in `tsc` and why Phase 2 should *also* assert at
  the artifact
- lines 737–758 — artifact facts: the export list, and the `serverChallengeBrand` /
  `ConsentAckBase` / `ReadbackAttestation` distinctions
- lines 1006–1011 — the P6, P7 and P11 rows
- lines 1081–1085 — Pitfalls 9 and 10

Also read `.planning/phases/02-packaging-build-and-release/02-PATTERNS.md` lines 448–532 (the
`vitest.config.ts` convention and the line that looks wrong and is right) and lines 535–565 (what
transfers from `test-d/` as documentary convention and what does not).

`packages/concierge/test/` does not exist. This plan creates it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vitest config, a real test script, and the single-instance suite</name>
  <files>vitest.config.ts, package.json, packages/concierge/test/single-instance.test.ts</files>
  <read_first>
    - `package.json` (root) — the `scripts` block; `test` is currently `pnpm -r test`
    - `packages/concierge/src/contract.ts` — the registry key string, the record shape, and the thrown message, all of which this suite hard-codes
    - `02-RESEARCH.md` lines 152–215 and 279–321
    - `02-PATTERNS.md` lines 448–532 and 535–565
    - `packages/concierge/test-d/transport.test-d.ts` lines 38–44 — the model for a prose header that says what escapes without this file
  </read_first>
  <action>
    **1. `vitest.config.ts` at the repo root.** A `test.projects` array with a **single** `node`
    project: `environment: "node"`, `include: ["packages/*/test/**/*.test.ts"]`. **Typecheck mode
    stays off.** Comment that line the way `tsconfig.test-d.json` comments TS6059: Vitest 4's
    `typecheck.include` defaults to `**/*.{test,spec}-d.?(c|m)[jt]s?(x)`, which **matches** Phase 1's
    four `test-d/*.test-d.ts` files — files containing no `describe`/`it` and compiled by a tsconfig
    Vitest does not know about. Enabling typecheck mode errors in `startTypechecker` and exits 1.
    Vitest's default `test.include` does **not** match `*.test-d.ts`, which is why the two suites
    coexist safely as long as this stays off. Also state that the type-level suite stays under
    `tsc -p tsconfig.test-d.json` — the ROADMAP fixes this, and it costs ~0.08 s.

    Add a second comment stating the deliberate divergence: **the test runner is centralized at the
    root while the builder is not.** One shared runner, per-package builders. The builder must stay
    per-package so a future `concierge-svelte` cannot be swept into tsdown; the runner has no such
    hazard.

    Add a third comment recording an **accepted, named limitation**: the files under
    `packages/concierge/test/` are in **no TypeScript program**. `tsconfig.json` includes
    `["src/**/*.ts"]` and `tsconfig.test-d.json` includes `["src/**/*.ts", "test-d/**/*.ts"]`;
    neither covers `test/`. Vitest transpiles without typechecking, so a type error in
    `single-instance.test.ts`, `artifact.test.ts`, `export-surface.test.ts` or (plan 02-08's)
    `fixtures.test.ts` is invisible to `pnpm typecheck` and surfaces only as a runtime failure under
    `vitest run`. **This is accepted, not overlooked.** Extending `tsconfig.test-d.json`'s `include`
    to `test/**/*.test.ts` was considered and rejected for three concrete reasons, all of which must
    be written into the comment and the SUMMARY:

    1. These files use `node:fs`, `node:os` and `node:path`. Typechecking them requires
       `@types/node` in the package program, which CONTEXT.md locks out — it pulls DOM-adjacent
       globals and silently defeats the no-DOM guarantee `lib: ["ES2022"]` enforces.
    2. They import `../dist/index.js`, a real on-disk path. Including them would make
       `pnpm typecheck` fail on a clean checkout until `pnpm build` had run — inverting the
       **`typecheck` before `build`** order that mutant P4 exists to justify and that plan 02-10's
       `ci.yml` and plan 02-12's clean-checkout gate both depend on.
    3. `test/fixtures/probe.ts` imports `@fullselfbrowsing/concierge` by bare specifier and is
       compiled by a *foreign* program (plan 02-09's scratch project). Keeping `test/fixtures/` out
       of this repo's own program is a feature, not an omission.

    Do **not** enable Vitest typecheck mode as a substitute — the comment above already records why
    that breaks on `test-d/`. The residual is stated plainly: `vitest run` is the only thing that
    exercises these four files, and it runs in CI.

    **2. Root `test` script.** Change root `package.json`'s `"test"` from `"pnpm -r test"` to
    `"vitest run"`. This is required, not cosmetic: Vitest resolves its config from the working
    directory and does **not** search upward, so `pnpm -r test` running `vitest run` inside
    `packages/concierge` would never find a root config. Do **not** add a `test` script to
    `packages/concierge/package.json`. Leave root `build` (`pnpm -r build`) and `typecheck`
    (`pnpm -r typecheck`) exactly as they are. Never pass `--watch`; `vitest run` is the only form
    used anywhere in this phase.

    **3. `packages/concierge/test/single-instance.test.ts`.** Open with a prose header, in the style
    of `test-d/transport.test-d.ts:38-44`, saying what escapes without this file: that under
    `sideEffects: false` a module-scope duplicate detector is deleted from every bundled consumer
    even when the consumer imports a binding from that module, so the guard would test green under
    Node and do nothing in a real React or Svelte app.

    Import `../dist/index.js` — never `../src/`. Hard-code
    `const KEY = Symbol.for("@fullselfbrowsing/concierge.contract")`. Add a `beforeEach` that
    `delete`s `globalThis[KEY]`; do not define the property non-configurable or the suite cannot
    reset itself. If `packages/concierge/dist/index.js` is missing, fail with a message telling the
    reader to run `pnpm build` first.

    Three assertions:

    **F1a — two independent evaluations share one record.** Import the artifact twice, the second
    time with a `?dup=1` query so Node's module cache produces a genuinely second evaluation, which
    is what two `node_modules` copies produce. Assert the two namespaces are not the same object,
    that `alpha.assertSingleInstance !== beta.assertSingleInstance`, then call both and assert
    `globalThis[KEY]` is a single record equal to `{ version: alpha.CONTRACT_VERSION }`.

    **F1b — the tree-shaking survival assertion. This is the one that makes mutant P6 catchable, and
    it is a deliberate addition to the fixture sketched in RESEARCH.md:285-302.** F1a alone does
    **not** catch P6: research measured that the naive module-scope form *does* survive into
    `dist/index.js`, so under Node it still registers and F1a still passes. What the module-scope
    form does not survive is **bundling**. So bundle two synthetic consumers with rolldown and
    assert on the emitted code:

    - Create a temp directory (`mkdtempSync` under `os.tmpdir()` — outside the repo; a scratch dir
      under `packages/` is swallowed by the workspace glob).
    - Write `calls.mjs`: imports `assertSingleInstance` from the **absolute path** of
      `packages/concierge/dist/index.js` and calls it.
    - Write `uncalled.mjs`: imports only `MESSAGE_MAX_CHARS` from the same absolute path and
      re-exports it.
    - Bundle each with `rolldown({ input, platform: "neutral", onwarn() {} })` and
      `generate({ format: "es" })`, take `output[0].code`.
    - Assert `calls` **contains** the substring `@fullselfbrowsing/concierge.contract`.
    - Assert `uncalled` **does not contain** it — this is simultaneously the honest half of
      `sideEffects: false` and the executable form of the measured claim: **0 bytes of registry code
      when uncalled**. The uncalled bundle is not empty (63 B, for the constant it does import); what
      is zero is the registry's contribution.
    - Remove the temp directory in an `afterEach`/`finally` so a failing assertion does not leak it.

    Record both emitted byte lengths in the SUMMARY beside the measured 63 B uncalled / 587 B
    called. `rolldown` is a root devDependency (installed in plan 02-01) and resolves from this test
    file by ordinary upward `node_modules` walking.

    **F2 — a version mismatch throws with an actionable message.** Seed `globalThis[KEY]` with
    `{ version: 0 }` — exactly what a second, older copy would have written — then import the
    artifact under a fresh query string and assert the call throws matching **both** `/two different
    copies/` **and** `/peerDependency/`. Two separate assertions, because the message being
    *actionable* is a distinct claim from it being *thrown*.

    Name every test after the invariant it guards, following `_assert.ts:9-11` — the test name is
    the only carrier of meaning in the failure output.
  </action>
  <verify>
    <automated>pnpm build && pnpm test -- single-instance && test "$(grep -v '^[[:space:]]*[/*]' packages/concierge/test/single-instance.test.ts | grep -c '\.\./src/')" -eq 0 && grep -q '"test": "vitest run"' package.json && echo SINGLE_INSTANCE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` from the repo root runs Vitest and reports a non-zero number of tests — it is no longer a silent no-op
    - `pnpm test -- single-instance` exits 0 with three passing assertions (F1a, F1b, F2)
    - Root `package.json` `scripts.test` is exactly `vitest run`; `scripts.build` is still `pnpm -r build`
    - `packages/concierge/package.json` has **no** `test` script: `git diff --exit-code packages/concierge/package.json` exits 0
    - `vitest.config.ts` declares one project named `node`, `environment: "node"`, and does **not** enable `typecheck`
    - `grep -c "typecheck" vitest.config.ts` is at least 1 and every occurrence is inside a comment explaining why it is off
    - `vitest.config.ts` carries a comment recording that `packages/concierge/test/**` is in **no** TypeScript program, naming all three reasons the `tsconfig.test-d.json` `include` was deliberately **not** extended (`@types/node` would breach the locked no-DOM constraint; `../dist/index.js` would force `build` before `typecheck`; `test/fixtures/` must stay out of this repo's program), and the SUMMARY repeats them
    - `packages/concierge/tsconfig.test-d.json` and `packages/concierge/tsconfig.json` are unchanged: `git diff --exit-code packages/concierge/tsconfig.test-d.json packages/concierge/tsconfig.json` exits 0
    - No **executable** line references `../src/`: `grep -v '^[[:space:]]*[/*]' packages/concierge/test/single-instance.test.ts | grep -c '\.\./src/'` returns 0 — scoped to non-comment lines so the mandated header prose may name `../src/` as the thing this file must never import
    - The test file contains the literal `Symbol.for("@fullselfbrowsing/concierge.contract")`
    - F1b's two bundles are asserted in opposite directions — one contains the registry key, the other does not — and both byte lengths are recorded in the SUMMARY beside the measured 63 / 587
    - F2 asserts `/two different copies/` and `/peerDependency/` as two separate expectations
    - No temp directory remains: `git status --porcelain` is empty and `os.tmpdir()` cleanup is unconditional
    - No test anywhere in this plan passes `--watch`
  </acceptance_criteria>
  <done>`pnpm test` means something, and PKG-04's runtime claim is asserted against the built artifact including the bundling behaviour that is the whole reason the check sits where it does.</done>
</task>

<task type="auto">
  <name>Task 2: Artifact and export-surface guards, with the vacuous check recorded rather than written</name>
  <files>packages/concierge/test/artifact.test.ts, packages/concierge/test/export-surface.test.ts</files>
  <read_first>
    - `packages/concierge/src/types.ts` lines 182–206 and 348–354 — the four frozen constants and their exact values
    - `packages/concierge/dist/index.d.ts` — the trailing `export { … }` block; count it before writing the number
    - `.planning/phases/02-packaging-build-and-release/02-06-SUMMARY.md` — the recorded post-hinge export count (45) and whether more than one `export {` statement is emitted
    - `02-RESEARCH.md` lines 737–758 — the four artifact facts, including that `serverChallengeBrand` and `ConsentAckBase` are **present in the file as declarations** and absent only from the export list
    - `02-RESEARCH.md` lines 1084–1085 — Pitfall 10, the vacuous `ReadbackAttestation` assertion
    - `packages/concierge/test-d/consent.test-d.ts` lines 146–168 — the model for recording what a check does *not* prove
  </read_first>
  <action>
    Two files. Both import or read the **built artifact**, never `src/`.

    **`packages/concierge/test/artifact.test.ts` — value exports survive into `dist/index.js`.**
    Dynamically import `../dist/index.js` and assert: `MESSAGE_MAX_CHARS` is `180`;
    `CONSENT_GRADE_ORDER` equals `["none", "delivered", "relayed", "attested"]`;
    `Object.isFrozen(USER_CANCELLED)` and `Object.isFrozen(USER_DECLINED)` are both `true`;
    `CONTRACT_VERSION` is `1`; `typeof assertSingleInstance === "function"`.

    Header prose: this catches, at the artifact level, the regression that `verbatimModuleSyntax`
    enforcement is one-directional about. A value misplaced into `index.ts`'s `export type` block
    escapes at exit 0 from the emit build **and** from the existing type-test program, while
    silently erasing the runtime binding from `dist/index.js`. Plan 02-11 adds the type-level guard
    that catches it in 0.08 s during editing; this file catches it at the level where it actually
    harms a consumer. Different sampling rates, same defect — say so.

    **`packages/concierge/test/export-surface.test.ts` — the shipped export list.** A separate file,
    not a block inside `artifact.test.ts`, because `02-VALIDATION.md` names
    `pnpm test -- export-surface` as this guard's command and Vitest filters by filename.

    Read `dist/index.d.ts` as text, extract the names from its trailing `export { … }` (if the build
    emits more than one `export {` statement, union them — plan 02-06's SUMMARY records which is
    real), and assert:

    - the list has exactly **45** names — 39 types + 6 values, the post-`contract.ts` count;
    - it contains all six value names: `USER_CANCELLED`, `USER_DECLINED`, `CONSENT_GRADE_ORDER`,
      `MESSAGE_MAX_CHARS`, `CONTRACT_VERSION`, `assertSingleInstance`;
    - it does **not** contain `serverChallengeBrand` or `ConsentAckBase`.

    Two things must be commented, not merely coded, because both are traps:

    1. `serverChallengeBrand` and `ConsentAckBase` **are present in `dist/index.d.ts` as
       declarations** — rolldown bundles the whole declaration file and only the trailing
       `export { … }` defines the public surface. Assert their absence **from the export list**, not
       from the file; the latter fails on a correct artifact.
    2. **`ReadbackAttestation` has zero occurrences in `types.ts`.** It does not exist. A guard
       asserting it is not exported passes **vacuously**. Record that in a comment, in the style of
       `consent.test-d.ts:150-168`, and **do not write the assertion**. `02-VALIDATION.md` names
       this explicitly: it must not be counted as a passing check.

    This guard is the only check in the repository that would catch an export dropped by a *build
    config* change rather than a source change — say that in the header too.

    Do not import anything from `src/` in either file. Do not use `expectTypeOf` or any Vitest
    type-level API; the type-level suite stays under `tsc`.
  </action>
  <verify>
    <automated>pnpm build && pnpm test -- artifact && pnpm test -- export-surface && test "$(cat packages/concierge/test/artifact.test.ts packages/concierge/test/export-surface.test.ts | grep -v '^[[:space:]]*[/*]' | grep -c '\.\./src/')" -eq 0 && grep -q "ReadbackAttestation" packages/concierge/test/export-surface.test.ts && echo ARTIFACT_GUARDS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- artifact` exits 0
    - `pnpm test -- export-surface` exits 0
    - `export-surface.test.ts` asserts the export-list length is exactly `45`
    - It asserts membership of all six value names by name
    - It asserts `serverChallengeBrand` and `ConsentAckBase` are absent **from the export list**, and a comment states they are present in the file as declarations and that asserting file-absence would fail on a correct artifact
    - It contains a comment recording that `ReadbackAttestation` has 0 occurrences in `types.ts` and that no assertion about it is written because such an assertion passes vacuously
    - `grep -c "expect(.*ReadbackAttestation" packages/concierge/test/export-surface.test.ts` returns 0 — the vacuous assertion is recorded, not written
    - `artifact.test.ts` asserts `MESSAGE_MAX_CHARS === 180`, the four-element `CONSENT_GRADE_ORDER` array, `Object.isFrozen` on both frozen constants, `CONTRACT_VERSION === 1`, and that `assertSingleInstance` is a function
    - Neither file references `../src/` on an executable line: `cat packages/concierge/test/artifact.test.ts packages/concierge/test/export-surface.test.ts | grep -v '^[[:space:]]*[/*]' | grep -c '\.\./src/'` returns 0 — scoped to non-comment lines so the mandated header prose may name it
    - Neither file uses `expectTypeOf`
  </acceptance_criteria>
  <done>The published export list and the surviving runtime values are both pinned at the artifact, and the one assertion that would have passed vacuously is documented instead of written.</done>
</task>

<task type="auto">
  <name>Task 3: Mutants P6, P7 and P11 — prove the runtime suite is a guard (P6 NON-SKIPPABLE)</name>
  <files>packages/concierge/src/contract.ts, packages/concierge/src/index.ts</files>
  <read_first>
    - `packages/concierge/src/contract.ts` — copy the full `export function assertSingleInstance(): void { … }` block verbatim; it is P6's pattern
    - `packages/concierge/src/index.ts` — the value export block ending `  MESSAGE_MAX_CHARS,` then `} from "./types.js";`; that two-line sequence is P11's pattern and occurs once
    - `scripts/mutate-and-prove.sh` — the harness and its exit-code table
    - `02-RESEARCH.md` lines 1006–1011 — the P6, P7 and P11 rows with their measured signatures
    - `.planning/phases/02-packaging-build-and-release/02-VALIDATION.md` § "Suite Adequacy Requirement" — P6 is named non-skippable
  </read_first>
  <action>
    Three `mutate-and-prove.sh` invocations. Each dist-dependent gate must rebuild first, and after
    each proof `dist/` must be rebuilt from restored sources — the harness restores the *source*,
    not the build output. Confirm `pnpm build && pnpm test` is green before starting.

    **P6 — `assertSingleInstance` moved to module scope. NON-SKIPPABLE.**
    - Target `packages/concierge/src/contract.ts`
    - Pattern: the whole `export function assertSingleInstance(): void { … }` declaration, read
      verbatim from the file (multi-line; the harness slurps with `-0`)
    - Replacement: the same registry read/write/throw logic written as **module-scope statements**,
      followed by `export function assertSingleInstance(): void { return; }` so the export still
      exists and the consumer entry still type-resolves
    - Gate: `bash -c 'pnpm build && pnpm test -- single-instance'`
    - Expected: the harness reports `PASS: gate fired`, and **F1b is among the failing assertions** —
      its message shows the `calls.mjs` bundle no longer contains
      `@fullselfbrowsing/concierge.contract`, because `sideEffects: false` licensed rolldown to
      delete the module's evaluation. F1b is the assertion that matters here; it is **not**
      necessarily the only one that fails.
    - **Expect F2 to fail as well, and do not treat that as a surprise.** F2 seeds
      `globalThis[KEY]` with `{ version: 0 }` before importing; under the module-scope mutant the
      registration runs during *module evaluation*, so `await import(...)` rejects before the
      `expect(() => …).toThrow()` ever executes. A second failure from the same cause is expected
      behaviour of the mutant, not a defect in F2.
    - **Record which assertions failed, and which did not.** F1a is expected to still **pass** under
      this mutant: the module-scope form survives into `dist/index.js`, so under Node it registers
      and F1a is blind to the regression. That is precisely why F1b was added, and the SUMMARY must
      say so.
    - **If F1b does *not* fail** — i.e. rolldown retained the module-scope registration — stop.
      That contradicts a measurement the whole design rests on. Record the observed bundle output
      and escalate rather than adjusting the assertion to match.

    **P7 — a contract-version mismatch stops being detected.**
    - Target `packages/concierge/src/contract.ts`
    - Pattern `export const CONTRACT_VERSION = 1;`, replacement `export const CONTRACT_VERSION = 0;`
    - Gate: `bash -c 'pnpm build && pnpm test -- single-instance'`
    - Expected: harness `PASS`; **F2** fails because the seeded prior record `{ version: 0 }` now
      matches the current version, so the adopt branch is taken and nothing throws. This is the
      "bumped in one of two loaded copies" case from the other side, and it proves F2's throw
      assertion is load-bearing rather than incidental.

    **P11 — `MESSAGE_MAX_CHARS` dropped from `index.ts`'s export list.**
    - Target `packages/concierge/src/index.ts`
    - Pattern: the two-line sequence `  MESSAGE_MAX_CHARS,` followed by `} from "./types.js";`
      (it occurs once — the type block's tail ends with `  SessionConfig,`). Both lines contain a
      `/`, so this mutant also re-exercises the harness fix from plan 02-02.
    - Replacement: `} from "./types.js";` alone
    - Gate: `bash -c 'pnpm build && pnpm test -- export-surface'`
    - Expected: harness `PASS`; the guard reports **44 names where 45 are expected**.
      `02-VALIDATION.md`'s measured signature reads *"42 names instead of 43"* because it was
      measured before `contract.ts` added two names; the invariant proven is the same −1 delta.
      Record both numbers in the SUMMARY so the discrepancy is explained rather than discovered.

    After all three: `pnpm build`, then `pnpm typecheck && pnpm test && pnpm run check:deps` all
    green, `git diff --exit-code` clean, `git status --porcelain` empty.

    Record for each mutant: the command line, the harness stdout line and exit code, the name of the
    failing test, and the assertion message.
  </action>
  <verify>
    <automated>bash scripts/mutate-and-prove.sh packages/concierge/src/contract.ts 'export const CONTRACT_VERSION = 1;' 'export const CONTRACT_VERSION = 0;' -- bash -c 'pnpm build && pnpm test -- single-instance'; test $? -eq 0 && git diff --exit-code && pnpm build && pnpm test && echo P7_PROVEN</automated>
  </verify>
  <acceptance_criteria>
    - P6: harness exit 0 with `PASS: gate fired`; **F1b is among the failing assertions**, and its message shows the `calls.mjs` bundle no longer contains `@fullselfbrowsing/concierge.contract`
    - P6: F2 failing alongside F1b is **expected** (module-scope registration throws during module evaluation, so the dynamic import rejects before F2's `toThrow` runs) and is recorded as such rather than reported as an anomaly
    - P6: the SUMMARY records that F1a still passed under this mutant, and why that is expected
    - P6: the SUMMARY states explicitly that this mutant was **not** skipped
    - P7: harness exit 0 with `PASS: gate fired`; the failing test is **F2** and it failed because no error was thrown
    - P11: harness exit 0 with `PASS: gate fired`; the guard's message reports **44** names against an expected **45**, and the SUMMARY records both that pair and VALIDATION.md's earlier-measured `42 / 43` pair with the reason they differ
    - After each invocation, `git diff --exit-code` on the mutated file exits 0
    - After the task, `pnpm build && pnpm typecheck && pnpm test && pnpm run check:deps` all exit 0
    - `git status --porcelain` is empty at the end of the task
    - The SUMMARY records, for each of the three mutants, the command line, the harness stdout line, the harness exit code, the failing test name, and the assertion message
  </acceptance_criteria>
  <done>The three runtime guards have each been observed failing under a deliberate regression, including the tree-shaking one that no other check in the suite can see.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built artifact → consumer bundler | Tree-shaking decides which of the package's code reaches a real application |
| `dist/index.d.ts` export list → published API surface | Only the trailing `export { … }` is importable; declarations in the file are not |
| CI green signal → shipped behaviour | A silently no-op test command is a green signal with no behaviour behind it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-29 | Repudiation | `pnpm test` exiting 0 with no tests | mitigate | Root `test` becomes `vitest run` against a config Vitest can actually find. A silent green suite is the failure mode a CI author trusts most. |
| T-02-30 | Tampering | a duplicate-instance guard removed by tree-shaking | mitigate | F1b bundles a real consumer and asserts the registry key is present when called and absent when not. Mutant P6 proves it fires; F1a alone provably does not. |
| T-02-31 | Repudiation | consent armed on one core instance being invisible to another | mitigate | F1a asserts two independent module evaluations converge on one `globalThis` record; F2 asserts a version mismatch throws with actionable remediation. |
| T-02-32 | Information Disclosure | an internal declaration becoming publicly importable | mitigate | `export-surface.test.ts` pins the export list by count and by name and asserts `serverChallengeBrand` and `ConsentAckBase` are absent from it. This is the only check that catches an export added or dropped by a build-config change. |
| T-02-33 | Repudiation | a vacuous assertion counted as coverage | mitigate | The `ReadbackAttestation` check is documented as vacuous and deliberately **not** written; an acceptance criterion asserts it is absent. |
| T-02-34 | Tampering | Vitest typecheck mode silently collecting the `test-d/` suite | mitigate | Typecheck mode stays off, with the reason and the reproduced failure named in a config comment. |
| T-02-SC | Tampering | npm/pnpm installs | accept | This plan installs nothing. `vitest` and `rolldown` were installed and audited in plan 02-01. |
</threat_model>

<verification>
- `pnpm test` runs Vitest from the repo root and reports real tests
- `pnpm test -- single-instance`, `pnpm test -- artifact`, `pnpm test -- export-surface` each exit 0
- No test file references `../src/`
- F1b asserts the registry key present in the calling bundle and absent in the uncalled one
- P6, P7 and P11 each observed failing, with every failing test named (P6: F1b, expected alongside F2)
- `pnpm build && pnpm typecheck && pnpm test && pnpm run check:deps` all exit 0 at plan end
- `git status --porcelain` empty
</verification>

<success_criteria>
The repository has a runtime test suite that runs against the built artifact, PKG-04's two claims
are asserted including the bundling behaviour that motivated the design, the published export list
is pinned by name and count, and all three runtime guards have been observed failing under
deliberate regressions.
</success_criteria>

<output>
Create `.planning/phases/02-packaging-build-and-release/02-07-SUMMARY.md` when done.
</output>
