---
phase: 07-session-and-the-transport-seam
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - packages/concierge/src/session.ts
  - packages/concierge/src/index.ts
  - packages/concierge/src/contract.ts
  - packages/concierge/test/session-catalog.test.ts
  - packages/concierge/test-d/session.test-d.ts
  - packages/concierge/test-d/exports.test-d.ts
  - packages/concierge/test/artifact.test.ts
  - packages/concierge/test/export-surface.test.ts
  - packages/concierge/test/single-instance.test.ts
autonomous: true
requirements: [SES-01, SES-02, SES-04, TRN-02]
user_setup: []

must_haves:
  truths:
    - "Creating a Session immediately publishes the exact initial catalog reference, or one frozen empty catalog and null stage when context is absent."
    - "A context matching the catalog reference the transport actually holds publishes nothing, a differing reference publishes once even when it matches older confirmed authority, and a real connected transition replays the latest identical reference."
    - "Context and connected-status transitions drain through one reentrancy guard with latest-generation checks; the last successfully published transport catalog is tracked separately from confirmed authority, so nested context C reconciles against what the transport actually holds before any batch handler enters."
    - "A fixed-catalog transport stops synchronously before a catalog-identity-change error escapes and cannot accept reentrant work under stale authority."
    - "createSession is a callable public value, invokes the single-instance guard directly, and ships with exact declaration counts while the stub remains unexported."
  artifacts:
    - path: "packages/concierge/src/session.ts"
      provides: "Hot transactional Session factory, serialized latest-wins catalog/status drain, actual-published-catalog reconciliation, publication-gated batch admission, stage state, startup rollback, and direct package-instance guard"
      exports: ["createSession"]
    - path: "packages/concierge/test/session-catalog.test.ts"
      provides: "SES-01 built-artifact proof for publication, identity epochs, serialized reentrancy, admission gating, replay, stages, and fail-closed publication paths"
      contains: "[RED:C01:initial-publication]"
    - path: "packages/concierge/src/index.ts"
      provides: "Public createSession value and TransportStatus/SessionDiagnostic type exports with truthful phase prose"
      contains: "createSession"
    - path: "packages/concierge/test/export-surface.test.ts"
      provides: "Exact 69-name / 54-type / 15-value declaration surface"
      contains: "toHaveLength(69)"
    - path: "packages/concierge/test/single-instance.test.ts"
      provides: "F7 direct createSession single-instance call-site proof"
      contains: "F7"
  key_links:
    - from: "packages/concierge/src/session.ts"
      to: "Concierge.catalogFor / Concierge.stageFor"
      via: "Reference identity selects catalog epoch while string/null equality selects stage notification"
      pattern: "catalogFor|stageFor"
    - from: "packages/concierge/src/session.ts"
      to: "Transport.setTools / Transport.onStatusChange"
      via: "One guarded transition drain serializes hot publication and reconciles queued records against separate publishing, successfully published, and confirmed catalog state"
      pattern: "transitionDraining|transitionQueue|publishedCatalog|setTools"
    - from: "packages/concierge/src/session.ts"
      to: "Concierge.dispatchBatch"
      via: "Accepted records are captured under a provisional or confirmed context/epoch but the pump stays paused until the transition drain confirms the newest publication"
      pattern: "publicationPending|dispatchBatch"
    - from: "packages/concierge/src/index.ts"
      to: "packages/concierge/src/session.ts"
      via: "ESM .js value re-export"
      pattern: "createSession.*session\\.js"
    - from: "packages/concierge/src/session.ts"
      to: "packages/concierge/src/contract.ts"
      via: "Direct assertSingleInstance call before any outside capability"
      pattern: "assertSingleInstance\\(\\)"
---

<objective>
Create the hot Session factory's catalog/status loop and ship it as an exact guarded public value.

Purpose: Close the transport/cross-stage authority loop so initial state, navigation, reconnect, and reentrant publication cannot leave transport reality out of sync with confirmed authority or admit a batch before reconciliation completes.

Output: session.ts, serialized publication/admission regressions, public factory/type placement, exact built-surface gates, and direct single-instance evidence.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-session-and-the-transport-seam/07-CONTEXT.md
@.planning/phases/07-session-and-the-transport-seam/07-RESEARCH.md
@.planning/phases/07-session-and-the-transport-seam/07-PATTERNS.md
@.planning/phases/07-session-and-the-transport-seam/07-VALIDATION.md
@.planning/phases/07-session-and-the-transport-seam/07-01-SUMMARY.md
@.planning/phases/07-session-and-the-transport-seam/07-02-SUMMARY.md

<decision_traceability>
Planner-local aliases below point to locked bullets in 07-CONTEXT.md; they add no new product decisions.

- CTX-07-01: required neutral status lifecycle and connected replay.
- CTX-07-02: hot initialContext publication; otherwise one frozen empty catalog and null stage.
- CTX-07-03: retain every new context, publish only on catalog-reference change, notify only on stage string/null change, always replay on connected transition.
- CTX-07-04: dynamicCatalog false accepts its first catalog but stops before throwing on a later catalog-identity change.
- CTX-07-05/06: one Session FIFO captures context/catalog epoch at occurrence admission and old-epoch work aborts before new publication.
- CTX-07-08: every accepted occurrence crosses dispatchBatch exactly once; failure-stop drain still counts the occurrence while suppressing all responses.
- CTX-07-09: createSession is hot, standalone, transactional, direct-guarded, and returns a frozen Session handle.
- CTX-07-10/11: stop marks and invalidates transition/admission state before outside code, drains every accepted record, and emits no post-stop response or stage event.
- CTX-07-13: the stub remains test-only and absent from the public barrel.
</decision_traceability>

<interfaces>
From packages/concierge/src/types.ts after Plan 07-01:
- createSession target signature: (config: SessionConfig) =&gt; Session.
- SessionConfig: concierge, transport, initialContext?, onDiagnostic?.
- Session: setContext(ctx), stage(), onStageChange(cb), stop(): Promise&lt;void&gt;.
- Transport: capabilities, status, setTools, onStatusChange, onToolBatch, respond.

Existing Concierge methods consumed directly:
- catalogFor(ctx: StageContext): ReadonlyArray&lt;EmittedTool&gt; returns a memoized frozen reference per resolved stage projection.
- stageFor(ctx: StageContext): string | null.
- dispatchBatch(ctx: StageContext, batch: ToolBatch): Promise&lt;readonly correlation rows&gt;.

Selected fixed errors from the researched discretion:
- construction failure: `The session could not start.`
- fixed catalog change: `This transport does not support catalog changes.`
- catalog publication failure: `The session could not publish the current catalog.`
- use after stop: `This session has stopped.`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (07-03-01): Implement serialized latest-wins publication and gated batch admission</name>
  <read_first>
    - packages/concierge/src/bridge.ts — import order, direct guard, closure-local state, monotonic token, stale unsubscriber, and frozen handle analog.
    - packages/concierge/src/concierge.ts — catalogFor/stageFor reference identity, duplicate-stage-id behavior, and dispatchBatch boundary.
    - packages/concierge/src/host.ts — warnHost containment used by the runtime diagnostic path.
    - packages/concierge/src/types.ts — SessionConfig, Session, Transport, StageContext, EmittedTool, and SessionDiagnostic contracts from Plan 07-01.
    - packages/concierge/test/concierge.test.ts — STG-04 memo identity and duplicate-id stage cases.
    - packages/concierge/test/fixtures/stub-transport.ts — exact controls, histories, and capability profiles from Plan 07-02.
    - packages/concierge/test/dispatcher-batch.test.ts — built-dist dynamic import/reset harness.
    - .planning/phases/07-session-and-the-transport-seam/07-RESEARCH.md — Patterns 1-3, 7-9, exact error text, and catalog transition ordering.
    - .planning/phases/07-session-and-the-transport-seam/07-PATTERNS.md — session.ts composite analog and session-catalog test matrix.
  </read_first>
  <files>packages/concierge/src/session.ts, packages/concierge/test/session-catalog.test.ts</files>
  <behavior>
    - C01 [RED:C01:initial-publication]: initial context publishes `concierge.catalogFor(initialContext)` synchronously before return and records that same reference.
    - C02 [RED:C02:no-context-empty]: absent initial context publishes one frozen empty reference, stage() is null, and cross-stage actions cannot leak through catalogFor({}).
    - C03 [RED:C03:catalog-reference-epoch]: outside an in-flight publication, updates matching the exact `publishedCatalog` retain the newest context without publishing; a different reference publishes once even when its stage id/content matches.
    - C04 [RED:C04:connected-replay]: each actual transition into connected republishes the latest exact reference; other statuses and repeated identical status controls do not.
    - C05 [RED:C05:stage-value-change]: stage listeners fire only for string/null value changes, not catalog-only changes or reconnect replay.
    - C06 [RED:C06:fixed-catalog-stop-first]: a fixed transport accepts initial/same/replay catalogs, then becomes stopped with zero subscribers before the exact identity-change error; publication and reentrant batch work under the new catalog are absent.
    - C07 [RED:C07:transactional-start]: synchronous subscription/publication setup failure removes every registration obtained so far, best-effort clears tools, exposes only `The session could not start.`, and leaves no handle.
    - C08 [RED:C08:setcontext-publication-fail-closed]: when `setTools` throws during a later catalog-changing `setContext`, the newest context/stage is retained, stopped state and inert subscriptions are established before diagnostic or cleanup reentrancy, every later cleanup step still runs, and the triggering call exposes only `The session could not publish the current catalog.`.
    - C09 [RED:C09:replay-publication-fail-closed]: when `setTools` throws during a connected replay, the status callback establishes stopped state before diagnostic or cleanup reentrancy, continues independent cleanup, permits no later batch/stage work, and exposes only the same fixed publication error.
    - C10 [RED:C10:abort-reentry-latest-wins]: while outer `setContext(B)` aborts the old epoch, an abort listener's nested `setContext(C)` returns after queueing with publication history still A; resumed B cannot publish or notify, the drain then produces exact history A, C, final context/stage C, one dispatch entry for the original accepted occurrence, and no B handler/response/stage output.
    - C11 [RED:C11:settools-reentry-latest-wins]: successful `setTools(B)` emits a batch and nests a distinct-catalog `setContext(C)`; the nested call returns while still inside B's setTools with history exactly A, B and zero C publication/stage/dispatch, then the outer drain yields exact history A, B, C. B never becomes the confirmed stage, its held occurrence dispatches once only after C confirms with an aborted B epoch and zero handler entries, cancellation responses retain row order/cardinality, and final context/stage/catalog is C.
    - C12 [RED:C12:publication-admission-gate]: successful `setTools(B)` emits a valid batch and observes zero dispatch, handler, response, or stage output inside the call; after B is confirmed and the transition drain is empty, that occurrence dispatches once, enters its handler once, and responds once per ordered row.
    - C13 [RED:C13:setcontext-failure-batch-drain]: later `setTools(B)` emits one batch and then throws; the occurrence is recorded, synchronously aborted/detached under stopped state, dispatched exactly once during drain with zero handler/response/stage output, and no diagnostic/cleanup reentry can admit later work.
    - C14 [RED:C14:replay-failure-batch-drain]: connected replay of A emits one batch and then throws with the same stopped-before-reentry and one-dispatch drain guarantees; subscriber callbacks are inert and handler/response/stage counts remain zero afterwards.
    - C15 [RED:C15:reentrant-same-published-catalog]: successful `setTools(catalogB)` emits one single-call B batch and nests `setContext(C)` where `catalogFor(C) === catalogB`; nested return observes history A, B and zero stage/dispatch/handler/response output, final history remains exactly A, B, final authority is context C/catalogB/stageFor(C), exactly one final C stage notification occurs, B's epoch is promoted without abort, the held B batch dispatches/handles/responds once with arrival context B, and one later batch dispatches/handles/responds once with context C on the same active epoch.
    - C16 [RED:C16:reentrant-return-to-confirmed-catalog]: successful `setTools(catalogB)` emits one single-call B batch and nests `setContext(C)` where `catalogFor(C) === catalogA`; nested return observes history A, B and zero stage/dispatch/handler/response output, final history is exactly A, B, A, final authority is context C/catalogA/stageFor(C), zero post-registration stage notifications occur, the original A and provisional B epochs are aborted, a fresh C/A epoch remains active, the held B batch dispatches once with zero handler entry and one cancellation response, and one later C batch dispatches/handles/responds once with context C.
  </behavior>
  <action>
Create session-catalog.test.ts first using the built-dist harness and relative test-only stub. Register C01-C16 with unique markers, reset the package instance key before every case, and prove the focused file fails because createSession is absent; a missing dist artifact is not valid RED evidence. Include initial/no-context, connected/non-connected, same-object mutation, distinct contexts sharing a catalog, distinct catalogs with equal content/duplicate stage id, dynamic/fixed profile, and synchronous reentrant epoch-abort/setTools/status/batch controls. Use explicit counters for accepted occurrences, dispatchBatch entry, real handler entry, response attempts, stage notifications, cleanup steps, publication-attempt identity, and epoch-signal state; no timers or mock framework.

For C10, hold an A-epoch dispatch and have its composed-signal abort listener call `setContext(C)` while outer `setContext(B)` is aborting A; snapshot history/stage immediately after the nested call returns to prove it only enqueued, then assert exact A→C publication and no B commit after the outer drain resumes. For C11, make successful `setTools(B)` synchronously emit a batch and call `setContext(C)` whose catalog is distinct from both A and B; snapshot history/stage/dispatch counters after nested return but before B's setTools returns to prove C did not publish reentrantly, then require exact A→B→C attempt history, no B stage notification, one post-C dispatch of the aborted B record, zero handler entries, and ordered cancellation responses. C12 emits a valid batch inside successful `setTools(B)` without a newer transition and snapshots all counters inside the callback before requiring one normal dispatch/handler/response sequence after confirmation. C13 and C14 emit a batch immediately before a unique secret-bearing throw during later setContext publication and connected replay respectively; each attempted catalog reference must be recorded in order, the accepted occurrence must drain through exactly one dispatch with an already-aborted signal, and handler/response/stage counts must stay zero. In C08/C09/C13/C14, instrument both unsubscribers and `onDiagnostic` to attempt reentrant context, status, and batch work; stopped state and inert subscriptions must already be observable, every subsequent cleanup step must still run, and neither fixed error nor exact frozen `catalog_publish_failed` diagnostic may contain the caught sentinel, context, catalog, or identifier.

For C15, arrange `catalogFor(B) === catalogFor(C) === catalogB` and `stageFor(B) === stageFor(C) !== stageFor(A)` by reference/value, then use two one-call batches. Assert publication-reference history `[catalogA, catalogB]`, no B confirmation, exactly one stage notification carrying `stageFor(C)`, final `stage() === stageFor(C)`, the B epoch signal remains un-aborted, exact totals of two dispatch entries, two handler entries, and two response attempts, and handler-observed contexts `[B, C]` for the held-then-later batches. For C16, arrange `catalogFor(C) === catalogA` by reference after B was successfully published, with `stageFor(C) === stageFor(A)`. Assert history `[catalogA, catalogB, catalogA]`, zero stage notifications after listener registration, final `stage() === stageFor(C)`, original-A and B epoch signals aborted while the fresh C/A epoch is active, exact totals of two dispatch entries, one handler entry, and two response attempts (one ordered cancellation row for held B and one normal later-C row), and later handler context exactly C. Both cases snapshot the same zero counters inside B's callback and assert no extra `setTools`, dispatch, handler, response, or stage activity.

Implement `export function createSession(config: SessionConfig): Session` in a new session.ts with explicit annotations and `.js` imports. Call assertSingleInstance() directly as the first outside-effect line. Keep all mutable lifecycle, context, catalog, stage, epoch, admission, queue/listener, subscription, and stop-drain state in the factory closure; module scope may contain only immutable fixed strings, a frozen diagnostic table, and a frozen empty catalog. Construction installs both callbacks while lifecycle is `starting`, routes initial publication through the same transition machinery, confirms it before switching to `active`, and returns `Object.freeze({setContext,stage,onStageChange,stop})`. A batch synchronously emitted by a valid initial publication may be recorded against its provisional context/epoch but cannot pump until construction is active and publication is confirmed. Any synchronous setup failure marks stopped, invalidates the transition drain, aborts/detaches accepted provisional work, independently rolls back obtained registrations and best-effort clears the catalog, emits only a safe fixed diagnostic, drains accepted work without handler/response output, and throws `Error("The session could not start.")`.

Implement CTX-07-01 through CTX-07-04 with one explicit factory-local `transitionQueue`, boolean `transitionDraining`, monotonic requested generation, and separate requested state, publication-in-progress catalog, last-successfully-published `publishedCatalog`, provisional/published-but-unconfirmed epoch state, and confirmed application context-catalog-stage-epoch authority. Both `setContext` and each actual non-connected→connected edge enqueue a typed transition. A context enqueue synchronously retains the newest exact requested context and generation; a nested enqueue while draining returns after queueing and cannot resolve, publish, notify, or start the batch pump itself. Only the outermost drain processes records. Compare the record generation with the newest requested generation and stopped state immediately before and after old-scope `abort()` and `setTools`, then again before confirmation and before stage notification. A record superseded before publication aborts its provisional scope. A record superseded after successful publication never overwrites confirmed authority or emits its stage, but its published epoch is retained until the newest queued record is reconciled against `publishedCatalog`; do not abort it unconditionally. If a stage callback itself queues a newer context, make no later writes from the already-confirmed outer record and continue the transition drain after the listener snapshot completes. Thus abort-listener reentry can skip B before publication, while setTools reentry can leave an observable B publication without permitting B to become application authority after C was requested.

For a context record, obtain catalogFor/stageFor once, compare catalog references for authority and stage string/null values separately, and allocate provisional admission state before aborting older catalog epochs or publishing. Reconcile catalog identity against `publishedCatalog`, not merely the pre-transition confirmed catalog. Before `setTools(candidateCatalog)`, set the separate publication-in-progress value and `publicationPending`; if the call throws, leave `publishedCatalog` unchanged and take the existing stop-first path. Immediately after a successful return, and before clearing the in-progress value, inspecting queued transitions/generation, aborting any provisional scope, confirming authority, notifying stage, or resuming the pump, assign `publishedCatalog = candidateCatalog`. A successful current record promotes its epoch and confirms normally. If a newer context is queued, retain the now-published-but-unconfirmed epoch until that newest record is resolved: when its catalog is `=== publishedCatalog`, promote/reuse that epoch, retain its accepted B work, confirm the newest context, and do not abort or call `setTools` again; when its catalog differs, abort the published-but-unconfirmed epoch and publish the new reference. Therefore C returning to pre-transition confirmed catalog A after successful B must republish A, yielding A→B→A, while a distinct C yields A→B→C. Catalog-changing dynamic records abort older active/queued scopes before `setTools`; fixed transports synchronously enter stop before the fixed catalog-change error. A connected record forces replay of the latest confirmed catalog without changing epoch. Emit a changed stage only from a confirmed, still-current record. Resume the single batch pump only when lifecycle is active, the publication-in-progress value is clear, `publicationPending` is false, `transitionDraining` is false, and `transitionQueue` is empty.

The batch callback may accept during active lifecycle, or during starting only when a non-null provisional initial context exists. It captures a distinct occurrence against the exact provisional context/epoch when a transition is in flight, otherwise the confirmed context/epoch, and queues it without reading its envelope fields or starting the pump while publication is unconfirmed. If successful `setTools(B)` emits a batch and queues C, the B occurrence always keeps arrival context B. For distinct C or C returning to confirmed A, abort B's published-but-unconfirmed epoch and dispatch the held occurrence exactly once after C confirms with an already-aborted signal: Phase 6 authors ordered cancellation rows and no handler enters. When `catalogFor(C) === publishedCatalog === catalogB`, reuse/promote B's epoch instead; the held occurrence dispatches once under context B without cancellation, and later batches on that epoch capture context C. Without a newer transition, a batch emitted inside successful publication pumps once only after that publication confirms.

On any setTools throw, synchronously allocate/cache the stop drain, mark stopped, invalidate generations and clear pending transitions, abort active/queued/provisional scopes, detach every accepted record including one emitted inside the failing call, and suppress stage/response output before invoking diagnostics, unsubscribers, catalog clearing, or any other outside cleanup. Run cleanup independently and start the arrival-ordered detached drain; each record must cross dispatchBatch exactly once with response emission disabled, and its pre-aborted composite prevents handler entry. The outermost triggering setContext or status-control call throws only `Error("The session could not publish the current catalog.")` without waiting for asynchronous drain completion; its existing Session handle's `stop()` returns the same cached Promise, which C13/C14 await before asserting the final one-dispatch/zero-output counts. A nested enqueuer that already returned receives no second throw. Starting/stopped callbacks outside a live provisional publication are inert; no-context batches receive only `batch_without_context`. Do not call catalogFor({}), deep-compare tools, stop automatically on `closed`, add vendor vocabulary, or interpret grade/turn provenance/delivery.
  </action>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge build &amp;&amp; pnpm exec vitest run packages/concierge/test/session-catalog.test.ts</automated>
    <automated>pnpm --filter @fullselfbrowsing/concierge typecheck</automated>
    <automated>bash -c '! rg -n "catalogFor\\(\\{\\}\\)|onReconnect|setTimeout|setInterval|fetch\\s*\\(|WebSocket|RTCPeerConnection" packages/concierge/src/session.ts'</automated>
  </verify>
  <done>The hot factory serializes reentrant context/replay transitions with latest-wins confirmation, gates every accepted batch behind publication, fails closed with complete batch drain on publication errors, and never resumes stale authority.</done>
  <acceptance_criteria>
    - C01-C16 pass against dist and each intended marker executes.
    - Initial publication occurs before createSession returns and does not depend on transport.status.
    - No-context history contains one frozen empty array by reference and zero cross-stage tools.
    - Duplicate stage ids can change catalog/reference without emitting a stage event; reconnect replays exact identity without changing epoch.
    - Fixed-catalog failure establishes stopped state/subscriber cleanup before the error and rejects reentrant work.
    - Later setContext and connected-replay publication failures establish stopped state before diagnostic/cleanup reentrancy, attempt every cleanup step, emit only the exact safe diagnostic, expose only the fixed publication error, and permit no later work.
    - Abort-listener and successful-setTools reentrancy are serialized: final requested/confirmed context, catalog, and stage are C; history is A→C when B is superseded before publication, A→B→C for distinct C, A→B for C sharing published B, and A→B→A for C returning to confirmed A. B never resumes to overwrite C.
    - A batch emitted inside successful publication has zero dispatch/handler/response/stage output until reconciliation. A distinct/return-to-A C aborts B and produces one cancellation dispatch; C sharing published B promotes the same un-aborted epoch, handles held B once under B's arrival context, and handles a later batch under C without republishing.
    - Batches emitted inside failing later-setContext or replay publication are accepted once, aborted/detached under already-stopped state, dispatched once during drain, and produce zero handler, response, or stage output.
    - session.ts contains no mutable module-scope state, DOM/timer/network primitive, vendor reconnect name, consent-grade/provenance branch, or delivery-hook invocation.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (07-03-02): Publish the factory and exact source-level type placement</name>
  <read_first>
    - packages/concierge/src/session.ts — createSession and direct guard created by Task 1.
    - packages/concierge/src/index.ts — grouped type/value exports and stale module header that says Session is unimplemented.
    - packages/concierge/src/contract.ts — guarded production entry-point inventory and direct createBridge wording.
    - packages/concierge/test-d/session.test-d.ts — Session/config/diagnostic pins from Plan 07-01.
    - packages/concierge/test-d/exports.test-d.ts — public value-placement import and createBridge predicate analog.
    - .planning/phases/07-session-and-the-transport-seam/07-PATTERNS.md — index/contract/session/export type-test assignments.
  </read_first>
  <files>packages/concierge/src/index.ts, packages/concierge/src/contract.ts, packages/concierge/test-d/session.test-d.ts, packages/concierge/test-d/exports.test-d.ts</files>
  <behavior>
    - typeof createSession is exactly `(config: SessionConfig) =&gt; Session` through src/index.js.
    - createSession is a callable value export, never a type-only export.
    - TransportStatus, SessionDiagnosticCode, and SessionDiagnostic are public type exports.
    - Shipped/source prose states Session now owns catalog/transport orchestration while consent and framework adapters remain outside Phase 7.
  </behavior>
  <action>
Add the exact factory predicate to session.test-d.ts and import createSession as a value from `../src/index.js` in exports.test-d.ts before changing the barrel; run typecheck and record the intended missing-export RED diagnostics.

Then, per CTX-07-09 and CTX-07-13, export TransportStatus, SessionDiagnosticCode, and SessionDiagnostic in the existing grouped type block and export createSession from `./session.js` as the sole new public value. Replace only the now-false index header claims that Session/transport routing do not exist; retain the truthful boundaries that consent remains Phase 8 and framework adapters remain Phase 9. Update contract.ts prose to name createSession as the fourth guarded production entry point and a direct call site; do not change the guard implementation or invoke it at module scope. Keep source ESM-only and preserve isolated-declaration annotations.
  </action>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge typecheck</automated>
    <automated>pnpm --filter @fullselfbrowsing/concierge build</automated>
    <automated>bash -c '! rg -n "createStubTransport|stub-transport" packages/concierge/src/index.ts packages/concierge/src/session.ts'</automated>
  </verify>
  <done>The source entrypoint exports one callable createSession and three required public types with exact signatures and truthful package documentation.</done>
  <acceptance_criteria>
    - Both factory predicates compile through the public barrel and would fail under `export type` placement.
    - Source exports add exactly one value and three types; no fixture control or internal helper is public.
    - contract.ts identifies createSession as a direct guard caller without changing assertSingleInstance.
    - Source prose no longer says Session is unimplemented and does not claim consent or adapters exist.
    - Build and package typecheck exit 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3 (07-03-03): Synchronize built artifact, export counts, and direct single-instance evidence</name>
  <read_first>
    - packages/concierge/test/artifact.test.ts — callable createConcierge/createBridge built-value cases.
    - packages/concierge/test/export-surface.test.ts — VALUE_EXPORTS and exact 65/51/14 declaration parser.
    - packages/concierge/test/single-instance.test.ts — F6 direct createBridge case, registry reset, and unique query-string discipline.
    - packages/concierge/src/session.ts — direct guard and construction needs for a structural fake.
    - packages/concierge/src/types.ts — minimal Concierge and six-key Transport shapes required by F7.
    - .planning/phases/07-session-and-the-transport-seam/07-PATTERNS.md — artifact, export-surface, and F7 exact analogs.
  </read_first>
  <files>packages/concierge/test/artifact.test.ts, packages/concierge/test/export-surface.test.ts, packages/concierge/test/single-instance.test.ts</files>
  <action>
Per CTX-07-09 and CTX-07-13, add a dist/index.js artifact case requiring `typeof createSession === "function"`. Append createSession to VALUE_EXPORTS and synchronize the parsed declaration assertions/titles to exactly 69 names, 54 types, and 15 values; re-derive those counts from the built declaration after implementation rather than weakening the test if arithmetic differs.

Add F7 using a never-reused import query string. Destructure assertSingleInstance, createSession, and CONTRACT_VERSION but never call assertSingleInstance. Assert the registry is empty after import, call createSession with hand-written structural Concierge and six-key Transport fakes, assert the registry now contains this version, and await stop to leave no subscriber. Do not construct either fake via createConcierge/createBridge or import session.ts directly; F7 must go red only when createSession's direct guard call is removed.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm exec vitest run packages/concierge/test/artifact.test.ts packages/concierge/test/export-surface.test.ts packages/concierge/test/single-instance.test.ts</automated>
    <automated>pnpm --filter @fullselfbrowsing/concierge typecheck</automated>
  </verify>
  <done>The built package exposes the exact new value/types and F7 proves createSession records the package copy through its own direct guard call.</done>
  <acceptance_criteria>
    - Built createSession is callable and the declaration parser reports exactly 69 names, 54 types, and 15 values.
    - VALUE_EXPORTS contains createSession exactly once and no stub value.
    - F7 imports without registering, calls no other guarded factory, registers only after createSession, and awaits stop.
    - Removing `assertSingleInstance();` from createSession makes F7 fail after a successful build and non-zero test run.
    - Artifact/export/single-instance suites and package typecheck all pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application context → catalog authority | Context changes can change the action allowlist even when stage ids or tool contents appear equal. |
| Transport status/callback → Session | Reentrant or hostile lifecycle callbacks can observe partial setup or stale authority. |
| Transport publication callback → batch admission | `setTools` can synchronously reenter context/status callbacks or emit a batch before the catalog attempt is confirmed. |
| Source entrypoint → built package | A source-only factory or missing direct guard would split runtime state across package copies. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation and verification |
|-----------|----------|----------|-----------|-------------|-----------------------------|
| T-07-01 | high | Elevation of privilege / Tampering | catalog epoch, replay, and publication admission | mitigate | One guarded transition queue plus generation checks prevents nested C from being overwritten by resumed B; separate publishing/published/confirmed state reconciles against transport reality, and the pump remains blocked until the winning authority is confirmed; C01-C06/C10-C16 are blocking. |
| T-07-04 | high | Tampering / Denial of service | transition and teardown reentrancy | mitigate | Nested context/status callbacks only enqueue, published-but-unconfirmed epochs are promoted or aborted by actual published identity, stop invalidates the drain before outside code, and stale callbacks are inert; C06-C16 verify ordering and counts. |
| T-07-05 | high | Information disclosure / Denial of service | later catalog publication failures | mitigate | C08/C09/C13/C14 require stopped state and aborted admission before diagnostic/cleanup reentrancy, independent cleanup, one fixed frozen `catalog_publish_failed` diagnostic, fixed public error text, and absence of caught values or identifiers. |
| T-07-06 | high | Tampering | public package and core instance | mitigate | Exact type/value counts, callable artifact case, direct F7 guard proof, and no stub export; all artifact tests are blocking. |
| T-07-SC | low | Tampering | package supply chain | accept | No dependency or manifest edit exists; final Plan 07-06 verifies package/lockfile identity and tar contents. |

## Residual Risk

A hostile transport unsubscriber can retain its own callback; core can only establish stopped state first and make that callback inert. This residual is explicitly tested further in Plan 07-05 and does not leave a live authority path or unresolved high-severity threat.
</threat_model>

<verification>
Build before every runtime/artifact test because suites import dist. Run the catalog, artifact, export-surface, and single-instance suites plus package typecheck; verify only the nine declared files changed and no test fixture became a product export.
</verification>

<success_criteria>
- Initial, identity-change, and reconnect publications carry the correct frozen reference through one serialized latest-wins transition drain with separate publishing, successfully published, and confirmed catalog state.
- No batch handler enters before publication reconciliation; superseded work aborts only when the winning catalog differs from what the transport actually holds, while same-published-catalog work keeps its epoch and arrival context.
- Fixed transports stop before a stale-catalog error and partial construction rolls back.
- createSession is public, exactly typed, directly guarded, and callable from dist.
- The built surface is exactly 69/54/15 and excludes createStubTransport.
</success_criteria>

<output>
Create .planning/phases/07-session-and-the-transport-seam/07-03-SUMMARY.md when done.
</output>
