---
phase: 07-session-and-the-transport-seam
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/concierge/test/fixtures/stub-transport.ts
  - packages/concierge/test/stub-transport.test.ts
  - packages/concierge/test-d/stub-transport.test-d.ts
autonomous: true
requirements: [TRN-02]
user_setup: []

must_haves:
  truths:
    - "Tests can drive status changes and complete ToolBatch arrivals synchronously without a network, timer, browser, vendor SDK, or production-only test hook."
    - "The conversational and command-palette capability profiles are deeply frozen and differ by capabilities, never by production modality branches."
    - "Every setTools and respond attempt is recorded before deterministic occurrence-based failure injection, so once-only behavior is observable."
    - "Catalog, response, and subscriber-count histories are immutable snapshots while preserving catalog reference identity."
  artifacts:
    - path: "packages/concierge/test/fixtures/stub-transport.ts"
      provides: "Reusable Phase 7/8 test-only Transport harness with profiles, synchronous controls, failures, histories, and subscriber counts"
      contains: "createStubTransport"
    - path: "packages/concierge/test/stub-transport.test.ts"
      provides: "Phase 7 TRN-02 fixture-seam contract, immutability, deterministic failure, and no-I/O proof"
      contains: "[RED:U01:stub-profiles]"
    - path: "packages/concierge/test-d/stub-transport.test-d.ts"
      provides: "Compiler inclusion and exact structural Transport conformance for the test-only fixture"
      contains: "_stubTransportKeys"
  key_links:
    - from: "packages/concierge/test/fixtures/stub-transport.ts"
      to: "packages/concierge/src/types.ts"
      via: "The frozen transport object structurally implements the exact six-key Transport contract"
      pattern: "capabilities.*status.*setTools.*onStatusChange.*onToolBatch.*respond"
    - from: "packages/concierge/test/stub-transport.test.ts"
      to: "packages/concierge/test/fixtures/stub-transport.ts"
      via: "Synchronous event controls and attempt histories exercise the fixture without source imports"
      pattern: "emitStatus|emitBatch|catalogHistory|responseHistory|subscriberCounts"
    - from: "packages/concierge/test-d/stub-transport.test-d.ts"
      to: "packages/concierge/test/fixtures/stub-transport.ts"
      via: "A test-d import brings the fixture into the package TypeScript program and pins its transport to the six-key public contract"
      pattern: "createStubTransport|keyof Transport"
---

<objective>
Build the reusable deterministic Transport fixture that drives Phase 7 and later Phase 8 proofs without I/O.

Purpose: Make transport lifecycle, batches, hostile callbacks, and ambiguous response acceptance explicit and repeatable while keeping every test control outside the production Transport surface. This delivers the reusable Phase 7 seam slice of TRN-02; Phase 8 must exercise the same fixture against the consent kernel before that requirement is complete.

Output: A frozen test-only stub harness, two locked capability profiles, synchronous controls, occurrence failures, immutable histories, and its dedicated contract suite.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-session-and-the-transport-seam/07-CONTEXT.md
@.planning/phases/07-session-and-the-transport-seam/07-RESEARCH.md
@.planning/phases/07-session-and-the-transport-seam/07-PATTERNS.md
@.planning/phases/07-session-and-the-transport-seam/07-VALIDATION.md

<decision_traceability>
Planner-local aliases below point to locked bullets in 07-CONTEXT.md; they add no new product decisions.

- CTX-07-13: createStubTransport stays under test/fixtures, is not exported, and is absent from the package tarball.
- CTX-07-14: The stub has frozen configurable capabilities, explicit synchronous controls, occurrence failures, immutable histories/counts, and the conversational and command-palette profiles; it uses no timer, network, WebRTC, vendor SDK, or vendor event vocabulary.
</decision_traceability>

<interfaces>
Test-only API selected from the discretion resolved by 07-RESEARCH.md:
- CONVERSATIONAL_CAPABILITIES: consentGrade relayed, userTurnIdentity agent-forgeable, parallelCalls true, dynamicCatalog true.
- COMMAND_PALETTE_CAPABILITIES: consentGrade attested, userTurnIdentity human-attested, parallelCalls false, dynamicCatalog false.
- createStubTransport({ capabilities, initialStatus, failures }) returns a frozen harness.
- Harness members: transport, emitStatus(status), emitBatch(batch), catalogHistory(), responseHistory(), subscriberCounts().
- Failure members: setToolsAt, respondAt as readonly one-based occurrence arrays; subscribeStatus, subscribeBatch, unsubscribeStatus, unsubscribeBatch as booleans.
- The object supplied to production is only the frozen six-key transport; controls are siblings on the harness.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (07-02-01): Build frozen profiles and synchronous lifecycle/batch controls</name>
  <read_first>
    - packages/concierge/src/types.ts — TransportCapabilities, TransportStatus, ToolBatch, EmittedTool, and ActionResult structural contracts.
    - packages/concierge/test/dispatcher-batch.test.ts — createAbortController snapshot-listener/count-control analog.
    - packages/concierge/test/bridge.test.ts — frozen outward handle with live closure and stale-unsubscriber assertions.
    - packages/concierge/test-d/transport.test-d.ts — the two unrelated capability profiles and neutral six-key Transport contract from Plan 07-01.
    - packages/concierge/test-d/stub-transport.test-d.ts — create this disjoint Wave 1 compiler gate before implementing the fixture.
    - .planning/phases/07-session-and-the-transport-seam/07-CONTEXT.md — Stub transport locked decisions CTX-07-13 and CTX-07-14.
    - .planning/phases/07-session-and-the-transport-seam/07-PATTERNS.md — required test fixture behavior and runtime-test discipline.
  </read_first>
  <files>packages/concierge/test/fixtures/stub-transport.ts, packages/concierge/test/stub-transport.test.ts, packages/concierge/test-d/stub-transport.test-d.ts</files>
  <behavior>
    - U01 [RED:U01:stub-profiles]: both exact named capability profiles and every nested outward object are frozen and carry the locked values.
    - U02 [RED:U02:status-transitions]: emitStatus is synchronous, snapshots subscribers, updates readonly transport.status, and emits only on an actual value transition.
    - U03 [RED:U03:batch-snapshot]: emitBatch synchronously invokes the subscriber snapshot, so add/remove during delivery does not alter that occurrence.
    - U04 [RED:U04:subscriber-counts]: subscribing and unsubscribing updates frozen `{status,batch}` count snapshots without exposing the live sets.
    - Type gate: importing `createStubTransport` from `../test/fixtures/stub-transport.js` compiles the fixture inside tsconfig.test-d.json; `stub.transport` is assignable to Transport and `keyof typeof stub.transport` equals the exact six-key `keyof Transport` union.
  </behavior>
  <action>
Write U01-U04 in stub-transport.test.ts and create test-d/stub-transport.test-d.ts before creating the fixture; both imports must initially fail because the fixture is absent. In the type file import `createStubTransport`, both profiles, and any public fixture option type from `../test/fixtures/stub-transport.js`, import Transport from `../src/index.js`, assign `stub.transport` to Transport, and add `Expect<Equals<keyof typeof stub.transport, keyof Transport>>` as `_stubTransportKeys`. This import is the mechanism that brings the otherwise out-of-program test fixture into `tsconfig.test-d.json`. The runtime tests must not import executable production source or use timers/mocking APIs.

Implement the exact test-only interface above per CTX-07-13/14. Keep listener sets, status, histories, and counters in the factory closure. Freeze both profile objects, the supplied capability object recursively through its known primitive-only keys, the six-key transport, and the outer harness. Define transport.status as a readonly getter backed by closure state. `emitStatus` updates and synchronously delivers only when the status value changes; `emitBatch` snapshots the batch-listener set before delivery. Each subscription returns an identity-safe unsubscriber and subscriberCounts returns a newly frozen snapshot. Do not place controls on transport, import dist, create a timer, or add production exports.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/concierge/test/stub-transport.test.ts --testNamePattern="^\\[U0[1-4]\\]"</automated>
    <automated>bash -c '! rg -n "setTimeout|setInterval|fetch\\s*\\(|WebSocket|RTCPeerConnection|navigator|document|window" packages/concierge/test/fixtures/stub-transport.ts packages/concierge/test/stub-transport.test.ts'</automated>
  </verify>
  <done>The stub exposes two exact frozen capability profiles and deterministic synchronous status/batch controls through a test-only frozen harness.</done>
  <acceptance_criteria>
    - U01-U04 execute and pass with their exact RED markers.
    - Object.keys(stub.transport) is exactly capabilities, status, setTools, onStatusChange, onToolBatch, respond; controls are absent.
    - After Plans 07-01 and 07-02 merge, `pnpm --filter @fullselfbrowsing/concierge typecheck` compiles test-d/stub-transport.test-d.ts and its imported fixture with `_stubTransportKeys` green; deleting status/onStatusChange or adding a transport control makes the exact predicate fail.
    - Repeated identical statuses produce no callback, while a real transition updates status before the snapshot is invoked.
    - Mutation attempts against profiles, transport, harness, and count snapshots fail to alter subsequent observations.
    - No timer, network, WebRTC, platform DOM global, mock API, or production import appears in either file.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (07-02-02): Add deterministic failures and immutable attempt histories</name>
  <read_first>
    - packages/concierge/test/fixtures/stub-transport.ts — closure and synchronous controls created by Task 1.
    - packages/concierge/test/stub-transport.test.ts — U01-U04 conventions and exact RED marker style.
    - packages/concierge/test/dispatcher-batch.test.ts — frozen result/correlation and occurrence-count assertion patterns.
    - packages/concierge/test/diagnostic-safety.test.ts — hostile callback containment fixtures without Vitest mocking.
    - packages/concierge/package.json — existing files list that excludes test/ without a manifest change.
    - .planning/phases/07-session-and-the-transport-seam/07-RESEARCH.md — recommended failures, attempt-before-throw history, and reference-preservation rules.
  </read_first>
  <files>packages/concierge/test/fixtures/stub-transport.ts, packages/concierge/test/stub-transport.test.ts</files>
  <behavior>
    - U05 [RED:U05:attempt-before-throw]: configured one-based setTools/respond occurrences are recorded once before the exact injected throw.
    - U06 [RED:U06:subscription-failures]: each subscribe and unsubscribe failure is independently injectable and does not corrupt the other subscriber set.
    - U07 [RED:U07:immutable-histories]: history arrays and response rows are frozen snapshots; catalog entries retain the original array reference for reconnect identity assertions.
    - U08 [RED:U08:test-only-boundary]: neither production source nor the package barrel imports/exports the fixture, and the fixture contains no vendor or I/O primitive.
  </behavior>
  <action>
Add U05-U08 first and prove they fail because failure/history behavior is absent, not because U01-U04 regressed. Use unique sentinel results/catalogs and assert occurrence counts rather than only final values.

Implement one-based occurrence failure injection with readonly copied/frozen configuration. `setTools` and `respond` must append a frozen attempt record before checking whether the current occurrence throws; use fixed fixture-authored error text and never retry. Subscription and unsubscription failures are separate switches and each path mutates only the state it owns. catalogHistory and responseHistory return new frozen arrays; freeze response rows, retain each original catalog array by reference, and never deep-clone EmittedTool objects. Continue to return frozen subscriber counts. Per CTX-07-13, keep every fixture type/value out of src/index.ts and package exports; Phase 8 will reuse this relative test import.
  </action>
  <verify>
    <automated>pnpm exec vitest run packages/concierge/test/stub-transport.test.ts</automated>
    <automated>bash -c '! rg -n "createStubTransport|stub-transport" packages/concierge/src packages/concierge/package.json'</automated>
    <automated>bash -c '! rg -n "setTimeout|setInterval|fetch\\s*\\(|WebSocket|RTCPeerConnection|OpenAI|Anthropic|Realtime" packages/concierge/test/fixtures/stub-transport.ts'</automated>
  </verify>
  <done>The test transport exposes complete immutable attempt evidence and independent deterministic failures while remaining absent from production source and package configuration.</done>
  <acceptance_criteria>
    - U05-U08 and U01-U04 all pass in one focused run.
    - A configured failed call appears exactly once in history before its fixed throw; later occurrences obey their own configured indices.
    - Every subscribe/unsubscribe failure mode is independently observed and subscriber counts remain truthful.
    - Catalog history supports `toBe` identity checks and no caller mutation changes stored response/count observations.
    - `rg` finds no fixture export/import in production and no timer/network/WebRTC/vendor primitive in the fixture.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test controls → Transport interface | Test-only authority must not leak onto the object production Session receives. |
| Transport callback → test history | A throw can make acceptance ambiguous, so attempt order and cardinality must be recorded before failure. |
| Test fixture → published tarball | Reusable test code must remain available to Phase 8 without becoming package API or shipped content. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation and verification |
|-----------|----------|----------|-----------|-------------|-----------------------------|
| T-07-02 | high | Tampering / Repudiation | Stub response/setTools attempts | mitigate | Record every attempt before occurrence-based throw, freeze snapshots, and assert exact counts; Session once-only mutations use these histories later. |
| T-07-05 | high | Information disclosure / Denial of service | Injected errors and callbacks | mitigate | Fixture errors use fixed authored text, carry no batch/result details, and each injected path is independently exercised. |
| T-07-06 | high | Tampering | Test-only/package boundary | mitigate | Keep the fixture under test/fixtures, assert no source/barrel import, and make Plan 07-06 inspect the actual tarball. |
| T-07-SC | low | Tampering | Package supply chain | accept | No package install or manifest edit exists; static no-I/O checks and the final lockfile gate provide evidence. |

## Residual Risk

The stub is a deterministic structural transport, not proof that any future vendor adapter reports truthful capabilities. It fully covers Phase 7's no-I/O seam and leaves no unresolved high-severity fixture or packaging threat.
</threat_model>

<verification>
Run the dedicated fixture suite without a build, then run static negative checks over fixture and production paths. At the Wave 1 merge boundary run `pnpm --filter @fullselfbrowsing/concierge typecheck`; it must compile test-d/stub-transport.test-d.ts and transitively compile test/fixtures/stub-transport.ts against Plan 07-01's six-key Transport. Confirm only the three declared test files changed and no manifest or lockfile changed; Plan 07-01 and 07-02 still own disjoint paths.
</verification>

<success_criteria>
- Both locked profiles and the six-key Transport are frozen and exact.
- All lifecycle and batch controls are synchronous and timer-free.
- Histories prove attempt order/cardinality and cannot be mutated by callers.
- The fixture is reusable by tests but absent from production exports and package configuration.
</success_criteria>

<output>
Create .planning/phases/07-session-and-the-transport-seam/07-02-SUMMARY.md when done.
</output>
