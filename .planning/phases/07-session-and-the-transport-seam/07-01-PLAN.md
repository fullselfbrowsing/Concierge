---
phase: 07-session-and-the-transport-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/concierge/src/types.ts
  - packages/concierge/test-d/session.test-d.ts
  - packages/concierge/test-d/transport.test-d.ts
  - packages/concierge/test-d/actions.test-d.ts
autonomous: true
requirements: [SES-01, SES-03, SES-04, TRN-02]
user_setup: []

must_haves:
  truths:
    - "A consumer can implement a vendor-neutral Transport whose connection status and status subscription are required, readonly, and exhaustively typed."
    - "Session.stop is observably awaitable while Session retains exactly its four public members."
    - "A consumer can supply initialContext and onDiagnostic through computed optional values under exactOptionalPropertyTypes."
    - "Session diagnostics expose only a closed code and fixed message shape, with readonly fields and no raw-detail channel."
  artifacts:
    - path: "packages/concierge/src/types.ts"
      provides: "TransportStatus, six-key Transport, awaitable Session.stop, SessionConfig diagnostics, and closed SessionDiagnostic contracts"
      contains: "export type SessionDiagnosticCode"
    - path: "packages/concierge/test-d/transport.test-d.ts"
      provides: "Exact Transport key, status-union, callback, and readonly pins"
      contains: "_transportKeys"
    - path: "packages/concierge/test-d/session.test-d.ts"
      provides: "Exact Session, SessionConfig, and diagnostic shape pins"
      contains: "SessionDiagnosticCode"
    - path: "packages/concierge/test-d/actions.test-d.ts"
      provides: "Existing Session fixture adapted to Promise-returning stop"
      contains: "const _session: Session"
  key_links:
    - from: "packages/concierge/test-d/transport.test-d.ts"
      to: "packages/concierge/src/types.ts"
      via: "Equals pins for the exact six-key structural contract"
      pattern: "capabilities.*status.*setTools.*onStatusChange.*onToolBatch.*respond"
    - from: "packages/concierge/test-d/session.test-d.ts"
      to: "packages/concierge/src/types.ts"
      via: "Equals pins for Session.stop, SessionConfig, and the diagnostic union"
      pattern: "Promise<void>|SessionDiagnosticCode|onDiagnostic"
---

<objective>
Amend the pre-publish public contracts that the Session runtime and reusable stub will implement.

Purpose: Establish the neutral lifecycle, awaitable drain, and safe diagnostic types before runtime work so every later plan compiles against one exact interface rather than inventing local shapes.

Output: The six-member Transport contract, Promise-returning Session.stop, exact-optional SessionConfig, closed diagnostics, and mutation-sensitive type tests.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-session-and-the-transport-seam/07-CONTEXT.md
@.planning/phases/07-session-and-the-transport-seam/07-RESEARCH.md
@.planning/phases/07-session-and-the-transport-seam/07-PATTERNS.md
@.planning/phases/07-session-and-the-transport-seam/07-VALIDATION.md

<decision_traceability>
Planner-local aliases below point to locked bullets in 07-CONTEXT.md; they add no new product decisions.

- CTX-07-01: Transport has required status and onStatusChange, with idle/connecting/connected/closed as the neutral lifecycle.
- CTX-07-10: Session.stop returns one cached Promise whose first call begins the synchronous stop transition and drains owned work.
- CTX-07-15: SessionConfig.onDiagnostic receives immutable diagnostics from a closed code vocabulary and fixed safe messages; a supplied hook replaces warnHost.
</decision_traceability>

<interfaces>
Current contracts to amend in packages/concierge/src/types.ts:
- Transport has capabilities, setTools, onToolBatch, and respond.
- Session has setContext, stage, onStageChange, and stop: () =&gt; void.
- SessionConfig has concierge, transport, and initialContext?: StageContext.

Required exact contracts:
- TransportStatus = "idle" | "connecting" | "connected" | "closed".
- Transport keys are exactly capabilities, status, setTools, onStatusChange, onToolBatch, respond.
- Session keys remain exactly setContext, stage, onStageChange, stop; stop is () =&gt; Promise&lt;void&gt;.
- SessionConfig keys are exactly concierge, transport, initialContext, onDiagnostic; both optional properties explicitly admit undefined.
- SessionDiagnostic has exactly readonly code and readonly message.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (07-01-01): Pin and implement the neutral Transport lifecycle</name>
  <read_first>
    - packages/concierge/src/types.ts — current TransportCapabilities and four-member Transport blocks.
    - packages/concierge/test-d/transport.test-d.ts — both unrelated structural transports and the exact _transportKeys predicate.
    - packages/concierge/test-d/actions.test-d.ts — assertion aliases and existing exact-key conventions.
    - .planning/phases/07-session-and-the-transport-seam/07-CONTEXT.md — Catalog publication and reconnect locked decisions, especially CTX-07-01.
    - .planning/phases/07-session-and-the-transport-seam/07-PATTERNS.md — types.ts and transport.test-d.ts exact-self analogs.
  </read_first>
  <files>packages/concierge/src/types.ts, packages/concierge/test-d/transport.test-d.ts</files>
  <behavior>
    - TransportStatus accepts exactly idle, connecting, connected, and closed and rejects every other string.
    - Transport has exactly six keys: capabilities, status, setTools, onStatusChange, onToolBatch, and respond.
    - status is readonly and onStatusChange accepts a TransportStatus callback and returns an unsubscriber.
    - Two unrelated structural Transport fixtures compile without sharing any wire or vendor vocabulary.
    - The lifecycle neutrality gate inspects only non-comment identifiers/event spellings in the `Transport` interface block, passes on the current documented baseline and the proposed neutral names, and rejects a synthetic `onReconnect` member.
  </behavior>
  <action>
Per CTX-07-01, edit the two structural transport fixtures and exact predicates first. Add status and onStatusChange to both; add one-line Expect&lt;Equals&lt;...&gt;&gt; pins for the TransportStatus union, the callback signature, the exact six-key union, and readonly status. Run the package typecheck before production changes and record the expected diagnostics proving the old four-member contract cannot satisfy the new tests; an import/load failure or unrelated diagnostic is not valid RED evidence. Before changing types.ts, also run the scoped lifecycle-neutrality command from verify against the current baseline and require it to pass; its extraction deliberately excludes the file's valid historical prose about OpenAI Realtime and WebRTC.

Then add exported TransportStatus immediately beside the transport contracts in types.ts, add readonly status: TransportStatus, and add onStatusChange: (cb: (status: TransportStatus) =&gt; void) =&gt; () =&gt; void. Preserve readonly nested TransportCapabilities, the existing setTools/onToolBatch/respond signatures, neutral vocabulary, ES2022/no-DOM compatibility, and isolated declaration annotations. Do not add onReconnect, a vendor event name, an optional lifecycle member, a timer, or a new dependency.
  </action>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge typecheck</automated>
    <automated>node -e "const fs=require('node:fs');const src=fs.readFileSync('packages/concierge/src/types.ts','utf8');const m=/export interface Transport\\s*\\{[\\s\\S]*?\\n\\}/.exec(src);if(!m)process.exit(2);const strip=function(s){return s.replace(/\\/\\*[\\s\\S]*?\\*\\//g,'').replace(/\\/\\/.*$/gm,'')};const forbidden=/\\b(?:onReconnect|onReconnected|reconnect|reconnecting|connectionstatechange|session\\.updated|response\\.done|realtimeStatus|webRtcStatus|openAIStatus|anthropicStatus|OpenAI|Anthropic|WebRTC|WebSocket)\\b/i;const block=strip(m[0]);if(forbidden.test(block))process.exit(1);const mutant=block.replace(/\\n\\}/,'\\n  readonly onReconnect: unknown;\\n}');if(!forbidden.test(mutant))process.exit(3)"</automated>
  </verify>
  <done>The public Transport lifecycle is required, neutral, readonly, and pinned to exactly six members by two structurally unrelated fixtures.</done>
  <acceptance_criteria>
    - `TransportStatus` is exported with exactly four literals and no open string arm.
    - `_transportKeys` is an `Equals` predicate over the exact six-member union, never `Assignable`.
    - Both transport fixtures implement status and onStatusChange and package typecheck exits 0.
    - The scoped non-comment Transport-block gate exits 0 on both the current baseline and final neutral lifecycle, while its in-memory `onReconnect` member mutant exits through the forbidden-identifier branch; prose elsewhere in types.ts cannot affect the result.
    - package.json files and pnpm-lock.yaml are unchanged.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (07-01-02): Pin and implement awaitable Session and safe diagnostics</name>
  <read_first>
    - packages/concierge/src/types.ts — current Session, SessionConfig, InvocationMeta exact-optional precedent, and readonly interface style.
    - packages/concierge/test-d/actions.test-d.ts — existing D-08 Session fixture and stage/change pins.
    - packages/concierge/test-d/dispatcher.test-d.ts — one-line exact signature predicate convention.
    - packages/concierge/test-d/bridge.test-d.ts — exact-key, readonly, and excess-property test conventions.
    - .planning/phases/07-session-and-the-transport-seam/07-RESEARCH.md — recommended nine diagnostic codes/messages and exact public contract.
    - .planning/phases/07-session-and-the-transport-seam/07-PATTERNS.md — session.test-d.ts assignment and exactOptionalPropertyTypes guidance.
  </read_first>
  <files>packages/concierge/src/types.ts, packages/concierge/test-d/session.test-d.ts, packages/concierge/test-d/actions.test-d.ts</files>
  <behavior>
    - Session keeps exactly four keys and stop has the exact signature () =&gt; Promise&lt;void&gt;.
    - SessionConfig requires concierge and transport and optionally accepts initialContext and onDiagnostic, including computed values typed with `| undefined`.
    - SessionDiagnosticCode is exactly the nine researched codes; SessionDiagnostic has exactly two readonly keys.
    - Extra diagnostic/config keys, mutable diagnostic fields, a void stop implementation, and an arbitrary diagnostic code fail the type program.
  </behavior>
  <action>
Create session.test-d.ts first using the repository's Expect/Equals/Assignable aliases. Pin the exact Session keys and stop signature; the exact SessionConfig key set and property types; positive computed optional assignments for StageContext | undefined and diagnostic-hook | undefined; the exact nine-code union `catalog_publish_failed | batch_dispatch_failed | response_failed | stage_listener_failed | transport_subscribe_failed | transport_unsubscribe_failed | catalog_clear_failed | abort_signal_failed | batch_without_context`; and the exact readonly two-key SessionDiagnostic. Reserve @ts-expect-error for fresh excess-property and mutation negatives. Update only the existing actions.test-d.ts Session fixture so stop returns Promise.resolve(). Run typecheck before source changes and retain only diagnostics that prove the intended contract gaps.

Then, per CTX-07-10 and CTX-07-15, change Session.stop to () =&gt; Promise&lt;void&gt;; widen SessionConfig.initialContext to `initialContext?: StageContext | undefined`; add `onDiagnostic?: ((diagnostic: SessionDiagnostic) =&gt; void) | undefined`; and export the exact SessionDiagnosticCode and readonly SessionDiagnostic definitions. Do not add caught values, subject/id fields, severity, arbitrary metadata, a second hook, or fixture controls to production types. Factory typing belongs to Plan 07-03 after the callable value exists; this task must leave the type program green without inventing a declaration-only factory.
  </action>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge typecheck</automated>
    <automated>bash -c 'test "$(rg -n "^export type SessionDiagnosticCode" packages/concierge/src/types.ts | wc -l | tr -d " ")" = 1 &amp;&amp; test "$(rg -n "^export interface SessionDiagnostic" packages/concierge/src/types.ts | wc -l | tr -d " ")" = 1'</automated>
  </verify>
  <done>Session and diagnostic contracts are exact, readonly, EOPT-safe, and mutation-sensitive before the runtime factory is introduced.</done>
  <acceptance_criteria>
    - Session has exactly four keys and stop is exactly Promise-returning in source and both type-test files.
    - SessionConfig optional properties explicitly include undefined and computed optional positives compile.
    - The diagnostic union has exactly the nine named codes; arbitrary strings and extra fields fail.
    - Diagnostic code/message fields are readonly and no public field can carry a caught value, context, arguments, result, or identifier.
    - Package typecheck exits 0 with no new cast, dependency, DOM type, or runtime export.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer transport implementation → Session | Consumer-authored lifecycle values and callbacks cross a structural public interface. |
| Runtime failure → diagnostic consumer | Failure reporting must not become a raw-data exfiltration or fatal control-flow path. |
| Source declarations → published package | Exact public shapes can drift between source, type tests, and built declarations. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation and verification |
|-----------|----------|----------|-----------|-------------|-----------------------------|
| T-07-03 | high | Spoofing / Repudiation | Transport and SessionConfig contracts | mitigate | Keep turn/delivery fields outside the new lifecycle API and add no synthesis hook; exact type tests prove only status/diagnostic members were added. |
| T-07-05 | high | Information disclosure / Denial of service | SessionDiagnostic public shape | mitigate | Closed nine-code union plus readonly `{code,message}` only; type negatives reject arbitrary/detail fields. Runtime containment is verified in Plan 07-05. |
| T-07-06 | high | Tampering | Public declarations | mitigate | Equals pins cover six Transport keys, four Session keys, Promise stop, EOPT config, readonly diagnostics, and exact code union; the scoped non-comment Transport-block gate rejects vendor lifecycle identifiers without flagging historical prose; package typecheck is blocking. |
| T-07-SC | low | Tampering | Package supply chain | accept | Research's Package Legitimacy Audit says no install is required; manifests and lockfile remain untouched and are byte-checked in Plan 07-06. |

## Residual Risk

TypeScript cannot prove that a consumer emits truthful status values or safely contains callback throws. The runtime and deterministic stub plans exercise those hostile behaviors; no high-severity declaration threat is deferred.
</threat_model>

<verification>
Run package typecheck from the workspace root. Confirm exactly the four declared files changed, no package or lockfile changed, and the type suite contains a discriminating negative for every amended modifier, key set, callback, union, and optional property.
</verification>

<success_criteria>
- Transport lifecycle is neutral, required, readonly, and exactly six members.
- The scoped lifecycle vocabulary gate passes the current/final neutral Transport block and kills its in-memory vendor-shaped member control without scanning unrelated prose.
- Session.stop is Promise-returning without widening the Session key set.
- SessionConfig is exactOptionalPropertyTypes-safe and diagnostics expose only the fixed two-field contract.
- All public amendments are pinned before any runtime or fixture depends on them.
</success_criteria>

<output>
Create .planning/phases/07-session-and-the-transport-seam/07-01-SUMMARY.md when done.
</output>
