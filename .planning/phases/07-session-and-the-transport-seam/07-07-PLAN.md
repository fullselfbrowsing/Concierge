---
phase: 07-session-and-the-transport-seam
plan: 07
type: execute
wave: 6
depends_on: ["07-06"]
files_modified:
  - packages/concierge/src/session.ts
  - packages/concierge/test/session-catalog.test.ts
  - scripts/phase-07-mutation-battery.mjs
  - .planning/phases/07-session-and-the-transport-seam/07-MUTATION-REGISTER.json
  - .planning/phases/07-session-and-the-transport-seam/07-MUTATION-EVIDENCE.json
  - .planning/phases/07-session-and-the-transport-seam/07-VALIDATION.md
  - .planning/phases/07-session-and-the-transport-seam/07-SECURITY.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [SES-01, SES-02]
user_setup: []

must_haves:
  truths:
    - "When the setTools accessor for transition B synchronously queues context C whose catalog is the already-published catalog, B's unpublished attempt is abandoned completely and C becomes the sole admission authority."
    - "After that transition, stage() reports C and a later one-call batch dispatches exactly once with the exact C context and produces exactly one transport response."
    - "The repair aborts and clears only the superseded provisional B publication; it cannot invoke B's stale setTools function, restore stale authority, weaken generation checks, or emit after stop."
    - "An exact compiled M-07-C10 mutation recreates the abandoned-publication defect and is killed only by C17, bringing immutable evidence to 31/31 with a 10/9/8/2/2 distribution."
    - "Generated mutation, protected-input, immutable-snapshot release, validation, requirement, and security-handoff facts all describe the repaired revision while TRN-02 remains unchecked and Partial for Phase 8."
  artifacts:
    - path: "packages/concierge/src/session.ts"
      provides: "Identity-guarded cleanup for a publication invalidated by a reentrant setTools accessor"
      contains: "clearPublication"
    - path: "packages/concierge/test/session-catalog.test.ts"
      provides: "Built-artifact C17 regression for accessor-to-same-published-catalog reentry and later response cardinality"
      contains: "[RED:C17:abandoned-publication-cleanup]"
    - path: "scripts/phase-07-mutation-battery.mjs"
      provides: "Exact 31-row register schema, M-07-C10 detector, bounded regeneration, release capture, and ledger enforcement"
      contains: "M-07-C10"
    - path: ".planning/phases/07-session-and-the-transport-seam/07-MUTATION-REGISTER.json"
      provides: "Immutable ordered 31-row mutation register with ten catalog rows"
      contains: "M-07-C10"
    - path: ".planning/phases/07-session-and-the-transport-seam/07-MUTATION-EVIDENCE.json"
      provides: "Generated 31/31 mutation rows, protected input hashes, and one immutable-snapshot seven-command release record"
      contains: "release"
    - path: ".planning/phases/07-session-and-the-transport-seam/07-VALIDATION.md"
      provides: "C17/M-07-C10-aware task, threat, mutation, release, and sign-off ledger"
      contains: "07-07-03"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Re-signed SES-01/SES-02 traceability with unchanged Partial TRN-02 handoff"
      contains: "M-07-C10"
    - path: ".planning/phases/07-session-and-the-transport-seam/07-SECURITY.md"
      provides: "Truthful T-07-01/T-07-02 invalidation and mandatory post-execution re-audit handoff"
      contains: "re_audit_required"
  key_links:
    - from: "packages/concierge/src/session.ts setTools accessor boundary"
      to: "publicationPending / publishingContext / publishingEpoch"
      via: "An exact current-attempt guard aborts and clears only superseded unpublished B before returning to the transition drain"
      pattern: "isCurrent|publicationIsCurrent|abortEpoch|clearPublication"
    - from: "packages/concierge/test/session-catalog.test.ts C17"
      to: "Concierge.dispatchBatch and Transport.respond"
      via: "The later batch observes context C once and one returned row yields one response attempt"
      pattern: "C17|dispatchContexts|responseHistory"
    - from: "scripts/phase-07-mutation-battery.mjs M-07-C10"
      to: "C17 exact RED marker"
      via: "Successful compile, one selected case, exact fingerprint, byte restoration, and restored-green catalog gate"
      pattern: "M-07-C10|RED:C17:abandoned-publication-cleanup"
    - from: "generated register/evidence"
      to: "07-VALIDATION.md and REQUIREMENTS.md"
      via: "verify ledgers rejects stale 30-row counts, stale release facts, absent C17/M-07-C10 citations, or a Complete TRN-02 claim"
      pattern: "31/31|10/9/8/2/2|verify ledgers"
---

<objective>
Close the verifier-discovered abandoned-publication gap without changing the public Session/Transport contract or any unrelated lifecycle behavior.

Purpose: A reentrant structural accessor must not leave admission pinned to a publication that never reached the transport; the winning same-catalog context must remain able to route and answer every accepted call.

Output: One minimal runtime repair, one exact built-artifact regression, one new compiled catalog mutant, fully regenerated 31-row/release evidence, re-signed SES-01/SES-02 ledgers, and a truthful mandatory security re-audit handoff.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
@/Users/lakshman/.codex/get-shit-done/references/planner-gap-closure.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-session-and-the-transport-seam/07-CONTEXT.md
@.planning/phases/07-session-and-the-transport-seam/07-RESEARCH.md
@.planning/phases/07-session-and-the-transport-seam/07-PATTERNS.md
@.planning/phases/07-session-and-the-transport-seam/07-VERIFICATION.md
@.planning/phases/07-session-and-the-transport-seam/07-REVIEW.md
@.planning/phases/07-session-and-the-transport-seam/07-REVIEW-FIX.md
@.planning/phases/07-session-and-the-transport-seam/07-SECURITY.md
@.planning/phases/07-session-and-the-transport-seam/07-VALIDATION.md
@.planning/phases/07-session-and-the-transport-seam/07-03-SUMMARY.md
@.planning/phases/07-session-and-the-transport-seam/07-06-SUMMARY.md

<gap_traceability>
- Verification gap: latest-wins transition B is invalidated when its `transport.setTools` accessor queues C, but the early return leaves B's `publicationPending`, `publishingContext`, and `publishingEpoch` installed.
- Root-cause category: execution miss. Plan 07-03 specified latest-generation checks and publication reconciliation, but its matrix covered a successful B invocation followed by C and stop-from-accessor; it did not cover C invalidating B during the accessor read before B reached the transport.
- Failed truths closed here: verification truths 2, 15, 18, 19, and 25; SES-02 is blocked directly and SES-01's latest-wins authority proof is incomplete.
- Deferred item excluded: literal TRN-02 still requires Phase 8's full consent-kernel exercise and must remain unchecked/Partial.
</gap_traceability>

<decision_traceability>
Planner-local aliases preserve the locked bullets in 07-CONTEXT.md and add no new product decisions.

- CTX-07-01/02: neutral reconnect and hot initial/empty publication remain unchanged.
- CTX-07-03: every setContext retains the newest context and publishes only on catalog-reference change; Task 1 closes its uncovered accessor invalidation ordering.
- CTX-07-04: fixed-catalog stop-first behavior remains unchanged and regression-covered.
- CTX-07-05/06: one FIFO captures the winning arrival context/epoch; Task 1 restores progress and exact C authority after B is abandoned.
- CTX-07-07: Session still preserves all envelope evidence and replaces only signal.
- CTX-07-08: every accepted occurrence dispatches once and every returned row receives one non-retried response attempt; C17 proves this after the repaired transition.
- CTX-07-09: the hot frozen standalone factory and direct single-instance guard remain unchanged.
- CTX-07-10/11: cached stop, publication-token invalidation, independent cleanup, and zero post-stop output remain unchanged; the repair must not weaken the review-fixed accessor/stop ordering.
- CTX-07-12: tokenized stage-subscriber semantics remain unchanged.
- CTX-07-13/14: the reusable zero-I/O stub and both frozen profiles remain test-only and unchanged.
- CTX-07-15: diagnostics stay fixed, immutable, detail-free, and contained; the repair adds no error/detail path.
</decision_traceability>

<interfaces>
Existing runtime seam in packages/concierge/src/session.ts:
- `clearPublication(epoch, attemptToken)` clears only when both the current publishing epoch and publication token match.
- `publicationIsCurrent(attemptToken)` requires a live lifecycle and exact publication token.
- `isCurrent(record)` requires a live lifecycle plus the latest requested generation/context.
- `processContext(record)` installs B's publication state, reads the structural `transport.setTools` accessor, rechecks freshness, invokes the captured function only while current, records actual transport publication, clears pending state, and confirms authority.
- `acceptBatch(batch)` deliberately selects publishing context/epoch while `publicationPending`, otherwise confirmed context/epoch.

Required new proof identities:
- Runtime case: C17 with exact marker `[RED:C17:abandoned-publication-cleanup]`.
- Mutation row: M-07-C10, group `catalog`, exact intended case set `[C17]`.
- Final immutable totals: 31 rows; 10 catalog / 9 routing / 8 lifecycle / 2 diagnostics / 2 package-guard (`10/9/8/2/2`).
- Bounded shards remain ten: C01-C03, C04-C06, C07-C10, R01-R04, R05-R08, R09-R09, L01-L04, L05-L08, D01-D02, P01-P02.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (07-07-01): Reproduce and clear the accessor-abandoned publication</name>
  <read_first>
    - packages/concierge/src/session.ts — modify the `processContext` setTools-accessor freshness branch; preserve `clearPublication`, `publicationIsCurrent`, `isCurrent`, `enterStopped`, `acceptBatch`, and pump gating semantics.
    - packages/concierge/test/session-catalog.test.ts — modify the built-dist suite; reuse `accessorTransport`, catalog/context fixtures, exact C-marker style, explicit counters, and registry reset.
    - .planning/phases/07-session-and-the-transport-seam/07-VERIFICATION.md — primary failed truth, exact counterexample, affected artifacts, and missing closure items.
    - .planning/phases/07-session-and-the-transport-seam/07-CONTEXT.md — CTX-07-03, CTX-07-05/06, CTX-07-08, and CTX-07-10/11 locked behavior.
    - .planning/phases/07-session-and-the-transport-seam/07-REVIEW-FIX.md — preserve the fixed stop-from-accessor, per-boundary generation, and immutable release semantics.
    - .planning/phases/07-session-and-the-transport-seam/07-SECURITY.md — current T-07-01/T-07-02 authority and routing claims invalidated by this exact ordering.
  </read_first>
  <files>packages/concierge/test/session-catalog.test.ts, packages/concierge/src/session.ts</files>
  <behavior>
    - C17 RED: initial context A publishes catalog A; `setContext(B)` reaches the second `setTools` accessor read; that getter synchronously calls `setContext(C)` where `catalogFor(C) === catalogA`, the reference already held by the transport.
    - C17 RED: before the fix, the exact marker reports stage C with zero later dispatches/responses because abandoned B remains publication-pending; failure must not come from a missing dist artifact, incidental throw, or unrelated assertion.
    - C17 GREEN: B's returned setTools function is never invoked, publication history remains exactly `[catalogA]`, `stage()` and the stage notification identify C, and no B context/catalog becomes confirmed authority.
    - C17 GREEN: one valid later one-call batch enters `dispatchBatch` exactly once with the exact C object and produces exactly one non-retried `respond` attempt for its one row.
    - Existing accessor-stop, resolver/capability-generation, C01-C16, L01-L18, and J01-J18 behavior remains green.
  </behavior>
  <action>
Add C17 before production changes and run the built-artifact focused command after rebuilding. Use the existing structural accessor wrapper, but make the second `setTools` getter synchronously enqueue C while outer B is draining. Arrange `catalogFor(A) === catalogFor(C) === catalogA` by reference, `catalogFor(B) === catalogB`, and distinct stage values sufficient to observe final C. The getter may return a callable sentinel, but assert it is never invoked. After `setContext(B)` returns, assert exact publication history `[catalogA]`, final `stage() === stageFor(C)`, the exact C notification sequence, and zero premature dispatch/response. Then synchronously emit one valid one-call batch, await its explicit completion, and assert dispatch contexts equal `[contextC]` by identity, dispatch count is one, and response history has exactly the returned call/result row once. Give the load-bearing assertion the unique exact marker `[RED:C17:abandoned-publication-cleanup]`.

Capture the pre-fix RED observation, then make the minimal change in `processContext`. Immediately after the structural `setTools` accessor returns, distinguish two invalidation reasons before any invocation: if stop or a newer publication token already invalidated the attempt, return without touching stopped state; if the context record alone was superseded while this exact `(epoch, attemptToken)` is still the active unpublished attempt, abort only that provisional epoch, clear only that matching publication state through the existing identity-guarded helper, and return. Only a still-current record may invoke the captured setTools function. Do not record B in `publishedCatalog`/`publishedEpoch`, confirm B, start the pump, notify B, diagnose, or call outside code from the abandonment branch. Let the existing outer transition drain process C, whose same-`publishedCatalog` fast path confirms C and re-enables admission.

Preserve the review-fixed rules verbatim in behavior: never invoke a stale function returned after stop; keep an immediate `isCurrent` check after every outside resolver/capability/accessor boundary; retain stop's publication-token invalidation before cleanup; retain actual-publication reconciliation after a successful setTools invocation; and keep all no-binding catches/fixed errors. Add no timer, network, DOM/vendor primitive, package, public API, or raw diagnostic detail.
  </action>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge build &amp;&amp; pnpm exec vitest run packages/concierge/test/session-catalog.test.ts --testNamePattern="^\[C17\]"</automated>
    <automated>pnpm --filter @fullselfbrowsing/concierge build &amp;&amp; pnpm exec vitest run packages/concierge/test/session-catalog.test.ts</automated>
    <automated>pnpm --filter @fullselfbrowsing/concierge typecheck</automated>
    <automated>bash -c '! rg -n "AbortController|setTimeout|setInterval|Promise\\.race|catch[[:space:]]*\\(" packages/concierge/src/session.ts'</automated>
  </verify>
  <done>The getter-abandoned B attempt can no longer retain admission authority; C confirms without republishing A, and the next batch dispatches/responds exactly once under C.</done>
  <acceptance_criteria>
    - The pre-fix C17 run builds successfully, executes exactly the named test, and fails on `[RED:C17:abandoned-publication-cleanup]`; the restored/fixed run executes that same case and exits 0.
    - C17 observes exact publication references `[catalogA]`, final stage C, one exact C notification, zero B setTools invocation, one later dispatch with `contextC` by identity, and one response attempt.
    - The abandonment branch can clear only the matching epoch/token and abort only B's provisional epoch; it cannot modify published/confirmed authority or call transport/listener/diagnostic code.
    - Stop-from-accessor remains fail-closed with the empty catalog final; stale resolver/capability records remain inert; successful B publication followed by C still uses the existing actual-published reconciliation semantics.
    - The full catalog suite and package typecheck exit 0 with no public contract, dependency, manifest, lockfile, timer, network, DOM, vendor, or caught-detail change.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 (07-07-02): Register M-07-C10 and regenerate all 31 mutation rows</name>
  <read_first>
    - scripts/phase-07-mutation-battery.mjs — modify immutable definitions, expected id/group maps, required task ids, named-case checks, self-tests, shard/ledger constants, and atomic refresh/execution logic.
    - .planning/phases/07-session-and-the-transport-seam/07-MUTATION-REGISTER.json — replace only through harness refresh; current schemaVersion 2 register has 30 ordered rows and digest c2fc5caca8b0657bf3436e66227761d6c29ba64e945ac8e0ff622939a4180484.
    - .planning/phases/07-session-and-the-transport-seam/07-MUTATION-EVIDENCE.json — replace only through harness refresh/runs; current evidence has 30 green rows plus protected-input/release facts.
    - packages/concierge/src/session.ts — exact unique abandoned-publication cleanup literal introduced by Task 1.
    - packages/concierge/test/session-catalog.test.ts — exact C17 title, marker, and behavior introduced by Task 1.
    - .planning/phases/07-session-and-the-transport-seam/07-VERIFICATION.md — mutation gap and exact requirement to add a compiled behavior-sensitive cleanup detector.
    - .planning/phases/07-session-and-the-transport-seam/07-06-PLAN.md — immutable schema, non-vacuity, bounded execution, restoration, protected-input, release, and ledger contracts to preserve.
    - .planning/phases/07-session-and-the-transport-seam/07-VALIDATION.md — every current 30-row/group/shard/C01-C16/14-task assumption that the harness must reject after this plan.
  </read_first>
  <files>scripts/phase-07-mutation-battery.mjs, .planning/phases/07-session-and-the-transport-seam/07-MUTATION-REGISTER.json, .planning/phases/07-session-and-the-transport-seam/07-MUTATION-EVIDENCE.json</files>
  <action>
Add ordered catalog mutant M-07-C10 after M-07-C09. Its exact once-occurring source literal must be the Task 1 branch that aborts/clears the still-current unpublished B attempt after `isCurrent(record)` becomes false; its compiling replacement must omit that abandonment so the stale `publicationPending`/B admission defect returns without weakening generation checks globally. Map it to exactly `intendedCaseIds: ["C17"]` and the exact marker `[RED:C17:abandoned-publication-cleanup]`. Do not let C15, C16, the whole catalog file, a build error, zero selected tests, or an incidental exception receive credit.

Change every executable schema and ledger assumption coherently: catalog ids become C01-C10, total ids/rows become 31, group counts become `10/9/8/2/2`, `validateRequiredMappings` includes only C17 for M-07-C10, independent mutation identity includes C10, named catalog markers extend through C17, and `REQUIRED_TASK_IDS` extends through 07-07-01/02/03. Update approval/outcome/distribution/shard validators from 30/30 and 9/9/8/2/2 to 31/31 and 10/9/8/2/2, with the third catalog shard exactly C07-C10. Update all self-test fixtures and negative controls: exact 31 pending rows; missing/reordered/duplicate M-07-C10 rejection; wrong neighboring C16 marker rejection; no-op or multi-occurrence cleanup edit rejection; stale 30-row distribution/outcome/approval rejection; missing C17 marker and missing 07-07 task-row rejection. Preserve exact object-key schemas, atomic writes, immutable register/evidence digest agreement, successful compile, nonzero exact detector set, exact failure fingerprint, changed/restored target hashes, restored-green gate, clean scoped tree, and revision binding.

Run self-test, then refresh through the harness; never hand-edit either JSON artifact. Refresh must create exactly 31 pending rows, a new shared register digest, the same exact three protected input paths hashed from raw bytes, and null release evidence. Because Task 1 and the harness change revision inputs, rerun every row rather than carrying forward 30 old green results. Execute exactly ten contiguous same-group shards: C01-C03, C04-C06, C07-C10, R01-R04, R05-R08, R09-R09, L01-L04, L05-L08, D01-D02, P01-P02. Each shard remains at most four rows. Verify each group and then all 31 rows. If any definition/detector changes after refresh, refresh again and rerun every affected revision-bound row; never weaken a marker, count, source-literal uniqueness check, restoration rule, or fail-closed gate to obtain green evidence.
  </action>
  <verify>
    <automated>node scripts/phase-07-mutation-battery.mjs self-test &amp;&amp; node scripts/phase-07-mutation-battery.mjs refresh</automated>
    <automated>node -e "const fs=require('node:fs');const r=JSON.parse(fs.readFileSync('.planning/phases/07-session-and-the-transport-seam/07-MUTATION-REGISTER.json','utf8'));const e=JSON.parse(fs.readFileSync('.planning/phases/07-session-and-the-transport-seam/07-MUTATION-EVIDENCE.json','utf8'));const m=r.mutants.find(x=&gt;x.id==='M-07-C10');if(r.mutants.length!==31||r.expectedCatalogIds.length!==10||e.rows.length!==31||e.rows.some(x=&gt;x.status!=='pending')||r.registerDigest!==e.registerDigest||JSON.stringify(m?.intendedCaseIds)!=='[\"C17\"]')process.exit(1)"</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs run range M-07-C01 M-07-C03</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs run range M-07-C04 M-07-C06</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs run range M-07-C07 M-07-C10 &amp;&amp; node scripts/phase-07-mutation-battery.mjs verify catalog</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs run range M-07-R01 M-07-R04</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs run range M-07-R05 M-07-R08</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs run range M-07-R09 M-07-R09 &amp;&amp; node scripts/phase-07-mutation-battery.mjs verify routing</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs run range M-07-L01 M-07-L04</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs run range M-07-L05 M-07-L08 &amp;&amp; node scripts/phase-07-mutation-battery.mjs verify lifecycle</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs run range M-07-D01 M-07-D02 &amp;&amp; node scripts/phase-07-mutation-battery.mjs verify diagnostics</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs run range M-07-P01 M-07-P02 &amp;&amp; node scripts/phase-07-mutation-battery.mjs verify all</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs verify inputs</automated>
  </verify>
  <done>The immutable register/evidence system contains 31 fully regenerated green rows, and M-07-C10 compiles, runs only C17, recreates the abandoned-publication defect, and restores the exact target and gate.</done>
  <acceptance_criteria>
    - Register order is exactly C01-C10, R01-R09, L01-L08, D01-D02, P01-P02; distribution is 10/9/8/2/2 and register/evidence share one newly generated digest.
    - M-07-C10's source literal occurs exactly once, its replacement compiles, exactly C17 runs nonzero, the exact C17 marker fires, and C15/C16 or unrelated failures cannot satisfy it.
    - All 31 rows are green with `compiled`, `testsRan &gt; 0` or the exact package equivalent, `detectorSatisfied`, `targetRestored`, `restoredGreen`, and `scopedTreeClean` true and a current revision digest.
    - Exactly ten named shards cover every row once, the C07-C10 shard has four rows, and no old 30-row/9-catalog assumption survives in executable harness/schema/self-test logic.
    - The three protected inputs verify byte-identically; neither package manifest nor pnpm-lock.yaml changes; generated JSON is written only by the harness.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3 (07-07-03): Regenerate release facts, re-sign ledgers, and hand security back for audit</name>
  <read_first>
    - .planning/phases/07-session-and-the-transport-seam/07-MUTATION-EVIDENCE.json — modify only through `verify ledgers`; consume its 31 green rows, exact input hashes, and newly generated release object.
    - scripts/phase-07-mutation-battery.mjs — current `verify all`, `verify inputs`, immutable-snapshot `verify ledgers`, exact release schema, and final ledger validators from Task 2.
    - .planning/phases/07-session-and-the-transport-seam/07-VALIDATION.md — modify every stale C01-C16, 30-row, 9/9/8/2/2, shard, task-count, threat, runtime, digest, and approval claim.
    - .planning/REQUIREMENTS.md — modify only SES-01/SES-02 traceability after evidence is green; preserve all other requirements and the exact TRN-02 Partial handoff.
    - .planning/phases/07-session-and-the-transport-seam/07-SECURITY.md — invalidate the superseded T-07-01/T-07-02 closure pending an independent post-execution security re-audit; preserve other threat findings.
    - .planning/phases/07-session-and-the-transport-seam/07-VERIFICATION.md — failed truths and statement that the prior security zero-open claim is superseded for this ordering.
    - .planning/phases/07-session-and-the-transport-seam/07-REVIEW-FIX.md — fixed accessor-stop/generation/release behaviors that must remain cited and green.
    - .planning/phases/07-session-and-the-transport-seam/07-MUTATION-REGISTER.json — new immutable digest, 31 definitions, M-07-C10 mapping, and 10/9/8/2/2 distribution.
    - packages/concierge/test/session-catalog.test.ts — C01-C17 exact runtime evidence.
  </read_first>
  <files>.planning/phases/07-session-and-the-transport-seam/07-MUTATION-EVIDENCE.json, .planning/phases/07-session-and-the-transport-seam/07-VALIDATION.md, .planning/REQUIREMENTS.md, .planning/phases/07-session-and-the-transport-seam/07-SECURITY.md</files>
  <action>
Require `verify all` and `verify inputs` before signing anything. Invoke `verify ledgers` once to materialize a new release object from one read-only temporary snapshot of the complete revision manifest; while the human ledgers still contain 30-row facts, the command must fail closed only on those stale facts after atomically recording the release. Read the generated evidence rather than predicting counts. Require exact zero exits for build, typecheck, test, check:artifact, check:deps, check:pack, and check:node-floor; require a fully green test object with positive file/total counts and zero failed/pending/todo; require the pre/around/post snapshot digest checks to agree. Do not substitute seven workspace commands for this immutable-snapshot run.

Update 07-VALIDATION.md from the generated artifacts. Add green task rows 07-07-01, 07-07-02, and 07-07-03; extend catalog coverage through C17 and M-07-C10; record the exact new register digest, 31/31 outcome, 10/9/8/2/2 distribution, C07-C10 third shard, protected-input hashes, generated release digest/date/exits/counts, and unchanged 69/54/15/F7/tarball/dependency/Node-floor facts. Amend the behavioral matrix with the accessor→C→already-published-A ordering and exact later C dispatch/response observation. Reconcile T-07-01 and T-07-02 technical evidence with C17/M-07-C10, while preserving T-07-03 through T-07-06 and the accepted low supply-chain disposition. Set complete/true/true and approve 31/31 only after all generated facts agree.

Update only the SES-01 and SES-02 traceability rows in REQUIREMENTS.md. SES-01 must cite C01-C17 and M-07-C01..C10 for latest-wins publication authority. SES-02 must add C17/M-07-C10 to its existing FIFO/response evidence and may remain checked/Complete only after the exact later batch produces one dispatch and one response. Leave SES-03/SES-04 and every unrelated checkbox/status untouched. Leave TRN-02's requirement checkbox unchecked and preserve exactly: `Partial — Phase 7 delivers U01-U08 reusable no-network fixture and Session seam/package proof; Phase 8 must reuse this exact fixture to exercise the consent kernel before TRN-02 can be Complete.`

Do not let the pre-gap security report continue to claim a current independent audit. In 07-SECURITY.md, add an explicit gap-closure handoff and change frontmatter to `status: re_audit_required`, `threats_closed: 4`, and `threats_open: 2`; mark only T-07-01 and T-07-02 as requiring re-audit because the verifier invalidated their prior completeness claims. Cite C17, M-07-C10, the new register/release digests, and the preserved review regressions as evidence for the auditor to inspect, but do not self-assign `secured` or zero open threats. Preserve T-07-03 through T-07-06 and T-07-SC dispositions verbatim. The mandatory downstream `$gsd-secure-phase 7` audit must independently restore `status: secured` and `threats_open: 0` with exact current evidence before phase verification/advancement.

Run `verify ledgers` again after validation/requirements edits; it must rerun all seven commands from one fresh immutable snapshot and reject any stale count, digest, task row, C17/M-07-C10 citation, 30-row phrase, or TRN-02 completion. Then rerun `verify all` and `verify inputs`. Do not modify package.json, packages/concierge/package.json, pnpm-lock.yaml, public contracts, fixture/package boundaries, historical plans 07-01 through 07-06, or the verifier report.
  </action>
  <verify>
    <automated>node scripts/phase-07-mutation-battery.mjs verify all &amp;&amp; node scripts/phase-07-mutation-battery.mjs verify inputs</automated>
    <automated>node scripts/phase-07-mutation-battery.mjs verify ledgers</automated>
    <automated>node -e "const fs=require('node:fs');const e=JSON.parse(fs.readFileSync('.planning/phases/07-session-and-the-transport-seam/07-MUTATION-EVIDENCE.json','utf8'));const exits=e.release?.commandExits??{};const tests=e.release?.tests??{};if(e.rows.length!==31||e.rows.some(x=&gt;x.status!=='green')||Object.keys(exits).length!==7||Object.values(exits).some(x=&gt;x!==0)||tests.exitCode!==0||tests.numPassedTests!==tests.numTotalTests||tests.numFailedTests!==0||tests.numPendingTests!==0||tests.numTodoTests!==0)process.exit(1)"</automated>
    <automated>bash -c 'rg -q "^status: re_audit_required$" .planning/phases/07-session-and-the-transport-seam/07-SECURITY.md &amp;&amp; rg -q "^threats_open: 2$" .planning/phases/07-session-and-the-transport-seam/07-SECURITY.md &amp;&amp; rg -q "C17.*M-07-C10|M-07-C10.*C17" .planning/phases/07-session-and-the-transport-seam/07-SECURITY.md'</automated>
    <automated>bash -c 'rg -q "31/31 green" .planning/phases/07-session-and-the-transport-seam/07-VALIDATION.md &amp;&amp; rg -q "10/9/8/2/2" .planning/phases/07-session-and-the-transport-seam/07-VALIDATION.md &amp;&amp; rg -q "M-07-C10" .planning/REQUIREMENTS.md &amp;&amp; rg -q "^- \[ \] \*\*TRN-02\*\*" .planning/REQUIREMENTS.md'</automated>
    <automated>git diff --exit-code -- package.json packages/concierge/package.json pnpm-lock.yaml</automated>
  </verify>
  <done>All generated and human ledgers describe the repaired 31-row revision exactly, SES-01/SES-02 are re-signed, TRN-02 remains Partial, and the superseded security closure is visibly blocked on mandatory independent re-audit.</done>
  <acceptance_criteria>
    - Evidence has exactly 31 green rows, the new shared register digest, unchanged three protected input hashes, and a current immutable-snapshot release object with seven zero exits and fully green measured test counts.
    - 07-VALIDATION contains exactly seventeen green task rows through 07-07-03, C01-C17, M-07-C01..C10, 31/31, 10/9/8/2/2, the C07-C10 shard, current runtime/release facts, and approval matching the generated digest.
    - REQUIREMENTS re-signs SES-01/SES-02 with C17/M-07-C10, changes no unrelated requirement, and retains the exact unchecked/Partial Phase 8 TRN-02 handoff.
    - 07-SECURITY no longer reports `secured` or `threats_open: 0`; it truthfully reopens only T-07-01/T-07-02 and names the exact evidence the mandatory security auditor must verify.
    - `verify ledgers`, `verify all`, and `verify inputs` all exit 0 after restoration; package manifests and lockfile are byte-identical and no dependency/install, vendor SDK, network, timer, public API, or raw-detail path was added.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Structural Transport accessor → Session publication state | Reading a consumer-controlled property can synchronously enqueue context, stop, or throw before any catalog reaches the transport. |
| Publication state → batch admission/FIFO | A stale pending context can capture valid later work under authority the transport never received and permanently block dispatch/response progress. |
| Mutation definitions → generated evidence | A build-only, wrong-case, stale-count, or non-restored mutation can falsely close an authority branch. |
| Generated evidence → validation/requirements/security ledgers | Human claims can remain green after a verifier invalidates the exact behavior they cite. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation and verification |
|-----------|----------|----------|-----------|-------------|-----------------------------|
| T-07-01 | high | Elevation of privilege / Tampering | `processContext` publication/accessor authority | mitigate | Identity-guard the unpublished B abandonment, abort/clear only its epoch/token, and require C17 plus compiled M-07-C10 before re-signing; retain all review-fixed generation/stop cases. |
| T-07-02 | high | Tampering / Repudiation / Denial of service | publication-gated FIFO and response cardinality | mitigate | C17 observes final context C, one later dispatch, and one response; M-07-C10 must recreate zero progress and be killed exactly. |
| T-07-04 | high | Tampering / Denial of service | stop/accessor reentrancy | mitigate | Preserve publication-token invalidation before cleanup, never invoke a stale returned function, and run full C/L/J regression plus all existing mutations. |
| T-07-05 | high | Information disclosure | accessor/failure handling | mitigate | Reuse fixed no-binding error paths; add no diagnostic from ordinary supersession and scan for caught bindings/raw detail. |
| T-07-06 | high | Tampering | mutation/release/ledger integrity | mitigate | Exact 31-id schema, compiled named detector, byte restoration, atomic evidence, full rerun, three protected hashes, one immutable release snapshot, and fail-closed ledger validation. |
| T-07-SC | low | Tampering | package supply chain | accept | No install, manifest, lockfile, dependency, or vendor package change occurs; exact protected hashes and check:deps/check:pack remain blocking. |

## Residual Risk

The gap repair receives technical mutation/release evidence in this plan, but the previous independent security report was authored before C17/M-07-C10 existed. It remains explicitly non-secured until the mandatory post-execution security audit independently reviews T-07-01/T-07-02 against the repaired revision. No high-severity threat is silently accepted.
</threat_model>

<source_coverage_audit>
| Source | ID | Feature or constraint | Plan coverage | Status | Notes |
|--------|----|-----------------------|---------------|--------|-------|
| GOAL | — | Session owns the catalog↔transport loop so navigation/reconnect cannot leave stale tools, with no network | 07-03, 07-04, 07-07 | COVERED | 07-07 closes the only verifier-discovered disconnected accessor-reentry flow and proves later transport progress. |
| REQ | SES-01 | Publish current stage catalog on start/change/reconnect | 07-01, 07-03, 07-04, 07-06, 07-07 | COVERED | C17/M-07-C10 complete latest-wins authority without altering established start/change/reconnect behavior. |
| REQ | SES-02 | Route each batch through dispatch and return one result per call | 07-03, 07-04, 07-06, 07-07 | COVERED | C17 proves the formerly dropped later occurrence dispatches/responds once under C. |
| REQ | SES-03 | Carry turn identity and delivery hook intact | 07-01, 07-04, 07-06 | COVERED | Existing lazy facade and J07-J18 remain unchanged and fully rerun. |
| REQ | SES-04 | Stop unregisters and cancels/drains work | 07-01, 07-03, 07-05, 07-06 | COVERED | Existing accessor-stop and lifecycle evidence is a preservation gate, not expanded scope. |
| REQ | TRN-02 | Configurable no-network stub exercises the full consent kernel | 07-01 through 07-06, then Phase 8 | COVERED (Phase 7 seam); FINAL PARTIAL | 07-07 uses the same no-I/O fixture but does not implement consent; checkbox and exact Phase 8 handoff remain unchanged. |
| RESEARCH | architecture/catalog | Factory-local serialized latest-wins publication with separate publishing, published, and confirmed authority | 07-03, 07-07 | COVERED | The current unpublished B attempt is abandoned before queued same-published-A context C takes the fast path. |
| RESEARCH | routing/lifecycle | Publication-gated FIFO, arrival context, exact occurrence/response cardinality, and stop-first teardown | 07-03, 07-04, 07-05, 07-07 | COVERED | C17 joins publication repair to one later C dispatch/response; full existing routing/lifecycle rows rerun. |
| RESEARCH | diagnostics/security | Fixed immutable detail-free diagnostics and independent security evidence | 07-01, 07-05, 07-06, 07-07 | COVERED | Ordinary supersession emits no diagnostic; prior security claims are reopened pending independent re-audit. |
| RESEARCH | fixture/package | Reusable frozen zero-I/O test fixture remains absent from production and tarball | 07-02, 07-06 | COVERED | No fixture/package change; P01, pack, and release gates rerun. |
| RESEARCH | evidence | Built-dist cases, compiled mutations, immutable release snapshot, exact ledgers | 07-03 through 07-07 | COVERED | Exact total is 31 with ten catalog rows; every revision-bound row and all seven release commands regenerate. |
| RESEARCH | packages | No package install or manifest/lockfile change | 07-01, 07-02, 07-06, 07-07 | COVERED | Protected inputs and no-diff gate are blocking. |
| CONTEXT | CTX-07-01 | Neutral status lifecycle and connected replay | 07-01, 07-03, 07-06 | COVERED | Unchanged; C04/C09-C14 and mutations rerun. |
| CONTEXT | CTX-07-02 | Hot initial context or frozen empty catalog/null stage | 07-03, 07-06 | COVERED | Unchanged; C01/C02 rerun. |
| CONTEXT | CTX-07-03 | Retain newest context; publish by catalog identity; notify by stage value | 07-03, 07-04, 07-06, 07-07 | COVERED | C17 is the missing accessor→same-published-catalog ordering. |
| CONTEXT | CTX-07-04 | Fixed catalog stops before detail-free identity-change failure | 07-03, 07-05, 07-06 | COVERED | Preserved; fixed transport and accessor-stop cases rerun. |
| CONTEXT | CTX-07-05 | Session-wide complete-batch FIFO | 07-03, 07-04, 07-06, 07-07 | COVERED | C17 proves progress after abandoned publication cleanup. |
| CONTEXT | CTX-07-06 | Arrival context/epoch and old-work cancellation | 07-03, 07-04, 07-06, 07-07 | COVERED | B provisional epoch aborts; later batch captures exact C. |
| CONTEXT | CTX-07-07 | Preserve envelope evidence and compose only signal | 07-04, 07-06 | COVERED | Unchanged and fully rerun. |
| CONTEXT | CTX-07-08 | One dispatch occurrence and one non-retried response per returned row | 07-03, 07-04, 07-06, 07-07 | COVERED | C17 adds exact post-transition one/one observation. |
| CONTEXT | CTX-07-09 | Hot standalone frozen transactional factory | 07-03, 07-05, 07-06 | COVERED | No public/factory shape change. |
| CONTEXT | CTX-07-10 | Cached Promise stop and complete drain | 07-01, 07-03, 07-05, 07-06 | COVERED | Stop token semantics are a blocking regression gate. |
| CONTEXT | CTX-07-11 | Mark-first independent teardown and no post-stop output | 07-03, 07-05, 07-06 | COVERED | Existing stop-from-accessor cases prove the repair cannot invoke stale work after stop. |
| CONTEXT | CTX-07-12 | Monotonic tokenized reentrant stage subscribers | 07-05, 07-06 | COVERED | Unchanged and fully rerun. |
| CONTEXT | CTX-07-13 | Reusable test-only non-exported stub | 07-02, 07-03, 07-06 | COVERED | Unchanged; package exclusion reruns. |
| CONTEXT | CTX-07-14 | Frozen profiles, explicit controls/failures/histories, no I/O | 07-02, 07-06 | COVERED | C17 reuses structural controls without adding modality/vendor logic. |
| CONTEXT | CTX-07-15 | Narrow immutable safe replacement diagnostics | 07-01, 07-03, 07-04, 07-05, 07-06 | COVERED | No new diagnostic path; all diagnostic mutations rerun. |
| CONTEXT | Deferred | Consent kernel, adapters, real vendors, devtools, broad telemetry | Phase 8/9/later | EXCLUDED | None appears in 07-07. |
</source_coverage_audit>

<verification>
Run C17 against the built artifact, the complete catalog/type gates, all ten mutation shards, all group/all/input verifiers, and `verify ledgers`' seven-command immutable-snapshot release. Confirm register/evidence/validation/requirements use 31/31 and 10/9/8/2/2, the security report is explicitly pending re-audit for T-07-01/T-07-02, and manifests/lockfile remain byte-identical.
</verification>

<post_execution_security_gate>
After 07-07-SUMMARY.md exists, the orchestrator must run `$gsd-secure-phase 7`. Do not re-run final phase verification, mark Phase 7 secured, or advance to Phase 8 until the independent audit rewrites 07-SECURITY.md with `status: secured`, `threats_open: 0`, and exact current C17/M-07-C10/register/release evidence for T-07-01 and T-07-02.
</post_execution_security_gate>

<success_criteria>
- The exact accessor→C→already-published-A counterexample is green against dist, and one later batch dispatches/responds once under C.
- Only the superseded unpublished B attempt is aborted/cleared; review-fixed stop/accessor/generation and all other Session semantics remain green.
- M-07-C10 is a compiled, exact, behaviorally sensitive C17 detector and all 31 revision-bound mutation rows regenerate green with 10/9/8/2/2 distribution.
- Protected inputs remain byte-identical and one immutable snapshot records all seven release exits and exact fully green test counts.
- Validation and SES-01/SES-02 traceability match generated facts; TRN-02 remains unchecked/Partial and no unrelated requirement changes.
- Security no longer falsely reports zero open threats from pre-gap evidence and blocks advancement until the mandatory independent re-audit closes T-07-01/T-07-02 on the repaired revision.
</success_criteria>

<output>
Create .planning/phases/07-session-and-the-transport-seam/07-07-SUMMARY.md when done.
</output>
