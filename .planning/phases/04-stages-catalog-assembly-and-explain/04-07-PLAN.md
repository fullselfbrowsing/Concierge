---
phase: 04-stages-catalog-assembly-and-explain
plan: 07
type: execute
wave: 4
depends_on: ["04-04", "04-05", "04-06"]
files_modified:
  - packages/concierge/test/concierge.test.ts
autonomous: true
requirements: [CAT-03, STG-01, STG-02, STG-03, STG-04, SEC-03, DX-01]

must_haves:
  truths:
    - "Every rule this phase added was observed to have a test that goes red when the rule is removed."
    - "Every mutant that reported PASS was confirmed to have COMPILED and RUN TESTS, not to have failed at the build step."
    - "Every mutant literal count was re-taken unfiltered against the final tree, not inherited from research."
    - "The two behaviours no single-literal mutant can produce are written into the test file rather than faked."
  artifacts:
    - path: "packages/concierge/test/concierge.test.ts"
      provides: "The working-literal record for the four mutants whose obvious spelling does not work"
      contains: "M-04-"
  key_links:
    - from: "scripts/mutate-and-prove.sh"
      to: "packages/concierge/src/concierge.ts"
      via: "literal substitution against a clean, tracked tree"
      pattern: "mutate-and-prove"
---

<objective>
Run the sixteen-mutant battery against the final tree, confirm each PASS actually compiled and ran
tests, and write the four respelled literals into `test/concierge.test.ts` so the next reader does
not rediscover them.

Purpose: a validation rule with no observed mutant is a claim, not a guarantee. Two of this phase's
mutants were **unrunnable as written** and had to be designed around in the source rather than
discovered here: `Object.freeze(` occurs **four** times in `concierge.ts` (the three assembly freezes
plus `DISPATCH_NOT_IMPLEMENTED`'s module-scope one), and `stageFor`'s and `explain`'s iterations must
use distinct spellings or M-04-4's literal is not unique. A third — M-04-6 — has a bare prefix
(`return warnStage(`) that occurs **twice** in `runMatch`, so its literal is the complete statement
recorded in `04-03-SUMMARY.md`, never the prefix. Two more —
M-04-7 and M-04-12 — were written against shapes the corrected implementation does not have. The
harness also reports a **vacuous** `PASS: gate fired (exit 1), tree clean` for a mutant that fails to
compile, having run zero tests, so every PASS needs its gate output read.

Output: sixteen observed mutants, a results table in the summary, and a comment block in the test
file recording the four respellings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-stages-catalog-assembly-and-explain/04-CONTEXT.md
@.planning/phases/04-stages-catalog-assembly-and-explain/04-02-SUMMARY.md
@.planning/phases/04-stages-catalog-assembly-and-explain/04-03-SUMMARY.md
@.planning/phases/04-stages-catalog-assembly-and-explain/04-05-SUMMARY.md
@.planning/phases/04-stages-catalog-assembly-and-explain/04-06-SUMMARY.md
@CLAUDE.md

<interfaces>
The harness contract, verbatim from `scripts/mutate-and-prove.sh:16-29`:

  scripts/mutate-and-prove.sh <target-file> <literal-pattern> <replacement> -- <gate command...>

  The pattern is matched LITERALLY, not as a regex, and exactly one occurrence is replaced.

    0  PASS  — the gate exited non-zero (the mutant was caught) and the tree is clean
    1  FAIL  — the gate exited 0 (the mutant escaped)
    2  ABORT — the target is unusable: not tracked, already dirty, or not supplied
    3  ABORT — the substitution was a no-op (the pattern never matched)
    4  ABORT — the target file was not restored

Three harness behaviours to route around, all documented in the script and re-stated by RESEARCH:

  1. MANDATORY PRE-FLIGHT PER ROW: `grep -F -o -- '<literal>' <file> | wc -l` must print exactly 1,
     COMMENTS INCLUDED. `perl -0pi` slurps the whole file with no `/g`, so a literal occurring once
     in code and earlier in a doc comment mutates the COMMENT, the suite stays green, and the run is
     recorded as "FAIL: mutant escaped" — the inverse of the truth.
  2. A mutant that breaks the build exits 1 at the build step and prints
     `PASS: gate fired (exit 1), tree clean` having run ZERO tests. Confirm from the gate's OUTPUT
     that the mutant compiled and the tests ran.
  3. `tsc` exits **1**, not 2, on diagnostics under typescript 7.0.2. Six plans have now re-derived
     this; line 32 of the script is already corrected.

Command forms: `pnpm test test/concierge` — BARE. `pnpm test -- concierge` does NOT filter (vitest's cac
CLI discards after `--`); this is the tenth reproduction across Phases 2-4.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the twelve src/concierge.ts mutants against the final tree</name>
  <files>packages/concierge/test/concierge.test.ts</files>
  <read_first>
    - `scripts/mutate-and-prove.sh:1-50` in full — the usage contract, the four exit codes, and the
      three known limitations.
    - `.planning/phases/04-stages-catalog-assembly-and-explain/04-03-SUMMARY.md` — the recorded
      single-occurrence literals for `src/concierge.ts`, taken at implementation time. **Re-take every
      count before running; the file has changed since.**
    - `.planning/phases/04-stages-catalog-assembly-and-explain/04-05-SUMMARY.md` — the S-series case
      list, so each row's expected red names a real case rather than a paraphrase.
    - `packages/concierge/src/concierge.ts` in full — this is where the literals live, and where the
      comment noting the four-`Object.freeze(`-occurrence constraint sits (three distinct assembly
      statements plus `DISPATCH_NOT_IMPLEMENTED`'s).
    - `packages/concierge/test/catalog.test.ts:458-469` — the precedent for writing a *working*
      mutant literal into the test file when the obvious spelling does not work, including the
      explanation of what the failure looked like.
    - `04-RESEARCH.md:1216-1259` — the original battery and the two behaviours with no single-literal
      mutant.
    - `04-PATTERNS.md:403-416` (the two mutants known to conflict with the implementation shape and
      the source constraints they impose) and `:1760-1797` (the mutation-proof shared pattern).
    - `04-VALIDATION.md` — the whole `## Mutant Obligations` section, including its trap-literal list
      (`Object.freeze(` 4× and `return warnStage(` 2× in `src/concierge.ts`) and the standing rule
      that a literal already at its PASS value is no-coverage, not a pass. Cited by section name
      rather than by line number, because this revision moved the line numbers.
  </read_first>
  <action>
    Before anything: `git status --porcelain` must be empty. The harness ABORTs (exit 2) on a dirty
    or untracked target, and a dirty tree also makes "tree clean" meaningless as a pass condition.

    **For every row below, in this order:**
    1. `grep -F -o -- '<literal>' packages/concierge/src/concierge.ts | wc -l` — must print exactly
       **1**, comments included. If it prints anything else, STOP and record the count; do not run
       the row with a non-unique literal, and do not "fix" it by narrowing the grep.
    2. Run the harness with the gate.
    3. On PASS, read the gate's **output** and confirm two things: that `pnpm build` succeeded (the
       mutant compiled) and that vitest actually ran and reported the expected case failing. A PASS
       with zero tests run is a build failure wearing a pass, and is recorded as INVALID, not as a
       proof.
    4. Record: the literal, its count, the replacement, the gate, the exit code, and **which named
       case went red**.

    | # | Literal (in `src/concierge.ts`) | Replacement | Gate | Expected red |
    |---|---|---|---|---|
    | M-04-1 | `Object.freeze(projected)` | `projected` | `pnpm build && pnpm test test/concierge` | the array-level SEC-03 case — `push` throws / length unchanged |
    | M-04-16 | `Object.freeze(tool)` | `(tool)` | `pnpm build && pnpm test test/concierge` | the element-level SEC-03 case — `[0].name = "evil"` |
    | M-04-2 | `memo ??= new Map` | `memo = new Map` | `pnpm build && pnpm test test/concierge` | the STG-04 identity cases — a fresh cache per call means a fresh array per call |
    | M-04-3 | `memo.set(index, built);` | `;` | `pnpm build && pnpm test test/concierge` | the STG-04 identity cases, from the other direction |
    | M-04-4 | `for (const [index, stage] of stages.entries())` | `for (const [index, stage] of [...stages.entries()].reverse())` | `pnpm build && pnpm test test/concierge` | the first-match-wins case |
    | M-04-5 | `result === true` | `result !== false` | `pnpm build && pnpm test test/concierge` | the truthy-non-boolean matcher case (a truthy string is `!== false`) |
    | M-04-6 | the catch-body `return warnStage(…)` statement recorded in `04-03-SUMMARY.md` | `throw new Error(stage.id);` | `pnpm build && pnpm test test/concierge` | the throwing-matcher case — the exception propagates instead of the stage being skipped |
    | M-04-7 | `index === null ? crossNames` | `index === null ? []` | `pnpm build && pnpm test test/concierge` | the no-stage-returns-cross-stage case |
    | M-04-8 | `...crossNames]` | `]` | `pnpm build && pnpm test test/concierge` | STG-01 — `signOut` missing from the results catalog |
    | M-04-12 | `const firstMatch: number = rows.findIndex((row) => row.matched);` | `const firstMatch: number = rows.map((row) => row.matched).lastIndexOf(true);` | `pnpm build && pnpm test test/concierge` | the two-overlapping-matchers case — `stage` becomes the last match, not the first |
    | M-04-13 | `deepFreeze(` | `Object.freeze(` | `pnpm build && pnpm test test/concierge` | the deep-freeze case — `explain(ctx).stages[0].matched = true` no longer throws |
    | M-04-15 | `warnHost(duplicateStageIdMessage(stage.id));` | `void duplicateStageIdMessage(stage.id);` | `pnpm build && pnpm test test/concierge` | the duplicate-stage-id warn case |

    **Four of these are repairs or respellings of the battery as `04-RESEARCH.md` wrote it, and the
    reasons are load-bearing** — record all four in the summary:

    - **M-04-1** was written as the bare `Object.freeze(`, which occurs **four** times in the shipped
      file: `Object.freeze(tool)`, `Object.freeze(toolByName)`, `Object.freeze(projected)`, and
      `DISPATCH_NOT_IMPLEMENTED`'s `/* @__PURE__ */ Object.freeze({ … })` at module scope.
      `src/catalog.ts` has exactly one, so the trap is new to this file. 04-03 was required to write
      the three assembly freezes as three textually distinct single-occurrence statements precisely so
      M-04-1 and M-04-16 each have a unique literal — `Object.freeze(tool)` is not a substring of
      `Object.freeze(toolByName)`, so neither is invalidated by the fourth occurrence. If a later
      refactor inlines them, both mutants become unrunnable. **Re-measure the four before running:**
      `grep -F -o -- 'Object.freeze(' packages/concierge/src/concierge.ts | wc -l` must print 4.
    - **M-04-4** requires `resolveIndex` and `explain` to use **distinct loop spellings**. That is a
      constraint on the SOURCE, not on the test. 04-03 shipped `for (const [index, stage] of
      stages.entries())` in `resolveIndex` and `stages.map(...)` in `explain`; unifying them is a
      mutation regression, not a tidy-up.
    - **M-04-7** was written as `id === null ? crossNames`, against the **superseded id-keyed memo**.
      The shipped memo is keyed by the resolved stage's array index, so the literal is
      `index === null ? crossNames`.
    - **M-04-12** was written as `matched && active === null`, against a `for…of` accumulation
      `explain` does not use. The shipped `explain` maps every stage to a row and derives the active
      index from the recorded rows, so the mutatable literal is the `firstMatch` derivation.

    **If any row cannot be made to run** — a literal that is not unique, or a replacement that does
    not compile — do NOT skip it silently and do NOT weaken the assertion. Record the attempt, the
    exact failure, and either a working replacement that fires with the build green, or a plain
    statement that the behaviour has no single-literal mutant and why. `catalog.test.ts:458-469` is
    the precedent for both.

    After the last row: `git status --porcelain` must be empty again. The harness restores the target,
    but confirm it rather than assuming — exit code 4 means it did not.
  </action>
  <verify>
    <automated>git status --porcelain | tee /dev/stderr | wc -l | grep -qx '0' && pnpm build && pnpm test && echo TREE_CLEAN_AFTER_BATTERY</automated>
  </verify>
  <acceptance_criteria>
    - All twelve rows were attempted. For each, the summary records: literal, unfiltered count,
      replacement, gate, harness exit code, and the named case that went red.
    - Every row's pre-flight `grep -F -o -- '<literal>' … | wc -l` printed exactly **1**. Any row that
      printed otherwise is recorded with its actual count and is NOT reported as a proof.
    - Every PASS was confirmed from the gate's OUTPUT to have compiled and run tests. State, per row,
      the vitest line showing the failing test name. A row whose output shows zero tests run is
      recorded as INVALID.
    - `git status --porcelain` is empty at the end of the task and `pnpm build && pnpm test` is green.
    - `git diff -- packages/concierge/src/` is empty.
    - No row's gate uses the `--` form. Confirm by reading the commands recorded in the summary.
  </acceptance_criteria>
  <done>Every rule `src/concierge.ts` implements has been observed to have a test that fails when the
  rule is removed, and every pass was confirmed to be a real one rather than a build failure.</done>
</task>

<task type="auto">
  <name>Task 2: Run the four src/catalog.ts and src/types.ts mutants, and record the respellings in the test file</name>
  <files>packages/concierge/test/concierge.test.ts</files>
  <read_first>
    - `.planning/phases/04-stages-catalog-assembly-and-explain/04-02-SUMMARY.md` — the recorded
      single-occurrence literals for `src/catalog.ts`. **Re-take every count.**
    - `.planning/phases/04-stages-catalog-assembly-and-explain/04-04-SUMMARY.md` — the C-series case
      list for CAT-03.
    - `.planning/phases/04-stages-catalog-assembly-and-explain/04-06-SUMMARY.md` — what was observed
      when `explain: (ctx: StageContext) => Explanation;` was deleted by hand, and specifically
      whether `test-d/concierge.test-d.ts` appeared among the diagnostics.
    - `packages/concierge/test-d/catalog.test-d.ts:55-63` — the terse-output caveat that decides how
      M-04-14 is asserted: on **exit code plus `file:line`**, never on a grep for a predicate's name,
      because the alias name never appears in non-TTY output and a grep that never matches reads as a
      passing check.
    - `packages/concierge/test/catalog.test.ts:458-469` — the working-literal-in-a-comment precedent.
    - `04-RESEARCH.md:817` — the verified trap literals in `src/catalog.ts`
      (`duplicate_action_name` occurs 2×; `action.consent` occurs 2×) which must not be used bare.
  </read_first>
  <action>
    **Part one — the four remaining rows.** Same protocol as Task 1: pre-flight count, run, read the
    gate output, record.

    | # | File | Literal | Replacement | Gate | Expected red |
    |---|---|---|---|---|---|
    | M-04-9 | `src/catalog.ts` | `!seenNames.has(requires)` | `false` | `pnpm build && pnpm test catalog` | the CAT-03 typo case |
    | M-04-10 | `src/catalog.ts` | `requires === action.name` | `false` | `pnpm build && pnpm test catalog` | the `consent_self_reference` case |
    | M-04-11 | `src/catalog.ts` | `!seenNames.has(requires)` | `!new Set<string>().has(requires)` | `pnpm build && pnpm test` | **both** the forward-reference clean-build case (catalog suite) **and** the cross-stage-target clean-build case (concierge suite). This is the only mutant that proves the check reads the COMPLETE name set — M-04-9 does not. The gate is the full suite for exactly that reason; a `pnpm test catalog` gate would miss half the claim. |
    | M-04-14 | `src/types.ts` | `explain: (ctx: StageContext) => Explanation;` | `` (empty — delete the line) `` | `pnpm typecheck` | `tsc` exits **1** (not 2) and the diagnostics name `packages/concierge/test-d/concierge.test-d.ts` |

    **M-04-14 needs its output read, not just its exit code.** Deleting the member also breaks
    `src/concierge.ts`'s returned object literal, so the gate would exit 1 even if the type-level suite
    pinned nothing. Confirm from the output that `packages/concierge/test-d/concierge.test-d.ts`
    appears among the diagnostics. If it does not, the mutant is proving the source and not the pin —
    record that plainly, name the predicate that would need to change, and do not report the row as a
    proof of the type-level suite.

    **Part two — write the respellings into `test/concierge.test.ts`.** Add one comment block, in
    `catalog.test.ts:458-469`'s register, under a divider, recording for the next reader:

    - The battery rows whose obvious spelling does **not** work, each with the reason and the literal
      that does: M-04-1 (`Object.freeze(` occurs **four** times — three assembly statements plus
      `DISPATCH_NOT_IMPLEMENTED`'s; use the single-occurrence statement form), M-04-16 (the element
      freeze, split out of M-04-1's original claim), M-04-4 (needs distinct loop spellings in
      `resolveIndex` and `explain`, naming the two spellings shipped), M-04-6 (the bare
      `return warnStage(` occurs **twice** in `runMatch` — the `catch` branch and the non-boolean
      branch — so the literal is the complete statement, and the two argument lists must stay
      textually distinct), M-04-7 (written against the superseded id-keyed memo; the shipped key is
      the array index), M-04-12 (written against a `for…of` accumulation `explain` does not use; the
      mutatable literal is the `firstMatch` derivation).
    - The trap literals in `src/catalog.ts` that must not be used bare (`duplicate_action_name` ×2,
      `action.consent` ×2, and any new one this phase created — 04-02 recorded
      `consent_target_missing` and `consent_self_reference` as ×2 each).
    - The harness's vacuous-PASS behaviour, so a reader running the battery again knows to read the
      gate output.

    Spell the mutation-harness path and the row IDs in prose; do not add executable code. This is a
    comment block, and the file's existing convention is that where the obvious mutant literal does
    not work, the working one is written into the test file.

    **Do not** add the "two behaviours with no single-literal mutant" note here — 04-05 Task 1 already
    put it in the header. Confirm it is present and say so in the summary; if it is missing, add it
    and record that it was.
  </action>
  <verify>
    <automated>git status --porcelain -- packages/concierge/src | wc -l | grep -qx '0' && pnpm build && pnpm typecheck && pnpm test && echo BATTERY_COMPLETE</automated>
  </verify>
  <acceptance_criteria>
    - All four rows were attempted, each with its pre-flight count printing exactly 1 and each
      recorded with literal, replacement, gate, exit code and the named case that went red.
    - M-04-11's gate was the FULL suite (`pnpm build && pnpm test`), and the summary names both the
      forward-reference case and the cross-stage-target case as having gone red. If only one went red,
      record which and why — the row's whole value is that it proves the check reads the complete set.
    - M-04-14's recorded evidence includes the `tsc` exit code (**1**) and the list of files named in
      the diagnostics, with an explicit statement of whether
      `packages/concierge/test-d/concierge.test-d.ts` was among them.
    - `grep -c 'M-04-' packages/concierge/test/concierge.test.ts` returns at least 4 — the respelling
      block names its rows.
    - `grep -c 'no single-literal mutant' packages/concierge/test/concierge.test.ts` returns at least
      1 (from 04-05's header).
    - `git status --porcelain` is empty; `git diff -- packages/concierge/src/` is empty;
      `pnpm build && pnpm typecheck && pnpm test` all green.
    - Sixteen rows total across both tasks are recorded in the summary as one table.
  </acceptance_criteria>
  <done>The CAT-03 rules and the `Concierge.explain` type pin have observed mutants, the one mutant
  that proves the complete-name-set property was run against the full suite, and the four literals
  that do not work as written are recorded where the next person will look.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the mutation harness → the working tree | The harness edits tracked source in place and restores it. A dirty tree makes "tree clean" meaningless as a pass condition, and exit 4 means the file was not restored. |
| a harness PASS → the claim it is taken to support | The harness cannot tell *why* the gate exited non-zero. A build failure prints PASS having run zero tests, which is a false claim of coverage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-20 | Repudiation | a validation rule with no observed mutant | mitigate | Sixteen rows, each with a named expected-red case. Two behaviours genuinely have no single-literal mutant and are written into the test file rather than faked. |
| T-04-24 | Repudiation | a vacuous PASS from a mutant that did not compile | mitigate | Every PASS is confirmed from the gate's OUTPUT to have built and run tests, and the row records the vitest line showing the failing test name. A zero-tests-run row is recorded INVALID rather than as a proof. |
| T-04-25 | Repudiation | a mutant that silently edits a doc comment | mitigate | Mandatory unfiltered pre-flight count per row, comments INCLUDED. `perl -0pi` slurps with no `/g`, so a literal appearing earlier in a comment mutates the comment and the run is recorded as the inverse of the truth. Four trap literals are named explicitly. |
| T-04-26 | Tampering | an unrestored source file leaking into the phase gate | mitigate | `git status --porcelain` empty and `git diff -- packages/concierge/src/` empty are acceptance criteria on both tasks, and the phase gate (04-08) re-checks. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing; `pnpm-lock.yaml` must be byte-identical. |
</threat_model>

<verification>
- `git status --porcelain` empty at the end of each task.
- `git diff -- packages/concierge/src/` empty.
- `pnpm build && pnpm typecheck && pnpm test` all exit 0.
- No command anywhere in this plan uses `pnpm test -- <fragment>`.
</verification>

<success_criteria>
- Sixteen mutants attempted, each with an unfiltered pre-flight count of exactly 1.
- Every PASS confirmed to have compiled and run tests.
- The one mutant that proves the complete-name-set property was gated on the full suite.
- The four literals that do not work as `04-RESEARCH.md` wrote them are recorded in the test file
  with the reason each needed respelling.
- The tree is byte-identical to where it started.
</success_criteria>

<output>
Create `.planning/phases/04-stages-catalog-assembly-and-explain/04-07-SUMMARY.md` when done.
Record: one table of all sixteen rows with `# | file | literal | unfiltered count | replacement |
gate | exit code | case that went red | compiled-and-ran-tests (yes/no)`; the four respelling
rationales verbatim; M-04-14's diagnostic file list; and any row that could not be made to run, with
its exact failure and either a working replacement or a plain statement that the behaviour has no
single-literal mutant.
</output>
