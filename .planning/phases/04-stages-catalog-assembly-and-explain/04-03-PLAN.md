---
phase: 04-stages-catalog-assembly-and-explain
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - packages/concierge/src/concierge.ts
  - packages/concierge/src/index.ts
  - packages/concierge/src/contract.ts
  - packages/concierge/test/export-surface.test.ts
  - packages/concierge/test-d/exports.test-d.ts
  - packages/concierge/test/artifact.test.ts
autonomous: true
requirements: [STG-01, STG-02, STG-03, STG-04, SEC-03, DX-01, CAT-03, CAT-01]

must_haves:
  truths:
    - "An agent on the results page is offered the results actions plus the cross-stage actions; the checkout actions are absent from the array, not rejected when called."
    - "Two `catalogFor` calls with equivalent context return the identical array reference."
    - "A page script cannot push a tool onto the array handed to the model, nor rewrite an element's description."
    - "Two stages that share an id serve their OWN actions, and the developer is told once that the id is ambiguous."
    - "A matcher that throws skips its stage instead of taking down the consumer's render, and says which stage, without echoing what was thrown."
    - "`explain(ctx)` reports the active stage, every stage's matched flag, each stage's bridge status, and the live catalog — in one pass, so it cannot contradict itself."
  artifacts:
    - path: "packages/concierge/src/concierge.ts"
      provides: "createConcierge — assembly, stage resolution, memoized projection, explain, the dispatch stub"
      exports: ["createConcierge"]
      min_lines: 300
    - path: "packages/concierge/src/index.ts"
      provides: "createConcierge as a value export, Explanation and StageExplanation as type exports, an honest module doc comment"
      contains: "createConcierge"
  key_links:
    - from: "packages/concierge/src/concierge.ts"
      to: "packages/concierge/src/catalog.ts"
      via: "one flat buildCatalog over [...allStageActions, ...crossStage], plus deepFreeze for explain()"
      pattern: "buildCatalog\\("
    - from: "packages/concierge/src/concierge.ts"
      to: "packages/concierge/src/host.ts"
      via: "the matcher warning and the duplicate-stage-id warning"
      pattern: "warnHost\\("
    - from: "packages/concierge/src/index.ts"
      to: "packages/concierge/src/concierge.ts"
      via: "the barrel's sixth value-export statement (the five that exist today source from ./types.js, ./contract.js, ./json-schema.js, ./catalog.js and ./define-action.js)"
      pattern: "from \"./concierge.js\""
---

<objective>
Write `src/concierge.ts` — the package's first factory, its first memo, and its first guarded call
into consumer code that must not echo what it caught — then reach it from the barrel and move the
export-surface pins in the same commit.

Purpose: this is the phase's centre. `catalogFor` is what an adapter hands to
`useSyncExternalStore`, so its referential identity is a correctness constraint rather than a
performance one (React compares with `Object.is`; Svelte 5's `$derived` with `===`). The array it
returns is what a model reads, so an unfrozen projection is a live tool-injection channel — measured:
`push` onto a `.filter()` result succeeded and the agent's list became `['a','injected']`. And the
memo key is the resolved stage's **array index**, not its id, because two stages sharing an id build
cleanly and then silently serve each other's catalogs.

Output: `src/concierge.ts`, a barrel that reaches it, an honest module doc comment, and the export
surface moved from 59/49/10 to 62/51/11 in one commit with its two pins.

**Three tasks, and the cut between Task 1 and Task 2 is deliberate.** Task 1 writes the whole module —
every statement, the file header's three numbered constraints in full, and a one-line `ANCHOR(T2)`
comment at each of the nineteen sites that owe a recorded decision. Task 2 expands every anchor into
its full doc comment and corrects `contract.ts`'s now-false claim in the same pass, because both are
the same kind of work: annotate in place, with the measurement, in the house register. The file
compiles and `pnpm test` reads 6 files / 55 tests at **both** boundaries, and Task 2's diff is
comment-only. The reason for the cut: the doc comments carry the two corrections this phase exists to
get right — the memo is instance-local because of **SSR cross-request pollution** and never because of
tree-shaking, and the shallow projection freeze is complete only **because** the elements are shared
and already deep-frozen — and prose is what degrades first when a single task holds sixteen numbered
steps, fourteen mutant-literal constraints and twenty-five prose claims at once.

This plan also **closes CAT-01**, which `REQUIREMENTS.md:157` records as *Partial — 4/5 derived
artifacts ship*. The fifth, per-stage catalogs, arrives here as `createConcierge().catalogFor`.
04-05 S1/S2 are its evidence and 04-08 Task 2(c) records the closure; this plan produces it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-stages-catalog-assembly-and-explain/04-CONTEXT.md
@.planning/phases/04-stages-catalog-assembly-and-explain/04-01-SUMMARY.md
@.planning/phases/04-stages-catalog-assembly-and-explain/04-02-SUMMARY.md
@CLAUDE.md

<!-- `04-RESEARCH.md` and `04-PATTERNS.md` are deliberately NOT @-included; this plan already reads
     six source files and authors a ~250-line module. Every range either document carries that this
     plan needs is named, with line numbers, in each task's `<read_first>` block.
     WHERE CONTEXT AND RESEARCH DISAGREE, CONTEXT WINS. `04-PATTERNS.md:27-40` is the correction
     table; the three that bite this plan are: the memo key is the ARRAY INDEX (`number | null`),
     not the stage id — RESEARCH's Pattern 1 sketch and its architecture diagram both still show the
     superseded id-keyed form; the reason the memo is instance-local is SSR cross-request pollution,
     NEVER tree-shaking (that claim was measured and does not reproduce); and the export baseline is
     59/49/10, not the 42 an early draft recorded. -->

<interfaces>
Consumed here, verbatim from the current tree. The executor should NOT re-derive these.

From `packages/concierge/src/catalog.ts`:

  export function buildCatalog<const A extends readonly AnyActionDefinition[]>(
    actions: A, options?: BuildCatalogOptions,
  ): Catalog<A[number]["name"]>;
  export interface Catalog<Name extends string = string> {
    readonly entries: readonly CatalogEntry[];
    readonly names: readonly Name[];
    readonly byName: Readonly<Record<Name, CatalogEntry>>;
    readonly diagnostics: readonly CatalogDiagnostic[];
  }
  export interface CatalogEntry {
    readonly action: AnyActionDefinition;
    readonly parameters: JsonSchemaObject;      // ALREADY deep-frozen by buildCatalog
  }
  export function deepFreeze<T>(value: T, skip: ReadonlySet<object>, seen: WeakSet<object>): T;
                                              // ← gains `export` in plan 04-02

From `packages/concierge/src/host.ts`:

  export function warnHost(message: string): void;

From `packages/concierge/src/types.ts` (after plan 04-01):

  export interface ConciergeConfig {
    stages: ReadonlyArray<StageDefinition<any>>;          // REQUIRED, not optional
    crossStage?: ReadonlyArray<AnyActionDefinition>;
    …
  }
  export interface StageDefinition<B extends Bridge = Bridge> {
    id: string;
    match: (ctx: StageContext) => boolean;
    actions: ReadonlyArray<AnyActionDefinition<B>>;
    bridge?: BridgeRegistry<B>;
  }
  export interface StageContext { pathname?: string; [key: string]: unknown; }
  export interface EmittedTool {
    readonly type: "function"; readonly name: string;
    readonly description: string; readonly parameters: JsonSchemaObject;
  }
  export interface StageExplanation {
    readonly id: string; readonly matched: boolean;
    readonly bridge: { readonly id: string; readonly registered: boolean } | null;
  }
  export interface Explanation {
    readonly stage: string | null;
    readonly stages: ReadonlyArray<StageExplanation>;
    readonly catalog: ReadonlyArray<string>;
  }
  export interface Concierge {
    dispatch: (name: string, args: unknown, meta?: InvocationMeta) => Promise<ActionResult>;
    catalogFor: (ctx: StageContext) => ReadonlyArray<EmittedTool>;
    stageFor: (ctx: StageContext) => string | null;
    explain: (ctx: StageContext) => Explanation;
  }
  export interface ActionResult { ok: boolean; reason?: ReasonCode | undefined; message: string; }
  export const MESSAGE_MAX_CHARS = 180;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write src/concierge.ts — every statement, the file header, and nineteen anchors</name>
  <files>packages/concierge/src/concierge.ts</files>
  <read_first>
    - `packages/concierge/src/catalog.ts:1-63` — the file-header convention (a numbered list of the
      constraints whose violation is SILENT, each with the measurement behind it, closing with the
      standing no-DOM paragraph) and the import discipline at `:59-63` (value imports first, then
      `import type`, all with the `.js` extension on a `.ts` path, because `verbatimModuleSyntax` is
      on). Copy the SHAPE, not the words.
    - `packages/concierge/src/catalog.ts:240-268` — `Catalog.byName`'s doc comment. It is the
      argument to CITE for `toolByName` being a frozen null-prototype record, and `:260-268` is the
      sentence that settles `Map`-vs-record for the memo: "A `Map` remains correct for Phase 6's own
      **mutable** per-dispatch state … and is wrong for anything that must be frozen." The memo is
      never frozen, so `Map` is right there. Do not re-argue it.
    - `packages/concierge/src/catalog.ts:513-591` — `deepFreeze`, its `skip`/`seen` parameters, its
      refusal to early-out on `Object.isFrozen` (`:539-546`) and the corrected projection paragraph
      04-02 leaves at `:554-558`. The no-early-out is exactly why `deepFreeze` per projection
      measured 510× slower.
    - `packages/concierge/src/catalog.ts:715-745` and `:840-865` — `buildCatalog`'s signature, the
      explicit-annotation-on-every-`const` house rule, the aggregate throw, and the
      `Object.create(null)` + `deepFreeze` assembly at the end.
    - `packages/concierge/src/json-schema.ts:230-245` and `:384-398` — the two existing `try`/`catch`
      blocks in `src/`. **Copy the structure and INVERT one decision.** Both echo the caught value
      through `describeCause`, legitimately, because they are build-time developer diagnostics with
      the exemption stated at `:259-261`. `runMatch` is the opposite case — runtime, per navigation,
      in a shipped app — so its `catch` takes NO binding (`catch {`, exactly as `describeCause`
      itself does at `:242`) and its message carries only the stage id and fixed prose.
    - `packages/concierge/src/host.ts` in full (96 lines) — the seam, its header's three conventions,
      and why `console` is TS2304 under `lib: ["ES2022"]`.
    - `packages/concierge/src/catalog.ts:142-146` and `:506-511` — the warn-once GRANULARITY
      precedent ("One diagnostic per offending action, each naming its action. An aggregated summary
      line … loses exactly the name DX-03 requires") and the message shape
      `concierge: [code] action "name": problem Fix: fix`. Both new warnings take that shape with the
      stage id substituted for the action name.
    - `packages/concierge/src/define-action.ts:212-233` — the `isolatedDeclarations` return
      annotation, and the register for a doc comment that records a rejected extension with its
      measurement.
    - `packages/concierge/src/types.ts:1114-1134` (`BridgeRegistry`, `StageContext`), `:1290-1335`
      (`EmittedTool` after 04-01), `:1396-1435` (`ConciergeConfig`), `:1497-1515` (`Concierge` after
      04-01), and `:96-140` + `:279` (`ActionResult`, `ReasonCode`'s closed-union note,
      `MESSAGE_MAX_CHARS = 180`).
    - `04-RESEARCH.md:240-345` (*Pattern 1* — read for the cache SHAPE only; **its sketch at
      `:305-321` is the SUPERSEDED id-keyed form, so key by index**), `:440-506` (*Pattern 3* —
      `runMatch`'s structure and the warn-once granularity), `:575-658` (*Pattern 5* — the one-pass
      `explain` shape and `bridgeStatus`), `:659-668` (*Anti-Patterns*), `:670-682`
      (*Don't Hand-Roll*), `:825-874` (*Code Examples* — the working prototype), and `:1078-1083`
      (the `dispatch` stub, now settled by CONTEXT).
      **The measurement-bearing ranges — `:346-439` (Pattern 2's seven tamper vectors, the 510×
      timing and the element-sharing measurement), `:745-758` (*Pitfall 7*), `:760-791` (*Pitfall 8*)
      and the probe figures inside Patterns 1 and 3 — are Task 2's and are listed there. Do not read
      them in this task; the numbers are not needed to write the statements.** The one exception is
      Pitfall 8's conclusion, which the file HEADER states in this task and which is given verbatim
      in the action below, so the range still need not be opened here.
    - `04-CONTEXT.md:37-93` (assembly, the corrected index-keyed memo and the duplicate-id warning,
      and the no-stage behaviour), `:95-119` (`explain`), `:144-192` (API surface, the `dispatch`
      stub, the matcher policy, `stageFor` not memoized).
    - `04-PATTERNS.md:63-418` — this file's complete pattern assignment. Task 1 needs the
      **mutant-uniqueness constraints the file's SHAPE must honour**; the prose registers in the same
      range are Task 2's.
  </read_first>
  <action>
    Create `packages/concierge/src/concierge.ts`. RESEARCH recommends a new file rather than folding
    into `catalog.ts` on one measured ground: `catalog.ts` is 865 lines and its header states "every
    rule lives here, so 'did we check X?' is a one-file question" — stage resolution is not a catalog
    rule. CONTEXT grants the layout as discretion; take the recommendation.

    **File header**, in `catalog.ts:1-57`'s register — three constraints whose violation is SILENT,
    numbered, each with its measurement, closing with the standing no-DOM paragraph:

    1. **The memo is instance-local and lazily allocated, and the reason is SSR cross-request
       pollution — NOT tree-shaking.** Write the SSR reason and only the SSR reason: module instances
       are reused across server requests, so a module-scope catalog memo would be shared across every
       `createConcierge` in the process and two configs in one server would serve each other's
       catalogs. Cite `.planning/research/ARCHITECTURE.md:380-405`, which quotes Vue's own definition
       and cites TanStack Router shipping this exact bug. **The `sideEffects: false` / tree-shaking
       justification recorded in an earlier draft was measured under rolldown 1.2.0 and does NOT
       reproduce** — a module-scope `Map` read by an exported function is retained and behaves
       identically bundled and unbundled. Writing it into a shipped doc comment would be a false
       claim of exactly the kind 03-08 spent a whole plan removing from `dist/index.d.ts`.
    2. **The shallow `Object.freeze` on a projection is complete ONLY because the elements are shared
       and already deep-frozen.** The two decisions are coupled. Building fresh `EmittedTool` objects
       per projection turns the shallow freeze into `catalog.ts:513-521`'s breach-that-reports-success
       — `Object.isFrozen(projection)` returns `true` while every element stays mutable. Record the
       measurements: all seven tamper vectors throw under the shallow-plus-shared form, and it is
       510× cheaper than `deepFreeze` per projection (0.0074 ms vs 3.78 ms for 40 projections),
       because `deepFreeze` deliberately has no `Object.isFrozen` early-out and re-walks every
       already-frozen JSON Schema subtree.
    3. **`stage.match` is called from exactly one place.** Each additional call site is a second copy
       of the throw policy, the non-boolean policy and the warn-once latch, and a second place for
       `explain` and `stageFor` to disagree.

    **Imports** — value first, then `import type`, `.js` extensions:
    `import { buildCatalog, deepFreeze } from "./catalog.js";`,
    `import { warnHost } from "./host.js";`, and a single `import type { … } from "./types.js";`
    bringing `ActionResult`, `Concierge`, `ConciergeConfig`, `EmittedTool`, `Explanation`,
    `InvocationMeta`, `StageContext`, `StageExplanation`. Do **not** import `assertSingleInstance` —
    `createConcierge` reaches it transitively through `buildCatalog` on its first line, and 04-03's
    Task 3 corrects the `contract.ts` sentence rather than adding a second call.

    **Module scope holds constants only, never mutable state.** Two are needed:
    - `const NO_SKIP: ReadonlySet<object> = /* @__PURE__ */ new Set<object>();` — `deepFreeze`'s
      `skip` argument for `explain()`'s result, which contains no validator instances. Doc-comment
      the contrast explicitly: this is never written, so the SSR argument that forbids a module-scope
      memo does not reach it. The `/* @__PURE__ */` annotation follows 03-08's finding that
      unannotated module-scope calls retain dead bytes in every consumer bundle.
    - `const DISPATCH_NOT_IMPLEMENTED: ActionResult = /* @__PURE__ */ Object.freeze({ ok: false, message: … });`
      See the dispatch stub below.

    **Module-private helpers, above `createConcierge`:**

    - `function duplicateStageIdMessage(id: string): string` — returns the house-shaped warning:
      `concierge: ` prefix, the quoted stage id, the problem, then `Fix: `. The problem states that
      two stages share this id and that `stageFor()`, `Session.stage()` and `explain()` all report
      it, so the two are indistinguishable to a developer reading them — **and that catalog scoping
      is unaffected, because the per-stage catalog is keyed by declaration order, not by id.** The fix
      is to give each stage a distinct id. Putting the message in a named function is deliberate:
      mutant M-04-15 targets its single call site, and `warnHost(` occurs twice in this file.
    - `function bridgeStatus(stage: ConciergeConfig["stages"][number]): StageExplanation["bridge"]` —
      `undefined` bridge → `null`; otherwise `try { live = registry.read(); } catch { live = null; }`
      and return `{ id: registry.id, registered: live !== null && live !== undefined }`. Use
      `ConciergeConfig["stages"][number]` as the parameter type rather than writing `StageDefinition<any>`:
      the `any` already lives in `types.ts` where D-07's contravariance measurement justifies it, and
      re-spelling it here would be a second, unargued occurrence. Doc-comment the three states and
      why the shape survives Phase 5 unchanged (`id` and `read()` are both on `BridgeRegistry` today),
      and record the rejected alternative of warning on a throwing `read()`: it runs only inside
      `explain`, which is a human-debugging-rate call, so a warning there prints during the very
      activity it would interrupt.

    **`export function createConcierge(config: ConciergeConfig): Concierge`** — the explicit return
    annotation is required by `isolatedDeclarations` (TS9010 without it).

    Body, in this order. Every local gets an explicit type annotation; that is the house rule visible
    throughout `src/`, not optional style.

    *Assembly (runs once, per instance):*

    1. `const stages` = `config.stages` (REQUIRED on the interface — do not write `?? []`);
       `const crossStage` = `config.crossStage ?? []`.
    2. **One flat `buildCatalog`** over `[...stages.flatMap((stage) => stage.actions), ...crossStage]`.
       Not one per stage: CAT-03 needs the complete name set including cross-stage actions, and a
       single build means a single aggregated `CatalogValidationError` rather than one throw per
       stage. Doc-comment that the per-stage view is a projection of that one catalog, and that a
       duplicate action name across two stages is therefore rejected globally by `buildCatalog`'s
       existing `duplicate_action_name` with no new code (measured).
    3. **The `EmittedTool` per action, built once, shared by reference.** Build a null-prototype
       record `toolByName` with `Object.create(null)`; for each `catalog.entries` element construct
       `const tool: EmittedTool = { type: "function", name: entry.action.name, description: entry.action.description, parameters: entry.parameters };`
       then `toolByName[entry.action.name] = Object.freeze(tool);` and finally
       `Object.freeze(toolByName);`. **`parameters` is assigned BY REFERENCE — never re-emitted.**
       `buildCatalog` already emitted, validated and deep-froze it; re-emitting would run a vendor
       converter a second time, produce a different object, destroy element identity and un-freeze the
       subtree. Cite `Catalog.byName`'s doc comment for the null-prototype-and-freeze pair rather than
       restating it.

       **Write the three assembly freeze calls as three textually distinct single-occurrence
       statements** — `Object.freeze(tool)`, `Object.freeze(toolByName)`, and
       `Object.freeze(projected)` below. A bare `Object.freeze(` occurs **four** times in the finished
       file: those three plus `DISPATCH_NOT_IMPLEMENTED`'s `/* @__PURE__ */ Object.freeze({ … })` at
       module scope. Mutants M-04-1 and M-04-16 each need a unique literal, so neither may be spelled
       bare. `Object.freeze(tool)` is not a substring of `Object.freeze(toolByName)`, so those two
       stay distinguishable as written. Anchor the **four**-occurrence constraint in a comment so a
       later reader does not "simplify" them back into inline calls — and state four, not three; a
       count in a shipped comment is a claim, and 04-08 audits this file's prose.
    4. `const crossNames: readonly string[] = crossStage.map((action) => action.name);`
    5. **`namesByStage` is INDEXED, parallel to `stages` — never keyed by id.**
       `const namesByStage: ReadonlyArray<readonly string[]> = stages.map((stage) => [...stage.actions.map((action) => action.name), ...crossNames]);`
       Doc-comment the correction in the house form (a bolded lead, the measurement, then what it does
       and does not widen): an id-keyed lookup was the earlier form and was measured to COLLAPSE —
       two stages sharing an id build cleanly, the lookup resolves to the last one's actions, and the
       agent on stage A is offered stage B's actions. `buildCatalog` cannot see it because it receives
       a flat action array and has no concept of a stage, and `duplicate_action_name` does not fire
       because the action *names* differ. Keying by declaration index makes the collapse impossible at
       zero new surface cost. Record the two rejected remedies: throwing (needs a `CatalogIssue` whose
       `action` field holds a stage id, corrupting the `issues.map(i => i.action)` semantics DX-03
       depends on) and warn-only (leaves a real correctness bug in place).
    6. **Warn once per distinct duplicated stage id.** Two build-local sets, both discarded when
       `createConcierge` returns — say so, so a reader does not mistake them for instance state.
       Name them `seenStageIds` and `reportedStageIds`. Iterate with the loop head
       `for (const stage of stages)`; when the id is already in `seenStageIds` and not yet in
       `reportedStageIds`, add it to `reportedStageIds` and call
       `warnHost(duplicateStageIdMessage(stage.id));` — that exact call spelling, on its own line, is
       M-04-15's literal. Add the id to `seenStageIds` unconditionally at the end of each iteration.
       Two sets rather than one so a third stage sharing the id does not produce a second warning
       naming the same id — the construction-time analogue of the matcher warn-once latch, and the
       same granularity `CatalogDiagnostic`'s doc comment settles. **This loop must be spelled
       `for (const stage of stages)`, distinct from the resolution loop below**, or M-04-4's literal
       is not unique.

    *Instance-local state — two `let`s, both `null` until first use:*

    7. `let memo: Map<number | null, ReadonlyArray<EmittedTool>> | null = null;` and
       `let warnedStages: Set<string> | null = null;`. Doc-comment both against header constraint 1.
       `Map` and not a null-prototype record because the key type is `number | null`: a record cannot
       hold `null` and cannot hold a number key without stringifying it, every sentinel string is a
       legal stage id, and `String(null)` collides with a stage literally named `"null"` — all
       measured. Cite `catalog.ts:260-268` for why `Map` is correct precisely because this structure
       is never frozen.

    *Closures over that state:*

    8. `function warnStage(id: string, problem: string, fix: string): false` — allocates
       `warnedStages` lazily, returns early if the id is already latched, otherwise latches and calls
       `warnHost` with the house-shaped message. **Returns the literal type `false`** so the
       warn-and-skip decision is a single expression at both call sites, which is what makes M-04-6
       a single-literal swap. Note that in a comment.
    9. `function runMatch(stage, ctx): boolean` — the ONLY call site of `stage.match`:
       declare `let result: unknown;`, assign it inside a `try` from `stage.match(ctx)`, and in the
       `catch` return `warnStage` with the stage id and the throw-case problem/fix prose. Then
       `if (result === true)` return `true`; then `if (result !== false)` return `warnStage` with the
       non-boolean problem/fix prose; then return `false`. **Three literals in that body carry
       constraints, and all three are named here:**
       (i) the `result === true` comparison — M-04-5's literal, which must occur exactly once in the
       file;
       (ii) the `catch` branch's **whole `return warnStage(…);` statement, including its full argument
       list** — M-04-6's literal. The bare prefix `return warnStage(` occurs **twice** in `runMatch`
       (the `catch` branch and the non-boolean branch), so the bare form is NOT unique and must never
       be used as the mutant literal. Only the complete statement is;
       (iii) the non-boolean branch's whole `return warnStage(…);` statement. This one is a constraint
       rather than a mutant target: its argument list must be **textually distinct** from the `catch`
       branch's, or M-04-6's literal stops being unique. Different problem and fix prose achieves that
       naturally — do not factor the two calls into a shared local or a shared message constant.
       Record **both** complete statements verbatim in this plan's summary; 04-07 runs M-04-6 from
       what is recorded there, and a summary carrying only the bare prefix makes the row unrunnable.
       **`catch {` takes no binding and the message must NOT echo the caught error.** Write that
       reason into the doc comment or a later reader comparing this block with `json-schema.ts`'s two
       will "fix" the inconsistency: those are build-time developer diagnostics with a stated
       exemption; this fires at runtime, on every navigation, in a shipped app, and the caught
       message is whatever the app's matcher put in it — a covert channel for user input. Also
       doc-comment `=== true` over truthiness with the measured reason: strict equality fails closed,
       but failing closed *silently* reproduces P14's first-run experience (a matcher returning a
       truthy object never matches and the developer reads "the agent says it can't do anything"), so
       strict semantics PLUS a named warning is the only combination that is both fail-closed and
       diagnosable. Record both rejected alternatives: a silent strict check and a permissive truthy
       check.
    10. `function resolveIndex(ctx: StageContext): number | null` —
        `for (const [index, stage] of stages.entries()) { if (runMatch(stage, ctx)) { return index; } }`
        then `return null;`. Short-circuits at the first match: STG-02's first-match-wins.
        Doc-comment that `stageFor` is NOT memoized and why (matchers are pure and cheap and their
        input is the caller's arbitrary `ctx`; only the projected catalog is memoized), and that the
        ordered array — rather than a keyed object — is what makes resolution independent of stage
        naming, with the measured key-ordering result: object key iteration hoists integer-like keys,
        so `["results","checkout","2","home","10"]` becomes `["2","10","results","checkout","home"]`.
    11. `function projectFor(index: number | null): ReadonlyArray<EmittedTool>` —
        `memo ??= new Map<number | null, ReadonlyArray<EmittedTool>>();` then a `memo.get(index)` hit
        returns the identical reference; on a miss,
        `const names: readonly string[] = index === null ? crossNames : (namesByStage[index] ?? crossNames);`
        then build `projected` by mapping names through `toolByName` and filtering out `undefined`
        with a type predicate, then `const built: ReadonlyArray<EmittedTool> = Object.freeze(projected);`,
        then `memo.set(index, built);` and return. **`projectFor` never sees a `ctx`** — that is what
        makes "memoize by resolved stage, not by `ctx` identity" mechanical rather than a discipline.
        Doc-comment the no-stage branch: it returns the cross-stage actions, not an empty array,
        because `crossStage` is declared "available in every stage" and silently stripping actions the
        developer explicitly marked global would contradict the declaration they wrote. Record the
        rejected alternative — an empty frozen array; "fail closed" is right for *consent* and here it
        would silently disable `signOut`-shaped actions on any unrouted page — and note that the
        situation is not hidden, since `stageFor` returns `null` and `explain` reports every stage's
        `matched: false`.

    *The four members and the return:*

    12. `dispatch` — returns `Promise.resolve(DISPATCH_NOT_IMPLEMENTED)`. Name the parameters with a
        leading underscore so the signature stays readable; the stub reads none of them.
        `DISPATCH_NOT_IMPLEMENTED` is `{ ok: false, message: "concierge: dispatch is not implemented in this build, which ships catalog assembly and stage scoping only." }`
        with **`reason` deliberately OMITTED**. Doc-comment why, because it looks like an oversight:
        `ReasonCode` is a closed union of twelve and `types.ts:159-163` states that adding a member is
        a breaking change *by design*; none of the twelve means "this runtime is not built yet";
        `unknown_action` would be a lie for an action plainly in the catalog and `handler_error` would
        be a lie. Omitting `reason` asserts nothing false. Record the rejected alternative
        (`not_implemented` added to the union — a `types.ts` contract change Phase 6 would
        immediately have to remove) and state that **Phase 6's DSP-09 normalizer must REPLACE this
        shape, not normalize it.** Confirm the message is shorter than `MESSAGE_MAX_CHARS` (180) and
        record its length in the summary.
    13. `catalogFor: (ctx) => projectFor(resolveIndex(ctx))`.
    14. `stageFor: (ctx) => { const index = resolveIndex(ctx); return index === null ? null : (stages[index]?.id ?? null); }`.
    15. `function explain(ctx: StageContext): Explanation` — **one pass, every matcher, exactly
        once**:
        build `const rows: StageExplanation[]` by calling `stages.map(...)` with an annotated
        `(stage): StageExplanation` callback returning `{ id: stage.id, matched: runMatch(stage, ctx),
        bridge: bridgeStatus(stage) }`; then derive the active position with the single line
        `const firstMatch: number = rows.findIndex((row) => row.matched);` — that exact spelling is
        M-04-12's literal — and then `const activeIndex: number | null = firstMatch === -1 ? null : firstMatch;`.
        Finally return `deepFreeze({ stage: activeIndex === null ? null : (stages[activeIndex]?.id ?? null), stages: rows, catalog: projectFor(activeIndex).map((tool) => tool.name) }, NO_SKIP, new WeakSet<object>())`.

        Three constraints on this shape, all doc-commented:
        - **`stages.map(...)` and not a `for…of`.** `map` evaluates every matcher exactly once and
          structurally cannot short-circuit, which is the property DX-01 needs: the single most
          likely answer to "why didn't my action fire" is *an earlier stage shadowed yours*, and a
          short-circuiting `explain` reports `matched: false` for the shadowed stage — which is not a
          measurement, it is "we never asked", rendered as a negative. It is also textually distinct
          from `resolveIndex`'s loop, which mutant M-04-4 requires.
        - **`firstMatch` is derived from the recorded rows, not from a second matcher evaluation.**
          Calling `stageFor` here would be a second evaluation of consumer code that need not agree
          with the first; measured, a matcher with an internal counter produced the self-contradictory
          `{"stage":"flaky","stages":[{"id":"flaky","matched":false}]}`. A diagnostic that contradicts
          itself is worse than no diagnostic, because the developer debugs the tool.
        - **`projectFor(activeIndex)`, not `catalogFor(ctx)`** — reading the memo directly is what
          guarantees `explain().catalog` and `explain().stage` cannot disagree; `catalogFor` would
          re-resolve and could land on a different stage under a non-deterministic matcher.

        `explain` writes nothing to the console: structured return only, no `warnHost`. Phase 3's
        precedent is that the structured value is the assertable channel and console output is the
        convenience one, and a convenience with no test is a surface with no guarantee. (`runMatch`
        may still warn on a broken matcher — that is the matcher policy, not `explain` printing.)
    16. `return { dispatch, catalogFor, stageFor, explain };` — **do NOT freeze the returned object.**
        Anchor it as a deliberate non-claim; Task 2 writes the reason, and the reason is only this:
        SEC-03 names the action **registry**, which is frozen, and the `Concierge` object is not part
        of it; Phase 6 replaces `dispatch` wholesale; and `ServerSafeConcierge` (deferred to Phase 9)
        may yet change its shape. **Do not justify it with a freeze count.** An earlier draft of this
        step said freezing would add "a fourth `Object.freeze(` occurrence to a file whose mutant
        literals depend on there being exactly three" — the file has four already (step 3), so writing
        that into a shipped comment would put a false claim into the one prose channel 04-08 audits.

    **The anchor inventory — nineteen sites, one line each.** Write the file header IN FULL (the
    three numbered constraints above are the header, and they carry both of the phase's load-bearing
    corrections). Everywhere else, leave a single line of the exact form
    `// ANCHOR(T2): <the claim this site owes>` — no more. Task 2 expands each into its full doc
    comment with the measurement. The marker is the handshake: `grep -c 'ANCHOR(T2)'` reads 0 before
    Task 1, 19 after Task 1, and 0 again after Task 2, so neither half can be silently skipped.

     1. `duplicateStageIdMessage` — why the message lives behind a named function (M-04-15 targets its
        single call site, and `warnHost(` occurs twice in this file), why the scan uses **two** sets
        rather than one, and that catalog scoping is unaffected because the per-stage catalog is keyed
        by declaration order rather than by id.
     2. `bridgeStatus` — the three states, why the shape survives Phase 5 unchanged, and the rejected
        alternative of warning on a throwing `read()`.
     3. `NO_SKIP` — never written, so header constraint 1's SSR argument does not reach it; plus
        `/* @__PURE__ */` and 03-08's dead-bytes finding.
     4. Step 2 — one flat `buildCatalog`; the per-stage view is a projection of that one catalog; a
        duplicate action name across two stages is rejected globally with no new code.
     5. Step 3 — `parameters` is assigned BY REFERENCE and never re-emitted, citing `Catalog.byName`;
        and the **four**-`Object.freeze(` uniqueness constraint.
     6. Step 5 — `namesByStage` is indexed, never keyed by id; the measured collapse; both rejected
        remedies.
     7. Step 7 — `Map` rather than a null-prototype record, because the key type is `number | null`;
        the `String(null)` collision; cite `catalog.ts:260-268`.
     8. Step 8 — `warnStage` returns the literal type `false`, which is what makes the `catch` body a
        single statement and M-04-6 a single-literal swap.
     9. Step 9, the `catch` — bare `catch {` with no binding; the decision inverted relative to
        `json-schema.ts`'s two; the covert-channel reason.
    10. Step 9, the comparison — `=== true` over truthiness; the measured probe; P14's first-run
        experience; both rejected alternatives.
    11. Step 10 — `stageFor` is not memoized and why; the measured key-ordering result.
    12. Step 11 — the no-stage branch returns the cross-stage actions rather than an empty array; the
        rejected fail-closed alternative; and that the situation is not hidden.
    13. Step 12 — `reason` deliberately omitted; the closed twelve-member union; the rejected
        `not_implemented`; and that Phase 6's DSP-09 normalizer must REPLACE this shape.
    14. Step 15 — `stages.map(...)` and not a `for…of`, and why the property matters for DX-01.
    15. Step 15 — `firstMatch` is derived from the recorded rows, not from a second matcher
        evaluation; the measured self-contradiction.
    16. Step 15 — `projectFor(activeIndex)` and not `catalogFor(ctx)`; and that `explain` writes
        nothing to the console.
    17. Step 16 — the returned object is deliberately not frozen, and the reason carries no freeze
        count.
    18. The **CAT-01 name-union erasure stops at the config boundary**, and that is correct. Measured
        three ways: `buildCatalog([alpha, beta])` derives `"alpha" | "beta"`, but
        `buildCatalog([...config.stages.flatMap(s => s.actions), ...crossStage])` derives `string` —
        caused by `ConciergeConfig.stages: ReadonlyArray<StageDefinition<any>>`, D-07's deliberate
        erasure, not by `flatMap`. Nothing downstream wants the union today:
        `Concierge.dispatch(name: string, …)`, `EmittedTool.name: string`,
        `Session.stage(): string | null`. `createConcierge<const C extends ConciergeConfig>(config: C)`
        **would** recover it (measured) and is deliberately not taken — CONTEXT locks the non-generic
        signature, and the union has nowhere to go without making `Concierge` generic and rippling
        into `Session`, `SessionConfig` and every adapter.
    19. `explain()`'s **deliberate non-identity**, in those words: the returned object is a fresh
        object every call by design, so nobody wires it into `useSyncExternalStore` and reproduces the
        exact infinite-render defect STG-04 exists to prevent. The requirement that motivates the
        whole phase is one line away from being violated by the phase's own diagnostic.

    Do NOT add `createConcierge` to `src/index.ts` in this task — the export and both its pins move
    together in Task 3. This task leaves `concierge.ts` unreachable from the barrel, exactly as
    03-03 left `catalog.ts` before 03-04 exported it, so `pnpm test` must stay at 6 files / 55 tests.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm build && pnpm test && pnpm check:deps && echo CONCIERGE_COMPILES</automated>
  </verify>
  <acceptance_criteria>
    - **Standing rule for every grep in this plan.** `packages/concierge/src/concierge.ts` does not
      exist on the pre-task tree, so every literal below measures **0** before this task and every
      one of them is a positive criterion that inaction cannot satisfy. Where a criterion demands a
      count of 0 on a file that already exists, its pre-edit count is recorded beside it. A criterion
      already sitting at its PASS value is no-coverage, not a pass — if one of yours measures that
      way, replace it rather than accept it.
    - `pnpm typecheck`, `pnpm build`, `pnpm test`, `pnpm check:deps` exit 0. `pnpm test` still reports
      6 files / 55 tests green — `concierge.ts` is not reachable from the barrel yet, so `dist/`'s
      surface is unchanged and `test/export-surface.test.ts` still reads 59/49/10.
    - `grep -c 'export function createConcierge(config: ConciergeConfig): Concierge' packages/concierge/src/concierge.ts`
      returns 1 — the explicit return annotation `isolatedDeclarations` requires.
    - Every one of these unfiltered counts prints exactly 1 (run
      `grep -F -o -- '<literal>' packages/concierge/src/concierge.ts | wc -l` for each, comments
      INCLUDED) and each is recorded in the summary with its count:
      `Object.freeze(projected)`, `Object.freeze(tool)`, `Object.freeze(toolByName)`,
      `memo ??= new Map`, `memo.set(index, built);`,
      `for (const [index, stage] of stages.entries())`, `result === true`,
      `index === null ? crossNames`, `...crossNames]`, `deepFreeze(`,
      `const firstMatch: number = rows.findIndex((row) => row.matched);`, and
      `warnHost(duplicateStageIdMessage(stage.id));`.
    - **The two `runMatch` return statements, spelled out.**
      `grep -F -o -- 'Object.freeze(' packages/concierge/src/concierge.ts | wc -l` prints **4** — the
      three assembly freezes plus `DISPATCH_NOT_IMPLEMENTED`'s. `grep -F -o -- 'return warnStage('
      packages/concierge/src/concierge.ts | wc -l` prints **2**. Neither bare prefix is a usable
      mutant literal. Copy the `catch` branch's **complete** `return warnStage(…);` statement and the
      non-boolean branch's **complete** statement into the summary verbatim, confirm each is unique
      with `grep -F -o -- '<statement>' … | wc -l` printing 1, and confirm the two differ textually.
      04-07 runs M-04-6 from the `catch` branch's statement as recorded.
    - `grep -c 'ANCHOR(T2)' packages/concierge/src/concierge.ts` returns **19**, one per inventory
      entry, and every line matches the exact form `// ANCHOR(T2): `. **Pre-task measured: 0** (no
      such marker exists anywhere in `packages/concierge/src/`).
    - `grep -F -o -- 'for (const stage of stages)' packages/concierge/src/concierge.ts | wc -l`
      prints 1 (the duplicate-id scan) and it is textually distinct from the resolution loop.
    - `grep -v '^[[:space:]]*[/*]' packages/concierge/src/concierge.ts | grep -c 'catch (' ` returns 0
      — both catches are bare `catch {`, so no caught value is in scope to echo.
    - `grep -v '^[[:space:]]*[/*]' packages/concierge/src/concierge.ts | grep -c 'console\.'` returns
      0; `grep -c 'warnHost(' packages/concierge/src/concierge.ts` returns 2.
    - `grep -v '^[[:space:]]*[/*]' packages/concierge/src/concierge.ts | grep -c 'Object.create(null)'`
      returns 1, and the same filtered file contains no `for (const … in …)`.
    - `grep -ci 'sideEffects\|tree-shak\|treeshake' packages/concierge/src/concierge.ts` returns 0 —
      the measured-false justification appears nowhere. `grep -ci 'SSR\|cross-request'
      packages/concierge/src/concierge.ts` returns at least 1, from the file header's constraint 1.
    - `grep -c 'reason' packages/concierge/src/concierge.ts` — read every hit and confirm none of
      them is a `reason:` property on `DISPATCH_NOT_IMPLEMENTED`.
    - The `DISPATCH_NOT_IMPLEMENTED` message string is shorter than 180 characters; state its measured
      length in the summary.
    - `wc -l packages/concierge/src/concierge.ts` is at least 200 at the close of THIS task; the file
      passes 300 in Task 2, when the anchors become doc comments. Record both numbers.
    - `pnpm check:deps` reports the same byte count as before this plan, or records the delta.
  </acceptance_criteria>
  <done>`createConcierge` assembles one flat catalog, resolves stages in declaration order with one
  guarded matcher call, serves per-stage projections from an index-keyed instance-local memo whose
  arrays are frozen and identical across calls, and answers DX-01's three questions in one pass.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the nineteen anchors into doc comments, and correct contract.ts's now-false claim</name>
  <files>packages/concierge/src/concierge.ts, packages/concierge/src/contract.ts</files>
  <read_first>
    - `packages/concierge/src/concierge.ts` as Task 1 left it — the nineteen `ANCHOR(T2)` lines ARE
      the work list, and the file header is already written. Read the file once; do not re-read it
      per anchor.
    - `packages/concierge/src/catalog.ts:1-63` (the header register), `:142-146` and `:506-511` (the
      warn-once granularity argument and the message shape), `:240-268` (`Catalog.byName`, and the
      sentence that settles `Map`-vs-record: "A `Map` remains correct for Phase 6's own **mutable**
      per-dispatch state … and is wrong for anything that must be frozen"), `:348-359` and `:601-612`
      (the two models for annotating a correction in place — a bolded lead, the measurement, then
      what it does and does not widen), and `:513-591` (`deepFreeze`'s refusal to early-out on
      `Object.isFrozen`, which is why per-projection `deepFreeze` measured 510× slower).
    - `packages/concierge/src/define-action.ts:212-233` — the register for a doc comment that records
      a rejected extension together with its measurement.
    - `packages/concierge/src/json-schema.ts:230-245`, `:259-261` and `:384-398` — the two existing
      `try`/`catch` blocks and the stated build-time exemption that `runMatch` deliberately inverts.
    - `packages/concierge/src/contract.ts:100-173` in full — the doc comment (b) edits, the
      instruction at `:109-113` ("Call this from the first reachable entry point … and never at
      module scope") which governs the alternative, the same-version adopt path at `:115-120` that
      makes a second direct call a documented no-op, and the paragraph at `:140-147` whose closing
      sentence is what goes false.
    - **The measurement-bearing ranges Task 1 deferred — this task's raw material.**
      `04-RESEARCH.md:240-345` (*Pattern 1* — React's `checkIfSnapshotChanged`, Svelte 5's `equals`,
      the measured identity probe, and the `Map`-vs-record measurement), `:346-439` (*Pattern 2* —
      the seven tamper vectors, the 0.0074 ms vs 3.78 ms timing for 40 projections, the
      element-sharing measurement, and the tool-injection consequence), `:440-506` (*Pattern 3* — the
      measured truthy probe and the key-ordering table), `:575-658` (*Pattern 5* — the measured
      two-pass contradiction `{"stage":"flaky","stages":[{"id":"flaky","matched":false}]}`),
      `:745-758` (*Pitfall 7* — the measured id-collapse), `:760-791` (*Pitfall 8* — the tree-shaking
      claim measured false under rolldown 1.2.0 and the SSR reason that is true), `:1048` (the two
      options for `contract.ts` and which is cheaper), and `:1078-1089` (the `dispatch` stub and the
      `const`-type-parameter measurement).
    - `04-CONTEXT.md:37-93`, `:95-119`, `:144-192`.
    - `04-PATTERNS.md:63-418` (the prose registers for this file) and `:733-758` (the `contract.ts`
      edit's pattern assignment).
  </read_first>
  <action>
    Two files, and **both edits are prose**. `git diff` for this task must show only comment lines in
    `concierge.ts` and only block-comment lines in `contract.ts`. If a statement moves, that is a
    defect, not a tidy-up.

    **(a) Expand every `ANCHOR(T2)` line in `src/concierge.ts` into its full doc comment**, in the
    house form: a bolded lead, the measurement, then what the decision or correction does and does
    not widen. **Delete the marker as you expand it** — a surviving `ANCHOR(T2)` is an unwritten
    comment, and the acceptance criteria count them. Each anchor's content is specified in Task 1's
    nineteen-entry inventory; the measurements come from the ranges named above.

    Three of the nineteen are load-bearing beyond their own site and must not be compressed:
    - **Anchor 6 (`namesByStage` is indexed).** The bolded lead, the measured collapse, what it does
      not widen, and BOTH rejected remedies — throwing needs a `CatalogIssue` whose `action` field
      holds a stage id, which corrupts the `issues.map(i => i.action)` semantics DX-03 depends on;
      warn-only leaves a real correctness bug in place.
    - **Anchor 13 (`dispatch`'s omitted `reason`).** It looks like an oversight and someone will
      "fix" it if the reason is not written down. State that `ReasonCode` is a closed union of
      twelve, that `types.ts:159-163` makes adding a member a breaking change by design, that none of
      the twelve means "this runtime is not built yet", that `unknown_action` and `handler_error`
      would each be a lie, and that **Phase 6's DSP-09 normalizer must REPLACE this shape, not
      normalize it.**
    - **Anchor 9 (the bare `catch {`).** Without it, a later reader comparing this block with
      `json-schema.ts`'s two will "fix" the inconsistency. Those are build-time developer diagnostics
      with the exemption stated at `json-schema.ts:259-261`; this one fires at runtime, on every
      navigation, in a shipped app, and the caught message is whatever the app's matcher put in it.

    **Two claims are forbidden anywhere in this file**, and each has a grep below: the memo may never
    be justified by `sideEffects` or tree-shaking (measured under rolldown 1.2.0 and does not
    reproduce — a module-scope `Map` read by an exported function is retained and behaves identically
    bundled and unbundled), and no comment may state an `Object.freeze(` count other than **four**.

    **(b) Correct `src/contract.ts:143-147`.** It currently closes with "…though the `createConcierge`
    and adapter-registration call sites it also names remain future work and should be added when
    those arrive." `createConcierge` arrives in Task 1 of this plan, and this comment ships — verified
    at `dist/index.d.ts:1837`. Note that the phrase **wraps** across `:146`/`:147`, so it is not a
    contiguous string in the source; the contiguous, greppable half is `future work and should be
    added`.

    **Take the sentence correction, not the second call.** Rewrite the closing clause in the house
    annotate-in-place form to state that `createConcierge` (`./concierge.ts`) arrived in Phase 4 and
    **reaches this guard transitively**, on `buildCatalog`'s first line, because assembling a catalog
    is the first thing it does; that a second direct call would be a documented no-op via the
    same-version adopt path at `:115-120`, so it was measured unnecessary rather than forgotten; and
    that the adapter-registration call site remains genuinely future work and stays named as such.

    Do not weaken the instruction at `:109-113`. Do not touch `CONTRACT_VERSION`, the registry key,
    or the thrown message — `test/single-instance.test.ts` and `test/artifact.test.ts` pin all three.
    Change nothing outside the block comment.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm build && pnpm test && git diff -U0 -- packages/concierge/src/contract.ts</automated>
  </verify>
  <acceptance_criteria>
    - **Standing rule.** Every literal below was measured against the tree before this task and its
      pre-edit count is recorded. A criterion already at its PASS value cannot distinguish "done"
      from "never started"; if one of yours measures that way, replace it.
    - `pnpm typecheck`, `pnpm build`, `pnpm test` exit 0, and `pnpm test` still reports 6 files /
      55 tests green — this task changes no statement.
    - `grep -c 'ANCHOR(T2)' packages/concierge/src/concierge.ts` returns **0**. **Pre-task measured:
      19** (Task 1's output; 0 on the tree before Task 1).
    - `grep -c 'future work and should be added' packages/concierge/src/contract.ts` returns **0**.
      **Pre-edit measured: 1** (`contract.ts:147`). Do NOT substitute `remain future work` — that
      phrase wraps in the source and measures **0** on the unmodified tree, which is why it was
      removed as a criterion.
    - `grep -c 'reaches this guard transitively' packages/concierge/src/contract.ts` returns **1** —
      the positive half, without which the negative alone cannot tell "rewritten" from "deleted".
      **Pre-edit measured: 0.** If the rewrite words it differently, substitute the new sentence's own
      distinctive literal and record both counts: 0 before, 1 after.
    - `grep -c 'createConcierge' packages/concierge/src/contract.ts` — **pre-edit measured: 2**, so
      this count alone proves nothing and is deliberately NOT an acceptance criterion. Read both
      existing hits and every new one instead; confirm each is inside a comment and none describes
      `createConcierge`'s call site as pending.
    - `grep -c 'export const CONTRACT_VERSION = 1;' packages/concierge/src/contract.ts` returns 1.
      **Pre-edit measured: 1** — a must-not-change guard, labelled as such rather than as progress.
    - `git diff -U0 -- packages/concierge/src/contract.ts` shows changes only inside the block comment
      at `:105-150`; read every changed line and confirm no non-comment line moved.
    - `git diff -U0 -- packages/concierge/src/concierge.ts` shows **only comment lines** changed.
    - `grep -ci 'sideEffects\|tree-shak\|treeshake' packages/concierge/src/concierge.ts` returns 0 and
      `grep -ci 'SSR\|cross-request' packages/concierge/src/concierge.ts` returns at least 1.
    - `grep -F -o -- 'Object.freeze(' packages/concierge/src/concierge.ts | wc -l` still prints 4.
      Read every comment stating a freeze count and confirm each says **four**.
    - `grep -c 'DSP-09' packages/concierge/src/concierge.ts` returns at least 1 — the Phase 6
      hand-off lives in the source, not only in the summary. **Pre-task measured: 0.**
    - `wc -l packages/concierge/src/concierge.ts` is at least 300; record it against Task 1's value so
      the delta is visible.
    - `pnpm test single-instance` and `pnpm test artifact` both pass (bare form, no `--`).
  </acceptance_criteria>
  <done>Every recorded decision this module owes is written where the next reader meets it; the two
  corrections the phase exists to get right are in the file and their measured-false alternatives are
  in it nowhere; and the shipped declarations no longer describe `createConcierge`'s call site as
  future work while `createConcierge` sits two modules away calling `buildCatalog` on its first
  line.</done>
</task>

<task type="auto">
  <name>Task 3: Reach concierge.ts from the barrel and move the export surface to 62/51/11 in one commit</name>
  <files>packages/concierge/src/index.ts, packages/concierge/test/export-surface.test.ts, packages/concierge/test-d/exports.test-d.ts, packages/concierge/test/artifact.test.ts</files>
  <read_first>
    - `packages/concierge/src/index.ts` in full (111 lines) — the module doc comment at `:1-25`, the
      three `export type { … }` blocks with their category comments, and the one-statement-per-source-
      module rule for value exports at `:105-111`.
    - `packages/concierge/test/export-surface.test.ts` in full (160 lines) — the parser at `:74-99`
      (**do not touch it**; its throw is the guard against "fixing" a red count by weakening the
      regex), the `VALUE_EXPORTS` array at `:106-121` and the header paragraph at `:101-105`
      explaining that the third `it` title states this array's LENGTH while its assertion carries no
      number, so the array is the only thing the title can be checked against, and the three `it`
      blocks at `:132-149`. Current measured state: `blocks 1 names 59 values 10 types 49`.
    - `packages/concierge/test-d/exports.test-d.ts` in full (94 lines) — the header at `:51-68`
      (the TS1485-at-the-shared-import-line behaviour, and the deliberate looseness of the
      `Assignable` signature which must NOT be tightened), the shared import line at `:71`, and the
      five existing predicates.
    - `packages/concierge/test/artifact.test.ts:1-24` and `:107-127` — the header's "different
      sampling rates, same defect" paragraph and the four existing value-export cases.
    - `04-RESEARCH.md:997-1048` — the corrected export baseline (59/49/10, NOT 42), the three names
      this phase adds, the seven pins that move together, and the two `src/index.ts` doc-comment
      paragraphs that become false.
    - `04-PATTERNS.md:673-730` (the barrel's three edits) and `:1404-1482` (the seven things that move
      together in `export-surface.test.ts`) and `:1627-1652` (the `artifact.test.ts` planner
      decision).
  </read_first>
  <action>
    One task, four files, because the export surface is pinned twice and both pins must move in the
    same commit as the export itself. Splitting this leaves `pnpm test export-surface` red between
    commits.

    Six pre-edit counts, taken so the criteria below can distinguish done from not-started:
    `grep -c '"createConcierge"' packages/concierge/test/export-surface.test.ts` = **0**;
    `grep -c 'createConcierge' packages/concierge/test-d/exports.test-d.ts` = **0**;
    `grep -c 'createConcierge' packages/concierge/test/artifact.test.ts` = **0**;
    `grep -c 'defineStage' packages/concierge/src/index.ts` = **1**;
    `grep -c 'is still being implemented' packages/concierge/src/index.ts` = **1**;
    `grep -c 'deepFreeze' packages/concierge/src/index.ts` = **0** — a must-STAY-0 guard, not a
    progress check, and labelled as such below.

    **(a) `src/index.ts` — three edits.**

    - Add `Explanation` and `StageExplanation` to the **first** `export type { … }` block (the one
      sourced from `./types.js`), under the existing `// Concierge` category comment beside
      `Concierge`, `ConciergeConfig`, `Session`, `SessionConfig`. Not a new block.
    - Add `export { createConcierge } from "./concierge.js";` as its own statement, following the
      one-statement-per-source-module convention at `:105-111`.
    - **`deepFreeze` must NOT appear here.** It is a module-internal export from `catalog.ts` only.
    - **Rewrite the two doc-comment paragraphs that this phase makes false** (`:16-24`). RESEARCH is
      explicit that this needs *an honest rewrite, not a deletion — the "not oversold" posture is the
      point.* The `:22-24` sentence lists `createConcierge` as unimplemented (it ships now) and lists
      `defineStage` (which is now **not going to ship at all**, locked — listing it as pending is a
      promise the project has decided not to keep). The `:16-20` paragraph is still true about
      dispatch, session, transport and consent, but now under-sells: stage scoping and `explain()`
      ship. The replacement must say plainly what now exists (a catalog assembled from stages, a
      stage-scoped frozen tool list with a stable reference, and `explain()`), what still does not
      (nothing dispatches — `dispatch` returns a not-implemented result — and there is no session, no
      transport, no consent prompt), and that `defineStage` is not planned because stage matching
      needs no identity mechanism; the remaining unimplemented runtime is `createSession` and
      `createBridge`.

    **(b) `test/export-surface.test.ts` — six numbers and one array entry, all at once.**
    `:133` title `59` → **62**; `:135` `toHaveLength(59)` → **62**; `:138` title "splits 49 types to
    10 values" → **"splits 51 types to 11 values"**; `:140` `49` → **51**; `:141` `10` → **11**;
    `:144` title "all ten runtime value exports" → **"all eleven runtime value exports"**; and add
    `"createConcierge"` to `VALUE_EXPORTS`. **Do not touch the parser at `:74-99`.**

    **(c) `test-d/exports.test-d.ts` — one name, one predicate, one count.**
    Add `createConcierge` to the **shared import line at `:71`** (from `../src/index.js`, never
    `../src/types.js` — that is the whole point of the file), add the sixth predicate in the house
    shape on ONE line however long:
    `/** createConcierge reaches the public entrypoint as a callable VALUE, not only as a type. */`
    then `type _createConciergeExportedAsValue = Expect<Assignable<typeof createConcierge, (...args: never[]) => unknown>>;`
    and update the header's count from five predicates to six. Keep the signature deliberately loose
    — `(...args: never[]) => unknown` asserts only "this is a function value", which is all the
    export-PLACEMENT guarantee needs; the real signature is pinned in `test-d/concierge.test-d.ts`
    (plan 04-06). The header already argues why tightening it would be wrong.

    **(d) `test/artifact.test.ts` — add the `createConcierge` case. This is the planner's decision on
    the open item `04-PATTERNS.md:1627-1652` raises, and it is ADD rather than record-why-not**: the
    established convention is that every value export gets an artifact-level case, all four of Phase
    3's have one, and this is one `it`. Copy `:107-115`'s shape, including the consequence sentence in
    the comment — a `createConcierge` lost to the `export type { … }` block is
    `TypeError: createConcierge is not a function` at the consumer's module scope, which reads as
    "the package is broken" rather than "one export moved". Record in this plan's summary that the
    decision was taken and why, so the open item is closed rather than left inferred.

    Order of operations: edit all four files, then `pnpm build`, then `pnpm test`. Every dist-reading
    suite goes red until `pnpm build` re-runs after new exports land.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm build && pnpm test && pnpm check:artifact && pnpm check:pack && echo SURFACE_MOVED</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck`, `pnpm build`, `pnpm test`, `pnpm check:artifact`, `pnpm check:pack` exit 0.
    - `pnpm test export-surface` (bare — **never** `pnpm test -- export-surface`, which does not
      filter) passes, and the three `it` titles read 62 / "splits 51 types to 11 values" /
      "all eleven runtime value exports".
    - Independently re-derive the numbers rather than trusting the test:
      `grep -c '' packages/concierge/dist/index.d.ts` is not the check — instead run the suite's own
      regex over `dist/index.d.ts` and confirm it reports `names 62 values 11 types 51`. Paste the
      output into the summary.
    - `grep -c '"createConcierge"' packages/concierge/test/export-surface.test.ts` returns 1.
    - `grep -c 'createConcierge' packages/concierge/test-d/exports.test-d.ts` returns 2 (the shared
      import line and the predicate).
    - `grep -c 'deepFreeze' packages/concierge/src/index.ts` returns 0 — **pre-edit measured 0**, so
      this is a must-stay-0 regression guard against re-exporting a module-internal symbol, not a
      progress check, and it is recorded as such.
    - `grep -c 'defineStage' packages/concierge/src/index.ts` returns 0 OR every remaining occurrence
      states that it is not planned — read each hit. **Pre-edit measured: 1.**
    - `grep -c 'is still being implemented' packages/concierge/src/index.ts` returns 0, because that
      phrasing listed `createConcierge`. **Pre-edit measured: 1.**
    - `grep -c 'createConcierge' packages/concierge/test/artifact.test.ts` returns at least 1 and
      `pnpm test artifact` passes.
    - `git diff -- packages/concierge/test/export-surface.test.ts` shows no change between lines 74
      and 99 (the parser).
  </acceptance_criteria>
  <done>`createConcierge`, `Explanation` and `StageExplanation` reach the published entrypoint; both
  export-surface pins and the artifact-level probe moved with them in one commit; and the module doc
  comment describes what the package actually does.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party page script → the tool array handed to the model | Any script on the page shares the app's authority and can reach the array `catalogFor` returns. This is the still-open half of SEC-03 and the half this plan closes. |
| consumer `match()` and `BridgeRegistry.read()` → core | Core invokes code it did not author, on every navigation for `match()` and on every `explain()` for `read()`. Neither may take down the render, and neither's thrown value may leave the boundary. |
| caller `StageContext` → consumer matchers | Arbitrary app state, deliberately unvalidated (STG-03 requires it), and therefore never interpolated into a warning, an error, or a tool description. |
| SSR process → per-request `Concierge` instances | Module instances are reused across server requests; instance-local lazily-allocated state is what keeps two requests apart. |
| core → host `console` | A global core cannot type-see under `lib: ["ES2022"]`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering / Elevation | the array returned by `catalogFor` | mitigate | `Object.freeze(projected)` on every projection, over elements that are shared and already deep-frozen. Measured: `push` onto an unfrozen `.filter()` result succeeded and the agent's list became `['a','injected']`. The injected tool has no handler (dispatch resolves through the frozen `byName`), so the payload is the **description** — CAT-07's compile-time guarantee defeated at runtime. |
| T-04-02 | Elevation of Privilege | capability leakage across stages | mitigate | STG-01 by projection, not by rejection at call time: a checkout action is *absent from the results catalog* rather than refused when called. `namesByStage` is built per stage and the projection maps only those names. |
| T-04-03 | Elevation of Privilege | wrong-stage catalog via colliding stage ids | mitigate | The memo and the name lookup are keyed by declaration **index** (`number \| null`), which makes the measured collapse structurally impossible, **plus** one `warnHost` per distinct duplicated id, because the id is still what `stageFor`, `Session.stage()` and `explain()` report. Both halves are required; either alone leaves a defect. |
| T-04-04 | Denial of Service | a matcher that throws inside the consumer's render | mitigate | `runMatch` is the single call site; the throw is caught, the stage is treated as a non-match, and the stage id is warned once per instance. Letting it propagate is rejected: the loudness lands on the end user's blank screen, on a path that runs on every navigation. |
| T-04-05 | Information Disclosure | the matcher warning message | mitigate | `catch {` takes **no binding**, so the caught error's `message` is not in scope to echo. The warning carries only the stage id and fixed prose. The inverted decision relative to `json-schema.ts`'s two build-time catches is written into the doc comment so a later reader does not "fix" the inconsistency. |
| T-04-06 | Information Disclosure | cross-request state pollution under SSR | mitigate | Both mutable structures (`memo`, `warnedStages`) are instance-local `let`s initialised to `null` and allocated on first use. Module scope holds only `NO_SKIP` and `DISPATCH_NOT_IMPLEMENTED`, both immutable, and the doc comment states the SSR reason rather than the measured-false tree-shaking one. |
| T-04-13 | Tampering | prototype pollution through `toolByName` | mitigate | `Object.create(null)` plus `Object.freeze(toolByName)` — the same two properties `catalog.byName` has, measured: `tools['__proto__']` and `tools['constructor']` are ordinary absent keys and every write throws. |
| T-04-09 | Tampering | handler substitution through the catalog | mitigate | Closed in Phase 3 by `deepFreeze`; unchanged here. `EmittedTool` deliberately carries neither `handler` nor `schema`, so the projection is a privilege reduction rather than a rename. `test/catalog.test.ts` C17/C18 must stay green. |
| T-04-10 | Spoofing | two independently-resolved copies of core | mitigate | `createConcierge` reaches `assertSingleInstance` transitively through `buildCatalog`'s first line. Task 3 corrects the shipped claim rather than adding a second call, which the same-version adopt path makes a documented no-op. |
| T-04-11 | Tampering | `concierge.dispatch = attackerFn` on the returned object | **accept** | The returned `Concierge` is deliberately not frozen. SEC-03 names the action *registry*, which is frozen; the object is not part of it, Phase 6 replaces `dispatch` wholesale, and `ServerSafeConcierge` (deferred to Phase 9) may change its shape. Recorded in the doc comment (anchor 17) so a reviewer does not add the freeze silently. **No freeze-count justification** — the file already contains four `Object.freeze(` occurrences, so the earlier "it would be a fourth, and the mutant literals depend on there being three" argument was false and is removed. |
| T-04-07 | Tampering | a getter inside a consumer-supplied `jsonSchema` | **accept** — inherited, out of scope | Measured: the accessor still varies after the freeze, because `deepFreeze` deliberately skips accessors so that walking the catalog never invokes application code, and for `emission.source === "explicit"` the `parameters` object IS the consumer's own object by reference. Re-freezing the projection changes nothing — it is the same object. **No plan in this phase may record "SEC-03 closed" without this carve-out.** |
| T-04-12 | Information Disclosure | `explain()`'s return value | accept | Carries stage ids, booleans, bridge ids and action names — all developer-authored strings already in the config. `StageContext` is never echoed into it. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing. `04-RESEARCH.md:145-158` records the Package Legitimacy Gate as not applicable for this phase; `pnpm-lock.yaml` must be byte-identical at phase exit and `pnpm check:deps` must stay exit 0. |
</threat_model>

<verification>
- `pnpm typecheck`, `pnpm build`, `pnpm test`, `pnpm check:artifact`, `pnpm check:deps`,
  `pnpm check:pack` all exit 0.
- `pnpm test` reports 6 files / 55 tests green after Task 1 and after Task 2; the export-surface
  counts move only in Task 3 and land at 62 / 51 / 11.
- `git status --porcelain` shows only this plan's six files.
- `git diff --stat -- pnpm-lock.yaml` is empty.
- `git diff -U0 -- packages/concierge/src/contract.ts` shows comment-only changes.
</verification>

<success_criteria>
- The catalog an agent is offered is scoped to where the user is, and the actions for elsewhere are
  absent rather than refused.
- Two calls with equivalent context return the identical array reference, so a subscriber cannot be
  driven into an infinite re-render.
- The array and its elements cannot be mutated by anything on the page.
- A broken matcher degrades honestly, names itself once, and echoes nothing it caught.
- `explain()` answers DX-01's three questions in one pass and cannot contradict itself.
- The published surface, both its pins, and the module doc comment all describe the same package.
</success_criteria>

<output>
Create `.planning/phases/04-stages-catalog-assembly-and-explain/04-03-SUMMARY.md` when done.
Record: every mutant literal from the acceptance list with its unfiltered count, verbatim, so 04-07
runs the battery without re-deriving them — including **both** complete `return warnStage(…);`
statements (the `catch` branch's is M-04-6's literal; the bare prefix occurs twice and is unusable)
and M-04-12's `firstMatch` line; the measured `Object.freeze(` count of **4**; the measured length of
`DISPATCH_NOT_IMPLEMENTED.message` against `MESSAGE_MAX_CHARS`; the `ANCHOR(T2)` count at the close of
Task 1 (19) and of Task 2 (0), with the `wc -l` at each; the two `contract.ts` counts —
`future work and should be added` 1 → 0 and the new sentence's distinctive literal 0 → 1; the
export-surface regex output showing `names 62 values 11 types 51`; the `check:deps` byte count and its
delta; the decision to ADD a `createConcierge` case to `test/artifact.test.ts` and why, which closes
`04-PATTERNS.md:1627-1652`'s open item; the statement that **CAT-01's fifth derived artifact,
per-stage catalogs, now ships** as `createConcierge().catalogFor`, for 04-08 Task 2(c) to record as
the closure; and the hand-forward that **Phase 6's DSP-09 normalizer must REPLACE the
`{ok:false, message}` dispatch stub rather than normalize it**, because the stub deliberately omits
`reason`.
</output>
