---
phase: 04-stages-catalog-assembly-and-explain
plan: 08
type: execute
wave: 5
depends_on: ["04-07"]
files_modified:
  - .planning/phases/04-stages-catalog-assembly-and-explain/04-VALIDATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CAT-03, STG-01, STG-02, STG-03, STG-04, SEC-03, DX-01, CAT-01]

must_haves:
  truths:
    - "Every gate that exited 0 before this phase still exits 0 after it."
    - "No prose shipped inside `dist/index.d.ts` or `dist/index.js` claims something this phase has made false, asserted with literals each shown able to fire on the pre-correction tree."
    - "CAT-01 is recorded closed — its fifth derived artifact, per-stage catalogs, ships as `createConcierge().catalogFor`."
    - "`pnpm-lock.yaml` is byte-identical to where the phase started."
    - "The record says SEC-03's handler and tool-array halves are closed and the consumer-supplied `jsonSchema` getter channel is not — it does not say SEC-03 is closed."
  artifacts:
    - path: ".planning/phases/04-stages-catalog-assembly-and-explain/04-VALIDATION.md"
      provides: "The signed-off Per-Task Verification Map with observed statuses"
      # `contains` is a plain substring check (`verify.cjs:315` uses `String.includes`),
      # NOT a regex. `nyquist_compliant: true` is therefore the WRONG literal here: it is
      # already a substring of 04-VALIDATION.md:301, the checklist line that QUOTES the key:
      #   - [ ] `nyquist_compliant: true` set in frontmatter
      # while frontmatter line 5 still reads nyquist_compliant: false.
      # Measured on the untouched document: `nyquist_compliant: true` -> 1 (pre-satisfied),
      # `status: signed-off` -> 0. The grep gates below use the line-anchored regex form,
      # which substring semantics cannot express.
      contains: "status: signed-off"
  key_links:
    - from: ".planning/phases/04-stages-catalog-assembly-and-explain/04-VALIDATION.md"
      to: "packages/concierge/test/concierge.test.ts"
      via: "each map row names the automated command and the case that carries it"
      pattern: "pnpm test test/concierge"
---

<objective>
Close the phase: run all seven gates against the final tree, audit the shipped declarations for prose
this phase made false, confirm the lockfile did not move, and sign off the validation map with
observed statuses rather than predicted ones.

Purpose: `types.ts`, `catalog.ts`, `contract.ts` and `index.ts` all ship verbatim inside
`dist/index.d.ts`, so consumers read their doc comments — 03-08 spent a whole plan removing claims
this repository had made false, and this phase adds prose to all four. Two corrections are
load-bearing in the other direction too: the tree-shaking justification for the memo was measured
false and must appear nowhere, and SEC-03 must not be recorded as closed, because the
consumer-supplied-`jsonSchema` getter channel is measured open and is not this phase's to fix.

Output: seven green gates, a clean tree, a byte-identical lockfile, and a signed validation map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-stages-catalog-assembly-and-explain/04-CONTEXT.md
@.planning/phases/04-stages-catalog-assembly-and-explain/04-VALIDATION.md
@CLAUDE.md

<interfaces>
The seven gates that must stay green, all currently exit 0:

  pnpm typecheck        # tsc -p packages/concierge/tsconfig.test-d.json — tsdown does NOT typecheck
  pnpm build
  pnpm test             # every runtime suite reads dist/, so build MUST precede it
  pnpm check:artifact
  pnpm check:deps       # scripts/pkg05-zero-runtime-deps.mjs — the zero-runtime-dependency assertion
  pnpm check:pack
  pnpm check:node-floor

Measured baseline before this phase: `pnpm test` gave 6 files / 55 tests green in 328 ms;
`pnpm typecheck` exit 0; `dist/index.d.ts` parsed to 59 names / 49 types / 10 values.
Expected after this phase: 7 files, and 62 names / 51 types / 11 values.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the seven gates, audit the shipped prose, and confirm the tree and lockfile</name>
  <files>.planning/phases/04-stages-catalog-assembly-and-explain/04-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/03-action-declaration-and-build-time-validation/03-08-PLAN.md` — the
      phase-gate plan this one is modelled on, and in particular how it framed the prose audit, the
      gate set, and what it recorded per gate.
    - `.planning/phases/04-stages-catalog-assembly-and-explain/04-VALIDATION.md` in full — the Test
      Infrastructure table, the **four** command traps (the fourth being that `pnpm test concierge`
      does not filter — it runs the whole suite; `pnpm test test/concierge` is the discriminating
      fragment), the Sampling Rate section, the seeded Requirement to Evidence map, the Mutant
      Obligations table and its standing pre-measurement rule, the Manual-Only Verifications table,
      the "Explicitly NOT closed by this phase" section, and the Validation Sign-Off checklist.
    - Every `04-0*-SUMMARY.md` produced so far — the gate outputs, counts and observations recorded
      per plan are what this task cross-checks rather than re-derives.
    - `04-RESEARCH.md:793-807` (*Pitfall 9*) and `:1391-1394` — the two claims RESEARCH deliberately
      does NOT make, the first of which is that SEC-03 is fully closed by this phase.
    - `04-RESEARCH.md:1038-1048` — the three doc-comment sites that become false this phase, so the
      audit confirms each was corrected rather than assumed.
  </read_first>
  <action>
    **(a) Run the seven gates against the final tree, in order, and record each exit code and its
    salient output.** `pnpm build` must precede `pnpm test`; every runtime suite imports
    `../dist/index.js` and two of them read `../dist/index.d.ts` from disk. Use the bare form
    everywhere — `pnpm test -- <fragment>` does not filter.

    Record for `pnpm test`: the file count, the test count and the duration, against the
    6 files / 55 tests / 328 ms baseline. Record for `pnpm check:deps`: the byte count and its delta
    across the phase.

    **(b) Re-derive the export surface independently of the test.** Run
    `test/export-surface.test.ts`'s own regex over `packages/concierge/dist/index.d.ts` and confirm it
    reports 62 names, 11 values, 51 types. Paste the output into the summary. A test that agrees with
    itself is not the check here; the artifact is.

    **(c) Audit the shipped prose.**

    **Standing rule, and it is the point of this whole sub-task: every audit literal must be shown
    ≥1 on the PRE-CORRECTION tree, or the grep is recorded as no-coverage rather than as a pass.**
    A grep that never matches reads as a passing check — this repository's own named defect class,
    stated at 04-06 T1 and 04-07 T2, and the reason 04-04, 04-06 and 04-07 all require their checks be
    *observed red* before being accepted green. An audit whose literals cannot produce a hit proves
    nothing about the prose; it proves the literal was written without being run.

    Concretely: before correcting anything, run each literal below against the **pre-correction**
    tree — the phase merge base for source files, and a build of it for `dist/` — and record the
    count. Any literal measuring 0 there is replaced with one that measures ≥1, or it is struck and
    logged as no-coverage with the reason. The counts measured at planning time, on the tree as it
    stood before this phase, are given so the executor can tell a genuine correction from a literal
    that was never able to fire.

    **Grep five files, not one.** `dist/index.d.ts` carries only what survives declaration emit —
    interface and const doc comments. `src/index.ts`'s module doc comment and every *implementation*
    comment reach `dist/index.js` instead, or neither. Auditing `.d.ts` alone is what let four of the
    six original literals sit at 0 with nothing wrong being detected. The set is:
    `packages/concierge/dist/index.d.ts`, `packages/concierge/dist/index.js`,
    `packages/concierge/src/index.ts`, `packages/concierge/src/contract.ts`,
    `packages/concierge/src/catalog.ts` — and, once it exists, `packages/concierge/src/concierge.ts`.

    READ every hit rather than counting it.

    | Literal | Pre-phase measured | Where it actually lives | What must be true after |
    |---|---|---|---|
    | `sideEffects` (case-insensitive) | d.ts **11**, js **6**, src **13** | package metadata prose, legitimately | no hit justifies the memo being instance-local — that claim was measured under rolldown 1.2.0 and does not reproduce; the true reason is SSR cross-request pollution |
    | `tree-shak` (case-insensitive) | d.ts **1**, js **1**, src **1** | an existing, unrelated use | same — read the hit and confirm it is not the memo's justification |
    | `future work and should be added` | d.ts **1**, js **1**, contract.ts **1** | `contract.ts:147` | **0** everywhere. This is the contiguous, greppable half of the false claim |
    | `remain future work` | **0 everywhere** | nowhere — the phrase WRAPS across `contract.ts:146`/`:147` | struck: it is **no-coverage**, not a pass. Do not reinstate it |
    | `Hand-forward to Phase 4` | d.ts **0**, js **1**, catalog.ts **1** | `catalog.ts`, an implementation comment | **0** in `catalog.ts` and in `dist/index.js`. It was never in `.d.ts` |
    | `defineStage` | d.ts **0**, index.ts **1** | `src/index.ts`'s module doc comment | 0, or every hit says it is **not planned**. The module doc comment does not reach `.d.ts` |
    | `is still being implemented` | d.ts **0**, index.ts **1** | `src/index.ts`'s module doc comment | 0 — the phrasing listed `createConcierge`, which now ships |
    | `SEC-03` | d.ts **1**, js **6**, src **8** | catalog/type prose | no sentence asserts SEC-03 is closed, complete or fully satisfied without the getter-channel carve-out |

    Any hit that is still false is a defect this task fixes, in the source file that produced it,
    annotated in place per the house form: a bolded lead, the measurement, then what the correction
    does and does not widen. Record every fix; if a fix is needed, `files_modified` grows and the
    gates re-run.

    **(d) Confirm the tree and the lockfile.**
    - `git status --porcelain` — empty apart from this plan's own file.
    - `git diff --stat -- pnpm-lock.yaml` — empty. This phase installs nothing;
      `04-RESEARCH.md:145-158` records the Package Legitimacy Gate as not applicable and requires the
      lockfile to be **byte-identical at phase exit**.
    - `git diff -- packages/concierge/src/` against the phase's merge base shows only intended
      changes — in particular no residue from 04-05's or 04-07's temporary sensitivity probes.
    - `grep -rn 'vi\.' packages/concierge/test/` returns no matches.
    - For every runtime test file, `grep -v '^[[:space:]]*//' <file> | grep -c '\.\./src/'` returns 0.

    **(e) Perform the two manual-only verifications** that `04-VALIDATION.md`'s
    `## Manual-Only Verifications` table lists, and
    record a judgment rather than a checkbox:
    - Read `explain()`'s output for a **shadowed-stage** config and for a **no-bridge** config, and
      judge whether the next debugging step is obvious without opening the source. If it is not, say
      what is missing. DX-01's requirement is that a developer can diagnose "why didn't my action
      fire" without a debugger; the mechanical half (the fields exist) is already automated.
    - Read both new CAT-03 `problem` / `fix` pairs and confirm each names the referring action **and**
      the missing target, and that the `fix` states what to do rather than only what is wrong.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm build && pnpm test && pnpm check:artifact && pnpm check:deps && pnpm check:pack && pnpm check:node-floor && echo SEVEN_GATES_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - All seven gates exit 0. Each exit code and its salient output line is recorded.
    - `pnpm test` reports **7 files**, and its test count is recorded against the 55 baseline.
    - The independent regex over `dist/index.d.ts` reports 62 names, 11 values, 51 types. Pasted.
    - Every prose grep in (c) was run **against all five files**, every hit was read, and **every
      literal's pre-correction count was recorded and is ≥1**. Any literal measuring 0 pre-correction
      is logged as **no-coverage** with the reason, never as a pass — a grep that cannot fire has
      audited nothing. The summary records the hit list per literal per file and states explicitly
      that no surviving hit justifies the memo with tree-shaking, describes Phase 4 as future work,
      lists `defineStage` as pending, or claims SEC-03 is closed without the carve-out.
    - `grep -c 'future work and should be added' packages/concierge/src/contract.ts` returns **0**
      (**pre-phase measured: 1**) and the same literal returns **0** in both `dist/index.d.ts` and
      `dist/index.js` (**pre-phase measured: 1** in each).
    - `grep -c 'Hand-forward to Phase 4' packages/concierge/src/catalog.ts` returns **0**
      (**pre-phase measured: 1**) and **0** in `dist/index.js` (**pre-phase measured: 1**). It was
      never in `dist/index.d.ts`, so a `.d.ts`-only check for it proves nothing.
    - `git status --porcelain` shows only this plan's own file, plus any prose fix this task made,
      each recorded individually.
    - `git diff --stat -- pnpm-lock.yaml` is empty.
    - `grep -rn 'vi\.' packages/concierge/test/` returns no matches.
    - Both manual verifications are recorded with a judgment and a reason, not a checkmark.
  </acceptance_criteria>
  <done>Seven gates green against the final tree, the published declarations carry no claim this
  phase has made false, and the lockfile is where it started.</done>
</task>

<task type="auto">
  <name>Task 2: Fill and sign off 04-VALIDATION.md's Per-Task Verification Map</name>
  <files>.planning/phases/04-stages-catalog-assembly-and-explain/04-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/04-stages-catalog-assembly-and-explain/04-VALIDATION.md` — the map's column
      headers, the status legend (pending / green / red / flaky), the Mutant Obligations table and the
      Sign-Off checklist.
    - `.planning/phases/03-action-declaration-and-build-time-validation/03-VALIDATION.md` — the
      signed-off precedent from the previous phase; match its level of specificity.
    - Every `04-0*-SUMMARY.md`, for the observed statuses, case IDs and mutant results.
    - This plan's own Task 1 output, for the gate results.
  </read_first>
  <action>
    **(a) Fill the Per-Task Verification Map** with one row per task across all eight plans, starting
    from the planner-seeded rows below and correcting any that the summaries show landed differently.
    Every status must be **observed**, not predicted — a row whose evidence was never run is `pending`
    and blocks sign-off.

    | Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command |
    |---------|------|------|-------------|------------|-----------------|-----------|-------------------|
    | 04-01-T1 | 04-01 | 1 | SEC-03, DX-01 | T-04-08 | `EmittedTool` members are `readonly`; `Explanation` and `StageExplanation` are named exported interfaces; `Concierge.explain` is declared | type | `pnpm typecheck` |
    | 04-01-T2 | 04-01 | 1 | STG-03 | T-04-19 | The inline-declaration widening and its working spelling are recorded where a developer meets them | type | `pnpm typecheck` |
    | 04-02-T1 | 04-02 | 1 | CAT-03 | T-04-16, T-04-15 | A missing or self-referential consent target fails the build; every `consent` read is `Object.hasOwn`-guarded | runtime | `pnpm build && pnpm test catalog` |
    | 04-02-T2 | 04-02 | 1 | CAT-03 | T-04-19 | `deepFreeze` is reachable without a second freeze walk; three stale-prose sites corrected | runtime | `pnpm build && pnpm test && pnpm check:artifact` |
    | 04-03-T1 | 04-03 | 2 | STG-01, STG-02, STG-03, STG-04, SEC-03, DX-01, CAT-01 | T-04-01 through T-04-06, T-04-13 | Every statement of `src/concierge.ts` — index-keyed instance-local memo; shallow freeze over shared deep-frozen elements; one guarded matcher call that echoes nothing it caught — plus the file header and nineteen `ANCHOR(T2)` markers | runtime | `pnpm build && pnpm test && pnpm check:deps` |
    | 04-03-T2 | 04-03 | 2 | SEC-03, DX-01 | T-04-19, T-04-10 | The nineteen anchors expanded into doc comments with their measurements, and the shipped guard doc comment naming its real second call site. Comment-only diff | runtime | `pnpm build && pnpm test && pnpm test single-instance` |
    | 04-03-T3 | 04-03 | 2 | SEC-03 | T-04-19 | The published surface, both its pins, and the module doc comment describe the same package | runtime | `pnpm build && pnpm test export-surface` |
    | 04-04-T1 | 04-04 | 2 | CAT-03 | T-04-16, T-04-20 | Typo, self-reference, forward reference and aggregation, asserted on structured fields | runtime | `pnpm build && pnpm test catalog` |
    | 04-04-T2 | 04-04 | 2 | CAT-03 | T-04-22 | `CatalogIssueCode` pinned as a closed six-member union | type | `pnpm typecheck` |
    | 04-05-T1 | 04-05 | 3 | STG-01, STG-02, STG-03, STG-04, CAT-03 | T-04-02, T-04-06 | Stage scoping, first-match-wins, rename-independence, referential identity, instance-local memo | runtime | `pnpm build && pnpm test test/concierge` |
    | 04-05-T2 | 04-05 | 3 | SEC-03, DX-01 | T-04-01, T-04-03, T-04-04, T-04-05, T-04-07 | Array, element and nested tamper vectors; `explain`'s shape, freeze and non-identity; matcher and stage-id policy; the covert-channel assertion | runtime | `pnpm build && pnpm test test/concierge` |
    | 04-06-T1 | 04-06 | 3 | SEC-03, DX-01, STG-03 | T-04-08, T-04-22, T-04-23 | `readonly`, signature and `StageContext` pins, all in `Equals` spellings | type | `pnpm typecheck` |
    | 04-06-T2 | 04-06 | 3 | SEC-03 | T-04-10 | `createConcierge` reaches the contract guard | runtime | `pnpm build && pnpm test single-instance` |
    | 04-07-T1 | 04-07 | 4 | STG-01, STG-02, STG-03, STG-04, SEC-03, DX-01 | T-04-20, T-04-24, T-04-25 | Twelve `src/concierge.ts` mutants observed, each confirmed to have compiled and run tests | mutation | `scripts/mutate-and-prove.sh` with the `pnpm build && pnpm test test/concierge` gate |
    | 04-07-T2 | 04-07 | 4 | CAT-03, DX-01 | T-04-20, T-04-24 | Four `src/catalog.ts` and `src/types.ts` mutants observed; M-04-11 gated on the full suite | mutation | `scripts/mutate-and-prove.sh` with the `pnpm build && pnpm test` gate |
    | 04-08-T1 | 04-08 | 5 | all seven | T-04-19, T-04-26, T-04-SC | Seven gates green, no false prose in `dist/index.d.ts`, lockfile byte-identical | gate | the seven-gate command |
    | 04-08-T2 | 04-08 | 5 | all seven | T-04-20 | Every map row carries an observed status | gate | this plan's verify |

    Add the `File Exists` and `Status` values for each row from the summaries. Any row that is not
    green must carry a one-line reason in the summary.

    **(b) Fill the Mutant Obligations table's outcomes.** Replace the seeded literals with the ones
    actually run — 04-07's summary is authoritative — add M-04-16, and mark each row with its harness
    exit code, the named case that went red, and whether the gate output confirmed the mutant compiled
    and ran tests. Rows M-04-1, M-04-4, M-04-7 and M-04-12 each carry a note that they were repaired
    or respelled, and why.

    **(c) Record CAT-01's closure, then confirm and if necessary extend the "Explicitly NOT closed by
    this phase" section.**

    **CAT-01 closes in this phase and must be recorded as closed.** `REQUIREMENTS.md:157` marks it
    *Partial — 4/5 derived artifacts ship; `per-stage catalogs` is Phase 4 (`catalogFor`
    unimplemented, unexported)*; the Phase-3 closure commit says *"Phase 4 closes it. Not rounded
    up."*; and `04-CONTEXT.md:293-295` says *"This phase is what closes it."*

    **Most of the CAT-01 record inside `04-VALIDATION.md` already exists — do not claim credit for
    writing it.** Measured on the untouched document, `CAT-01` appears **5×**: the two map rows
    (`:79`, `:91`), the Requirement → Evidence row at `:144` which already names
    `createConcierge().catalogFor` and cites 04-05 S1/S2, the explicit paragraph at `:261-264`, and
    the sign-off box at `:298`. All five were seeded by the previous revision of these plans. What
    this task owes on top of them is exactly two things:

    1. **The `REQUIREMENTS.md:157` flip — this is the primary deliverable of (c).** It is the one
       clause in the CAT-01 group that inaction cannot satisfy. The row today reads, verified present:
       `| CAT-01 | Phase 3 — Action declaration and build-time validation | Partial — 4/5 derived artifacts ship; \`per-stage catalogs\` is Phase 4 (\`catalogFor\` unimplemented, unexported) |`
       Rewrite the Status to `Complete` and make the Phase column name Phase 4 as the closer, using
       the literal `closed by Phase 4` (measured **0** in `REQUIREMENTS.md` today) so the positive
       half is greppable. A requirement recorded Partial after the phase that closed it is the same
       defect class as prose that ships a claim the code has made false.
    2. **The phase-close statement**, which does not exist in this document yet, written in these
       terms: **CAT-01's fifth derived artifact, per-stage catalogs, now ships via
       `createConcierge().catalogFor`; evidence 04-05 S1 and S2, plan 04-03.** Carry the same wording
       into `04-08-SUMMARY.md`.

    Verify the five existing hits still say what they claim, correct any that the summaries show
    landed differently, and record in the summary that they were pre-existing rather than added here.

    The "Explicitly NOT closed by this phase" section must read, unambiguously:
    - **SEC-03 is not fully closed.** The handler-replacement and tool-array halves close here; the
      consumer-supplied-`jsonSchema` getter channel is measured open and is not this phase's to fix.
      No plan, summary or record may write "SEC-03 closed" without that carve-out.
    - **CAT-04** (transport grade ceiling) — Phase 8; it needs a transport, which is Phase 7.
    - **A non-string or missing `consent.requires`** — skipped deliberately, recorded as a residual in
      `catalog.ts`'s doc comment, revisited with Phase 8's kernel.
    - **The inline-`defineAction` contextual widening** — documented, not fixed; revisit in Phase 8 per
      D-12.2, and `_inlineDefineActionLosesTheUnion` stays red-as-pinned.

    **The Phase 6 hand-off is already present — confirm it, do not re-add it.** `## Hand-off to
    Phase 6 — read before planning DSP-09` sits at `:275-281` and already states that the Phase 4
    `dispatch` stub returns `{ok: false, message}` with `reason` deliberately omitted, and that
    **DSP-09's normalizer must REPLACE this shape, not normalize it** — not a contradictory
    `ActionResult` to be repaired, a placeholder to be deleted. `DSP-09` measures **4** in this
    document today (`:80`, `:91`, `:275`, `:280`). Read the section, confirm it still matches what
    04-03 T2 actually wrote into `src/concierge.ts`, and say in the summary that it was confirmed
    rather than authored. Only if 04-03's summary shows the source wording diverged does this become
    an edit.

    **(d) Complete the Validation Sign-Off checklist** and set `nyquist_compliant: true` and
    `wave_0_complete: true` in the frontmatter — but **only if every box is genuinely true**. Check
    each, do not assume:
    - every task has an `<automated>` verify or a Wave 0 dependency;
    - sampling continuity holds, with no three consecutive tasks lacking an automated verify;
    - Wave 0 covers every MISSING reference from the seeded list;
    - no watch-mode flags anywhere;
    - feedback latency under 5 s;
    - every mutant literal count was re-taken unfiltered at implementation time (04-07's summary is
      the evidence);
    - every mutant PASS was confirmed to have compiled and run tests, not to be a build-failure PASS.

    Two of these boxes are about **this task's own criteria**, so read them literally rather than
    ticking them by habit. `:299-300` asserts *"Every acceptance-criteria grep in every plan carries a
    measured pre-edit count, and none of them already sat at its PASS value before the edit it
    checks."* That box was **false** when this plan was first written — five of this task's eight
    criteria were satisfied by an untouched document — and it is true now only because each criterion
    below is labelled **discriminating** or **must-stay guard** with its measured count. If you find a
    criterion in any plan sitting at its PASS value with no such label, that box stays `- [ ]`.

    Set `status: signed-off` and record the approval line. If any box is false, leave
    `nyquist_compliant: false`, state which box and why, and do not sign off.

    **The frontmatter is the thing being signed.** `04-VALIDATION.md:301` is a *checklist line
    quoting the key* — `` - [ ] `nyquist_compliant: true` set in frontmatter `` — so the unanchored
    substring `nyquist_compliant: true` matches the untouched document. Line 5 is the value that
    matters. Every gate on this task uses the line-anchored form `^nyquist_compliant: true$`, which
    measures **0** today and **1** after sign-off.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm build && pnpm test && grep -c '^nyquist_compliant: true$' .planning/phases/04-stages-catalog-assembly-and-explain/04-VALIDATION.md && grep -c '^status: signed-off$' .planning/phases/04-stages-catalog-assembly-and-explain/04-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - **Standing rule, and read it before running anything below.** Every criterion in this task
      carries its **measured** count on the untouched `04-VALIDATION.md` / `REQUIREMENTS.md`, taken at
      revision time. Criteria fall into two registers and are labelled as such:
      **discriminating** (measured at a value inaction cannot produce, so it proves this task ran) and
      **must-stay guard** (already at its PASS value and kept only to protect an existing property —
      it proves nothing was broken, and it is **never** evidence of progress). A guard silently
      counted as progress is this repository's named defect class; three of the criteria below used to
      be exactly that. **Re-measure every count yourself before editing** — if a value has moved since
      revision time, record the value you measured and say so.

    - **DISCRIMINATING — every seeded row's Status is now observed.**
      `grep -c '⬜ pending' .planning/phases/04-stages-catalog-assembly-and-explain/04-VALIDATION.md`
      returns **1** — the legend line at `:93` (`*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*`),
      and nothing else. **Pre-edit measured: 18** (17 map rows + the legend). This is the criterion
      that replaces the former `filled by planner` check, which measured **0 before and 0 after**
      because the phrase never existed — the document says "filled by *the* planner" (`:14`), so the
      literal could not fire and the check audited nothing.
    - **DISCRIMINATING — the sign-off boxes are ticked.**
      `grep -c '^- \[x\]' .planning/phases/04-stages-catalog-assembly-and-explain/04-VALIDATION.md`
      returns **at least 11** — every box in `## Validation Sign-Off` (`:287-301`).
      **Pre-edit measured: 0** (21 boxes, all `- [ ]`). If a box is genuinely false, leave it `- [ ]`,
      do not sign off, and name it in the summary — a lower count with a stated reason is a correct
      outcome; a higher count with an unverified box is not.
    - **MUST-STAY GUARD — the map keeps all seventeen rows.**
      `grep -c '^| 04-0' .planning/phases/04-stages-catalog-assembly-and-explain/04-VALIDATION.md`
      returns **17**, one per task across the eight plans, each with a Requirement, a Threat Ref, an
      Automated Command and a Status. **Pre-edit measured: 17** — the rows are planner-seeded, so this
      count is a guard against losing one while filling the Status column, not evidence of work. The
      work is the `⬜ pending` criterion above.
    - **MUST-STAY GUARD — every requirement still appears in a map row's Requirement column.**
      Run `grep '^| 04-0' …/04-VALIDATION.md | grep -c '<ID>'` per ID. **Pre-edit measured:**
      CAT-03 **8**, STG-01 **5**, STG-02 **5**, STG-03 **7**, STG-04 **5**, SEC-03 **10**, DX-01 **9**,
      CAT-01 **2**. All eight are already ≥1, so this is a guard: no count may fall to 0 while rows
      are being corrected against the summaries.
    - **CAT-01's closure is recorded. The primary criterion is the `REQUIREMENTS.md` flip, not a
      grep of this document.**
      - **DISCRIMINATING (primary):**
        `grep -c 'Partial — 4/5 derived artifacts ship' .planning/REQUIREMENTS.md` returns **0**.
        **Pre-edit measured: 1**, at `REQUIREMENTS.md:157`, verified present:
        `| CAT-01 | Phase 3 — Action declaration and build-time validation | Partial — 4/5 derived artifacts ship; \`per-stage catalogs\` is Phase 4 (\`catalogFor\` unimplemented, unexported) |`.
        This is the one clause in the whole CAT-01 group that inaction cannot satisfy.
      - **DISCRIMINATING (primary, positive half):**
        `grep -c 'closed by Phase 4' .planning/REQUIREMENTS.md` returns **1** — the rewritten row's
        Status reads `Complete` and its Phase column names Phase 4 as the closer. **Pre-edit
        measured: 0.** The negative alone cannot tell "rewritten" from "deleted". If you word the row
        differently, substitute your own distinctive literal and record both counts: 0 before, 1 after.
      - **MUST-STAY GUARD:**
        `grep -c 'CAT-01' .planning/phases/04-stages-catalog-assembly-and-explain/04-VALIDATION.md`
        returns **at least 5**. **Pre-edit measured: 5** — `:79`, `:91`, `:144`, `:261`, `:298`. These
        five were added by the previous revision of this phase's plans, **not** by this task, and the
        clauses "the hit names `createConcierge().catalogFor`" and "cites 04-05 S1/S2" are already
        satisfied today by `:144`. Do not read this count as progress; read it as the assertion that
        none of the five is lost.
    - **MUST-STAY GUARD — the Mutant Obligations table keeps its sixteen rows.**
      `grep -c '^| M-04-' .planning/phases/04-stages-catalog-assembly-and-explain/04-VALIDATION.md`
      returns **16**. **Pre-edit measured: 16** — the rows and their named red cases are
      planner-seeded. The work in (b) is the **outcome** columns, which the next criterion measures.
    - **DISCRIMINATING — the mutant table records outcomes, which it currently does not.**
      Every M-04 row carries its harness exit code and whether the gate output confirmed the mutant
      compiled and ran tests. Measure with `grep -c 'exit 0\|exit 1' …/04-VALIDATION.md`, which must
      return **at least 20** (16 row outcomes + the 4 pre-existing prose hits). **Pre-edit measured:
      4**, and all four are prose describing the harness or the baseline, not row outcomes: `:31`
      (`pnpm typecheck` → exit 0, in the Measured baseline row), `:42` and `:188` (both the
      `PASS: gate fired (exit 1), tree clean` warning about vacuous passes), and `:50`
      (`pnpm test test/concierge` → *"No test files found", exit 1* in command trap 4). Read those
      four hits first so you do not count them as rows. The `exit 1`-only form measures **3** — the
      combined form is the one this criterion uses, so do not substitute the narrower pattern and
      carry the smaller number.
    - **MUST-STAY GUARD — the SEC-03 carve-out survives.**
      `grep -c 'SEC-03 is not fully closed' .planning/phases/04-stages-catalog-assembly-and-explain/04-VALIDATION.md`
      returns **at least 1**. **Pre-edit measured: 1** (`:254`). The sentence is already written; this
      guard exists because (c) rewrites the section around it and the carve-out must not be
      compressed away. It is not evidence that (c) ran.
    - **MUST-STAY GUARD — the Phase 6 hand-off survives.**
      `grep -c 'DSP-09' .planning/phases/04-stages-catalog-assembly-and-explain/04-VALIDATION.md`
      returns **at least 4**. **Pre-edit measured: 4** — `:80`, `:91`, `:275`, `:280`, including the
      whole `## Hand-off to Phase 6` section at `:275-281`. Already present; the criterion asserts it
      is not lost. If (c) finds it already complete, say so in the summary rather than claiming to
      have added it.
    - **DISCRIMINATING — the frontmatter is what gets signed, not the checklist line that quotes it.**
      `grep -c '^nyquist_compliant: true$' .planning/phases/04-stages-catalog-assembly-and-explain/04-VALIDATION.md`
      returns **1**, and `grep -c '^status: signed-off$' <same>` returns **1**.
      **Pre-edit measured: 0 and 0.** The anchors are load-bearing: unanchored,
      `grep -c 'nyquist_compliant: true'` measures **1 on the untouched document** — it matches the
      checklist line `` - [ ] `nyquist_compliant: true` set in frontmatter `` at `:301` while
      frontmatter `:5` still reads `nyquist_compliant: false`. The unanchored form was this task's
      `<automated>` gate, its `must_haves` `contains:` and one of its acceptance criteria, and it
      passed all three on a completely untouched validation map.
      **OR** both return 0, with the summary naming the box that is false and why. Never set either
      true on an unverified box.
    - `pnpm typecheck && pnpm build && pnpm test` all exit 0 and `git status --porcelain` shows only
      this plan's files (`04-VALIDATION.md` and `.planning/REQUIREMENTS.md`).
  </acceptance_criteria>
  <done>The validation map records what was actually observed, the mutant table records sixteen
  observed outcomes, and the record carries the two carve-outs — SEC-03's open getter channel and
  the dispatch stub Phase 6 must replace — where the next phase's planner will find them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped `dist/index.d.ts` prose → every consumer | `types.ts`, `catalog.ts`, `contract.ts` and `index.ts` doc comments are published verbatim. A false claim there is a false claim to every user of the package. |
| this phase's record → the next phase's planner | What is written as closed determines what Phase 5 through Phase 8 will not re-check. |
| the working tree → the published artifact | An unrestored mutation probe or an uncommitted lockfile change would ship. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-19 | Repudiation | shipped doc-comment prose | mitigate | Eight literals across **five** files — `dist/index.d.ts`, `dist/index.js`, `src/index.ts`, `src/contract.ts`, `src/catalog.ts` — each hit read rather than counted, and **each literal shown ≥1 on the pre-correction tree or logged as no-coverage**. Specifically: no tree-shaking justification for the memo (measured false under rolldown 1.2.0), no "Hand-forward to Phase 4" (it lives in `catalog.ts` and `dist/index.js`, never `.d.ts`), no `future work and should be added` covering `createConcierge` (the wrapped phrase `remain future work` is not greppable and is struck), no `defineStage` listed as pending (`src/index.ts`'s module doc comment does not reach `.d.ts`), and no unqualified SEC-03 closure claim. |
| T-04-07 | Tampering | a getter inside a consumer-supplied `jsonSchema` | **accept** — inherited, out of scope | Recorded in "Explicitly NOT closed by this phase" as an open, measured channel. The disposition is `accept` for Phase 4 and the fix (flattening accessors during emission) is a `json-schema.ts` contract change with its own decision. |
| T-04-26 | Tampering | an unrestored mutation probe reaching the artifact | mitigate | `git status --porcelain` empty, `git diff -- packages/concierge/src/` reviewed against the merge base, and all seven gates re-run against the final tree. |
| T-04-SC | Tampering | supply chain | mitigate | `pnpm-lock.yaml` byte-identical, asserted with `git diff --stat`. `pnpm check:deps` — the zero-runtime-dependency assertion — exits 0 with its byte count recorded. This phase installs nothing, which `04-RESEARCH.md:145-158` records as a verified fact rather than an omission. |
| T-04-20 | Repudiation | a validation map with predicted rather than observed statuses | mitigate | Every row's status comes from a summary that names the command that produced it; a row with no run evidence stays pending and blocks sign-off. |
</threat_model>

<verification>
- `pnpm typecheck`, `pnpm build`, `pnpm test`, `pnpm check:artifact`, `pnpm check:deps`,
  `pnpm check:pack`, `pnpm check:node-floor` all exit 0.
- `pnpm test` reports 7 files green; `dist/index.d.ts` parses to 62 / 51 / 11.
- `git status --porcelain` shows only this plan's file.
- `git diff --stat -- pnpm-lock.yaml` is empty.
- `grep -rn 'vi\.' packages/concierge/test/` returns no matches.
</verification>

<success_criteria>
- Every gate that was green before this phase is green after it.
- The published declarations describe the package that actually shipped.
- The lockfile did not move.
- The validation map is signed off on observed evidence, and the two carve-outs — SEC-03's open
  getter channel and the dispatch stub Phase 6 must replace rather than normalize — are recorded
  where the next planner will read them.
</success_criteria>

<output>
Create `.planning/phases/04-stages-catalog-assembly-and-explain/04-08-SUMMARY.md` when done.
Record: the seven gate results with exit codes, the `pnpm test` file/test/duration figures against
the 6/55/328 ms baseline, the `check:deps` byte count and delta, the independent export-surface regex
output, the full hit list for each of the eight prose literals across all five files with a verdict
per hit **and its pre-correction count**, the two manual verification judgments in prose, and a
phase-close statement of what is and is not closed — naming **CAT-01 as closed** (its fifth derived
artifact, per-stage catalogs, via `createConcierge().catalogFor`, evidence 04-05 S1/S2), SEC-03's two
closed halves and its one open channel, and the DSP-09 hand-off — in the exact wording the next
phase's planner should inherit. A literal with no pre-correction hit is reported as **no-coverage**,
never as a green row.
</output>
