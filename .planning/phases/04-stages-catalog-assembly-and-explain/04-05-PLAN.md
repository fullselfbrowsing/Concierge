---
phase: 04-stages-catalog-assembly-and-explain
plan: 05
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - packages/concierge/test/concierge.test.ts
# Transient, restored before the plan closes — see <sensitivity_probes> in <objective>.
# Declared so the wave-3 file-ownership check sees them; 04-06 declares its own two.
files_touched_transiently:
  - packages/concierge/src/concierge.ts
autonomous: true
requirements: [STG-01, STG-02, STG-03, STG-04, SEC-03, DX-01, CAT-03]

must_haves:
  truths:
    - "An agent on the results page is offered the results actions plus the cross-stage actions, and the checkout actions are absent from the array."
    - "Two calls with equivalent-but-not-identical context return the identical array reference; two separate instances do not."
    - "Pushing onto the returned array throws, rewriting an element's description throws, and rewriting a nested schema key throws."
    - "The same `EmittedTool` object appears in two stage arrays — the invariant that makes the shallow projection freeze sufficient."
    - "Renaming a LATER stage to an integer-like id does not change resolution."
    - "`explain()` reports both overlapping stages as matched while naming the first as active, and is a fresh object every call."
    - "A throwing matcher warns once across three calls, naming the stage, and never echoes what it caught."
  artifacts:
    - path: "packages/concierge/test/concierge.test.ts"
      provides: "The behavioural suite for STG-01/02/03/04, SEC-03's projection half, DX-01, the matcher policy, and the stage-id policy"
      min_lines: 480
  key_links:
    - from: "packages/concierge/test/concierge.test.ts"
      to: "packages/concierge/dist/index.js"
      via: "beforeAll dynamic import guarded by existsSync, never a static import of ../src/"
      pattern: "\\.\\./dist/index\\.js"
---

<objective>
Write `test/concierge.test.ts` — the behavioural suite for everything `createConcierge` promises,
asserted against the built artifact.

Purpose: five of this phase's defects pass a naive test. A fresh-but-deep-equal array satisfies
every structural assertion and loops React forever, because `useSyncExternalStore` compares with
`Object.is` and the dev warning is `__DEV__`-only with a once-per-process latch. An unfrozen
projection satisfies `Object.isFrozen(catalog)` at the array level while page script pushes a tool
onto it. A rename test that renames the *first* stage passes under an object-keyed implementation
too. A two-pass `explain()` produces `{"stage":"flaky","stages":[{"matched":false}]}` and looks fine
on a deterministic matcher. And a shallow projection freeze is sufficient only *because* elements are
shared and already deep-frozen — an invariant that no single assertion expresses.

Output: one file whose header names all five, and ~25 cases that can each tell the correct build from
the plausible one.

<sensitivity_probes>
**This plan makes transient edits to `packages/concierge/src/concierge.ts`** — commenting out
`memo.set(index, built);` for S7, changing `Object.freeze(tool)` to `(tool)` for S12, and changing
`explain`'s `firstMatch` derivation to take the last match for S19 — each rebuilt, observed red,
then restored. That file is **not** in `files_modified`, because nothing this plan commits touches
it; it is declared under `files_touched_transiently` so the wave-3 ownership check can see it.

**Precondition:** wave 3 runs 04-05 and 04-06 in parallel and BOTH make transient `src/` edits
(04-06 probes `src/types.ts` and, through it, `src/concierge.ts`). Run each plan in its **own
worktree**, or run them serially. Two agents mutating and restoring the same source file
concurrently produce a restore that reverts the other's probe and a `git status` that is dirty for
reasons neither can explain. Both plans already assert `git diff -- packages/concierge/src/` is
empty at close; that assertion is what contains the damage, not what prevents it.
</sensitivity_probes>
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-stages-catalog-assembly-and-explain/04-CONTEXT.md
@.planning/phases/04-stages-catalog-assembly-and-explain/04-03-SUMMARY.md
@CLAUDE.md

<!-- `04-RESEARCH.md` and `04-PATTERNS.md` are deliberately NOT @-included; the ranges this plan
     needs are named per task. -->

<interfaces>
Under test, from `packages/concierge/dist/index.js` (bound in `beforeAll`, never statically
imported):

  createConcierge(config: ConciergeConfig): Concierge
  concierge.catalogFor(ctx) -> ReadonlyArray<EmittedTool>   // frozen, memoized by stage INDEX
  concierge.stageFor(ctx)   -> string | null
  concierge.explain(ctx)    -> { stage, stages: [{id, matched, bridge}], catalog }   // deep-frozen, NOT memoized
  concierge.dispatch(...)   -> Promise<{ ok: false, message }>                       // `reason` omitted

  EmittedTool = { type: "function", name, description, parameters }   // no handler, no schema

`test/fixtures/schemas.ts` already provides every validator shape needed (`zodObject`,
`zodEmptyObject`, `arktypeObject`, …). **No new fixture file. No new devDependency.**

A hand-rolled `BridgeRegistry` is exactly what the exported interface admits and needs no Phase 5
code: `{ id: "results", read: () => mounted, register: () => () => {} }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the file header, the harness, and the STG-01/02/03/04 blocks — S1 through S10</name>
  <files>packages/concierge/test/concierge.test.ts</files>
  <read_first>
    - `packages/concierge/test/catalog.test.ts:1-163` in full — the exact analog. The header's
      "What escapes without this file:" heading and its numbered defects (`:1-29`); the four standing
      header blocks with their dividers (`:31-69`), of which **two apply verbatim**: "dist, not src —
      the same decision its three siblings state", and "This suite writes the global contract registry,
      once per test" (`createConcierge` reaches `assertSingleInstance` transitively through
      `buildCatalog`, so this suite writes the registry on every test too); the imports, the `beforeAll`
      `existsSync` guard whose message says ``Run `pnpm build` first``, the hard-coded `KEY` symbol and
      why it is hard-coded rather than imported, the `beforeEach` `delete` reset, and the `declare` /
      `noopHandler` helpers (`:71-163`).
    - `packages/concierge/test/emission.test.ts:41-74` — the `vitest.config.ts` fact that removes cast
      ceremony (`packages/concierge/test/` is in NO TypeScript program) used deliberately rather than
      worked around, and the measured-table-in-a-comment convention.
    - `packages/concierge/test/catalog.test.ts:165-166` — the titling convention: `describe` states the
      requirement ID plus the claim; each `it` is prefixed with its finding ID and states a full
      proposition.
    - `packages/concierge/test/catalog.test.ts:458-469` — the precedent for writing a *working* mutant
      literal into the file when the obvious one does not work.
    - `04-RESEARCH.md:240-345` (*Pattern 1* — the verbatim React `checkIfSnapshotChanged`, the exact
      DEV string, the once-per-process latch, and Svelte's `equals`), `:440-506` (*Pattern 3* — the
      measured key-ordering table at `:477-483` and the sensitive rename-test shape at `:485-492`),
      `:825-909` (*Code Examples* — the working prototype and its measured output, including the exact
      expected name arrays), and `:971-995` (the identity assertion, and why no React install is
      needed).
    - `04-RESEARCH.md:1256-1259` and `04-VALIDATION.md`'s
      `## Two behaviours with no single-literal mutant — stated rather than faked` — the **two behaviours with no
      single-literal mutant**, which the house rule says to write into the test file rather than fake.
    - `04-PATTERNS.md:764-1010` — this file's complete pattern assignment.
  </read_first>
  <action>
    Create `packages/concierge/test/concierge.test.ts`.

    **Header — "What escapes without this file:", then the numbered defects**, in
    `catalog.test.ts:1-29`'s register, naming how many and flagging which pass a naive test. All five
    are measured and quotable:

    1. **A fresh array per call.** React's `useSyncExternalStore` compares snapshots with `Object.is`
       (`ReactFiberHooks.js`, `return !is(prevValue, nextValue)`) and Svelte 5's `$derived` compares
       with `value === this.v`. Quote React's DEV string **exactly**:
       `'The result of getSnapshot should be cached to avoid an infinite loop'` — react.dev
       abbreviates it and a comment quoting the docs quotes something React never prints. State that
       `didWarnUncachedGetSnapshot` is a module-level latch firing **once per process** and the whole
       detector is `__DEV__`-only, so the framework is not a detector; `toBe` is.
    2. **An unfrozen projection is a live tool-injection channel.** Measured: `push` onto the
       `.filter()` result succeeded and the agent's list became `['a','injected']`. The injected tool
       has no handler — dispatch resolves through the frozen `byName` — so the payload is the
       **description**, which is CAT-07's compile-time guarantee defeated at runtime.
    3. **The element-sharing invariant.** The shallow projection freeze is sufficient *because* the
       elements are shared and already deep-frozen. Removing **either** the `toBe` sharing assertion
       (S14) or the nested-schema-write assertion (S13) leaves the shallow freeze silently
       insufficient. Say that in the header explicitly.
    4. **A naive rename test passes under a broken implementation.** Renaming the *first* stage proves
       nothing — it is first under array iteration and under object-key iteration alike. Include the
       measured table: array impl `results` → `results`; object impl `results` → `2`.
    5. **A two-pass `explain()` can contradict itself** — `stage: "flaky"` alongside
       `stages: [{id: "flaky", matched: false}]`, measured from a matcher with an internal counter.

    **Three more header blocks, each with its own divider:**

    - The **two behaviours with no single-literal mutant**, stated rather than faked, per the house
      convention: (a) rename-independence is a property of the data structure — an ordered
      `ReadonlyArray` rather than a keyed object — not of a branch, so no `<literal> <replacement>`
      swap produces it; M-04-4 covers the adjacent mutatable property, first-match-wins. (b) The
      element-sharing invariant cannot be mutated into existence, because building fresh elements per
      projection is a restructuring; M-04-1 proves the array freeze fires, and S13 + S14 together pin
      the invariant.
    - The **M-04-4 design constraint on the source**: `resolveIndex` and `explain` must use distinct
      loop spellings or the mutant literal is not unique. Record the two spellings 04-03 actually
      shipped (from `04-03-SUMMARY.md`) so a later refactor that unifies them is visibly a mutation
      regression, not a tidy-up.
    - The two standing blocks copied from `catalog.test.ts`: **dist, not src** (`:31-44`, including the
      parenthetical that every mention of `../src/` in this file is inside a comment and the acceptance
      check for that rule is scoped to non-comment lines), and **this suite writes the global contract
      registry, once per test** (`:46-57`) — with the addition that `createConcierge` reaches
      `assertSingleInstance` transitively through `buildCatalog`.

    **Declare the ID series in the header.** Use a fresh **S-series** (`S1`…`S25`), not a continuation
    of `catalog.test.ts`'s C-series, and say why: C-numbers are cited by ID across `03-*-SUMMARY.md`
    and a silent collision is a citation defect; two files are separate blast radii — the same argument
    `consent-variance.test-d.ts:59-62` makes for re-declaring `Booking` locally.

    **Also include, as measured-table comments** (`emission.test.ts:49-74` is the convention): the
    key-ordering table (`object key order: ['2','10','results','checkout','home']` vs
    `array order: ['results','checkout','2','home','10']`) and the frozen-array-derivation table
    (`filter`, `map`, `slice`, spread and `concat` all return **unfrozen** results from a frozen
    source; `push` onto the `filter()` result succeeded).

    **Harness** — copy `catalog.test.ts:71-124` almost verbatim, substituting `createConcierge`:
    `existsSync` guard on `DIST_PATH` in `beforeAll` with the ``Run `pnpm build` first`` message; the
    hard-coded `KEY = Symbol.for("@fullselfbrowsing/concierge.contract")` with its
    cross-realm-contract comment; `beforeEach` doing `delete` (never assignment to `undefined`); and
    unannotated `let` bindings resolved from the dynamic import. Bind `createConcierge` and, where a
    case needs it, `CatalogValidationError`.

    **Local helpers**, in `catalog.test.ts:126-163`'s register:
    - `noopHandler` — identifiable by reference.
    - `declare(name, schema, extra)` — **but with `redact: "drop"` defaulted here**, and a comment
      saying why this file inverts `catalog.test.ts`'s decision: that file omits `redact` on purpose,
      to keep SEC-01's failing branch in reach; this file builds through `createConcierge`, which must
      **succeed**, so a missing `redact` would throw `redaction_missing` and every case would fail for
      the wrong reason.
    - `stage(id, match, actions, bridge?)` — the natural addition in the same register.

    **The blocks and cases for this task.** `describe` per requirement, stating the claim:

    *STG-01 — the catalog contains only the actions valid for the current stage, plus cross-stage.*
    Build the canonical config: stages `results` (`applyFilter`, `sortResults`) and `checkout`
    (`confirmBooking`), `crossStage` `[signOut]`.
    - **S1** — `catalogFor({pathname:"/results"}).map(t => t.name)` equals
      `["applyFilter","sortResults","signOut"]`, and additionally assert
      `expect(names).not.toContain("confirmBooking")` — the requirement's wording is *absent from the
      catalog rather than rejected when called*, so the absence is the claim and deserves its own
      expectation. Assert `checkout`'s catalog is `["confirmBooking","signOut"]` in the same case.
    - **S2** — an unrouted context returns cross-stage only (`["signOut"]`) and `stageFor` returns
      `null`. Comment why this is not an empty array: `crossStage` is declared "available in every
      stage", and the rejected alternative — failing closed with an empty array — would silently
      disable `signOut`-shaped actions on any unrouted page.
    - **S3** — every element is an `EmittedTool` and nothing more: `type` is `"function"`, `name` and
      `description` are strings, `parameters` is an object, and **`"handler" in tool` is `false` and
      `"schema" in tool` is `false`**. Comment that the projection is a privilege reduction, not a
      rename: `CatalogEntry` carries `action.handler` and `action.schema` and `EmittedTool`
      deliberately carries neither.

    *STG-02 — declaration order, first match wins, independent of naming.*
    - **S4** — two stages both `match: () => true`; `stageFor` is the **first** and `catalogFor`
      returns the first's actions.
    - **S5** — the **sensitive** rename shape. Two stages, both `match: () => true`, ids
      `["results","checkout"]`; then the same config with the **later** stage renamed to the
      integer-like id `"2"`. Assert `stageFor` is still `"results"` and the catalog is still the
      results actions. Comment that renaming the *first* stage would prove nothing, with the measured
      before/after table for both implementations.

    *STG-03 — arbitrary app context, not only pathname.*
    - **S6** — a config whose only stage matches on `{modalOpen: true, cartCount: 3}` with **no
      `pathname` anywhere** — not in the context, not in the matcher. Comment that a `pathname` in
      this case would make it a second STG-01 test. This is a shape a TypeScript consumer could write,
      but note that the throwing and non-boolean matchers in Task 2 are not, and that no cast ceremony
      is needed because this file is in no TypeScript program.

    *STG-04 — a memoized frozen array, referentially identical across calls.*
    - **S7** — `catalogFor({pathname:"/results"})` `toBe` `catalogFor({pathname:"/results", scrollY:900, ts:Date.now()})`.
      The second context is deliberately a **different object with extra keys**: a test passing the
      same `ctx` twice would also pass under the `WeakMap<StageContext, …>` memo that
      `PITFALLS.md:556` exists to forbid. Assert both `toBe` and `Object.is(a,b)` explicitly, with the
      comment that `toBe` IS `Object.is` and the second line is spelled out so the requirement is
      legible.
    - **S8** — two distinct no-stage contexts share one reference under the null key.
    - **S9** — two separate `createConcierge` instances built from equivalent configs do **NOT** share
      an array (`expect(a).not.toBe(b)`), and their contents are still equal. This is the case that
      proves the cache is instance-local rather than module-scope. Comment the reason the cache must
      be instance-local: cross-request state pollution under SSR, where module instances are reused
      across server requests. **Do not write the tree-shaking reason** — it was measured under
      rolldown 1.2.0 and does not reproduce.

    *CAT-03 — the cross-stage half, which only `createConcierge` can produce.*
    - **S10** — an action in a stage declaring `consent: { requires: "signOut" }` where `signOut` is a
      **cross-stage** action builds clean through `createConcierge`. Comment that this is the row that
      decided CAT-03's placement: cross-stage actions are appended last, so an in-loop check would
      fail every build with this shape. Prove the case can go red — see the acceptance criteria.

    No Vitest mocking API anywhere. Console capture (Task 2) is a plain global assignment restored in
    a `finally`.
  </action>
  <verify>
    <automated>pnpm build && pnpm test test/concierge && echo STG_BLOCKS_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build && pnpm test test/concierge` passes and reports **1 file** with **ten** cases,
      S1…S10. **`pnpm test concierge` does not filter** — measured on the current tree it reports
      6 files / 55 tests, because every test file in the repo lives under `packages/concierge/` and
      the positional argument is matched as a substring of the whole path. `test/concierge` is the
      discriminating fragment: no other test file's path contains it (they are
      `packages/concierge/test/catalog.test.ts` and friends), and it measures **"No test files found",
      exit 1** on the tree before this task, so the criterion cannot be satisfied by inaction. The
      `--` trap is separate and still applies: `pnpm test -- <fragment>` discards the fragment.
    - `pnpm build && pnpm test` reports 7 files green — the full-suite figure, deliberately a
      different command from the line above rather than the same one twice.
    - `grep -c 'What escapes without this file:' packages/concierge/test/concierge.test.ts` returns 1.
    - `grep -F -c 'The result of getSnapshot should be cached to avoid an infinite loop' packages/concierge/test/concierge.test.ts`
      returns 1 — the SOURCE string, not react.dev's abbreviation.
    - `grep -rn 'vi\.' packages/concierge/test/` returns no matches.
    - `grep -v '^[[:space:]]*//' packages/concierge/test/concierge.test.ts | grep -c '\.\./src/'`
      returns 0 — every mention of the source path is inside a comment.
    - `grep -ci 'sideEffects\|tree-shak\|treeshake' packages/concierge/test/concierge.test.ts`
      returns 0; the S9 comment states SSR cross-request pollution instead.
    - `grep -c 'existsSync' packages/concierge/test/concierge.test.ts` returns at least 1 and the
      guard message contains ``pnpm build``.
    - **S5 is proved sensitive, not merely green.** Temporarily change it to rename the FIRST stage
      instead of the later one and confirm it still passes (it must — that is the point: the naive
      shape cannot tell the implementations apart). Restore. Record the observation in the summary as
      the executable evidence for header defect 4.
    - **S7 is proved sensitive.** Temporarily comment out `memo.set(index, built);` in
      `src/concierge.ts`, rebuild, confirm S7 goes RED, restore, rebuild, confirm green. Record both
      observations. (This is M-04-3 run by hand; 04-07 runs it through the harness.)
    - **S10 is proved able to go red.** Temporarily point its `requires` at a name declared nowhere,
      confirm `createConcierge` throws `CatalogValidationError`, restore. Record the observation.
    - `wc -l packages/concierge/test/concierge.test.ts` is at least 300 after this task.
  </acceptance_criteria>
  <done>The suite exists with a header that names every defect a naive version would miss, and the
  four stage requirements are asserted against the built artifact — including the two cases (S5, S7)
  that were observed able to distinguish a correct build from a plausible one.</done>
</task>

<task type="auto">
  <name>Task 2: Add the SEC-03, DX-01, matcher-policy and stage-id-policy blocks — S11 through S25</name>
  <files>packages/concierge/test/concierge.test.ts</files>
  <read_first>
    - `packages/concierge/test/catalog.test.ts:596-700` — the whole SEC-03 block. C17's four-assertion
      freeze case with the comment that the FIRST assertion passes on a breached shallow build; C18's
      **both-halves** shape where the second half (the value is unchanged) is the load-bearing one
      because the write was measured SILENT under a shallow freeze in some modes; and C22's register
      for asserting a deliberate negative as a positive claim.
    - `packages/concierge/test/catalog.test.ts:433-501` — C12, the **exact sanctioned console-capture
      form**: the four load-bearing notes (plain global assignment and never the Vitest mocking API;
      the real console SPREAD rather than replaced wholesale; restoration in a `finally`, never after
      the assertions; and no cast ceremony because this file is in no TypeScript program), the
      capture body, and the two-expectations-per-claim assertion pair (that the sink FIRED at all, and
      that what it emitted carried the identity). Also `:494-500` — recording a deliberate
      NON-assertion in prose rather than as a vacuously-passing check.
    - `04-RESEARCH.md:346-439` (*Pattern 2* — the seven tamper vectors verbatim, the element-identity
      measurement, and the tool-injection consequence), `:575-658` (*Pattern 5* — the one-pass
      `explain`, the measured two-pass contradiction, the `bridgeStatus` shape and the three bridge
      states, and the statement that DX-01's bridge clause is fully testable with no Phase 5 code),
      `:440-473` (the matcher policy, the measured truthy probe, and the warn-once-per-stage-id
      granularity), and `:745-758` (*Pitfall 7* — the measured id-collapse that the index-keyed memo
      makes impossible).
    - `04-RESEARCH.md:793-807` (*Pitfall 9*) and `04-VALIDATION.md`'s
      `## Explicitly NOT closed by this phase` — **what this phase must
      NOT claim.** SEC-03 is closed for handler and entry replacement and for the tool array handed to
      the agent; a getter inside a consumer-supplied `jsonSchema` remains a channel and is recorded,
      not fixed.
    - `04-CONTEXT.md:95-119` — the locked `explain()` shape, the deep freeze, the deliberate
      non-identity, and that `explain()` does not print.
    - `04-PATTERNS.md:1012-1176` — the freeze-assertion form, the deliberate-negative register, the
      console-capture form, the deliberate-non-assertion form, and the hand-rolled `BridgeRegistry`.
  </read_first>
  <action>
    Add four `describe` blocks to `packages/concierge/test/concierge.test.ts`, continuing the S-series
    at **S11**.

    *SEC-03 — the tool list handed to the agent cannot be tampered with.*
    Every case takes C17/C18's **both-halves** shape: assert the write throws **and** that the value
    is unchanged. A case asserting only `Object.isFrozen` passes on a breached build — measured.
    - **S11** — `catalogFor(ctx).push(evilTool)` throws **and** `length` is unchanged. Comment the
      consequence rather than the freeze report: an unfrozen projection is how attacker-chosen prose
      reaches the `description` field a model reads and reasons over, which is CAT-07's compile-time
      guarantee defeated at runtime.
    - **S12** — `catalogFor(ctx)[0].name = "evil"` throws **and** the name is unchanged and is not
      `"evil"`. This is the element-level case; the array-level freeze passes on a breached build.
    - **S13** — `catalogFor(ctx)[0].parameters.properties.<key>.type = "number"` throws. This proves
      the elements are *deep*-frozen, which is what makes the shallow projection freeze sufficient.
      Use a fixture whose emitted schema actually has that nesting; take the property name from the
      real emitted `parameters` rather than assuming it.
    - **S14** — the **element-sharing invariant**: the `signOut` tool object in the `results` array is
      `toBe` the `signOut` tool object in the `checkout` array. Comment, in both S13's and S14's
      bodies, that **removing either one leaves the shallow freeze silently insufficient** — the pair
      is the pin, and neither half alone is.
    - **S15** — the deliberate NON-claim, written down rather than asserted, in
      `catalog.test.ts:494-500`'s register (whose own precedent is `export-surface.test.ts:31-46`).
      State that **SEC-03 is not fully closed by this phase**: the handler-replacement and tool-array
      halves close here; a getter inside a consumer-supplied `jsonSchema` survives the freeze
      (measured — `deepFreeze` skips accessors so that walking the catalog never invokes application
      code, and for an explicit `jsonSchema` the `parameters` object IS the consumer's object by
      reference), and re-freezing the projection changes nothing because it is the same object. Write
      this as a comment block, not as a test — a vacuously-passing check would be worse.

    *DX-01 — `explain()` answers "why didn't my action fire".*
    - **S16** — `Object.keys(explain(ctx))` has length 3 and is exactly `stage`, `stages`, `catalog`.
    - **S17** — `explain(ctx).stage` equals `stageFor(ctx)` for three configs: one where a stage
      matches, one where none does (both `null`), and one whose only stage's matcher throws.
    - **S18** — `explain(ctx).catalog` equals `catalogFor(ctx).map(t => t.name)`, for both a matched
      and an unmatched context.
    - **S19** — the **shadowed-stage** case: two stages whose matchers both return `true`. `explain`
      reports `matched: true` on **both** rows while `stage` is the first. Comment that this is the
      single most likely answer to "why didn't my action fire" and the reason `explain` does not
      short-circuit: a short-circuiting implementation reports `matched: false` for the shadowed
      stage, which is not a measurement — it is "we never asked", rendered as a negative.
    - **S20** — the bridge clause, with a **hand-rolled `BridgeRegistry`** and no Phase 5 code:
      `{ id: "results", read: () => mounted, register: () => () => {} }`. Three assertions:
      `read()` returning `null` gives `{id:"results", registered:false}`; `read()` returning a bridge
      object gives `registered: true`; a stage with no `bridge` gives `null`. Comment that nothing
      about this test changes when `createBridge` ships, because `id` and `read()` are both on the
      exported interface today.
    - **S21** — the return value is deep-frozen: `explain(ctx).stages.push({})` throws **and** the
      length is unchanged; `explain(ctx).stages[0].matched = true` throws **and** the value is
      unchanged. The second is the load-bearing one — a shallow `Object.freeze` would pass the first.
    - **S22** — the deliberate **non**-identity, asserted as a positive claim in C22's register:
      `expect(explain(ctx)).not.toBe(explain(ctx))`. Comment that this is deliberate and must not be
      "optimized" into the memo later: `explain` is the one member of `Concierge` that would loop
      `useSyncExternalStore` forever, which is exactly the defect STG-04 exists to prevent, one line
      away from being reintroduced by the phase's own diagnostic.
    - **S23** — `explain` writes nothing to the console. Use the C12 console-capture form exactly:
      plain global assignment with the real console SPREAD, restored in a `finally`, and assert
      `captured` has length 0. Build the config so no matcher throws and no stage id repeats, or the
      case measures the matcher policy instead. Carry C12's four load-bearing notes forward.

    *Matcher policy.*
    - **S24** — a throwing `match()`: the stage is skipped (the other stage's catalog is served),
      **three** `catalogFor` calls produce **exactly one** warning, the warning contains the stage id,
      and `explain` shows `matched: false` for the thrower. Two expectations for the warning, per the
      two-expectations-per-claim rule: that the sink fired at all, and that what it emitted carried
      the stage's identity. **Add a third assertion that the warning does NOT contain the thrown
      error's message** — construct the matcher to `throw new Error("SECRET-FROM-THE-APP")` and assert
      `captured[0]` does not contain that string. That assertion is the executable form of the
      security decision, and without it the inverted `try`/`catch` decision relative to
      `json-schema.ts` is a comment with no guarantee.
    - **S25** — a matcher returning a truthy non-boolean (e.g. a string, or an object) does **not**
      match, and warns naming the stage. Comment the measurement: `"yes" === true` is `false` while
      `Boolean("yes")` is `true`, and a silent strict check reproduces the first-run experience where
      a developer reads "the agent says it can't do anything".

    *Stage-id policy.*
    - **S26** — two stages sharing one `id`, with **different actions**, and the second matching a
      different context. Assert: `createConcierge` warns **once**, the warning names the id; and —
      the correctness half — each stage's context yields **its own** actions, not the other's. Comment
      that the id-keyed form was measured to collapse (`buildCatalog` is happy, the projection under
      that id resolves to the last stage's actions, and `duplicate_action_name` does not fire because
      the action *names* differ), and that index-keying is what makes the collapse impossible while
      the warning is what keeps the ambiguity visible in `stageFor`, `Session.stage()` and `explain`.

    (The S-numbering runs to S26; adjust the header's stated range to match whatever the file ends
    with, and record the final range in the summary.)
  </action>
  <verify>
    <automated>pnpm build && pnpm test test/concierge && pnpm test && echo BEHAVIOUR_SUITE_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build && pnpm test test/concierge` passes and reports **1 file** with the full S-series
      case count; `pnpm build && pnpm test` reports **7 files** green. These are two different
      measurements, which is why they are two different commands — `pnpm test concierge` would run
      the whole suite for both. Record both file counts and both test counts in the summary.
    - `grep -rn 'vi\.' packages/concierge/test/` returns no matches.
    - `grep -c 'finally' packages/concierge/test/concierge.test.ts` returns at least 2 — every console
      capture restores in a `finally`, never after the assertions.
    - `grep -c 'SECRET-FROM-THE-APP' packages/concierge/test/concierge.test.ts` returns at least 2
      (the throw and the negative assertion) — the covert-channel case is executable.
    - `grep -c 'not.toBe' packages/concierge/test/concierge.test.ts` returns at least 2 — S9's
      instance-locality and S22's deliberate non-identity are both positive claims about a negative.
    - Every SEC-03 and DX-01 freeze case asserts **two** things: that the write throws and that the
      value is unchanged. Read S11, S12, S13, S21 and confirm; a case with only a `toThrow` is
      incomplete.
    - `grep -c 'SEC-03 is not fully closed' packages/concierge/test/concierge.test.ts` returns at
      least 1, or the equivalent carve-out sentence is present — read it and confirm it names the
      consumer-supplied `jsonSchema` getter channel.
    - **S12 is proved sensitive.** Temporarily change `Object.freeze(tool)` to `(tool)` in
      `src/concierge.ts`, rebuild, confirm S12 goes RED while S11 stays green (the array freeze is
      separate), restore, rebuild. Record both. (This is M-04-16 run by hand.)
    - **S19 is proved sensitive.** Temporarily change `explain`'s `firstMatch` derivation to take the
      last match, rebuild, confirm S19 goes RED, restore. Record. (This is M-04-12 run by hand.)
    - **S24's warn-once is proved.** Confirm from the captured array that three `catalogFor` calls
      produced exactly one entry — assert `toHaveLength(1)`, not `toBeGreaterThan(0)`.
    - `wc -l packages/concierge/test/concierge.test.ts` is at least 480.
  </acceptance_criteria>
  <done>The projection cannot be tampered with at the array, element or nested-schema level;
  `explain()` reports the shadowed stage, the bridge status, and a frozen non-identical object every
  call; a broken matcher degrades once, names itself, and demonstrably does not echo what it caught;
  and two stages sharing an id serve their own actions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party page script → the tool array handed to the model | The blocks in Task 2 are the executable evidence that this boundary holds at the array, element and nested-schema levels. |
| consumer `match()` / `BridgeRegistry.read()` → core | The suite deliberately supplies shapes a TypeScript consumer could not write — a matcher that throws, a matcher returning a truthy non-boolean, a hand-rolled registry. No cast ceremony is needed because `test/` is in no TypeScript program, and the population these rules exist for is JavaScript consumers, who have no checker either. |
| a caught matcher error → the host console | S24 asserts, executably, that the thrown message does not reach the warning. |
| SSR process → two `Concierge` instances | S9 is the evidence that the memo is instance-local. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering / Elevation | the array returned by `catalogFor` | mitigate | S11, S12, S13 assert write-throws **and** value-unchanged at three levels, because `Object.isFrozen` alone was measured to pass on a breached build. S14 pins the element-sharing invariant the shallow freeze depends on; the header states that removing either S13 or S14 leaves it silently insufficient. |
| T-04-05 | Information Disclosure | the matcher warning as a covert channel | mitigate | S24 throws `new Error("SECRET-FROM-THE-APP")` from the matcher and asserts the captured warning does not contain it. Without this assertion the bare `catch {` is a convention with no guarantee. |
| T-04-03 | Elevation of Privilege | wrong-stage catalog via colliding stage ids | mitigate | S26 asserts both halves: the warning fires once naming the id, and each stage's context yields its own actions rather than the last stage's. |
| T-04-04 | Denial of Service | a matcher that throws inside the consumer's render | mitigate | S24 asserts the stage is skipped rather than the call throwing, and that three calls produce exactly one warning. |
| T-04-06 | Information Disclosure | cross-request state pollution under SSR | mitigate | S9 asserts two instances do not share an array. The comment states the SSR reason; the measured-false tree-shaking reason appears nowhere, enforced by a grep in the acceptance criteria. |
| T-04-07 | Tampering | a getter inside a consumer-supplied `jsonSchema` | **accept** — inherited, out of scope | S15 records the carve-out in prose rather than as a vacuously-passing check, so no reader of this suite concludes SEC-03 is fully closed. |
| T-04-12 | Information Disclosure | `explain()`'s output | accept | S16 pins the field set at exactly three, so a future field carrying context or argument data cannot be added without this case going red. |
| T-04-21 | Repudiation | a test that cannot distinguish the correct build from the plausible one | mitigate | Four cases (S5, S7, S12, S19) must be *observed* red under a deliberate regression before being accepted green, and S10 likewise. The header names the five defects that pass a naive test. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new fixture file, no new devDependency, no React and no Svelte — `Object.is` and `===` are what those frameworks compute, and the sources are quoted in the header instead. `pnpm-lock.yaml` must be byte-identical. |
</threat_model>

<verification>
- `pnpm build && pnpm typecheck && pnpm test` all exit 0; `pnpm test` reports 7 files green.
- `grep -rn 'vi\.' packages/concierge/test/` returns no matches.
- Every mention of `../src/` in this file is inside a comment.
- `git status --porcelain` shows only `packages/concierge/test/concierge.test.ts`.
- `git diff --stat -- pnpm-lock.yaml` is empty.
- Every temporary source edit made to prove a case sensitive was restored, and `git diff --
  packages/concierge/src/` is empty at plan close. The three probes touch
  `packages/concierge/src/concierge.ts` only; see `<sensitivity_probes>` for the per-worktree
  precondition that keeps them from colliding with 04-06's.
</verification>

<success_criteria>
- Every ROADMAP success criterion for this phase has an executable assertion behind it, against the
  built artifact.
- The five defects that pass a naive suite are named in the file's own header, with their
  measurements.
- Five cases were observed able to go red before being accepted green.
- The two behaviours that no single-literal mutant can produce are written down rather than faked.
- Nothing in the file claims SEC-03 is fully closed.
</success_criteria>

<output>
Create `.planning/phases/04-stages-catalog-assembly-and-explain/04-05-SUMMARY.md` when done.
Record: the final S-series range and a one-line claim per case; the file/test totals from `pnpm test`;
the five sensitivity observations (S5 naive-shape-still-passes, S7 red without `memo.set`, S10 red on
a genuinely missing target, S12 red without the element freeze, S19 red under last-match) with what
was mutated and what went red; the exact property path S13 used for the nested-schema write, taken
from the real emitted `parameters`; and confirmation that `git diff -- packages/concierge/src/` is
empty at close.
</output>
