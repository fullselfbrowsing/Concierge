---
phase: 06-dispatcher
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/concierge/test/dispatcher-batch.test.ts
autonomous: true
requirements: [DSP-06, DSP-07, DSP-08, TRN-04]
user_setup: []

must_haves:
  truths:
    - "A ToolBatch is parsed and executed by Concierge without constructing a Transport."
    - "Calls enter handlers serially in stable outputIndex order and every input call receives one correlated result row."
    - "An abort leaves no call unanswered and all batch metadata reaches InvocationMeta without casts."
  artifacts:
    - path: "packages/concierge/test/dispatcher-batch.test.ts"
      provides: "Wave 0 batch parse, ordering, seriality, metadata, immutability, and abort tests"
      contains: "describe(\"DSP-07"
  key_links:
    - from: "packages/concierge/test/dispatcher-batch.test.ts"
      to: "packages/concierge/dist/index.js"
      via: "guarded dynamic import and createConcierge().dispatchBatch"
      pattern: "dispatchBatch"
---

<objective>
Create the transport-independent batch behavior suite before the batch method exists.

Purpose: Tests must exercise Concierge's own ToolBatch parser and serial executor, rather than sorting or looping in the test harness and accidentally proving test code.

Output: one failing built-artifact runtime suite with complete DSP-06/DSP-07/TRN-04 coverage.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-dispatcher/06-CONTEXT.md
@.planning/phases/06-dispatcher/06-RESEARCH.md
@.planning/phases/06-dispatcher/06-VALIDATION.md
@.planning/phases/06-dispatcher/06-PATTERNS.md

<interfaces>
- `ToolBatch` carries `responseId`, `calls`, optional `userTurnId`, optional `signal`, and optional `deferUntilDelivered`.
- Each `ToolCall` carries `callId`, `name`, raw `arguments`, and `outputIndex`.
- Final method: `dispatchBatch(ctx, batch): Promise&lt;ReadonlyArray&lt;Readonly&lt;{callId, result}>>>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (06-02-T1): Prove parsing, metadata forwarding, stable ordering, and strict seriality</name>
  <read_first>
    - `packages/concierge/test/dispatcher-batch.test.ts` — NEW target file.
    - `packages/concierge/test/concierge.test.ts` — artifact guard, single-instance reset, and local action/stage helpers.
    - `packages/concierge/src/types.ts` — ToolCall, ToolBatch, InvocationMeta, and Concierge contracts.
    - `packages/concierge/test-d/transport.test-d.ts` — canonical ToolBatch construction with optional sources.
    - `.planning/phases/06-dispatcher/06-CONTEXT.md` — serial outputIndex and metadata decisions.
    - `.planning/phases/06-dispatcher/06-VALIDATION.md` — DSP-06/DSP-07 test map.
  </read_first>
  <files>packages/concierge/test/dispatcher-batch.test.ts</files>
  <action>
Create the built-artifact harness and hard-coded contract-registry reset used by the other runtime suites. Every title starts with its stable token. Assign Q01-Q08, in order, to: copied/frozen caller array; stable outputIndex ordering with input-order ties; maximum concurrency one; malformed JSON through validation with later-call continuation; valid JSON primitive validation; exact metadata values/references; ordered callId correlation and exact row keys; and frozen rows/container. Use this exact ID-to-fingerprint registry: Q01=`[RED:Q01:copied-frozen-input]`, Q02=`[RED:Q02:stable-order]`, Q03=`[RED:Q03:serial-concurrency]`, Q04=`[RED:Q04:malformed-json-validation]`, Q05=`[RED:Q05:primitive-validation]`, Q06=`[RED:Q06:metadata-forwarding]`, Q07=`[RED:Q07:callid-correlation]`, and Q08=`[RED:Q08:frozen-results]`. Each case must reach exactly one intentional Wave 0 failing assertion and pass its registered marker as Vitest's custom assertion message; never throw the marker, reuse a marker, or let a hook, fixture, or shared helper fail before that assertion. The absent `dispatchBatch` capability must therefore be observed and asserted with the case's marker rather than invoked as `undefined` and allowed to raise TypeError. The RED gate targets only Q01-Q08 and writes Vitest JSON to `/tmp/concierge-06-02-t1-vitest.json`; it must prove exactly eight collected tests, exactly those eight failed IDs, no pass/skip/todo, and no suite-level or unhandled failure. It must inspect every `assertionResults[].failureMessages`, require the one exact registered fingerprint for that assertion's ID, and reject missing, foreign, duplicate, or multiple fingerprints; more than one failed assertion for an ID; hook/fixture/shared-helper failures; incidental runtime error classes; and syntax, transform, or load failures.
  </action>
  <verify>
    <automated>bash -c 'set -euo pipefail; report=/tmp/concierge-06-02-t1-vitest.json; log=/tmp/concierge-06-02-t1-vitest.log; expected=Q01,Q02,Q03,Q04,Q05,Q06,Q07,Q08; registry="Q01=[RED:Q01:copied-frozen-input],Q02=[RED:Q02:stable-order],Q03=[RED:Q03:serial-concurrency],Q04=[RED:Q04:malformed-json-validation],Q05=[RED:Q05:primitive-validation],Q06=[RED:Q06:metadata-forwarding],Q07=[RED:Q07:callid-correlation],Q08=[RED:Q08:frozen-results]"; rm -f "$report" "$log"; pnpm build; set +e; pnpm exec vitest run packages/concierge/test/dispatcher-batch.test.ts --reporter=json --outputFile="$report" --testNamePattern="^\\[Q0[1-8]\\]" &gt;"$log" 2&gt;&amp;1; rc=$?; set -e; test "$rc" -ne 0 &amp;&amp; test -s "$report"; ! rg -n "Unhandled (Error|Rejection)|Failed Suites|TypeError|ReferenceError|SyntaxError|RangeError|EvalError|URIError|AggregateError|RollupError|Transform failed|Failed to load|No test suite found|before(All|Each)|after(All|Each)|hook (failed|failure|error)|fixture (failed|failure|error)|shared[- ]helper (failed|failure|error)" "$log"; node -e "const fs=require('node:fs'),r=JSON.parse(fs.readFileSync(process.argv[1],'utf8')),expected=process.argv[2].split(','),entries=process.argv[3].split(',').map(entry=&gt;{const at=entry.indexOf('=');return [entry.slice(0,at),entry.slice(at+1)]}),registry=Object.fromEntries(entries),suites=Array.isArray(r.testResults)?r.testResults:[],assertions=suites.flatMap(suite=&gt;Array.isArray(suite.assertionResults)?suite.assertionResults:[]),idOf=item=&gt;(/^\\[([RQ]\\d{2})\\]/.exec(item.title||'')||[])[1],bad=/(?:\\b(?:TypeError|ReferenceError|SyntaxError|RangeError|EvalError|URIError|AggregateError)\\b|Unhandled (?:Error|Rejection)|RollupError|Transform failed|Failed to load|Failed Suites|No test suite found|\\b(?:beforeAll|beforeEach|afterAll|afterEach)\\b|\\b(?:hook|fixture|shared[- ]helper)\\s+(?:failed|failure|error)\\b)/i,ids=assertions.map(idOf),suiteMessages=suites.flatMap(suite=&gt;[suite.message,suite.failureMessage].filter(Boolean)),unhandled=[...(Array.isArray(r.unhandledErrors)?r.unhandledErrors:[]),...suites.flatMap(suite=&gt;Array.isArray(suite.unhandledErrors)?suite.unhandledErrors:[])],fingerprintsOk=assertions.every(item=&gt;{const id=idOf(item),messages=Array.isArray(item.failureMessages)?item.failureMessages.map(String):[],body=messages.join('\\n'),markers=body.match(/\\[RED:[RQ]\\d{2}:[a-z0-9-]+\\]/g)||[];return item.status==='failed'&amp;&amp;messages.length===1&amp;&amp;markers.length===1&amp;&amp;markers[0]===registry[id]&amp;&amp;!bad.test(body)}); if(r.success!==false||r.numTotalTests!==expected.length||r.numFailedTests!==expected.length||(r.numPassedTests||0)!==0||(r.numPendingTests||0)!==0||(r.numTodoTests||0)!==0||suites.length!==1||assertions.length!==expected.length||entries.length!==expected.length||Object.keys(registry).length!==expected.length||new Set(ids).size!==expected.length||ids.slice().sort().join(',')!==expected.slice().sort().join(',')||Object.keys(registry).sort().join(',')!==expected.slice().sort().join(',')||suiteMessages.length!==0||unhandled.length!==0||!fingerprintsOk)process.exit(1)" "$report" "$expected" "$registry"'</automated>
  </verify>
  <done>The batch suite is red because Concierge has no batch method, while its harness and fixtures load successfully.</done>
  <acceptance_criteria>
    - No test sorts or loops over `concierge.dispatch`; every case invokes `dispatchBatch` directly.
    - The seriality detector asserts maximum concurrency equals one.
    - Malformed JSON is distinguished from schema validation, and a later call still settles.
    - Metadata equality uses exact values/references and no cast.
    - Both row and array immutability are asserted.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 (06-02-T2): Prove abort completeness, dedup reuse, and transport-free batch use</name>
  <read_first>
    - `packages/concierge/test/dispatcher-batch.test.ts` — file created by Task 1.
    - `packages/concierge/src/types.ts` — AbortSignalLike, ActionResult, and `aborted` ReasonCode.
    - `.planning/phases/06-dispatcher/06-RESEARCH.md` — Serial Batch Loop and cancellation pitfalls.
    - `.planning/phases/06-dispatcher/06-CONTEXT.md` — `batch_aborted` remains collapsed into `aborted`.
  </read_first>
  <files>packages/concierge/test/dispatcher-batch.test.ts</files>
  <action>
Add cases using a local AbortSignalLike fixture. Every new title starts with its stable token. Assign Q09-Q14, in order, to: already-aborted completeness; abort during the first commit window; abort after the first handler; zero later handler entries plus one canceller; repeated-callId single-cache reuse; and a direct application loop with no Transport. Use this exact ID-to-fingerprint registry: Q09=`[RED:Q09:preaborted-completeness]`, Q10=`[RED:Q10:abort-commit-window]`, Q11=`[RED:Q11:abort-after-handler]`, Q12=`[RED:Q12:suppress-later-handlers]`, Q13=`[RED:Q13:repeated-callid-dedup]`, and Q14=`[RED:Q14:direct-no-transport]`. Each case must reach exactly one intentional Wave 0 failing assertion and pass its registered marker as Vitest's custom assertion message; never throw the marker, reuse a marker, or let a hook, fixture, or shared helper fail before that assertion. Observe and assert an absent `dispatchBatch` capability with the case marker instead of invoking `undefined`. Each abort case asserts one sorted/correlated row per input and uses only `aborted`, never `batch_aborted`. The RED gate targets only Q09-Q14 and writes Vitest JSON to `/tmp/concierge-06-02-t2-vitest.json`; it must prove exactly six collected tests, exactly those six failed IDs, no pass/skip/todo, and no suite-level or unhandled failure. It must inspect every `assertionResults[].failureMessages`, require the one exact registered fingerprint for that assertion's ID, and reject missing, foreign, duplicate, or multiple fingerprints; more than one failed assertion for an ID; hook/fixture/shared-helper failures; incidental runtime error classes; and syntax, transform, or load failures. Q01-Q08 are intentionally not collected because they are already red from Task 1.
  </action>
  <verify>
    <automated>bash -c 'set -euo pipefail; report=/tmp/concierge-06-02-t2-vitest.json; log=/tmp/concierge-06-02-t2-vitest.log; expected=Q09,Q10,Q11,Q12,Q13,Q14; registry="Q09=[RED:Q09:preaborted-completeness],Q10=[RED:Q10:abort-commit-window],Q11=[RED:Q11:abort-after-handler],Q12=[RED:Q12:suppress-later-handlers],Q13=[RED:Q13:repeated-callid-dedup],Q14=[RED:Q14:direct-no-transport]"; rm -f "$report" "$log"; pnpm build; set +e; pnpm exec vitest run packages/concierge/test/dispatcher-batch.test.ts --reporter=json --outputFile="$report" --testNamePattern="^\\[Q(09|1[0-4])\\]" &gt;"$log" 2&gt;&amp;1; rc=$?; set -e; test "$rc" -ne 0 &amp;&amp; test -s "$report"; ! rg -n "Unhandled (Error|Rejection)|Failed Suites|TypeError|ReferenceError|SyntaxError|RangeError|EvalError|URIError|AggregateError|RollupError|Transform failed|Failed to load|No test suite found|before(All|Each)|after(All|Each)|hook (failed|failure|error)|fixture (failed|failure|error)|shared[- ]helper (failed|failure|error)" "$log"; node -e "const fs=require('node:fs'),r=JSON.parse(fs.readFileSync(process.argv[1],'utf8')),expected=process.argv[2].split(','),entries=process.argv[3].split(',').map(entry=&gt;{const at=entry.indexOf('=');return [entry.slice(0,at),entry.slice(at+1)]}),registry=Object.fromEntries(entries),suites=Array.isArray(r.testResults)?r.testResults:[],assertions=suites.flatMap(suite=&gt;Array.isArray(suite.assertionResults)?suite.assertionResults:[]),idOf=item=&gt;(/^\\[([RQ]\\d{2})\\]/.exec(item.title||'')||[])[1],bad=/(?:\\b(?:TypeError|ReferenceError|SyntaxError|RangeError|EvalError|URIError|AggregateError)\\b|Unhandled (?:Error|Rejection)|RollupError|Transform failed|Failed to load|Failed Suites|No test suite found|\\b(?:beforeAll|beforeEach|afterAll|afterEach)\\b|\\b(?:hook|fixture|shared[- ]helper)\\s+(?:failed|failure|error)\\b)/i,ids=assertions.map(idOf),suiteMessages=suites.flatMap(suite=&gt;[suite.message,suite.failureMessage].filter(Boolean)),unhandled=[...(Array.isArray(r.unhandledErrors)?r.unhandledErrors:[]),...suites.flatMap(suite=&gt;Array.isArray(suite.unhandledErrors)?suite.unhandledErrors:[])],fingerprintsOk=assertions.every(item=&gt;{const id=idOf(item),messages=Array.isArray(item.failureMessages)?item.failureMessages.map(String):[],body=messages.join('\\n'),markers=body.match(/\\[RED:[RQ]\\d{2}:[a-z0-9-]+\\]/g)||[];return item.status==='failed'&amp;&amp;messages.length===1&amp;&amp;markers.length===1&amp;&amp;markers[0]===registry[id]&amp;&amp;!bad.test(body)}); if(r.success!==false||r.numTotalTests!==expected.length||r.numFailedTests!==expected.length||(r.numPassedTests||0)!==0||(r.numPendingTests||0)!==0||(r.numTodoTests||0)!==0||suites.length!==1||assertions.length!==expected.length||entries.length!==expected.length||Object.keys(registry).length!==expected.length||new Set(ids).size!==expected.length||ids.slice().sort().join(',')!==expected.slice().sort().join(',')||Object.keys(registry).sort().join(',')!==expected.slice().sort().join(',')||suiteMessages.length!==0||unhandled.length!==0||!fingerprintsOk)process.exit(1)" "$report" "$expected" "$registry"'</automated>
    <automated>bash -c 'test $(rg -n "batch_aborted" packages/concierge/test/dispatcher-batch.test.ts | wc -l | tr -d " ") -le 2'</automated>
  </verify>
  <done>All abort-completeness and transport-free batch assertions exist and fail against the absent implementation.</done>
  <acceptance_criteria>
    - Every aborted scenario returns one row per input call; no loop uses `break` as the observable behavior.
    - Handler-entry counters prove remaining calls did not actuate.
    - `aborted` is the only batch-abort ReasonCode.
    - Repeated callId within a batch executes once.
    - The direct-loop test imports or constructs no Transport.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ToolBatch → parser | Raw JSON strings, ordering indices, metadata, and signal are untrusted. |
| batch executor → handlers | Parallel entry or lost result rows can double-actuate or hang the caller. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-06-03 | HIGH | Tampering | JSON arguments | mitigate | Malformed parse fallback is forced through schema validation; later-call continuation is asserted. |
| T-06-07 | HIGH | Tampering / Repudiation | abort propagation | mitigate | Three abort positions assert complete rows and zero later handler entries. |
| T-06-08 | HIGH | Tampering / Denial of service | batch ordering | mitigate | Stable-order, maximum-concurrency-one, and exact result-count detectors. |
| T-06-01 | HIGH | Tampering | repeated batch call | mitigate | Repeated-callId handler-once assertion delegates to single-call dedup. |
| T-06-SC | LOW | Tampering | package supply chain | accept | No dependency or install task exists. |
</threat_model>

<verification>
The suite loads the built artifact and fails because `dispatchBatch` is absent, not because of a fixture, parse, or syntax error.
</verification>

<success_criteria>
- DSP-06 and DSP-07 have mutation-sensitive Wave 0 coverage.
- Batch execution is tested as a Concierge capability independent of Transport.
- Abort and ordering tests cannot pass under `Promise.all`, unsorted iteration, or early loop termination.
</success_criteria>

<output>
Create `.planning/phases/06-dispatcher/06-02-SUMMARY.md` when done.
</output>
