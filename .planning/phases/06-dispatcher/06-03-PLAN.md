---
phase: 06-dispatcher
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - packages/concierge/src/message.ts
  - packages/concierge/src/bridge.ts
  - packages/concierge/src/host.ts
autonomous: true
requirements: [DSP-08, SEC-06]
user_setup: []

must_haves:
  truths:
    - "Message bounding has one shared implementation and still cannot split a surrogate pair."
    - "Dispatcher-bound messages can strip C0/C1 controls, collapse whitespace, trim, and cap at MESSAGE_MAX_CHARS."
    - "An omitted Scheduler can resolve to a cancellable structural host timer without DOM or Node timer types."
  artifacts:
    - path: "packages/concierge/src/message.ts"
      provides: "Internal boundedMessage and sanitizeMessage helpers"
      contains: "export function sanitizeMessage"
    - path: "packages/concierge/src/host.ts"
      provides: "Structural host Scheduler fallback"
      exports: ["readHostScheduler", "warnHost"]
    - path: "packages/concierge/src/bridge.ts"
      provides: "offPageResult using the shared bound"
      contains: "boundedMessage"
  key_links:
    - from: "packages/concierge/src/bridge.ts"
      to: "packages/concierge/src/message.ts"
      via: "internal .js import"
      pattern: "from \"\\./message\\.js\""
    - from: "packages/concierge/src/message.ts"
      to: "packages/concierge/src/types.ts"
      via: "MESSAGE_MAX_CHARS import"
      pattern: "MESSAGE_MAX_CHARS"
---

<objective>
Land the two shared primitives the dispatcher depends on: one message-safety module and the sanctioned host-timer adapter.

Purpose: SEC-06 must extend the existing surrogate-safe bound rather than duplicate it, and DSP-08 needs a platform timer without weakening the ES2022-only core contract.

Output: `message.ts`, updated `bridge.ts`, and updated `host.ts`; no public barrel export and no new package.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-dispatcher/06-CONTEXT.md
@.planning/phases/06-dispatcher/06-RESEARCH.md
@.planning/phases/06-dispatcher/06-PATTERNS.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-07-SUMMARY.md
@.planning/phases/06-dispatcher/06-01-SUMMARY.md

<interfaces>
- `MESSAGE_MAX_CHARS` is the literal 180 and remains the only bound.
- `Scheduler` remains `(fn: () => void, delayMs: number) => () => void`.
- `host.ts` must use a module-private minimal view, read `globalThis` inside a function, and make timer capability optional.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (06-03-T1): Extract the shared bound and add the dispatcher sanitizer</name>
  <read_first>
    - `packages/concierge/src/message.ts` — NEW target file.
    - `packages/concierge/src/bridge.ts` — existing `boundedMessage` and `offPageResult` implementation and documentation.
    - `packages/concierge/src/types.ts` — MESSAGE_MAX_CHARS and ActionResult message policy.
    - `packages/concierge/src/index.ts` — public surface that must not export the internal helpers.
    - `packages/concierge/test/bridge-snapshot.test.ts` — existing D19 bound and surrogate detector.
    - `.planning/phases/06-dispatcher/06-CONTEXT.md` — locked SEC-06 ordering and shared-bound decision.
  </read_first>
  <files>packages/concierge/src/message.ts, packages/concierge/src/bridge.ts</files>
  <action>
Move `boundedMessage(message: string): string` byte-for-behavior from `bridge.ts` into new internal `message.ts`, including the high-surrogate check at `MESSAGE_MAX_CHARS - 1`. Export it only from that internal module and import it into `bridge.ts`; do not re-export it from `index.ts`. Add `sanitizeMessage(message: string): string` beside it. Its exact order is: replace every C0/C1 run (`U+0000..U+001F`, `U+007F..U+009F`) with one ASCII space, collapse all whitespace runs to one ASCII space, trim both ends, then call `boundedMessage`. Move the bounding-versus-sanitizing documentation with the implementation and update `offPageResult` prose to say it uses the shared bound while final dispatcher sanitization occurs at the outbound boundary. Keep `offPageResult`'s result and message wording unchanged.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm exec vitest run packages/concierge/test/bridge-snapshot.test.ts</automated>
    <automated>bash -c 'test $(rg -n "function boundedMessage" packages/concierge/src --glob "*.ts" | wc -l | tr -d " ") = 1 &amp;&amp; ! rg -n "message\\.js" packages/concierge/src/index.ts'</automated>
  </verify>
  <done>One internal bound remains, bridge behavior is unchanged, and sanitizeMessage implements the exact SEC-06 transform using the shared constant.</done>
  <acceptance_criteria>
    - `boundedMessage` occurs once in source and retains the `0xd800..0xdbff` cut adjustment.
    - `sanitizeMessage` replaces controls with spaces before collapsing and bounding.
    - `MESSAGE_MAX_CHARS` is imported, not re-spelled as 180.
    - Existing bridge and surrogate tests pass against the built artifact.
    - Neither helper is on the public barrel.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 (06-03-T2): Add the cancellable structural host Scheduler fallback</name>
  <read_first>
    - `packages/concierge/src/host.ts` — modified file and the only sanctioned host-capability seam.
    - `packages/concierge/src/types.ts` — exact Scheduler contract and ES2022-only explanation.
    - `packages/concierge/src/contract.ts` — structural globalThis reads and function-body cast convention.
    - `packages/concierge/test-d/dispatcher.test-d.ts` — exact Scheduler pin created by 06-01.
    - `.planning/phases/06-dispatcher/06-CONTEXT.md` — optional fallback and no-host behavior.
  </read_first>
  <files>packages/concierge/src/host.ts</files>
  <action>
Add a module-private `TimerHost` view with optional `setTimeout(fn, delayMs): unknown` and `clearTimeout(handle): void`. Export internal `readHostScheduler(): ((fn: () => void, delayMs: number) => () => void) | undefined`. Inside its body, cast `globalThis` to the minimal view, require both functions, capture them, and return a Scheduler that calls setTimeout with the host receiver and returns an idempotent canceller that calls clearTimeout with the same receiver at most once. Return `undefined` when either capability is absent. Do not use bare `setTimeout`, DOM timer types, Node `Timeout`, a module-scope host read, or a second warning channel. Update the host header's zero-module/second-occupant prose truthfully without importing a runtime dependency. After the green build, run `tsc` with `--pretty false`; dynamically resolve the `_dispatchSignature`, `_dispatchBatchSignature`, and `_conciergeKeys` predicate lines in `packages/concierge/test-d/dispatcher.test-d.ts` and require exactly three remaining diagnostics: `_dispatchSignature`/TS2344, `_dispatchBatchSignature`/TS2339, and `_conciergeKeys`/TS2344. Reject every extra, duplicate, missing, wrong-file, wrong-line, or wrong-code diagnostic; `_schedulerSignature` must remain green.
  </action>
  <verify>
    <automated>bash -c 'set -euo pipefail; pnpm build; file=packages/concierge/test-d/dispatcher.test-d.ts; log=/tmp/concierge-06-03-type.log; set +e; pnpm exec tsc --project packages/concierge/tsconfig.test-d.json --noEmit --pretty false &gt;"$log" 2&gt;&amp;1; rc=$?; set -e; test "$rc" -ne 0 &amp;&amp; test -s "$log"; node -e "const fs=require(\"node:fs\"),path=require(\"node:path\"),log=fs.readFileSync(process.argv[1],\"utf8\"),source=process.argv[2],src=fs.readFileSync(source,\"utf8\").split(/\\r?\\n/); const lineOf=name=&gt;{const hits=[];src.forEach((line,index)=&gt;{if(line.includes(\"type \"+name))hits.push(index+1)});if(hits.length!==1)process.exit(1);return hits[0]}; const expected=[{name:\"_dispatchSignature\",line:lineOf(\"_dispatchSignature\"),code:\"TS2344\"},{name:\"_dispatchBatchSignature\",line:lineOf(\"_dispatchBatchSignature\"),code:\"TS2339\"},{name:\"_conciergeKeys\",line:lineOf(\"_conciergeKeys\"),code:\"TS2344\"}]; const headers=log.split(/\\r?\\n/).filter(line=&gt;/\\berror TS\\d+:/.test(line)); const parsed=headers.map(text=&gt;{const match=/^(.+)\\((\\d+),(\\d+)\\): error (TS\\d+): /.exec(text);return match?{file:path.resolve(match[1]),line:Number(match[2]),code:match[4]}:null}); const target=path.resolve(source); if(headers.length!==3||parsed.some(item=&gt;!item)||parsed.some(item=&gt;item.file!==target)||expected.some(want=&gt;parsed.filter(item=&gt;item.line===want.line&amp;&amp;item.code===want.code).length!==1)||parsed.some(item=&gt;expected.filter(want=&gt;want.line===item.line&amp;&amp;want.code===item.code).length!==1))process.exit(1)" "$log" "$file"'</automated>
    <automated>bash -c 'hits=$(rg -n "(^|[^.[:alnum:]_])setTimeout\\(" packages/concierge/src --glob "*.ts" | grep -v "src/host.ts" | grep -v "^[^:]*:[0-9]*:[[:space:]]*\\*" | wc -l | tr -d " "); test "$hits" = "0"'</automated>
  </verify>
  <done>Core can obtain a Scheduler structurally when available and can detect its absence without importing platform timer types.</done>
  <acceptance_criteria>
    - The exported internal function has the exact Scheduler-compatible return signature.
    - Both host functions are required; partial capability returns undefined.
    - The canceller is idempotent and carries no platform handle type into declarations.
    - The global cast occurs inside `readHostScheduler`, never at module scope.
    - Build and existing bridge tests remain green; package typecheck has exactly the three registered Wave 0 diagnostics (`_dispatchSignature`/TS2344, `_dispatchBatchSignature`/TS2339, `_conciergeKeys`/TS2344), with every extra or mismatched diagnostic rejected.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handler prose → model channel | Consumer-controlled strings can carry controls, whitespace tricks, or malformed UTF-16 cuts. |
| core → host timer | Host scheduling functions may be absent, substituted, synchronous, or throw when later invoked. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-06-05 | HIGH | Spoofing / Information disclosure | message.ts | mitigate | One ordered sanitizer and shared surrogate-safe bound; Wave 0 has independent detectors. |
| T-06-07 | HIGH | Tampering / Repudiation | host timer | mitigate | Exact cancellable Scheduler adapter, optional capability detection, and idempotent canceller. |
| T-06-09 | MEDIUM | Denial of service | platform mismatch | mitigate | No DOM/Node timer types or module-scope reads; partial hosts return absence. |
| T-06-SC | LOW | Tampering | package supply chain | accept | No dependency installation; existing registry audit says none required. |
</threat_model>

<verification>
Build passes; existing bridge behavior remains green; source audits show one bound and no unsanctioned timer access; the targeted typecheck allowlist proves only the intentional Wave 0 dispatch/dispatchBatch pins remain red.
</verification>

<success_criteria>
- The shared sanitizer and timer seams compile under the repository's ES2022-only contract.
- Existing bridge output remains behaviorally identical.
- No public export, package, or second policy copy is introduced.
</success_criteria>

<output>
Create `.planning/phases/06-dispatcher/06-03-SUMMARY.md` when done.
</output>
