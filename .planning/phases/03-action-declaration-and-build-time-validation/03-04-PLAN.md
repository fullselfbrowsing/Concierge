---
phase: 03-action-declaration-and-build-time-validation
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - packages/concierge/src/index.ts
  - packages/concierge/test/export-surface.test.ts
  - packages/concierge/test-d/exports.test-d.ts
  - packages/concierge/test/artifact.test.ts
autonomous: true
requirements: [CAT-01, CAT-06, CAT-07, DX-03]

must_haves:
  truths:
    - "A consumer can import defineAction and buildCatalog from the package entrypoint."
    - "Every new runtime value reaches dist/index.js as a real binding, not undefined."
    - "The pinned export count moves in step across all four places it is written."
  artifacts:
    - path: "packages/concierge/src/index.ts"
      provides: "The public barrel, grown by 10 types and 4 values"
      contains: "export { defineAction }"
    - path: "packages/concierge/test/export-surface.test.ts"
      provides: "The pinned counts, updated in assertions AND in `it` titles"
    - path: "packages/concierge/test-d/exports.test-d.ts"
      provides: "A value-placement predicate per new runtime export"
    - path: "packages/concierge/test/artifact.test.ts"
      provides: "An artifact-level case per new runtime export"
  key_links:
    - from: "packages/concierge/src/index.ts"
      to: "packages/concierge/src/catalog.ts"
      via: "value re-export of buildCatalog and CatalogValidationError"
      pattern: "from \"./catalog.js\""
    - from: "packages/concierge/test-d/exports.test-d.ts"
      to: "packages/concierge/src/index.ts"
      via: "value import — index.js, NOT types.js. That is the whole point."
      pattern: "from \"../src/index.js\""
---

<objective>
Grow the public barrel by ten types and four values, and move the export-surface pin in lockstep
across the four files that carry it.

Purpose: `verbatimModuleSyntax` enforcement is **one-directional**. A type written into the value
block is TS1205 and nobody can miss it. A value moved the other way — out of the value block and
into `export type { … }` — is silently legal: `tsc -p tsconfig.test-d.json` exits 0, the emit build
exits 0, and `dist/index.js` quietly loses the runtime binding. `attw` and `publint` are BLIND to
it: mutant P8 measured both reporting "No problems found" with exit 0 on a build whose
`dist/index.js` had lost an export. Only the typecheck and the artifact cases catch it, which is why
all four files move together.

Output: the barrel, and its three guards, all agreeing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-action-declaration-and-build-time-validation/03-CONTEXT.md
@.planning/phases/03-action-declaration-and-build-time-validation/03-PATTERNS.md
@.planning/phases/03-action-declaration-and-build-time-validation/03-01-SUMMARY.md
@.planning/phases/03-action-declaration-and-build-time-validation/03-02-SUMMARY.md
@.planning/phases/03-action-declaration-and-build-time-validation/03-03-SUMMARY.md
@CLAUDE.md

<interfaces>
The intended new public surface. Ten types and four values, on top of the committed 39 types and
6 values, giving an EXPECTED 59 names = 49 types + 10 values.

  export type { JsonSchemaTarget, JsonSchemaConverterOptions, JsonSchemaConverter } from "./json-schema.js";
  export { JSON_SCHEMA_TARGET } from "./json-schema.js";

  export type { Catalog, CatalogEntry, CatalogIssue, CatalogIssueCode,
                CatalogDiagnostic, CatalogDiagnosticCode, BuildCatalogOptions } from "./catalog.js";
  export { buildCatalog, CatalogValidationError } from "./catalog.js";

  export { defineAction } from "./define-action.js";

Deliberately NOT exported: `LiteralDescription`, `IsNotConcrete`, `HoleProbe` (guard machinery a
consumer has no reason to name), `hasJsonSchemaConverter`, `vendorOf`, `emitSchema`,
`SchemaEmission` (internal plumbing), and `warnHost` (a host seam, not an API).

Current committed state, from `test/export-surface.test.ts:119-129`:
  45 names total · 39 types · 6 values
  VALUE_EXPORTS = [USER_CANCELLED, USER_DECLINED, CONSENT_GRADE_ORDER, MESSAGE_MAX_CHARS,
                   CONTRACT_VERSION, assertSingleInstance]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grow src/index.ts and refresh its now-stale module doc comment</name>
  <files>packages/concierge/src/index.ts</files>
  <read_first>
    - `packages/concierge/src/index.ts` in full (75 lines) — the two-block structure, the
      `// Category` comments matching `types.ts`'s section dividers, and the module doc comment at
      `:1-13` which lists `defineAction` among things "still being implemented".
    - `packages/concierge/test-d/exports.test-d.ts:7-18` — the measured one-directional hazard.
    - The three source modules this barrel now re-exports: `src/define-action.ts`,
      `src/json-schema.ts`, `src/catalog.ts`.
  </read_first>
  <action>
    Add the exports listed in this plan's `<interfaces>` block, following the file's own
    conventions: `export type { … }` statements first, grouped under `// Category` comments; value
    statements second; **one value-export statement per source module**, so `define-action.ts`,
    `json-schema.ts` and `catalog.ts` each get their own line. New *type* exports cannot join the
    existing `from "./types.js"` block — they need their own
    `export type { … } from "./catalog.js";` and `export type { … } from "./json-schema.js";`
    statements. Suggested category comments: `// Schema emission` and `// Catalog`.

    Update the module doc comment at `:1-13`. It currently says the package exports "the design
    contract, plus the single-instance contract guard" and lists `defineAction` among
    `createConcierge`, `createSession`, `defineStage` and `createBridge` as "still being implemented
    against these types". Move `defineAction` out of that list, add `buildCatalog` and
    `defineAction` to the sentence describing what ships, and leave the remaining four in the
    still-to-come list. Do not oversell: the catalog is built here but nothing dispatches from it
    yet — say so.

    Do not reorder or re-group any existing export; a diff that churns the committed block makes the
    real change unreviewable.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm build &amp;&amp; pnpm check:artifact &amp;&amp; node -e 'import("./packages/concierge/dist/index.js").then(m=>{for(const n of ["defineAction","buildCatalog","CatalogValidationError","JSON_SCHEMA_TARGET"]){if(typeof m[n]==="undefined")throw new Error("missing runtime binding: "+n)}console.log("BARREL_OK")})'</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` and `pnpm build` exit 0; `pnpm check:artifact` (publint --strict plus attw
      --profile esm-only) exits 0.
    - The dynamic-import probe above prints `BARREL_OK` — all four new values are real bindings, not
      `undefined`. This is the check `attw` and `publint` measurably cannot make.
    - `grep -c 'export {' packages/concierge/src/index.ts` returns 4 — one value statement per
      source module (`types.js`, `contract.js`, `json-schema.js`, `catalog.js`) plus
      `define-action.js` makes 5; confirm the actual count and record it rather than forcing this
      number if the grouping differs.
    - `grep -c 'defineAction' packages/concierge/src/index.ts` returns at least 2 — the export and
      the corrected doc comment.
    - `grep -c 'still being implemented' packages/concierge/src/index.ts` returns 1, and the line it
      matches no longer contains `defineAction`.
    - `grep -c 'LiteralDescription\|hasJsonSchemaConverter\|emitSchema\|warnHost' packages/concierge/src/index.ts`
      returns 0 — internal machinery stays internal.
  </acceptance_criteria>
  <done>The entrypoint exports the phase's public surface and its own doc comment tells the truth
  about what ships.</done>
</task>

<task type="auto">
  <name>Task 2: Move the export-surface pin across export-surface.test.ts, exports.test-d.ts and artifact.test.ts</name>
  <files>packages/concierge/test/export-surface.test.ts, packages/concierge/test-d/exports.test-d.ts, packages/concierge/test/artifact.test.ts</files>
  <read_first>
    - `packages/concierge/test/export-surface.test.ts` in full — `:74-99` (`readSurface`, WHICH MUST
      NOT BE TOUCHED), `:101-108` (`VALUE_EXPORTS`), `:119-129` (the counts, which appear in the
      `it` TITLES as well as the assertions).
    - `packages/concierge/test-d/exports.test-d.ts` in full (59 lines) — the single predicate and
      the `:32-42` note that this guard's diagnostic is TS1485 at the IMPORT line, not TS2344 at the
      assertion.
    - `packages/concierge/test/artifact.test.ts:59-79` — the freeze case and the two Phase 2
      value-export cases (`CONTRACT_VERSION` reaches dist as the integer 1; `assertSingleInstance`
      reaches dist as a callable function), which are the model for the new cases.
  </read_first>
  <action>
    **(a) `test/export-surface.test.ts`.** Run `pnpm build`, then read the actual trailing
    `export { … };` block out of `packages/concierge/dist/index.d.ts` and count it. Update:
      - the total-count assertion AND the `it` title that states it (a stale title lies in the test
        report, which is why the number is written twice);
      - the types/values split assertion AND its `it` title;
      - `VALUE_EXPORTS`, adding `defineAction`, `buildCatalog`, `CatalogValidationError` and
        `JSON_SCHEMA_TARGET`.
    The EXPECTED values are 59 / 49 / 10. **If the built artifact reports different numbers, do not
    force them.** Reconcile the actual trailing block against this plan's `<interfaces>` list, find
    which name is extra or missing, and either fix the barrel or record the measured numbers with an
    explanation. `CatalogValidationError` is a class and should appear as a VALUE (no `type `
    prefix) in the emitted block — confirm that it does, since the parser splits on that prefix.
    **Do not touch `readSurface`.** Its `no trailing export { … } statement found` error exists
    precisely to stop a red count being "fixed" by weakening the regex.

    **(b) `test-d/exports.test-d.ts`.** Add one predicate per new runtime value, each on ONE line,
    each importing from `../src/index.js` and NOT from the declaring module — that distinction is
    the entire point of the file, and `results.test-d.ts`'s existing `_messageBound` is structurally
    blind to this defect because it imports from `../src/types.js`.
      - `_jsonSchemaTargetExportedAsValue` — `Expect<Equals<typeof JSON_SCHEMA_TARGET, "draft-2020-12">>`.
      - `_defineActionExportedAsValue` — assert the imported binding is callable, e.g.
        `Expect<Assignable<typeof defineAction, (...args: never[]) => unknown>>`.
      - `_buildCatalogExportedAsValue` — the same shape for `buildCatalog`.
      - `_catalogValidationErrorExportedAsValue` — a class is a value AND a type; pin the value side
        with `Expect<Assignable<typeof CatalogValidationError, new (...args: never[]) => Error>>`.
    Extend the header to say the new predicates inherit the same TS1485-at-the-import-line
    behaviour, so a reader does not look at the wrong line.

    **(c) `test/artifact.test.ts`.** Add one case per new runtime value, modelled on the two Phase 2
    cases: dynamic-import the built artifact and assert the binding's runtime shape —
    `typeof m.defineAction === "function"`, `typeof m.buildCatalog === "function"`,
    `typeof m.CatalogValidationError === "function"`, `m.JSON_SCHEMA_TARGET === "draft-2020-12"`.
    Each with a comment naming what escapes: a guard exported as `undefined` makes every call site a
    silent no-op rather than an error. The two guards catch the same defect at different sampling
    rates (`artifact.test.ts:14-19`), so both are needed, not either.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm typecheck &amp;&amp; pnpm test &amp;&amp; pnpm check:artifact &amp;&amp; echo LOCKSTEP_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build && pnpm typecheck && pnpm test` all exit 0, and `pnpm test` now reports **6** test
      files (the four committed ones are unchanged in count; no new test FILE is added by this plan —
      confirm the file count is still 4 and only the case count grew). Record the measured
      files/tests numbers.
    - In `test/export-surface.test.ts`, the number in each `it` title equals the number in the
      assertion beneath it. Verify by reading both, not by grepping.
    - `VALUE_EXPORTS` in `test/export-surface.test.ts` has 10 entries and contains `defineAction`,
      `buildCatalog`, `CatalogValidationError` and `JSON_SCHEMA_TARGET`.
    - `git diff -- packages/concierge/test/export-surface.test.ts` shows NO change inside
      `readSurface` or the `EXPORT_BLOCK` regex.
    - `test-d/exports.test-d.ts` contains exactly one import from `../src/index.js` and zero imports
      from `../src/types.js` or any other `../src/` module.
    - Mutant P8-equivalent, run and recorded: move ONE new value out of the value block into the
      `export type { … }` block and confirm `pnpm typecheck` exits **non-zero** while `pnpm build`
      exits 0 — reproducing the measured one-directional hazard on this phase's own surface. Use
      `bash scripts/mutate-and-prove.sh packages/concierge/src/index.ts '<the one-occurrence literal>' '<the moved form>' -- pnpm --config.verify-deps-before-run=false --filter @fullselfbrowsing/concierge typecheck`
      and require harness exit 0. Rebuild afterwards: `dist/` is gitignored, so the harness's
      "tree clean" report says nothing about it.
  </acceptance_criteria>
  <done>All four places that carry the export surface agree with the built artifact, and the
  one-directional hazard is proved to be caught on this phase's own exports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package entrypoint → consumer | Everything crossing `src/index.ts` is API a consumer can depend on and this project cannot remove without a breaking change. |
| build toolchain → published artifact | `tsdown` decides what reaches `dist/index.js`; `attw` and `publint` were measured blind to a lost export. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-20 | Tampering | value moved into the `export type` block | mitigate | `test-d/exports.test-d.ts` predicate per value (TS1485 at the import line) plus a runtime `typeof` case in `artifact.test.ts`, plus a P8-equivalent mutant run on this phase's own surface. `pnpm build`, `attw` and `publint` all exit 0 on the defect — measured — so build-alone is never proposed as the gate. |
| T-03-21 | Tampering | internal machinery leaking into the public API | mitigate | `LiteralDescription`, `hasJsonSchemaConverter`, `emitSchema`, `SchemaEmission` and `warnHost` are deliberately unexported and asserted absent from `index.ts`. A published internal is a permanent compatibility obligation, and `warnHost` in particular is a host seam whose shape Phase 6 will change when `setTimeout` joins it. |
| T-03-22 | Repudiation | a stale count in an `it` title | mitigate | The count is written twice on purpose — in the assertion and in the title — and the acceptance criterion requires reading both. A stale title lies in the test report even while the assertion passes. |
| T-03-23 | Denial of Service | weakening `readSurface` to "fix" a red count | mitigate | `readSurface` is forbidden to this plan and the prohibition is a `git diff` acceptance criterion. Its own error message (`the parser, not the surface, is what changed`) is the standing guard. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing. |
</threat_model>

<verification>
- `pnpm build && pnpm typecheck && pnpm test && pnpm check:artifact && pnpm check:deps` all exit 0.
- The dynamic-import probe confirms all four new values are real runtime bindings.
- The P8-equivalent mutant PASSES (harness exit 0), and `dist/` is rebuilt afterwards.
- `git status --porcelain` shows only this plan's four files.
</verification>

<success_criteria>
- `defineAction` and `buildCatalog` are importable from the package entrypoint.
- The pinned surface is the MEASURED surface, and all four sites that carry it agree.
- The one-directional export hazard is proved caught on this phase's own exports, not assumed from
  Phase 2's proof on someone else's.
</success_criteria>

<output>
Create `.planning/phases/03-action-declaration-and-build-time-validation/03-04-SUMMARY.md` when done.
Record: the MEASURED names/types/values counts from `dist/index.d.ts` and any divergence from the
expected 59/49/10 with its cause; the exact literal used for the P8-equivalent mutant; and the
`pnpm test` files/tests numbers before and after.
</output>
