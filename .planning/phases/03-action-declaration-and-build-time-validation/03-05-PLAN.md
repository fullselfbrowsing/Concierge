---
phase: 03-action-declaration-and-build-time-validation
plan: 05
type: execute
wave: 3
depends_on: ["03-01", "03-03"]
files_modified:
  - packages/concierge/test-d/catalog.test-d.ts
autonomous: true
requirements: [CAT-01, CAT-07, SEC-01]

must_haves:
  truths:
    - "Two declarations passed to buildCatalog yield a literal union of their two names, not `string`."
    - "The literal union survives the round trip from `defineAction` through `buildCatalog`."
    - "byName is keyed by the derived literal union, so a typo'd lookup is a compile error."
  artifacts:
    - path: "packages/concierge/test-d/catalog.test-d.ts"
      provides: "CAT-01's compile-time half — the derived name union and what it keys"
      min_lines: 70
  key_links:
    - from: "packages/concierge/test-d/catalog.test-d.ts"
      to: "packages/concierge/src/catalog.ts"
      via: "reading the INFERRED return type of an unannotated buildCatalog call"
      pattern: "from \"../src/catalog.js\""
---

<objective>
Pin CAT-01's compile-time half: one declaration yields the action's literal name type, and the
catalog's lookup is keyed by that type rather than by open `string`.

Purpose: the mechanism is a single `const` modifier on `buildCatalog`'s type parameter, and losing
it is invisible. Measured across 50 actions — with `const A extends readonly AnyActionDefinition[]`
the names derive `"action00" | … | "action49"`; with a plain `readonly AnyActionDefinition[]`
parameter they derive `string`. Nothing else in the repository notices: the code still compiles,
the catalog still builds, every runtime test still passes, and the property CAT-01 exists to deliver
is simply gone. This file is that property's only detector.

Output: `test-d/catalog.test-d.ts`, and mutant M-03-3 proved to fire.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-action-declaration-and-build-time-validation/03-CONTEXT.md
@.planning/phases/03-action-declaration-and-build-time-validation/03-PATTERNS.md
@.planning/phases/03-action-declaration-and-build-time-validation/03-01-SUMMARY.md
@.planning/phases/03-action-declaration-and-build-time-validation/03-03-SUMMARY.md
@CLAUDE.md

<interfaces>
From `packages/concierge/src/catalog.ts` (plan 03-03):

  export function buildCatalog<const A extends readonly AnyActionDefinition[]>(
    actions: A, options?: BuildCatalogOptions,
  ): Catalog<A[number]["name"]>;

  export interface Catalog<Name extends string = string> {
    readonly entries: readonly CatalogEntry[];
    readonly names: readonly Name[];
    readonly byName: Readonly<Record<Name, CatalogEntry>>;
    readonly diagnostics: readonly CatalogDiagnostic[];
  }

From `packages/concierge/src/define-action.ts` (plan 03-01):

  export function defineAction<N extends string, D extends string, S extends StandardSchemaV1,
                               B = unknown, Snap = unknown, Ack = unknown>(
    def: Omit<ActionDefinition<N, S, B, Snap, Ack>, "description"> & { description: LiteralDescription<N, D> },
  ): ActionDefinition<N, S, B, Snap, Ack>;

From `packages/concierge/test-d/_assert.ts`:
  Expect<T extends true>, Equals<A, B>, Assignable<From, To>, Not<T extends boolean>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test-d/catalog.test-d.ts — the derived name union and what it keys</name>
  <files>packages/concierge/test-d/catalog.test-d.ts</files>
  <read_first>
    - `packages/concierge/test-d/actions.test-d.ts` in full — the EXACT analog. In particular
      `:32-53` (the export-nothing rule and its measured justification: annotating the const under
      test hands the test the answer it exists to derive, and the TS9010 diagnostic lands on the
      innocent line so the first fix a developer reaches for silently disables the assertion),
      `:55-66` (the one-line-per-predicate rule), `:88-109` (the local fixture block),
      `:182-202` (the unannotated-const technique this file reuses wholesale).
    - `packages/concierge/test-d/_assert.ts:1-38` — the four aliases and the `@ts-expect-error`
      house rule.
    - `packages/concierge/src/catalog.ts` and `src/define-action.ts` — this phase's own output.
    - `03-RESEARCH.md` *Pattern 2*, including the note that `tsc` truncates large unions in
      diagnostics.
  </read_first>
  <behavior>
    - `buildCatalog([applyFilter, cancelBooking])` yields `names` typed
      `readonly ("applyFilter" | "cancelBooking")[]` — NOT `readonly string[]`.
    - The same call's `byName` is keyed by that union: `Record<"applyFilter" | "cancelBooking", CatalogEntry>`.
    - A single-action catalog yields a single-member union, not `string`.
    - An empty catalog yields `never` for its name parameter, and that is not an error.
    - The union comes from `name` ALONE: an action whose `consent.requires` names a different action
      must not widen the union to include the target. This is the same defect `types.ts:478-487`
      records being hit in Phase 1 with `Name`, and `actions.test-d.ts:199` is its sibling detector
      at the declaration level; this one guards it at the catalog level.
    - `entries` is `readonly` at the type level AND the elements are `readonly` — a compile-time
      companion to the runtime freeze, worth pinning because `Readonly<…>` is erased at emit and
      only `Object.freeze` survives (`artifact.test.ts:62-63`).
  </behavior>
  <action>
    Create `packages/concierge/test-d/catalog.test-d.ts`.

    Header in the `test-d/` register, `WHAT ESCAPES WITHOUT THIS FILE` in caps: dropping the `const`
    modifier from `buildCatalog`'s type parameter compiles, builds, and passes every runtime test —
    the catalog is still correct, only its *type* has silently degraded from a literal union to open
    `string`. Downstream, Phase 4's stage scoping and Phase 8's grade gate both read that union, so
    the loss surfaces phases later as "why is my action name not autocompleting" rather than as a
    failure here. Also state the terse-output caveat (RESEARCH *Pitfall 10*): a failing `Expect<…>`
    prints only `Type 'false' does not satisfy the constraint 'true'` with no alias name in non-TTY
    output, so the mutant is asserted on exit code and `file:line`, never on a grep for a predicate
    name.

    Fixtures — local, doc-commented, none exported, following `actions.test-d.ts:88-109`:
    `declare const filterSchema: StandardSchemaV1<unknown, { key: string; value: string }>;` and a
    second schema if a second shape helps. Then two or three **deliberately unannotated**
    `defineAction({ … })` consts with inline-literal descriptions (CAT-07 accept cases), one of
    which carries `consent: { requires: "<a different action name>", bindTo: "userTurn" }` so the
    no-widening predicate has something to catch. Annotating any of them would hand the test the
    answer — say so in a comment pointing at `actions.test-d.ts:32-53`.

    Then the catalog consts, also unannotated: a two-action `buildCatalog([...])`, a one-action
    call, and an empty `buildCatalog([])`.

    Predicates, one line each however long, `_`-prefixed, each doc-commented with one sentence
    naming what breaks:
      - `_namesAreALiteralUnion` — `Expect<Equals<(typeof catalog)["names"], readonly ("applyFilter" | "cancelBooking")[]>>`.
      - `_namesAreNotWidenedToString` — `Expect<Not<Assignable<readonly string[], (typeof catalog)["names"]>>>`.
        A second, differently-shaped detector for the same mutant: `Equals` alone can be satisfied by
        an accident of alias resolution, and this one fails from the other direction.
      - `_byNameIsKeyedByTheUnion` — `Expect<Equals<keyof (typeof catalog)["byName"], "applyFilter" | "cancelBooking">>`.
      - `_singleActionYieldsASingletonUnion` — `Expect<Equals<(typeof soloCatalog)["names"], readonly "applyFilter"[]>>`.
      - `_emptyCatalogNamesAreNever` — `Expect<Equals<(typeof emptyCatalog)["names"], readonly never[]>>`.
        MEASURE this one first: if the compiler produces something other than `readonly never[]`,
        record the measured type and pin THAT, with a note saying what was measured. Do not bend the
        implementation to match a guess.
      - `_nameUnionIgnoresConsentRequires` — assert the union of the two-action catalog does not
        contain the `consent.requires` target name.
      - `_entriesAreReadonly` — `Expect<Not<Assignable<(typeof catalog)["entries"], CatalogEntry[]>>>`.

    No `@ts-expect-error`. Export nothing.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; bash scripts/mutate-and-prove.sh packages/concierge/src/catalog.ts '&lt;const A extends' '&lt;A extends' -- pnpm --config.verify-deps-before-run=false --filter @fullselfbrowsing/concierge typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` exits 0 with the new file in the program.
    - M-03-3: the mutant above exits 0 (PASS) with the gate reporting exit **1**. Record the observed
      exit code — `scripts/mutate-and-prove.sh:32` says 2 and is stale under TypeScript 7.0.2. If
      the pattern `<const A extends` does not occur exactly once in `catalog.ts`, the harness exits
      3 (ABORT, no-op); use the one-occurrence literal recorded in `03-03-SUMMARY.md` and note it.
    - `git diff --exit-code -- packages/concierge/src/catalog.ts` exits 0 after the mutant.
    - At least 6 `Expect<` predicates are present, each on one line, each `_`-prefixed.
    - `grep -v '^[[:space:]]*[/*]' packages/concierge/test-d/catalog.test-d.ts | grep -c 'ts-expect-error'`
      returns 0, and the same filtered grep for `^export` returns 0.
    - No `defineAction` or `buildCatalog` const in this file carries a type annotation. Verify by
      reading every `const` declaration.
    - The measured type of `_emptyCatalogNamesAreNever` is recorded in the summary, whether or not it
      matched the prediction.
  </acceptance_criteria>
  <done>CAT-01's derivation is pinned by two independent detectors, the lookup key is pinned, and the
  one-modifier mutant that silently destroys it is proved to fire.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| declaration → derived name union | The only place the catalog's identity vocabulary is established. Everything downstream — Phase 4 stage scoping, Phase 8's grade gate, a consumer's autocomplete — reads it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-24 | Tampering | `const` type parameter on `buildCatalog` | mitigate | Two independent predicates in opposite directions (`Equals` on the exact type, `Not<Assignable>` against `readonly string[]`) plus mutant M-03-3. Losing the modifier compiles, builds, and passes every runtime test; this file is the only detector. |
| T-03-25 | Tampering | name union widened by a second inference site | mitigate | `_nameUnionIgnoresConsentRequires`. `types.ts:478-487` records the identical defect being hit in Phase 1, where typing `consent.requires` as the action's own `Name` widened it to the union of two sites and "silently corrupt[ed] the name-union derivation the whole catalog depends on". |
| T-03-26 | Repudiation | annotating a const under test | mitigate | The export-nothing rule plus an explicit acceptance criterion requiring every `defineAction`/`buildCatalog` const to be unannotated. `actions.test-d.ts:32-53` measured why this matters: the TS9010 diagnostic lands on the innocent line, so the first fix a developer reaches for is the one that disables the assertion. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing. |
</threat_model>

<verification>
- `pnpm typecheck` exits 0.
- Mutant M-03-3 PASSES with the gate exiting 1, and the source is provably restored.
- `pnpm test` is untouched by this plan and still green.
- `git status --porcelain` shows only `packages/concierge/test-d/catalog.test-d.ts`.
</verification>

<success_criteria>
- One declaration yields the action's literal name type, proved at compile time by two detectors
  that fail in different directions.
- The catalog's lookup is keyed by the derived union, so a typo'd name is a compile error rather
  than an `undefined` at runtime.
- The empty-catalog case is measured rather than assumed.
</success_criteria>

<output>
Create `.planning/phases/03-action-declaration-and-build-time-validation/03-05-SUMMARY.md` when done.
Record: the observed `tsc` exit code under M-03-3; the exact one-occurrence literal used as the
mutant pattern; and the measured type of an empty catalog's `names`, with a note if it diverged from
`readonly never[]`.
</output>
