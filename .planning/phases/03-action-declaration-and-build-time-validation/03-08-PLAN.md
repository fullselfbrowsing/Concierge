---
phase: 03-action-declaration-and-build-time-validation
plan: 08
type: execute
wave: 5
depends_on: ["03-05", "03-06", "03-07"]
files_modified:
  - packages/concierge/src/types.ts
  - .planning/phases/03-action-declaration-and-build-time-validation/03-VALIDATION.md
autonomous: true
requirements: [CAT-01, CAT-02, CAT-05, CAT-06, CAT-07, SEC-01, SEC-05, DX-03]

must_haves:
  truths:
    - "No prose shipped inside `dist/index.d.ts` claims something this repository has made false."
    - "The three frozen constants no longer retain dead calls in every consumer bundle."
    - "Every phase rule has a mutant that was observed to fire against the FINAL tree."
    - "Every gate that exited 0 before this phase still exits 0 after it."
  artifacts:
    - path: "packages/concierge/src/types.ts"
      provides: "Corrected prose and three `/* @__PURE__ */` annotations"
    - path: ".planning/phases/03-action-declaration-and-build-time-validation/03-VALIDATION.md"
      provides: "The signed-off per-task verification map"
  key_links:
    - from: "packages/concierge/src/types.ts"
      to: "packages/concierge/dist/index.d.ts"
      via: "types.ts ships verbatim inside the published declarations, so consumers read its prose"
      pattern: "readsUntrusted"
---

<objective>
Close the phase: correct the prose this repository has made false, stop shipping dead `Object.freeze`
calls into every consumer bundle, re-run the whole mutant battery against the final tree, and sign
off the validation map.

Purpose: `types.ts` ships verbatim inside `dist/index.d.ts`, so **consumers read its doc comments**.
Three claims in it are currently false — one made false by plan 02-11, two made false by this phase
— and a fourth file-level defect (three `Object.freeze` calls with no `/* @__PURE__ */`) retains
roughly 205 bytes of dead calls in every consumer bundle now that `assertSingleInstance` keeps the
module alive. This is the plan that touches `types.ts`, so per CONTEXT `<specifics>` all of it lands
in one commit.

Output: a truthful `types.ts`, a measured bundle-size delta, a battery of twelve observed mutants,
and a signed validation map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-action-declaration-and-build-time-validation/03-CONTEXT.md
@.planning/phases/03-action-declaration-and-build-time-validation/03-PATTERNS.md
@.planning/phases/03-action-declaration-and-build-time-validation/03-VALIDATION.md
@.planning/phases/03-action-declaration-and-build-time-validation/03-03-SUMMARY.md
@.planning/phases/03-action-declaration-and-build-time-validation/03-06-SUMMARY.md
@.planning/phases/03-action-declaration-and-build-time-validation/03-07-SUMMARY.md
@CLAUDE.md

<interfaces>
The four edit sites in `packages/concierge/src/types.ts`, with the exact replacement wording where
it has already been agreed:

  :505-506  "Its only symptom is one unused suppression directive in the type-test suite — the kind
             of thing a reviewer 'fixes' by deleting the test."
        ->  "Its symptoms are one unused suppression directive and, since plan 02-11, a TS2344 on
             `_policyNotBivariant`."
            (verbatim from `02-11-SUMMARY.md:317`)

  :975-984  "**Phase 1 ships this field and its type test, and nothing else reads it.** … Until that
             lands, setting this to `true` changes no behaviour."
        ->  made false by THIS phase. SEC-05 landed: `buildCatalog` emits a
            `reads_untrusted_without_consent` diagnostic. New wording must name the code and the
            `onDiagnostic` hook, and must NOT overstate — the diagnostic does not block the build.

  :873-878  "**Enforcement of the wider requirement lives in Phase 3, under SEC-01** … The owner is
             named here so a reader who finds nothing checking this at runtime concludes the
             requirement is *elsewhere* rather than unowned."
        ->  made true by THIS phase. Restate as shipped, naming the two branches: a non-empty schema
            with no `redact` is a `redaction_missing` build failure; an empty schema with no
            `redact` resolves to `"drop"`.

  :844      "`buildCatalog` warns when `destructive: true` carries no `consent` policy."
        ->  now accurate but vague. Name the diagnostic code `destructive_without_consent` and say
            it reports without blocking.

  :243, :265, :467 — three `Object.freeze(` calls, currently with ZERO `/* @__PURE__ */`
  annotations anywhere in `src/` (verified by grep).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the four prose sites and add three /* @__PURE__ */ annotations</name>
  <files>packages/concierge/src/types.ts</files>
  <read_first>
    - `packages/concierge/src/types.ts:494-518` (the `snapshotEquality` doc comment carrying the
      stale M9 claim), `:835-853` (`SideEffects` and CAT-05's ownership line), `:859-883`
      (`RedactionPolicy` and SEC-01's ownership paragraph), `:947-991` (`readsUntrusted` and its
      "nothing else reads it" paragraph), and `:236-268` and `:463-473` (the three
      `Object.freeze` sites).
    - `.planning/phases/02-packaging-build-and-release/02-11-SUMMARY.md:315-317` — the agreed
      replacement wording, and `:448` which names this exact deferral and its release condition
      ("02-12, or any Phase 3+ plan that legitimately opens both").
    - `packages/concierge/src/catalog.ts` — so every rewritten claim names what actually shipped:
      the two diagnostic codes, the `redaction_missing` issue, and the `onDiagnostic` hook.
    - `packages/concierge/test/artifact.test.ts:59-66` — the frozen-constants case, which must stay
      green after the annotations.
  </read_first>
  <action>
    **(a) Measure first.** Record `wc -c packages/concierge/dist/index.js` and the `pnpm check:deps`
    output BEFORE any edit.

    **(b) Correct the four prose sites** listed in this plan's `<interfaces>` block. Use the agreed
    verbatim wording where it exists (`:505-506`). For the three sites this phase itself made stale
    or vague, write replacements that name the concrete shipped artefacts — the issue code, the two
    diagnostic codes, the `onDiagnostic` hook, and the two branches of SEC-01's redaction
    resolution — and that do NOT overstate: `readsUntrusted` now has a gate that *reports*, and
    saying it blocks would be exactly the overstatement `types.ts:975-984` was written to prevent.
    Keep every paragraph in the file's existing register: what was measured, what the
    wrong-looking-right alternative is, what breaks silently.

    **(c) Add `/* @__PURE__ */` before each of the three `Object.freeze(` calls** at `:243`, `:265`
    and `:467`. There are currently zero such annotations in `src/`. Without them the calls are
    retained in every consumer bundle, because `assertSingleInstance` now keeps the module alive —
    measured by 02-06 and independently by 02-07 and the Phase 2 verifier.

    **(d) Measure again and record the delta.** Run `pnpm build`, then `wc -c packages/concierge/dist/index.js`
    and `pnpm check:deps`. RESEARCH's estimate is ~205 B. **Record the measured number, whatever it
    is, and flag any divergence from the estimate rather than restating the estimate.** If the delta
    is zero, that is a finding: it would mean the calls were already being dropped, or that the
    annotation is not reaching the bundler, and it must be reported rather than assumed away.

    **(e) Confirm nothing regressed.** `artifact.test.ts` asserts `Object.isFrozen(USER_CANCELLED)`
    and `Object.isFrozen(USER_DECLINED)` in the built artifact. `/* @__PURE__ */` marks a call as
    side-effect-free for tree-shaking; it must NOT cause the freeze to be dropped when the value IS
    used. If either assertion goes red, the annotation is wrong for this site — revert that site and
    report, do not weaken the test.

    Change nothing else in `types.ts`. CONTEXT is explicit that a `types.ts` change is otherwise a
    deviation, not a task, and RESEARCH *Open Question 1* recommends guarding CAT-07 on
    `defineAction` alone precisely so that no amendment is needed. **No type in this file is
    amended by this phase** — state that plainly in the summary so a verifier does not read the
    file's appearance in a diff as a contract change.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm typecheck &amp;&amp; pnpm test &amp;&amp; pnpm check:deps &amp;&amp; pnpm check:artifact &amp;&amp; node -e 'const s=require("node:fs").readFileSync("packages/concierge/src/types.ts","utf8");const n=(s.match(/\/\* @__PURE__ \*\/ ?Object\.freeze\(/g)||[]).length;if(n!==3)throw new Error("expected 3 pure-annotated freezes, found "+n);if(/only symptom is one unused suppression directive/.test(s))throw new Error("stale M9 claim still present");console.log("TYPES_PROSE_OK")'</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build && pnpm typecheck && pnpm test && pnpm check:deps && pnpm check:artifact` all exit 0.
    - Exactly three `/* @__PURE__ */`-annotated `Object.freeze(` calls exist in
      `packages/concierge/src/types.ts`, and no un-annotated `Object.freeze(` remains in that file.
    - `grep -c "only symptom is one unused suppression directive" packages/concierge/src/types.ts`
      returns 0; `grep -c "_policyNotBivariant" packages/concierge/src/types.ts` returns at least 1.
    - `grep -c "nothing else reads it" packages/concierge/src/types.ts` returns 0.
    - `grep -c "reads_untrusted_without_consent" packages/concierge/src/types.ts` returns at least 1,
      and the same for `destructive_without_consent` and `redaction_missing`.
    - `git diff -U0 -- packages/concierge/src/types.ts` shows changes ONLY inside comments and on the
      three `Object.freeze` lines. Read every changed line to confirm no type declaration moved.
    - `pnpm test artifact` passes, including both `Object.isFrozen` assertions.
    - The before/after `dist/index.js` byte counts and the `check:deps` figures are recorded, with
      the measured delta stated as a number and any divergence from the ~205 B estimate flagged.
  </acceptance_criteria>
  <done>Nothing false ships inside the published declarations, the dead calls are gone, and the delta
  is a measurement rather than a repetition of a prediction.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the full twelve-mutant battery against the final tree</name>
  <files>packages/concierge/src/types.ts</files>
  <read_first>
    - `scripts/mutate-and-prove.sh` in full — the exit-code contract and the known lockfile hazard.
    - `03-01-SUMMARY.md`, `03-02-SUMMARY.md`, `03-03-SUMMARY.md`, `03-05-SUMMARY.md`,
      `03-06-SUMMARY.md`, `03-07-SUMMARY.md` — the exact one-occurrence literal pattern recorded for
      every mutant. Reuse them; do not re-derive.
  </read_first>
  <action>
    Each mutant was proved in the plan that introduced it, against the tree as it stood then. The
    tree has moved since — new exports, new tests, corrected prose, `/* @__PURE__ */` annotations.
    Re-run all twelve against the FINAL tree, in one pass, and record each observed gate exit code.

    | # | Target | Mutation | Gate |
    |---|---|---|---|
    | M-03-1 | `src/define-action.ts` | `description: LiteralDescription<N, D>` to `description: D` | `pnpm typecheck` |
    | M-03-2 | `src/define-action.ts` | drop the interior-hole probe from `HoleProbe` | `pnpm typecheck` |
    | M-03-3 | `src/catalog.ts` | `<const A extends` to `<A extends` | `pnpm typecheck` |
    | M-03-4 | `src/json-schema.ts` | derivation before the escape hatch | build + `pnpm test emission` |
    | M-03-5 | `src/json-schema.ts` | `.input(` to `.output(` | build + `pnpm test emission` |
    | M-03-6 | `src/json-schema.ts` | remove the root-`type` check | build + `pnpm test emission` |
    | M-03-7 | `src/catalog.ts` | `deepFreeze(...)` to shallow `Object.freeze(...)` | build + `pnpm test catalog` |
    | M-03-8 | `src/catalog.ts` | remove `assertSingleInstance();` | build + `pnpm test single-instance` |
    | M-03-9 | `src/catalog.ts` | throw on first issue instead of aggregating | build + `pnpm test catalog` |
    | M-03-10 | `src/json-schema.ts` | drop `vendor` from the not-emittable failure | build + `pnpm test emission` |
    | M-03-11 | `src/catalog.ts` | `Object.create(null)` to `{}` | build + `pnpm test catalog` |
    | M-03-12 | `src/catalog.ts` | delete the `redaction_missing` issue push | build + `pnpm test catalog` |

    Rules that apply to every row:
      - Expect gate exit **1**, not 2. `scripts/mutate-and-prove.sh:32` says "tsc exits 2 on
        diagnostics" and is stale prose under TypeScript 7.0.2; every Phase 2 measurement already
        recorded 1. Record the observed code for each row.
      - Every gate uses `pnpm --config.verify-deps-before-run=false`. `CI=true` and
        `--frozen-lockfile` are actively wrong: they produce a vacuously-green PASS.
      - Every gate that runs a runtime test must build first, and **every such mutant must be
        followed by `pnpm build`** — `dist/` is gitignored, so both `git status --porcelain` and the
        harness's own "tree clean" line are blind to a mutated artifact left behind.
      - A FAIL (harness exit 1, mutant escaped) means the SUITE is the defect, not the mutant. Fix
        the suite in the file that owns it and re-run. An ABORT 3 means the pattern no longer matches
        exactly once; find the new unique literal and record it.

    Also re-run the two guard mutants this phase added outside the numbered battery, for the same
    reason: the SEC-01 `redact`-optionality mutant on `types.ts` (from plan 03-01) and the
    `JSON_SCHEMA_TARGET` annotation mutant on `json-schema.ts` (from plan 03-02).
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm typecheck &amp;&amp; pnpm test &amp;&amp; git status --porcelain &amp;&amp; git diff --exit-code</automated>
  </verify>
  <acceptance_criteria>
    - All twelve numbered mutants plus the two guard mutants report `PASS: gate fired`, harness exit 0.
    - Every observed gate exit code is recorded in a table in the summary — fourteen rows.
    - After the battery, `git diff --exit-code` exits 0 and `git status --porcelain` is empty.
    - After the battery, `pnpm build && pnpm typecheck && pnpm test` all exit 0, proving `dist/` was
      restored from clean source.
    - Any mutant whose pattern had to change is recorded with both the old and new literal and the
      reason.
  </acceptance_criteria>
  <done>Every rule this phase added is proved to fire against the tree that will actually ship.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full gate set and sign off 03-VALIDATION.md</name>
  <files>.planning/phases/03-action-declaration-and-build-time-validation/03-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/03-action-declaration-and-build-time-validation/03-VALIDATION.md` — the
      per-task verification map to complete.
    - All seven prior summaries in this phase directory, for the measured numbers each recorded.
    - `package.json` (repo root) `:19-27` — the seven gate scripts.
  </read_first>
  <action>
    **(a) Run every gate on a clean tree, in this order, and record each exit code and each headline
    number:**
      `pnpm build` · `pnpm typecheck` · `pnpm test` · `pnpm check:artifact` · `pnpm check:deps` ·
      `pnpm check:pack` · `pnpm check:node-floor`.
    All seven exited 0 before this phase and all seven must exit 0 after it. `check:pack` packs a
    tarball and typechecks a scratch consumer against the shipped declarations — it is the gate that
    would catch a `defineAction` whose signature does not survive `.d.ts` emission, which is the
    single most plausible late failure in this phase. If it fails, that is a real finding about the
    CAT-07 guard's emitted form, not a harness problem.

    **(b) Record the final export surface** measured from `dist/index.d.ts`: total names, types,
    values — and confirm it matches what `test/export-surface.test.ts` pins.

    **(c) Complete `03-VALIDATION.md`.** Fill in:
      - *Test Infrastructure*: Vitest 4.1.10 for `test/*.test.ts`; `tsc -p tsconfig.test-d.json`
        (via `pnpm typecheck`) for `test-d/*.test-d.ts`; quick command `pnpm test <fragment>` in the
        **bare** form; full command `pnpm build && pnpm typecheck && pnpm test`. Note explicitly
        that `pnpm test -- <fragment>` does NOT filter — vitest's cac CLI discards everything after
        `--` — reproduced six times.
      - *Sampling Rate*: per task commit, `pnpm typecheck` plus the one `pnpm test <fragment>` the
        task touches; per wave merge, the full three-gate run; phase gate, all seven scripts.
      - *Per-Task Verification Map*: one row per task across plans 03-01 through 03-08, each with
        its plan, wave, requirement IDs, threat ref, secure behaviour, test type, automated command,
        whether the file existed, and its final status.
      - *Wave 0 Requirements*: list the six files that did not exist at planning time —
        `test-d/description-literal.test-d.ts`, `test-d/json-schema.test-d.ts`,
        `test-d/catalog.test-d.ts`, `test/catalog.test.ts`, `test/emission.test.ts`,
        `test/fixtures/schemas.ts` — each annotated with the plan that created it. Mark
        `wave_0_complete: true` once all six exist and are green.
      - *Manual-Only Verifications*: state the accepted CAT-07 `${number}` / `${bigint}` gap here as
        a documented, accepted residual with its reasoning and its known-gap pin, so a verifier
        reads it as a decision rather than an omission. Note also that `ci.yml` and `release.yml`
        have still never executed (STATE.md deferral, carried from Phase 2, unchanged by this
        phase).
      - *Validation Sign-Off*: tick each box that is genuinely satisfied and set
        `nyquist_compliant: true` only if every task row carries an `<automated>` command and no
        three consecutive rows lack one. If a row cannot honestly be ticked, leave it unticked and
        say why — an untrue sign-off is worse than a visible gap.
      - Set `status: complete` in the frontmatter and stamp the approval date.

    **(d) Record two handoffs to Phase 4** prominently in the summary, because both are decisions
    this phase made that Phase 4 will otherwise re-derive or contradict:
      1. `frozenArray.filter(...)` returns a NEW, UNFROZEN array, so `catalogFor` must re-freeze its
         filtered result or STG-04's memoized catalogs are mutable.
      2. `catalog.byName` is a frozen null-prototype record, NOT a `Map` — a frozen `Map` still
         accepts `.set()`. The ROADMAP Phase 6 note says handler lookup should go "in a `Map`, not
         an object literal"; the null prototype satisfies that note's actual concern
         (`__proto__` / `constructor` keys) while also being freezable, which a `Map` is not.

    **(e) Confirm the two requirement-ownership questions were resolved as planned, not absorbed
    silently:** CAT-03 (consent target exists) and CAT-04 (grade ceiling) remain with Phases 4 and 8
    per REQUIREMENTS.md — CAT-03 needs the whole assembled catalog including cross-stage actions,
    and CAT-04 needs a transport that does not exist until Phase 7. SEC-03 was closed HERE rather
    than in Phase 4, per CONTEXT, because `buildCatalog` is the only place a freeze can happen.
    State all three in the summary.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm typecheck &amp;&amp; pnpm test &amp;&amp; pnpm check:artifact &amp;&amp; pnpm check:deps &amp;&amp; pnpm check:pack &amp;&amp; pnpm check:node-floor &amp;&amp; git status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - All seven gate scripts exit 0 and each exit code is recorded.
    - `git status --porcelain` is empty except for `03-VALIDATION.md`.
    - `03-VALIDATION.md` has no `{placeholder}` text remaining, has one map row per task across all
      eight plans, and its frontmatter reads `status: complete` with `wave_0_complete: true`.
    - `nyquist_compliant` is set to the honest value, and if `false`, the reason is written in the
      sign-off section.
    - The measured final export surface is recorded and matches the pinned counts in
      `test/export-surface.test.ts`.
    - Both Phase 4 handoffs and all three requirement-ownership statements appear in the summary.
  </acceptance_criteria>
  <done>Every gate is green on a clean tree, the validation map is complete and honestly signed, and
  the two decisions Phase 4 must inherit are written where Phase 4 will find them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `types.ts` prose → published `dist/index.d.ts` | Doc comments in this file ship verbatim to consumers. A false claim here is a false claim in the product. |
| repository source → consumer bundle | `/* @__PURE__ */` changes what a bundler keeps. Getting it wrong either retains dead code or drops a live freeze. |
| mutation harness → repository working tree | Twelve mutations across three source files, plus a gitignored artifact rebuilt under each. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-41 | Repudiation | false prose shipping inside the published declarations | mitigate | Four sites corrected in one commit, three of them asserted absent by grep in the task's automated verify. `types.ts` ships inside `dist/index.d.ts`, so a stale claim is read by consumers, not just by maintainers. |
| T-03-42 | Repudiation | overstating what `readsUntrusted` now does | mitigate | The replacement wording must say the marker *reports* and does not block, matching what shipped. `types.ts:975-984` exists precisely to prevent an unenforced marker being mistaken for a control; replacing it with an overstatement would invert its purpose. |
| T-03-43 | Tampering | `/* @__PURE__ */` dropping a live freeze | mitigate | `artifact.test.ts`'s two `Object.isFrozen` assertions run against the built artifact after the annotation. If either goes red the annotation is wrong for that site and is reverted — the test is not weakened. |
| T-03-44 | Repudiation | reporting an unmeasured byte delta | mitigate | Before/after `wc -c` and `check:deps` are both required, and a zero delta is explicitly named as a finding to report rather than a result to assume away. RESEARCH's ~205 B is a prediction, not an assertion. |
| T-03-45 | Tampering | a mutated `dist/` or `src/` surviving the battery | mitigate | `git diff --exit-code` and an empty `git status --porcelain` after the battery, plus a full three-gate run proving the artifact was rebuilt from clean source. `dist/` is gitignored and invisible to both git checks, which is why the rebuild is asserted separately. |
| T-03-46 | Repudiation | an untruthful validation sign-off | **accept, with control** | The task explicitly instructs that a box which cannot honestly be ticked is left unticked with the reason written down, and that `nyquist_compliant` takes the honest value. A green-looking map over a real gap is the failure this milestone is built to avoid. |
| T-03-47 | Denial of Service | `check:pack` failing on the emitted `defineAction` signature | mitigate | `check:pack` is run as a phase gate specifically because it typechecks a scratch consumer against the SHIPPED declarations, and the CAT-07 guard's survival through `.d.ts` emission is the most plausible late failure. A failure there is treated as a real finding about the guard, not as a harness problem. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing. `check:pack` and `check:node-floor` run their own scratch installs outside the repository; `check:node-floor`'s unverified Node download (T-02-44) remains an accepted, recorded deferral and is not invoked from CI. |
</threat_model>

<verification>
- All seven gate scripts exit 0 on a clean tree.
- Fourteen mutants observed firing, each with its gate exit code recorded.
- `git diff --exit-code` exits 0 and `git status --porcelain` is empty after the battery.
- `03-VALIDATION.md` is complete, with no placeholders and an honest sign-off.
</verification>

<success_criteria>
- Nothing false ships inside the published declarations.
- The three dead `Object.freeze` calls are annotated and the bundle delta is a recorded measurement.
- Every rule this phase added has been observed to fire against the final tree.
- Every gate that was green before this phase is green after it.
- The validation map is complete and its sign-off is true.
</success_criteria>

<output>
Create `.planning/phases/03-action-declaration-and-build-time-validation/03-08-SUMMARY.md` when done.
Record: before/after `dist/index.js` byte counts and the measured `/* @__PURE__ */` delta, with any
divergence from the ~205 B estimate flagged; a fourteen-row table of mutants with observed gate exit
codes; the final measured export surface; the two Phase 4 handoffs (re-freeze after `.filter()`, and
`byName` as a null-prototype record rather than a `Map`); the three requirement-ownership statements
(CAT-03 stays in Phase 4, CAT-04 in Phase 8, SEC-03 closed here); and an explicit statement that no
type in `types.ts` was amended by this phase.
</output>
