---
phase: 03-action-declaration-and-build-time-validation
plan: 06
type: execute
wave: 4
depends_on: ["03-04"]
files_modified:
  - packages/concierge/test/catalog.test.ts
  - packages/concierge/test/single-instance.test.ts
autonomous: true
requirements: [CAT-01, CAT-02, CAT-05, SEC-01, SEC-03, SEC-05, DX-03, PKG-04]

must_haves:
  truths:
    - "Four bad declarations produce four issues in ONE throw, each naming its own action."
    - "Every issue carries `action` and `fix` as fields a test reads, not as substrings."
    - "A destructive action with no consent policy BUILDS and appears in `catalog.diagnostics`."
    - "A readsUntrusted action with no consent policy appears under a different code."
    - "Replacing a built handler fails, and the original handler is still there afterwards."
    - "buildCatalog([]) populates the contract registry — the guard's first production call site."
  artifacts:
    - path: "packages/concierge/test/catalog.test.ts"
      provides: "The behavioural suite for CAT-01/02/05, SEC-01/03/05 and DX-03"
      min_lines: 220
  key_links:
    - from: "packages/concierge/test/catalog.test.ts"
      to: "packages/concierge/dist/index.js"
      via: "dynamic import of the BUILT artifact"
      pattern: "dist/index.js"
    - from: "packages/concierge/test/single-instance.test.ts"
      to: "buildCatalog"
      via: "the global registry record, the suite's established observable"
      pattern: "registry\\[KEY\\]"
---

<objective>
Prove the catalog behaves — that errors aggregate and name their actions, that both warning markers
report without blocking, that redaction fails closed, that a built catalog cannot be tampered with,
and that `assertSingleInstance` finally fires from a path a consumer reaches.

Purpose: every one of these is a claim that would otherwise be true only in a doc comment. Two in
particular are known to pass a naive test: `Object.isFrozen(catalog)` returns `true` on the breached
shallow freeze, and a `destructive_without_consent` warning written to the console is an annotation
nothing reads and nothing can assert.

Output: `test/catalog.test.ts`, a new SC-5 case in `test/single-instance.test.ts`, and four mutants
proved to fire.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-action-declaration-and-build-time-validation/03-CONTEXT.md
@.planning/phases/03-action-declaration-and-build-time-validation/03-PATTERNS.md
@.planning/phases/03-action-declaration-and-build-time-validation/03-03-SUMMARY.md
@.planning/phases/03-action-declaration-and-build-time-validation/03-04-SUMMARY.md
@CLAUDE.md

<interfaces>
Imported from `../dist/index.js` (the BUILT artifact — see the dist-only rule below):

  buildCatalog(actions, options?) -> Catalog
  CatalogValidationError  with  .issues: readonly { code, action, vendor?, problem, fix }[]
  defineAction(def) -> ActionDefinition
  JSON_SCHEMA_TARGET === "draft-2020-12"
  assertSingleInstance()
  CONTRACT_VERSION === 1

Catalog shape: { entries: [{ action, parameters }], names, byName, diagnostics }
CatalogIssueCode: "duplicate_action_name" | "schema_not_emittable" | "schema_root_not_object" | "redaction_missing"
CatalogDiagnosticCode: "destructive_without_consent" | "reads_untrusted_without_consent"
BuildCatalogOptions: { jsonSchemaTarget?, onDiagnostic? }

Fixtures from `./fixtures/schemas.js` (plan 03-02): zodObject, zodDiscriminatedUnion, zodStringRoot,
zodEmptyObject, zodWithDefault, arktypeObject, arktypeEmptyObject, valibotObject,
valibotEscapeHatchSchema, probeSchema, probeSchemaThatThrows.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write test/catalog.test.ts — aggregation, both diagnostics, SEC-01 and SEC-03</name>
  <files>packages/concierge/test/catalog.test.ts</files>
  <read_first>
    - `packages/concierge/test/single-instance.test.ts` in full — the EXACT analog: the
      `What escapes without this file:` header heading, the import ordering (node builtins, blank
      line, third-party, then vitest named imports alphabetically), the `beforeAll` dist-existence
      guard at `:59-66`, the per-test reset with a doc comment on the mechanism at `:68-82`, the
      hard-coded `Symbol.for` key and its justification at `:44-53`, the `describe`/`it` title
      convention at `:106-107` and `:125`, and the two-expectations-per-claim rule at `:154-166`.
    - `packages/concierge/test/artifact.test.ts:59-79` — the freeze-assertion form and the sentence
      explaining why `Readonly<…>` is not enough.
    - `packages/concierge/test/export-surface.test.ts:31-46` — the precedent for writing a
      deliberately-absent guard DOWN rather than writing it as a vacuously-passing assertion.
    - `packages/concierge/src/catalog.ts` — the implementation under test.
    - `03-RESEARCH.md` *Pitfall 7* (the measured shallow-freeze breach) and the *Code Examples*
      measured `CatalogValidationError` output.
  </read_first>
  <action>
    Create `packages/concierge/test/catalog.test.ts` importing `../dist/index.js` and
    `./fixtures/schemas.js`.

    **Header.** Use the literal heading `What escapes without this file:` and name the defects: a
    `buildCatalog` that throws on the first issue instead of aggregating (a developer with twenty
    bad actions then fixes and rebuilds twenty times, and nothing fails); a
    `destructive_without_consent` warning that goes only to the console, which is an annotation
    nothing reads and nothing can assert; and a shallow freeze, which leaves
    `Object.isFrozen(catalog) === true` while `catalog.entries[0].action.handler = attacker`
    succeeds *silently* and the replacement runs.

    **State the dist-versus-src decision in a header paragraph**, per the house rule that the three
    existing suites each state theirs. This suite reads `../dist/index.js` — the same as its three
    siblings — because it is the artifact a consumer imports and because
    `vitest.config.ts:48-80` records that `test/` is in no TypeScript program, so a `src/` import
    would be untypechecked anyway and would additionally test code that never shipped. Mention
    `../src/` only inside comments, and say so explicitly, because an acceptance check greps
    non-comment lines. Add the `beforeAll` dist-existence guard verbatim from
    `single-instance.test.ts:59-66`, adjusted to this file.

    **Note the registry side effect.** `buildCatalog` calls `assertSingleInstance()`, so every test
    in this file writes the global contract registry. Vitest's default isolation gives each test
    file its own process, so this does not leak into `single-instance.test.ts` — say that in a
    comment rather than leaving a reader to wonder, and add the same `delete registry[KEY]`
    `beforeEach` reset the sibling suite uses, with the hard-coded `Symbol.for` key and the same
    justification for hard-coding it.

    **Cases.** Group into `describe` blocks named for the requirement plus the claim.

    *CAT-01 — one declaration derives everything:*
      1. A two-action catalog exposes `names` in declaration order, `byName` resolving each name to
         its entry, and `entries[i].parameters` carrying the emitted schema — with no second
         registry anywhere in the call.
      2. `entries[i].action.redact` is the declared policy, unchanged, when one was declared.

    *DX-03 — aggregation and structure:*
      3. FOUR distinct bad actions in one call throw ONCE, and `err.issues` has length 4 with the
         four expected `code` values and the four distinct `action` names. Model the mix on the
         measured prototype output: a `discriminatedUnion` root, a validator with no Standard JSON
         Schema and no escape hatch, a non-object root, and a duplicate name.
      4. Every issue carries `action` and `fix` as non-empty string FIELDS — assert on
         `err.issues[i].action` and `.fix` directly, not by substring-matching the message. Then
         ALSO assert the composed `err.message` matches two independent regexes: one for the
         offending action's name and one for the fix text. Two expectations, not one: that the
         problem is DETECTED and that it is ACTIONABLE are distinct claims
         (`single-instance.test.ts:154-166`).
      5. `err instanceof CatalogValidationError` and `err.name === "CatalogValidationError"` — a
         subclass that forgets to set `name` reports as a bare `Error` in every log.

    *CAT-05 and SEC-05 — report without blocking:*
      6. An action with `effects: { destructive: true }` and no `consent` BUILDS — assert the
         catalog is returned and `entries` has the expected length — and
         `catalog.diagnostics` contains exactly one entry with code
         `destructive_without_consent` naming that action, with a non-empty `fix`.
      7. The same for `readsUntrusted: true` under `reads_untrusted_without_consent`. Assert the two
         codes are DIFFERENT on a catalog containing both, so "the same way" does not become "the
         same code".
      8. An `onDiagnostic` hook supplied through options receives each diagnostic exactly once, and
         a hook that throws propagates out of `buildCatalog` — that propagation is the supported
         mechanism for an app making a diagnostic fatal in its own build, so it is asserted rather
         than tolerated.
      9. With no `onDiagnostic` supplied, `buildCatalog` still returns and `diagnostics` is still
         populated. Do NOT assert on console output: `console` is not type-visible in core and the
         host sink is optional by construction. Write that non-assertion DOWN in a comment, per
         `export-surface.test.ts:31-46`, so it is not mistaken for missing coverage.

    *SEC-01 — redaction fails closed, both branches:*
     10. An action with a NON-EMPTY emitted schema and no `redact` throws a `redaction_missing`
         issue naming the action.
     11. An action with an EMPTY emitted schema and no `redact` builds, and
         `entries[i].action.redact === "drop"`. Cover both emptiness shapes measured in 03-02 — zod
         emits `properties: {}` and arktype emits no `properties` key at all.
     12. A declared `"passthrough"` survives untouched — the default is fail-closed, not a
         rewrite of what the author chose.

    *SEC-03 — the built catalog cannot be tampered with:*
     13. `Object.isFrozen(catalog)`, `Object.isFrozen(catalog.entries)`,
         `Object.isFrozen(catalog.entries[0])` and `Object.isFrozen(catalog.entries[0].action)` are
         all `true`. Comment that the first of these alone passes on the breached shallow form.
     14. THE REAL ONE: attempt `catalog.entries[0].action.handler = attacker`, then assert the
         handler is STILL the original function. Assert both that the write throws in strict mode
         AND that the value is unchanged — the assignment was measured to be silent in some modes,
         so the value assertion is the load-bearing half.
     15. Attempt `catalog.byName["applyFilter"] = evilEntry` and assert the lookup still resolves to
         the original entry. This is the case a `Map`-based lookup would fail, because
         `Object.freeze` does not stop `.set()`.
     16. `Object.getPrototypeOf(catalog.byName) === null` — the lookup has no prototype chain, so
         `catalog.byName["__proto__"]` and `["constructor"]` are ordinary absent keys.
     17. A nested object is frozen too: `catalog.entries[0].action.effects` cannot be mutated.
     18. The validator instance is NOT frozen — `Object.isFrozen(entries[0].action.schema)` is
         `false` — and the schema still validates and still re-emits afterwards. This pins the
         deliberate `skip` in `deepFreeze`: SEC-03 names the handler, not the validator, and
         freezing a third-party library's internals is untested and not obviously safe.

    Use `expect(...).toThrow(CatalogValidationError)` plus a captured-error assertion for the
    aggregation cases; a bare `toThrow(/regex/)` cannot reach `.issues`.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm test catalog</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build && pnpm test catalog` exits 0. Use the BARE form: `pnpm test -- catalog` does NOT
      filter — vitest's cac CLI discards everything after `--` and the whole suite runs. Reproduced
      six times across Phases 2 and 3.
    - `pnpm test` (full) exits 0 and now reports 5 test files. Record the files/tests numbers.
    - At least 18 `it(` cases are present.
    - `grep -v '^[[:space:]]*//' packages/concierge/test/catalog.test.ts | grep -c '\.\./src/'`
      returns 0 — `../src/` appears only inside comments, as in the three sibling suites.
    - The suite asserts on `err.issues[i].action` and `err.issues[i].fix` as fields in at least one
      case, in ADDITION to two independent `message` regexes.
    - The SEC-03 tamper case asserts the handler VALUE is unchanged, not only that the write threw.
    - `Object.getPrototypeOf(catalog.byName) === null` is asserted.
    - The deliberate non-assertion about console output is written down as a comment.
  </acceptance_criteria>
  <done>Every behavioural claim the catalog makes has a test that would fail if the claim stopped
  being true, including the two that pass naive tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add the SC-5 case to test/single-instance.test.ts</name>
  <files>packages/concierge/test/single-instance.test.ts</files>
  <read_first>
    - `packages/concierge/test/single-instance.test.ts` in full — `:1-27` (the header paragraph that
      must change), `:44-53` (the hard-coded key and registry view), `:68-82` (the reset), and
      `:154-166` (the `it` shape to copy).
    - `packages/concierge/src/catalog.ts` — to confirm `assertSingleInstance()` is the first
      statement of `buildCatalog`.
    - `.planning/phases/02-packaging-build-and-release/02-VERIFICATION.md` finding W5 — the origin
      of ROADMAP Phase 3 SC-5.
  </read_first>
  <action>
    **(a) Extend the header.** `:1-27` currently frames the file around a guard with no production
    call site. Phase 3 supplies one, so add a paragraph: a `buildCatalog` that stops calling
    `assertSingleInstance` is invisible to F1a, F1b and F2, because all three call the guard
    directly. Cite ROADMAP Phase 3 SC-5 and `02-VERIFICATION.md` finding W5 — Phase 2 shipped the
    guard with every invocation being a test, a harness, or CI.

    **(b) Add one `it` case.** Title it in the file's convention with a fresh finding ID (F4 or the
    next unused), stating the full proposition: `buildCatalog` records this copy in the registry on
    its first line. Body:
      - after the existing `beforeEach` reset, dynamic-import the artifact with a fresh query string
        (the established cache-busting idiom in this file);
      - assert `registry[KEY]` is `undefined` IMMEDIATELY after the import. This half is a check in
        its own right: it proves there is no module-scope call, which 02-06 measured would be
        deleted from every bundled consumer under `sideEffects: false` and would therefore test
        green here while doing nothing in a real app;
      - call `buildCatalog([])`;
      - assert `registry[KEY]` now equals `{ version: CONTRACT_VERSION }`, reading
        `CONTRACT_VERSION` from the same import.

    **Use the existing observable. Do not introduce a spy.** There is no `vi.` anywhere in `test/`;
    `single-instance.test.ts:122` already asserts on the global registry record, and that is the
    established mechanism. Introducing `vi.spyOn` here would add a technique the repository does not
    use, to observe something an existing observable already reports.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm test single-instance</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build && pnpm test single-instance` exits 0 with one more test than before. Record the
      before/after counts.
    - `grep -c 'vi\.' packages/concierge/test/single-instance.test.ts` returns 0.
    - The new case asserts `registry[KEY]` is `undefined` before the `buildCatalog` call and equal
      to `{ version: CONTRACT_VERSION }` after it.
    - `grep -c 'buildCatalog' packages/concierge/test/single-instance.test.ts` returns at least 2 —
      the header paragraph and the case.
    - The three existing cases (F1a, F1b, F2) are unmodified. Confirm with
      `git diff -U0 -- packages/concierge/test/single-instance.test.ts` and read every changed line.
  </acceptance_criteria>
  <done>ROADMAP Phase 3 SC-5 is closed: the guard has a production call site and a test that fails
  when the call is removed.</done>
</task>

<task type="auto">
  <name>Task 3: Prove mutants M-03-7, M-03-8, M-03-9 and the two new catalog mutants</name>
  <files>packages/concierge/test/catalog.test.ts, packages/concierge/test/single-instance.test.ts</files>
  <read_first>
    - `scripts/mutate-and-prove.sh` in full — the exit-code contract (0 PASS, 1 FAIL/escaped,
      2 ABORT unusable target, 3 ABORT no-op, 4 ABORT not restored), the literal (non-regex,
      non-global) substitution, and the `trap` that restores only `$TARGET`.
    - `03-03-SUMMARY.md` — the exact one-occurrence literals recorded there for the `deepFreeze`
      call site and the aggregate throw.
    - `03-RESEARCH.md` *Pitfall 8* (the filter trap) and *Pitfall 9* (`tsc` exits 1, not 2).
  </read_first>
  <action>
    Run five mutants. Each gate must (a) rebuild, because `test/` reads `dist/` and the harness
    mutates `src/`, and (b) use `pnpm --config.verify-deps-before-run=false`, because the harness's
    `trap` restores only `$TARGET` and would print "tree clean" over a rewritten lockfile. The gate
    form is `bash -c 'pnpm --config.verify-deps-before-run=false build && pnpm --config.verify-deps-before-run=false test <fragment>'`.

    **`dist/` is gitignored, so the harness's "tree clean" report says nothing about it. After EVERY
    mutant in this task, run `pnpm build` again to restore the artifact from the restored source,
    or the next test run reads a mutated bundle.** Verify with a full `pnpm test` at the end.

      - **M-03-7** — in `src/catalog.ts`, replace the single `deepFreeze(...)` call site with a
        shallow `Object.freeze(...)` of the same value. Gate: `pnpm test catalog`. Expected: the
        nested-tamper case (handler replacement) fails. Use the one-occurrence literal recorded in
        `03-03-SUMMARY.md`.
      - **M-03-8** — in `src/catalog.ts`, remove `assertSingleInstance();`. Gate:
        `pnpm test single-instance`. Expected: the SC-5 case fails.
      - **M-03-9** — in `src/catalog.ts`, make the aggregation throw on the first issue instead of
        after the loop. The cleanest one-occurrence mutation is to change the aggregate throw's
        guard so it fires with one issue mid-loop; whichever literal is chosen, record it. Gate:
        `pnpm test catalog`. Expected: the four-issues case fails.
      - **M-03-11 (new)** — in `src/catalog.ts`, replace `Object.create(null)` with `{}`. Gate:
        `pnpm test catalog`. Expected: the `getPrototypeOf(byName) === null` case fails. This mutant
        exists because the null prototype is a deliberate divergence from the ROADMAP Phase 6 note
        ("a `Map`, not an object literal") and a future reader will be tempted to "simplify" it.
      - **M-03-12 (new)** — in `src/catalog.ts`, delete the `redaction_missing` issue push. Gate:
        `pnpm test catalog`. Expected: SEC-01's non-empty-schema case fails. Without this mutant the
        SEC-01 rule is asserted only by a test that would also pass if the rule silently defaulted
        everything to `"drop"`.

    If any mutant reports FAIL (exit 1, escaped), the test is the defect, not the mutant — add the
    missing assertion and re-run. If any reports ABORT 3 (no-op), the pattern did not match exactly
    once; choose a different unique literal and record it. Record every observed gate exit code:
    expectations must say **1**, not the stale 2 at `scripts/mutate-and-prove.sh:32`.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm typecheck &amp;&amp; pnpm test &amp;&amp; git status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - All five mutants report `PASS: gate fired` and the harness exits 0 for each.
    - After each mutant, `git diff --exit-code -- packages/concierge/src/catalog.ts` exits 0.
    - After the final mutant, `pnpm build && pnpm typecheck && pnpm test` all exit 0 — proving
      `dist/` was restored from the restored source rather than left mutated.
    - `git status --porcelain` is empty except for this plan's two intended files.
    - The observed gate exit code for every mutant is recorded in the summary.
  </acceptance_criteria>
  <done>Five rules in the catalog each have a mutant proving the rule fires, and the artifact is
  provably rebuilt from clean source afterwards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party page script → built catalog | The tamper cases in this suite ARE the SEC-03 threat, exercised. |
| mutation harness → repository working tree | The harness mutates tracked source and rebuilds a gitignored artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-27 | Tampering | a SEC-03 test that only asserts `Object.isFrozen(catalog)` | mitigate | The suite asserts the tamper ATTEMPT fails and the handler VALUE is unchanged. `Object.isFrozen(catalog)` alone was measured to return `true` on the breached shallow form, and the assignment is silent in some modes — so the value assertion, not the throw assertion, is load-bearing. M-03-7 proves it. |
| T-03-28 | Tampering | `byName` swapped through the lookup | mitigate | An explicit replace-through-`byName` case plus a null-prototype assertion, both mutant-proved by M-03-11. |
| T-03-29 | Repudiation | a diagnostic that only reaches the console | mitigate | Every diagnostic assertion reads `catalog.diagnostics` or the `onDiagnostic` hook. The console path is deliberately NOT asserted, and that non-assertion is written down as a comment so it is not mistaken for missing coverage. |
| T-03-30 | Information Disclosure | redaction silently defaulting where it should fail | mitigate | Both SEC-01 branches are tested and M-03-12 proves the failing branch is a real rule rather than a coincidence of the default. |
| T-03-31 | Spoofing | a `buildCatalog` that stops arming PKG-04 | mitigate | The SC-5 case asserts the registry is EMPTY after import and POPULATED after the call, so both a removed call and a smuggled module-scope call are caught. M-03-8 proves it. |
| T-03-32 | Tampering | a mutated `dist/` surviving a mutant run | mitigate | `dist/` is gitignored, so `git status --porcelain` and the harness's own "tree clean" report are both blind to it. Every mutant in this plan is followed by an explicit `pnpm build`, and the task's final gate is a full `pnpm build && pnpm typecheck && pnpm test`. |
| T-03-33 | Denial of Service | a lockfile rewritten under a mutant gate | mitigate | Every gate uses `pnpm --config.verify-deps-before-run=false`. `CI=true` and `--frozen-lockfile` are actively wrong — they produce a vacuously-green PASS. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing; the gates are explicitly configured not to trigger an install. |
</threat_model>

<verification>
- `pnpm build && pnpm typecheck && pnpm test` all exit 0 at the end of the plan.
- `pnpm test catalog` and `pnpm test single-instance` each exit 0 in the bare form.
- All five mutants PASS, and the source and artifact are provably restored.
- `git status --porcelain` shows only this plan's two files.
</verification>

<success_criteria>
- CAT-01's runtime derivation, CAT-02's aggregation, CAT-05, SEC-05 and DX-03 all have behavioural
  tests reading structured fields rather than substrings.
- SEC-01 is tested in both branches and the failing branch is mutant-proved.
- SEC-03 is tested against the actual tamper, not against `Object.isFrozen`.
- ROADMAP Phase 3 SC-5 is closed with the suite's own established observable and no new tooling.
</success_criteria>

<output>
Create `.planning/phases/03-action-declaration-and-build-time-validation/03-06-SUMMARY.md` when done.
Record: `pnpm test` files/tests before and after; the exact one-occurrence literal used for each of
the five mutants; every observed gate exit code; and confirmation that `pnpm build` was re-run after
each mutant, with the final full-suite result as the evidence.
</output>
