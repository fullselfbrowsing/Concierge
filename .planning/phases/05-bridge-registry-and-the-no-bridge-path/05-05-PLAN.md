---
phase: 05-bridge-registry-and-the-no-bridge-path
plan: 05
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - packages/concierge/test/bridge-snapshot.test.ts
autonomous: true
requirements: [BRG-03, BRG-05, DX-02]
user_setup: []

must_haves:
  truths:
    - "A snapshot captured from a proxy-backed store does not move when the store moves, and the store is not frozen as a side effect."
    - "Cycles and shared references survive the clone: c.self === c, c.l === c.r, c.l !== the original."
    - "A snapshot getter that throws is caught, that key becomes undefined, and nothing the getter threw reaches the warning."
    - "A stage whose bridge is declared but unmounted resolves to null; a handler given null returns an honest off-page sentence rather than throwing."
    - "A stage that declares no bridge at all still runs, so the first useful action costs no instrumentation."
  artifacts:
    - path: "packages/concierge/test/bridge-snapshot.test.ts"
      provides: "BRG-05, BRG-03 and DX-02 asserted against the built artifact, with the Shape F fixture inline"
      contains: "describe(\"BRG-05"
      min_lines: 300
  key_links:
    - from: "packages/concierge/test/bridge-snapshot.test.ts"
      to: "packages/concierge/dist/index.js"
      via: "dynamic import in beforeAll behind an existsSync guard"
      pattern: "await import\\(DIST_URL\\.href\\)"
    - from: "the criterion-4 case"
      to: "the Shape F accessor-backed proxy"
      via: "inline factory, never test/fixtures/"
      pattern: "Object\\.defineProperty"
---

<objective>
Create `packages/concierge/test/bridge-snapshot.test.ts` — the detachment and no-bridge half of the
phase's runtime proof: BRG-05 (criterion 4 against a hand-rolled `Proxy` in core, before any adapter
exists), BRG-03 (resolution yields `null`; a handler given `null` returns a sentence), and DX-02 (an
action runs with no bridge registered at all).

Purpose: BRG-05 is a **security** requirement, not a hygiene one — it is what makes Phase 8's CON-04
drift check meaningful. A captured snapshot that is still a live proxy view makes that check compare a
value against itself and pass unconditionally, with the consent gate open and every naive test green.

**Companion file.** Plan 05-04 owns `test/bridge.test.ts` (the registry: BRG-01, BRG-02, BRG-04). This
file owns capture, detachment and the no-bridge path. Disjoint concerns, same wave, both matched by
the `pnpm test -- bridge` filter.

Output: one new test file. The whole suite must be green at the end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-CONTEXT.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-PATTERNS.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-VALIDATION.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-01-SUMMARY.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-02-SUMMARY.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-03-SUMMARY.md

<interfaces>
Reachable from `../dist/index.js` after plan 05-03:
  `createBridge(id)`, `captureSnapshot(bridge, id, normalize?)`, `offPageResult(what, where)`,
  `createConcierge(config)`.
`resolveBridge` is module-private in `src/concierge.ts` (plan 05-02). Its only observable at this
layer is `explain().stages[i].bridge.registered`, which is exactly why plan 05-02 routed
`bridgeStatus` through it.
</interfaces>

**CASE ID PREFIX: `D`** (detachment). Prefixes already in use across `test/` are `C`, `F`, `S`, and
`B` (plan 05-04's new file). Use `D1`, `D2`, … sequentially.

**SHAPE F IS NOT A FREE CHOICE.** Measured across five hand-rolled proxy shapes, only one makes the
deep-freeze mutant fail *visibly and without throwing*:

| Shape | Under the mutant (`Object.freeze` in place) | Verdict for this test |
|---|---|---|
| A — forwarding proxy, target holds the values | appears detached, but the app's own store is now frozen solid | **mutant PASSES while destroying the app** — worst outcome |
| B — signal-backed, target is a husk | throws `TypeError: 'ownKeys' on proxy: trap returned extra keys but proxy target is non-extensible` | mutant "caught" for the wrong reason — proves the proxy is malformed |
| E — read-only view proxy | throws `TypeError: 'preventExtensions' on proxy…` | same |
| **F — accessor-backed target, all traps forwarding honestly to `Reflect`** | **reads `"boots"` where the test asserted `"shoes"`; no throw, no collateral** | **the only correct fixture** |
| C — plain object carrying a getter, no proxy | accessor skipped, stays live | correct but weaker; no proxy involved |

Re-measured live this session against both implementations:
```
CORRECT:        capturedBefore=shoes  capturedAfter=shoes  proxyAfter=boots  isFrozen(proxy)=false
M-05-3 MUTANT:  capturedBefore=shoes  capturedAfter=boots  proxyAfter=boots  isFrozen(proxy)=true
```
Two of the three criterion-4 assertions go red under the mutant, with no throw.
</context>

<tasks>

<task type="auto">
  <name>Task 1 (05-05-T1): Scaffold, the Shape F fixture, and criterion 4</name>

  <read_first>
    - `packages/concierge/test/concierge.test.ts` lines 1-130 (header convention) and lines 300-360 (imports, `DIST_URL`, the artifact guard, the contract-registry reset).
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-PATTERNS.md` lines 649-760 and 871-876 — the runtime test-file conventions and the requirement that the Shape F factory live inline rather than in `test/fixtures/`.
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-RESEARCH.md` lines 527-582 (the freeze-vs-clone matrix and the Shape F fixture, reproduced verbatim from the measured script) and lines 1057-1078 (the criterion-4 test with its three assertions).
  </read_first>

  <files>packages/concierge/test/bridge-snapshot.test.ts</files>

  <action>
Create `packages/concierge/test/bridge-snapshot.test.ts` with the header, imports, guards, the Shape F
factory, and the criterion-4 case.

HEADER — "What escapes without this file", numbered, with measured evidence. It must name:
  1. **The deep-freeze default.** `types.ts` shipped the claim that the default normalizer is a
     freeze; freezing a `Proxy` does not detach it, so the stored snapshot stays a live view and
     Phase 8's CON-04 drift check compares a value against itself and passes unconditionally. This is
     the "failing while appearing to work" shape, and it is why BRG-05 is a security requirement.
  2. **The fixture trap.** Three of five hand-rolled proxy shapes give a misleading result under the
     mutant — two make it throw (so the mutation "passes" having proved only that the proxy is
     malformed), and one makes it pass while permanently freezing the host app's store. Reproduce the
     shape table from this plan's `<context>`, and state that Shape F is used for that reason and must
     not be "simplified" to a signal-backed proxy, which is the intuitive way to hand-roll a reactive
     one.
  3. **The narrow `try`.** A `try` scoped to `snapshot[key]()` alone does not catch a getter nested
     inside the returned value — measured, that one throws from inside the normalizer — and the
     escaping `Error`'s message echoes user input.
  4. **The `Object.create(null)` arm.** Dropping `|| proto === null` from the plain-object test
     silently passes through a null-prototype record undetached, which is exactly the shape
     `catalog.byName` uses.
  5. **The proxied `Date`.** A naively proxied `Date` is unextractable by every route measured — six,
     all `TypeError` — so a clone branch without a `catch` crashes the capture path.

IMPORTS AND GUARDS — same shape as plan 05-04's file and `concierge.test.ts:308-352`: `existsSync`,
`fileURLToPath`, the `vitest` imports, `DIST_URL`, `DIST_PATH`, the `beforeAll` existence guard with
its actionable `pnpm build` message, and the dynamic `await import(DIST_URL.href)` binding of
`createBridge`, `captureSnapshot`, `offPageResult` and `createConcierge`, left unannotated. Include
the hard-coded `const KEY = Symbol.for("@fullselfbrowsing/concierge.contract");` and the
`delete (globalThis as Record<symbol, unknown>)[KEY];` reset in `beforeEach`, with both reasons as
comments — `createBridge` and `createConcierge` both reach `assertSingleInstance`.

THE SHAPE F FACTORY — **inline in this file, never in `test/fixtures/`.**
`scripts/pack-install-check.sh` copies that directory into a foreign scratch project and a sibling
module gets pulled into that program by accident; `test/fixtures/probe.ts`'s own header states it "is
never compiled by this repository". Build it exactly as measured:
  - a backing record holding `q: "shoes"` and `page: 1`;
  - a separate target object whose properties are defined with `Object.defineProperty` as
    accessor pairs — a `get` returning the backing value and a `set` writing it — both `enumerable`
    and `configurable`;
  - a `Proxy` over that target whose `get`, `set`, `ownKeys`, `getOwnPropertyDescriptor`,
    `defineProperty` and `preventExtensions` traps all forward honestly to the corresponding `Reflect`
    method;
  - returning both the proxy and the backing record, so the test writes through the backing record and
    asserts the captured snapshot did not move.
Add a comment above it stating that the accessor-backed target and the honest traps are the two
properties that make the mutant fail correctly, and that a signal-backed variant makes it throw
instead.

THE CRITERION-4 CASE — under
`describe("BRG-05 — a snapshot captured from a proxy-backed store does not move when the store moves", …)`:
  - Build the Shape F store, wrap it in a bridge whose `snapshot` has one getter returning the proxy,
    register it on a real `createBridge("results")` registry, and capture with
    `captureSnapshot(registry.read(), "results")` — no normalizer argument, so the default clone runs.
  - Assert the captured value reads `"shoes"` **before** the store moves. Without this the case could
    pass vacuously on a capture that returned `undefined`.
  - Write `"boots"` through the backing record.
  - Assert three things, and comment what each one alone would miss:
    1. the captured value still reads `"shoes"` — detachment;
    2. the live proxy now reads `"boots"` — the store genuinely moved, so assertion 1 is not passing
       because nothing happened;
    3. `Object.isFrozen(proxy)` is `false` — we detached without breaking the app. This third
       assertion is the **only** detector for the freeze-the-app failure mode; under Shape A plus a
       freeze, assertions 1 and 2 both pass.
  - Comment that this case is mutant **M-05-3**'s detector: replacing `cloneDetached(value, seen) as T`
    with `Object.freeze(value) as T` makes assertions 1 and 3 red, with no throw — re-measured this
    session.

This case is success criterion 4 in full, and CONTEXT requires it to be demonstrated against a
hand-rolled `Proxy` in core before any framework adapter exists. Do not import any framework.
  </action>

  <verify>
    <automated>pnpm build</automated>
    <automated>pnpm test -- bridge</automated>
    <automated>pnpm test</automated>
    <automated>bash -c 'f=packages/concierge/test/bridge-snapshot.test.ts; grep -qF "Object.defineProperty" $f &amp;&amp; grep -qF "Reflect.preventExtensions" $f &amp;&amp; grep -qF "Object.isFrozen" $f &amp;&amp; grep -qF "existsSync(DIST_PATH)" $f &amp;&amp; echo "shape F + guards: OK"'</automated>
    <automated>bash -c 'git status --porcelain packages/concierge/test/fixtures/ | wc -l | tr -d " " | grep -qx 0 &amp;&amp; echo "fixtures dir untouched: OK"'</automated>
  </verify>

  <done>
The file exists, guards the built artifact, resets the contract registry, carries the Shape F factory
inline, and asserts success criterion 4 with all three assertions. The whole suite is green.
  </done>

  <acceptance_criteria>
    - `pnpm test` passes the whole suite; `pnpm test -- bridge` runs both bridge files.
    - The Shape F factory is inline in this file and uses `Object.defineProperty` accessor pairs over a separate backing record, with all six traps forwarding to `Reflect`. Assert the file contains `Reflect.get`, `Reflect.set`, `Reflect.ownKeys`, `Reflect.getOwnPropertyDescriptor`, `Reflect.defineProperty` and `Reflect.preventExtensions`.
    - `git status --porcelain packages/concierge/test/fixtures/` is empty — nothing was added to that directory.
    - The criterion-4 case makes exactly four assertions in this order: captured reads `"shoes"` before the move; captured still reads `"shoes"` after; the proxy reads `"boots"`; `Object.isFrozen(proxy)` is `false`.
    - The case calls `captureSnapshot` with no normalizer argument, so the default clone is what is under test.
    - The header reproduces the five-shape table and states why Shape F is mandatory, so a later reader cannot "simplify" it to a signal-backed proxy (Pitfall 2).
    - No framework package is imported: `grep -cE "from \"(react|vue|svelte)" ` returns 0.
    - The contract-registry reset is present and uses `delete`, with the key hard-coded as a string-identity `Symbol.for`.
    - Behavioural discrimination is proven in plan 05-07: mutant **M-05-3** must make `pnpm test -- bridge` exit non-zero, with the gate output confirming it compiled and ran tests and containing no `TypeError` — a PASS whose output names `TypeError` means the fixture is wrong, not the normalizer.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 (05-05-T2): The clone's measured properties and the two capture warns</name>

  <read_first>
    - `packages/concierge/test/bridge-snapshot.test.ts` — the file Task 1 wrote.
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-RESEARCH.md` lines 584-676 — cycle safety and DAG identity, plain-object detection with the `Object.create(null)` row, array detection, the `Date`/`Map`/`Set` detection matrix, the six measured proxied-`Date` throw routes, the cosmetic-freeze finding, and symbol keys.
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-RESEARCH.md` lines 441-464 — Pattern 2, where the `try` goes and the measured nested-getter leak.
    - `packages/concierge/test/concierge.test.ts` lines 1050-1165 — the console-capture idiom and the warn-once assertion triad.
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-01-SUMMARY.md` — the exact rendered text of the `snapshot_threw` and `snapshot_exotic` warn messages.
  </read_first>

  <files>packages/concierge/test/bridge-snapshot.test.ts</files>

  <action>
Add cases under the existing BRG-05 `describe`, plus one new `describe` for the capture warns. Every
expectation below was measured; do not re-derive them.

CLONE STRUCTURE:
  - **Cycles.** A snapshot value with `cyc.self = cyc` captures without hanging, and the result
    satisfies `captured.self === captured` (`toBe`). Mutant **M-05-4**: moving `seen.set` after the
    recursion makes this recurse forever.
  - **DAG identity.** A value `{ l: shared, r: shared }` where both fields hold the same object
    captures to a result where `captured.l === captured.r` (`toBe`) **and** `captured.l !== shared`
    (`not.toBe`). Both halves are required: the first alone passes on a normalizer that returned the
    original, the second alone passes on one that cloned twice. Mutant **M-05-4**.
  - **Arrays.** An array of plain objects clones to a distinct array whose elements are distinct
    objects, and the result is frozen.
  - **Plain objects with a live getter.** A plain object carrying an accessor captures to a result
    holding a DATA property, not an accessor — assert via `Object.getOwnPropertyDescriptor(...).get`
    being `undefined` and `.value` being present. Invoking the getter IS the detachment, and this is
    the property that distinguishes the clone from `deepFreeze`, which skips accessors.
  - **`Object.create(null)`.** A null-prototype record inside a snapshot clones rather than passing
    through: assert the captured value is not the same object (`not.toBe`) and carries the same keys.
    Mutant **M-05-6** (drop the `|| proto === null` arm) makes this red.
  - **`Date` / `Map` / `Set`.** Each clones to a distinct instance carrying the same content
    (`not.toBe` plus a content assertion). Mutant **M-05-5** (the branch returns the value unchanged)
    makes these red. Add a comment recording the measured cosmetic-freeze finding — a frozen `Map`,
    `Set` or `Date` still accepts its own mutators — so a later reader does not add an immutability
    assertion that would fail for a reason that is not a defect.
  - **Pass-through by reference.** A class instance inside a snapshot comes back as the SAME object
    (`toBe`) and is NOT frozen (`Object.isFrozen` is `false`). The second half is the detector for the
    Pitfall-4 belt-and-braces `deepFreeze` pass, which was measured to freeze the consumer's own
    objects. Comment that this is a documented limit, not an oversight, and that freezing a value we
    did not construct is a bug.
  - **Symbol keys are not carried.** A snapshot value carrying a symbol-keyed property captures to a
    result where that symbol key is absent. Comment that this is deliberate — the three target
    frameworks use symbol keys for internal markers, which is precisely the framework reactivity
    BRG-05 exists to drop.

THE TWO CAPTURE WARNS, under `describe("capture degrades honestly rather than propagating", …)`, using
the console-capture idiom (plain global assignment, real console spread, restoration in a `finally`,
never the Vitest mocking API):
  - **A throwing snapshot getter.** A bridge whose getter throws
    `new Error("SECRET-FROM-THE-APP user@example.com")`. Assert: `captureSnapshot` does NOT throw
    (`expect(() => captureSnapshot(...)).not.toThrow()`); the key is present in the result with value
    `undefined`; exactly one warning was captured; it contains `[snapshot_threw]`; it contains both the
    registry id and the key; and it does **not** contain `SECRET-FROM-THE-APP` or
    `user@example.com`. That negative pair is the executable form of the security decision — without
    it the `catch`-with-no-binding is a convention with no guarantee.
  - **A NESTED throwing getter — the case that distinguishes the wide `try` from the narrow one.** A
    snapshot getter that returns `{ ok: 1, get boom() { throw new Error("SECRET-FROM-THE-APP") } }`.
    The outer getter does not throw; the *clone* does, while reading `boom`. Assert `captureSnapshot`
    does not throw and the secret does not appear in any captured warning. This is mutant **M-05-9**'s
    neighbour and Pitfall 3's detector: with the `try` scoped to `snapshot[key]()` alone, this case
    throws out of `captureSnapshot` and the message reaches the caller. Comment that explicitly.
  - **The exotic-clone fallback warns with a distinct code.** A snapshot value that is a naively
    proxied `Date` — measured unextractable by all six routes, every one a `TypeError`. Assert
    `captureSnapshot` does not throw; the value comes back by reference; exactly one warning was
    captured; and it contains `[snapshot_exotic]`, which is a DIFFERENT code from `[snapshot_threw]`.
    Assert the two codes are distinct rather than assuming it — CONTEXT requires "the same latch shape
    as the throwing-getter warn but a distinct code", and a single shared code would make the two
    conditions indistinguishable in the warn channel.
  - **A clean capture warns not at all.** A bridge whose getters all succeed on plain values captures
    under console capture with `toHaveLength(0)`, so the two warns are proven conditional.

Match every warn assertion against the exact rendered text recorded in `05-01-SUMMARY.md`, at minimum
asserting the bracketed code and the subject string.
  </action>

  <verify>
    <automated>pnpm build</automated>
    <automated>pnpm test -- bridge</automated>
    <automated>pnpm test</automated>
    <automated>bash -c 'f=packages/concierge/test/bridge-snapshot.test.ts; for s in "[snapshot_threw]" "[snapshot_exotic]" "SECRET-FROM-THE-APP" "Object.create(null)" "get boom()" "not.toThrow()"; do grep -qF "$s" $f || { echo "missing: $s"; exit 1; }; done; echo "clone + warn assertions: OK"'</automated>
    <automated>bash -c 'f=packages/concierge/test/bridge-snapshot.test.ts; grep -qF "globalThis.console = { ...realConsole" $f &amp;&amp; grep -qF "} finally {" $f &amp;&amp; ! grep -qE "vi\.(spyOn|fn|mock)" $f &amp;&amp; echo "console capture idiom: OK"'</automated>
  </verify>

  <done>
Every measured property of the clone has a case, both capture warns are asserted with distinct codes,
and the nested-throwing-getter case pins the wide `try`. The whole suite is green.
  </done>

  <acceptance_criteria>
    - `pnpm test` passes the whole suite.
    - Cycle safety asserts `captured.self === captured` with `toBe`.
    - DAG identity asserts BOTH `captured.l === captured.r` and `captured.l !== theOriginal`. Either alone is insufficient and the plan says why.
    - The accessor case asserts the captured property is a DATA property — `Object.getOwnPropertyDescriptor(...).get` is `undefined` and `.value` is defined.
    - `Object.create(null)` is cloned, not passed through: `not.toBe` the original, same keys. Mutant **M-05-6**.
    - `Date`, `Map` and `Set` each clone to a distinct instance with matching content. Mutant **M-05-5**.
    - The class-instance case asserts BOTH `toBe` the original AND `Object.isFrozen(...)` is `false`. The second half is Pitfall 4's detector.
    - A symbol-keyed property is asserted absent from the captured result.
    - The throwing-getter case asserts `not.toThrow()`, the key present at `undefined`, `toHaveLength(1)`, `[snapshot_threw]` present, id and key present, and `SECRET-FROM-THE-APP` **absent**.
    - The NESTED throwing-getter case exists and uses a `get boom()` accessor inside the returned value. This is Pitfall 3's only detector; without it the narrow `try` ships undetected.
    - The exotic case asserts `[snapshot_exotic]` and asserts the two codes are distinct.
    - A clean-capture case asserts `toHaveLength(0)`.
    - No Vitest mocking API; console restored in a `finally`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3 (05-05-T3): BRG-03 and DX-02 — the no-bridge path, both halves</name>

  <read_first>
    - `packages/concierge/test/bridge-snapshot.test.ts` — the file as Tasks 1 and 2 left it.
    - `packages/concierge/test/concierge.test.ts` lines 358-414 (the `declare` and `stage` helpers, and why `redact: "drop"` is mandatory on every declaration) and lines 950-990 (S20, the existing hand-rolled bridge case whose three states must keep passing).
    - `packages/concierge/src/concierge.ts` lines 195-245 as plan 05-02 left them — `resolveBridge` and the rewritten `bridgeStatus`.
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-02-SUMMARY.md` — `resolveBridge`'s final text.
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-01-SUMMARY.md` — the exact `offPageResult` message and its length.
  </read_first>

  <files>packages/concierge/test/bridge-snapshot.test.ts</files>

  <action>
Add two `describe` blocks. Phase 5 proves BRG-03 as **two separate halves**, because CONTEXT decision
3.3 locks the `dispatch` stub as untouched — Phase 6 replaces it wholesale, so the end-to-end form is
not provable here. Do not write a test that pretends otherwise, and do not call `dispatch`.

BLOCK 1 — `describe("BRG-03 — resolution yields null for a declared-but-unmounted bridge", …)`.
`resolveBridge` is module-private, so its observable at this layer is
`explain().stages[i].bridge.registered`, which is exactly why plan 05-02 routed `bridgeStatus` through
it. Build a `createConcierge` config using the `declare` and `stage` helper shapes from
`concierge.test.ts:358-414` — `redact: "drop"` on every declaration is mandatory, because a missing
`redact` on a non-empty schema throws `redaction_missing` during `buildCatalog`; Zod fixtures come from
`./fixtures/schemas.js`. Then:
  - A stage declaring a **real** `createBridge("results")` registry with nothing registered:
    `explain(ctx).stages[i].bridge` equals `{ id: "results", registered: false }`. Comment that this
    is mutant **M-05-13**'s detector — `resolveBridge` returning `null` unconditionally leaves this
    green, so pair it with the next case, which is the one that goes red.
  - The same stage after `registry.register(bridge)`: `registered` is now `true`. **This is M-05-13's
    real detector** — with `return null;` substituted for the guarded read, `registered` stays `false`
    here.
  - A stage declaring a registry whose `read()` **throws**: `registered` is `false`, `explain()` does
    not throw, and under console capture zero warnings are emitted. `concierge.ts:210-215` records the
    reason for the silence — this runs only inside `explain`, a human-debugging-rate call, and the
    structured `registered: false` row is already in front of the person who asked for it.
  - A stage declaring **no** `bridge` at all: `explain(ctx).stages[i].bridge` is `null`, not
    `{ id: …, registered: false }`. This is mutant **M-05-14**'s detector — removing `bridgeStatus`'s
    `stage.bridge === undefined` early return collapses the three-state row to two, and there is no
    `id` to put in the collapsed row.

BLOCK 2 — `describe("BRG-03 / DX-02 — a handler given bridge: null returns a sentence, not an exception", …)`.
Invoke handlers **directly** with a context object carrying `bridge: null`; do not route through
`dispatch`.
  - A handler that calls `offPageResult` when `ctx.bridge` is `null` returns
    `{ ok: false, reason: "no_bridge", message: <string> }`. Assert `ok` is `false`, `reason` is
    exactly `"no_bridge"`, and `message` is a non-empty string.
  - `message.length` is at most 180. Read the bound from the artifact's own `MESSAGE_MAX_CHARS` export
    rather than hard-coding 180, so the assertion cannot drift from the constant. This is mutant
    **M-05-12**'s detector: with the `slice` removed, an over-long composition exceeds the bound —
    so include a case that passes a deliberately long `what`/`where` pair whose composed length
    exceeds 180 and assert the returned message is exactly 180 characters.
  - `expect(() => handler(ctx)).not.toThrow()`.
  - **DX-02, criterion 5:** a stage declaring no bridge whose handler reads a stubbed router value
    from its own arguments and returns `{ ok: true }` with `ctx.bridge` being `null`. Core does not
    auto-fail an action because a bridge is absent — the handler decides. Assert `ok` is `true` and
    that `ctx.bridge` was `null` during the call. This is the "first useful action costs no
    instrumentation" claim.
  - **DX-02, the declared-but-unmounted variant:** the same, for a stage that DOES declare a bridge
    with nothing registered. The handler still runs and can still succeed. Comment that an
    implementation which short-circuited to `no_bridge` whenever `stage.bridge` is declared but
    unmounted would make this case red — that is the auto-fail CONTEXT rejects, and it would strip
    handlers of legitimate partial-capability paths.

**THE DX-03 REVIEW OBLIGATION — record it, do not fake it.** VALIDATION.md marks "the message says
what to *do*, not merely what is wrong" as manual-only: a regex over `/open|go to|navigate/i` pins
vocabulary rather than meaning and goes red on a legitimate rewording, so writing it would be an
assertion that passes vacuously. Add a comment block in this `describe` — following
`export-surface.test.ts`'s Trap 2 precedent of writing down what is deliberately NOT asserted —
stating that the two automated halves which DO carry weight are the length bound (M-05-12) and
`not.toThrow()`, and that the what-to-do half is a plan-author review obligation whose verdict is
recorded in the SUMMARY.

**The plan author's verdict, to be restated in the SUMMARY:** the shipped sentence renders as
`The result count is not available because the results page is not open. Open the results page and try again.`
It names the concrete next action ("Open the results page") in addition to the fault, so it meets the
DX-03 standard. PASS.

**Success criterion 3's end-to-end form is DEFERRED to Phase 6** by CONTEXT decision 3.3. State that in
a comment here so a later reader does not read its absence as an omission.
  </action>

  <verify>
    <automated>pnpm build</automated>
    <automated>pnpm test -- bridge</automated>
    <automated>pnpm test</automated>
    <automated>pnpm typecheck</automated>
    <automated>bash -c 'f=packages/concierge/test/bridge-snapshot.test.ts; for s in "no_bridge" "MESSAGE_MAX_CHARS" "not.toThrow()" "registered: false" "DX-02"; do grep -qF "$s" $f || { echo "missing: $s"; exit 1; }; done; echo "BRG-03 + DX-02 assertions: OK"'</automated>
    <automated>bash -c 'f=packages/concierge/test/bridge-snapshot.test.ts; ! grep -qE "\.dispatch\(" $f &amp;&amp; echo "dispatch fence honoured: OK"'</automated>
    <automated>bash -c 'pnpm test 2>&amp;1 | grep -E "S20|Tests +[0-9]+ passed"'</automated>
  </verify>

  <done>
Both halves of BRG-03 and both halves of DX-02 are asserted, `dispatch` is untouched and uncalled, the
DX-03 review obligation is written into the file rather than faked, and the whole suite is green.
  </done>

  <acceptance_criteria>
    - `pnpm test` passes the whole suite; `pnpm typecheck` exits 0. S20 in `concierge.test.ts` still passes.
    - The file never calls `dispatch`: `grep -cE "\.dispatch\("` returns 0. CONTEXT decision 3.3 fences it and Phase 6 replaces the stub wholesale.
    - Block 1 asserts all four `explain()` states: declared-and-unmounted (`{ id, registered: false }`), declared-and-mounted (`registered: true`), a throwing `read()` (`registered: false`, no throw, zero warnings), and not-declared (`bridge` is `null`).
    - The not-declared case asserts `null`, not `{ id: …, registered: false }`. This is mutant **M-05-14**'s detector.
    - The declared-and-mounted case is present. Without it, mutant **M-05-13** (`return null;`) escapes, because the unmounted case is green under both implementations.
    - The message-bound assertion reads `MESSAGE_MAX_CHARS` from the artifact rather than hard-coding `180`, and a deliberately over-long composition is asserted to come back at exactly that length. Mutant **M-05-12**.
    - `expect(() => handler(ctx)).not.toThrow()` is asserted for the off-page handler.
    - Both DX-02 variants exist — a stage with no `bridge` key at all, and a stage with `bridge` declared but nothing registered — and both assert the handler ran and returned `{ ok: true }` with `ctx.bridge` being `null`.
    - A comment block records the DX-03 what-to-do half as a plan-author review obligation, names the two automated halves that do carry weight, and explains why the regex form would pass vacuously.
    - A comment records that criterion 3's end-to-end form is deferred to Phase 6 by CONTEXT decision 3.3.
    - `redact: "drop"` appears on every action declaration in this file's configs.
    - `git diff --exit-code` at the repo root is clean after commit.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer snapshot getters → the capture loop | Consumer-authored code that may throw arbitrary messages echoing user input |
| consumer snapshot values → the clone walk | Proxied and exotic values measured to throw on every extraction route |
| the captured snapshot → Phase 8's CON-04 drift check | A live view here silently opens the consent gate |
| core → `warnHost` (host `console`) | Outbound diagnostic channel |
| the off-page message → the agent and telemetry | Every message leaving core is a potential PII channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Elevation of privilege | a captured snapshot that is still a live proxy view, making Phase 8's CON-04 drift check compare a value against itself and pass unconditionally | mitigate | The criterion-4 case against Shape F, with all three assertions. Mutant M-05-3, re-measured live this session: captured follows the store to `"boots"` and `isFrozen(proxy)` becomes `true` |
| T-05-06 | Denial of service (against the consumer) | freezing the host app's own reactive store, or the consumer's class instances, through a belt-and-braces `deepFreeze` pass | mitigate | `Object.isFrozen(proxy) === false` in criterion 4, and `Object.isFrozen(classInstance) === false` in the pass-through case. Pitfall 4 |
| T-05-05 | Information disclosure | a snapshot getter's thrown message — which echoes user input — reaching the caller, the model or telemetry | mitigate | The throwing-getter case and the NESTED throwing-getter case, both asserting `not.toThrow()` and `not.toContain("SECRET-FROM-THE-APP")`. The nested case is Pitfall 3's only detector |
| T-05-07 | Tampering | a snapshot key silently becoming `undefined`, which a downstream consent comparison reads as "unchanged" | mitigate | `[snapshot_threw]` asserted present, naming registry id and key; `[snapshot_exotic]` asserted present with a distinct code; a clean capture asserted to warn zero times |
| T-05-16 | Denial of service | a proxied `Date` crashing the capture path — six extraction routes measured, all `TypeError` | mitigate | The exotic case asserts `captureSnapshot` does not throw and the value comes back by reference. Pitfall 5 |
| T-05-09 | Repudiation | `explain()`'s bridge row collapsing "not declared" into "declared but unmounted", so a developer cannot tell which | mitigate | Block 1's four-state assertion set. Mutant M-05-14 |
| T-05-17 | Denial of service | core auto-failing an action because a declared bridge is unmounted, stripping handlers of legitimate partial-capability paths | mitigate | Both DX-02 variants assert the handler still runs and can return `{ ok: true }` |
| T-05-SC | Tampering | npm/pnpm package installs | accept | Zero installs. `pnpm check:deps` asserts the empty external module graph |
</threat_model>

<verification>
1. `pnpm build && pnpm test && pnpm typecheck` — all green.
2. Shape F is inline; `git status --porcelain packages/concierge/test/fixtures/` is empty.
3. No framework import, no `dispatch` call, no Vitest mocking API.
4. S20 in `concierge.test.ts` still passes — the existing three-state detector is unbroken.
5. `git diff --exit-code` clean at the repo root after commit.
</verification>

<success_criteria>
- Criterion 4 is proven against a hand-rolled `Proxy` in core, with the third assertion separating
  "detached" from "detached by breaking the app".
- Every measured property of the clone has a case, and each names the mutant that makes it red.
- BRG-03 is proven as two halves, with the end-to-end form explicitly deferred rather than faked.
- DX-02 is proven in both variants: no bridge declared, and a bridge declared but unmounted.
- The DX-03 what-to-do standard is recorded as a review obligation with a verdict, not as a vacuous
  regex.
</success_criteria>

<output>
Create `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-05-SUMMARY.md` when done.

The SUMMARY must record:
- The DX-03 review verdict on the shipped off-page sentence, quoted in full, with the reviewer's
  judgement that it names a concrete next action.
- The case-id-to-claim map, so plan 05-07 can name which cases each mutant turned red.
- The new total test count for the suite.
- Confirmation that criterion 4's three assertions were observed to be individually load-bearing —
  in particular that `Object.isFrozen(proxy) === false` is asserted and would be the only detector
  under a Shape A fixture.
</output>
