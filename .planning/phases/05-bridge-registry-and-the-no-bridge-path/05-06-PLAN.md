---
phase: 05-bridge-registry-and-the-no-bridge-path
plan: 06
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - packages/concierge/test-d/bridge.test-d.ts
  - packages/concierge/test/single-instance.test.ts
autonomous: true
requirements: [BRG-01, BRG-03, BRG-05]
user_setup: []

must_haves:
  truths:
    - "createBridge's signature is pinned: it takes one string and returns BridgeRegistry<B> at the requested B."
    - "offPageResult's signature is pinned as (what: string, where: string) => ActionResult."
    - "createBridge records this copy of core in the process-wide contract registry, so PKG-04's third production call site is asserted."
  artifacts:
    - path: "packages/concierge/test-d/bridge.test-d.ts"
      provides: "compile-time signature pins for the three new values, invisible to the runtime suite"
      contains: "from \"../src/index.js\""
    - path: "packages/concierge/test/single-instance.test.ts"
      provides: "F6 — the third production assertSingleInstance call site, and the first direct one"
      contains: "F6"
  key_links:
    - from: "packages/concierge/test-d/bridge.test-d.ts"
      to: "packages/concierge/src/index.ts"
      via: "value import from the BARREL, never from ../src/bridge.js"
      pattern: "from \"\\.\\./src/index\\.js\""
    - from: "packages/concierge/test/single-instance.test.ts F6"
      to: "packages/concierge/dist/index.js"
      via: "a fresh module evaluation under a unique query string"
      pattern: "\\?sc7=1"
---

<objective>
Two files, both closing gaps that the runtime behavioural suites structurally cannot see.

1. `packages/concierge/test-d/bridge.test-d.ts` (NEW) — the type-level half. `createBridge`'s
   signature, `captureSnapshot`'s return type, and `offPageResult`'s exact shape, spelled as
   predicates that go red when a signature silently widens.

2. `packages/concierge/test/single-instance.test.ts` (EDIT) — **`05-PATTERNS.md` § "Files CONTEXT and
   RESEARCH Missed" flags this file as absent from both upstream artifacts' lists, and it is the only
   home for mutant M-05-8.** RESEARCH's register lists M-05-8 (`assertSingleInstance();` deleted from
   `createBridge`) but assigns it to no file. Nothing in `test/bridge.test.ts` can catch it —
   `createBridge` behaves identically with the call removed. This file already carries the convention
   "one case per production `assertSingleInstance` call site": F4 pins `buildCatalog`'s, F5 pins
   `createConcierge`'s. `createBridge` is the third, and the first **direct** one.

Output: one new type-test file, one edited runtime file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-CONTEXT.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-PATTERNS.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-01-SUMMARY.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-03-SUMMARY.md

<interfaces>
The four assertion aliases, from `packages/concierge/test-d/_assert.ts:18-38` — the entire mechanism:
  `Expect<T extends true> = T`
  `Equals<A, B>` via the deferred-conditional identity formulation. Must not be "simplified" to the naive bidirectional form.
  `Assignable<From, To> = [From] extends [To] ? true : false`
  `Not<T extends boolean>`

Signatures to pin (from plan 05-01):
  `createBridge<B extends Bridge = Bridge>(id: string): BridgeRegistry<B>`
  `captureSnapshot<B extends Bridge>(bridge: B, id: string, normalize?: SnapshotNormalizer): Record<string, unknown>`
  `offPageResult(what: string, where: string): ActionResult`

Existing query strings in `test/single-instance.test.ts`, measured this session: `?dup=1`,
`?mismatch=1`, `?sc5=1`, `?sc6=1`. **F6 must use `?sc7=1`** — a fresh one nothing else uses, because
two cases sharing a specifier share Node's cached namespace and the second would skip module scope
entirely.
</interfaces>

**Fixture types in `test-d/` are file-local and are NOT exported** — verified this session:
`actions.test-d.ts:416,419` declares `ResultsBridge` and `CartBridge` with no `export`, and
`concierge.test-d.ts` imports nothing from it. So "reuse the fixtures" means reuse their **shape**,
declared locally in the new file. What must NOT be duplicated is the assertion
`_registryReadIsNullable` (`actions.test-d.ts:436`), which already discriminates mutant P-05-3.
</context>

<tasks>

<task type="auto">
  <name>Task 1 (05-06-T1): test-d/bridge.test-d.ts — the signature pins</name>

  <read_first>
    - `packages/concierge/test-d/_assert.ts` in full — 38 lines, the four aliases and the header rules.
    - `packages/concierge/test-d/concierge.test-d.ts` lines 1-160 — the header ("WHAT ESCAPES WITHOUT THIS FILE", the terse-output caveat, the house rules), the barrel-vs-module import split at 98-115, and the `_createConciergeSignature` pin at 150-151 which is the form to copy.
    - `packages/concierge/test-d/actions.test-d.ts` lines 410-450 — the bridge fixture shapes and `_registryReadIsNullable`, which must NOT be duplicated.
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-PATTERNS.md` lines 879-1010 — the `test-d/bridge.test-d.ts` section.
  </read_first>

  <files>packages/concierge/test-d/bridge.test-d.ts</files>

  <action>
Create `packages/concierge/test-d/bridge.test-d.ts`.

HEADER — copy `concierge.test-d.ts:1-96`'s structure, adapted. It must carry:
  - "WHAT ESCAPES WITHOUT THIS FILE": everything pinned here is invisible to `test/`, because the
    runtime suites import `../dist/index.js` dynamically and get untyped bindings. A `createBridge`
    that silently gained a `const` type parameter, or an `offPageResult` whose parameters widened to
    `unknown`, passes every runtime assertion in the phase.
  - THE TERSE-OUTPUT CAVEAT: measured non-TTY, which is what CI sees, a failing `Expect<…>` prints
    exactly `Type 'false' does not satisfy the constraint 'true'.` and **no alias name**. A mutant
    against this file must therefore be asserted on its **exit code** (`tsc` exits 1, not 2, under
    TypeScript 7.0.2) plus `file:line` — never by grepping the output for a predicate's name, which
    will never match, and a grep that never matches reads as a passing check.
  - HOUSE RULES INHERITED: nothing below is exported, because `isolatedDeclarations` demands an
    explicit annotation on anything reaching the declaration surface and non-exported locals are
    exempt. Every predicate is on ONE line however long, because `tsc` echoes only the line the
    failing type argument sits on — do not let a formatter wrap them. Zero suppression directives:
    predicates, never `@ts-expect-error`, which suppresses any error on the next line and has twice
    passed green over unrelated typos, and is reserved for object-literal freshness only. Never
    `expectTypeOf`.

IMPORTS — note the barrel-vs-module split, and it is load-bearing.
  - `import type { Assignable, Equals, Expect } from "./_assert.js";`
  - Types (`ActionResult`, `Bridge`, `BridgeRegistry`, `SnapshotNormalizer`) from `../src/types.js`.
  - **The VALUE imports come from `../src/index.js` — the BARREL — never from `../src/bridge.js`.**
    `concierge.test-d.ts:110-115` states the reason: the point of pinning a signature is that the
    PUBLIC entrypoint carries the callable value at that signature. Import `createBridge`,
    `captureSnapshot` and `offPageResult` from the barrel.
  - Do **not** import `MESSAGE_MAX_CHARS` here, and do not import anything from `../src/types.js` as a
    value. RESEARCH § Q6 records that a `MESSAGE_MAX_CHARS` predicate reading from `types.js` recreates
    the exact blind guard STATE.md warns about; the placement guard lives in `exports.test-d.ts` and
    already reads from `index.js`.

FIXTURES — declare locally, matching the shape at `actions.test-d.ts:416`: a `ResultsBridge` alias
being `Bridge` at a concrete actions record and a concrete snapshot record. These types are file-local
in `actions.test-d.ts` and not exported, so this file declares its own rather than importing them.
Add a comment saying so, and saying that the *assertion* `_registryReadIsNullable` is deliberately not
duplicated here because `actions.test-d.ts:436` already discriminates mutant P-05-3 — a second copy
would be two things to update and one place for them to disagree.

PREDICATES — each with a JSDoc line above it naming the invariant it guards, each on ONE line.
  - `_createBridgeReturnsRegistry` asserting
    `Equals<ReturnType<typeof createBridge<ResultsBridge>>, BridgeRegistry<ResultsBridge>>`. This is
    the pin PATTERNS specifies: instantiating at a concrete `B` and checking the return type is exactly
    the registry at that `B`.
  - `_createBridgeTakesOneString` asserting `Equals<Parameters<typeof createBridge>, [id: string]>`.
    Together with the previous predicate this covers "did not silently gain a second parameter" and
    "did not silently gain a `const` type parameter", the same pair `_createConciergeSignature` covers
    for `createConcierge`.
  - `_createBridgeDefaultsToBridge` asserting
    `Equals<ReturnType<typeof createBridge>, BridgeRegistry<Bridge>>` — the `= Bridge` default, which
    is what makes `createBridge("results")` typecheck with no explicit type argument. Removing the
    default makes this red.
  - `_offPageResultSignature` asserting
    `Equals<typeof offPageResult, (what: string, where: string) => ActionResult>`. **`Equals`, not
    `Assignable`**, per `actions.test-d.ts:469-476`: an `Assignable` spelling stays true when a
    parameter is widened to `unknown`, to a bare function type, or to a union that swallows the
    declared type, which is exactly the silent-widening regression worth guarding. Put that reason in
    the JSDoc line.
  - `_captureSnapshotReturnsPlainRecord` asserting
    `Equals<ReturnType<typeof captureSnapshot<ResultsBridge>>, Record<string, unknown>>`.
  - `_captureSnapshotNormalizerIsOptional` asserting that `captureSnapshot` is callable with two
    arguments — spell this as an `Assignable` from `typeof captureSnapshot` to a two-parameter function
    type returning `Record<string, unknown>`, so the optionality of the third parameter is the claim.

**If `Equals` on a generic-instantiation form does not hold under TypeScript 7.0.2**, do not weaken the
predicate to `Assignable` and do not add a suppression. The deterministic fallback is the
`ReturnType`/`Parameters` decomposition already used above — apply the same decomposition to whichever
predicate fails, and record in the SUMMARY which spelling was needed and why. A predicate that was
weakened to make it pass is worse than none, because a reader stops looking.

DEAD-LOCAL GUARD — if any locally declared fixture type ends up otherwise unread, tie it to a
predicate the way `concierge.test-d.ts:189-190` does, so the shapes travel together and an unread local
is not one refactor away from being deleted as dead.
  </action>

  <verify>
    <automated>pnpm build</automated>
    <automated>pnpm typecheck</automated>
    <automated>bash -c 'f=packages/concierge/test-d/bridge.test-d.ts; grep -qF "from \"../src/index.js\"" $f &amp;&amp; ! grep -qF "from \"../src/bridge.js\"" $f &amp;&amp; echo "barrel import: OK"'</automated>
    <automated>bash -c 'f=packages/concierge/test-d/bridge.test-d.ts; ! grep -qE "@ts-expect-error|expectTypeOf|^export " $f &amp;&amp; echo "house rules: OK"'</automated>
    <automated>bash -c 'f=packages/concierge/test-d/bridge.test-d.ts; n=$(grep -c "^type _" $f); echo "predicates: $n"; [ "$n" -ge 6 ]'</automated>
    <automated>bash -c 'f=packages/concierge/test-d/bridge.test-d.ts; ! grep -qF "_registryReadIsNullable" $f &amp;&amp; ! grep -qF "MESSAGE_MAX_CHARS" $f &amp;&amp; echo "no duplication, no blind guard: OK"'</automated>
  </verify>

  <done>
`test-d/bridge.test-d.ts` exists, `pnpm typecheck` passes, and six or more one-line predicates pin the
three new signatures from the public barrel.
  </done>

  <acceptance_criteria>
    - `pnpm typecheck` exits 0.
    - Value imports come from `"../src/index.js"`; the file contains no import from `"../src/bridge.js"`.
    - At least six predicate aliases named `_…` exist, each declared with `type _name = Expect<…>` on a single line. Assert no predicate line exceeds one physical line: every line beginning `type _` also contains `>;`.
    - `_offPageResultSignature` uses `Equals`, not `Assignable`. A grep for `Assignable<typeof offPageResult` returns 0.
    - The file contains no `@ts-expect-error`, no `expectTypeOf`, and no top-level `export`.
    - The file does not redefine `_registryReadIsNullable` and does not import or reference `MESSAGE_MAX_CHARS`.
    - The header carries the terse-output caveat naming `tsc` exit code **1** under TypeScript 7.0.2 and warning against grepping output for predicate names.
    - Fixture types are declared locally with a comment explaining that `test-d/` fixtures are file-local and unexported (verified: `actions.test-d.ts:416,419` carry no `export`).
    - If any predicate needed the `ReturnType`/`Parameters` decomposition rather than a whole-signature `Equals`, that is recorded in the SUMMARY with the reason. No predicate was weakened to `Assignable` to make it pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 (05-06-T2): single-instance.test.ts — F6, the third call site and the first direct one</name>

  <read_first>
    - `packages/concierge/test/single-instance.test.ts` lines 40-120 — the header block stating "The guard now has TWO production call sites, not one" and the paragraph at 61-71 warning a reader against "restoring" a direct call that does not exist, plus the `KEY`, `registry` and reset bindings.
    - `packages/concierge/test/single-instance.test.ts` lines 205-270 — cases F4 and F5, the shape F6 copies.
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-PATTERNS.md` lines 34-51 and 1181-1220 — why this file is the only home for M-05-8, and the F6 specification including the query string.
  </read_first>

  <files>packages/concierge/test/single-instance.test.ts</files>

  <action>
Two edits.

EDIT A — the header block at lines 57-71. It currently says "The guard now has **TWO** production call
sites, not one. Phase 4 ships `createConcierge`, which records this copy as well, and the last case in
this file, F5, is what makes ITS removal fail something." Move that to **THREE**, and add the
distinction that matters: `createBridge` is the third call site and the **first DIRECT one** —
`createConcierge` reaches the guard transitively through `buildCatalog`'s first line, and
`buildCatalog` reaches it directly but is itself the catalog path.

**Do not delete or weaken the paragraph at lines 61-71.** It states that F5 "does not claim the call
is DIRECT, and must not be read as claiming it", explains that either route satisfies PKG-04 equally,
and exists to stop someone "restoring" a direct `createConcierge` call which does not exist. That
latitude is still correct for F5. What changes is that F6 **does** claim directness for
`createBridge`, because `createBridge` reaches nothing else — it has no `buildCatalog` in its path. Say
that explicitly, so the two cases' different claims are legible side by side rather than looking
inconsistent.

EDIT B — add case **F6** after F5, copying F5's shape (lines 239-268) with one query-string bump and a
different production path.

  - Query string: **`?sc7=1`**. Measured this session, `?dup=1`, `?mismatch=1`, `?sc5=1` and `?sc6=1`
    are taken. Every case needing a fresh module evaluation must use a specifier nothing else in the
    file uses: two cases sharing one share Node's cached namespace, so the second would skip module
    scope entirely and its "empty after import" half would assert against state the first left behind.
    Carry that reason forward as a comment, as F5 does.
  - Destructure `assertSingleInstance`, `createBridge` and `CONTRACT_VERSION` from the fresh import.
  - Half one — EMPTY immediately after evaluation. Assert `typeof createBridge` is `"function"` and
    `registry[KEY]` is `undefined`. This half is a check in its own right rather than setup: it is what
    catches a guard smuggled up to module scope, the form `"sideEffects": false` licenses a bundler to
    delete outright. `assertSingleInstance` is destructured and deliberately NOT called — importing a
    binding is not invoking it, and calling it here would populate the registry itself and make half
    two pass no matter what the factory does. Carry both notes as comments.
  - The production path, at its minimum: `createBridge("results")`. No config is needed, unlike F5's
    `createConcierge({ stages: [] })`.
  - Half two — POPULATED afterwards. Assert `registry[KEY]` equals `{ version: CONTRACT_VERSION }`,
    through the same global record F1a, F4 and F5 assert on. No spy: the observable already exists and
    reports exactly this.

The `it` title follows the file's convention — case id plus the claim — and must state that this is the
guard's third production call site and its first direct one.

This case is mutant **M-05-8**'s only detector. With `assertSingleInstance();` deleted from
`createBridge`'s body, half two goes red here and nowhere else in the repository: `createBridge`
behaves identically with the call removed, so nothing in `test/bridge.test.ts` or
`test/bridge-snapshot.test.ts` can see it. Say that in a comment on the case.

Do not modify F1a, F1b (`test/single-instance.test.ts:168`), F2, F4 or F5. Do not add a `beforeEach` — the reset at lines 111-118 and the
`registry` binding already exist.
  </action>

  <verify>
    <automated>pnpm build</automated>
    <automated>pnpm test -- single-instance</automated>
    <automated>pnpm test</automated>
    <automated>pnpm typecheck</automated>
    <automated>bash -c 'f=packages/concierge/test/single-instance.test.ts; grep -qF "?sc7=1" $f &amp;&amp; grep -qF "createBridge(\"results\")" $f &amp;&amp; grep -q "it(\"F6" $f &amp;&amp; echo "F6 present: OK"'</automated>
    <automated>bash -c 'f=packages/concierge/test/single-instance.test.ts; n=$(grep -c "TWO production call sites" $f); echo "stale TWO hits: $n"; [ "$n" = "0" ]'</automated>
    <automated>bash -c 'f=packages/concierge/test/single-instance.test.ts; n=$(grep -o "sc7=1" $f | wc -l | tr -d " "); echo "sc7 uses: $n"; [ "$n" = "1" ]'</automated>
  </verify>

  <done>
`test/single-instance.test.ts` carries F6, the header states THREE production call sites and
distinguishes the direct one, and the full suite is green. Mutant M-05-8 now has a home.
  </done>

  <acceptance_criteria>
    - `pnpm test` passes the whole suite; `pnpm typecheck` exits 0.
    - The file contains an `it` whose title begins `F6` and names the guard's third production call site and its first direct one.
    - F6 uses the query string `?sc7=1`, and `sc7=1` occurs exactly once in the file. Reusing `?sc5=1` or `?sc6=1` would share Node's module cache and silently void half one.
    - F6's production path is `createBridge("results")` with no config argument.
    - F6 asserts both halves: `registry[KEY]` is `undefined` before the call, and equals `{ version: CONTRACT_VERSION }` after.
    - F6 destructures `assertSingleInstance` without calling it, with the comment explaining why calling it would make half two pass unconditionally.
    - `grep -c "TWO production call sites"` returns **0** — the header sentence moved to three. This is the stale-prose half of the edit and is easy to skip while adding the case.
    - The paragraph at lines 61-71 warning against "restoring" a direct `createConcierge` call is retained, with F6's contrasting directness claim added rather than replacing it.
    - Cases F1a, F1b, F2, F4 and F5 are unmodified: `git diff` shows no hunk inside them. F1b is at line 168 and is easy to miss because it sits between F1a and F2.
    - `git diff --exit-code` at the repo root is clean after commit.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a second copy of core → `globalThis[Symbol.for("@fullselfbrowsing/concierge.contract")]` | The cross-realm contract slot. Identity is the STRING, not an imported binding |
| a bundler under `"sideEffects": false` → core's module scope | A registration hoisted out of a function body is deleted from every bundled consumer while still running under `node dist/index.js` |
| core → the published type surface | A silently widened signature is invisible to a runtime suite that imports the artifact dynamically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Denial of service (self-inflicted) | two core copies split the registry, so every handler sees `bridge: null` forever on a page that is definitely open | mitigate | F6 asserts `createBridge` records this copy in the process-wide registry. Mutant **M-05-8**, whose only detector this is — `createBridge` behaves identically with the call removed, so no behavioural test can see it |
| T-05-18 | Tampering | `assertSingleInstance()` hoisted to module scope, where `"sideEffects": false` licenses a bundler to delete it — green under `node dist/index.js`, absent in every React or Svelte app | mitigate | F6 half one asserts the registry slot is EMPTY immediately after module evaluation, before any call. That half is the detector, not setup |
| T-05-19 | Tampering | a signature widening silently — `createBridge` gaining a `const` type parameter, or `offPageResult`'s parameters widening to `unknown` | mitigate | `Equals`-spelled predicates in `test-d/bridge.test-d.ts`, never `Assignable` for `offPageResult`, because `Assignable` stays true under exactly that widening |
| T-05-11 | Denial of service | a value smuggled into the `export type { … }` block, erasing the runtime binding | mitigate | Owned by plan 05-03's placement predicates. This file deliberately does not duplicate them — `exports.test-d.ts:63-69` forbids tightening there and PATTERNS puts tight signature pins here instead |
| T-05-SC | Tampering | npm/pnpm package installs | accept | Zero installs. `pnpm check:deps` asserts the empty external module graph |
</threat_model>

<verification>
1. `pnpm build && pnpm test && pnpm typecheck` — all green.
2. `test-d/bridge.test-d.ts` imports values from the barrel only, exports nothing, and carries no
   suppression directives.
3. `single-instance.test.ts` has six cases, `sc7=1` used exactly once, and zero hits for
   `TWO production call sites`.
4. `git diff --exit-code` clean at the repo root after commit.
</verification>

<success_criteria>
- Mutant M-05-8 has a detector. Before this plan it had none anywhere in the repository.
- `createBridge`, `captureSnapshot` and `offPageResult` have signature pins that a silent widening
  makes red, spelled with `Equals` rather than `Assignable` where widening is the risk.
- `single-instance.test.ts`'s header no longer understates the number of production call sites, and
  the direct-versus-transitive distinction is legible.
</success_criteria>

<output>
Create `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-06-SUMMARY.md` when done.

The SUMMARY must record:
- Each predicate alias with its final spelling, and — for any that needed the
  `ReturnType`/`Parameters` decomposition instead of a whole-signature `Equals` — the reason.
- Confirmation that no predicate was weakened to `Assignable` in order to pass.
- The new total test count for the suite.
</output>
