---
phase: 05-bridge-registry-and-the-no-bridge-path
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/concierge/src/concierge.ts
  - packages/concierge/src/types.ts
  - packages/concierge/src/contract.ts
autonomous: true
requirements: [BRG-03, BRG-05, DX-02]
user_setup: []

must_haves:
  truths:
    - "A stage whose bridge is declared but unmounted resolves to null, through one named seam."
    - "A stage that declares no bridge at all resolves to null without error and without auto-failing."
    - "explain()'s bridge row still distinguishes three states: not declared, declared-and-unmounted, declared-and-mounted."
    - "The shipped declaration file no longer claims the default snapshot normalizer is a deep freeze."
  artifacts:
    - path: "packages/concierge/src/concierge.ts"
      provides: "module-private resolveBridge(stage) seam; bridgeStatus routed through it"
      contains: "function resolveBridge"
    - path: "packages/concierge/src/types.ts"
      provides: "corrected SnapshotNormalizer and ConciergeConfig.normalizeSnapshot doc comments"
    - path: "packages/concierge/src/contract.ts"
      provides: "re-scoped reserved-call-site paragraph naming createBridge as the first direct call site"
  key_links:
    - from: "packages/concierge/src/concierge.ts bridgeStatus"
      to: "resolveBridge"
      via: "the only read() call path"
      pattern: "resolveBridge\\(stage\\)"
    - from: "packages/concierge/src/concierge.ts resolveBridge"
      to: "registry.read()"
      via: "guarded consumer call"
      pattern: "registry\\.read\\(\\)"
---

<objective>
Two independent corrections in `src/`, both wave-1-safe because neither changes the export surface
nor any existing assertion:

1. Add the module-private `resolveBridge(stage)` seam to `src/concierge.ts` and route the existing
   `bridgeStatus` through it. This is the phase's stage-to-bridge resolution path; Phase 6's
   dispatcher becomes its second caller. Without this redirect the seam has no caller reachable from
   `dist/index.js` and BRG-03's resolution half is unprovable at the layer this project tests at.

2. Correct three doc comments that have already shipped or are about to: the two false "deep freeze"
   claims in `src/types.ts` (both reach `dist/index.d.ts`, measured), and the reserved-call-site
   paragraph in `src/contract.ts` (reaches `dist/index.d.ts:2062`, measured).

Purpose: `resolveBridge` is what makes DX-02 structural rather than aspirational — core never
auto-fails an action because a declared bridge is unmounted. The prose corrections are a shipped
defect: a consumer reading `dist/index.d.ts` today gets a false statement about a security-relevant
default.

Output: three edited source files. The export surface does not move; the 87-test suite stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-CONTEXT.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-PATTERNS.md

<interfaces>
From `packages/concierge/src/types.ts:1593`: `ConciergeConfig` has `stages: ReadonlyArray<StageDefinition<any>>` — verified this session. So `ConciergeConfig["stages"][number]["bridge"]` is `BridgeRegistry<any> | undefined` and `read()` yields `any`.

From `packages/concierge/src/types.ts:1147`: `StageDefinition<B>.bridge?: BridgeRegistry<B>`
From `packages/concierge/src/types.ts:1432-1452`: `StageExplanation["bridge"]` is `{ id: string; registered: boolean } | null` — the three-state row. Pinned by `test-d/concierge.test-d.ts:141` (`_stageExplanationBridgeShape`), which must not move.
From `packages/concierge/src/types.ts:668`: `SnapshotNormalizer = <T>(value: T) => T`
</interfaces>

**Measurement correction to `05-PATTERNS.md`, made this session — this plan is authoritative over it.**
PATTERNS.md:492-493 states that `src/index.ts`'s module header paragraph "reaches `dist/index.d.ts`
verbatim". It does not. Measured: `grep -c "not yet constructible"` and
`grep -c "Stated plainly so this is not oversold"` both return **0** against `dist/index.d.ts` and
`dist/index.js`. The `index.ts` module header is not carried into the bundle. Consequences:
- `src/types.ts`'s two `deep freeze` occurrences DO ship — 2 hits in `dist/index.d.ts`, 0 in
  `dist/index.js`. Those are a real shipped defect and this plan closes them.
- `src/contract.ts:159-163` DOES ship — found at `dist/index.d.ts:2062`.
- `src/index.ts`'s stale prose is a **source**-prose defect, not a shipped one. It is still corrected
  (plan 05-03 owns it), but its audit target is `src/index.ts`, never `dist/`. A `dist/` grep for
  `not yet constructible` passes vacuously today and would read as a green check that proves nothing.
</context>

<tasks>

<task type="auto">
  <name>Task 1 (05-02-T1): The resolveBridge seam</name>

  <read_first>
    - `packages/concierge/src/concierge.ts` lines 195-245 — the doc comment and body of `bridgeStatus`, which is the edit target, plus the module-private helpers block it sits in.
    - `packages/concierge/src/concierge.ts` lines 40-60 (header constraint 3, the "called from exactly one place" rule) and lines 440-475 (`runMatch`'s doc comment, which is the template for how a single-call-site seam is documented, and its `try {} catch {}` with no binding).
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-PATTERNS.md` lines 401-463 — the `src/concierge.ts` section, which reproduces the current `bridgeStatus` verbatim and lists the three details that must carry forward exactly.
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-RESEARCH.md` lines 978-1031 — the recommended seam, and the note on why the `stage.bridge === undefined` early return must stay in `bridgeStatus`.
  </read_first>

  <files>packages/concierge/src/concierge.ts</files>

  <action>
Add a module-private `resolveBridge` to the `// Module-private helpers` block in
`packages/concierge/src/concierge.ts`, immediately before `bridgeStatus`, and rewrite
`bridgeStatus`'s body to call it. Do not export `resolveBridge`; the export surface must not move in
this plan.

SIGNATURE. `function resolveBridge(stage: ConciergeConfig["stages"][number]): Bridge | null`.

The parameter is spelled `ConciergeConfig["stages"][number]`, matching `bridgeStatus` exactly, and
NOT `StageDefinition<any>`. The reason is already written at `concierge.ts:217-220`: the `any` lives
in `types.ts` where D-07's measured contravariance reason justifies it, and re-spelling it here would
be a second, unargued occurrence of an erasure that was argued once. Because `ConciergeConfig.stages`
is `ReadonlyArray<StageDefinition<any>>`, `registry.read()` yields `any`; the `Bridge | null` return
annotation is what makes the seam honest rather than propagating `any` to callers. Add
`Bridge` to the existing `import type { … } from "./types.js"` list if it is not already there.

BODY, in order:
  - `const registry: ConciergeConfig["stages"][number]["bridge"] = stage.bridge;`
  - `if (registry === undefined) { return null; }` — DX-02: a stage that declares no bridge is a
    supported configuration, not an error. Core never auto-fails an action because a stage's declared
    bridge is unmounted, and it certainly does not fail one for a stage that declares nothing.
  - `try { return registry.read() ?? null; } catch { return null; }` — the `catch` binds nothing and
    echoes nothing, exactly as `runMatch` and the current `bridgeStatus` do. `read()` is consumer
    code, so a throwing `read()` is not a registration.

THE `?? null` COALESCE IS A DECISION, and PATTERNS requires it to be stated rather than left
implicit. Today `bridgeStatus` tests `live !== null && live !== undefined`, because a JavaScript
consumer's `read()` can return `undefined` even though the interface types it `B | null`. That
`undefined` arm does not disappear — it MOVES into `resolveBridge`'s `?? null`. The observable is
identical (`registered` is still `false` for such a consumer) and the handler contract
`ctx.bridge: B | null` becomes structurally true rather than merely annotated. Write both halves of
that reasoning into `resolveBridge`'s doc comment, naming the case it exists for.

DOC COMMENT. Give `resolveBridge` the same treatment `runMatch` gets at `concierge.ts:440-447`: it is
the ONE place a stage becomes a bridge. Phase 6's dispatcher is the second caller; there must never
be a third, for the same reason `stage.match` is called from exactly one place — so the
throw policy and the not-declared policy exist once and cannot drift into two readers that disagree.
Also state that `resolveBridge` deliberately collapses "not declared" and "declared but unmounted"
into the same `null`, which is why `bridgeStatus` must keep its own early return (below).

REWRITE `bridgeStatus`. Keep its doc comment, its signature, and its
`if (registry === undefined) { return null; }` early return **exactly as they are**. Replace only the
`let live: unknown; try { … } catch { … }` block with `const live: Bridge | null = resolveBridge(stage);`
and change the final return's second field to `registered: live !== null`.

**The `stage.bridge === undefined` early return must stay in `bridgeStatus`, before the
`resolveBridge` call.** This is mutant **M-05-14**'s target. `resolveBridge` returns `null` for both
"not declared" and "declared but unmounted"; `explain()`'s row distinguishes them and
`types.ts:1432-1452` plus `test-d/concierge.test-d.ts:141` pin that distinction. Collapsing it would
silently change `explain()`'s output for a stage with no bridge from `null` to
`{ id: …, registered: false }` — and there is no `id` to put there. Add a short comment at the early
return saying exactly this, so a later reader does not "simplify" it away.

DO NOT TOUCH `DISPATCH_NOT_IMPLEMENTED` (`concierge.ts:129`) or `dispatch` (`concierge.ts:578-584`).
Phase 6 replaces the stub wholesale; `test/concierge.test.ts` S27 pins the absence of `reason` on it
and its header records that this was the one mutant of fifteen that survived Phase 4's battery.
Phase 5 ships the seam Phase 6 will call; it does not call it from `dispatch`.

`test/concierge.test.ts` S20 must keep passing unchanged — it is the existing regression detector for
`bridgeStatus`'s three states, driven by a hand-rolled registry whose `read` returns `null`.
  </action>

  <verify>
    <automated>pnpm typecheck</automated>
    <automated>pnpm build</automated>
    <automated>pnpm test 2>&amp;1 | grep -E "Tests +87 passed"</automated>
    <automated>bash -c 'grep -v "^\s*[*/]" packages/concierge/src/concierge.ts > /tmp/c1.code; for p in "return registry.read() ?? null;" "resolveBridge(stage)" "registered: live !== null"; do n=$(grep -o -F "$p" /tmp/c1.code | wc -l | tr -d " "); echo "$n  $p"; [ "$n" = "1" ] || exit 1; done; echo "seam anchors unique: OK"'</automated>
    <automated>bash -c 'grep -qF "function resolveBridge(stage: ConciergeConfig[\"stages\"][number]): Bridge | null" packages/concierge/src/concierge.ts &amp;&amp; ! grep -qE "^export function resolveBridge" packages/concierge/src/concierge.ts &amp;&amp; echo "resolveBridge private: OK"'</automated>
    <automated>pnpm test 2>&amp;1 | grep -E "S20|Tests +87 passed"</automated>
  </verify>

  <done>
`resolveBridge` exists as a module-private function in `src/concierge.ts`, `bridgeStatus` calls it,
`explain()`'s three-state bridge row is unchanged, and the 87-test suite including S20 is green.
The export surface has not moved.
  </done>

  <acceptance_criteria>
    - `pnpm typecheck` exits 0; `pnpm build` exits 0.
    - `pnpm test` output matches `Tests  87 passed (87)`. In particular S20 ("the bridge field reports declared-and-unmounted, declared-and-mounted, and not declared") still passes — it is the existing detector for the three-state row.
    - Source contains verbatim: `function resolveBridge(stage: ConciergeConfig["stages"][number]): Bridge | null`.
    - `resolveBridge` is NOT exported: `grep -cE "^export function resolveBridge"` returns 0.
    - `bridgeStatus` still contains `if (registry === undefined) {` followed by `return null;` BEFORE any `resolveBridge(` occurrence in the same function. This is mutant **M-05-14**'s target; removing it collapses `explain()`'s three-state row to two.
    - `bridgeStatus` no longer contains `let live: unknown;` and now contains `const live: Bridge | null = resolveBridge(stage);`.
    - `registered: live !== null` occurs exactly once; `live !== undefined` no longer occurs in `bridgeStatus`.
    - `return registry.read() ?? null;` occurs **exactly once** in comment-stripped source. This anchors mutant **M-05-13** (substitute `return null;`), whose effect is "resolution always off-page".
    - The `catch` in `resolveBridge` binds nothing: the source contains `} catch {` and not `} catch (` anywhere inside `resolveBridge`.
    - `packages/concierge/src/concierge.ts` still contains `DISPATCH_NOT_IMPLEMENTED` exactly as before and the `dispatch` stub is byte-identical. Assert with `git diff packages/concierge/src/concierge.ts` showing no hunk touching lines within the `dispatch` function or the `DISPATCH_NOT_IMPLEMENTED` constant.
    - The built export surface still parses to `62 51 11`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 (05-02-T2): Correct the three shipped doc comments</name>

  <read_first>
    - `packages/concierge/src/types.ts` lines 654-670 (occurrence 1, `SnapshotNormalizer`) and lines 1604-1614 (occurrence 2, `ConciergeConfig.normalizeSnapshot`). Both reproduced verbatim in `05-PATTERNS.md` lines 538-599.
    - `packages/concierge/src/contract.ts` lines 105-170 — the instruction line at `:109-113`, the Phase-4 correction of the adjacent sentence at `:147-157` (which is the template for this move), and the reserved-call-site paragraph at `:159-163` (the edit target).
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-PATTERNS.md` lines 538-646 — the `types.ts` and `contract.ts` sections, including the precedent for correcting shipped prose in place rather than silently applying it.
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-RESEARCH.md` lines 525-560 (the measured freeze-vs-clone matrix, which is the evidence the new prose asserts) and lines 658-676 (the cosmetic-freeze and symbol-key findings).
  </read_first>

  <files>packages/concierge/src/types.ts, packages/concierge/src/contract.ts</files>

  <action>
Three doc-comment corrections. No runtime code changes, no signature changes, no export changes.

**THE HARD CONSTRAINT ON ALL OF THEM: the replacement prose must not contain the two-word literal
`deep freeze`.** The phase gate greps `dist/index.d.ts` and `dist/index.js` for that exact phrase
expecting zero hits. CONTEXT's own decision text reads "a deep structural clone followed by a freeze
— not a deep freeze", and shipping that phrasing would make the audit fire on the correction itself.
Write "freezing in place" or "a freeze applied in place" wherever the contrast is needed.

CORRECTION 1 — `packages/concierge/src/types.ts:665-666`, the last sentence of `SnapshotNormalizer`'s
doc comment. Current text: "The Svelte adapter fills this with `$state.snapshot`. Frameworks without
proxy-based reactivity supply a deep freeze." Everything above that sentence is correct and must
survive untouched, including the `structuredClone` clause. Replace the second sentence only, with
prose stating that leaving this unset gets core's default — a structural clone followed by a freeze
— and cross-referencing `{@link ConciergeConfig.normalizeSnapshot}` for the mechanism and the
documented limits. Do not touch the type's function-property syntax; `snapshotEquality` must stay
function-property syntax per Phase 1 D-03 and `DigestLike` is the deliberate opposite, so any
snapshot-related type obeys the same rule and this plan must not harmonize them.

CORRECTION 2 — `packages/concierge/src/types.ts:1607-1611`, `ConciergeConfig.normalizeSnapshot`'s doc
comment. Current text: "Detaches snapshots from framework reactivity before storage. Supplied by the
framework adapter; defaults to a deep freeze." This is the substantive one and carries the whole
argument, because it is what a consumer reads in `dist/index.d.ts`. The replacement must assert, in
this order:
  - What it does: detaches snapshots from framework reactivity before storage; supplied by the
    framework adapter; left unset, core supplies a structural clone followed by a freeze.
  - Why that is not the same as freezing in place: freezing a Proxy does not detach it — the stored
    snapshot stays a live view, so Phase 8's drift check compares a value against itself and passes
    unconditionally, which is the "failing while appearing to work" shape this type's sibling comment
    already names.
  - The mechanism, in the one sentence RESEARCH specifies verbatim: cloning fires only *read* traps
    (`ownKeys` / `getOwnPropertyDescriptor` / `get`); freezing fires *write* traps
    (`preventExtensions` / `defineProperty`).
  - The two further measured failure modes of freezing in place: it can freeze the host app's own
    reactive store through the proxy, permanently breaking the app it was observing; and it can throw
    `TypeError` out of the capture path on proxy shapes whose traps do not satisfy the freeze
    invariants.
  - The documented limits of the default clone, stated as limits rather than implied: plain objects,
    arrays, `Date`, `Map` and `Set` are cloned; everything else — class instances, cross-realm plain
    objects, and any value whose extraction throws — is passed through **by reference**.
    Symbol-keyed properties are not carried. A frozen `Map`, `Set` or `Date` is still mutable through
    its own methods, so the default delivers detachment, not immutability.

Follow the in-place-correction precedent at `concierge.ts:263-282` and `catalog.ts:635-640`: annotate
that this replaced an earlier claim rather than silently applying the change, and say which direction
the correction runs. Here it runs from a claim that was false to one that is measured.

CORRECTION 3 — `packages/concierge/src/contract.ts:159-163`, the reserved-call-site paragraph. Current
text says the adapter-registration call site "is genuinely still to come… an adapter can be imported
and mounted in a module that never builds a catalog, so it needs a call of its own and inherits
nothing from this one." **Re-scope; do not delete.** The move is the same one Phase 4 made for the
adjacent `createConcierge` sentence at `contract.ts:147-157` — read that paragraph and match its
register — but the outcome differs: `createBridge` in `./bridge.ts` **is** a direct call site, and the
first direct one, where `createConcierge` reaches the guard transitively through `buildCatalog`.

The re-scoped paragraph must say: `createBridge` arrived in Phase 5 and calls this function directly
from its own body, which discharges the obligation **for apps that call `createBridge` directly**; and
that a Phase 9 adapter which mounts without any `createBridge` call in the graph would still need a
call of its own, so the reservation stands narrowed rather than closed. Do not claim the obligation is
discharged outright.

Also update the instruction line at `contract.ts:109-113`, which currently names `createConcierge` and
each adapter's registration hook as the entry points to call from. Add `createBridge` to that list.
It is now a real production call site and the line is the normative instruction a reader follows.

Both `contract.ts` edits reach `dist/index.d.ts` verbatim — measured at `:2062` this session — so they
are shipped prose, not internal notes.

**THE RE-SCOPE NEEDS A ZERO-BASELINE ANCHOR PHRASE, AND `createBridge` IS NOT ONE.** Measured this
session: `grep -c "createBridge" packages/concierge/dist/index.d.ts` returns **1** today, before any
edit — at `dist/index.d.ts:1245`, emitted from `src/types.ts:1446`, where `StageExplanation["bridge"]`'s
doc comment already mentions it. So a bare `createBridge` grep passes on the pre-correction tree and
proves nothing. It is doubly useless by the time the phase gate runs, because plan 05-03 adds
`declare function createBridge` and an export entry whether or not this re-scope shipped at all.

So the re-scoped paragraph must contain a **distinctive phrase that returns 0 hits against the current
`dist/index.d.ts`**, and the audit greps for that phrase rather than for the bare identifier.

**The phrase is fixed by this plan, not chosen at execution time.** The re-scoped paragraph MUST
contain this clause verbatim, as a contiguous run of characters on one line:

    the first direct production call site

Measured this session against the uncorrected tree: **0 hits** in `packages/concierge/dist/index.d.ts`,
**0** in `packages/concierge/dist/index.js`, **0** in `packages/concierge/src/contract.ts`. So the
literal is shown able to fire on the pre-correction tree, which is the rule plan 05-07 states for every
shipped literal.

Fixing the phrase here rather than delegating the choice is deliberate, and it closes a hazard rather
than adding ceremony. An executor-chosen phrase has to travel from this plan (Wave 1) to the phase gate
(Wave 4) through some channel, and every available channel fails open: a `/tmp` file does not survive
three waves, separate worktrees (`use_worktrees: true`) or a swept `/tmp`, and `grep -q "$(cat missing)"`
expands to `grep -q ""`, which matches every line and reports success against an artifact carrying no
correction at all — measured, exit 0. Reading the phrase back out of `05-02-SUMMARY.md` does not work
either, because this task's own gate runs BEFORE that SUMMARY exists. A literal fixed in both plans
needs no channel, so it cannot fail open. Record the measured before/after counts in the SUMMARY for
the evidence table, but nothing greps the SUMMARY.

Write the clause naturally into the sentence — for example, that `createBridge` reaches this guard from
its own body and so is the first direct production call site, where `buildCatalog` is direct but is the
catalog path and `createConcierge` is transitive. Do not wrap the clause in backticks or markdown
emphasis, and do not let a formatter break it across lines: the gate matches it as a fixed string.
  </action>

  <verify>
    <automated>pnpm typecheck</automated>
    <automated>pnpm build</automated>
    <automated>pnpm test 2>&amp;1 | grep -E "Tests +87 passed"</automated>
    <automated>bash -c 'for f in packages/concierge/src/types.ts packages/concierge/src/contract.ts packages/concierge/dist/index.d.ts packages/concierge/dist/index.js; do n=$(grep -c "deep freeze" "$f"); echo "$n  $f"; [ "$n" = "0" ] || exit 1; done; echo "deep-freeze prose audit: 0 hits everywhere"'</automated>
    <automated>bash -c 'echo -n "createBridge baseline hits in dist (pre-existing, NOT a gate): "; grep -c "createBridge" packages/concierge/dist/index.d.ts'</automated>
    <automated>bash -c 'grep -qF "the first direct production call site" packages/concierge/dist/index.d.ts &amp;&amp; echo "re-scope anchor phrase reached dist: OK"'</automated>
    <automated>bash -c 'grep -qF "the first direct production call site" packages/concierge/src/contract.ts &amp;&amp; echo "anchor clause present in source: OK"'</automated>
    <automated>bash -c 'grep -c "read traps" packages/concierge/dist/index.d.ts | grep -qv "^0$" &amp;&amp; echo "mechanism sentence ships: OK"'</automated>
  </verify>

  <done>
All three doc comments are corrected, `pnpm build` has regenerated `dist/`, and the phrase
`deep freeze` appears zero times in `src/types.ts`, `src/contract.ts`, `dist/index.d.ts` and
`dist/index.js`. The 87-test suite is green and the export surface has not moved.
  </done>

  <acceptance_criteria>
    - `pnpm typecheck` exits 0; `pnpm build` exits 0; `pnpm test` matches `Tests  87 passed (87)`.
    - `grep -c "deep freeze"` returns **0** for each of: `packages/concierge/src/types.ts`, `packages/concierge/src/contract.ts`, `packages/concierge/dist/index.d.ts`, `packages/concierge/dist/index.js`. Baseline before this task: 2, 0, 2, 0. This is the gate the whole correction exists for, and the replacement prose must not re-introduce the phrase.
    - `dist/index.d.ts` contains the mechanism sentence: `grep -c "read traps"` returns at least 1, and the same region names `preventExtensions` and `defineProperty`.
    - The re-scoped paragraph contains the clause `the first direct production call site` verbatim, on one line, unwrapped and without backticks or markdown emphasis. Measured baseline against the uncorrected tree: **0** in `dist/index.d.ts`, **0** in `dist/index.js`, **0** in `src/contract.ts` — so the literal was shown able to fire before the correction.
    - After `pnpm build`, `grep -cF "the first direct production call site" packages/concierge/dist/index.d.ts` returns at least **1**. The grep is `-F`: the phrase is matched as a fixed string, never as a basic regular expression. BRE behaviour here is binary-dependent — BSD grep 2.6.0-FreeBSD errors with exit 2 on a `**`-bearing pattern, while the ugrep 7.5.0 first on this PATH silently matches with exit 0 (a false positive on a line not containing the phrase) — which is why the flag is `-F` rather than a reliance on any one platform's error. A stray `*` or `.` would silently widen the match set — over-matching in an audit is the same defect class as not matching.
    - No gate in this plan or in plan 05-07 depends on a file under `/tmp` or on parsing `05-02-SUMMARY.md`. Both grep a literal fixed in the plans, so there is no channel that can fail open.
    - `grep -c "createBridge" packages/concierge/dist/index.d.ts` is recorded but is **not** a gate. Its measured pre-change baseline is **1**, at `dist/index.d.ts:1245` from `src/types.ts:1446`, so a bare-identifier assertion would pass on the uncorrected tree. Asserting on it would be threat T-05-12 in this plan's own register — an audit that cannot fail.
    - The re-scoped `contract.ts` paragraph still asserts a remaining obligation. Assert the paragraph contains both `createBridge` and a clause naming the adapter case — the words "adapter" and "its own" must both still occur in it. Deleting the reservation outright fails this.
    - `contract.ts:109-113`'s instruction line now names `createBridge` alongside `createConcierge`.
    - `SnapshotNormalizer`'s declaration is unchanged: the file still contains `export type SnapshotNormalizer = <T>(value: T) => T;` verbatim. This is a doc-comment-only correction; the function-property syntax must not be touched.
    - `ConciergeConfig.normalizeSnapshot`'s declaration is unchanged: `normalizeSnapshot?: SnapshotNormalizer;` verbatim.
    - The corrected `normalizeSnapshot` comment states all five required claims: the clone-then-freeze default, why it differs from freezing in place, the read-traps/write-traps mechanism, the two further measured failure modes, and the documented limits including symbol keys and the cosmetic freeze on `Map`/`Set`/`Date`.
    - The built export surface still parses to `62 51 11`.
    - `src/index.ts` is untouched by this plan — it belongs to 05-03, which owns the export line and the `not yet constructible` prose together.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer-supplied `BridgeRegistry.read()` → `resolveBridge` | `read()` is consumer code reached on every `explain()` call; it may throw |
| core → the published `dist/index.d.ts` | Shipped prose is read by consumers and by agents; a false claim about a security-relevant default is a defect in the artifact, not a note |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information disclosure | `resolveBridge`'s guarded `registry.read()` | mitigate | `try { … } catch { return null; }` with a `catch` that binds nothing, so no caught message is in scope to interpolate. Matches `runMatch` (`concierge.ts:469-473`) and the current `bridgeStatus` |
| T-05-08 | Denial of service | a throwing `read()` propagating out of `explain()` | mitigate | The same `catch`. A throwing `read()` degrades to "not registered" rather than taking down the explanation the developer asked for |
| T-05-09 | Repudiation | `explain()`'s bridge row losing the three-state distinction | mitigate | The `stage.bridge === undefined` early return stays in `bridgeStatus` before the `resolveBridge` call. Mutant M-05-14; pinned by `test-d/concierge.test-d.ts:141` and `test/concierge.test.ts` S20 |
| T-05-04 | Elevation of privilege | shipped prose telling a consumer the default normalizer is a freeze, so they do not supply one and Phase 8's drift check silently passes | mitigate | Corrections 1 and 2. This is why the prose is treated as a defect: `dist/index.d.ts:553` and `:1409` currently assert it |
| T-05-03 | Denial of service (self-inflicted) | `contract.ts`'s reserved-call-site paragraph going false, so a Phase 9 adapter author believes the obligation is discharged | mitigate | Correction 3 re-scopes rather than deletes: `createBridge` covers apps that call it directly; an adapter mounting without one still needs its own call |
| T-05-SC | Tampering | npm/pnpm package installs | accept | Zero installs in this plan and this phase. `pnpm check:deps` asserts the empty external module graph |
</threat_model>

<verification>
1. `pnpm build && pnpm test && pnpm typecheck` — green, `Tests  87 passed (87)`.
2. `grep -c "deep freeze"` returns 0 across `src/types.ts`, `src/contract.ts`, `dist/index.d.ts`,
   `dist/index.js`. Baseline was 2 / 0 / 2 / 0.
3. `dist/index.d.ts` gained `createBridge` inside the `assertSingleInstance` doc block and gained the
   read-traps/write-traps mechanism sentence.
4. The built export surface is still `62 51 11` — this plan adds no export.
5. `git diff --exit-code` at the repo root is clean after commit.
</verification>

<success_criteria>
- `resolveBridge` exists, is module-private, is called from exactly one place (`bridgeStatus`), and
  guards `read()` with a binding-free `catch`.
- `explain()`'s three-state bridge row is behaviourally unchanged; S20 passes untouched.
- The `dispatch` stub and `DISPATCH_NOT_IMPLEMENTED` are byte-identical.
- The phrase `deep freeze` is gone from source and from both built artifacts.
- `contract.ts`'s reservation is narrowed, not closed.
</success_criteria>

<output>
Create `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-02-SUMMARY.md` when done.

The SUMMARY must record:
- The final text of `resolveBridge`, including its doc comment.
- The `deep freeze` grep counts before and after, for all four files.
- The `dist/index.d.ts` line number at which the re-scoped `contract.ts` paragraph now lands.
- The re-scope anchor clause `the first direct production call site`, on a line beginning exactly
  `re-scope anchor phrase: `, with its measured hit count in `dist/index.d.ts` before the edit
  (must be 0) and after `pnpm build` (must be at least 1). This is recorded for plan 05-07's evidence
  table; **no gate parses it** — both gates grep the literal fixed in the plans.
- The recorded, non-gating `createBridge`-in-`dist/index.d.ts` count, before and after.
- The unfiltered occurrence count of `return registry.read() ?? null;` and of the `bridgeStatus`
  early return, for plan 05-07's mutation battery.
</output>
