---
phase: 05-bridge-registry-and-the-no-bridge-path
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/concierge/src/index.ts
  - packages/concierge/test/export-surface.test.ts
  - packages/concierge/test-d/exports.test-d.ts
  - packages/concierge/test/artifact.test.ts
autonomous: true
requirements: [BRG-01, BRG-03, BRG-05]
user_setup: []

must_haves:
  truths:
    - "createBridge, captureSnapshot and offPageResult reach dist/index.js as callable functions."
    - "The published export surface is exactly 65 names, 51 types, 14 values."
    - "src/index.ts no longer claims bridges are declared but not constructible."
  artifacts:
    - path: "packages/concierge/src/index.ts"
      provides: "the value export line for ./bridge.js and the corrected module header prose"
      contains: "from \"./bridge.js\""
    - path: "packages/concierge/test/export-surface.test.ts"
      provides: "three counts, three it titles, and the VALUE_EXPORTS array at 14 entries"
    - path: "packages/concierge/test-d/exports.test-d.ts"
      provides: "three placement predicates on the shared import line; header numbers at nine"
    - path: "packages/concierge/test/artifact.test.ts"
      provides: "one runtime it case per new value export"
  key_links:
    - from: "packages/concierge/src/index.ts"
      to: "packages/concierge/src/bridge.ts"
      via: "value re-export line"
      pattern: "export \\{ createBridge, captureSnapshot, offPageResult \\} from \"\\./bridge\\.js\";"
    - from: "packages/concierge/test-d/exports.test-d.ts"
      to: "packages/concierge/src/index.ts"
      via: "the shared import line — index.js, never types.js"
      pattern: "from \"\\.\\./src/index\\.js\""
---

<objective>
Barrel the three new values and move all eleven export pins in one change.

**This plan is one unit because the pins cannot be split.** The moment `src/index.ts` exports three
new values, `test/export-surface.test.ts`'s three counts and three `it` titles, its `VALUE_EXPORTS`
array, `test-d/exports.test-d.ts`'s import line and header numbers, and `test/artifact.test.ts`'s
per-export cases all go stale simultaneously. Split across plans, whichever lands first leaves a red
suite for every plan after it.

Purpose: `createBridge`, `captureSnapshot` and `offPageResult` are unreachable from the runtime test
layer until barrelled — `test/*.test.ts` imports `../dist/index.js` and never `../src/`. Wave 3's
behavioural suites depend entirely on this plan.

Output: four edited files. The export surface moves 62/51/11 to **65/51/14**.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-CONTEXT.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-PATTERNS.md
@.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-01-SUMMARY.md

<interfaces>
The three new values, from plan 05-01 (`packages/concierge/src/bridge.ts`):
  `createBridge<B extends Bridge = Bridge>(id: string): BridgeRegistry<B>`
  `captureSnapshot<B extends Bridge>(bridge: B, id: string, normalize?: SnapshotNormalizer): Record<string, unknown>`
  `offPageResult(what: string, where: string): ActionResult`
</interfaces>

**THE NUMBERS.** RESEARCH § Q5 computes `+2 values, 64/51/13`. That is stale: CONTEXT § "Settled
after research" exports the capture function too, so growth is **+3 values**. Live baseline
re-measured this session with the test's own regex against `packages/concierge/dist/index.d.ts`:
`names 62, types 51, values 11`.

| # | Pin | File:line | Current | After |
|---|-----|-----------|---------|-------|
| 1 | `it` title | `test/export-surface.test.ts:134` | `is exactly 62 names` | **65** |
| 2 | count | `test/export-surface.test.ts:136` | `toHaveLength(62)` | **65** |
| 3 | `it` title | `test/export-surface.test.ts:139` | `splits 51 types to 11 values` | **`splits 51 types to 14 values`** |
| 4 | count | `test/export-surface.test.ts:141` | `toHaveLength(51)` | **51 — UNCHANGED** |
| 5 | count | `test/export-surface.test.ts:142` | `toHaveLength(11)` | **14** |
| 6 | `it` title | `test/export-surface.test.ts:145` | `carries all eleven runtime value exports by name` | **`all fourteen`** |
| 7 | `VALUE_EXPORTS` array | `test/export-surface.test.ts:106-122` | 11 entries | **14 entries** |
| 8 | header sentence, **three numbers inside it** | `test-d/exports.test-d.ts:53-61` | `the total to six` / `at six names` / `whichever of the six` | **nine** x3 |
| 9 | shared import line | `test-d/exports.test-d.ts:72` | 6 names | **9 names** |
| 10 | placement predicates | `test-d/exports.test-d.ts:79-102` | 6 | **9** |
| 11 | per-export `it` cases | `test/artifact.test.ts:43-150` | 10 | **13** |

Pin 4 is listed precisely because it does **not** move. Types stay at 51. Writing 54 there is the
most likely arithmetic slip in this plan.

**Measurement correction to `05-PATTERNS.md`, made this session.** PATTERNS.md:492-493 says
`src/index.ts`'s module header "reaches `dist/index.d.ts` verbatim" and instructs a `dist/` grep for
`not yet constructible`. Measured: that phrase and its surrounding paragraph return **0 hits** in both
`dist/index.d.ts` and `dist/index.js`. The `index.ts` module header is not carried into the bundle.
The stale prose is real and CONTEXT locks its correction, but **its audit target is
`packages/concierge/src/index.ts`, never `dist/`.** A `dist/` grep for it passes vacuously today and
would read as a green check that proves nothing — the exact failure
`test-d/exports.test-d.ts`'s own header warns about.
</context>

<tasks>

<task type="auto">
  <name>Task 1 (05-03-T1): Barrel the three values and correct the stale header prose</name>

  <read_first>
    - `packages/concierge/src/index.ts` in full — 127 lines, one read.
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-PATTERNS.md` lines 466-535 — the `src/index.ts` section, reproducing the stale paragraph verbatim with line numbers and showing the existing export-block register.
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-01-SUMMARY.md` — confirm the three exported names and signatures match what this task barrels.
  </read_first>

  <files>packages/concierge/src/index.ts</files>

  <action>
Two edits to `packages/concierge/src/index.ts`.

EDIT A, the value export line. The values block runs lines 112-127: one `export { … } from "…";`
statement per source module in dependency order, each on its own line with a blank line between. The
last is `export { createConcierge } from "./concierge.js";` at line 127. Add exactly one new line
after it, in the same register, reading:
`export { createBridge, captureSnapshot, offPageResult } from "./bridge.js";`

The order of the three names inside the braces is load-bearing downstream — `VALUE_EXPORTS` in
`test/export-surface.test.ts` is maintained in source order and Task 2 appends them in this order. Use
`createBridge`, then `captureSnapshot`, then `offPageResult`.

The line goes in the **values** block, never in an `export type { … }` block. That placement is what
mutants P-05-1 and P-05-2 attack, and the resulting diagnostic is TS1485 at
`test-d/exports.test-d.ts`'s shared import line, not TS2344 at the predicate.

Add no new `export type { … }` entries. `Bridge` and `BridgeRegistry` are already exported as types at
lines 71-73; this phase adds no new type, which is why the types count stays at 51.

EDIT B, the stale module-header prose at lines 24-36. Two clauses are false and must go; one adjacent
sentence is still true and must stay.

False clause 1, lines 27-28: "There is no session, no transport and no consent prompt in this package
today, and bridges are declared but not yet constructible." The session/transport/consent half remains
true. Rewrite so the bridges half asserts the opposite — bridges are now constructible, `createBridge`
returns a registry a page component registers into — while making clear nothing dispatches through one
yet, which is what keeps the surrounding "stated plainly so this is not oversold" paragraph honest.

False clause 2, line 32: "The runtime still to come is `createSession` and `createBridge`." Only
`createSession` remains to come (Phase 7). Remove `createBridge` from that list.

**Keep the `defineStage` sentence at lines 32-36 intact** — "a stage needs no identity mechanism, a
plain `StageDefinition` object literal already typechecks, and the unforgeable bridge identity that
would have justified it belongs to `createBridge`." CONTEXT cites this exact sentence as the reason
`createBridge` absorbs `defineStage`'s only justification, and `test-d/concierge.test-d.ts:183`
(`_plainStageLiteral`) is its compiled half. It became MORE true this phase, not less.

**The replacement prose must not contain the literal string `not yet constructible`.** The audit greps
`packages/concierge/src/index.ts` expecting zero hits, so a rewrite reading "no longer not yet
constructible" would fail it. Write the positive claim directly.

Touch nothing else in the file — not the type blocks, not the other module-header paragraphs.
  </action>

  <verify>
    <automated>pnpm build</automated>
    <automated>pnpm typecheck</automated>
    <automated>bash -c 'grep -qF "export { createBridge, captureSnapshot, offPageResult } from \"./bridge.js\";" packages/concierge/src/index.ts &amp;&amp; echo "export line: OK"'</automated>
    <automated>bash -c 'n=$(grep -c "not yet constructible" packages/concierge/src/index.ts); echo "hits: $n"; [ "$n" = "0" ]'</automated>
    <automated>node -e "const s=require('fs').readFileSync('packages/concierge/dist/index.d.ts','utf8');const m=s.match(/export\s*\{[^}]*\}/g);const l=m[m.length-1].replace(/export\s*\{/,'').replace(/\}/,'').split(',').map(x=>x.trim()).filter(Boolean);const t=l.filter(n=>n.startsWith('type ')).length;console.log(l.length,t,l.length-t);if(l.length!==65||t!==51||l.length-t!==14)process.exit(1)"</automated>
    <automated>node -e "import('./packages/concierge/dist/index.js').then(m=>{const r=[typeof m.createBridge,typeof m.captureSnapshot,typeof m.offPageResult].join(' ');console.log(r);if(r!=='function function function')process.exit(1)})"</automated>
  </verify>

  <done>
`src/index.ts` re-exports the three new values from `./bridge.js` in the values block, and its module
header no longer claims bridges are not constructible or that `createBridge` is still to come. After
`pnpm build`, `dist/index.d.ts` parses to 65 names / 51 types / 14 values and `dist/index.js` exports
all three as functions. `pnpm test` is now RED on `export-surface.test.ts` and `artifact.test.ts` —
expected, and closed by Tasks 2 and 3 of this same plan.
  </done>

  <acceptance_criteria>
    - `pnpm build` exits 0, including `publint --strict` and `attw` at `level: "error"`.
    - `pnpm typecheck` exits 0.
    - `src/index.ts` contains verbatim: `export { createBridge, captureSnapshot, offPageResult } from "./bridge.js";`
    - That line sits in the values block after `export { createConcierge } from "./concierge.js";` and is not inside any `export type {` block.
    - `grep -c "not yet constructible" packages/concierge/src/index.ts` returns **0**; baseline is 1. This is a **source** audit, not a `dist/` audit — the paragraph does not ship, measured.
    - `src/index.ts` still contains `is **not planned**` and the clause ending ``belongs to `createBridge` `` — the `defineStage` paragraph survives.
    - `src/index.ts` no longer contains the substring ``still to come is `createSession` and `createBridge` ``.
    - The built surface parses to exactly `65 51 14`.
    - `dist/index.js` exports all three names as `function`.
    - `pnpm test` is expected to FAIL here on `export-surface.test.ts` and `artifact.test.ts`. Record the failing assertions in the SUMMARY; do not "fix" them by reverting the export line.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 (05-03-T2): Move the ten pins in export-surface.test.ts and exports.test-d.ts</name>

  <read_first>
    - `packages/concierge/test/export-surface.test.ts` lines 1-50 (header, Trap 1, Trap 2) and lines 100-161 (the `VALUE_EXPORTS` array and the four `it` cases). Reproduced verbatim in `05-PATTERNS.md` lines 1013-1082.
    - `packages/concierge/test-d/exports.test-d.ts` lines 40-105 — the header sentence carrying three numbers, the shared import line, and the six existing predicates. Reproduced verbatim in `05-PATTERNS.md` lines 1086-1151.
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-PATTERNS.md` lines 72-102 — the pin table with exact old and new values.
  </read_first>

  <files>packages/concierge/test/export-surface.test.ts, packages/concierge/test-d/exports.test-d.ts</files>

  <action>
Move pins 1 through 10. Every number below is measured, not inferred. Do not recompute them.

IN `packages/concierge/test/export-surface.test.ts`:

Pin 7, `VALUE_EXPORTS` at lines 106-122. Append three entries after `"createConcierge"`, in this
order: `"createBridge"`, `"captureSnapshot"`, `"offPageResult"`. The array is maintained in SOURCE
order, not the alphabetised order the parser produces, and the assertion is `toContain` per entry so
order does not affect the test — but the array's length is the only thing the third `it` title's
number can be checked against, which the file's own comment at lines 101-105 states explicitly. Leave
the `CatalogValidationError` comment block where it is. Final length: **14**.

Pin 1, line 134's `it` title. `is exactly 62 names` becomes `is exactly 65 names`. Keep the rest of
the title, including the em-dash clause, unchanged.

Pin 2, line 136. `expect(names).toHaveLength(62)` becomes `toHaveLength(65)`.

Pin 3, line 139's `it` title. `splits 51 types to 11 values` becomes `splits 51 types to 14 values`.

Pin 4, line 141. `expect(types).toHaveLength(51)` is **UNCHANGED**. Phase 5 adds no type. Any other
number here is wrong.

Pin 5, line 142. `expect(values).toHaveLength(11)` becomes `toHaveLength(14)`.

Pin 6, line 145's `it` title. `carries all eleven runtime value exports by name` becomes
`carries all fourteen runtime value exports by name`. The number is spelled as a word, matching the
existing style.

Do not touch `readSurface()` (lines 74-99), the `EXPORT_BLOCK` regex (line 59), the `beforeAll` guard,
or the fourth `it` case about `serverChallengeBrand` and `ConsentAckBase`.

IN `packages/concierge/test-d/exports.test-d.ts`:

Pin 8, the header sentence at lines 53-61, which carries **three separate numbers**. All three move
from six to nine; missing one leaves prose that contradicts itself.

There is a **fourth stale number in this same header**, at line 63: "The two `Assignable` predicates
are deliberately loose about the signature". Measured, the file already has **four** `Assignable`
predicates today (`defineAction`, `buildCatalog`, `CatalogValidationError`, `createConcierge`), so the
sentence is already stale; this phase's three additions make it **seven**. Correct it to seven while
the paragraph is open. Every other stale sentence in this phase's scope is corrected in place, and
leaving this one is how a file ends up with prose nobody trusts.
  - Line 54, "Phase 4 added `createConcierge`, bringing the total to six." The total is now nine, and
    the sentence should name Phase 5's three additions the way it already names Phase 3's four and
    Phase 4's one.
  - Line 56, "counter-intuitive at six names as much as at one" becomes **nine** names.
  - Line 60, "The import is shared, so the line number is identical whichever of the six regressed"
    becomes whichever of the **nine**.

Pin 9, the shared import line at line 72. Add `createBridge`, `captureSnapshot` and `offPageResult`
to the existing six names. **It must stay on ONE line however long** — lines 47-49 state the reason:
`tsc` echoes only the line the failing type argument sits on, so a formatter-wrapped import destroys
the diagnostic. **Keep the trailing comment verbatim**, including its arrow:
`// ← index.js. NOT types.js. This is the whole point.` The import must remain from
`"../src/index.js"` and never from `"../src/types.js"` — that distinction is this file's entire
purpose and STATE.md records the Phase 1 deferred item it closes.

Pin 10, three new predicates at the end of the file under a new banner comment matching the existing
Phase 3 and Phase 4 banners in style, reading
`// Phase 5 — the three values this phase adds to the entrypoint`. Each predicate copies
`_createConciergeExportedAsValue`'s form exactly, with its own JSDoc line above it, each on ONE line:
  - `_createBridgeExportedAsValue` asserting `Assignable<typeof createBridge, (...args: never[]) => unknown>`
  - `_captureSnapshotExportedAsValue` asserting `Assignable<typeof captureSnapshot, (...args: never[]) => unknown>`
  - `_offPageResultExportedAsValue` asserting `Assignable<typeof offPageResult, (...args: never[]) => unknown>`

**Keep them deliberately loose.** Lines 63-69 forbid tightening: `(...args: never[]) => unknown`
asserts only "this is a function value", which is all the export-PLACEMENT guarantee needs. Tightening
duplicates `test-d/bridge.test-d.ts` (plan 05-06) and makes this file fail for reasons unrelated to
placement. Do not add a `MESSAGE_MAX_CHARS` predicate and do not import anything from
`../src/types.js` — RESEARCH § Q6 records that this would recreate the blind guard STATE.md warns
about.

Nothing in this file is exported and it carries zero suppression directives. Keep both properties.
  </action>

  <verify>
    <automated>pnpm build</automated>
    <automated>pnpm typecheck</automated>
    <automated>pnpm test -- export-surface</automated>
    <automated>bash -c 'f=packages/concierge/test/export-surface.test.ts; grep -qF "toHaveLength(65)" $f &amp;&amp; grep -qF "toHaveLength(51)" $f &amp;&amp; grep -qF "toHaveLength(14)" $f &amp;&amp; grep -qF "is exactly 65 names" $f &amp;&amp; grep -qF "splits 51 types to 14 values" $f &amp;&amp; grep -qF "all fourteen runtime value exports" $f &amp;&amp; echo "export-surface pins: OK"'</automated>
    <automated>bash -c 'f=packages/concierge/test-d/exports.test-d.ts; [ "$(grep -c "of the six\|total to six\|at six names" $f)" = "0" ] &amp;&amp; [ "$(grep -c "_createBridgeExportedAsValue\|_captureSnapshotExportedAsValue\|_offPageResultExportedAsValue" $f)" = "3" ] &amp;&amp; grep -qF "← index.js. NOT types.js. This is the whole point." $f &amp;&amp; echo "exports.test-d pins: OK"'</automated>
  </verify>

  <done>
All ten pins in these two files have moved together. `pnpm test -- export-surface` is green at
65/51/14 and `pnpm typecheck` is green with nine placement predicates on one shared import line.
`test/artifact.test.ts` is still red — Task 3 closes it.
  </done>

  <acceptance_criteria>
    - `pnpm test -- export-surface` passes all four `it` cases.
    - `pnpm typecheck` exits 0.
    - `export-surface.test.ts` contains `toHaveLength(65)`, `toHaveLength(51)` and `toHaveLength(14)`, each exactly once.
    - `export-surface.test.ts` contains the three updated `it` titles verbatim: `is exactly 65 names`, `splits 51 types to 14 values`, `carries all fourteen runtime value exports by name`.
    - `VALUE_EXPORTS` has exactly 14 string entries, ending `"createConcierge", "createBridge", "captureSnapshot", "offPageResult"` in that order.
    - `exports.test-d.ts` has **zero** occurrences of `total to six`, `at six names` and `of the six`. All three header numbers moved; leaving any one is the failure this criterion exists to catch.
    - `exports.test-d.ts` has **zero** occurrences of ``The two `Assignable` predicates``. That sentence was already stale at four before this phase and would be stale at seven after it.
    - `exports.test-d.ts` line 72 remains a single line, imports from `"../src/index.js"`, now names nine symbols, and still ends with the comment `// ← index.js. NOT types.js. This is the whole point.`
    - `exports.test-d.ts` defines exactly three new predicate aliases: `_createBridgeExportedAsValue`, `_captureSnapshotExportedAsValue`, `_offPageResultExportedAsValue`, each using `Assignable<…, (...args: never[]) => unknown>` and each on one line.
    - `exports.test-d.ts` contains no `@ts-expect-error`, no `expectTypeOf`, and no import from `../src/types.js`.
    - `grep -c "export " packages/concierge/test-d/exports.test-d.ts` — no top-level `export` statements are added; the file's aliases stay unexported.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3 (05-03-T3): Add the three per-export cases to artifact.test.ts</name>

  <read_first>
    - `packages/concierge/test/artifact.test.ts` in full — the header at lines 1-24 stating what escapes without the file, the `DIST_URL` binding, and all ten existing `it` cases.
    - `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-PATTERNS.md` lines 1154-1178 — the `artifact.test.ts` section, which reproduces the `createConcierge` case verbatim as the shape to copy three times, and names what each comment must say.
  </read_first>

  <files>packages/concierge/test/artifact.test.ts</files>

  <action>
This file is pin 11, and `05-PATTERNS.md` § "Files CONTEXT and RESEARCH Missed" flags it as absent
from both upstream artifacts' file lists. Its convention is **one `it` per shipped value export**, and
all eleven are covered today. Three new value exports means three new `it` cases or the convention
breaks silently.

Add three cases after the existing `createConcierge` case (lines 129-139), copying its shape exactly:
each `it` re-imports `DIST_URL.href` inside its own body rather than hoisting the namespace — keep
that — and each asserts `expect(typeof m.<name>).toBe("function")`.

Each case's comment must name **what specifically is lost** if the export regresses into the
`export type { … }` block, and **how the failure reads to a consumer**. That is the existing
convention, visible in every case in the file; a comment that merely restates the assertion breaks it.

  - `createBridge` — the loss is that every registry becomes unconstructible and every stage's
    `bridge` is permanently absent. The symptom is `bridge: null` forever on a page that is definitely
    open, which CONTEXT calls the single most undebuggable failure in the design, because the page is
    visibly present and nothing in the app reports an error.
  - `captureSnapshot` — the loss is that the detachment path becomes unreachable from the published
    entrypoint, so Phase 8's consent kernel would have to re-implement it and BRG-05 would be proven
    only against source that no consumer can call. The symptom is a consumer who supplies no
    `normalizeSnapshot` silently storing live proxy views.
  - `offPageResult` — the loss is that every handler hand-writes its own off-page sentence, which is
    the outcome DX-03 exists to prevent, and the 180-character bound stops being shared with Phase 6's
    SEC-06 truncation. The symptom is inconsistent, unbounded messages reaching the agent.

Also update the file header at lines 1-24 if it states a count of covered exports. Read it first; if it
names a number, move it to fourteen, and if it does not, leave the header alone rather than inventing a
number for it.

Do not change any existing case. Do not add a `beforeAll` or hoist the import.
  </action>

  <verify>
    <automated>pnpm build</automated>
    <automated>pnpm test -- artifact</automated>
    <automated>pnpm test</automated>
    <automated>pnpm typecheck</automated>
    <automated>bash -c 'f=packages/concierge/test/artifact.test.ts; n=$(grep -c "^  it(" $f); echo "it cases: $n"; [ "$n" = "13" ]'</automated>
    <automated>bash -c 'f=packages/concierge/test/artifact.test.ts; grep -qF "m.createBridge" $f &amp;&amp; grep -qF "m.captureSnapshot" $f &amp;&amp; grep -qF "m.offPageResult" $f &amp;&amp; echo "three new cases present: OK"'</automated>
  </verify>

  <done>
`test/artifact.test.ts` carries one `it` per shipped value export again — 13 cases covering all
fourteen value exports (the frozen-constants case covers three at once). The full suite is green:
`pnpm build && pnpm test && pnpm typecheck` all pass, and the export surface is 65/51/14.
  </done>

  <acceptance_criteria>
    - `pnpm test` passes the WHOLE suite with no failures. This is the first point in the phase where the full suite is green again after Task 1 deliberately reddened it.
    - `pnpm typecheck` exits 0.
    - `test/artifact.test.ts` has exactly 13 `it` cases at top-of-describe indentation (`grep -c "^  it("` returns 13; baseline 10).
    - The file contains `m.createBridge`, `m.captureSnapshot` and `m.offPageResult`, each asserted with `toBe("function")`.
    - Each new case re-imports `DIST_URL.href` inside its own `it` body — `grep -c "await import(DIST_URL.href)"` increases by exactly 3.
    - Each new case carries a comment naming the specific loss and the consumer-visible symptom, not a restatement of the assertion.
    - No existing case is modified: `git diff` on this file shows only additions plus, at most, one header-number edit.
    - Final state check for the whole plan: `pnpm build && pnpm test && pnpm typecheck` all exit 0, and `git diff --exit-code` at the repo root is clean after commit — the containment `mutate-and-prove.sh`'s Known Limitation 1 depends on.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core → the published `dist/index.d.ts` and `dist/index.js` | The export surface is the contract. A value silently filed as a type is a runtime `TypeError` in the consumer's module scope |
| core → consumers reading the module header | Shipped and source prose that overstates or understates what exists misleads adopters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering | the published export surface — a build-config change silently adding or dropping an export | mitigate | `test/export-surface.test.ts` pins 65 names / 51 types / 14 values against the built `.d.ts`, plus a per-name `toContain` loop over the 14-entry `VALUE_EXPORTS` array |
| T-05-11 | Denial of service | `createBridge` re-exported through the `export type { … }` block, erasing the runtime binding under `verbatimModuleSyntax` | mitigate | `test-d/exports.test-d.ts` placement predicates on a shared import line from `../src/index.js`; diagnostic is TS1485 at the import line. Mutants P-05-1 and P-05-2. Backed at runtime by the three new `artifact.test.ts` cases asserting `typeof` is `function` in `dist/index.js` |
| T-05-01 | Tampering | the registry capability reaching consumers at all | mitigate | Without this barrel, `createBridge` is unreachable and the frozen-capability guarantee is untestable at the layer this project tests at |
| T-05-12 | Repudiation | shipped/source prose claiming bridges are not constructible after they are | mitigate | Edit B, audited by `grep -c "not yet constructible"` over `src/index.ts` — the correct target, since the paragraph does not reach `dist/` (measured) |
| T-05-SC | Tampering | npm/pnpm package installs | accept | Zero installs in this plan and this phase. `pnpm check:deps` asserts the empty external module graph |
</threat_model>

<verification>
1. `pnpm build && pnpm test && pnpm typecheck` — all green at the end of Task 3.
2. `dist/index.d.ts` parses to `65 51 14` with the test's own regex.
3. `dist/index.js` exports `createBridge`, `captureSnapshot`, `offPageResult` as functions.
4. `grep -c "not yet constructible" packages/concierge/src/index.ts` returns 0.
5. All eleven pins moved: verified by the two grep gates in Tasks 2 and 3 plus the `it`-count check.
6. `git diff --exit-code` clean at the repo root after commit.
</verification>

<success_criteria>
- The export surface is 65 names / 51 types / 14 values, and every pin that names one of those
  numbers agrees with it.
- The whole suite is green — no plan after this one starts from a red baseline.
- Wave 3's runtime suites can now import `createBridge`, `captureSnapshot` and `offPageResult` from
  `../dist/index.js`.
</success_criteria>

<output>
Create `.planning/phases/05-bridge-registry-and-the-no-bridge-path/05-03-SUMMARY.md` when done.

The SUMMARY must record:
- The final parsed surface numbers, taken from the built artifact rather than asserted from the plan.
- Each of the eleven pins with its before and after value, as a checklist, so plan 05-07's gate can
  confirm none was skipped.
- Confirmation that `pnpm test` was red after Task 1 and green after Task 3, with the names of the
  cases that were red — evidence the pins were genuinely load-bearing rather than already correct.
</output>
