---
phase: 10-close-v0-1-release-certification-and-evidence-gaps
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase-09-package-check.mjs
  - scripts/phase-09-contract-check.mjs
  - scripts/phase-09-mutation-battery.mjs
  - .gitignore
  - examples/adapter-ssr/.astro/content.d.ts
  - examples/adapter-ssr/.astro/types.d.ts
autonomous: true
requirements: [ADP-01, ADP-02, ADP-03, ADP-04, PKG-04]
must_haves:
  truths:
    - "The public `pnpm run check:phase09:packages` path accepts only pnpm's exact benign `PNPM_CONFIG_VERIFY_DEPS_BEFORE_RUN=false` decoration at the parent boundary and strips it from ordinary children per Audit 1."
    - "Authenticated mutation children alone may retain the exact false policy; wrong values, case-folded duplicates, ambient credentials/config, hooks, and caller-selected stores remain rejected per T-10-04."
    - "The two Astro declarations are untracked generated state, exactly `/examples/adapter-ssr/.astro/` is ignored, and no `.astro` byte appears in a release input or seal per D-10-05/D-10-06."
    - "A disposable clean checkout starts without `.astro`, regenerates it with pinned Astro check/build, and still has zero tracked generated paths per D-10-07/D-10-08 and Audit 2."
  artifacts:
    - path: "scripts/phase-09-package-check.mjs"
      provides: "Ordinary-parent versus authenticated-mutation-child environment classification"
      contains: "PNPM_CONFIG_VERIFY_DEPS_BEFORE_RUN"
    - path: ".gitignore"
      provides: "Harness-scoped Astro generated-state exclusion"
      contains: "/examples/adapter-ssr/.astro/"
    - path: "scripts/phase-09-mutation-battery.mjs"
      provides: "Clean-baseline regeneration and no-release-input assertions"
      contains: "examples/adapter-ssr/.astro"
  key_links:
    - from: "package.json"
      to: "scripts/phase-09-package-check.mjs"
      via: "authoritative `pnpm run check:phase09:packages` entry point"
      pattern: "check:phase09:packages"
    - from: "scripts/phase-09-mutation-battery.mjs"
      to: "git ls-files -z"
      via: "single tracked release-input model excludes untracked Astro state"
      pattern: "releaseInputPaths"
---

<objective>
Repair the real Phase 9 package gate and remove Astro-generated state from tracked release authority, closing Audit 1 and Audit 2 without weakening hostile-environment or seal controls.

Purpose: The commands CI/release actually invoke must pass under ordinary pnpm, and reproducible Astro behavior—not cached generated bytes—must be the release proof.
Output: Exact environment classification, discriminating self/static tests, scoped generated-state exclusion, and clean-checkout regeneration assertions.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/v0.1-MILESTONE-AUDIT.md
@.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-CONTEXT.md
@.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-RESEARCH.md
@.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-PATTERNS.md
@.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md
@.planning/phases/09-react-and-svelte-adapters/09-VALIDATION.md
@.planning/phases/09-react-and-svelte-adapters/09-13-SUMMARY.md
@scripts/phase-09-secure-environment.mjs
@package.json
@examples/adapter-ssr/package.json

<interfaces>
From scripts/phase-09-package-check.mjs:
- `sourceEnvironmentValue(source, expectedName)` performs exact case-folded lookup and rejects duplicate spellings.
- `mutationRunnerPnpmChildOverride(source)` currently requires `PHASE09_CREDENTIAL_FREE_ENV=1` for any pnpm policy.
- `createPackageChildEnvironment(root, source, options)` must remain the only child-environment constructor.

From scripts/phase-09-mutation-battery.mjs:
- `releaseInputPaths()` derives one sorted path set from `git ls-files -z`.
- `GENERATED_PATHS` are generator outputs, not release inputs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 10-03-01: Consume benign pnpm decoration without granting mutation authority</name>
  <files>scripts/phase-09-package-check.mjs, scripts/phase-09-contract-check.mjs</files>
  <read_first>
    <file>scripts/phase-09-package-check.mjs</file>
    <file>scripts/phase-09-secure-environment.mjs</file>
    <file>scripts/phase-09-contract-check.mjs</file>
    <file>package.json</file>
    <file>.planning/phases/09-react-and-svelte-adapters/09-SECURITY.md</file>
    <file>.planning/v0.1-MILESTONE-AUDIT.md</file>
    <file>.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-CONTEXT.md</file>
    <file>.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-RESEARCH.md</file>
  </read_first>
  <behavior>
    - "Unmarked exact `PNPM_CONFIG_VERIFY_DEPS_BEFORE_RUN=false` is accepted only as parent decoration and is absent in an ordinary spawned child."
    - "`PHASE09_CREDENTIAL_FREE_ENV=1` plus exact false is retained through the authenticated disposable mutation child."
    - "Wrong values, case-folded duplicate keys, unmarked non-false authority, credentials, config paths, hooks, capture paths, and caller store roots are rejected or stripped."
  </behavior>
  <action>
Split benign parent consumption from authenticated child retention while keeping `sourceEnvironmentValue()` and `createPackageChildEnvironment()` as the single parser/construction boundaries. Classify exactly one unmarked `PNPM_CONFIG_VERIFY_DEPS_BEFORE_RUN=false` as ordinary pnpm decoration: accept it at the package-check parent, omit it from the secure ordinary child, and grant no mutation authority. Retain the exact false value only when the separately parsed `PHASE09_CREDENTIAL_FREE_ENV` is exactly `1`. Reject a true/empty/other policy, duplicate case variants, or an invalid marker. Never spread ambient `process.env`; preserve owned HOME, empty npm/git configs, public registry, owned pnpm store, credential stripping, NODE_OPTIONS stripping, capture-path stripping, and repository-realpath rejection.

Invert the current unauthenticated-false self-test into accepted-parent/absent-child and prove it through a real child process. Preserve authenticated retention and all hostile controls. Update the contract checker to pin the exact classification/strip/retain predicates and add a negative fixture whose wrong implementation either leaks ordinary false into a child or treats it as authenticated authority. The final evidence command is the public `pnpm run check:phase09:packages`, never direct Node execution.
  </action>
  <verify>
    <automated>node scripts/phase-09-package-check.mjs self-test &amp;&amp; node scripts/phase-09-contract-check.mjs final &amp;&amp; pnpm run check:phase09:packages</automated>
  </verify>
  <acceptance_criteria>
    - "The exact ordinary false value is accepted at the parent and absent in the observed ordinary child environment; it cannot authenticate a mutation child (Audit 1; T-10-04 HIGH)."
    - "The exact marker `PHASE09_CREDENTIAL_FREE_ENV=1` plus exact false is retained in the observed authenticated child, and no other reviewed authority is retained."
    - "Wrong policy values and case-folded duplicate spellings fail with `CHILD_ENVIRONMENT`; caller-selected stores/configs/hooks/credentials never reach child output."
    - "`node scripts/phase-09-package-check.mjs self-test`, the contract checker, and `pnpm run check:phase09:packages` all exit 0."
    - "`scripts/phase-09-secure-environment.mjs` remains byte-identical unless the package boundary cannot express the locked behavior; any edit to it requires rerunning every existing secure-environment self-test."
  </acceptance_criteria>
  <done>The ordinary pnpm entry point passes without turning ambient configuration into mutation or filesystem authority.</done>
</task>

<task type="auto">
  <name>Task 10-03-02: Untrack Astro state and prove clean pinned regeneration</name>
  <files>.gitignore, examples/adapter-ssr/.astro/content.d.ts, examples/adapter-ssr/.astro/types.d.ts, scripts/phase-09-mutation-battery.mjs</files>
  <read_first>
    <file>.gitignore</file>
    <file>examples/adapter-ssr/.astro/content.d.ts</file>
    <file>examples/adapter-ssr/.astro/types.d.ts</file>
    <file>examples/adapter-ssr/package.json</file>
    <file>pnpm-lock.yaml</file>
    <file>scripts/phase-09-mutation-battery.mjs</file>
    <file>.planning/phases/09-react-and-svelte-adapters/09-RELEASE-EVIDENCE.json</file>
    <file>.planning/v0.1-MILESTONE-AUDIT.md</file>
    <file>.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-CONTEXT.md</file>
    <file>.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-RESEARCH.md</file>
  </read_first>
  <action>
Implement generated-state handling exactly per D-10-05, D-10-06, D-10-07, and D-10-08.
Delete the two tracked Astro-generated declarations without preserving or editing their bytes. Add exactly `/examples/adapter-ssr/.astro/` to the root `.gitignore`; do not add a repository-wide `.astro/` pattern. Extend the existing Phase 9 generator's disposable-checkout proof so the baseline asserts `examples/adapter-ssr/.astro` is absent, runs the pinned package-local `check` and then `build`, proves regeneration succeeds, and finally requires `git ls-files -- examples/adapter-ssr/.astro` to return zero paths. Keep `releaseInputPaths()` based on tracked files and add explicit rejection if any harness-local `.astro/` path appears in a release manifest, digest table, or seal. Never compare or hash generated declaration bytes.
  </action>
  <verify>
    <automated>test -z "$(git ls-files -- examples/adapter-ssr/.astro)" &amp;&amp; grep -Fxq '/examples/adapter-ssr/.astro/' .gitignore</automated>
    <automated>scratch=$(mktemp -d) &amp;&amp; git clone --quiet --no-hardlinks . "$scratch/repo" &amp;&amp; test ! -e "$scratch/repo/examples/adapter-ssr/.astro" &amp;&amp; corepack pnpm --dir "$scratch/repo" install --frozen-lockfile &amp;&amp; corepack pnpm --dir "$scratch/repo" --filter @fullselfbrowsing/concierge-adapter-ssr check &amp;&amp; corepack pnpm --dir "$scratch/repo" --filter @fullselfbrowsing/concierge-adapter-ssr build &amp;&amp; test -z "$(git -C "$scratch/repo" ls-files -- examples/adapter-ssr/.astro)"</automated>
  </verify>
  <acceptance_criteria>
    - "Both former declaration paths are absent from `git ls-files`; the only new ignore entry is exactly `/examples/adapter-ssr/.astro/` (D-10-05/D-10-06)."
    - "A disposable clean clone starts with no `.astro`, pinned Astro check and build both exit 0, generated state may appear locally, and Git still tracks zero harness-local `.astro` paths (D-10-07/D-10-08)."
    - "No `.astro` path or generated declaration digest appears in the prospective release input manifest/seal (Audit 2; T-10-06 HIGH)."
    - "No generated bytes are copied, normalized, compared, or made authoritative."
  </acceptance_criteria>
  <done>Astro state is rebuildable, harness-scoped, untracked, unsealed, and proven from an initially absent clean checkout.</done>
</task>

</tasks>

<source_coverage_audit>
| Source | Item | Status | Task |
|---|---|---|---|
| AUDIT | Audit 1 ordinary pnpm gate | COVERED | 10-03-01 |
| AUDIT | Audit 2 generated Astro state | COVERED | 10-03-02 |
| CONTEXT | D-10-05 through D-10-08 | COVERED | 10-03-02 |
| RESEARCH | Real pnpm caller differs from direct Node | COVERED | 10-03-01 |
| RESEARCH | Clean pinned regeneration, no generated-byte seal | COVERED | 10-03-02 |
| SECURITY | T-10-04/T-10-06 | COVERED | 10-03-01/02 |
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| ambient pnpm process → secure child | Benign decoration must be consumed without becoming authority. |
| generated Astro directory → tracked release inputs | Local tool output must not enter Git or release seals. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-04 | Elevation of Privilege / Tampering | HIGH | package child environment | mitigate | Exact-value/marker classification, case-fold duplicate rejection, narrow owned child environment, real-child observations, and hostile negative fixtures. |
| T-10-06 | Tampering / Repudiation | HIGH | Astro harness and release manifest | mitigate | Scoped ignore, remove tracked files, clean-clone absence/regeneration proof, zero tracked paths, and explicit no-seal assertion. |
</threat_model>

<verification>
Run both task commands, then `pnpm run check:phase09:packages` and the mutation generator's prospective input verification. Plan 05 supplies named M-10-E01/M-10-G01 mutants and final evidence.
</verification>

<success_criteria>
- Audit 1 and Audit 2 are closed through the real public commands.
- D-10-05 through D-10-08 are implemented exactly.
- T-10-04/T-10-06 HIGH mitigations retain all existing hostile controls.
- The release manifest contains tracked source/config/evidence inputs and no Astro cache byte.
</success_criteria>

<output>
Create `.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-03-SUMMARY.md` when done.
</output>
