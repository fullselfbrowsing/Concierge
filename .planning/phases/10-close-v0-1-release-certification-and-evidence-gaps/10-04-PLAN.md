---
phase: 10-close-v0-1-release-certification-and-evidence-gaps
plan: 04
type: execute
wave: 2
depends_on: [10-03]
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - scripts/phase-09-workflow-check.mjs
  - scripts/phase-10-certify-candidate.mjs
autonomous: true
requirements: [ADP-01, ADP-02, ADP-03, ADP-04, PKG-04]
must_haves:
  truths:
    - "A clean Ubuntu checkout installs, builds workspace packages, then aggregate-typechecks, tests, and runs the exact Phase 9 release chain; the observed build-before-typecheck failure is structurally impossible."
    - "Static workflow checks reject a fixture containing every required command in the former wrong order, not merely a missing command."
    - "After Plan 03's deletion commit, a disposable clean clone begins without `.astro`, regenerates with pinned check/build, and retains zero tracked or sealed Astro-generated paths."
    - "Hosted CI emits a run-scoped receipt with repository, workflow, ref, exact SHA, run ID, attempt, truthful overall conclusion, and required job conclusions, while no run ID is committed."
    - "`id-token: write` remains scoped only to the separately approved release publish job; Phase 10 CI/certification has no publish or registry-write step per D-10-10/T-10-08."
    - "The certification CLI accepts only the fully closed post-GSD handoff, explicitly pushes the clean current HEAD, confirms remote/local SHA equality, validates one explicit exact-SHA successful run/attempt and matching receipt/jobs, and leaves worktree/HEAD unchanged."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Hosted build-before-typecheck gate and external candidate receipt artifact"
      contains: "candidate-certification"
    - path: ".github/workflows/release.yml"
      provides: "Corrected pre-publication verify order with unchanged publish-only OIDC scope"
      contains: "id-token: write"
    - path: "scripts/phase-09-workflow-check.mjs"
      provides: "Positive structural pins and wrong-order/OIDC/publication negative fixtures"
      contains: "build-before-typecheck"
    - path: "scripts/phase-10-certify-candidate.mjs"
      provides: "No-repository-write exact-SHA hosted receipt verifier"
      contains: "github.run_attempt"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pnpm run check:phase09:release"
      via: "hosted Ubuntu candidate gate after build/typecheck/test"
      pattern: "check:phase09:release"
    - from: "scripts/phase-10-certify-candidate.mjs"
      to: ".github/workflows/ci.yml"
      via: "workflow run ID/head SHA/attempt and downloaded receipt validation"
      pattern: "ci.yml"
---

<objective>
Repair clean-checkout workflow order and install exact-SHA external certification machinery, closing the hosted portion of Audit 9 without performing publication.

Purpose: The real Ubuntu gate exposed build-before-typecheck as load-bearing, and certification must bind one immutable candidate to one external run without a self-invalidating receipt commit.
Output: Corrected CI/release verification, discriminating static controls, run-scoped receipt upload, and a dependency-free no-repository-write certification CLI with an explicit authenticated push gate.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/v0.1-MILESTONE-AUDIT.md
@.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-CONTEXT.md
@.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-RESEARCH.md
@.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-PATTERNS.md
@.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md
@scripts/phase-09-package-check.mjs
@scripts/phase-09-mutation-battery.mjs
@package.json
@RELEASING.md

<interfaces>
Current required clean-checkout order:
- `corepack pnpm install --frozen-lockfile`
- `pnpm build`
- `pnpm typecheck`
- `pnpm test`
- `pnpm run check:phase09:release`

Required receipt identity:
- repository, workflow path/name, head SHA, ref, run ID, run attempt, overall conclusion, and every required job conclusion.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 10-04-01: Prove committed Astro regeneration and make hosted verification build first</name>
  <files>.github/workflows/ci.yml, .github/workflows/release.yml, scripts/phase-09-workflow-check.mjs</files>
  <read_first>
    <file>.github/workflows/ci.yml</file>
    <file>.github/workflows/release.yml</file>
    <file>scripts/phase-09-workflow-check.mjs</file>
    <file>package.json</file>
    <file>packages/concierge-react/package.json</file>
    <file>packages/concierge-svelte/package.json</file>
    <file>.planning/phases/09-react-and-svelte-adapters/09-CONTEXT.md</file>
    <file>.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-RESEARCH.md</file>
  </read_first>
  <action>
Implement hosted exact-candidate verification without publication per D-10-09 and D-10-10.
First consume Plan 03's committed output, not its former uncommitted worktree. Run `node scripts/phase-09-mutation-battery.mjs verify astro-regeneration` from this dependent plan and require its disposable clone to begin with no `examples/adapter-ssr/.astro`, run the pinned package-local Astro `check` then `build`, and retain zero tracked or release-input `.astro` paths per D-10-05 through D-10-08. This is the authoritative clean-snapshot proof; do not reconstruct or compare generated bytes.
In CI and the nonpublishing release `verify` job, preserve pinned checkout/setup/tooling and frozen install, then enforce `pnpm build` before aggregate `pnpm typecheck`, followed by `pnpm test` and the Phase 9 release/static chain. CI must call `pnpm run check:phase09:release` so the hosted candidate exercises the same package, evidence, workflow, archive, and Node-floor gates used for certification. Keep exact checkout-SHA binding and existing artifact flow. Do not run the release `publish` job or add any npm command to CI.

Update `validateCi()` and `validateRelease()` to compare command indices and reject duplicates/out-of-order commands. Add separate synthetic controls where install/build/typecheck/test/release commands all exist but typecheck precedes build, for both CI and release. Preserve action SHA pins, failure propagation, Node 22.12.0 floor, archive identity, permissions, and every existing negative control.
  </action>
  <verify>
    <automated>test -z "$(git ls-files -- examples/adapter-ssr/.astro)" &amp;&amp; grep -Fxq '/examples/adapter-ssr/.astro/' .gitignore &amp;&amp; node scripts/phase-09-workflow-check.mjs</automated>
    <automated>node -e 'const fs=require("node:fs");for(const f of [".github/workflows/ci.yml",".github/workflows/release.yml"]){const s=fs.readFileSync(f,"utf8");const b=s.indexOf("pnpm build"),t=s.indexOf("pnpm typecheck");if(b&lt;0||t&lt;0||b&gt;t)process.exit(1)}' &amp;&amp; node scripts/phase-09-mutation-battery.mjs verify astro-regeneration</automated>
  </verify>
  <acceptance_criteria>
    - "Both clean-checkout verification paths contain frozen install → build → aggregate typecheck → test in that exact order, closing hosted run 31513847865's failure."
    - "The post-Plan-03 disposable clone begins without `.astro`, pinned check/build succeeds, and no generated path becomes tracked or sealed (D-10-05 through D-10-08; Audit 2)."
    - "CI executes `pnpm run check:phase09:release` and no publish, `npm publish`, registry write, release tag, or provenance inspection."
    - "A wrong-order CI fixture and wrong-order release fixture each contain every required command yet fail the static checker with their own identifying control."
    - "All existing action-pin, failure-propagation, Node-floor, exact-archive, permission, and OIDC negative controls remain green."
  </acceptance_criteria>
  <done>Clean Ubuntu verification can resolve built core declarations before adapter typecheck and still blocks on the complete release-check chain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 10-04-02: Emit and verify an exact-SHA run-scoped certification receipt</name>
  <files>.github/workflows/ci.yml, scripts/phase-09-workflow-check.mjs, scripts/phase-10-certify-candidate.mjs</files>
  <read_first>
    <file>.github/workflows/ci.yml</file>
    <file>.github/workflows/release.yml</file>
    <file>scripts/phase-09-workflow-check.mjs</file>
    <file>scripts/phase-09-secure-environment.mjs</file>
    <file>.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-CONTEXT.md</file>
    <file>.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-RESEARCH.md</file>
    <file>RELEASING.md</file>
  </read_first>
  <behavior>
    - "Receipt fixtures fail on a mismatched repository, workflow, ref, head SHA, run ID, attempt, `overall_conclusion`, missing job, or non-success job conclusion."
    - "Push-order fixtures fail if dispatch/selection occurs before the explicit authenticated push and remote-SHA equality recheck."
    - "A matching explicit run and receipt pass while HEAD and repository status remain unchanged."
    - "OIDC/publication fixture violations remain rejected."
  </behavior>
  <action>
Implement the run-scoped external receipt per D-10-09, D-10-10, and D-10-11.
Add `workflow_dispatch` to CI and a final `candidate-certification` job depending on every required CI job. Have that job create a small JSON receipt from GitHub context/`needs` values and upload it under an attempt-scoped name containing `${{ github.sha }}`, `${{ github.run_id }}`, and `${{ github.run_attempt }}`. Include repository, workflow, ref, head SHA, run ID, attempt, schema/version, an explicit truthful `overall_conclusion`, and a required-job conclusion map. For a receipt-producing successful run, `overall_conclusion` must be exactly `success`; any non-success prerequisite makes the job fail closed rather than writing a success receipt. Include digests of the tracked Phase 10 verifier, milestone audit, validation, and certification runbook so the external artifact identifies the exact evidence bytes in the candidate. Do not commit a run ID, grant `id-token`, add checkout to the release publish job, or expose secrets.

Create dependency-free `scripts/phase-10-certify-candidate.mjs` with `self-test`, `handoff-check`, `certify`, and `verify-run <sha> <run-id> <attempt>` modes. `self-test` uses fixtures for every identity/result mismatch, every supported audit/status parse, and push/remote/dispatch ordering, and verifies no filesystem write outside a `mkdtemp` root. `handoff-check` is read-only and requires seven Phase 10 PLANs with seven SUMMARYs, an independently generated committed `10-VERIFICATION.md` whose supported status is `gaps_found` solely for the hosted exact-SHA gate, a committed milestone audit whose supported status is `gaps_found` with separately parsed 62/62 requirements, 9/9 original phases, 10/10 current directories, 12/12 integrations, 10/10 flows, and Phase 9 Nyquist compliance, plus the explicit ROADMAP certification-status line and STATE session marker saying the exact-SHA receipt is still awaited. The ROADMAP progress cell may say all plans are complete; that is not a phase-goal pass. Reject only a phase-completion marker or tracked hosted-success claim.

`certify` first runs `handoff-check`, records current HEAD/branch/full status, and requires an entirely clean tracked and untracked worktree. It then performs an explicit authenticated `git push --porcelain <configured-remote> HEAD:refs/heads/<current-branch>`; a failed/non-fast-forward push aborts. Refetch the exact remote branch and require its SHA to equal the unchanged local HEAD before dispatching or awaiting CI. Trigger or select one explicit `ci.yml` run for that exact SHA, wait for completion, require the run API's overall conclusion and every required job conclusion to be `success`, download the unique matching attempt-scoped receipt into a temporary directory, and compare every receipt identity, `overall_conclusion`, job conclusion, and tracked-evidence digest against the API/candidate. Delete the temporary directory, refetch remote SHA, and reassert remote SHA = local HEAD, unchanged HEAD, and empty full status. `verify-run` accepts only the explicitly supplied identity tuple, refetches that run and receipt, applies the same overall/job/evidence checks, and reasserts local/remote exact SHA and empty worktree without triggering a run or pushing. Both hosted modes print the candidate SHA, run ID, attempt, URL, overall/job conclusions, artifact name, and receipt digest to stdout only. The script never stages, commits, tags, publishes, edits repository files, invokes GSD mutation handlers, or chooses “latest successful” without exact SHA/run selection. Extend the workflow checker with receipt-schema/artifact/permissions pins and negative fixtures for omitted/misreported `overall_conclusion`, broadened OIDC, a publish command in CI, and checkout in the release publish job.
  </action>
  <verify>
    <automated>node scripts/phase-10-certify-candidate.mjs self-test &amp;&amp; node scripts/phase-09-workflow-check.mjs</automated>
  </verify>
  <acceptance_criteria>
    - "The CI receipt contains exact repository, workflow, ref, head SHA, run ID, run attempt, schema version, and required job results; its artifact name is SHA/run/attempt-scoped (T-10-07 HIGH)."
    - "The receipt itself contains `overall_conclusion: success`, and hosted verification compares that field plus every required job conclusion to the selected run/attempt API metadata (T-10-07 HIGH)."
    - "External `certify` explicitly pushes the final clean branch, then requires remote SHA = unchanged local HEAD before run dispatch/selection and again after receipt verification."
    - "Every mismatch fixture fails with its own identity/result diagnostic; a fully matching fixture exits 0."
    - "`certify` refuses a dirty worktree, remote-SHA mismatch, ambiguous run/artifact, non-success conclusion, missing job, or any receipt mismatch, and asserts HEAD/status again before exit."
    - "CI has no `id-token: write`, npm publication, tag, registry provenance inspection, or secret echo; release keeps `id-token: write` only on `publish` and no checkout in that job (D-10-10; T-10-08 HIGH)."
    - "Task verification is local/self-test only; no hosted run or publication occurs before the final tracked candidate is frozen."
  </acceptance_criteria>
  <done>The repository can later certify one exact clean SHA through an external receipt without creating a successor commit or broadening publish authority.</done>
</task>

</tasks>

<source_coverage_audit>
| Source | Item | Status | Task |
|---|---|---|---|
| AUDIT | Audit 2 committed clean-clone Astro regeneration | COVERED | 10-04-01 |
| AUDIT | Audit 9 hosted gate infrastructure | COVERED | 10-04-01/02 |
| CONTEXT | D-10-05 through D-10-08 committed-snapshot proof | COVERED | 10-04-01 |
| CONTEXT | D-10-09/D-10-10 | COVERED | 10-04-01/02 |
| CONTEXT | D-10-11 exact clean commit mechanics | COVERED | 10-04-02; executed by the post-GSD runbook created in Plan 07 |
| RESEARCH | Hosted Ubuntu build-before-typecheck failure | COVERED | 10-04-01 |
| RESEARCH | Exact-SHA external receipt | COVERED | 10-04-02 |
| SECURITY | T-10-07/T-10-08 | COVERED | 10-04-02 |
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| tracked candidate → hosted Ubuntu | Workflow order and exact SHA determine what actually runs. |
| GitHub run/artifact → local certification | External metadata must bind one explicit run, attempt, SHA, and job set. |
| release verify → publish job | OIDC authority must not leak into candidate verification. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-07 | Repudiation / Tampering | HIGH | CI receipt and certification CLI | mitigate | Exact SHA/run/attempt/artifact/job binding, dirty/remote mismatch rejection, pre/post HEAD/status assertions, no tracked receipt. |
| T-10-08 | Elevation of Privilege | HIGH | CI/release permissions | mitigate | CI has no OIDC/publish; release `id-token: write` remains publish-job-only; static negatives reject broad permissions, checkout, tokens, or publication drift. |
</threat_model>

<verification>
Run both self/static commands and inspect the workflow command indices. Task 10-04-01 also runs the committed-snapshot Astro proof after a fast path/status preflight. The actual hosted `certify` mode is deliberately reserved for the post-GSD runbook after every tracked SUMMARY/verifier/audit/bookkeeping byte is committed.
</verification>

<success_criteria>
- The clean-checkout order is frozen install → build → typecheck → test → release checks.
- Exact-SHA receipt infrastructure and mismatch self-tests are green.
- T-10-07/T-10-08 HIGH mitigations are structurally enforced.
- No package publication, provenance inspection, tag, or registry mutation occurs.
</success_criteria>

<output>
Create `.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-04-SUMMARY.md` when done.
</output>
