---
phase: 10-close-v0-1-release-certification-and-evidence-gaps
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/concierge/src/catalog.ts
  - packages/concierge/src/contract.ts
  - packages/concierge/test/catalog.test.ts
  - packages/concierge/test-d/catalog.test-d.ts
  - .planning/phases/03-action-declaration-and-build-time-validation/03-VERIFICATION.md
  - .planning/phases/04-stages-catalog-assembly-and-explain/04-VERIFICATION.md
autonomous: true
requirements: [CAT-02, CAT-06, SEC-03, DX-03]
must_haves:
  truths:
    - "`buildCatalog([null])` and every non-object declaration produce an ordinary aggregated, index-addressed `CatalogValidationError`, never a raw TypeError, per D-10-13 and Audit 4."
    - "The new diagnostic has one stable distinct code, a truthful `declaration at index N` subject, a nonempty problem, and an actionable fix; no synthetic action name is invented per D-10-14."
    - "Current built-byte S15a/S15b/S15c evidence proves schema accessors never execute and explicit/derived schemas remain detached; source snapshot logic is unchanged if the probe passes per Audit 5."
    - "Inaccurate shipping comments are corrected while historical Phase 3/4 verification remains append-only per D-10-15/D-10-16."
  artifacts:
    - path: "packages/concierge/src/catalog.ts"
      provides: "Indexed declaration precheck and precise current shipping comments"
      contains: "invalid_declaration"
    - path: "packages/concierge/test/catalog.test.ts"
      provides: "Mixed malformed/ordinary aggregate diagnostic regression"
      contains: "declaration at index"
    - path: "packages/concierge/test-d/catalog.test-d.ts"
      provides: "Bidirectional exact CatalogIssueCode union pin"
      contains: "invalid_declaration"
    - path: ".planning/phases/04-stages-catalog-assembly-and-explain/04-VERIFICATION.md"
      provides: "Append-only SEC-03 and dispatch-stub correction addendum"
      contains: "Phase 10 correction addendum"
  key_links:
    - from: "packages/concierge/src/catalog.ts"
      to: "CatalogValidationError.issues"
      via: "pre-property-access issue appended to the existing aggregate"
      pattern: "invalid_declaration"
    - from: ".planning/phases/04-stages-catalog-assembly-and-explain/04-VERIFICATION.md"
      to: "packages/concierge/test/concierge.test.ts"
      via: "dated S15a/S15b/S15c current-byte correction evidence"
      pattern: "S15a"
---

<objective>
Close Audit 4 and Audit 5 by routing unreadable declarations through the normal diagnostic channel and correcting SEC-03/current-prose evidence without rewriting history.

Purpose: Release certification cannot retain a raw declaration TypeError or a stale schema-accessor claim when current bytes already enforce a detached, non-executing schema boundary.
Output: Indexed aggregate diagnostics, exact type/runtime tests, precise shipping comments, and append-only Phase 3/4 verifier addenda.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/v0.1-MILESTONE-AUDIT.md
@.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-CONTEXT.md
@.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-RESEARCH.md
@.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-PATTERNS.md
@.planning/phases/03-action-declaration-and-build-time-validation/03-VERIFICATION.md
@.planning/phases/04-stages-catalog-assembly-and-explain/04-VERIFICATION.md

<interfaces>
From packages/concierge/src/catalog.ts:
- `CatalogIssueCode` is a closed exported literal union currently containing ten codes.
- `CatalogIssue` is `{code, action, vendor?, required?, declared?, problem, fix}`.
- `buildCatalog()` owns one `issues` array and throws one `CatalogValidationError` after scanning all declarations.

From packages/concierge/test/concierge.test.ts:
- S15a rejects a root schema accessor without invoking it.
- S15b rejects a nested schema accessor without invoking it.
- S15c proves explicit schema data is detached and frozen.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 10-02-01: Aggregate null and unreadable declarations by stable array index</name>
  <files>packages/concierge/src/catalog.ts, packages/concierge/test/catalog.test.ts, packages/concierge/test-d/catalog.test-d.ts</files>
  <read_first>
    <file>packages/concierge/src/catalog.ts</file>
    <file>packages/concierge/test/catalog.test.ts</file>
    <file>packages/concierge/test-d/catalog.test-d.ts</file>
    <file>packages/concierge/src/types.ts</file>
    <file>.planning/phases/03-action-declaration-and-build-time-validation/03-VERIFICATION.md</file>
    <file>.planning/v0.1-MILESTONE-AUDIT.md</file>
    <file>.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-CONTEXT.md</file>
    <file>.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-PATTERNS.md</file>
  </read_first>
  <behavior>
    - "A null, undefined, primitive, or otherwise non-object declaration never reaches `.name`, `schema`, or any other property read."
    - "Malformed declarations append `invalid_declaration` with action `declaration at index N`, then independent declaration issues continue aggregating in input order."
    - "CatalogIssueCode remains an exact closed eleven-member union."
  </behavior>
  <action>
Implement the structural aggregate diagnostic exactly per D-10-13 and D-10-14.
Add `invalid_declaration` to `CatalogIssueCode`. Iterate `actions` by numeric index, view each element as `unknown`, and before the first property read reject `candidate === null || typeof candidate !== "object"`. Push one ordinary issue with `code: "invalid_declaration"`, `action: "declaration at index N"` using the zero-based index, `problem: "The declaration is not a readable action object."`, and `fix: "Pass an action declaration object with a nonempty name and valid schema, redaction, consent, and handler fields."`; then continue scanning. Do not throw early, invent an action name, add another error class, invoke a candidate accessor, or alter formatting/diagnostic behavior for valid objects.

Add a mixed runtime case containing `null` at index 0 plus an independent valid-object catalog fault at index 1. Assert one `CatalogValidationError`, ordered exact codes/subjects, two nonempty fixes, formatted text containing both fixes, and no raw TypeError. Add separate undefined/primitive coverage through runtime-unsafe casts. Update both directions of the exact `CatalogIssueCode` type equality so an extra, removed, renamed, or widened code fails typecheck.
  </action>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge build &amp;&amp; node_modules/.bin/vitest run packages/concierge/test/catalog.test.ts &amp;&amp; pnpm --filter @fullselfbrowsing/concierge typecheck</automated>
  </verify>
  <acceptance_criteria>
    - "`buildCatalog([null] as never)` throws `CatalogValidationError`, not `TypeError`, and `issues[0]` exactly equals the specified `invalid_declaration` code, index-0 subject, problem, and fix (D-10-13)."
    - "A mixed malformed/ordinary input yields both ordered issues and proves scanning continues after the unreadable element."
    - "No name/schema/accessor on the malformed element executes before classification (T-10-05 HIGH)."
    - "Every issue has a stable distinct code, truthful action/index subject, nonempty problem, and nonempty actionable fix; no human prose checkpoint is required (D-10-14)."
    - "The exact closed `CatalogIssueCode` equality contains eleven members and typecheck exits 0."
  </acceptance_criteria>
  <done>Unreadable declarations use the normal aggregate build-diagnostic channel with deterministic, actionable index evidence.</done>
</task>

<task type="auto">
  <name>Task 10-02-02: Re-verify SEC-03 on current bytes and append historical corrections</name>
  <files>packages/concierge/src/catalog.ts, packages/concierge/src/contract.ts, .planning/phases/03-action-declaration-and-build-time-validation/03-VERIFICATION.md, .planning/phases/04-stages-catalog-assembly-and-explain/04-VERIFICATION.md</files>
  <read_first>
    <file>packages/concierge/src/catalog.ts</file>
    <file>packages/concierge/src/contract.ts</file>
    <file>packages/concierge/src/json-schema.ts</file>
    <file>packages/concierge/test/concierge.test.ts</file>
    <file>packages/concierge/test/catalog.test.ts</file>
    <file>.planning/phases/03-action-declaration-and-build-time-validation/03-VERIFICATION.md</file>
    <file>.planning/phases/04-stages-catalog-assembly-and-explain/04-VERIFICATION.md</file>
    <file>.planning/phases/06-dispatcher/06-08-SUMMARY.md</file>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/v0.1-MILESTONE-AUDIT.md</file>
    <file>.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-CONTEXT.md</file>
    <file>.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-RESEARCH.md</file>
  </read_first>
  <action>
Apply the historical/current-evidence correction rules per D-10-15 and D-10-16.
Build core first and run only S15a/S15b/S15c against `dist`. Record that all three execute and pass, root/nested schema getters remain at invocation count zero, and emitted parameters are detached/frozen. If this exact current-byte probe passes, leave `json-schema.ts` and `concierge.test.ts` byte-identical; source repair is authorized only if the focused probe fails. Correct the over-broad live comments in `contract.ts` and `catalog.ts` to state the measured rule: module-evaluation registration can be tree-shaken when no retained runtime reference keeps evaluation reachable, so `assertSingleInstance()` must remain on retained API paths. Remove the catalog comment that presents null's raw TypeError as an accepted residual.

Append, after each existing verifier footer, a dated `Phase 10 correction addendum` that preserves all original frontmatter/status/tables/timestamps. In `03-VERIFICATION.md`, link the prior DX-03 null exception to Task 10-02-01's exact `invalid_declaration` proof. In `04-VERIFICATION.md`, link the prior SEC-03 partial to current S15a/S15b/S15c and state that accessors do not execute and schemas are detached on current bytes; also close the historical Phase 4 dispatch-stub note as superseded by Phase 6 R37-R45/Q-series normalization and the 57/57 mutation evidence, without recreating the stub test. Each addendum names original observation, current command, current evidence, and superseded conclusion per D-10-15/D-10-16.
  </action>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge build &amp;&amp; node_modules/.bin/vitest run packages/concierge/test/concierge.test.ts -t 'S15[abc]'</automated>
    <automated>git diff --exit-code -- packages/concierge/src/json-schema.ts packages/concierge/test/concierge.test.ts &amp;&amp; grep -q 'Phase 10 correction addendum' .planning/phases/03-action-declaration-and-build-time-validation/03-VERIFICATION.md &amp;&amp; grep -q 'S15a' .planning/phases/04-stages-catalog-assembly-and-explain/04-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - "The focused built-byte run reports exactly S15a, S15b, and S15c passing; root and nested getters execute zero times and explicit schema output is detached/frozen (Audit 5; T-10-05 HIGH)."
    - "When the probe passes, `json-schema.ts` and `concierge.test.ts` have zero diff; no duplicate clone/snapshot implementation is introduced."
    - "Live `catalog.ts`/`contract.ts` comments contain the qualified retained-runtime-reference rule and no longer claim every module-scope effect is always erased (D-10-16)."
    - "Phase 3 and Phase 4 verifier frontmatter, original findings, timestamps, and conclusions remain byte-present before dated addenda; corrections are append-only."
    - "The Phase 4 addendum cites current S15a/S15b/S15c plus Phase 6 normalization/mutation evidence and creates no obsolete dispatch-stub test (D-10-15)."
  </acceptance_criteria>
  <done>SEC-03 has independent current-byte proof, live prose is accurate, and historical records retain their original observations with explicit correction addenda.</done>
</task>

</tasks>

<source_coverage_audit>
| Source | Item | Status | Task |
|---|---|---|---|
| AUDIT | Audit 4 null declaration | COVERED | 10-02-01 |
| AUDIT | Audit 5 SEC-03 | COVERED | 10-02-02 |
| CONTEXT | D-10-13/D-10-14 | COVERED | 10-02-01 |
| CONTEXT | D-10-15/D-10-16 | COVERED | 10-02-02 |
| RESEARCH | Current-byte SEC-03 pass supersedes stale diagnosis | COVERED | 10-02-02 |
| SECURITY | T-10-05 | COVERED | 10-02-01/02 |
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| caller declaration → catalog validation | Runtime-unsafe/null input must not escape through raw property access. |
| consumer schema → emitted catalog | Getters and mutable references must not execute or survive detachment. |
| historical evidence → current release claim | Stale claims must be corrected without erasing what prior verification observed. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-05 | Tampering / Denial of Service | HIGH | declaration and schema catalog boundary | mitigate | Pre-property declaration classification; current built-byte S15a/S15b/S15c accessor-count and detachment proofs; append-only correction evidence. |
</threat_model>

<verification>
Run the Plan 02 focused catalog/type command and the exact S15a/S15b/S15c built-byte probe. Plan 05 adds compiling `M-10-C01` discrimination before final evidence generation.
</verification>

<success_criteria>
- Audit 4 and Audit 5 are closed with objective built-byte evidence.
- D-10-13 through D-10-16 are implemented exactly.
- T-10-05 HIGH is mitigated without schema accessor execution or shared schema references.
- No historical verifier record is silently rewritten and no obsolete dispatch stub is revived.
</success_criteria>

<output>
Create `.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-02-SUMMARY.md` when done.
</output>
