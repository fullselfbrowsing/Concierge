---
phase: 10-close-v0-1-release-certification-and-evidence-gaps
plan: 08
type: execute
wave: 6
depends_on: [10-07]
files_modified: []
autonomous: false
requirements: [SEC-03, ADP-01, ADP-02, ADP-03, ADP-04, PKG-04]
must_haves:
  truths:
    - "The exact clean HEAD containing every source, test, workflow, evidence, verifier, audit, SUMMARY, ROADMAP, STATE, and requirement byte passes hosted Ubuntu CI per D-10-09/D-10-11."
    - "The selected run, attempt, SHA, ref, workflow, repository, and all required job conclusions exactly match the downloaded run-scoped receipt."
    - "The certified HEAD and repository status do not change before, during, or after receipt verification; no tracked 10-08 SUMMARY or bookkeeping file is created."
    - "The milestone is complete only when the external receipt exists and all locked 62/62, 9/9, 10/10, 12/12, 10/10, and Nyquist facts remain true."
    - "No npm publication, provenance inspection, release tag, registry write, or OIDC publish job executes."
  artifacts:
    - path: "external GitHub Actions artifact"
      provides: "Run-scoped candidate certification receipt"
      contains: "candidate SHA, run ID, attempt, and required job conclusions"
    - path: "scripts/phase-10-certify-candidate.mjs"
      provides: "Read-only exact-SHA receipt validation"
      contains: "certify"
  key_links:
    - from: "external GitHub Actions run"
      to: "git rev-parse HEAD"
      via: "exact headSha equality"
      pattern: "40-character Git SHA"
    - from: "external receipt artifact"
      to: "GitHub Actions run/jobs API"
      via: "repository/workflow/ref/runId/runAttempt/conclusion/job equality"
      pattern: "candidate-certification"
---

<objective>
Perform the terminal no-write hosted certification ceremony for the exact final candidate and stop at an external receipt, without creating an uncertified successor commit.

Purpose: This is the only ordering compatible with D-10-09 through D-10-12 and normal GSD SUMMARY/bookkeeping behavior: every tracked write finishes first, hosted CI runs second, and nothing tracked happens afterward.
Output: A successful exact-SHA GitHub Actions run and matching external receipt; no repository file output.
</objective>

<execution_context>
@/Users/lakshman/.codex/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-CERTIFICATION.md
@.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-VALIDATION.md
@.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-VERIFICATION.md
@.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-07-SUMMARY.md
@.planning/v0.1-MILESTONE-AUDIT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/phase-10-certify-candidate.mjs
@.github/workflows/ci.yml
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto">
  <name>Task 10-08-01: Certify the exact clean candidate in hosted GitHub Actions</name>
  <files>None — this task may create only temporary files outside the repository and a GitHub Actions run/artifact.</files>
  <read_first>
    <file>.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-CERTIFICATION.md</file>
    <file>.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-VALIDATION.md</file>
    <file>.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-VERIFICATION.md</file>
    <file>.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-07-SUMMARY.md</file>
    <file>.planning/v0.1-MILESTONE-AUDIT.md</file>
    <file>.planning/ROADMAP.md</file>
    <file>.planning/STATE.md</file>
    <file>scripts/phase-10-certify-candidate.mjs</file>
    <file>.github/workflows/ci.yml</file>
    <file>.github/workflows/release.yml</file>
  </read_first>
  <action>
Execute hosted certification per D-10-09, exclude publication per D-10-10, preserve the exact final bytes per D-10-11, and reject any score waiver per D-10-12.
This task starts only after `10-07-SUMMARY.md` and centralized GSD ROADMAP/STATE/REQUIREMENTS updates are committed. Run `git status --porcelain=v1 --untracked-files=all` and require empty output; capture `candidate_sha=$(git rev-parse HEAD)` and the current branch; require the already configured remote branch to resolve to the same SHA before any hosted action. Execute `node scripts/phase-10-certify-candidate.mjs certify`; it must not push or mutate the branch. Trigger/select one explicit `ci.yml` run for `candidate_sha`, wait for completion, and download the uniquely matching receipt artifact to an owned temporary directory. Require overall success and successful `build`, `node-floor`, and `candidate-certification` jobs plus exact receipt equality for repository, workflow, ref, head SHA, run ID, run attempt, schema, and job map. Capture stdout fields for the checkpoint. Reassert the same HEAD and empty status after verification. Do not run `release.yml`, publish, inspect registry provenance, create a tag, edit/stage/commit any file, or invoke a GSD state/summary/audit handler.
  </action>
  <verify>
    <automated>node scripts/phase-10-certify-candidate.mjs certify</automated>
  </verify>
  <acceptance_criteria>
    - "Pre-run and post-run HEAD are the same exact 40-character SHA and both full-status checks are empty (D-10-11; T-10-07 HIGH)."
    - "One explicit `ci.yml` run has `headSha === candidate_sha`, completed `conclusion === success`, and successful required jobs on hosted Ubuntu (D-10-09)."
    - "One receipt artifact exactly matches repository, workflow, ref, SHA, run ID, run attempt, schema version, and all required job conclusions; ambiguous/missing/mismatched receipts fail."
    - "The receipt's candidate still has 62/62 requirements, 9/9 original implementation phases, 10/10 current directories, 12/12 links, 10/10 flows, and Phase 9 Nyquist true (D-10-12)."
    - "No release workflow, OIDC publish job, npm publication, provenance inspection, tag, registry write, or tracked receipt/SUMMARY/bookkeeping mutation occurs (D-10-10; T-10-08 HIGH)."
  </acceptance_criteria>
  <done>The exact final candidate SHA has a successful hosted run and a matching external receipt, with repository bytes unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-terminal">
  <name>Task 10-08-02: Acknowledge the external receipt and terminate without tracked writes</name>
  <files>None — terminal checkpoint; no repository or external state mutation.</files>
  <read_first>
    <file>.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-CERTIFICATION.md</file>
    <file>.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-VALIDATION.md</file>
    <file>.planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-VERIFICATION.md</file>
    <file>.planning/v0.1-MILESTONE-AUDIT.md</file>
  </read_first>
  <action>
Enforce the terminal no-write acknowledgment per D-10-11.
Present the exact candidate SHA, run URL, run ID, attempt, overall conclusion, required job conclusions, artifact name, and receipt digest emitted by Task 10-08-01. Re-run read-only `verify-run` using those exact values and show that HEAD/status remain unchanged. This is a terminal acknowledgment, not a resume point: after approval, return `CERTIFICATION COMPLETE` with the external fields and terminate without the normal execute-plan summary, ROADMAP, STATE, REQUIREMENTS, verifier, audit, or commit steps. The absence of tracked `10-08-SUMMARY.md` is deliberate and load-bearing; creating it would produce an uncertified successor SHA.
  </action>
  <verify>
    <automated>node scripts/phase-10-certify-candidate.mjs verify-run "${CANDIDATE_SHA}" "${RUN_ID}" "${RUN_ATTEMPT}" &amp;&amp; test "$(git rev-parse HEAD)" = "${CANDIDATE_SHA}" &amp;&amp; test -z "$(git status --porcelain=v1 --untracked-files=all)"</automated>
  </verify>
  <acceptance_criteria>
    - "The displayed SHA/run/attempt/jobs/artifact/digest exactly match the independently revalidated external receipt."
    - "HEAD is still `${CANDIDATE_SHA}` and full Git status is empty after acknowledgment."
    - "No `10-08-SUMMARY.md` exists and no tracked file/commit is created after hosted success."
    - "The user is explicitly told that any later tracked change invalidates this receipt and requires a new hosted run."
  </acceptance_criteria>
  <what-built>One exact clean pre-publication candidate with a successful hosted Ubuntu run and matching external certification receipt.</what-built>
  <how-to-verify>
    1. Confirm the shown candidate SHA equals the current `git rev-parse HEAD`.
    2. Open the shown run URL and confirm the run ID/attempt and required jobs are successful.
    3. Confirm the shown artifact/receipt fields and digest match the read-only verifier output.
    4. Confirm `git status --porcelain=v1 --untracked-files=all` is empty and no 10-08 SUMMARY exists.
  </how-to-verify>
  <resume-signal>Type `approved-terminal` to acknowledge the external receipt. The orchestrator must then return CERTIFICATION COMPLETE and terminate without resuming normal tracked finalization.</resume-signal>
  <done>The user has acknowledged the exact external receipt and the orchestrator has terminated without changing the certified commit.</done>
</task>

</tasks>

<source_coverage_audit>
| Source | Item | Status | Task |
|---|---|---|---|
| GOAL | Exact hosted-certified pre-publication candidate | COVERED | 10-08-01/02 |
| AUDIT | Audit 9 hosted success | COVERED | 10-08-01 |
| CONTEXT | D-10-09 hosted CI | COVERED | 10-08-01 |
| CONTEXT | D-10-10 no publication | COVERED | 10-08-01/02 |
| CONTEXT | D-10-11 exact clean commit/no later writes | COVERED | 10-08-01/02 |
| CONTEXT | D-10-12 no gap waivers | COVERED | 10-08-01 |
| SECURITY | T-10-07/T-10-08 | COVERED | 10-08-01/02 |
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| final tracked HEAD → GitHub hosted run | The workflow must execute exactly the candidate being certified. |
| hosted run/jobs/artifact → completion claim | Every external identity and conclusion must agree. |
| hosted success → GSD finalization | Any normal tracked finalization would invalidate the candidate. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-07 | Repudiation / Tampering | HIGH | candidate SHA/run/receipt | mitigate | Clean/remote-equal HEAD, explicit run/attempt, exact receipt/job matching, post-run unchanged HEAD/status, terminal no-summary checkpoint. |
| T-10-08 | Elevation of Privilege | HIGH | CI versus publish authority | mitigate | Execute only CI; statically verified publish-only OIDC remains dormant; forbid registry/tag/provenance actions. |
</threat_model>

<verification>
Task 10-08-01's `certify` command and Task 10-08-02's exact `verify-run` command must both pass on the same SHA/run/attempt. No local-only substitute is accepted.
</verification>

<success_criteria>
- Exact candidate SHA has one successful hosted Ubuntu run and matching external receipt.
- All required job conclusions and locked milestone totals are successful/current.
- No tracked write, summary, verifier, audit, state update, publication, provenance inspection, or tag follows success.
- The terminal acknowledgment returns external completion facts only.
</success_criteria>

<output>
Do not create `10-08-SUMMARY.md` or invoke normal execute-plan metadata finalization. Return `CERTIFICATION COMPLETE` with candidate SHA, run URL, run ID, attempt, required job conclusions, artifact name, and receipt digest from the external record only.
</output>
