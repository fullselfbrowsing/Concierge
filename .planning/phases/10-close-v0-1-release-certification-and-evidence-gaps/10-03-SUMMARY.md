---
phase: 10-close-v0-1-release-certification-and-evidence-gaps
plan: 03
subsystem: release-boundaries
tags: [pnpm, child-environment, astro, generated-state, release-inputs]
requires:
  - phase: 09-react-and-svelte-adapters
    provides: authoritative package gate, mutation harness, and Astro SSR harness
provides:
  - exact benign-parent versus authenticated-mutation pnpm environment classification
  - harness-scoped Astro generated-state exclusion with zero tracked declarations
  - committed clean-clone regeneration verifier for the dependent hosted-build plan
  - explicit rejection of Astro generated paths from release manifests and seals
affects: [phase-10-hosted-receipt, phase-10-mutation-evidence, release-certification]
tech-stack:
  added: []
  patterns: [exact-environment-classification, authenticated-child-retention, generated-state-regeneration]
key-files:
  created:
    - .planning/phases/10-close-v0-1-release-certification-and-evidence-gaps/10-03-SUMMARY.md
  modified:
    - scripts/phase-09-package-check.mjs
    - scripts/phase-09-contract-check.mjs
    - scripts/phase-09-mutation-battery.mjs
    - .gitignore
  deleted:
    - examples/adapter-ssr/.astro/content.d.ts
    - examples/adapter-ssr/.astro/types.d.ts
key-decisions:
  - Consume one exact unmarked pnpm false policy as benign parent decoration while stripping it from ordinary children.
  - Retain the false policy only for a separately authenticated mutation child carrying the exact marker value.
  - Treat pinned Astro regeneration, not generated declaration bytes, as release evidence.
requirements-completed: [ADP-01, ADP-02, ADP-03, ADP-04, PKG-04]
metrics:
  duration: 7m
  completed: 2026-08-12
---

# Phase 10 Plan 03: Package Authority and Astro Generated-State Summary

The real pnpm package gate now distinguishes benign caller decoration from authenticated mutation-child authority, while Astro declarations are untracked local output that cannot enter release inputs or evidence seals.

## Performance

- **Started:** 2026-08-12T17:35:01Z
- **Completed:** 2026-08-12T17:41:24Z
- **Duration:** 7 minutes
- **Tasks:** 2
- **Files changed:** 6

## Accomplishments

- Accepted exactly one unmarked `PNPM_CONFIG_VERIFY_DEPS_BEFORE_RUN=false` at the package-check parent and proved it is absent from an ordinary child.
- Preserved the same exact false policy only for authenticated mutation children carrying `PHASE09_CREDENTIAL_FREE_ENV=1`, with wrong values and case-folded duplicates still rejected.
- Expanded package and static-contract controls to discriminate child leakage, false authentication, and guard deletion while retaining the owned HOME/config/store and credential-stripping boundary.
- Removed both tracked Astro declarations and added only the harness-scoped `/examples/adapter-ssr/.astro/` ignore rule.
- Added a disposable exact-HEAD verifier that starts without `.astro`, installs from the owned offline store, runs pinned package-local `check` then `build`, and proves regenerated state remains untracked and unsealed.
- Added release-input, manifest, mutation-evidence, and release-evidence guards that reject any harness-local `.astro` authority without reading, hashing, or comparing generated bytes.

## Task Commits

1. **Task 10-03-01: Consume benign pnpm decoration without granting mutation authority** — `a85c1a4` (test), `9eda317` (fix)
2. **Task 10-03-02: Untrack Astro state and prove clean pinned regeneration** — `b2a9935` (fix)

## Files Created, Modified, or Deleted

- `scripts/phase-09-package-check.mjs` — classifies exact pnpm parent decoration separately from authenticated mutation-child retention and expands real-child self-tests.
- `scripts/phase-09-contract-check.mjs` — statically pins the split classification and includes a negative fixture that exposes ordinary-child leakage or false authentication.
- `scripts/phase-09-mutation-battery.mjs` — excludes and rejects Astro generated state throughout release authority and adds the committed-snapshot regeneration verifier.
- `.gitignore` — ignores only the adapter SSR harness's generated `.astro` directory.
- `examples/adapter-ssr/.astro/content.d.ts` — removed as generated local state.
- `examples/adapter-ssr/.astro/types.d.ts` — removed as generated local state.

## Decisions Made

- Parsed the pnpm policy and authentication marker independently: the parent may consume exact false without granting authority, while retention requires both exact values.
- Kept `createPackageChildEnvironment()` as the sole package-child construction boundary and left `phase-09-secure-environment.mjs` byte-identical.
- Made any tracked harness-local `.astro` path an invariant failure before filtering release inputs, so future accidental tracking cannot be hidden by exclusion.
- Committed the clean-clone verifier without executing it against the old pre-deletion HEAD; dependent Plan 10-04 owns the first honest execution from this committed state.

## Deviations from Plan

None — the plan executed as written.

## Issues Encountered

- Once the new ignore rule was staged, ordinary `git add` would not stage deletion paths beneath the ignored directory. The already-removed tracked entries were staged directly in the index; the resulting commit contains exactly the intended two deletions.

## Verification

- Package checker self-test: 17 controls passed.
- Contract checker final: 0 missing IDs across 56 required nonempty artifacts.
- Public `CI=true pnpm run check:phase09:packages`: passed end to end, including all three archives, TypeScript 7.0.2, SSR, consent, and mismatch checks.
- Mutation harness syntax and self-test: 36 controls passed.
- Before the Task 2 commit: both generated files absent and exactly two tracked deletions observed.
- After the Task 2 commit: `git ls-files -- examples/adapter-ssr/.astro` returned zero paths and the exact ignored declaration probe passed.
- Existing mutation and release evidence: zero occurrences of the harness-local `.astro` path.
- Clean-clone regeneration mode: intentionally deferred to dependent Plan 10-04, which runs it from the committed deletion state.

## User Setup Required

None.

## Next Phase Readiness

- The first honest clean-clone Astro regeneration proof can now run against commit `b2a9935` or a descendant.
- Plan 10-04 can repair hosted build ordering and build the push-bound exact-SHA receipt tooling on the corrected package and generated-state boundaries.

## Self-Check: PASSED

All three task commits resolve, all six claimed implementation paths match their committed dispositions, package and contract checks are green, `.astro` has zero tracked paths, and unrelated user-owned working-tree changes remain unstaged.
