---
phase: 01-type-surface-completion
plan: 15
type: execute
wave: 15
gap_closure: true
closes: []
depends_on: ["01-14"]
files_modified:
  - packages/concierge/src/types.ts
  - .planning/phases/01-type-surface-completion/01-VALIDATION.md
autonomous: true
requirements: [SC-7, TRN-01, TRN-05]
user_setup: []

must_haves:
  truths:
    - "None of the twelve gap-closure fixes disarmed an existing Phase 1 guard — all ten original mutants still go red against the final surface"
    - "All fourteen mutations that escaped the code review are now caught, or compile where they were previously broken"
    - "Phase 2's six pinned patterns still match the shipped text, and any line-number drift is reported rather than absorbed"
    - "The export surface of src/index.ts is byte-identical to the state Phase 2 was planned against"
    - "The type surface is left byte-identical to how plan 01-14 committed it"
  artifacts:
    - path: .planning/phases/01-type-surface-completion/01-15-SUMMARY.md
      provides: "The two battery tables, the Phase 2 pattern recheck, and the handoff"
      min_lines: 80
    - path: .planning/phases/01-type-surface-completion/01-VALIDATION.md
      provides: "A gap-closure section carrying one row per gap-closure task, appended below the signed-off original"
      contains: "Gap-Closure Validation Map"
  key_links:
    - from: "each of the 24 mutations"
      to: "its named guard"
      via: "tsc echoing the offending source line"
      pattern: "TS2344|TS2578|TS2322|TS2339|TS2375|TS2315|TS2540|TS2542"
    - from: "packages/concierge/src/types.ts and src/index.ts"
      to: "Phase 2 plans 02-02, 02-04, 02-07, 02-11"
      via: "six pinned patterns matched verbatim after the gap-closure edits"
      pattern: "export const MESSAGE_MAX_CHARS = 180;"
---

<objective>
The gap-closure gate. **A green suite is not evidence of a working suite, and the reason this
sequence exists is that the phase's own gate already proved that once and was still wrong.**

Plan 01-09 ran a ten-mutant battery against the final Phase 1 surface. All ten were caught. An
independent verifier then re-ran the same ten plus nine more, in an isolated sandbox, and all
nineteen were caught with zero escapees. The phase passed. A code reviewer then wrote **seventeen
new mutations against surface the battery does not cover, and fourteen escaped** — two of them
critical. The failure was never "the tests failed"; it was "the tests were shaped to miss this".

This plan does the only two things that establish the sequence worked:

1. **Re-run Phase 1's ten-mutant battery against the final surface.** Twelve fixes have landed since
   01-09 signed off, four of them changing declarations the existing guards read — `readonly` on
   `ConsentAck` and `DeliveryReport`, `| undefined` on the invocation path, a possibly-reshaped
   `ActionResult`, and `Bridge`'s defaults. An assertion that fired in isolation can stop firing
   after a later edit changes an adjacent declaration; that is the exact reason 01-09 existed and it
   applies with more force now.
2. **Re-run the reviewer's fourteen escapees against the final surface.** Each was proven
   defect-first in the plan that closed it, but each was proven against a partially-fixed tree. The
   only thing that establishes them collectively is running them collectively, at the end.

Then it hands off: Phase 2 is already planned, its mutation harness pins **exact source text** in the
two files this sequence edited, and its published exit code `3` means "pattern never matched" — an
abort, not a caught mutant. Nobody but this plan is positioned to tell Phase 2 whether its pins
survived.

Purpose: make "the surface is closed" a measured claim rather than an intended one.
Output: two battery tables, a Phase 2 pattern recheck, and an appended gap-closure validation map.

**Serial phase.** Plan 15 of the gap-closure sequence, and the last. `packages/concierge/src/types.ts`
is in `files_modified` because the batteries edit it transiently. **Every mutant must be reverted.**
This plan leaves the file byte-identical to how plan 01-14 committed it.

## ⚠ Judge each mutant against ITS OWN guard, and against the shape that actually shipped

Two standing corrections from 01-09 carry forward unchanged and must not be re-litigated:

- **M4 does not produce TS2578.** No suppression directive anywhere becomes unused when `ConsentAck`
  flattens. Its guards are `_attestedNeedsHash` (TS2344) and the narrowing function (TS2322).
- **M10 does not fire `_requiresIsString`.** `ConsentPolicy<Booking>["requires"]` is still `string`
  once the mutation's `Name` parameter carries its `= string` default. Its guards are
  `_nameNotWidened` and `_snapshotInferred`, both TS2344.

Two more apply only to this run:

- **M1 depends on plan 01-13's outcome.** If the user selected the flat `ActionResult`, M1 is
  unchanged: revert `reason` to open `string`. If they selected the discriminated union, M1 must be
  restated as "revert the failure branch's `reason` to open `string`" and its expected diagnostic set
  read from 01-13's SUMMARY rather than from the 01-09 table. Read the shipped declaration first.
- **Mutants must change ONE axis.** M4 flattens `ConsentAck` — apply the flatten **keeping
  `readonly` on every member**, or the mutant changes the union axis and the mutability axis together
  and proves neither. The same discipline applies anywhere a 01-09-era mutation now overlaps a
  gap-closure fix.

If a mutant exits non-zero and names the guards listed in its row, that mutant passes. **A guard not
listed in a row staying silent is not a failure.** Do not "fix" a working suite to make a silent pin
speak — that is the specific failure mode T-01-36 was written for, at the point of maximum pressure
to just make it pass.

## Mutation ID namespaces — this plan uses the reviewer's, unsuffixed

Task 2 keys its before/after table on `01-REVIEW.md`'s identifiers, which are the stable external
referent. Plans 01-10 and 01-11 needed mutations of the same names with **opposite** meanings, and
suffixed theirs `-SRC` to avoid the clash. The mapping, so no SUMMARY row is misread:

| ID here (reviewer's) | Meaning here | Colliding ID elsewhere | Meaning there |
|---|---|---|---|
| `MUT-A`, `MUT-B` | exploit programs; must **fail** to compile | `MUT-A-SRC`, `MUT-B-SRC` (01-10) | strip `readonly`; must be **caught** |
| `MUT-D`, `MUT-E`, `MUT-F` | exploit programs; must **fail** | `MUT-DEF-SRC`, `MUT-DH-SRC` (01-10) | strip `readonly`; must be **caught** |
| `MUT-C` | positive; must **compile** at exit 0 | `MUT-C-SRC`, `MUT-C2-SRC` (01-11) | revert the defaults; must be **caught** |
| `MUT-G`…`MUT-N` | source mutations; must be **caught** | same IDs in 01-14 / 01-11 | identical meaning — no clash |

Use the unsuffixed IDs in this plan's table and say in the SUMMARY that they are the reviewer's.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-type-surface-completion/01-VALIDATION.md
@.planning/phases/01-type-surface-completion/01-REVIEW.md
@packages/concierge/src/types.ts

Read the SUMMARYs of plans 01-10 through 01-14 for what each fix landed and which mutations each one
already proved. Where `01-VALIDATION.md` § "Ten-mutant battery" and `01-RESEARCH.md` disagree,
**VALIDATION wins** — it carries the 2026-07-28 M4 and M10 corrections.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run Phase 1's ten-mutant battery against the final surface</name>
  <files>packages/concierge/src/types.ts</files>
  <read_first>
    - `.planning/phases/01-type-surface-completion/01-VALIDATION.md` § "Ten-mutant battery — final phase gate" — the corrected table
    - `.planning/phases/01-type-surface-completion/01-09-PLAN.md` Task 1 — the exact mutation each row applies, including M10's "apply this exact mutation, not an improvised one"
    - `.planning/phases/01-type-surface-completion/01-13-SUMMARY.md` — which `ActionResult` shape shipped, which determines M1
    - `packages/concierge/src/types.ts` — the final surface being mutated
  </read_first>
  <action>
    Before starting, confirm the working tree is clean so `git checkout --` is a reliable restore, and
    that `pnpm --filter @fullselfbrowsing/concierge typecheck` exits 0 at baseline.

    Apply each of the ten mutants **one at a time**, never stacked. Each mutation is applied, run, and
    restored **inside a single Bash tool call**, and the tree is asserted clean before the next:

      `perl -0pi -e '<substitution>' packages/concierge/src/types.ts && pnpm --filter @fullselfbrowsing/concierge typecheck; EXIT=$?; git checkout -- packages/concierge/src/types.ts; git diff --exit-code -- packages/concierge/src/types.ts && echo "MUTANT_EXIT=$EXIT TREE_CLEAN"`

    The reviewer's `/tmp` sandbox idiom is an accepted and safer alternative for the whole battery:
    copy `src/`, `test-d/`, `packages/concierge/tsconfig.json`, `tsconfig.base.json`, and
    `packages/concierge/node_modules/@standard-schema` into a scratch directory, repoint the `extends`
    path, add a `tsconfig.test-d.json` carrying `noEmit: true` and `rootDir: "."`, and run `tsc`
    there. If you use it, say so explicitly in the SUMMARY and still assert `git status --porcelain`
    is empty afterward. Phase 1 already had a near-miss on exactly this: an interrupted executor left
    `ConsentAckBase.payload` mutated to `unknown` uncommitted, invisible to a green typecheck, one
    unexamined merge from shipping.

    The ten mutants and the guards that must appear, carried verbatim from `01-VALIDATION.md`:

    - **M1** — `ActionResult.reason` to open `string` (restate per 01-13's outcome; see the objective).
      Guards: TS2322 on the `never` exhaustiveness arm and TS2375 on the computed-reason idiom, plus
      TS2578 on the freshness directive.
    - **M2** (escapee) — `ToolBatch.deferUntilDelivered` back to a bare-id effect.
      Guards: 2 × TS2344 naming `_batchHook` and `_batchRejectsBareId`.
    - **M3** — drop the type argument from `ActionDefinition.consent`.
      Guards: TS2344 naming `_snapshotInferred`, plus TS2322 on the comparator.
    - **M4** — flatten `ConsentAck` to one interface with `grade: ConsentGrade` and an optional
      `readbackHash`. **Keep `readonly` on every member** so the mutant changes one axis.
      Guards: TS2344 naming `_attestedNeedsHash` and TS2322 inside the narrowing function.
      **TS2578 will NOT fire and its absence is correct.**
    - **M5** (escapee) — `ReadbackSink` as a defaulted generic alias.
      Guards: TS2344 naming `_sinkShape` AND TS2578 on `_sinkTakesNoTypeArgs`. Both, not one.
    - **M6** — `TransportCapabilities.userTurnIdentity` back to `boolean`, keeping `readonly`.
      Guards: TS2344 naming `_provenanceNotBoolean`, plus errors on the two capability literals.
    - **M7** — move `readsUntrusted` into `SideEffects`.
      Guards: TS2339 on `_readsUntrustedOnDefinition` and TS2578 on the `SideEffects` directive.
    - **M8** (escapee) — `ActionDefinition.handler` back to two type arguments.
      Guards: TS2344 naming `_handlerAck`. Note the parameter is now spelled `B` after IN-02.
    - **M9** — `ConsentPolicy.snapshotEquality` to method syntax.
      Guards: TS2578 on `_policyDegraded`. **This is the subtle one** — a single unused directive is
      the entire symptom. If M9 exits 0 the suite has a hole and this plan is not done.
    - **M10** — the exact mutation from 01-09 Task 1: `ConsentPolicy<Snapshot, Name extends string = string>`
      with `requires: Name`, threaded through `ActionDefinition.consent`.
      Guards: 2 × TS2344 naming `_nameNotWidened` and `_snapshotInferred`. `_requiresIsString` stays
      silent and that is correct.

    Record every mutant in the SUMMARY as a row: id, the edit applied, exit code, observed diagnostic
    codes, and the guard names echoed on the offending source lines. **Diagnostic counts may differ
    from the 01-09 table** — twelve fixes have landed since — and a *higher* count is expected where a
    gap-closure guard also happens to see the mutation. That is fine and should be recorded. What is
    not acceptable is a zero exit, or a non-zero exit whose diagnostics do not include the guards
    listed in that mutant's row. Either means a gap-closure fix disarmed an existing guard: stop,
    identify which fix, and report it rather than editing the suite.
  </action>
  <verify>
    <automated>git diff --exit-code packages/concierge/src/types.ts &amp;&amp; pnpm --filter @fullselfbrowsing/concierge typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All ten mutants applied and reverted; `git diff --exit-code -- packages/concierge/src/types.ts` exits 0 at the end
    - `pnpm --filter @fullselfbrowsing/concierge typecheck` exits 0 after the final revert
    - Every one of the ten produced a **non-zero** exit code
    - Every one of the ten produced the guards listed in its row
    - M4 confirmed via `_attestedNeedsHash` + the narrowing TS2322, and the SUMMARY records that TS2578 correctly did not fire
    - M10 confirmed via `_nameNotWidened` + `_snapshotInferred`, and the SUMMARY records that `_requiresIsString` correctly stayed silent
    - M9 explicitly confirmed non-zero
    - M1's restatement (if any) is recorded, with the shipped `ActionResult` shape named
    - The SUMMARY contains a ten-row table with id, exit code, observed diagnostics, and guard names matched
    - `git status --porcelain` is empty at the end of the task
  </acceptance_criteria>
  <done>Twelve gap-closure fixes have landed and every one of Phase 1's original ten detectors still fires — no fix bought new coverage by silently costing old coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the code review's fourteen escapees against the final surface</name>
  <files>packages/concierge/src/types.ts</files>
  <read_first>
    - `.planning/phases/01-type-surface-completion/01-REVIEW.md` § "Mutations run (17 new, none from the phase's battery)" — the full table, and the exploit programs quoted under CR-01, WR-01, and CR-02
    - The SUMMARYs of plans 01-10 through 01-14 — which guard each escapee was proven against
    - `packages/concierge/src/types.ts` — the final surface
  </read_first>
  <action>
    Fourteen mutations escaped the four-file suite when the reviewer ran them. Each was proven
    defect-first in the plan that closed it, but each was proven against a **partially fixed tree**.
    Run all fourteen against the final surface, with the same one-at-a-time, restore-in-the-same-call
    discipline as Task 1.

    Five of them are **exploit programs**, not source mutations — they add a file that attempts a
    write and must fail to compile. Do **not** add these to `test-d/`, which is inside the typecheck
    program and would leave residue. Use the `/tmp` sandbox: copy `src/`, `test-d/`,
    `packages/concierge/tsconfig.json`, `tsconfig.base.json`, and
    `packages/concierge/node_modules/@standard-schema` into a scratch directory, repoint the `extends`
    path, add a `tsconfig.test-d.json` carrying `noEmit: true`, `rootDir: "."`, and an `include` that
    also picks up a `probe/` directory, and put the exploit there. Assert `git status --porcelain` is
    empty afterward regardless.

    **Group 1 — exploits that must now fail (source unmutated):**

    - **MUT-A** — the reviewer's `launder` function: write `"attested"` into `ack.grade`, then read
      `ack.readbackHash` inside a `grade === "attested"` guard. Expect **TS2540** on the write.
    - **MUT-B** — overwrite `ack.snapshot`, `ack.payload`, and `ack.userTurnId` in place. Expect
      3 × TS2540. Combined with MUT-A, the reviewer measured **6 × TS2540** total for the six-write
      exploit; record the count you observe.
    - **MUT-D** — `t.capabilities.userTurnIdentity = "human-attested"` and
      `t.capabilities.consentGrade = "attested"`. Expect 2 × TS2540.
    - **MUT-E** — `r.outcome = "completed"` on a `DeliveryReport`. Expect TS2540.
    - **MUT-F** — `rc.hash = …` and `rc.canonical[0] = 0` on a `ReadbackReceipt`. Expect TS2540 on
      the hash write and **TS2542** on the element write. TS2542 is the one that proves the
      `Readonly<Uint8Array>` decision landed rather than a bare `readonly`; if it is absent, the
      element-write hole is still open.

    **Group 2 — a positive that must now compile (source unmutated):**

    - **MUT-C** — declare `BridgeRegistry<ResultsBridge>` and `StageDefinition<ResultsBridge>` for a
      bridge carrying real members, and collect two unrelated concrete-bridge stages into one
      `ConciergeConfig`. Expect **exit 0 with no `as` cast**. This is the only row in the table that
      must be green rather than red; the review recorded it as "BROKEN TODAY" rather than "ESCAPED".
      Use `CLAUDE.md`'s canonical `applyFilter({key, value})` shape. Check the no-cast claim with a
      comment-filtered grep — a naive `grep ' as '` matches the word in prose and is unsatisfiable.

    **Group 3 — source mutations that must now be caught:**

    - **MUT-G** — `ConsentAckBase.userTurnId` → optional (keeping `readonly`). Guard:
      `_ackCarriesTurnIdentity`.
    - **MUT-H** — `DeliveryReport.outcome` → `string`. Guard: `_deliveryOutcomeIsClosed`.
    - **MUT-I** — `ConsentPolicy.bindTo` → `string`. Guard: `_bindToIsClosed`.
    - **MUT-J** — `ReadbackReceipt.canonical` → `unknown`. Guard: `_receiptCanonicalIsBytes`.
    - **MUT-K** — `ActionHandler` ctx `bridge: B | null` → `B`. Guard: `_handlerBridge`.
    - **MUT-L** — `BridgeRegistry.read: () => B | null` → `() => B`. Guard: `_registryReadIsNullable`.
    - **MUT-M** — `ActionResult.ok` → optional. Guard: `_resultOkRequired`.
    - **MUT-N** — `ActionResult.message` → optional. Guard: `_resultMessageRequired`.

    For MUT-M and MUT-N, read the shipped `ActionResult` shape from plan 01-13's outcome before
    writing the mutation; under a discriminated union the mutation applies per branch.

    Record all fourteen in the SUMMARY as a table with the reviewer's original result in one column
    and this run's result in the next, so the before/after is a single readable artifact rather than
    a claim. **Every row must flip from ESCAPED to CAUGHT, or from BROKEN to COMPILES.** A row that
    does not flip is an unclosed finding and must be reported as such, not absorbed.
  </action>
  <verify>
    <automated>git diff --exit-code packages/concierge/src/types.ts &amp;&amp; pnpm --filter @fullselfbrowsing/concierge typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All fourteen run; `git diff --exit-code -- packages/concierge/src/types.ts` exits 0 at the end and `git status --porcelain` is empty
    - MUT-A + MUT-B together produce TS2540 on every write; the observed count is recorded
    - MUT-D and MUT-E each produce TS2540
    - MUT-F produces TS2540 on the hash write **and TS2542 on the element write** — the second is the criterion that proves `Readonly<Uint8Array>` landed
    - MUT-C compiles at exit 0 and the probe file contains no cast. Use the comment-filtered form — `grep -v '^[[:space:]]*[*/]' <probe> | grep -v '^[[:space:]]*//' | grep -cE '\bas\b'` returns 0 — **not** `grep -c ' as '`, which counts the word in English prose: measured, that naive form returns 4 on the untouched `actions.test-d.ts` and every hit is a doc comment. A criterion that cannot pass is the defect `01-VALIDATION.md` row 01-08-T2 already had to repair
    - MUT-G, MUT-H, MUT-I, MUT-J, MUT-K, MUT-L, MUT-M, MUT-N each produce a non-zero exit naming the guard listed for it
    - The SUMMARY contains a fourteen-row before/after table, and every row flips
    - If any row does not flip, it is reported in the SUMMARY as an unclosed finding with its ID, and the plan does not claim completion
  </acceptance_criteria>
  <done>Every mutation that escaped an independent adversarial code review is now caught by a named guard, and the one that was broken rather than unguarded now compiles — measured against the final surface, not against the tree each fix was authored on.</done>
</task>

<task type="auto">
  <name>Task 3: Recheck Phase 2's pinned patterns, confirm the export surface, and record the handoff</name>
  <files>packages/concierge/src/types.ts, .planning/phases/01-type-surface-completion/01-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/02-packaging-build-and-release/02-02-PLAN.md` lines 100–140 and 200–250 — the harness exit-code contract and the `MESSAGE_MAX_CHARS` target
    - `.planning/phases/02-packaging-build-and-release/02-04-PLAN.md` lines 150–205 — mutant P4's pattern and its `lines 194–210` read window
    - `.planning/phases/02-packaging-build-and-release/02-07-PLAN.md` lines 240–250 and 320–380 — the `types.ts:182-206` / `348-354` read-only window and mutant P11's two-line `index.ts` pattern
    - `.planning/phases/02-packaging-build-and-release/02-11-PLAN.md` lines 175–300 — mutants P8 and P9, and P9's exact whitespace-sensitive pattern
    - `.planning/phases/01-type-surface-completion/01-VALIDATION.md` — the signed-off original map this task appends below
  </read_first>
  <action>
    **Part 1 — the Phase 2 pinned-pattern recheck.** Phase 2 is already planned with twelve plans, and
    six of them read a mutation harness (`scripts/mutate-and-prove.sh`, built by 02-02) whose published
    exit code `3` means *pattern never matched* — an abort that looks nothing like a caught mutant. The
    harness matches by **pattern, not by line number**, so exit 3 fires only if the pinned *text*
    changed.

    Check each of these six against the shipped files and record the result as a table row carrying the
    pattern, whether it still matches, the line it is on now, and the line Phase 2's prose claims:

    1. `types.ts` — `export const MESSAGE_MAX_CHARS = 180;` (02-04 mutant P4; 02-02 Task 2's harness
       proof). 02-02 and 02-04 both name **line 206**; 02-04's read window is "lines 194–210".
    2. `types.ts` — `  snapshotEquality?: (a: Snapshot, b: Snapshot) => boolean;` **including its two
       leading spaces** (02-11 mutant P9). 02-11 names **line 399**.
    3. `types.ts` — the four frozen constants at **lines 182–206** and `CONSENT_GRADE_ORDER` at
       **348–354** (02-07 Task 2's read-only window).
    4. `index.ts` — the two-line sequence `  MESSAGE_MAX_CHARS,` followed by `} from "./types.js";`
       (02-07 mutant P11), pinned by a uniqueness argument. Confirm the sequence still occurs exactly
       once.
    5. `index.ts` — the contiguous span at **lines 62–70** (02-11 mutant P8).
    6. `types.ts` — `CONSENT_GRADE_ORDER`'s frozen array contents, which 02-07 Task 2 reads.

    Use `grep -n` for each and report the actual line numbers. **Plan 01-13 was the only one permitted
    to touch a pinned region** (`types.ts:182-206`) and was required to preserve the line count; if it
    reported a shift, propagate it here.

    Then state the finding plainly in the SUMMARY under a heading naming plans 02-02, 02-04, 02-07,
    and 02-11:
    - Every pattern that still matches → Phase 2's harness is unaffected; no exit 3.
    - Every *prose* line number that is now wrong → named explicitly, with the correct number, so
      Phase 2 can be corrected before it executes rather than debugged during it.
    - If any pattern **stopped matching** → that is a blocking handoff. Name the plan, the mutant ID,
      the old text, and the new text, and say that the Phase 2 plan must be amended before execution.

    **Part 2 — the export surface.** `src/index.ts` was declared off-limits for the whole gap-closure
    sequence, because any change to its export block invalidates 02-07/P11 and 02-11/P8 simultaneously
    with a silent exit 3.

    **Confirm it against a pinned ref, not against the working tree.** A bare
    `git diff --exit-code -- packages/concierge/src/index.ts` only proves the file is uncommitted-clean
    *right now*; it cannot see a change that plan 01-11 committed four waves ago, which is exactly the
    scenario T-01-51 exists for. Use the commit that closed plan 01-09 — resolved as **`8c5b1a3`**
    ("chore: merge executor worktree (01-09 phase gate)"), which is also the state Phase 2's plans were
    written against. Verify the hash resolves before relying on it; if the history has moved, resolve
    it by locating the last commit that touched `packages/concierge/src/index.ts` before the
    gap-closure sequence began.

    Run **both**: `git diff --exit-code 8c5b1a3 -- packages/concierge/src/index.ts` (content identical)
    and `test -z "$(git log --oneline 8c5b1a3..HEAD -- packages/concierge/src/index.ts)"` (no commit in
    the range touched it at all). The second catches a change-and-revert, which the first would miss.
    Then count exported names against the 42-plus-`StandardSchemaV1` figure the verifier recorded. If a
    name was added or removed, say so and name the plan that did it.

    **Part 3 — the four-file suite and the tree.** Run `pnpm typecheck` from the repo root. Run a real
    build with `pnpm --filter @fullselfbrowsing/concierge exec tsc -p tsconfig.json`, confirm no
    `*.test-d.*` and no `_assert*` path appears in `packages/concierge/dist`, then remove `dist`
    (it is gitignored; leaving it makes the tree dirty). Confirm `git diff --exit-code pnpm-lock.yaml`
    — this sequence added zero dependencies.

    **Part 4 — append the gap-closure validation map.** `01-VALIDATION.md` is signed off for the
    original nine plans and must not be rewritten. **Append** a new section titled
    `## Gap-Closure Validation Map` below the existing sign-off, carrying one row per gap-closure task
    in the same column shape as the original per-task map — Task, Finding ID, Behavior, Threat Ref,
    Test Type, Automated Command, File Exists — with task IDs in the form `01-1{plan}-T{n}`. Note in a
    sentence above the table that the phase gate's existing row count command (`grep -c '^| 01-0'`)
    matches only the original rows by construction and is therefore unaffected.

    Close with a one-paragraph statement of what this sequence did and did not establish. Be exact:
    it establishes that fourteen specific mutations are now caught and that ten prior detectors still
    fire. It does **not** establish that no further uncovered surface exists — the reviewer found
    fourteen escapees against a suite that had already survived nineteen mutations and two independent
    verification passes, and the honest lesson is that a battery measures the surface it was written
    for.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm --filter @fullselfbrowsing/concierge exec tsc -p tsconfig.json &amp;&amp; ls -R packages/concierge/dist | grep -c "test-d\|_assert" | grep -qx 0 &amp;&amp; rm -rf packages/concierge/dist &amp;&amp; git diff --exit-code 8c5b1a3 -- packages/concierge/src/index.ts &amp;&amp; test -z "$(git log --oneline 8c5b1a3..HEAD -- packages/concierge/src/index.ts)" &amp;&amp; git diff --exit-code pnpm-lock.yaml &amp;&amp; grep -q "Gap-Closure Validation Map" .planning/phases/01-type-surface-completion/01-VALIDATION.md &amp;&amp; echo GAP_GATE_PASS</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` from the repo root exits 0
    - A build emits into `packages/concierge/dist` and `ls -R packages/concierge/dist` contains no `*.test-d.*` and no `_assert*` path; `dist` is removed afterwards and `git status --porcelain` is empty
    - **Ref-pinned**, not working-tree-only: `git diff --exit-code 8c5b1a3 -- packages/concierge/src/index.ts` exits 0 **and** `test -z "$(git log --oneline 8c5b1a3..HEAD -- packages/concierge/src/index.ts)"` succeeds — measured clean at planning time, so both are real gates. The unpinned form is insufficient and must not be substituted
    - `git diff --exit-code pnpm-lock.yaml` exits 0 — the gap-closure sequence added zero dependency edges
    - All six Phase 2 pinned patterns were checked with `grep -n` and each result recorded: pattern text, still-matches yes/no, current line, line Phase 2's prose claims
    - `export const MESSAGE_MAX_CHARS = 180;` and `  snapshotEquality?: (a: Snapshot, b: Snapshot) => boolean;` (with its two leading spaces) both still match verbatim
    - The two-line `index.ts` sequence `  MESSAGE_MAX_CHARS,` then `} from "./types.js";` still occurs exactly once
    - The SUMMARY carries a heading naming plans 02-02, 02-04, 02-07, and 02-11, listing every stale prose line number with its correct value — or stating explicitly that none moved
    - Any pattern that stopped matching is reported as a **blocking handoff** naming the plan, the mutant ID, the old text and the new text
    - `01-VALIDATION.md` gained a `## Gap-Closure Validation Map` section appended below the existing sign-off, with one row per gap-closure task and task IDs of the form `01-1{plan}-T{n}`; the original per-task map and the `status: complete` / `nyquist_compliant: true` frontmatter are unchanged
    - The SUMMARY's closing paragraph states what this sequence establishes and what it does not, without overstating it
    - `git diff --exit-code -- packages/concierge/src/types.ts` exits 0 — neither battery left residue
  </acceptance_criteria>
  <done>Phase 2 has been told, in writing, whether every pattern it pins still matches and where its prose line numbers now point; the export surface is proven untouched; and the validation map records the gap-closure work beside the phase's original sign-off rather than in place of it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| type-test suite → confidence in the contract | A suite that has never failed provides no evidence; a suite that failed once for surface A provides no evidence about surface B |
| this phase → Phase 2's already-written plans | Six Phase 2 mutants pin exact source text in the two files this sequence edited |
| build program → published artifact | A test file reachable from the emit program ships executable dead code and an inaccurate declaration surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-57 | **Repudiation** | a gap-closure fix disarming an existing Phase 1 guard | mitigate | Re-run all ten original mutants against the final surface. Four fixes changed declarations the existing guards read — `readonly` on `ConsentAck` and `DeliveryReport`, `\| undefined` on the invocation path, a possibly-reshaped `ActionResult`, and `Bridge`'s defaults. A zero exit on any of the ten means coverage was traded, not added, and must be reported rather than patched. |
| T-01-65 | **Repudiation** | a green suite over surface nobody wrote a mutation for | **accept, and state the residual honestly** | This is the failure that produced this whole sequence: nineteen mutations and two independent verification passes preceded fourteen escapees. Running fourteen more does not prove there is no fifteenth. Task 3 requires the SUMMARY to say so in plain terms rather than claim closure — the alternative is a second false all-clear, which is strictly worse than a disclosed limit. |
| T-01-50 | **Tampering** | Phase 2's pinned patterns silently displaced | mitigate | Six patterns rechecked with `grep -n` and every result recorded. The harness matches by pattern, so a text change produces exit 3 — "pattern never matched" — which reads as an abort rather than a caught mutant and would be debugged as a harness bug. A line-number change alone produces no failure but leaves four Phase 2 plans carrying stale prose; both outcomes are reported. |
| T-01-51 | **Tampering** | `src/index.ts` export surface changed | mitigate | Asserted **ref-pinned** against `8c5b1a3`, the commit that closed plan 01-09, with both a content diff and a `git log` range check — the second catches a change-and-revert. An unpinned `git diff --exit-code` proves only working-tree cleanliness and cannot see a change committed during 01-11, which is the scenario this threat is about. A change here invalidates 02-07/P11 and 02-11/P8 **simultaneously**, both with a silent exit 3. |
| T-01-58 | **Tampering** | a mutant left un-reverted in `types.ts` | mitigate | Every mutation applies and restores inside a single tool call; `git diff --exit-code -- packages/concierge/src/types.ts` is an acceptance criterion on all three tasks. Never stack two mutants. Phase 1 already had a near-miss of exactly this shape. |
| T-01-30 | Information Disclosure | `*.test-d.*` or `_assert.js` emitted into `dist` | mitigate | Re-asserted after a real build, because this sequence added assertions to all four test files and one new probe pattern. `files: ["dist"]` does not help — the pollution would be inside `dist`. |
| T-01-36 | **Tampering** | a remediation loop weakening a correct suite at the gate | mitigate | The M4 and M10 silences are restated in the objective as correct-by-construction, and the standing rule is repeated: a guard not listed in a row staying silent is not a failure. This is the point of maximum pressure to just make it pass, and it is the last plan of the sequence. |
| T-01-SC | Tampering | npm/pnpm installs | accept | Zero dependency edges added across the whole gap-closure sequence; `pnpm-lock.yaml` asserted unchanged as a closing criterion. No package-manager install task exists in this plan. |
</threat_model>

<verification>
- All ten original mutants produced a non-zero exit with the guards named in their own rows
- All fourteen review escapees flipped: thirteen from ESCAPED to CAUGHT, and MUT-C from BROKEN to COMPILES
- MUT-F produced TS2542 on the element write, confirming `Readonly<Uint8Array>` rather than a bare `readonly`
- M4 confirmed without TS2578 and M10 without `_requiresIsString` — both recorded as expected silences, not holes
- `pnpm typecheck` from the repo root exits 0; a real build emits no test artifact into `dist`, and `dist` is removed
- All six Phase 2 pinned patterns still match, and every stale prose line number is named with its correct value
- `git diff --exit-code -- packages/concierge/src/types.ts` and `git diff --exit-code pnpm-lock.yaml` exit 0, and the **ref-pinned** `index.ts` checks against `8c5b1a3` both pass
- `01-VALIDATION.md` carries an appended `## Gap-Closure Validation Map` and its original sign-off is unmodified
</verification>

<success_criteria>
Every one of Phase 1's original ten detectors still fires against the final surface, so no gap-closure
fix bought coverage by costing coverage. Every one of the fourteen mutations that escaped an
independent adversarial code review is now caught, and the one that was broken rather than unguarded
now compiles with no cast. Phase 2 has been told in writing whether its six pinned patterns survived
and where its prose line numbers now point. The export surface is proven untouched. The residual risk
— that a battery only measures the surface it was written for — is stated rather than papered over.
</success_criteria>

<output>
Create `.planning/phases/01-type-surface-completion/01-15-SUMMARY.md` when done.
</output>
