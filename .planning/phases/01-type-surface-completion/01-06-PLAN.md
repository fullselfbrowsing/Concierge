---
phase: 01-type-surface-completion
plan: 06
type: execute
wave: 6
depends_on: ["01-05"]
files_modified:
  - packages/concierge/src/types.ts
  - packages/concierge/test-d/actions.test-d.ts
autonomous: true
requirements: [SC-7a, SC-7b, SC-7g]
user_setup: []

must_haves:
  truths:
    - "snapshotEquality on a declared action is typed against that action's real snapshot, not unknown"
    - "A Booking comparator does not fit ConsentPolicy<unknown>"
    - "The action's own Name is not widened by a consent.requires target"
    - "The handler's ctx.ack carries both the action's Snapshot and its AckPayload"
    - "readsUntrusted is declared on the action, and is absent from SideEffects"
    - "Actions with different Snapshot types still assemble into a stage and into crossStage"
  artifacts:
    - path: packages/concierge/src/types.ts
      provides: "ActionHandler and ActionDefinition threaded with Snapshot and AckPayload, readsUntrusted, AnyActionDefinition"
      contains: "export type AnyActionDefinition"
    - path: packages/concierge/test-d/actions.test-d.ts
      provides: "SC-7a, SC-7b, SC-7g, escapee 3, the erasure positives, and the TS9010 demonstration"
      min_lines: 60
  key_links:
    - from: "ActionDefinition.handler"
      to: "ActionHandler"
      via: "forwarded Snapshot and AckPayload type arguments"
      pattern: "ActionHandler<InferOutput<Schema>, Bridge, Snapshot, AckPayload>"
    - from: "StageDefinition.actions"
      to: "AnyActionDefinition"
      via: "erased collection element type"
      pattern: "ReadonlyArray<AnyActionDefinition<B>>"
    - from: "ConciergeConfig.crossStage"
      to: "AnyActionDefinition"
      via: "erased collection element type"
      pattern: "crossStage\\?: ReadonlyArray<AnyActionDefinition"
---

<objective>
Thread `Snapshot` through the declaration chain (D-07) and add the one surviving taint marker (D-04).

Today `ActionDefinition.consent?: ConsentPolicy` carries no type argument, so `Snapshot` defaults to
`unknown` and `snapshotEquality` really does degrade to `(a: unknown, b: unknown)` — exactly where
correctness matters. In the same chain, `ActionHandler` accepts an `AckPayload` parameter that
`ActionDefinition.handler` silently drops, so `ctx.ack` is always `ConsentAck<unknown, unknown>`.

Purpose: snapshot equality is what makes "any drift between review and confirm destroys the consent"
real. A comparator typed against `unknown` is a gate that cannot see what it is guarding.
Output: the threaded chain, `AnyActionDefinition`, `readsUntrusted`, and `actions.test-d.ts`.

**Serial phase.** Plan 6 of 9 in a single serial sequence. Do not parallelize.

## ⚠ Task 1 is ATOMIC. It does not compile in pieces.

Threading `Snapshot` breaks `StageDefinition.actions` and `ConciergeConfig.crossStage` with TS2375 —
`ConsentPolicy<Booking>` is not assignable to `ConsentPolicy<unknown>` because `Snapshot` sits in two
contravariant positions. The erased collection view MUST land in the same edit. Do not attempt the
threading first and the erasure as a follow-up; `types.ts` will not compile in between.

## This plan owns the TS9010 demonstration

Wave 0 (Plan 01-01) deliberately does **not** test TS9010. That diagnostic is a *variable*-annotation
error and `test-d/_assert.ts` holds only type aliases, so it cannot fire there in any configuration —
all three variants were compiled and every one exits 0. It fires **here**, on
`const confirm = defineAction({…})`, the moment an exported alias reads `typeof confirm`. Task 2
demonstrates it once and then deletes the probe, and states the phase-wide rule it implies:
**export nothing from `test-d/*.test-d.ts`.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@packages/concierge/src/types.ts
@packages/concierge/test-d/_assert.ts

Read only these ranges of `.planning/phases/01-type-surface-completion/01-RESEARCH.md`:
- lines 578–621 — Mechanics §4: the verified threaded signatures and the inference behaviour
- lines 671–695 — Pitfall 1: the TS2375 that makes the erasure mandatory, and the two verified erasure options
- lines 696–707 — Pitfall 2: escapee 3 (M8), the assertion that catches a dropped handler forward
- lines 761–766 — Pitfall 7: `snapshotEquality` must stay function-property syntax
- lines 838–865 — the SC-7a/SC-7b example, compiled verbatim
- lines 950–958 — the SC-7g (D-04) example, compiled verbatim
- lines 1004–1027 — what a regression actually looks like to a developer

Read `.planning/phases/01-type-surface-completion/01-CONTEXT.md` § D-04 (lines 209–234) and § D-07
(lines 325–351), including the **⚠️ Added 2026-07-28** callout and D-12 item 2 (line 443).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread Snapshot and AckPayload, add readsUntrusted, and land the erasure — one atomic edit</name>
  <files>packages/concierge/src/types.ts</files>
  <read_first>
    - `packages/concierge/src/types.ts` lines 156–163 — `ActionHandler` as it stands, with `AckPayload` in position 3
    - `packages/concierge/src/types.ts` lines 212–246 — `ConsentPolicy`, including `requires` and `snapshotEquality`
    - `packages/concierge/src/types.ts` lines 286–304 — `SideEffects`, which `readsUntrusted` must NOT join
    - `packages/concierge/src/types.ts` lines 323–356 — `ActionDefinition`
    - `packages/concierge/src/types.ts` lines 402–408 — `StageDefinition.actions`
    - `packages/concierge/src/types.ts` lines 478–505 — `ConciergeConfig.crossStage`
    - `.planning/phases/01-type-surface-completion/01-RESEARCH.md` lines 578–621 and 671–695
    - `.planning/phases/01-type-surface-completion/01-CONTEXT.md` § D-04 and § D-07
  </read_first>
  <action>
    Four changes, one edit, in this order within the file. Every step must be present before the
    file compiles again.

    **1. `ActionHandler` gains `Snapshot`.** Its parameter list becomes
    `<Args, Bridge, Snapshot = unknown, AckPayload = unknown>`, and `ctx.ack` becomes
    `ConsentAck<Snapshot, AckPayload>` instead of `ConsentAck<unknown, AckPayload>`. Note that
    `AckPayload` **moves from position 3 to position 4** — this is a positional change to an exported
    type. It is safe here because `ActionDefinition.handler` is the only in-repo reference and it
    passes two arguments, but record it in the SUMMARY as a positional change to the public surface.

    **2. `ActionDefinition` gains `Snapshot` and `AckPayload`.** Its parameter list becomes
    `<Name extends string = string, Schema extends StandardSchemaV1 = StandardSchemaV1, Bridge = unknown, Snapshot = unknown, AckPayload = unknown>`.
    Then, in the same declaration:
    - `handler` becomes `ActionHandler<InferOutput<Schema>, Bridge, Snapshot, AckPayload>`. Forwarding
      **both** is the point — forwarding only `Snapshot` reproduces the exact defect this plan closes.
    - `consent` becomes `ConsentPolicy<Snapshot>`.
    - Add `readsUntrusted?: boolean` as a **sibling of `effects`, not a member of `SideEffects`**
      (D-04). Placement is load-bearing: `SideEffects` is documented as the MCP-hint mirror and its
      entire value is 1:1 fidelity; MCP is actively reconsidering `openWorldHint` for conflating
      ingress with egress, and importing a defective name into that block would corrupt it. A future
      `concierge-mcp` can still derive `openWorldHint: true` from `readsUntrusted` as a safe
      over-approximation.
    - Do NOT add `maxPerTurn`, `conflictsWith`, or `impact` as members. D-04 cut all three
      deliberately and CONTEXT names reinstating them as "cheap while we're in here" as an
      anti-pattern. Naming them in a doc comment to explain *why* they were cut is expected and
      correct — the acceptance grep filters comment lines for exactly this reason.

    `readsUntrusted`'s doc comment must state why it is the only one of four that survived: two of
    the three lethal-trifecta legs are structurally always on — actions run inside the app the user
    is logged into, and `ActionResult.message` returns to the model — so untrusted ingress is the
    single variable leg. It must also state plainly that **Phase 1 ships the field and the type test
    only**, and that the build-time gate is SEC-05 in Phase 3, because a safety field nothing reads
    sitting beside a redaction policy that genuinely fails closed is this project's named failure mode.

    **3. `AnyActionDefinition` — the erased collection view.** Add an exported alias
    `AnyActionDefinition<Bridge = unknown>` equal to
    `ActionDefinition<string, StandardSchemaV1, Bridge, any, any>`. Use **`any`-erasure**, settled by
    D-12 item 2, not `never`-erasure. Its doc comment must say the `any` is deliberate and
    load-bearing rather than laziness: the concrete `Snapshot` lives on the individual declaration,
    and the kernel is the one place that must actually call `snapshotEquality` — forcing a cast there
    is worse than one documented `any` in a collection type. Note that the tradeoff surfaces in
    Phases 4, 6, and 8 and should be revisited in Phase 8 against a real kernel.

    **4. Apply the erasure at both collection sites.** `StageDefinition.actions` becomes
    `ReadonlyArray<AnyActionDefinition<B>>`; `ConciergeConfig.crossStage` becomes
    `ReadonlyArray<AnyActionDefinition>`. Without this the file does not compile — TS2375, because
    an action declared with a concrete `Snapshot` stops being assignable to the erased-to-`unknown`
    form these two sites use today.

    Do NOT change `snapshotEquality` to method syntax while you are in `ConsentPolicy`. It must stay
    a function property. Method parameters are bivariant, which would let a `(a: Booking, b: Booking)`
    comparator assign to `ConsentPolicy<unknown>` and silently un-break the very defect this plan
    exists to close — with no symptom except one unused directive.

    Do NOT change `ConsentPolicy.requires` from `string`, and do NOT give `ConsentPolicy` a second
    type parameter. Its doc comment already explains why a typed version was wrong: `Name` is
    inferred from both `name` and `requires`, so typing it would widen `Name` to the union of the two
    and corrupt the name-union derivation the catalog depends on. That is precisely the shape of
    mutant M10, which Task 2 applies deliberately and then reverts.
  </action>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @fullselfbrowsing/concierge typecheck` exits 0
    - `ActionHandler` is declared `<Args, Bridge, Snapshot = unknown, AckPayload = unknown>` and its `ack` is `ConsentAck<Snapshot, AckPayload>`
    - `ActionDefinition` declares five type parameters ending in `Snapshot = unknown, AckPayload = unknown`
    - `ActionDefinition.handler` is `ActionHandler<InferOutput<Schema>, Bridge, Snapshot, AckPayload>` — both arguments forwarded
    - `ActionDefinition.consent` is `ConsentPolicy<Snapshot>`
    - `readsUntrusted?: boolean` is a direct member of `ActionDefinition` and does not appear inside the `SideEffects` declaration
    - `SideEffects` still declares exactly `readOnly`, `destructive`, `idempotent`
    - No cut field survives as a member: `grep -v '^[[:space:]]*[*/]' packages/concierge/src/types.ts | grep -c "maxPerTurn\|conflictsWith\|impact?:"` returns 0. The filter is deliberate — `readsUntrusted`'s doc comment names all three to explain why they were cut, and "impact" is an ordinary English word, so an unfiltered grep on the bare token would be self-invalidating.
    - `export type AnyActionDefinition<Bridge = unknown>` exists and uses `any` in the Snapshot and AckPayload positions, with a doc comment stating the erasure is deliberate
    - `StageDefinition.actions` is `ReadonlyArray<AnyActionDefinition<B>>` and `ConciergeConfig.crossStage` is `ReadonlyArray<AnyActionDefinition>`
    - `ConsentPolicy.snapshotEquality` is still function-property syntax, `ConsentPolicy.requires` is still `string`, and `ConsentPolicy` still takes exactly one type parameter
    - The `readsUntrusted` doc comment names SEC-05 and Phase 3 as the gate
  </acceptance_criteria>
  <done>An action declared with a concrete snapshot types its comparator and its `ctx.ack` against that snapshot, and heterogeneous actions still assemble into stages.</done>
</task>

<task type="auto">
  <name>Task 2: Author actions.test-d.ts defect-first — including escapee 3 and the TS9010 demonstration (SC-7a, SC-7b, SC-7g)</name>
  <files>packages/concierge/test-d/actions.test-d.ts</files>
  <read_first>
    - `.planning/phases/01-type-surface-completion/01-RESEARCH.md` lines 838–865 (SC-7a/7b), 950–958 (SC-7g), 603–621 (the inference behaviour and the escapee-3 assertion)
    - `.planning/phases/01-type-surface-completion/01-VALIDATION.md` § "The three escapees" item 3, and the ⚠️ corrected callout under § "Traps Wave 0 must avoid" (where TS9010 actually fires)
    - `.planning/phases/01-type-surface-completion/01-VALIDATION.md` § "Ten-mutant battery" — the corrected M10 row
    - `packages/concierge/test-d/_assert.ts`
    - `packages/concierge/src/types.ts` as edited in Task 1
  </read_first>
  <action>
    Create `packages/concierge/test-d/actions.test-d.ts`, importing the four aliases from
    `"./_assert.js"` and the types under test from `"../src/types.js"`.

    **Rule, phase-wide and load-bearing: export NOTHING from this file.** The `import` statements
    already make it a module, so no `export {}` is needed and no `export` is wanted. Every assertion
    alias, fixture, and probe here is a plain non-exported declaration. Non-exported locals inside a
    module carry no `isolatedDeclarations` annotation requirement, which is what makes
    `const confirm = defineAction({…})` legal and is exactly the inference the test needs — do not
    annotate it, because annotating the thing under test defeats the assertion. **That exemption
    holds only while nothing exported reads it.** The demonstration below proves the boundary.

    Fixtures, declared locally in this file: a `Booking` type with an `amount: number`; a
    `declare const schema: StandardSchemaV1<unknown, { q: string }>;`; and a `defineAction` stand-in
    declared as a generic function over `<Name, Schema, Bridge, Snapshot, AckPayload>` that takes an
    `ActionDefinition<…>` and returns it unchanged. **Phase 3 owns the real `defineAction`** — this is
    a `declare function` in the test file only, never a runtime implementation, and it must not be
    exported.

    **TS9010 demonstration — do it once, then delete the probe.** Wave 0 could not test TS9010:
    `_assert.ts` holds only type aliases and the diagnostic is a *variable*-annotation error, so it
    cannot fire there in any configuration. It fires here. Once `const confirm = defineAction({…})`
    exists (see SC-7b below), temporarily add a single exported alias that reads its inferred type —
    `export type _ConfirmProbe = typeof confirm;` — run the typecheck, and confirm a non-zero exit
    with `error TS9010`. Note where the diagnostic lands: on the **`const confirm` declaration**, not
    on the exported alias. That is why the rule is stated as "export nothing" rather than "annotate
    your exports". Delete the probe line, confirm the typecheck returns to exit 0, and record the
    diagnostic verbatim in the SUMMARY.

    Required assertion alias names: `_requiresIsString`, `_nameNotWidened`, `_snapshotInferred`,
    `_readsUntrustedOnDefinition`, `_handlerAck`, `_handlerArgs`, `_handlerBridge`.

    **SC-7a — `snapshotEquality` must not degrade to `unknown`.** Declare a
    `const eq = (a: Booking, b: Booking): boolean => …`. Assign it into an explicitly annotated
    `ConsentPolicy<Booking>` (positive). Then declare `_policyDegraded` — a `ConsentPolicy` with no
    type argument carrying the same comparator, with `@ts-expect-error` immediately above the
    `snapshotEquality` property line, proving a `Booking` comparator does not fit
    `ConsentPolicy<unknown>`. This is the one case in the file where the directive is correct
    placement-wise; put it directly above the property, not above the declaration, because the error
    is reported on the property line. `_policyDegraded` is also mutant M9's sole detector — under
    method syntax the directive goes unused and TS2578 is the only symptom.

    **SC-7b — `requires` must not widen `Name`.** Three assertions, with distinct jobs:
    - `_requiresIsString` — `Equals<ConsentPolicy<Booking>["requires"], string>`. This is a **static
      pin** against `requires` being silently retyped. It is *not* mutant M10's detector; see the
      defect-first criterion below for why, and do not repurpose it into one.
    - Declare an action through `defineAction` whose `name` is a literal (`"confirmBooking"`) and
      whose `consent.requires` names a *different* action (`"reviewBooking"`), bound to
      `const confirm`. Assert `_nameNotWidened` —
      `Equals<(typeof confirm)["name"], "confirmBooking">` — proving `Name` did not widen to the
      union of the two.
    - Assert `_snapshotInferred` —
      `Equals<NonNullable<(typeof confirm)["consent"]>, ConsentPolicy<Booking>>` — proving `Snapshot`
      inferred from the comparator.

    `_nameNotWidened` and `_snapshotInferred` are the pair that actually catches M10, because the
    mutation gives `ConsentPolicy` a `Name` parameter that infers from `requires` as well as from
    `name`. Both must read `typeof confirm` — the *inferred* declaration — not an explicitly
    annotated one, or neither can see the widening.

    **Escapee 3 — the handler forward. This is the assertion nothing else catches.** Reverting
    `handler` to `ActionHandler<Args, Bridge>` escapes *every* consent assertion above, because
    `consent?: ConsentPolicy<Snapshot>` still infers `Snapshot` correctly on its own and `ctx.ack`
    still typechecks — as `ConsentAck<unknown, unknown>`. Write a direct assertion instead: extract
    the handler's context type with
    `Parameters<ActionDefinition<"x", typeof schema, null, Booking, AckShape>["handler"]>[0]`, then
    assert `_handlerAck` (`Equals<Ctx["ack"], ConsentAck<Booking, AckShape> | undefined>`),
    `_handlerArgs` (`Equals<Ctx["args"], { q: string }>`), and `_handlerBridge`
    (`Equals<Ctx["bridge"], null>`). All three, not just the first.

    **SC-7g — `readsUntrusted` placement.** `_readsUntrustedOnDefinition` asserts
    `Equals<ActionDefinition["readsUntrusted"], boolean | undefined>`. Then a companion negative:
    a type alias reaching for `NonNullable<ActionDefinition["effects"]>["readsUntrusted"]` with
    `@ts-expect-error` above it, proving the field is absent from `SideEffects` (TS2339 under the
    correct placement, unused directive if someone moves it in).

    **The erasure positives.** Declare two actions with *different* concrete `Snapshot` types and
    assemble them into a single `StageDefinition`'s `actions` array, and a third into a
    `ConciergeConfig.crossStage` array, both with no casts. This is the positive half of Pitfall 1
    and it is what proves the erasure actually accepts heterogeneous actions rather than merely
    compiling in isolation.
  </action>
  <verify>
    <automated>pnpm --filter @fullselfbrowsing/concierge typecheck && test -z "$(grep -l '^[[:space:]]*export' packages/concierge/test-d/*.test-d.ts)" && echo NO_EXPORTS_IN_TEST_D</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @fullselfbrowsing/concierge typecheck` exits 0 with the file present
    - **TS9010 demonstration (mandatory):** the exported-alias probe was added, observed to exit non-zero with `TS9010` reported on the `const confirm` declaration, and then deleted. The diagnostic is recorded verbatim in the SUMMARY.
    - **No exports survive:** `test -z "$(grep -l '^[[:space:]]*export' packages/concierge/test-d/*.test-d.ts)"` exits 0 across all four test files
    - **Defect-first proof for M3 (mandatory):** temporarily revert `ActionDefinition.consent` to `ConsentPolicy` with no type argument; confirm non-zero exit with TS2322 on the comparator and TS2344 naming `_snapshotInferred`; restore.
    - **Defect-first proof for M8 (mandatory, escapee 3):** temporarily revert `ActionDefinition.handler` to `ActionHandler<InferOutput<Schema>, Bridge>`; confirm non-zero exit with TS2344 naming `_handlerAck`; restore. **If the suite stays green, the assertion is wrong — fix it before proceeding.**
    - **Defect-first proof for M9 (mandatory):** temporarily change `ConsentPolicy.snapshotEquality` to method syntax; confirm non-zero exit with TS2578 on the `_policyDegraded` directive; restore. A single unused directive is M9's *only* symptom — do not expect more.
    - **Defect-first proof for M10 (mandatory) — apply this exact mutation, not an improvised one:** change `ConsentPolicy` to `ConsentPolicy<Snapshot, Name extends string = string>` with `requires: Name`, and thread it through `ActionDefinition.consent` as `ConsentPolicy<Snapshot, Name>`. Confirm non-zero exit with **two** TS2344s, naming `_nameNotWidened` and `_snapshotInferred`; restore.
    - **M10 expectation, stated so a correct suite is not "fixed":** `_requiresIsString` will stay **silent** under M10 and that is correct — `ConsentPolicy<Booking>["requires"]` is still `string` once `Name` carries its `= string` default, so the pin cannot see this mutation. The mutant is caught by the two guards above, so there is no suite hole. Do not alter `_requiresIsString` to make it fire, do not add a member to make it fire, and do not report M10 as escaping.
    - **Defect-first proof for M7 (mandatory):** temporarily move `readsUntrusted` into `SideEffects`; confirm non-zero exit with TS2339 and an unused-directive TS2578; restore.
    - All five mutant diagnostic sets plus the TS9010 diagnostic are recorded in the SUMMARY
    - The file contains all seven named aliases `_requiresIsString`, `_nameNotWidened`, `_snapshotInferred`, `_readsUntrustedOnDefinition`, `_handlerAck`, `_handlerArgs`, `_handlerBridge`
    - `_nameNotWidened` and `_snapshotInferred` both read `typeof confirm` — the inferred declaration — not an explicitly annotated `ActionDefinition`
    - The file assembles at least two actions with different concrete `Snapshot` types into one `StageDefinition.actions` array with no `as` cast
    - The file's `defineAction` is a `declare function`, is not exported, and has no runtime body
    - Exactly two `@ts-expect-error` directives appear in the file (`_policyDegraded`'s property line and the `SideEffects` negative)
  </acceptance_criteria>
  <done>M3, M7, M8, M9, and M10 have all been observed to break the build with named assertions — including escapee 3 which every other assertion misses, and M10 through the two guards that actually detect it — and TS9010 has been demonstrated where it genuinely fires.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review-time snapshot → confirm-time snapshot | Any drift between the two destroys the consent; the comparator is what detects it |
| untrusted external content → model context | An action that reads third-party content is the variable leg of the lethal trifecta |
| individual action declaration → erased catalog collection | `any` enters the public surface at exactly one documented point |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-18 | **Tampering** | `ConsentPolicy.snapshotEquality` degraded to `(a: unknown, b: unknown)` | mitigate | Thread `Snapshot` so the comparator is typed against the action's real snapshot. A comparator over `unknown` turns "any drift destroys consent" into a gate that cannot see what it guards. Asserted by `_snapshotInferred` and `_policyDegraded`. |
| T-01-19 | Tampering | `ActionDefinition.handler` silently dropping its type arguments | mitigate | Forward both `Snapshot` and `AckPayload`. This regression is invisible to every consent assertion — escapee 3 — so `_handlerAck`, `_handlerArgs`, `_handlerBridge` are purpose-built for it. |
| T-01-20 | **Elevation of Privilege** | untrusted ingress into model context | **transfer** | Phase 1 ships `readsUntrusted` and the type test only. **The build-time gate is SEC-05, Phase 3.** An unenforced marker beside a redaction policy that genuinely fails closed is this project's named failure mode — the doc comment must say so, so nobody mistakes the field for a control. |
| T-01-21 | Elevation of Privilege | `readsUntrusted` placed inside `SideEffects` | mitigate | Sibling placement, asserted by `_readsUntrustedOnDefinition` plus the `SideEffects` negative. MCP's `openWorldHint` conflates ingress with egress and is under active revision; importing a defective name into the 1:1 mirror block would corrupt it. |
| T-01-22 | Information Disclosure | `any` in the public surface via `AnyActionDefinition` | accept | Deliberate and documented, settled by D-12 item 2. Confined to one exported alias whose doc comment explains the tradeoff; the concrete `Snapshot` still lives on every individual declaration. Revisit in Phase 8 against a real kernel. |
| T-01-35 | Repudiation | a remediation loop run against a correct suite because a mutant's expected guard was misnamed | mitigate | M10's detectors are `_nameNotWidened` and `_snapshotInferred`; `_requiresIsString` is a static pin that stays silent under M10 by construction. Naming the wrong guard would send the executor to "fix" a working suite at the phase gate. The exact mutation and its real guards are written into both this plan and Plan 09. |
| T-01-SC | Tampering | npm/pnpm installs | accept | No packages installed. |
</threat_model>

<verification>
- `pnpm --filter @fullselfbrowsing/concierge typecheck` exits 0
- Mutants M3, M7, M8, M9, M10 have each been observed to exit non-zero with their expected diagnostics — M10 via `_nameNotWidened` and `_snapshotInferred`, not via `_requiresIsString`
- TS9010 has been observed on `const confirm` under an exported probe, and the probe has been removed
- `test -z "$(grep -l '^[[:space:]]*export' packages/concierge/test-d/*.test-d.ts)"` exits 0
- `SideEffects` still declares exactly three members
- No `maxPerTurn`, `conflictsWith`, or `impact?:` member anywhere in `types.ts` outside doc comments
</verification>

<success_criteria>
`snapshotEquality` on a declared action is typed against that action's real snapshot (SC-7a). A
`requires` target does not widen the action's own name union, and the assertion pair that proves it
has been seen to fire against the exact mutation (SC-7b). `readsUntrusted` is on the declaration and
absent from `SideEffects` (SC-7g). The handler forwards both type arguments to `ctx.ack`. Actions
with different snapshot types still assemble into a stage and into `crossStage`. The test files
export nothing, and the TS9010 boundary that rule exists for has been demonstrated.
</success_criteria>

<output>
Create `.planning/phases/01-type-surface-completion/01-06-SUMMARY.md` when done.
</output>
